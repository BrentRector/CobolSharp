---
title: "PB497 — §13.18.60.3 SR19's `PROGRAM-POINTER TO program-prototype-name` is staged LOUD with NO screen behind the staging, while its data-pointer twin SR18 is implemented twenty lines away; the same shape refuses `FUNCTION-POINTER` by name, so §13.18.60.4 GR25/GR26 have no subject at all and the REQUIRED Annex A.1 item 210 determination has no DOC row"
id: PB497
kind: analysis
status: landed
severity: MINOR
area: binding/data-division
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.18.60.3"]
inventory_rows: ["SR-13.18.60.3-19", "GR-13.18.60.4-25", "GR-13.18.60.4-26"]
tags: [cobolsharp, work, analysis]
---

# PB497 — LANDED

## LANDED — 2026-09-12, with [[PB452]]'s SET Format 8 and [[PB817]] (batch `pb452-pb817-set-format-8-function-pointer`)

All three rows are `CONFORMS`. This note's diagnosis was right in every part, including the prediction that made it urgent: *“the staging that currently hides the gap will be lifted by the P13 prototype-registry work, at which point the missing screen becomes a live under-rejection with nothing in the corpus to catch it.”* The staging WAS lifted by PB452/PB817, and SR19 was implemented in the same change set for exactly that reason.

* **SR-13.18.60.3-19** — SR18 and SR19 are now ONE screen in `DataBinder`, over the two operands §13.18.60.2's general format gives them. The literal reading is kept: the subject is the entry carrying the `TO` phrase, so `01 PP USAGE PROGRAM-POINTER TO PROTO.` is itself nonconforming (`conformance:negative/pb452-program-pointer-to-without-typedef`, COBOLNET0881 — the SR18 band).
* **GR-13.18.60.4-25** — the restricted program-pointer is declarable and its NULL-or-same-signature invariant is enforced at every SET into it by §14.9.39.3 SR22 (COBOLNET1959).
* **GR-13.18.60.4-26** — USAGE FUNCTION-POINTER is a real declaration on the `FunctionPointer` carrier, its implementor-defined alignment/size/representation/allowable-languages published as `docs/CONFORMANCE.md` §7 **DOC-A.1-210** (this note's own “the REQUIRED Annex A.1 item 210 determination has no DOC row”), and its signature invariant enforced at bind (SR20) and at run time (§14.9.39.4 GR14). Editions NARROWED to 2014,2023: the reserved-word evidence dates FUNCTION-POINTER to 2014.
* **Residue, not claimed:** [[PB609]]'s `SR-14.9.4.3-14` (the CALL-side consumer) and `StrongTypeModel.PointerRestriction`'s missing `ProgramPointer` arm.

## The original analysis (2026-09-01)

## Mechanism — usage-clause-p1
usage-p1-F10-sr19-has-no-screen-behind-its-staging-while-its-data-pointer-twin-sr18-is-implemented-twenty-lines-away

### Expected, from the standard
SR19 is SR18's exact drafting twin -- same sentence, different operand -- so `01 P USAGE PROGRAM-POINTER TO PROTO.` shall require TYPEDEF on the subject of the entry, and `01 P IS TYPEDEF USAGE PROGRAM-POINTER TO PROTO.` shall be the conforming spelling.

```
(the finding quotes no rule sentence verbatim, so there is nothing to --check; the implementer MUST quote §13.18.60.3 and re-run `cite.py --check` before writing a code comment)
```

### Observed
Measured at 2023, the verdict is IDENTICAL whether the subject carries TYPEDEF or not and whether the operand names a typedef or an ordinary group: COBOLNET0899 'USAGE PROGRAM-POINTER TO program-prototype-name (ISO §13.18.60 GR25)' in every case (u_sr19_bad, u_sr19_ok). No nonconforming program is silently accepted TODAY -- the staging is loud and by name -- so the harm is latent rather than live, which is why it is filed as silent. It is filed at all because of the shape: SR18 and SR19 are one rule written twice, one is implemented and one is not, they live twenty lines apart in the same method, and the staging that currently hides the gap will be lifted by the P13 prototype-registry work, at which point the missing screen becomes a live under-rejection with nothing in the corpus to catch it. docs/DIAGNOSTICS.md's `program-pointer-restricted` row cites GR25/SR22 and never mentions SR19.

### Repro
Pinned compiler E:/CobolSharp/.claude/worktrees/lane3-pin-89044aa0/src/Cobol.Net.Cli/bin/Debug/net10.0/cobol.exe. Probe dirs E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/u1/{u_sr19_bad,u_sr19_ok} (contrast the working twin at {u_sr18_bad,u_sr18_ok,u_sr18_subject,u_sr18_subject2}). Minimal: `01 P IS TYPEDEF USAGE PROGRAM-POINTER TO T.` and `01 P USAGE PROGRAM-POINTER TO T.` -> the same COBOLNET0899 for both.

### Code site
src/Cobol.Net.Compiler/Binding/DataBinder.cs:2473-2477 stages the whole `PROGRAM-POINTER TO ...` form loud (DiagnosticCatalog.ProgramPointerRestricted / COBOLNET0899, cited to §13.18.60 GR25) and returns; src/Cobol.Net.Compiler/Binding/DataBinder.cs:2489-2503 is SR18's screen for the data-pointer form, keyed on the entry's own `isTypedef`. Nothing between them asks SR19's question.

## Sibling sweep the fixer owes
Every bug here is a pattern (CLAUDE.md rule 4). Before closing this note, sweep the sibling arms named in the mechanism above and add the ones that fail to `inventory_rows`.

## Provenance
Lane-3 adjudication batch b3 (subjects: usage-clause-p1), adjudicated against the compiler pinned at 89044aa0 and RE-PROBED on the registrar's worktree cut from 6e7caa91 (a 1942-program sweep of `out-b3/probe/` plus targeted re-probes); the finding reproduces on today's tree.

## Repro programs
`E:\Temp\claude\E--CobolSharp\a9d94900-79fe-4a38-89f0-921976861543\scratchpad\repros\PB497\` — u1. The implementer's opening move is to rerun them against a freshly built `cobol.exe`.

