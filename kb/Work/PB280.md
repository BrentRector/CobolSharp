---
title: "PB280 — THREE OWNER QUESTIONS the Annex A.1 DOC-row shape could not answer for itself: whether a 'Not provided.' determination on an A.1-OPTIONAL item closes as CONFORMS or as DOCUMENTED-NON-SUPPORT (~30 items); whether a DOC row with nothing in the compiler to observe may close on the register alone (this widens the design doc's definition of DONE); and whether this repo's CONFORMANCE.md is the §4.2.16 user documentation and may discharge an obligation BY REFERENCE"
id: PB280
kind: decision
status: landed
severity: OWNER
area: process/traceability
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: ["4.2.5", "4.2.7", "4.2.16", "A.1"]
inventory_rows: []
tags: [cobolsharp, work, decision]
---

# PB280 — the three owner questions the A.1 DOC-row shape left open

> ⚙ **Raised by the landing of the Annex A.1 DOC-row closing shape (2026-09-02).** The mechanism landed with its
> OBSERVABLE arm only, and each question below is a place where answering it myself would have moved the owner's
> burn-down on the owner's own definition. Every one is stated as a bare question with its options and the
> consequence of each, so it can be answered in one word.

## ✅ ANSWERED — all three, by the owner, 2026-09-02

| | question | answer | where it lives now |
|---|---|---|---|
| **Q1** | a "Not provided." determination on an A.1-**optional** item | **DOCUMENTED-NON-SUPPORT** | `inventory-schema.json` → `derived-verdicts.a1-optional-not-provided` |
| **Q2** | may an anchor-only DOC row (nothing observable) close on the register alone? | **No — it stays a GAP**; DESIGN §1(a) is not widened | unchanged behaviour, now recorded as a decision rather than a default |
| **Q3** | is `docs/CONFORMANCE.md` the §4.2.16 user documentation, and may a row discharge BY REFERENCE? | **Yes**, it is; and **no** — reference the governing document *and* state the value | CONFORMANCE.md §7 preamble |

**`inventory_rows` is now empty and that is the point.** `DOC-A.1-127` and `DOC-A.1-206` were held here because
the answer was owed; they are now owned by the **selector** — a predicate over the requirement class and the §7
determination — so the next optional element recorded as not provided is handled with no note, no adjudication
and no edit to this file. Their remaining WITNESS debt is `kb/Work/PB373`.

## Q1 — does a "Not provided." determination on an A.1-**optional** item close as CONFORMS, or as DOCUMENTED-NON-SUPPORT?

> ### ✅ ANSWERED 2026-09-02 — **DOCUMENTED-NON-SUPPORT**
>
> **The ground the owner gave** is A.1's own preamble, which conditions the documentation duty on provision —
> *"Documentation required: If the element is provided by the implementor, the implementor's user documentation
> shall document the element or shall reference other documentation that fulfills this requirement."*
> (`cite.py --check A.1` → OK) — together with **§4.2.7**, which this verdict's `meaning` in
> `inventory-schema.json` already named as one of its three grounds (`cite.py --check 4.2.7` → OK).
>
> **The mechanism, not thirty adjudications.** `derived-verdicts.a1-optional-not-provided` selects on an AND of
> two things somebody else wrote down: the **requirement class the standard states** (`requirement`, parsed out
> of A.1 by `extract_rule_catalog.py`) and **what §7 says** (`determination-prefix`, read off the register row
> keyed by the rule's own id — exactly the anchor `kinds.DOC.anchor-template` computes). Both arm fields were
> new; both engines read them; and because *today's* document cannot separate them — all 30 optional items and
> all 47 §7 rows agree — each axis is driven against fabricated data by
> `python scripts/spec/derive_verdict_batch.py --self-test`, which the C# gate shells every build.
>
> **Rows:** `DOC-A.1-127` and `DOC-A.1-206`, stamped, anchored, and **still GAP** — a declined row closes on a
> witness proving the documented posture is what actually happens, not on its verdict (GAP 3227 → 3227).
>
> ⛔ **It moved a rule that had nothing to do with the question.** `anchor-exempt-verdicts` keyed the anchor
> obligation on the VERDICT, which was right while DOCUMENTED-NON-SUPPORT had one meaning on a DOC row and
> became wrong the moment it had two. An item an A.4 module WITHDREW has no §7 row and owes no anchor; an
> optional element recorded as not provided HAS one, because stating the non-provision *is* the determination.
> Keyed on the verdict, items 127 and 206 would have been excused their own determination, `check_pins` would
> have stopped holding their witness in agreement with §7's `Pinned by` at the moment the witness landed, and
> `audit_annex_a1.py` would have printed "⊘ CANNOT ARISE" about an element this register documents in full,
> dropping REMAINING by two for no work done. The exemption is now keyed on **"does §7 carry a row"**, in one
> predicate per language over one parser per language.

**Options:** `CONFORMS` · `DOCUMENTED-NON-SUPPORT`.

**Consequence.** One answer settles **~30 items** (A.1 has 30 optional elements) and becomes a
`derived-verdicts` selector in `inventory-schema.json` rather than thirty adjudications — the PB198 mechanism.
`CONFORMS` closes them as ordinary rows; `DOCUMENTED-NON-SUPPORT` makes each an owner decision requiring `notes`,
which is a slower path but the one the schema already reserves for a declined element.

**The evidence on both sides, measured rather than argued.** Exactly two §7 rows literally open with
"**Not provided.**", and they are exactly the two A.1-`optional` items in the back-fill: **item 127**
(OBJECT-COMPUTER computer-name and implied equipment configuration — "there is ONE object computer, the .NET
runtime") and **item 206** (USAGE BINARY-CHAR/-SHORT/-LONG/-DOUBLE wider range than the GR12 minimum — "each usage
holds exactly the GR12 minimum range"). For CONFORMS: both are *positive, observable answers* to A.1's question,
not declines of a facility, and routing them to an owner-only verdict makes that verdict an agent's default escape
hatch. For DOCUMENTED-NON-SUPPORT: A.1's preamble says "— Optional: The element may be provided at the
implementor's option" and "— Documentation required: **If the element is provided by the implementor**, the
implementor's user documentation shall document the element…", so for a not-provided optional element the
documentation duty does not arise at all; and `inventory-schema.json`'s own `DOCUMENTED-NON-SUPPORT.meaning`
already names "4.2.7 for an OPTIONAL element" as one of its three grounds and states the verdict is
"An OWNER decision (never an agent's)".

**Held pending the answer:** `DOC-A.1-127` and `DOC-A.1-206` were **dropped from the back-fill** and remained
verdict-less GAPs until the answer landed. Both §7 determinations stayed exactly as written — the register was
never waiting on this, only the two inventory rows were.

## Q2 — may a DOC row whose only location is its §7 anchor — nothing in the compiler to observe — close on the register gate alone?

> ### ✅ ANSWERED 2026-09-02 — **No. It stays a GAP.**
>
> `DESIGN-spec-conformance-review.md` §1(a)'s definition of DONE is **not** widened, so what landed is what
> stands and no arm is added. The owner's reasoning is the measurement this note already carried: greenfield
> source cites just **13 of the 222** A.1 items in `implementor item N` comments, so for the other 209 the claim
> such an arm would rest on — "there is nothing in the compiler to observe" — would be contradicted by nothing
> *and confirmed by nothing*, which is `feedback_verdict_evidence_invariant` exactly. An item with genuinely
> nothing to observe is discharged by an **explicit owner decline for that item**, or it stays open.
>
> Recorded in `inventory-schema.json` (`kinds` `$comment`), in the design doc §4, and in
> `SpecTraceabilityInventoryDriftTests`, all three of which had described this as an open question and now
> describe it as a decision. The fabricated self-test drives the anchor-only row on BOTH verdict shapes: it must
> not close with no test, and it must not close **with** a spec-derived one either — the arm a future "it has a
> test, surely it closes" reading would break.

**Options:** `yes, close it` · `no, it stays a GAP` (what landed).

**Consequence.** This is **a widening of the definition of DONE**, and that is why the arm is not landed.
`DESIGN-spec-conformance-review.md` §1(a) requires "a located implementation (a traceability link into
`src/Cobol.Net.*`), OR a recorded **owner documented-non-support decision** (optional modules only, per §4.2.16)".
A register-only row has neither: its one `code-location` is `docs/CONFORMANCE.md#DOC-A.1-<n>`, and its verdict is
CONFORMS, not DOCUMENTED-NON-SUPPORT. Answering `yes` means amending §1(a) with a third arm naming the register
determination explicitly — the owner's completion metric, redefined — and the amendment lands in the same change
set as the arm.

**Why it is not landed, measured.** All 37 back-fill rows carry a greenfield `src/Cobol.Net.*` site, so the arm
would have had **zero instances**: speculative generality that relaxes a gate before a case for it exists. And its
own negative claim ("there is nothing in the compiler to observe") is cross-checkable for only **13 of 222** items
— greenfield source cites exactly thirteen distinct A.1 items in `implementor item N` comments, a convention no
gate enforces — so for the other 209 the claim would be contradicted by nothing *and confirmed by nothing*. A
MISSING observation standing in for a NEGATIVE one is precisely what `feedback_verdict_evidence_invariant`
forbids.

**Today's behaviour, so the cost of `no` is visible:** such a row stays a GAP forever, whatever its test-ref.
When a real item needs the arm, the honest shapes are (a) amend §1(a) as above, or (b) require the §7 row to name,
in a machine-readable cell, the *reason* nothing is observable — the unclaimed A.4 module row in §5, or the
§4.2.3 interaction the owner recorded as unclaimed — and have the gate RESOLVE that reference, which is the
discipline `DOCUMENTED-NON-SUPPORT.requires: ["notes"]` already imposes.

## Q3 — is this repository's `docs/CONFORMANCE.md` the §4.2.16 user documentation, and may a row discharge its obligation BY REFERENCE?

> ### ✅ ANSWERED 2026-09-02 — **Q3a: yes. Q3b: no — state the value locally.**
>
> **Q3a.** `docs/CONFORMANCE.md`, as shipped with the compiler, **is** the §4.2.16 user documentation. Every
> mechanism that landed already treated it as that form; the answer makes the premise explicit rather than
> inherited, which is the difference between a rule and an assumption nobody wrote down.
>
> **Q3b.** A determination **may cite** the governing document — and shall name its **version** when it does —
> but it **shall also state the resulting value**. ⚠ This is deliberately NARROWER than the standard permits:
> §4.2.16 allows requirements to be met "by reference to other documents", and A.1's preamble allows a row to
> "reference other documentation that fulfills this requirement" (`cite.py --check A.1` → OK). The reason for
> taking the narrower path is Q2's reason one layer along: **a by-reference row states no value, so no test can
> pin it**, and the gate can only check that a row exists — the same unfalsifiable shape Q2 refused. The value
> is what the `Pinned by` golden asserts.
>
> Recorded as one sentence in the `docs/CONFORMANCE.md` §7 preamble, where the rows that must obey it live.
> It costs: several of the remaining 154 obligations are answered entirely by the host (item 20 case mapping,
> item 12 bits per byte, item 144's unseeded seed), and each now owes a stated value and a witness rather than a
> citation. That cost is the point — it is what makes those rows closeable at all.

Two halves of one question about what the register IS. §4.2.16: "An implementation shall satisfy the user
documentation requirements specified in 4.2.3, 4.2.4, 4.2.5, 4.2.6, 4.2.10, 4.2.12, and 4.2.13 **by specification
in at least one form of documentation**", and "Documentation requirements **may be met by reference to other
documents**, including those of the operating environment and other COBOL implementations."

**Q3a — does `docs/CONFORMANCE.md`, as shipped in this repository, count as "the implementor's user
documentation"?** Options: `yes` · `no, it must also be published in a customer-facing form first`.
**Consequence:** every mechanism that landed treats the repo file as that form. If it is not, then 222 rows would
close against an artifact that does not exist yet, **and the gate cannot tell** — it resolves an anchor into the
file, and the file is there.

**Q3b — may a §7 row discharge its obligation BY REFERENCE — naming the .NET / Unicode / OS document instead of
stating the value locally — and still close?** Options: `yes` · `no, the value must be stated locally`.
**Consequence:** several of the remaining 163 obligations are answered entirely by the host (item 20 case mapping,
item 12 bits per byte, item 144's unseeded seed), so the answer materially changes the cost of finishing the
register. But a by-reference row states **no value**, so no test can pin it and the gate can only check that a row
exists — which collides with Q2: a by-reference row is very often exactly a row with nothing to observe.

## What this note did NOT hold up

The mechanism itself. The computed anchor, `implementation-pattern`, the anchored-file alternation, the §7 re-key,
the `Pinned by` agreement check and the 37-record back-fill all landed; **18 rows closed and 19 came back
CONFORMS-but-untested** (item 87 among them — the orchestrator refused the back-fill's close of it because its
only candidate test asserts two fraction digits where the determination claims seven-then-zeros, which is the
`Pinned by` check's own lesson applied one notch harder: a test that names the right function still has to assert
the claim). Only the two `inventory_rows` waited on Q1, and Q2/Q3 shaped the *remaining* rows rather than anything
already recorded.

## What the three answers cost, together

One landing, and one structural correction none of the three questions asked for: the anchor obligation had to
stop being keyed on the VERDICT (see Q1). Two rows moved from blank to DOCUMENTED-NON-SUPPORT, the GAP moved by
**zero**, and the register's REMAINING counter is unchanged at 154 — a documented decline is a *discharged*
obligation, not a withdrawn one, which is the distinction the whole mechanism now turns on.
