---
title: "PB1304 — a level-66 RENAMES inside a TYPEDEF is staged loud (COBOLNET1535 typedef-renames-staged) because CloneItem does not carry Renames66 into a TYPE reference — the condition-name arm of the same §13.18.58.4 GR1 is implemented, the RENAMES arm is not"
id: PB1304
kind: defect
status: open
severity: MINOR
area: binding/data-division
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB608", "PB1283"]
spec_refs: ["13.18.58.4"]
inventory_rows: ["GR-13.18.58.4-1"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14e** from adjudication batch d1-14e (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probe sources under the batch's `out13/probe/<subject>/`). Checked against main 071ff04ea: trains 58/59 did not touch this mechanism.

# PB1304 — OPEN

## The rules (every quotation run through `cite.py --check` by the registrar)

- `python scripts/spec/cite.py --check 13.18.58.4 "Subordinate data description entries, condition-name entries, and RENAMES clauses are part of the type declaration of that type"` → `OK  §13.18.58.4 1)  (General rules)`

## Measured

### Finding typedef-clause — ISO/IEC 1989:2023 §13.18.58.4 GR1 (rejects-legal-source; rows GR-13.18.58.4-1)

**Mechanism.** A level-66 RENAMES inside a TYPEDEF is staged loud (COBOLNET1535) because CloneItem does not carry Renames66 into a TYPE reference -- the condition-name arm of the same general rule is implemented, the RENAMES arm is not

**Repro.** 01 T TYPEDEF. 05 A PIC X. 05 B PIC X. 66 AB RENAMES A THRU B. 01 R TYPE T.  PROCEDURE: MOVE "XY" TO AB OF R. DISPLAY A OF R B OF R. STOP RUN.  (probe out13/probe/typedef-clause/td08renames.cob)

**Expected, from the standard.** Compiles; R receives its own AB alias over its own A..B; output XY. §13.18.58.4 GR1: 'Subordinate data description entries, condition-name entries, and RENAMES clauses are part of the type declaration of that type.'

**Observed.** COBOLNET1535 'a level-66 RENAMES inside a TYPEDEF ... is recognized but not yet cloned into TYPE references (data-model D17 residue)' plus COBOLNET1639 on AB OF R. DataBinder.BindEntries' level-66 arm emits the stage when rootIsTemplate, while the level-88 arm clones (BindCondition registerGlobal:false + CloneConditionOnto) -- the two-arm shape. No owner decision or register note owns the rejection (PB608 owns only the duplicate-code catalog defect of COBOLNET1535).

**Code site.** src/Cobol.Net.Compiler/Binding/DataBinder.cs#BindEntries (lvl == 66 && rootIsTemplate); #CloneItem

## What the fixer owes

Re-probe on your own build first (MANDATORY-PRACTICES I3). Fix the mechanism at the code sites above — one rule, one place (the `cluster:` notes share these files; take them as one fill unit). One positive golden at the introducing edition and one negative below it / per violated rule, each derived from the quoted rule. Sweep the sibling arms named in the findings (CLAUDE.md rule 4). On landing flip `status`, write `closes_rows` from `inventory_rows`, and re-verdict each row with `record_verdicts.py`.
