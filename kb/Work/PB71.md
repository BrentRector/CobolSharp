---
title: "PB71 — OPEN (NEW 2026-08-09) — ALL over a NATIONAL (and boolean?) literal fails to parse"
id: PB71
kind: defect
status: open
severity: MINOR
area: grammar/figurative
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["8.3.3.6"]
tags: [cobolsharp, work, defect]
---

# PB71 — OPEN — `MOVE ALL N"Q" TO NR` draws COBOL0001 "no viable alternative"

> ⚙ **flags:** rejects-legal-source (compile-time, loud) · found writing PB59-6's ALL-substitution
> fixture 2026-08-09 (probe `pb59_alln.cob`).

**Measured repro** (`--std 2023`): `01 NR PIC N(3).` + `MOVE ALL N"Q" TO NR` →
`error COBOL0001: no viable alternative at input 'ALLN"Q"'`. The same statement with an alphanumeric
literal (`ALL "Q"`) parses and runs.

**The charge:** §8.3.3.6's ALL-literal figurative admits a national (and boolean) literal-1; the
grammar's ALL production evidently admits only the alphanumeric/hex literal forms. Sweep at fix time:
`ALL N"…"`, `ALL NX"…"`, `ALL B"…"`, `ALL BX"…"`, `ALL X"…"` (the last worked in PB59-4's probes) in
every figurative position — MOVE source, VALUE clause, intrinsic argument, INSPECT operand — and the
materialization path (the national repeat-to-width in `FigurativeConstants`/`OperandText`) once the
parse admits them. Grammar changes are pre-authorized; the edition-gate sweep applies (national
literals are 2002+, so the gated recognition must follow the NATLIT posture).

> ⚙ **2026-08-09 PROBE SWEEP (wf_fdd1492c — probe + adversarial refute, every verdict a pasted CLI run + a --check'd citation; full per-row evidence/mechanisms/blockers frozen in `docs/rearchitecture/backlog-probe-sweep-2026-08-09.json`):** 9 rows LIVE, 1 fixed-upheld (test-needed).
