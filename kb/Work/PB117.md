---
title: "PB117 — ✅ LANDED 2026-08-28 (sequential folds; golden 2023/pb117_trim_sequential pins the interleaved edge + order sensitivity). WAS: FUNCTION TRIM with several argument-2 characters trims the SET UNION where §15.96.4 r5 requires SEQUENTIAL processing (TRIM(a b c) ≡ TRIM(TRIM(a b) c)) — TRIM("bcab" "c" "b") returns "a" where the standard requires "ca"; a silent wrong answer both lanes share"
id: PB117
kind: defect
status: landed
severity: MAJOR
area: intrinsics
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["15.96.4"]
tags: [cobolsharp, work, defect]
---

# PB117

Inventory: RV-15.96.4-5 (DIVERGES). `CobolIntrinsics.Trim` collapses every argument-2 into one char[] and
performs ONE TrimStart/TrimEnd/Trim. The rule's own NOTE example ("aabbcc" "c" "b") agrees only by accident
(no edge interleaving) — a golden built from the NOTE passes while the defect stands, so the fix's golden
must use an interleaved edge. Fix: fold left — one Trim per argument-2, in written order.

*(Found by the Phase-B §15 close-out batch, 2026-08-28 — one adjudicator + one independent refuter per subject;
the full forensic evidence lives in the named inventory rows' `notes` fields,
`tests/version-matrix/traceability-inventory.json`.)*
