---
title: "PB51 — LANDED — COMPUTE gained the F2→F1 re-route that mirrors the F1→F2 one it already had; the grammar re-architecture this note proposed was NOT needed"
id: PB51
kind: defect
status: landed
severity: MAJOR
area: binder
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: [8.8.1.1, 14.9.8]
tags: [cobolsharp, work, defect]
---

# PB51 — LANDED — the commonest way to write zero compiles again

> ⚙ **FOUND BY [[PB48]]'s PROBE MATRIX, AND MEASURED ON THE PRE-PB48 TREE — pre-existing, not a regression.**
> It was found only because the probe swept the ARITHMETIC controls as well as the intrinsic cases; nothing in
> PB48's subject would have touched it.

## MEASURED (2026-08-05)

```
01 WS-N PIC S9(9)V99.
COMPUTE WS-N = ZERO
  ⛔ error COBOLNET1511: a boolean COMPUTE expression shall not consist solely of an
     ALL literal (ISO §14.9.8 Format 2 SR3)
```

For contrast, every neighbouring form compiles and runs correctly: `COMPUTE WS-N = (ZERO)`,
`COMPUTE WS-N = ZERO + 7`, `COMPUTE WS-N = 7 + ZERO`, `MOVE ZERO TO WS-N`.

## WHY IT IS WRONG, TWICE OVER

§8.8.1.1 (`--check`ed): "An arithmetic expression may be an identifier referencing a numeric data item, a numeric
literal, **the figurative constant ZERO** (ZEROS, ZEROES), …" — so `ZERO` alone is a well-formed
arithmetic-expression-1 for §14.9.8 Format 1, and `WS-N` is a legal Format-1 receiver.

The diagnostic is wrong on its own terms as well: **the source contains no ALL literal.** The message names
§14.9.8 Format 2 SR3, a rule about `ALL literal-1`, for a statement whose operand is the bare figurative.

## THE MECHANISM (hypothesis — MEASURE IT, do not inherit it)

`ArithmeticBinder.BindComputeBoolean` rejects when the bound RHS is a `BoundBoolAll`, and `BoundBoolAll`'s own
doc says it carries "the figurative ZERO, normalized to `ALL B"0"` at bind". A bare `ZERO` is not adjacent to any
arithmetic operator or paren, so `ZeroTokenRewriter` leaves it figurative, `primaryExpression` cannot match it,
and the parser takes the COMPUTE Format-2 boolean alternative instead — where the normalization then makes it
indistinguishable from `ALL B"0"`. ⚠ `ArithmeticBinder` documents an "F1→F2 sole-boolean re-route"; the reverse
re-route appears not to exist, and SR2 (a Format-2 receiver shall be an elementary boolean item) should have
rejected the `PIC S9(9)V99` receiver before SR3 was ever consulted — **that ordering is itself worth checking.**

## ▶ WHAT TO CHECK FIRST

⛔ **THIS SHARES A ROOT WITH [[PB50]] AND THE DECISION IS THE SAME ONE: `primaryExpression` cannot express
§8.8.1.1's figurative ZERO, and a token-adjacency pass has been patching over the gap.** PB48 fixed the
adjacency pass where it fired WRONGLY; these two are where it does not fire at all. The structural answer is to
let the grammar carry the rule the standard states — `ZERO` as a `primaryExpression` alternative, with
`ZeroTokenRewriter` retired.
⚠ **That change is NOT small and must be measured, not assumed, in both directions.** (a) The rewriter exists
because adding `ZERO` to `primaryExpression` "causes exponential ANTLR prediction time" — a claim in a source
comment that has never been re-measured, and §0's standing rule is that a named root cause is a claim until it
is measured. (b) More importantly it injects a real ambiguity everywhere `figurativeConstant` and
`arithmeticExpression` are sibling alternatives — relation conditions, `valueOperand`, `booleanFactor` — where
ANTLR's first-match would silently decide figurative-vs-numeric semantics that differ observably (`IF X = ZERO`
over `PIC X(5)` compares against `"00000"`, not against 0). Enumerate those sites BEFORE editing the grammar.

## ⚙ LANDED 2026-08-06 — and the MEASUREMENT shrank the fix from a re-architecture to a re-route

**THIS NOTE PROPOSED RETIRING `ZeroTokenRewriter` FOR A GRAMMAR ALTERNATIVE, and demanded an ambiguity survey
first. The survey is what made the change unnecessary.** Sweeping every bare-ZERO position:

| shape | before |
|---|---|
| `COMPUTE N = ZERO` / `ZEROS` | ⛔ COBOLNET1511 |
| `ADD ZERO TO N` · `SUBTRACT ZERO FROM N` · `MULTIPLY ZERO BY N` | ✅ |
| `ADD ZERO TO I GIVING N` · `IF N = ZERO` · `MOVE ZERO TO N` | ✅ |
| `COMPUTE N = ZERO + 3` · `FUNCTION ABS(ZERO)` | ✅ |

**Every other arithmetic position already accepted it** — the verbs have their own operand rules admitting the
figurative. COMPUTE is the ONE verb whose RHS is a bare `arithmeticExpression`, so the blast radius is a single
construct, not the grammar. The premise "`primaryExpression` cannot express §8.8.1.1's figurative ZERO" is TRUE
and was still the wrong basis for sizing the work ([[feedback_validate_the_premise_not_only_the_rule]]).

**THE FIX IS THE MIRROR OF ONE THAT ALREADY EXISTED.** `ArithmeticBinder` carried an F1→F2 re-route
(`COMPUTE bool = bool` parses as Format 1 because a sole-identifier RHS predicts the arithmetic alt) documented
as "the ANTLR alternative-order reality precedent". The F2→F1 direction was simply absent: a Format-2 parse whose
source is a BARE ZERO and whose receiver is not boolean is Format 1, value 0 (§8.3.3.6.4 GR4 — "the numeric value
'0' … depending on context"; a numeric receiver IS that context).

⚠ **THE TEST IS ON THE PARSE TREE, NOT THE BOUND NODE**, and that is forced: `BindBoolExpr` normalises a
figurative ZERO to `BoundBoolAll("0")` — the same node `ALL B"0"` produces — so by bind time the two are
indistinguishable, which is exactly why SR3 fired with a message about a construct the source did not contain.
`SoleFigurativeZero` asks what the SOURCE wrote, so `ALL ZERO` and `ALL B"0"` keep their verdicts
(§8.3.3.6.3 SR1a — "the only figurative constant permitted is ZERO … WITHOUT the ALL phrase").

## ▶ RESIDUE — one question this deliberately did NOT answer

⚠ `COMPUTE <boolean-item> = ZERO` still reports COBOLNET1511. The re-route excludes a boolean receiver on
purpose (there GR4's BOOLEAN reading is the right one and Format 2 genuinely applies), so the behaviour is
unchanged rather than newly decided — but **whether §14.9.8 Format 2 SR3's "an ALL literal" should bar a bare
`ZERO` at all is a separate reading question.** §8.3.3.6.2 Format 1 is `[ALL] ZERO`, and SR3's phrase most
naturally means the Format-6 `ALL literal-1`, which a bare ZERO is not. Left as measured, not silently widened.
[[PB50]] (a bare ZERO in a SUBSCRIPT or ref-mod position) is untouched and remains the one genuinely open member
of this family.

