---
title: "PB1282 — Sr12Sr14Violation exempts a data-name-2 whose strong root equals the subject's, so a REDEFINES written inside a TYPEDEF STRONG template is accepted — yet by §13.18.58.4 GR3 every TYPE subject assumes that clause, making data-name-2 an item subordinate to a strongly-typed group (§13.18.44.3 SR14)"
id: PB1282
kind: defect
status: open
severity: MINOR
area: binding/redefines
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1280", "PB1301"]
spec_refs: ["13.18.44.3"]
inventory_rows: ["SR-13.18.44.3-14"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14e** from adjudication batch d1-14e (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probe sources under the batch's `out13/probe/<subject>/`). Checked against main 071ff04ea: trains 58/59 did not touch this mechanism.

# PB1282 — OPEN

## The rules (every quotation run through `cite.py --check` by the registrar)

- `python scripts/spec/cite.py --check 13.18.44.3 "Data-name-2 shall not be of class object, message-tag, or pointer, a strongly-typed group item, or an item subordinate to a strongly-typed group item"` → `OK  §13.18.44.3 14)  (Syntax rules)`
- ⚠ NOT validated verbatim (the adjudicator's paraphrase or a cross-clause quote — re-quote before it reaches code, a golden or the DEVLOG): "Data-name-2 shall not be ... an item subordinate to a strongly-typed group item"

## Measured

### Finding redefines-clause — §13.18.44.3 SR14; §13.18.58.4 GR3; §13.18.57.3 SR2 (under-rejects; rows SR-13.18.44.3-14)

**Mechanism.** strong-template-internal-redefines: Sr12Sr14Violation exempts a data-name-2 whose strong root equals the subject's, so a REDEFINES written inside a TYPEDEF STRONG template is accepted; by §13.18.58.4 GR3 that clause is assumed by every TYPE subject, making data-name-2 'an item subordinate to a strongly-typed group item'. Because §13.18.57.3 SR2 forbids written subordinates under a TYPE entry, the template is the ONLY way SR14's subordinate disjunct can be met - the exemption makes it vacuous. The landed PB183 derivation (golden pb183_redefines_in_strong_typedef_ok) answered §13.18.57.3 SR4, a different rule, and never tested §13.18.44.3 SR14's letter; that golden must flip with the fix.

**Repro.** `01 T IS TYPEDEF STRONG. 05 X PIC X(4). 05 Y REDEFINES X PIC X(4). 01 S TYPE T.` PROCEDURE: `MOVE "ABCD" TO X IN S. DISPLAY Y IN S.` (sr14_in_template.cob).

**Expected, from the standard.** Compile-time rejection: SR14 'Data-name-2 shall not be ... an item subordinate to a strongly-typed group item.'

**Observed.** Compiles clean and prints ABCD. Cross-root and whole-strong-group targets are rejected (COBOLNET1697/1532), so only the same-root arm is exempt.

**Code site.** src/Cobol.Net.Compiler/Binding/DataBinder.cs#Sr12Sr14Violation (`StrongTypeModel.StrongRoot(target) is { } tRoot && !ReferenceEquals(StrongTypeModel.StrongRoot(subject), tRoot)`); tests/conformance/2023/pb183_redefines_in_strong_typedef_ok.cob

## What the fixer owes

Re-probe on your own build first (MANDATORY-PRACTICES I3). Fix the mechanism at the code sites above — one rule, one place (the `cluster:` notes share these files; take them as one fill unit). One positive golden at the introducing edition and one negative below it / per violated rule, each derived from the quoted rule. Sweep the sibling arms named in the findings (CLAUDE.md rule 4). On landing flip `status`, write `closes_rows` from `inventory_rows`, and re-verdict each row with `record_verdicts.py`.
