---
title: "PB1039 — a zero-valued CONSTANT-NAME written where an integer-n goes escapes the §5.5 1) nonzero screen: `01 ZC CONSTANT AS 0.` + `OCCURS ZC TIMES` compiles clean while the literal `OCCURS 0 TIMES` draws COBOLNET2386"
id: PB1039
kind: defect
status: open
severity: MINOR
area: binding
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB859"]
spec_refs: ["5.5"]
inventory_rows: []
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-23 by **REGISTRAR #13** from `leads-w53-w57.md` and the train-57 lander report (w56e lead `wf_ccbf74f1-936-5`). Re-probed on registrar #13's own Debug build of `c54434a8d` (BelowNormal).

# PB1039 — OPEN

## Measured
```cobol
       IDENTIFICATION DIVISION.
       PROGRAM-ID. B10OC.
       DATA DIVISION.
       WORKING-STORAGE SECTION.
       01 ZC CONSTANT AS 0.
       01 T.
          05 E PIC X OCCURS ZC TIMES.
       01 T2.
          05 E2 PIC X OCCURS 0 TIMES.
       PROCEDURE DIVISION.
           DISPLAY "RAN"
           STOP RUN.
```

→ ONE diagnostic, for line 9 (`OCCURS 0 TIMES`: COBOLNET2386). Line 7 (`OCCURS ZC TIMES`) is accepted.

## The rule
`cite.py --check 5.5 "shall be unsigned and nonzero unless otherwise specified in the associated rules"` → `OK §5.5 1)`.
SR-5.5-1 is CONFORMS (PB859) for the literal spelling; the screen reads the TOKEN, and a constant-name is resolved
at bind time after it.

## What the fixer owes
Screen the RESOLVED value at the one place a constant-name is substituted for an integer-n (the constant arm of the
integer-operand decoder), so every integer-n position gets it — sweep OCCURS, BLOCK CONTAINS, RECORD CONTAINS/VARYING,
PAGE/LINE/COLUMN, LINAGE, ALPHABET/SYMBOLIC integers. Negative golden with a constant operand.
