---
title: "PB597 — both citation audits stop at the CLAUSE, so a wrong GR/SR NUMBER inside a correct clause is invisible: `kb/Work/R10`'s landed sweep of `§14.9.25 GR8` (the unsigned-magnitude rule is §14.9.25.4 GR6 d) 2. b.) left NINE live sites in EIGHT files including the greenfield runtime and a current-state design doc, and round 3 found two more of the shape in SEARCH — all while `audit_code_citations` and `audit_doc_citations` report zero"
id: PB597
kind: defect
status: open
severity: MINOR
area: tooling/spec-audit
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: ["14.9.25.4", "14.9.37.4"]
inventory_rows: ["GR-14.9.25.4-6", "GR-14.9.37.4-6", "SR-14.9.37.3-6"]
tags: [cobolsharp, work, defect]
---

# PB597 — OPEN — the audits validate the clause, and the defect lives one level down

Two independent round-3 findings turned out to be the same mechanism, so they are one note.

## The blind spot, demonstrated

`scripts/spec/cite.py --check 14.9.25.4 "When an unsigned numeric item is the receiving item, the absolute value
of the sending value is used, and no operational sign is generated for the receiving item."` → **OK**. The clause
is right and the text is really in it, so a comment reading `§14.9.25.4 GR8 — "the absolute value"` passes
`--check`, passes `audit_code_citations.py --check` (which screens the clause against the CONSTRUCT the comment
names) and passes `audit_doc_citations.py --check` (which screens a QUOTED fragment against the clause it is
filed under). **The GR number is never compared to anything.** Both audits report 0 on this tree while every site
below ships.

## Occurrence 1 — R10's sweep is incomplete (9 sites, 8 files)

`kb/Work/R10.md` is `status: landed` and records: *"Citation sweep: the unsigned-magnitude rule is §14.9.25.4
**GR6d2b** — '§14.9.25 GR8' was the inherited wrong clause in CobolNum.cs (5×) and CONFORMANCE.md item 205."*
Nine live sites remain — found by the round-3 `misc-p2` refuter re-running the writer's own repro pattern, and
re-grepped by the lander:

| site | tier |
|---|---|
| `src/Cobol.Net.Runtime/Values/Numeric/CobolNum.Image.cs:169` | **greenfield runtime** |
| `docs/COBOLNET_DATA_MODEL_DESIGN.md:1132` | **current-state design doc** |
| `src/CobolSharp.Runtime/Numeric/CobolNum.cs:74` | legacy oracle |
| `src/CobolSharp.Runtime/PicRuntime.cs:2231` | legacy oracle |
| `tests/Cobol.Net.Tests.Unit/BinaryCapacityTests.cs:14` and `:44` | greenfield tests |
| `tests/Cobol.Net.Tests.Unit/RecordImageCodecTests.cs:76` | greenfield tests |
| `tests/CobolSharp.Tests.Unit/Runtime/PicRuntimeRegressionTests.cs:77` | legacy tests |
| `tests/CobolSharp.Tests.Unit/Runtime/Numeric/CobolNumDifferentialTests.cs:278` | legacy tests |

(`DEVLOG.md` and `docs/rearchitecture/DESIGN-SPEC-RECONCILIATION.md` item 37 also carry the string; both are
correct as written — one is history, the other is the ledger recording the defect.) §14.9.25.4 GR8 is really the
DYNAMIC-LENGTH rule, so every one of these attributes an unsigned-magnitude claim to a rule about something else.

## Occurrence 2 — SEARCH, found this round

- `tests/conformance/2023/ec_range_search.cob` lines 1 and 4 attribute the SERIAL scan's no-match to
  §14.9.37.4 **GR6**. GR6 is FORMAT 2 (SEARCH ALL) and applies only when GR5 is violated; the serial no-match is
  **GR4** (*"…in which case the search operation is unsuccessful, the EC-RANGE-SEARCH-NO-MATCH exception
  condition is set to exist"*).
- `src/Cobol.Net.Compiler/CodeGen/Verbs/ControlFlowEmitter.cs:447` calls the Format-1 varied item "the GR8 varied
  item". GR8 is the Format-2 search-index rule and **Format 2 has no varied item at all**; the owning rules are
  GR3 b) and GR3 c).
- The `SR-14.9.37.3-6` inventory row's adjudicator notes carry six wrong sub-clause parentheticals: condition-name
  is §8.8.4.5, class §8.8.4.4, sign §8.8.4.7, complex/combined §8.8.4.11 (precedence §8.8.4.11.3), abbreviated
  combined relation §8.8.4.12, negated §8.8.4.10 — the parenthesised-condition probe is tagged `(8.8.4.7)` and
  belongs to §8.8.4.11, and the negated probe carries no clause at all.

## The fix

Two halves, and the second is the one that matters.

1. Correct the eleven sites.
2. ⛔ **Extend the audit to the sub-rule level** so the next one is impossible rather than merely fixed. Where a
   citation carries a `GR<n>` / `SR<n>` (or `rule <n>`) alongside a quoted fragment, resolve the fragment to its
   OWN numbered item within the clause and compare — `cite.py` already reports that path (it printed
   `§14.9.25.4 6) 1.` for the fragment above, which is how the mismatch with `GR8` becomes decidable). ⚠ Its
   nested-list numbering is imprecise (three round-3 refuters each hit a label that renders one level off), so
   the comparison has to tolerate depth while still refusing a different TOP-level item — `GR8` vs `6) …` is a
   top-level disagreement and must go red; `6) 1.` vs `6) d) 2. b.` must not.

A guard at that level is also what would have kept R10's sweep honest: the note recorded a sweep, and no gate
could tell that it had covered 5 of 14.

## Instance 2026-09-05 (found by the PB325 implementer, fixed in passing on its branch)

`RelativeConnector.MutationTargetRecordId` cited `§14.9.35 GR19` (the INVALID KEY transfer-of-control rule) for the sequential-access REWRITE target, which is §14.9.35.4 GR5 — a plausible-but-wrong GR inside the right clause, with no quoted text, so SUBJECT and HEADER both pass and the misfiled scan has nothing to test. The `#member` doc-comment shape wants the sweep this note asks for.

> ⚠ **ROW IDS RE-KEYED 2026-09-06 (kb/Work PB698).** The transcription repair returned the outdented sub-items of these rule blocks to their printed level, so the rows this note names moved: `GR-14.9.25.4-L2.2` -> `GR-14.9.25.4-6`. `inventory_rows` above is live; the ids in the prose are as they stood when it was written, and the adjudications they carry were migrated by the `pb698-rekey` and `pb698-fold` batches.
