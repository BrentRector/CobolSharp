---
title: "PB1133 — a RECURSIVE program that CONTAINS programs cannot declare WORKING-STORAGE or a FILE SECTION: COBOLNET0899 RecursiveContainedWs refuses legal source, so none of §14.6.2.3.2's initial-state occasions (CANCEL, first activation) or actions can be realized for its static storage"
id: PB1133
kind: defect
status: open
severity: MINOR
area: binder/program
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1132", "PB1134", "PB152", "PB1069"]
spec_refs: ["14.6.2.3.2", "11.10.3"]
inventory_rows: ["GR-14.6.2.3.2-3", "GR-14.6.2.3.2-L2.2", "GR-14.6.2.3.2-L2.3", "GR-14.6.2.3.2-L2.7", "GR-13.5.4-1", "SR-8.4.6.3-1", "GR-11.10.4-4", "GR-11.10.4-5"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14b** from lane-3 adjudication batch **d1-14b** (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d1` @`68bbdb00f`). Not re-probed by the registrar (MANDATORY-PRACTICES R2: every lead below carries its runnable repro and code site); main @`071ff04ea` was checked for trains 58/59 — no note landed since the pin touches this mechanism.

# PB1133 — OPEN

## The rule
> "after the execution of a CANCEL statement referencing the program or CANCEL statement referencing a program that directly or indirectly contains the program"
`cite.py --check 14.6.2.3.2 "after the execution of a CANCEL statement referencing the program or CANCEL statement referencing a program that directly or indirectly contains the program"` → `OK  §14.6.2.3.2 3)  (Initial state)`

> "The RECURSIVE clause shall not be specified if any program that directly or indirectly contains this program is an initial program"
`cite.py --check 11.10.3 "The RECURSIVE clause shall not be specified if any program that directly or indirectly contains this program is an initial program"` → `OK  §11.10.3 6)  (Syntax rules)`

## Measured (adjudicator / refuter evidence, verbatim)
### GR-14.6.2.3.2-3 (PARTIAL)
|| REFUTED: PARTIAL — Case 3 covers 'a CANCEL statement referencing the program', including a RECURSIVE program whose WORKING-STORAGE or FILE SECTION is static data and which contains programs. The pinned compiler REJECTS that legal source with COBOLNET0899 RecursiveContainedWs (BinderDriver.cs ~478-489, 'recognized but not yet implemented'). So neither the CANCEL occasion for that program's static data nor the cascade from it into its containees can be exercised. Measured: out/probe/refute-isl/r1.cob (RECURSIVE RFR1 with WS and contained RFI1/RFQ1) fails to compile with error COBOLNET0899. The shapes that do compile work: rerunning p3.cob shows CANCEL P3C re-initializing the doubly-contained P3CI (J=01). The cited golden is not spec-derived either. pb154_cancel_cascade contains RECURSIVE INNERA154 inside INITIAL OUTER154, which violates 11.10.3 SR6, so the compilation unit as a whole is non-conforming source even though the CANCEL CONT154 half is legal in isolation.
- **Code location (record):** src/Cobol.Net.Runtime/Control/ProgramTable.cs#Cancel; src/Cobol.Net.Runtime/Control/ProgramTable.cs#CancelNode; src/Cobol.Net.Compiler/CodeGen/DataDivision/RecordStructEmitter.cs#__ResetStatics; src/Cobol.Net.Compiler/CodeGen/DataDivision/ValueInitializer.cs

### GR-14.6.2.3.2-L2.2 (PARTIAL)
|| REFUTED: PARTIAL — Every shape that compiles conforms. My own probe out/probe/refute-isl/v1.cob gave, for a non-recursive unit after CANCEL, the group VALUE GROUPVAL, the table TE VALUE AB repeated, VALUE ALL 'XY', a signed display item, COMP-3 and a VALUE-less pointer back to NULL. A LOCAL-STORAGE item in a contained program was re-initialized on the second activation (L1=05 LT=QQ). Two defects remain. (1) The pinned compiler rejects legal source in which a RECURSIVE program that contains programs declares WORKING-STORAGE or a FILE SECTION: COBOLNET0899 RecursiveContainedWs, src/Cobol.Net.Compiler/Binding/BinderDriver.cs ~478-489, 'recognized but not yet implemented'. Measured on out/probe/refute-isl/r1.cob. So this initial-state action is never realized for that program's static storage on any of the case 1-3 occasions. (2) The cited test-ref cannot close the row. pb152_options_initialize_background pins NUM=[0000] for a VALUE-less PIC 9(4) under INITIALIZE ALL TO X"5A". Action 1 requires the fill 'ZZZZ' there (see L2.1), so the golden carries an expected value that is not spec-derived. The other goldens named in the notes include pb154_cancel_cascade, which is non-conforming source under 11.10.3 SR6.
- **Code location (record):** src/Cobol.Net.Compiler/CodeGen/DataDivision/ValueInitializer.cs; src/Cobol.Net.Compiler/CodeGen/DataDivision/RecordStructEmitter.cs#__ResetStatics; src/Cobol.Net.Compiler/CodeGen/ProgramEmitter.cs#EmitCallMethod; src/Cobol.Net.Runtime/Control/StorageCell.cs

## What the fixer owes
Implement static WS/FILE SECTION for a RECURSIVE container (BinderDriver.cs ~478-489) with its CANCEL cascade into containees. ⚠ Test hygiene lead from the refuter: golden `pb154_cancel_cascade` nests RECURSIVE INNERA154 inside INITIAL OUTER154, which §11.10.3 SR6 (cited above) forbids — re-derive it from conforming source when this lands.

## Extended 2026-09-24 by REGISTRAR 14e (folded from PB1307 — the same mechanism)

Registrar 14b's PB1133 already owns the COBOLNET0899 RecursiveContainedWs refusal (a RECURSIVE program that contains programs cannot declare WORKING-STORAGE / a FILE SECTION); PB1307 measured the same stage from the §13.5.4 GR1 side.

PB1307 as filed: PB1307 — BinderDriver.BindUnitData gives §13.5.4 GR1's static working-storage only to a RECURSIVE program with NO contained programs and refuses the rest COBOLNET0899 RecursiveContainedWs, so a recursive program that directly contains any program and declares WORKING-STORAGE (or a FILE SECTION) is rejected — legal source


Filed 2026-09-24 by **REGISTRAR 14e** from adjudication batch d1-14e (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probe sources under the batch's `out13/probe/<subject>/`). Checked against main 071ff04ea: trains 58/59 did not touch this mechanism.

### PB1307 — as filed

## The rules (every quotation run through `cite.py --check` by the registrar)

- `python scripts/spec/cite.py --check 13.5.4 "Data items in the working-storage section of a program that does not have the initial attribute, a function, a factory, or an object are static data"` → `OK  §13.5.4 1)  (General rules)`

## Measured

### Finding working-storage-section — §13.5.4 GR1; §14.6.2.3.3; §3.168 (rejects-legal-source; rows GR-13.5.4-1)

**Mechanism.** BinderDriver.BindUnitData gives the §13.5.4 GR1 static-WS channel only to units with no contained programs, and refuses the rest with COBOLNET0899. A RECURSIVE (non-INITIAL) program that directly contains any program and declares WORKING-STORAGE or a FILE SECTION is rejected outright, even when nothing is GLOBAL. The staged-loud gap (the static fields do not compose with the containee's __outer/GLOBAL ref-bridges, §13.18.27.4 GR2) has no open kb/Work owner: PB168, which widened it to the FILE SECTION, is landed.

**Repro.** probe/ws/p5reccont.cob: P5MAIN CALLs P5REC twice. `PROGRAM-ID. P5REC RECURSIVE.` has `WORKING-STORAGE SECTION. 01 CTR PIC 9(2) VALUE 0.`, does ADD 1 TO CTR, DISPLAY "REC=" CTR, CALL "P5IN", and directly contains the plain program P5IN (DISPLAY "IN"). No GLOBAL item anywhere. `cobol p5reccont.cob --run` (default 2023) fails: error COBOLNET0899: program 'P5REC': a RECURSIVE program that directly contains programs and declares WORKING-STORAGE or a FILE SECTION is recognized but not yet implemented.

**Expected, from the standard.** §13.5.4 GR1: the WS of a program without the initial attribute is static data. §14.6.2.3.3: static data is in the last-used state on re-entry. Nothing in §13.5.3 or §11.10 forbids a RECURSIVE program from containing programs. The program must compile and print REC=01 / IN / REC=02 / IN at 2002, 2014 and 2023.

**Observed.** Rejected at compile time with COBOLNET0899 (measured with the pinned compiler). The code comment at BinderDriver.cs:471-491 says C# forbids `instance.staticField` for the GLOBAL/__outer bridges and stages the whole shape loud. The stage also fires when the containee references nothing of the container's, so it rejects far more than the bridge gap actually needs.

**Code site.** src/Cobol.Net.Compiler/Binding/BinderDriver.cs#BindUnitData (lines 471-491, `bool staticWs = unit.Recursive && !unit.Initial && unit.Children.Count == 0;` and the RecursiveContainedWs error); src/Cobol.Net.Editions/Diagnostics/DiagnosticCatalog.cs:1452

## What the fixer owes

Re-probe on your own build first (MANDATORY-PRACTICES I3). Fix the mechanism at the code sites above — one rule, one place (the `cluster:` notes share these files; take them as one fill unit). One positive golden at the introducing edition and one negative below it / per violated rule, each derived from the quoted rule. Sweep the sibling arms named in the findings (CLAUDE.md rule 4). On landing flip `status`, write `closes_rows` from `inventory_rows`, and re-verdict each row with `record_verdicts.py`.

## Extended 2026-09-24 by REGISTRAR 15c (lane-3 batch d2-15c, pinned tree `071ff04ea`)

Registrar 15c: the scope-of-names refuter hit this note's COBOLNET0899 RecursiveContainedWs stage while exercising §8.4.6.3 rule 1's recursive half (a contained program inheriting RECURSIVE under §11.10.4 GR4, with WORKING-STORAGE and a containee, at 85 and 2023) — the row cannot close until this lands.

## The rule (every citation re-run by the registrar through `cite.py --check`)

> if the program possesses the recursive attribute, in the program itself

`python scripts/spec/cite.py --check 8.4.6.3 "…"` → `OK  §8.4.6.3 1)  (Scope of program-names)`

### Refuter evidence — row `SR-8.4.6.3-1` (PARTIAL)

PARTIAL — The scope resolution itself is right: the containing-program arm and the non-COMMON exclusions (including CANCEL) behave as the rule derives. But the rule's second half, 'if the program possesses the recursive attribute, in the program itself', cannot be exercised in one legal shape. A recursive program, whether RECURSIVE is written or inherited under §11.10.4 GR4, that directly contains programs and declares WORKING-STORAGE or a FILE SECTION is refused at compile time with COBOLNET0899 ('recognized but not yet implemented', BinderDriver.cs:479-490). My first probe hit this: contained program XC, inheriting RECURSIVE from XR, with WS and containee XC1, at both --std 2023 and --std 85. No open kb/Work note owns this loud-stage; PB168, the only note that matches, has status landed. So one half is verified only for the shapes that compile, and legal source in the other shapes is rejected. That makes it PARTIAL. No spec-derived test pins the rule's NOT-FOUND arms (test-ref empty).

**Code site.** src/Cobol.Net.Runtime/Control/ProgramTable.cs#ResolveVisible; src/Cobol.Net.Runtime/Control/ProgramTable.cs#Cancel; src/Cobol.Net.Compiler/Binding/BinderDriver.cs#MakeUnit


## Extended 2026-09-24 by REGISTRAR 15d (batch 15d, pinned `071ff04ea`)

Rows added to `inventory_rows`: `GR-11.10.4-4`, `GR-11.10.4-5`.

Batch 15d program-id-paragraph: §11.10.4 GR4 (RECURSIVE makes the program and its containees recursive, no restriction on the container's data division) and GR5 (the §8.6.6 recursive paragraph, 'The program's internal data and that of any contained programs is initialized as described in 14.6.2') are PARTIAL because of this refusal (BinderDriver.cs:479-489, DiagnosticCatalog.RecursiveContainedWs); the spec-derived golden pb133_recursive_containee avoids WS in the container for exactly this reason.

## The rule (every citation re-run through `cite.py --check`)

> If the RECURSIVE clause is not specified in a program or implied for a program, the program shall not be called while it is active

`OK  §11.10.4 4)  (General rules)`

### Row `GR-11.10.4-4` — PARTIAL (editions 2002, 2014, 2023 (the RECURSIVE clause is a 2002 introduction; COBOLNET0885 gates it below 2002; the 'shall not be called while active' half holds at every edition via §14.9.4.4 GR3f))

Rule: 'specifies that the program and any programs contained within it are recursive. The program may be called while it is active and may call itself. If the RECURSIVE clause is not specified in a program or implied for a program, the program shall not be called while it is active' (--check 11.10.4 -> OK §11.10.4 4)). Three halves: (a) inheritance -- MakeUnit `if (parent is { Recursive: true }) recursive = true` cascades transitively (parents built first); (b) self/active call -- ResolveVisible rule 2 admits self when Recursive, CallProgram skips the re-entry check when n.Recursive; (c) non-recursive re-entry -> CallProgram throws EC-PROGRAM-RECURSIVE-CALL (§14.9.4.4 GR3 f), --check OK). Probe probe dir out/probe/program-id-paragraph/gr4.cob --std 2002/2023: contained GR4RC (no clause, inside RECURSIVE GR4R) calls itself -> 'RC 1 RC 2 RC 3'; non-recursive GR4N re-entered from its COMMON containee GR4M -> 'REENTRY REJECTED'. All three verified. Goldens cover (a) and (b); (c)'s rejection is pinned by no golden. Caveat owned by the SR5 finding: MakeUnit's comment declares an INITIAL containee of a recursive container 'legal', which §11.10.3 SR5 forbids -- a SR5 defect, not a GR4 one. || REFUTED: PARTIAL — Two faults in the CONFORMS. (1) REJECTS LEGAL SOURCE: a RECURSIVE program that contains programs AND declares WORKING-STORAGE (or a FILE SECTION) is refused with COBOLNET0899 'recognized but not yet implemented' (BinderDriver.cs:479-489, DiagnosticCatalog.RecursiveContainedWs). GR4 makes that program and its containees recursive with no restriction on the container's data division, so the most ordinary shape of the rule -- a recursive program with WS and nested programs -- does not compile at any edition that has RECURSIVE. Even the rule's own spec-derived golden pb133_recursive_containee says in its header that it avoids WS in the container because 'the recursive-container-with-WS composition [is] a separately staged posture'. A quick search of kb/Work found no OPEN note owning this staged loud (PB168, landed, only added the FILE SECTION to the gate) -> registrar should file one (rejects_legal_source). (2) The adjudicator's proof of half (c) was invalid: in gr4.cob GR4M calls GR4N, but GR4N is a non-COMMON program directly contained in GR4OUT, so by §8.4.6.3 1) it is NOT VISIBLE from GR4M -- 'REENTRY REJECTED' came from a scope miss, not from the re-entry check. I re-probed (c) correctly: outermost non-recursive R6A -> R6B -> CALL "R6A" gives EC-PROGRAM-RECURSIVE-CALL (and R6B calling itself gives the same), at --std 2023 and 85, so (c) itself holds. …
