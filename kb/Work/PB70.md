---
title: "PB70 — LANDED 2026-08-18 — a GROUP reference-modification: a sending one died at run time (NotImplemented), a receiving one was a SILENT no-op (the receiver dropped from the list); every §8.4.3.3.3 SR1 exclusion was a run-time stage"
id: PB70
kind: defect
status: landed
severity: MAJOR
area: codegen/move
wrong_answer: true
crashes: true
silent: true
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["8.4.3.3"]
tags: [cobolsharp, work, defect]
---

# PB70 — LANDED 2026-08-18 — `MOVE TB(1:1) TO …` with TB a GROUP died with NotImplemented; `MOVE "Z" TO TB(2:1)` did NOTHING

> ⚙ **flags:** wrong-answer, silent (the receiving side — the 2026-08-09 sweep's row 3), crashes (the sending
> side) · found incidentally by the PB59-6 SUB_HEXLIT reachability probe (`pb59f2_subhex.cob`), 2026-08-09 ·
> **landed 2026-08-18 (DEVLOG 1304)** — the flags were corrected from `wrong_answer: false / silent: false /
> MINOR` per the sweep's finding that a receiving group ref-mod was dropped from the receiver list.

**Landed (2026-08-18):** ONE admissibility rule — `ReferenceResolver.RefModExclusion` is §8.4.3.3.3 SR1 read as an
exclusion test (COBOLNET1647 at bind for a strongly-typed / variable-length group, a numeric item of a non-DISPLAY /
NATIONAL usage, an edited or numeric leaf subordinate to a strongly-typed group, an index item, a pointer, an object
reference); an admitted GROUP is viewed through a new `GroupImagePlace` decorator (the `NumericImagePlace` shape for
groups: Read = `AsImage()` — an occurs-depending group's CURRENT-count part, §13.18.38 GR8; Write = the ONE
group-image store `PlaceRenderer.WriteGroupImage`, which also replaced FIVE copies of the group-store rule in the
MOVE / INSPECT / STRING / ACCEPT emitters); `RefModPlace.Category` is THE reader of the unique item's category
(six spellings folded; a ref-modified group is ALPHANUMERIC, so Table 16 governs a MOVE into it — COBOLNET0819 for
a noninteger numeric sender). **The receiving swallow is closed structurally:** `ExpressionBinder.ResolveReceiving`
reports an undiagnosed null (`ReferenceResolver.WasDiagnosed`) as COBOLNET0899 recognized-not-implemented, so
`.OfType<Place>()` can no longer drop a receiver silently (the RENAMES-over-COMP receiver negative pins it). The
INSPECT and STRING image stores take a ref-mod receiver FIRST (over a group inner they fell into the group arm).
Golden `pb70_group_reference_modification` (20 shapes: MOVE both ways in a multi-receiver list, relation, DISPLAY,
ORD/LENGTH/UPPER-CASE arguments, INSPECT REPLACING/TALLYING, UNSTRING both ways, a figurative, a class condition,
EVALUATE, an occurs-depending group); negatives `pb70-refmod-{strong-group,binary-item,index-item,
leaf-under-strong-group,group-noninteger-numeric-sender}`, `pb70-receiving-reference-shape-not-implemented`.
Verdicts SR-8.4.3.3.3-1, GR-8.4.3.3.4-2, GR-8.4.3.3.4-6 → CONFORMS (GAP 4045 → 4042). Residues, loud and named: a
group with a float / COMP-5 / INDEX leaf (the Tier-C island — no image); a numeric item of usage NATIONAL (SR1
admits it; national-form numeric data is itself a recognized-not-implemented skeleton). Sibling found and
registered: [[PB88]] (STRING INTO a ref-modified / edited receiver — SR4/SR5 violations — stage loud at run time
instead of a bind-time rejection).

**Measured repro** (`--std 2023`): `01 TB. 05 EL PIC X OCCURS 3.` + `MOVE TB(1:1) TO EL(1)` compiles
clean and aborts at run time: `NotImplementedCobolFeatureException: reference 'TB(1:1)'`, exit 127.

**The charge:** §8.4.3.3 reference-modification applies to a group item (its unique data item is
alphanumeric, §8.4.3.3.3/§8.4.3.3.4 GR2), so the source is legal; the loud stage is the wrong-stage
family — better than silent, still a rejection of legal source at the worst possible time. Locate the
emitter arm that renders a refmod over a GROUP source (the MOVE channel probably reaches a loud
`NotImplemented.Value` where the elementary path has the slice). Probe the sibling contexts (DISPLAY,
relation operand, argument) before fixing — the arm may be one of several.

> ⚙ **2026-08-09 PROBE SWEEP (wf_fdd1492c — probe + adversarial refute, every verdict a pasted CLI run + a --check'd citation; full per-row evidence/mechanisms/blockers frozen in `docs/rearchitecture/backlog-probe-sweep-2026-08-09.json`):** all 5 rows LIVE.
