---
title: "PB552 — SR26 through range endpoints stored as a pair and never compared"
id: PB552
kind: defect
status: open
severity: MINOR
area: binding/data-division
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.18.63.3"]
inventory_rows: ["SR-13.18.63.3-26"]
tags: [cobolsharp, work, defect]
---

# PB552

## Mechanism — value-clause-p1
value-p1-F5-SR26-through-range-endpoints-stored-as-a-pair-and-never-compared

### Expected, from the standard
Each of these is nonconforming and shall draw a compile-time diagnostic: `88 C VALUE 5 THRU 1.` (SR26 a); `88 C VALUE 9.5 THRU 1.5.` (SR26 a, non-integer); `88 C VALUE "Z" THRU "A".` (SR26 b -- the native runtime collating sequence is known); `88 C VALUE 5 THRU 5.` (the rule says LESS THAN, so an equal pair violates it); and a reversed range anywhere in a LIST, e.g. `88 C VALUE 1 THRU 3, 9 THRU 7.`. `88 C VALUE 1 THRU 5.` stays legal.

```
(the finding quotes no rule sentence verbatim, so there is nothing to --check; the implementer MUST quote §13.18.63.3 and re-run `cite.py --check` before writing a code comment)
```

### Observed
ALL FIVE COMPILE CLEAN AND RUN. MEASURED at STRICT --std 2023: sr26_num_reversed (CV = 3 -> NO), c_sr26_reversed_edit (CV = 5.0 -> NO), sr26_alnum_reversed (CV = "M" -> NO), sr26_equal (compiles), b_sr26_multi_range (CV = 8 -> NO, the SECOND range reversed) and b_sr26_alpha_named (compiles even with an explicit IN ALPH1, where the sequence is certainly known). Edition-invariant -- the reversed numeric range compiles at --std 85 too (sr26_85); Annex E records no change. The CONFORMING control behaves: sr26_ok (CV = 3 -> YES). HARM is under-rejection with a silent consequence: the condition is simply never true, so a transposed range leaves a dead branch and the program runs to completion. ⛔ DO NOT LAND THIS ALONE -- SR26 b turns on the runtime collating sequence being KNOWN, and the `IN alphabet-name-1` phrase that names it is discarded (finding F2); a comparison added today would evaluate under the native sequence and would REJECT the conforming `88 C VALUE "C" THRU "A" IN ALPH1.` under an alphabet ordering C < B < A. The national leg is unreachable meanwhile (a national THROUGH range is staged COBOLNET0899, probe b_sr26_national), which is the correct posture for "the collating sequence is otherwise determined at runtime".

### Repro
Pinned worktree E:/CobolSharp/.claude/worktrees/lane3-pin-89044aa0 (89044aa0); its own cobol.exe at src/Cobol.Net.Cli/bin/Debug/net10.0/cobol.exe. Write:
       IDENTIFICATION DIVISION.
       PROGRAM-ID. SR26A.
       DATA DIVISION.
       WORKING-STORAGE SECTION.
       01 CV PIC 9.
       88 C VALUE 5 THRU 1.
       PROCEDURE DIVISION.
           MOVE 3 TO CV.
           IF C DISPLAY "YES" ELSE DISPLAY "NO" END-IF.
           STOP RUN.
then `cobol.exe sr26a.cob --std 2023 --run` -> prints NO, rc 0; expected a diagnostic naming 13.18.63.3 SR26. Probe tree: scratchpad/lane3/out-b3/probe/vcp1/sr26_num_reversed/.

### Code site
NO SITE. src/Cobol.Net.Compiler/Binding/DataBinder.cs#BindCondition:2131-2140 is the ONE place both endpoints exist -- `string rawLo = RawValueOperandText(range.valueClauseOperand(0)); string rawHi = RawValueOperandText(range.valueClauseOperand(1));` ... `cond.Values.Add((rawLo, rawHi));` -- and nothing between those statements compares them. The THROUGH screens that ARE there in the same block are SR29 (boolean -> COBOLNET0898, :2121-2124) and the SR31 national stage (DiagnosticCatalog.NationalThroughRange -> COBOLNET0899, :2125-2130), so the site is exactly where a reader would look for SR26 and finds its two neighbours instead.

## Sibling sweep the fixer owes
Every bug here is a pattern (CLAUDE.md rule 4). Before closing this note, sweep the sibling arms named in the mechanism above and add the ones that fail to `inventory_rows`.

## Provenance
Lane-3 adjudication batch b3 (subjects: value-clause-p1), adjudicated against the compiler pinned at 89044aa0 and RE-PROBED on the registrar's worktree cut from 6e7caa91 (a 1942-program sweep of `out-b3/probe/` plus targeted re-probes); the finding reproduces on today's tree.

## Repro programs
`E:\Temp\claude\E--CobolSharp\a9d94900-79fe-4a38-89f0-921976861543\scratchpad\repros\PB552\` — vcp1. The implementer's opening move is to rerun them against a freshly built `cobol.exe`.

