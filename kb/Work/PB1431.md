---
title: "PB1431 — §8.4.3.14.3 SR1 (LINAGE-COUNTER only in procedure division statements) is screened only for data-name clause operands (ScreenDataNameShape): report-section SOURCE LINAGE-COUNTER + 1 and PRESENT WHEN LINAGE-COUNTER > 1 compile clean and read the live counter, and the bare SOURCE LINAGE-COUNTER refusal comes from a COBOLNET0899 'does not resolve' arm that names the wrong rule"
id: PB1431
kind: defect
status: open
severity: MINOR
area: binder/report
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1199", "PB1401"]
spec_refs: ["8.4.3.14.3"]
inventory_rows: ["SR-8.4.3.14.3-1"]
closes_rows: []
tags: [cobolsharp, work, defect]
---
Filed 2026-09-24 by **REGISTRAR 15b** from lane-3 adjudication batch **15b** (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d2` @`071ff04ea`). Not re-probed by the registrar (MANDATORY-PRACTICES R2: every lead carries its repro and code site); origin/main since the pin carries only registrar landings (no src change), so nothing landed has touched this mechanism.

# PB1431 — OPEN

## The rule (every citation re-run by the registrar through `cite.py --check`)

**SR-8.4.3.14.3-1**

> LINAGE-COUNTER may be referenced only in procedure division statements. …

`python scripts/spec/cite.py --check 8.4.3.14.3 "LINAGE-COUNTER may be referenced only in procedure division statements."` → `OK  §8.4.3.14.3 1)  (Syntax rules)`

## Measured (rows this note claims beyond the findings above)

### `SR-8.4.3.14.3-1` — PARTIAL (adjudicator + refuter record, verbatim)

SR1: "LINAGE-COUNTER may be referenced only in procedure division statements." (cite.py --check 8.4.3.14.3 -> OK 1)). Every data-name clause operand passes the ONE screen ScreenDataNameShape, which refuses the register with COBOLNET2024 citing SR1. Probed (probe/linage-counter): OCCURS DEPENDING ON LINAGE-COUNTER (S1), FILE STATUS (E1), ASSIGN USING (E2), RELATIVE KEY (E3), LINAGE FOOTING AT LINAGE-COUNTER OF LPF (E4), RECORD VARYING DEPENDING ON (E6) -- all rejected with COBOLNET2024 naming SR1. Report section SOURCE LINAGE-COUNTER (S2b) and SUM LINAGE-COUNTER (S2c) are ALSO rejected (correct outcome) but by the resolver's Deferred(UnroutedSpecialRegister) path, drawing COBOLNET0899 report-source-operand-unresolved / report-sum-addend-unresolved: "SOURCE 'LINAGE-COUNTER' does not resolve to a data item (ISO §13.18.53.3 SR4)" -- the outcome conforms, but the sentence names a different rule and an 0899 (recognized-not-implemented band) id where §8.4.3.14.3 SR1 forbids the reference outright (the PB489 shape: right outcome, wrong sentence). Screen section FROM is COBOLNET1560 (optional module not claimed, CONFORMANCE.md §5). Procedure-division references bind (Q1/Q5/g4). Test-ref covers the FD-clause position. || REFUTED: PARTIAL — SR1 (cite.py --check 8.4.3.14.3 -> OK 1) forbids ANY reference outside procedure division statements. The adjudicator probed only bare/data-name clause operands. Two report-section references with the register inside an expression or condition COMPILE CLEANLY (exit 0, dll emitted) and read the live counter at run time: SOURCE LINAGE-COUNTER + 1 (arithmetic-expression SOURCE, accepted at --std 2023 AND --std 85) and PRESENT WHEN LINAGE-COUNTER > 1 / PRESENT WHEN LINAGE-COUNTER OF LPF = 2 (accepted at 2023; at 85 only the PRESENT WHEN edition gate fires, not SR1). The generated C# confirms binding, not a dropped clause: D2.g.cs line 40 `CobolFile.LinageCounter("D2::LPF") + 1L`, D1.g.cs line 40 `if ((CobolFile.LinageCounter("D1::LPF") > 1L))`. So the SR1 screen covers data-name clause operands (ScreenDataNameShape) but not the report-section expression/condition path, which goes through the procedure ExpressionBinder (LINAGE_COUNTER arm of RefExpr) with no data-division check. Also the bare SOURCE LINAGE-COUNTER / SOURCE LINAGE-COUNTER OF LPF rejection comes from COBOLNET0899 ('SOURCE LPF does not resolve to a data item') rather than from SR1, which is incidental (the D3 message even misreads the qualifier as the source item). The rule's reach is not whole, so PARTIAL.

## What the fixer owes

Screen LINAGE-COUNTER at the ExpressionBinder arm by BINDING CONTEXT (data division → COBOLNET2024 SR1), covering SOURCE / SUM / PRESENT WHEN expressions and conditions.
