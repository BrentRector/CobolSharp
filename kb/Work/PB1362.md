---
title: "PB1362 — the main source's reference format is AUTO-DETECTED (`ReferenceFormatProcessor.IsFixedForm` heuristic; the `switches.Count == 0` / `initialFixed` arm of `NormalizeToFreeFormMapped`) instead of defaulting to fixed form, with no option selecting the standard default — §7.3.24.3 GR2 'The default reference format of a compilation group is fixed form'; legal fixed-form text the heuristic misjudges is read as free form"
id: PB1362
kind: defect
status: open
severity: MAJOR
area: frontend/reference-format
wrong_answer: true
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1359", "PB1360", "PB1361", "PB1067", "PB1491", "PB1496"]
spec_refs: ["7.3.24.3"]
inventory_rows: ["GR-7.3.24.3-2", "GR-6.1-1", "GR-6.1-2"]
closes_rows: []
closes_rows_reason: "open — nothing landed yet"
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 15a** from adjudication batch 15a (subjects in §7.2 / §7.3, adjudicator pin `E:\CobolSharp-pins\adj-d2` @ `071ff04ea`; nothing under `src/` landed between the pin and `8bd4c7c9f`, so every measurement below stands). Probe programs: `E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d2\out\probe\<subject>\`.

# PB1362 — OPEN

If the owner wants auto-detection as a convenience it must be an explicit implementor extension behind an option, with the standard default reachable (CLAUDE.md rule 1: latitude only where the spec leaves it — here it does not).

## The rule (every citation re-run by the registrar)

> The default reference format of a compilation group is fixed form. …

`python scripts/spec/cite.py --check 7.3.24.3 "The default reference format of a compilation group is fixed form."` → `OK  §7.3.24.3 2)  (General rules)`

## Finding — source-format-directive (rejects-legal-source)

Main-source reference format is AUTO-DETECTED instead of defaulting to fixed form, with no option selecting the standard default

**Rows:** GR-7.3.24.3-2

**Repro:**

```
out/probe/source-format-directive/p1.cob / p2.cob / p3.cob (generated by gen.py): legal fixed-form programs with no >>SOURCE directive - p1 blank sequence area + card tags in cols 73-80; p2 the same plus a '*' comment line; p3 sequence area 'ABCDEF'. `cobol pN.cob --run`.
```

**Expected from the spec:** §7.3.24.3 GR2: 'The default reference format of a compilation group is fixed form.' Each program is read in fixed form (cols 73+ outside margin R, cols 1-6 the sequence area which §6.3.2 lets hold any character) and prints P1-OK / P2-OK / P3-OK.

**Observed:** All three are rejected: p1 COBOL0001 near 'SFP10001' (the col-73 tag parsed as code), p2 'unexpected *', p3 'unexpected ABCDEF'. With no directive, NormalizeToFreeFormMapped calls IsFixedForm, a heuristic that needs a numeric sequence area or a col-7 glyph not vetoed by text past col 72; anything else is read as FREE. CONFORMANCE.md DOC-A.1-158 records it as an 'implementor extension', but §4.2.10 3) admits different functionality for a required element only when 'standard-conforming behavior is also implemented and ... an implementor-defined mechanism exists for selection of the nonstandard behavior' - there is none. The fix shape (the standard default, with auto-detection kept only behind an option) is the owner's call to schedule; the verdict is not.

**Code site:** src/Cobol.Net.Frontend/Preprocessor/ReferenceFormatProcessor.cs#NormalizeToFreeFormMapped (switches.Count == 0 arm and initialFixed); src/Cobol.Net.Frontend/Preprocessor/ReferenceFormatProcessor.cs#IsFixedForm

## What the fixer owes (and the sibling sweep)

Default to fixed form; if detection is kept, put it behind an option (`--source-format auto`) documented in CONFORMANCE.md, and sweep the corpus/goldens that rely on detection (they must carry `>>SOURCE FORMAT FREE` or the option). Golden: a fixed-form program the heuristic would misjudge.

**Rows claimed:** GR-7.3.24.3-2

## Folded from the registrar-15d draft PB1495 (batch 15d, pinned `071ff04ea`) (the rows GR-6.1-1, GR-6.1-2 — the rest of the draft went to PB1361)

Registrar 15d drafted this mechanism before Registrar 15a's note landed; the draft is folded here rather than kept as a duplicate. Rows added to `inventory_rows`: `GR-6.1-1`, `GR-6.1-2`. The draft's title: the reference-format MODE is decided by a structural heuristic instead of the rules: with no SOURCE FORMAT directive IsFixedForm calls conforming FIXED-form source FREE when the sequence area holds non-digits or is blank with text past column 72 (the standard's default is fixed-form), and in free form every directive line is cut at column 72 before matching, so `>>SOURCE FORMAT FIXED` whose operand lies past column 72 is COBOLNET1911 and the mode never switches

Filed 2026-09-24 by **REGISTRAR 15d** from lane-3 adjudication batch **15d** (adjudicated on the pinned tree `071ff04ea`; everything that landed since — Registrar 14a/14b/14c — is register-only, so no code change touches this mechanism). Every citation below was re-run by the registrar through `cite.py --check`.


### The rule (every citation re-run through `cite.py --check`)

> The default reference format of a compilation group is fixed-form.

`OK  §6.1   (General)`

> 1) If the line is a SOURCE FORMAT directive, the reference format mode is determined and the SOURCE FORMAT directive line is logically discarded.

`OK  §6.5 1)  (Logical conversion)`


### Finding — `reference-format` (rejects-legal-source; rows `GR-6.1-1`, `GR-6.1-2`)

**Mechanism.** With no SOURCE FORMAT directive the reference format is chosen by a structural heuristic (IsFixedForm) rather than the standard's fixed-form default, and the heuristic calls conforming fixed-form source FREE when the sequence area holds non-digits (§6.3.2 allows any character) or when it is blank, no indicator glyph appears and columns 73+ hold text

**Repro.** E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d2b\out\probe\reference-format\rf7.cob: every line `ABC00n` + space + text -> COBOL0001 unexpected 'ABC000'. rf8.cob: blank sequence area, text from col 8, tag 'RF8nnnnn' in cols 73-80 -> COBOL0001 near 'RF800000'.

**Expected from the spec.** §7.3.24.3 2) and §6.1 intro: 'The default reference format of a compilation group is fixed form'; §6.3.2 sequence area content is any character; §6.3.1 text past margin R is not program text. Both programs compile and display [OK7] / [OK8].

**Observed / reasoning.** IsFixedForm requires cols 1-6 digits-or-space (seqOk) and, absent a numeric sequence, a non-space indicator glyph with nothing past col 72. DOC-A.1-158 documents auto-detection as an 'implementor extension', but an extension cannot make a program the standard's default governs fail to compile. The fix is structural (the default must be fixed-form, with auto-detection at most a fallback that cannot override it for conforming fixed-form source) — an owner-visible choice because the NIST/free-form corpus relies on the heuristic.

**Code site.** src/Cobol.Net.Frontend/Preprocessor/ReferenceFormatProcessor.cs#IsFixedForm; src/Cobol.Net.Frontend/Preprocessor/ReferenceFormatProcessor.cs#NormalizeToFreeFormMapped (switches.Count == 0 arm and initialFixed)

### Row `GR-6.5-1` — DIVERGES (editions 2002/2014/2023 (the >>SOURCE FORMAT directive is part of the §7.3 compiler-directive facility; a COBOL-85 source has no directive line — gated COBOLNET0900))

'If the line is a SOURCE FORMAT directive, the reference format mode is determined and the SOURCE FORMAT directive line is logically discarded.' (--check OK §6.5 1)). NormalizeToFreeFormMapped collects the switches (TryMatchDirective, word-keyed via CompilerDirectiveLine, margin-R cut, sequence area allowed), partitions into segments whose format is the declared one, and emits a BLANK line in the directive's slot (line 178) — discarded content, line numbering preserved. Malformed operand is consumed and diagnosed (PB794, landed). Adversarial: a '*' comment line whose text is '>>SOURCE FORMAT FREE' is NOT taken as a directive (rf15.cob -> [OK15]); a free-form '*> >>SOURCE FORMAT FIXED' comment likewise (rf16.cob -> [OK16]). Unit tests SourceFormatTests (tests/CobolSharp.Tests.Unit/Preprocessor) drive CobolNet.Frontend's ReferenceFormatProcessor directly with spec-derived expectations (directive line blank; following segment in the declared format). || REFUTED: DIVERGES — The directive line must be recognized (and the mode determined) in whichever format is in effect. TryMatchDirective / TryMatchStackOp cut EVERY raw line at MarginR (col 72) before matching, but in free form the ENTIRE line is the program-text area (§6.4.1) and a directive may be preceded by any number of spaces (§7.3.3 SR2). A legal free-form '>>SOURCE FORMAT FIXED' whose operand lies past column 72 is truncated to '>>SOURCE FORMA', diagnosed COBOLNET1911 as malformed, the mode is NOT switched, and the following fixed-form lines are then misread (COBOL0001). Main paths hold (fixed/free directive lines, indented free directive with inline comment, library-text directives + revert per §7.3.24.3 GR3/GR5: probes p3/p4/p5/p7/p8 all correct), so the defect is the margin-R cut applied format-blind.

### What the fixer owes

Make the mode a STATE of the logical-conversion pass: fixed-form by default (§6.1), switched only by a SOURCE FORMAT directive (or an explicit compiler option documented in CONFORMANCE.md as the implementor's selection), and recognise a directive line against the program-text area OF THE MODE IN EFFECT — the whole line in free form, columns 8–72 in fixed form. If the heuristic survives as a convenience it must be an option-gated extension documented as such, never the default. Goldens: rf7/rf8 (non-digit and blank sequence areas compile as fixed form); a free-form directive with its operand past column 72. Sweep TryMatchStackOp and CopyProcessor's copybook classifier (PB1067's ≥50 % sequence-number rule is the same heuristic in library text).
