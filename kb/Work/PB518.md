---
title: "PB518 — There is NO clause-site screen for EXTERNAL and GLOBAL"
id: PB518
kind: defect
status: landed
severity: MINOR
area: binding/data-division
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.16.3"]
inventory_rows: []
closes_rows: ["SR-13.16.3-6", "SR-13.16.3-7", "SR-13.18.22.3-1"]
tags: [cobolsharp, work, defect]
---

# PB518

## Mechanism — data-description-entry-p2
F2 — There is NO clause-site screen for EXTERNAL and GLOBAL. Their only structural gate lives inside the post-bind REGISTRATION scan (CallBindExternalAndGlobal), where the level test and the entry-name test are written as `continue` filters: an entry that fails either is skipped, not diagnosed. A `continue` is a selector, so it is evidence about what it registered and says nothing to the user about what it dropped — an out-of-place or FILLER-named EXTERNAL/GLOBAL clause is accepted, silently stripped of its attribute, and the program then behaves as if the clause had never been written. This one mechanism owns both the GLOBAL half of SR6 and the whole of SR7.

### Expected, from the standard
ISO/IEC 1989:2023 §13.16.3 SR6 (cite-checked): "The CONSTANT RECORD and GLOBAL clauses may be specified only in data description entries whose level-number is 1." §13.16.3 SR7 (cite-checked): "The data-name format of the entry-name clause shall be specified for any entry containing the GLOBAL or EXTERNAL clause, or for record descriptions associated with a file description entry that contains the EXTERNAL or GLOBAL clause." §13.18.27.3 SR1 b) (cite-checked) says the same level-1 restriction for GLOBAL from the clause's own side. Every one of the four repro entries is nonconforming and shall be rejected at compile time.

```
$ python scripts/spec/cite.py --check 13.16.3 "The data-name format of the entry-name clause shall be specified for any entry containing the GLOBAL or EXTERNAL clause, or for record descr..."
  OK  §13.16.3 7)  (Syntax rules)
```

### Observed
All four compile and run clean, at every edition, with no diagnostic. The mechanism is visible in DataBinder.Linkage.cs#CallBindExternalAndGlobal: `if (!int.TryParse(entry.levelNumber().GetText(), out int lvl) || lvl is not (1 or 77)) continue;   // §13.18.22 SR1 / §13.18.27 SR1b — record-description level-01/77 entries only` (~355) and `if (entry.dataName()?.GetText() is not { } name) continue;` (~357). Both comments describe a RULE while the code performs a SKIP. The consequence is not merely permissive: in (a) H is never added to _callGlobalRoots, so a contained program referencing H gets COBOLNET1639 'not defined' — the user's GLOBAL clause was accepted and then annulled. In (b) the record is never re-based onto the run-unit ExternalStore, so two programs that both declare it do NOT share storage (§13.18.22.4 GR4). kb/Work PB163 (status: landed) already names part of this as its residue item (a): 'the 01/77 admission is looser than both SRs (each says level 1; a 77-level GLOBAL/EXTERNAL clause is accepted and registered rather than screened at the clause site) ... a clause-site screen belonging to the §13.18.27 checks, not this registration pass.' The measurements here are WIDER than that text: level 5 (not just 77) is silently dropped, and the entry-name half (SR7) and the FD half are unmentioned there. Note also DataBinder.Reports.cs:317, 'A GENERATE-able detail always has a data-name (§13.16.3 SR7) — unreachable unless the binder let ...', which asserts an invariant the binder does not establish.

### Repro
Under E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/ddep2, `E:/CobolSharp/.claude/worktrees/lane3-pin-89044aa0/src/Cobol.Net.Cli/bin/Debug/net10.0/cobol.exe <f>.cob --run` (identical at --std 85/2002/2014/2023):
(a) SR6, GLOBAL below level 1 — prints ABCD, no diagnostic:
       01 G.
          05 H IS GLOBAL PIC X(4) VALUE "ABCD".
(b) SR7, EXTERNAL on a filler entry — prints OK, no diagnostic:
       01 FILLER IS EXTERNAL PIC X(4).
(c) SR7, GLOBAL on a filler entry — prints OK, no diagnostic:
       01 FILLER IS GLOBAL PIC X(4) VALUE "ABCD".
(d) SR7, the file-description half — prints OK, no diagnostic:
       FD F IS EXTERNAL.
       01 FILLER PIC X(10).

### Code site
src/Cobol.Net.Compiler/Binding/DataBinder.Linkage.cs:328 CallBindExternalAndGlobal (the level filter ~355, the entry-name filter ~357); no clause-site check exists in src/Cobol.Net.Compiler/Binding/DataBinder.cs#BindEntry, which decodes `hasExternal`/`hasGlobal` at ~2308-2312 for other rules only

## Sibling sweep the fixer owes
Every bug here is a pattern (CLAUDE.md rule 4). Before closing this note, sweep the sibling arms named in the mechanism above and add the ones that fail to `inventory_rows`.

## Provenance
Lane-3 adjudication batch b3 (subjects: data-description-entry-p2), adjudicated against the compiler pinned at 89044aa0 and RE-PROBED on the registrar's worktree cut from 6e7caa91 (a 1942-program sweep of `out-b3/probe/` plus targeted re-probes); the finding reproduces on today's tree.

## Repro programs
`E:\Temp\claude\E--CobolSharp\a9d94900-79fe-4a38-89f0-921976861543\scratchpad\repros\PB518\` — ddep2. The implementer's opening move is to rerun them against a freshly built `cobol.exe`.

## Landed (2026-09-23, wave 58 group B — PB507 + PB512 + PB518 + PB519)

**Re-probed first** (recreated from this note's text). (a) `05 H IS GLOBAL` printed ABCD, (b) `01 FILLER IS EXTERNAL`, (c) `01 FILLER IS GLOBAL` and (d) `FD F IS EXTERNAL. 01 FILLER` printed OK — all rc=0, REPRODUCED. Wider than the note: EXTERNAL at level 05/77, in LOCAL-STORAGE, in LINKAGE and on a FILE SECTION record were silently dropped too, and a GLOBAL clause on a level-1 FILE SECTION record — LEGAL per §13.18.27.3 SR1 b) — was UNDEFINED in a contained program (COBOLNET1639: legal source rejected), because the scan never read that section.

**Now** the entry-local rows refuse every misplaced clause at its entry (COBOLNET2404) and SR7's FD half is `ScreenFileRecordEntryNames`. `CallBindExternalAndGlobal` no longer reads the parse tree: it registers from `DataItem.HasExternalClause` / `HasGlobalClause`, which only an ADMITTED clause sets, over working-storage, local-storage, linkage AND file records — so the FILE SECTION GLOBAL record now works (golden `85/pb518_clause_placement_legal`). Level 77 is refused for both clauses: §13.16.3 SR6 and §13.18.22.3 SR1 say level 1, and §13.16.3 SR1 lists 77 as a distinct level-number (this also retires PB163's residue item (a)). Negative goldens `negative/pb518-global-below-level-1`, `negative/pb518-external-fd-filler-record`.

One table now owns the rule shape: `ClausePlacementRules.Rules` in `src/Cobol.Net.Compiler/Binding/DataBinder.ClausePlacement.cs`, four row kinds (ElementaryOnly, Residence, DataNameRequired, NotWith), read at two sites — `ScreenClausePlacement` in `BindEntry` for the entry-local rows and `CheckElementaryOnlyClauses` over the finished forest — plus the `CheckClauseSubjects` pass for the BLANK WHEN ZERO / JUSTIFIED subject rules. `DataClausePlacementDriftTests` requires every `DataClauseKind` to have its placement answered exactly once (a row, or a named site in `PlacementAnsweredElsewhere`) and every ElementaryOnly subject to be refused on a group. Codes COBOLNET2403 (§13.16.3 SR11), COBOLNET2404 (EXTERNAL/GLOBAL placement), COBOLNET2405 (clause subject). Every citation below was run through `scripts/spec/cite.py --check`. Verdict batch: `batch-w58b-clause-placement.json` (GAP 2060 → 2050).
