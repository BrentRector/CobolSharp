---
title: "PB430 — §14.9.25.4 GR4 diverges twice, silently, at all four editions: `MoveClassifier.IsGroupSender` excludes a level-66 RENAMES alias that §13.18.45.4 GR2 defines as an ALPHANUMERIC GROUP ITEM (so a RENAMES … THRU sender gets EDITED into '$2,345.00' where the rule forbids conversion), and `MoveEmitter.EmitGroupMove` renders every elementary sender as its zoned DISPLAY image, converting a COMP-3/COMP item into a group where 'there is no conversion'"
id: PB430
kind: defect
status: open
severity: MAJOR
area: codegen/move
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.25.4", "13.18.45.4", "13.18.38.4"]
inventory_rows: ["GR-14.9.25.4-4"]
tags: [cobolsharp, work, defect]
---

# PB430 — GR4's no-conversion clause is true only for a DISPLAY sender

## The mechanism

GR4 has two sentences and this compiler gets the FIRST one wrong on the sender side and the SECOND one wrong on
one of its two arms. Both are silent wrong answers on legal source, at every edition.

**(1) The sender-side classifier rejects the standard's own definition of a group.**
`src/Cobol.Net.Compiler/Binding/Bound/MoveClassifier.cs:65-72`:

```csharp
/// … and so is a RENAMES alias (the level-66 view is composed as ONE elementary
/// alphanumeric item, §13.18.45). …
public static bool IsGroupSender(BoundOperand source) =>
    source is BoundFieldOperand { Place: not (RefModPlace or RenamesPlace) } f
    && f.Place.Item.IsGroup && !f.Place.Item.IsAsIfElementary;
```

§13.18.45.4 GR2 says the opposite of the comment: with THROUGH, data-name-1 defines an alphanumeric GROUP item. A
group item is not an elementary item, so GR4's first sentence makes a MOVE from `66 R RENAMES a THRU b` a
NON-elementary move, and GR4's second sentence then forbids conversion — and with it the receiver-category
EDITING that only an elementary move performs. Excluding `RenamesPlace` classifies it as elementary and the
receiver's edit mask is applied. This is a MIS-CLASSIFICATION, not a missing feature, and the comment is the
textbook inherited-citation failure: a real clause number attached to the opposite claim.

**(2) The no-conversion clause is not implemented on the elementary-sender-into-a-group arm.**
`src/Cobol.Net.Compiler/CodeGen/Verbs/MoveEmitter.cs:146` `#EmitGroupMove` renders the source with
`OperandText.AsString(source, num, deSign: false)` (:191) — the ZONED DISPLAY image — for EVERY sender. So a
COMP-3 / COMP / COMP-5 elementary item moving into a group is converted from its packed or binary representation
to display digits, and occupies the wrong number of character positions. The compiler contradicts itself: the
group-SENDER direction, `#EmitGroupToElementaryMove` (:116), deposits the pinned representation bytes for the very
same item.

## The rules, quoted, with their `--check` lines

    python scripts/spec/cite.py --check 14.9.25.4 "there is no conversion of data from one form of internal representation to another"
    OK  §14.9.25.4 4)  (General rules)
        Any move that is not an elementary move, and does not reference a variable-length group, is treated
        exactly as if it were an alphanumeric to alphanumeric elementary move, except that there is no
        conversion of data from one form of internal representation to another.

    python scripts/spec/cite.py --check 13.18.45.4 "defines an alphanumeric group item"
    OK  §13.18.45.4 2)  (General rules)
        2) When the THROUGH phrase is specified, data-name-1 defines an alphanumeric group item that includes
           all elementary items starting with data-name-2 …

    python scripts/spec/cite.py --check 13.18.45.4 "When the THROUGH phrase is not specified"
    OK  §13.18.45.4 1)  (General rules)
        1) When the THROUGH phrase is not specified, all of the data attributes of data-name-2 become the data
           attributes of data-name-1 …

    python scripts/spec/cite.py --check 13.18.38.4 "the maximum length of the group"
    OK  §13.18.38.4 8) b)  (General rules)
        b) If the data item referenced by data-name-1 is included in the same group and the group data item is
           referenced as a sending operand, only that part of the table area that is specified by the value of
           the data item ref…

GR4's first sentence — "Any move in which the sending operand is either a literal or an elementary item and the
receiving item is an elementary item is an elementary move. Bit group items and national group items are treated
as elementary items in the MOVE statement." — is what the RENAMES classification turns on. §13.18.45.4 GR1 is the
control: WITHOUT THROUGH the alias takes data-name-2's attributes, so an alias of an elementary item IS
elementary and the compiler's WITHOUT-THROUGH behaviour is correct.

## Expected vs observed — RE-RUN ON HEAD (89044aa0)

    (1) RENAMES … THRU as a sender into a numeric-edited receiver.
        01 WS-REC. 05 WS-A PIC X(3) VALUE "123". 05 WS-B PIC X(2) VALUE "45". 05 WS-C PIC X(2) VALUE "ZZ".
        66 WS-R RENAMES WS-A THRU WS-B.
        01 WS-G2. 05 WS-G2A PIC X(3) VALUE "123". 05 WS-G2B PIC X(2) VALUE "45".
        01 WS-E PIC $$,$$9.99.
        MOVE WS-R TO WS-E / MOVE WS-G2 TO WS-E
        cobol.exe gr4_renames_sender.cob --std 2023 --run   rc=0
          R->E [$2,345.00]        EXPECTED [12345    ]  — the same as the real group sender below
          G->E [12345    ]
        The two senders are the same KIND of thing under §13.18.45.4 GR2 and must give the same result.
        CONTROL, the WITHOUT-THROUGH arm, correct:
        cobol.exe gr4_renames_nothru.cob --std 2023 --run   rc=0
          R  ->E [12345    ]
          SUB->E [12345    ]

    (2) an elementary PACKED-DECIMAL sender into a group, both directions in one program (ORD of each byte).
        01 PG. 05 P PIC 9(4) COMP-3 VALUE 1234.   01 G1 PIC X(6).   01 G2. 05 G2A PIC X(6).
        MOVE PG TO G1     (group sender  — A below)
        MOVE P  TO G2     (elementary sender into a group — B below)
        cobol.exe gr4_norepconv.cob --std 2023 --run   rc=0
          BYTE-LENGTH(P)=003
          A01=00002  A02=00036  A03=00080  A04=00033 …    -> bytes 0x01 0x23 0x4F, the packed representation
          B01=00050  B02=00051  B03=00052  B04=00053 …    -> characters '1' '2' '3' '4', DISPLAY digits
        EXPECTED: B deposits the same three representation bytes A does, and occupies three character
        positions. "There is no conversion of data from one form of internal representation to another."
        The BINARY sibling behaves identically:
        cobol.exe gr4_noconv_binary.cob --std 2023 --run   rc=0
          A01=00005 A02=00211   -> 0x04 0xD2, two big-endian two's-complement bytes for PIC 9(4) COMP
          B01=00050 B02=00051 B03=00052 B04=00053   -> '1' '2' '3' '4'
        And at COBOL-85:
        cobol.exe gr4_noconv85.cob --std 85 --run   rc=0   DIFFER / B-IS-DISPLAY-DIGITS

    THE REST OF GR4 HOLDS, re-measured or confirmed by the b2 refuter and NOT part of this note:
    the elementary/non-elementary decision itself (`MoveClassifier.Kind`, computed once at BoundMove
    construction); bit and national groups treated as elementary via `DataItem.IsAsIfElementary`; the group
    sender into an edited receiver NOT editing (`gr4_group_no_edit.cob --std 2023` -> `GRP->NE=[12345    ]`
    against `ELM->NE=[$2,345.00]`); the receiver's JUSTIFIED clause still applying; §13.18.38.4 GR8's OCCURS
    carve-out honoured on BOTH arms; and a group carrying a USAGE POINTER or USAGE OBJECT REFERENCE leaf moving
    without refusal.

## The code sites and the shape of the repair

- `src/Cobol.Net.Compiler/Binding/Bound/MoveClassifier.cs:65-72` `#IsGroupSender` — remove `RenamesPlace` from
  the exclusion and fix the comment. ⚠ Only the THROUGH form: §13.18.45.4 GR1 makes a RENAMES WITHOUT THROUGH
  take data-name-2's attributes, so the classifier must ask the alias whether it spans a range, not whether it is
  a `RenamesPlace`. A blanket removal would break the currently-correct `gr4_renames_nothru` case. `RefModPlace`
  stays excluded — §8.4.3.3.4 GR6 genuinely makes a ref-mod result elementary alphanumeric.
- `src/Cobol.Net.Compiler/CodeGen/Verbs/MoveEmitter.cs:146` `#EmitGroupMove` (:191, the
  `OperandText.AsString(..., deSign: false)` render) against `:116` `#EmitGroupToElementaryMove`.

**THE REPAIR IS ONE REPRESENTATION CODEC ASKED IN BOTH DIRECTIONS** (CLAUDE.md rule 5). The compiler already owns
the answer — `EmitGroupToElementaryMove` deposits the pinned representation bytes of exactly these items — and
`EmitGroupMove` re-derives a different one. That is the two-arm-dispatch shape with one arm written (memory:
`two_arm_dispatch`): extract the item→representation-image codec and have BOTH arms call it, so an elementary
sender into a group contributes the same bytes it would contribute as part of a group, and its width in the
receiving area is its representation width. Doing it in `EmitGroupMove` alone would leave the two arms as two
answers to one question.

## The sibling sweep the fixer must do

- Every other consumer of `MoveClassifier.IsGroupSender` and of the elementary/non-elementary decision — the
  RENAMES mis-classification is in the CLASSIFIER, so anything reading `BoundMove.Kinds` inherits it.
- Every other statement whose rules say "as if it were an alphanumeric to alphanumeric move" or otherwise move a
  group without conversion: the record-level file I/O paths, SORT/MERGE keys, REDEFINES views, CALL BY REFERENCE
  group parameters, and the group-image reads in `NumericRenderer`. Each must agree with the one codec.
- Every OTHER exclusion in a classifier whose justification is a code comment citing a clause. `IsGroupSender`'s
  RENAMES comment cited §13.18.45 for the opposite of what §13.18.45.4 GR2 says; the same shape is worth grepping
  for wherever a `not (…Place)` exclusion carries a § in its doc.
- `MoveEmitter`'s DISPLAY-sender coincidence is why this survived: `conformance:2023/move_group_overpunch`
  (`01 A PIC S9(3) VALUE -123. 01 G. 05 B PIC X(3). MOVE A TO G` -> '12L') has a DISPLAY sender, so it pins only
  GR6a's non-application and cannot see the no-conversion clause at all. The fix needs goldens with COMP-3, COMP,
  COMP-5 and floating-point senders into a group, in BOTH directions.

## Rows

- `GR-14.9.25.4-4` (DIVERGES) — every clause of the rule was located and measured; two of them diverge, each a
  silent wrong answer on legal source at all four editions. The rule's own exclusion, "and does not reference a
  variable-length group", routes those moves to GR9, which is not implemented — that failure is charged to
  `GR-14.9.25.4-9` and its L3.x sub-items, not here. The cited test-ref
  `conformance:2023/move_group_overpunch` does NOT pin this rule's diverging branch.

*(b2 adjudication + refutation, subject move-statement-p2, re-measured on HEAD 89044aa0 2026-09-04.)*
