---
title: "PB1430 — inlineInvocationSegment writes `(LPAREN argumentList? RPAREN)?`, admitting an EMPTY parenthesis pair the §8.4.3.4.2 general format does not print (its argument group repeats inside one parenthesis pair, so at least one argument) — `O :: \"M\" ( )` compiles and runs"
id: PB1430
kind: defect
status: open
severity: MINOR
area: frontend/grammar
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1425", "PB1429"]
spec_refs: ["8.4.3.4.2"]
inventory_rows: ["FMT-8.4.3.4.2", "SR-8.4.3.1.3-5"]
closes_rows: []
tags: [cobolsharp, work, defect]
---
Filed 2026-09-24 by **REGISTRAR 15b** from lane-3 adjudication batch **15b** (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d2` @`071ff04ea`). Not re-probed by the registrar (MANDATORY-PRACTICES R2: every lead carries its repro and code site); origin/main since the pin carries only registrar landings (no src change), so nothing landed has touched this mechanism.

# PB1430 — OPEN

## The rule (every citation re-run by the registrar through `cite.py --check`)

**FMT-8.4.3.4.2**

> OMITTED is the only underlined word in the printed figure (required word); :: and …

`python scripts/spec/cite.py --check 8.4.3.4.2 "OMITTED is the only underlined word in the printed figure (required word); :: and"` → `OK  §8.4.3.4.2   (General format)`

**SR-8.4.3.1.3-5**

> Inline-invocation-1 is defined by 8.4.3.4, Inline method invocation. …

`python scripts/spec/cite.py --check 8.4.3.1.3 "Inline-invocation-1 is defined by 8.4.3.4, Inline method invocation."` → `OK  §8.4.3.1.3 5)  (Syntax rules)`

## Finding — `inline-method-invocation` (under-rejects; rows `FMT-8.4.3.4.2`)

**Mechanism.** inlineInvocationSegment writes `(LPAREN argumentList? RPAREN)?`, admitting an empty parenthesis pair the general format does not print

**Repro.** MOVE A1 :: "GETNAME" () TO W.  (GETNAME has no USING) -> probe p2_emptyparens.cob, --std 2023 --run

**Expected from the spec.** §8.4.3.4.2: the bracketed group is '( {arith|bool|identifier-2|literal-2|OMITTED} ... )' - the brace selects exactly one form and '...' repeats it, so a written parenthesis pair holds at least one argument; '()' is not conforming and shall be diagnosed.

**Observed / reasoning.** Compiles and runs, W=ACCOUNT. Grammar optional argumentList inside the parens; InvocationSite.ArgsWritten is true with zero Args and the binder never checks. Drift test TheRuleMatchesThePrintedGeneralFormat asserts only that 'argumentList' appears. Sibling to sweep: §8.7.4 requires a separator space before and after '::' but spaceless `O::"CHK"(OMITTED)` is accepted and pinned by MethodOmittedArgumentTests.

**Code site.** src/Cobol.Net.Frontend/Grammar/Core/CobolOO.g4#inlineInvocationSegment

## Measured (rows this note claims beyond the findings above)

### `SR-8.4.3.1.3-5` — PARTIAL (adjudicator + refuter record, verbatim)

SR5 (cite.py --check 8.4.3.1.3 5) OK): Format 4 is the 8.4.3.4 inline invocation; the rule `objectReference inlineInvocationSegment+ refModPart*` exists and the spec-derived golden pb428 exercises nine positions (kb/Work PB428 landed); the below-2002 negative is conformance:negative/pb428-inline-method-invocation-below-2002. Probes under out/probe/ident with the pinned cobol.exe. || REFUTED: PARTIAL — The rule imports the 8.4.3.4.2 format, whose own verdict is DIVERGES. The format's argument brace group repeats inside ONE parenthesis pair, so at least one argument is required. inlineInvocationSegment is `(LPAREN argumentList? RPAREN)?`, so an empty pair is accepted and runs (an under-reject of the Format 4 shape). The pb428 golden covers the legal positions only.

## What the fixer owes

`(LPAREN argumentList RPAREN)?`; negative golden; check the render-spec-page for the underlining first (diagram-lossy clauses).
