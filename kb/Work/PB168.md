---
title: "PB168 — file-connector lifetime is keyed to INSTANCE identity while the registry is keyed by NAME: a fresh RECURSIVE activation re-registers and silently REPLACES the still-open connector (the displaced writer never disposed — the OS handle leaks, buffered writes are lost, the inner activation answers '42' and the resumed outer activation finds a closed stranger where §14.6.2.3.3 requires last-used state)"
id: PB168
kind: defect
status: landed
severity: MAJOR
area: codegen
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.6.2.3.2", "14.6.2.3.3", "8.6.4", "13.5.4"]
tags: [cobolsharp, work, defect]
---

# PB168

`__filesRegistered` is emitted as a per-INSTANCE field (ProgramEmitter, the registration guard in
DispatchEmitter's `__Activate`), and ProgramTable.CallProgram gives a RECURSIVE (or INITIAL) unit a FRESH
instance per activation — so every such activation re-runs `CobolFile.Register(...)`, and
`FileRegistry.Register` does `_files[cobolName] = new ...Connector(...)`, unconditionally REPLACING the live
entry. The displaced connector's StreamWriter/StreamReader is never disposed (dispose is only reachable from
Close), so the OS handle leaks and its buffered writes are lost.

CONSEQUENCE (recursive, non-INITIAL): §14.6.2.3.2 puts internal file connectors "in no open mode" only on
(1) first activation in the run unit, (2) an INITIAL program's activation, (3) after CANCEL — a second
activation of a RECURSIVE non-INITIAL program is §14.6.2.3.3 LAST-USED state and must keep its connector.
Today: a RECURSIVE program that OPENs F at depth 1 and recurses finds its connector replaced by a closed
stranger — the depth-2 READ/WRITE answers '42', and the resumed depth-1 activation's connector is gone. For
an INITIAL unit the re-registration is semantically the required re-initialization, but the REPLACE arm still
drops an open connector undisposed when the prior activation ended abnormally.

MASKING (recorded by the PB154 review fleet): `2023/pb154_cancel_initial`'s REOPEN=00 leg is
host-locking-dependent — on POSIX the next activation's re-registration hands back a fresh CLOSED connector
even if the CANCEL-time close never ran; only Windows' sharing violation ('30') discriminates. A fix here
should re-pin that leg in a host-independent way.

WHAT WOULD CLOSE IT: key the registration guard to the connector's true scope (the unit, not the instance —
a static flag for a RECURSIVE unit, per-activation only where §14.6.2.3.2 requires re-initialization), or
make Register close/dispose what it displaces; plus a golden where a RECURSIVE unit OPENs, recurses, and
WRITEs at both depths.

*(Found by the PB154 pre-landing review fleet, 2026-08-29 — findings [runtime-correctness]/[emitter-binder],
each adversarially confirmed 3-0; full claims in the session scratchpad `pb154-fleet-confirmed.json`. The
CANCEL-side half of the same lifetime mismatch — a returned recursive activation's connectors surviving the
null slot — LANDED with PB154's transient close.)*

## ✅ LANDED 2026-08-30 — DEVLOG Entry 1397

The registration guard now has the CONNECTOR'S storage duration (§14.6.2.3.2 action 3 fires only when data
enters the INITIAL state — cases 1–3 for a non-INITIAL unit's static data; §14.6.2.3.3 last-used
otherwise): `DataBinder.UnitStaticFiles` (RECURSIVE ∧ ¬INITIAL, every FUNCTION, no childless conjunct —
the flag never crosses an __outer bridge) makes `__filesRegistered` STATIC, `__ResetStatics` returns it to
false on exactly the three initial-state cases (run-unit registration invoke / CancelNode / the INITIAL
cascade), the emission+registration conditions collapsed into ONE shared predicate
(`DataBinder.EmitsStaticReset` — the hand-mirrored pair with its CS0103 warning comment retired), and
`FileRegistry.CloseDisplaced` closes what a re-registration displaces (the three Register* arms; only an
abnormally-abandoned connector can be open there).

**THE REVIEW FLEET (58 agents, 14 confirmed collapsing to FIVE distinct defects — three of them in the
fix's own first cut) closed the wave honest:**
- **CRITICAL — one guard, three scopes:** `__Activate`'s single block gated run-unit connector
  registration AND per-instance report-engine construction AND the LINAGE evaluator install. With the
  static guard, a RECURSIVE unit's second activation left `__RPT_n` null (NRE on INITIATE — refuters
  reproduced it with a running binary) and the LINAGE closure captured the FIRST activation's instance
  forever. Fix: one guard per scope — registration under the (static) flag, report construction under a
  new per-INSTANCE `__reportsConstructed`, `EmitLinageEvaluators` installed UNGUARDED so the shared
  connector always evaluates the CURRENT activation's geometry.
  **⛔ SUPERSEDED 2026-09-05 by [[PB673]]:** the unguarded install was the best of two wrong shapes —
  it reads the LAST activation to have run its prologue, which for a returned inner activation is DEAD
  storage, and for an EXTERNAL connector is another PROGRAM's item where §13.18.34 GR6 b) / §12.4.5.3
  GR3 b) name the element that EXECUTES the statement. Both are gone: the LINAGE page and the ASSIGN …
  USING association are now ARGUMENTS of the OPEN/WRITE statements. `2023/pb168_recursive_report_engine`
  pins the second-activation report run.
- **MAJOR — §8.6.4's other half:** "data items and file connectors defined in the working-storage or
  file section … are static items … one copy in a run unit" — the FD RECORD AREA had to move with the
  connector, or the shared connector reads into a per-activation area and the resumed outer activation
  sees its own stale record (a silent wrong answer replacing the loud '42'). `RouteStaticUnitStorage`
  now routes FILE SECTION records through the same extracted per-root routine as WS (EXTERNAL files stay
  on the ExternalStore; `__ResetStatics` deliberately does NOT re-seed them — §14.6.2.3.2 action 2 names
  only WS/LS); the `RecursiveContainedWs` staged loud widened to a FILE SECTION in a
  recursive-with-containees unit (same GLOBAL-bridge gap — an FD may be GLOBAL, §13.18.27).
  `2023/pb168_recursive_record_area` pins the ONE shared area (OUTER=D2 — the inner READ's record).
- The re-pinned `pb154_cancel_initial` comment overclaimed ("READABLE only because GR9's close") —
  CloseDisplaced flushes with the same observable effect; the comment now pins the OUTCOME (the write
  survives CANCEL on every host), not which same-effect mechanism delivered it.
- Killed 3-0: four CloseDisplaced concerns (sharing-table/registry-Close bypass), refuted against the
  actual sharing machinery.

MEASURED: `2023/pb168_recursive_file_connector` writes at depths 1 and 2 through ONE connector and reads
all three records back (the depth-2 write answered '42' pre-fix; CLI probe green). Wave gate GREEN
(Conformance 2471 with all three goldens by name · Unit 5027 · characterization 33 — the one interim red
was the diagnostics-doc drift guard, regenerated in-change; docs/COBOLNET_INTERPROGRAM_DESIGN.md's
recorded residue line for this exact defect closed with it). Battery #36 accrues (PB168). Registered
this session: [[PB175]] (the semgrep baseline drift the review's mechanical step exposed — PB168
contributes zero of it, proven by stash/verify/pop).
