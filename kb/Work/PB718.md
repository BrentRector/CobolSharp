---
title: "PB718 — 50 LETTERED sub-items are transcribed at column 0 in `specs/ISO_COBOL.md`, so `cite.py`'s `SUB` regex (which demands two leading spaces) never sees them and hands each one its PREVIOUS SIBLING's printed path: §7.2.3.4 a) and b) both resolve to `9) a)`, §7.2.4.4 b) resolves to `7) a) 2.` — PB698's family exactly one level up, and it moves no catalog row, so no drift test can see it"
id: PB718
kind: defect
status: open
severity: MINOR
area: spec/transcription
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: ["6.5", "7.2.3.4", "7.2.4.4", "7.3.14.4", "8.4.3.2.4", "9.3.6", "14.5.3.3", "14.9.10.4"]
inventory_rows: []
tags: [cobolsharp, work, defect, transcription]
---

# PB718 — OPEN — the page-break outdent, one level up: lettered items

Found 2026-09-06 by the PB697+PB698 implementer (report, "New defects", paragraph (a)) while repairing the
NUMBERED form of the same accident. Re-measured here on `main` at `c34f01ad` and re-confirmed at `f86a5764`
(a parallel lander pushed mid-session; `specs/`, `cite.py` and `extract_rule_catalog.py` are byte-identical
across the two). The numbers below are this tree's, not the report's.

⚠ **Where the repair's tooling lives.** The PB698 landing — the repaired transcription for the 23 NUMBERED
sites, the text-keyed migration batches, and `build_inventory.py`'s `rule-digest` carry guard — is on branch
`worktree-agent-a978d10810e78251a`, **not on main**. Everything measured in this note is main's state.

## The mechanism, read out of the code

`scripts/spec/cite.py` builds a printed path by walking backwards from the matched line and taking the first
line at each level (`rule_path`, `:61-72`). Its three level regexes are:

```
TOP    = re.compile(r"^(\d+)\\?\)\s")
SUB    = re.compile(r"^\s{2,}([a-z])\\?\)\s")
SUBSUB = re.compile(r"^\s{2,}(\d+)\\?\.\s")
```

`SUB` **requires two or more leading spaces**. A lettered item transcribed at column 0 therefore matches
nothing: the walk steps over it, `seen_sub` stays false, and the next indented `a)` it meets — the outdented
item's own preceding sibling — is recorded as that line's sub-item level. The reported path is the path of a
DIFFERENT rule, and it is reported as `OK`.

This is the same accident as [[PB698]] with the ordinal class changed. It is invisible to every existing check
for a reason that is mechanical, not accidental: the catalog's ordinal regexes are **digit-only** —
`extract_rule_catalog.py:102` `ORDINAL = ^(?P<n>\d+)\\?(?P<form>[.)])\s+` and `:110`
`INDENTED_ORDINAL = ^\s+(?P<n>\d+)\\?[.)]\s` — and `last_indented`, the state that decides sub-list
segmentation, is written only from `INDENTED_ORDINAL` (`:501`). A lettered line, indented or not, is body text
of the enclosing rule, and `flush()` joins body text with `" ".join(x.strip() …)`. **So no catalog row moves,
no rule text changes, and no `rule-digest` changes.** There is nothing for `extract_rule_catalog.py --check`,
the inventory drift suites or the digest guard to notice. The only artifact that is wrong is the one CLAUDE.md
rule 1 makes every citation pass through.

## The harm, measured — `cite.py --check` on this tree

Each of these is a real sentence of the standard resolving to a path the standard does not print for it:

| probe | `cite.py --check` answers | the printed path |
|---|---|---|
| `--check 7.2.3.4 "Pseudo-text-1 matches the library text only if the ordered sequence of text-words that forms pseudo-text-1"` | `OK §7.2.3.4 9) a)` | `9) b)` |
| `--check 7.2.3.4 "The leftmost library text-word that is not a separator comma or a separator semicolon is the first text-word"` | `OK §7.2.3.4 9) a)` | `9) a)` |
| `--check 6.5 "otherwise, the content of the program-text area, beginning with the first non-space character"` | `OK §6.5 6) a)` | `6) b)` |
| `--check 7.2.4.4 "A format 1 REPLACE statement without the ALSO phrase cancels the active REPLACE statement"` | `OK §7.2.4.4 7) a) 2.` | `7) b)` |
| `--check 7.3.14.4 "IO-STATUS-07: A CLOSE statement shall be flagged if it specifies either the WITH NO REWIND phrase"` | `OK §7.3.14.4 4) b)` | `4) c)` |
| `--check 8.4.3.2.4 "BY CONTENT is assumed when the BY REFERENCE phrase is specified or implied for the corresponding formal parameter"` | `OK §8.4.3.2.4 5) a)` | `5) b)` |
| `--check 14.9.10.4 "If multiple record locking is specified for the file connector associated with file-name-1"` | `OK §14.9.10.4 a) 1.` | `7) b)` |

⛔ **The first two lines are one path answering for two different rules** — the collision PB698 was opened for,
reproduced here at the lettered level. The §7.2.4.4 row is worse than an off-by-one: a lettered item is handed
a NUMBERED sub-sub-item's path (`7) a) 2.`), because the walk kept going past the outdented `b)` and picked up
a `2\.` belonging to a sibling's nested list. The §14.9.10.4 row loses the top-level ordinal entirely — that
clause is also a [[PB719]] site (its GR 1–7 use the `N.` form) and a PB698 site (`GR-14.9.10.4-L2.2` is the
flattened numeric tail in the same block), so its three defects are entangled and it should be repaired last,
not first.

## The population — 50 candidates, and the discriminator that ranks them

Detection reproduced with the report's rule ("a column-0 lettered item whose immediately preceding sibling
letter is indented"), scripted as
`E:\Temp\claude\E--CobolSharp\49b17747-ce58-4386-8c49-ecb2ce644f8d\scratchpad\pb718_detect.py`:

```
column-0 lettered items      : 572
CANDIDATES (sibling indented): 50
```

Two independent formulations of the filter — "scan back to the predecessor letter" and "the IMMEDIATELY
preceding lettered item is the predecessor letter" — return the **same 50 lines**, so the number is not an
artifact of one phrasing.

**The first five sites:**

| md line | clause | the outdented item |
|---|---|---|
| 2803 | §6.5 (Logical conversion) GR6 | `b) otherwise, the content of the program-text area, beginning with the first non-space character, …` |
| 3042 | §7.2.3.4 (General rules) GR9 | `b) Pseudo-text-1 matches the library text only if the ordered sequence of text-words …` |
| 3169 | §7.2.4.4 (General rules) GR7 | `b) A format 1 REPLACE statement without the ALSO phrase cancels the active REPLACE statement …` |
| 3814 | §7.3.14.4 (General rules) GR4 | `c) IO-STATUS-07: A CLOSE statement shall be flagged if it specifies either the WITH NO REWIND phrase …` |
| 6096 | §8.4.3.2.4 (General rules) GR5 | `b) BY CONTENT is assumed when the BY REFERENCE phrase is specified or implied …` |

The full 50: 2803 · 3042 · 3169 · 3814 · 6096 · 8013 · 8214 · 8677 · 8813 · 10641 · 10762 · 10802 · 11996 ·
12279 · 12959 · 14510 · 16517 · 17036 · 17344 · 17587 · 17707 · 17808 · 19335 · 19688 · 20198 · 20586 · 21884 ·
23167 · 23331 · 23894 · 24121 · 24259 · 24912 · 25082 · 25158 · 25198 · 26243 · 26514 · 26568 · 27758 · 27929 ·
28017 · 28931 · 28958 · 29326 · 30304 · 32646 · 33845 · 34104 · 43063.

**The page-break attribution is not a guess — it is measured.** Blank-line run immediately preceding the line
(`…\scratchpad\pb718_blanks.py`):

```
the 50 candidates            baseline: 794 correctly-indented lettered items
  1 blank : 12                 1 blank : 794
  2 blank : 18                 (nothing else occurs, at all)
  3 blank : 3
  4 blank : 13
  5 blank : 4
```

Every healthy lettered item in the whole standard has exactly one blank line before it. **38 of the 50 sit at a
multi-blank seam that occurs nowhere else in the population** — that seam is the transcription's page boundary,
and it is the page-break outdent the report named. The remaining **12 single-blank candidates are the triage
front**: they carry no page-break evidence and are the likeliest false positives (a letter that legitimately
opens a new level).

## Why it matters

No user program changes: nothing here reaches the compiler. What changes is the reliability of the ONE
mechanism CLAUDE.md rule 1 installs to stop a wrong clause number from propagating — *"⛔ VALIDATE EVERY
CITATION MECHANICALLY"*. `--check` answers `OK` for a wrong path, so a citation validated exactly as the rule
demands still ships the wrong printed path into a code comment, a golden, an adjudication and the DEVLOG. That
is `feedback_a_real_clause_can_answer_a_different_question` mechanized: the text is real, the clause path is
wrong, and the tool says OK. It is also `feedback_green_gates_arent_evidence` — the checker has never once been
shown to fire on this shape.

Severity is MINOR and `process_only` because it moves no verdict and no compiled behaviour. It is not
low-value: PB698's identical defect one level down produced two wrong CA10 citations.

## The fix shape

**PB698's method, per site — the printed page decides, never the ordinal arithmetic.** For each of the 50:

1. Locate the printed line on the canonical PDF (`python scripts/render-spec-page.py <page>`; the PDF is
   `specs-private/ISO+IEC+1989-2023_ for X_952804 COBOL.pdf`).
2. Read that page's **level ladder** — the x0 of the block's top-level ordinal, of its lettered items, of its
   numbered sub-items — and compare the candidate's **ordinal glyph x0** against it. ⛔ A modal-x0 "body
   margin" is unusable on a page that is mostly one nested list: PB698 measured it mislabelling 9 of 23 sites.
3. Repair only if the glyph is printed indented, taking the exact leading whitespace of its own preceding
   sibling. `specs/ISO_COBOL.md` is CRLF; byte-check after any scripted write (CRLF count, 0 lone LF, 0 NUL, no
   BOM) and keep the line COUNT unchanged — artifacts keyed to spec md-line numbers depend on it.

**Sequencing, and what the branch's tooling is for.** Because the lettered class touches no ordinal state, the
repair is expected to be catalog- and digest-NEUTRAL by construction. That expectation is a prediction, not a
measurement, so run it as PB698 ran its own: rebuild the catalog and the inventory before and after and diff
the id sets, under `build_inventory.py --check` with the `rule-digest` carry guard from branch
`worktree-agent-a978d10810e78251a`. If any id or digest does move at a site, that site is entangled with a
NUMBERED repair and inherits PB698's **text-keyed** migration obligation — a positional `L<sublist>.<n>` id
shifts when a sub-list changes, and six adjudicated rows kept their id while their text changed underneath
during PB698. **Land this behind that branch, not beside it.**

**Ship a check that fails first** (`feedback_prove_the_watchdog_fails`). The detection above belongs beside
`cite.py` as a `--check`-class gate over the transcription — assert the candidate count is zero after the
repair, and prove the gate fires by feeding it a fixture with one outdented lettered item. Without it, the
class silently regrows on the next transcription edit, exactly as the numbered class did.

## Sweep

Three artifacts of the same accident are now known; treat them as one family and finish it in order:

- **numbered tails at column 0** — [[PB698]], 23 sites, repaired on the branch;
- **lettered items at column 0** — this note, 50 candidates, main;
- **ordinal FORM drift (`N.` where the page prints `N)`)** — [[PB719]], §14.9.10.4, main.

One more shape surfaced while reproducing the detection and is **not** counted in the 50: **7 lines carry two
or more lettered ordinals run together on a single line** (md lines 10089 · 10665 · 13007 · 17499 · 20949 ·
26758 · 38666). Two, verified by `cite.py --check`:

- `--check 9.3.6 "is specified with the BY VALUE phrase"` → `OK §9.3.6 5) a)`, and the line it prints is
  `a) is specified with the BY VALUE phrase b) if the passed parameter is specified with the OMITTED
  phrase, …` — items a) and b) of §9.3.6 GR5 welded into one line. **This is why the report's count was 51
  where a sibling-based filter measures 50**: the `c)` and `d)` that follow at md 10668/10670 ARE outdented,
  but their `b)` sibling is buried mid-line, so no line-start filter can pair them.
- `--check 14.5.3.3 "any element that follows the exhaustion of the statement"` → `OK §14.5.3.3 1) a)`, whose
  line carries a), b) and c) together.

`cite.py` cannot address the second and later items on such a line at all — `rule_path` is line-keyed. Measure
all 7 on the PDF in the same pass; they are the same page-boundary damage, and §9.3.6 is already one of the 14
clauses PB698 repaired.
