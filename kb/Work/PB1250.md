---
title: "PB1250 — §13.7.3 SR5 has no check: a FUNCTION's formal parameter used as a RECEIVING operand (`ADD 1 TO P-X`, `MOVE 3 TO P-X(1:1)`) compiles clean"
id: PB1250
kind: defect
status: open
severity: MINOR
area: binding/call
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1249"]
spec_refs: ["13.7.3"]
inventory_rows: ["SR-13.7.3-5"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14d** from adjudication batch d1-14d (the adjudicators' pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probes under `scratchpad\adj-d1\out13\probe\`). The given repro was re-run once on registrar 14d's Debug build of `071ff04ea` (main after trains 58/59) and reproduces unless stated otherwise.

# PB1250 — OPEN

## Measured

**linkage-section** — §13.7.3 SR5 — a function's formal parameter used as a receiving operand — has no check: nothing keys receiving positions on (UnitIsFunction && operand is/under a LinkageFormal).

- **Repro:** out13/probe/linkage-section/sr5.cob --std 2023 --run: FUNCTION-ID LKDB5 ... LINKAGE 01 P-X PIC 9(4). 01 P-R PIC 9(5). PROCEDURE DIVISION USING P-X RETURNING P-R. ADD 1 TO P-X COMPUTE P-R = P-X * 2 GOBACK. -> compiles clean, prints R=00044 / R2=00012. sr5b.cob with MOVE 3 TO P-X(1:1) likewise clean (R=06042).
- **Expected (spec):** §13.7.3 SR5: 'A formal parameter of a function shall not be used as a receiving operand.' Both ADD 1 TO P-X and MOVE 3 TO P-X(1:1) are compile-time errors (2002+). The RETURNING item P-R is not a formal parameter and stays legal as a receiver.
- **Observed:** Clean compile and run. The only receiving-operand rail in the binder is COBOLNET1632 (§8.4.3.2.3 SR1, function-identifiers), which asks a different question.
- **Code site:** src/Cobol.Net.Compiler/Binding/DataBinder.Linkage.cs#CallBindLinkage (marks formals; UnitIsFunction) + the receiving-position classification shared by the statement binders (the COBOLNET1632 FunctionIdentifierReceiving rail is the precedent for a position-specific, not statement-specific, check)
- **Clause (adjudicator):** ISO/IEC 1989:2023 §13.7.3 SR5 (cite.py --check 13.7.3 "A formal parameter of a function shall not be used as a receiving operand" -> OK §13.7.3 5))

## The rules — every citation re-run through `cite.py --check` by registrar 14d

- `SR-13.7.3-5`: `cite.py --check 13.7.3 "A formal parameter of a function shall not be used as a receiving operand"` → OK  §13.7.3 5)  (Syntax rules)

## Rows claimed

- `SR-13.7.3-5` — batch 14d verdict DIVERGES

## What the fixer owes (sibling sweep)

Key on UnitIsFunction && operand is/under a LinkageFormal through the shared receiving-position classification (the COBOLNET1632 site), so every statement's receiving operands are covered at once.
