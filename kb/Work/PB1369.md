---
title: "PB1369 — the Annex A.1 determinations the §7 text-manipulation and directive rules require are missing or WRONG in `docs/CONFORMANCE.md`: the 'Compile-time arithmetic mode' bullet calls System.Decimal a 'standard' mode, says division truncates (System.Decimal division rounds its last digit), names a deleted evaluator (EvalConstExpr) and claims a closed GAP; the SR2 magnitude limit is reported as an SR1 b) violation; and there are no determinations for A.1 item 23 (characters prohibited in text-words), item 40 (locating library text), item 53 (>>DISPLAY conversion) or item 54 (compile-time-device-1)"
id: PB1369
kind: defect
status: open
severity: MINOR
area: docs
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
cluster: ["PB160", "PB1355", "PB1350", "PB807"]
spec_refs: ["7.3.6.2", "7.3.6.3", "7.3.12.4", "7.2.2.5", "7.2.3.4", "A.1"]
inventory_rows: ["GR-7.3.6.3-2", "SR-7.3.6.2-2", "GR-7.3.12.4-1", "GR-7.3.12.4-3", "GR-7.3.12.4-5"]
closes_rows: []
closes_rows_reason: "open — nothing landed yet"
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 15a** from adjudication batch 15a (subjects in §7.2 / §7.3, adjudicator pin `E:\CobolSharp-pins\adj-d2` @ `071ff04ea`; nothing under `src/` landed between the pin and `8bd4c7c9f`, so every measurement below stands). Probe programs: `E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d2\out\probe\<subject>\`.

# PB1369 — OPEN

PB160 owns the Annex A.1 register campaign; this note carries the §7 rows whose verdicts are PARTIAL only because the required documentation is absent or contradicts the code.

## The rule (every citation re-run by the registrar)

> The implementor shall define and document which mode of arithmetic is to be used when evaluating compile-time arithmetic. …

`python scripts/spec/cite.py --check 7.3.6.3 "The implementor shall define and document which mode of arithmetic is to be used when evaluating compile-time arithmetic."` → `OK  §7.3.6.3 2)  (General rules)`

> The implementor shall define and document any rules restricting the precision …

`python scripts/spec/cite.py --check 7.3.6.2 "The implementor shall define and document any rules restricting the precision"` → `OK  §7.3.6.2 2)  (Syntax rules)`

> Any conversion of data required is defined by the implementor …

`python scripts/spec/cite.py --check 7.3.12.4 "Any conversion of data required is defined by the implementor"` → `OK  §7.3.12.4 1)  (General rules)`

> data is transferred to the device defined by the implementor for receiving that data …

`python scripts/spec/cite.py --check 7.3.12.4 "data is transferred to the device defined by the implementor for receiving that data"` → `OK  §7.3.12.4 5)  (General rules)`

> The implementor may prohibit the use of one or more characters from outside the COBOL character repertoire …

`python scripts/spec/cite.py --check 7.2.2.5 "The implementor may prohibit the use of one or more characters from outside the COBOL character repertoire"` → `OK  §7.2.2.5 3)  (Text-words)`

## Finding — compile-time-arithmetic-expressions (silent)

The required §7.3.6.3 GR2 / §7.3.6.2 SR2 implementor documentation (docs/CONFORMANCE.md 'Compile-time arithmetic mode') does not describe the implemented mode: it calls System.Decimal a 'standard' mode chosen for portability, states that division truncates when System.Decimal division rounds its last digit, names a deleted evaluator (EvalConstExpr) and claims a directive-operand GAP that has been closed; and the evaluator reports the SR2 magnitude limit on an over-range expression operand as an SR1b violation.

**Rows:** GR-7.3.6.3-2, SR-7.3.6.2-2

**Repro:**

```
01 C7 CONSTANT AS 2 / 3 * 3.  01 C11 CONSTANT AS 100 / 7 * 7.  DISPLAY C7 C11 -> 2 and 100 (documented truncating division predicts 1 and 99). >>DEFINE V3 AS 2 / 3 * 3 / >>IF V3 = 2 selects. 01 K CONSTANT AS 1234567890123456789012345678901 + 0. -> COBOLNET1547 citing SR1b.
```

**Expected from the spec:** §7.3.6.3 GR2: 'The implementor shall define and document which mode of arithmetic is to be used'; §7.3.6.2 SR2: '... shall also document which intermediate rounding method is used' (both --check OK). The documented mode and rounding must be the implemented ones; the 'standard modes' of the GR2 NOTE are standard-binary/standard-decimal (§8.8.1.5.3, ISO/IEC 60559 decimal128), so System.Decimal must be documented as an implementor-unique mode. Either the doc states round-half-even division, or the evaluator implements the documented truncation -- an owner/implementor choice, but the two must agree; an over-range operand should be diagnosed as the documented SR2 limit.

**Observed:** Probe p1.cob/p2.cob outputs above; CONFORMANCE.md lines ~504-516 in the pinned tree; grep finds no EvalConstExpr symbol (only a comment in CompileTimeExpressionEvaluator.cs:97); ParseLiteral's TryParse failure message cites SR1b.

**Code site:** docs/CONFORMANCE.md (Compile-time arithmetic mode bullet); src/Cobol.Net.Frontend/Expressions/CompileTimeExpressionEvaluator.cs EvalArith ('/' case) and ParseLiteral

## Finding — text-manipulation-elements (silent)

F3 - Annex A.1 item 23 (characters prohibited from use in text-words in COPY ... REPLACING and REPLACE), required and documented, has no determination in docs/CONFORMANCE.md

**Rows:** GR-7.2.2.5-3

**Repro:**

```
grep docs/CONFORMANCE.md for DOC-A.1-23 / text-word: no row. The tokenizer prohibits no character.
```

**Expected from the spec:** §A.1 23): 'This item is required. This item shall be documented in the implementor's user documentation. (7.2.2.5, Text-words, rule 3)'. The determination (plausibly: no character of the compile-time coded character set is prohibited) must be written down.

**Observed:** No DOC-A.1-23 row exists; the implementation's actual behavior (no prohibition) is undocumented.

**Code site:** docs/CONFORMANCE.md (Annex A.1 determinations table); src/Cobol.Net.Frontend/Preprocessor/CopyProcessor.cs#TokenizeTextWords

## Row GR-7.3.12.4-1 — PARTIAL (adjudicator + refuter record)

cite.py --check 7.3.12.4 "Any conversion of data required is defined by the implementor" -> OK §7.3.12.4 1). GR1 transfers each operand to the source listing or compile-time-device-1. This compiler produces no source listing, so GR2 (OK §7.3.12.4 2)) makes the result implementor-defined; the directive is consumed with no transfer and no state (DirectiveStateRegistry: 'NONE HELD ... nothing persists past its own line'). Disposition: docs/CONFORMANCE.md (Recognized-and-ignored compiler directives, line 526) records that >>DISPLAY is recognized and consumed because no source listing and no compile-time device exist; the directive is edition-gated (dd1 at --std 2014 -> COBOLNET0900). The golden pb725_push_pop_display derives from 7.3.12.1 that >>DISPLAY contributes nothing to the run unit's output. Operand evaluation is not performed at all, so malformed operands are not diagnosed -- that is the FMT/SR2/SR3 defect owned by PB807, not a GR1 divergence. || REFUTED: PARTIAL — GR1 carries its own implementor-defined item, Annex A.1 item 53 (conversion of data transferred), which is REQUIRED and SHALL BE DOCUMENTED. The GR2 no-listing latitude covers the run-time behaviour (nothing transferred, verified), but docs/CONFORMANCE.md in the pin has no DOC-A.1-53 row (rows exist for 50, 56-59 only); the one sentence at line 526 says the directive is consumed and does not define the conversion. The cited golden pb725_push_pop_display pins only that the run unit prints nothing, which is the GR2 half, not GR1.

**Code location:** src/Cobol.Net.Frontend/Preprocessor/ConditionalCompilationProcessor.cs#CompilerDirectiveLine; src/Cobol.Net.Frontend/Preprocessor/DirectiveStateRegistry.cs#DisplayDirective2023; src/Cobol.Net.Editions/ConstructRegistry.g.cs#display-directive-2023

## Row GR-7.3.12.4-3 — PARTIAL (adjudicator + refuter record)

cite.py --check 7.3.12.4 "If no value is made available from the operating environment no transfer shall take place" -> OK §7.3.12.4 3). No transfer ever takes place under the GR2 no-listing disposition, so the no-value/no-transfer requirement is met and the implementor-defined value source is moot. Probe dd9 '>>DISPLAY PARAMETER XYZ' compiles clean. The PARAMETER phrase's own syntax is not checked -- owned by PB807 (FMT). || REFUTED: PARTIAL — The rule has two halves. The 'no value, no transfer' half is met vacuously, because nothing is ever transferred. The other half, the implementor-defined mechanism for obtaining compilation-variable-name-1's value from the operating environment, is Annex A.1 item 55, which is REQUIRED and SHALL BE DOCUMENTED. It is neither defined nor documented anywhere in the pin's docs (line 526 never mentions PARAMETER). A required, documented element that is missing makes this PARTIAL, not CONFORMS.

**Code location:** src/Cobol.Net.Frontend/Preprocessor/ConditionalCompilationProcessor.cs#CompilerDirectiveLine; src/Cobol.Net.Editions/ConstructRegistry.g.cs#display-directive-2023

## Row GR-7.3.12.4-5 — PARTIAL (adjudicator + refuter record)

cite.py --check 7.3.12.4 "data is transferred to the device defined by the implementor for receiving that data" -> OK §7.3.12.4 5) b). a) UPON LISTING: no listing device exists; b) compile-time-device-1: the implementor defines none (CONFORMANCE.md: 'transfers text to the (absent) source listing / compile-time device'). Both fall within the GR2 implementor-defined result. Probe dd9 '>>DISPLAY "A" UPON CONSOLE LISTING' compiles clean. Whether an undefined compile-time-device-1 name is diagnosed, and the §5.2.6.4 once-each rule (dd6), are syntax matters owned by PB807. || REFUTED: PARTIAL — GR5 b) sends data to 'the device defined by the implementor', and Annex A.1 item 54 (meaning of compile-time-device-1) is REQUIRED ('shall be provided by the implementor') and SHALL BE DOCUMENTED. The pin provides no such definition: CONFORMANCE.md:526 only calls the compile-time device '(absent)', and any word in that position is accepted silently. GR5 a) (LISTING) is discharged by GR2, but the b) half's required, documented item is missing. Two halves with one verified makes this PARTIAL.

**Code location:** src/Cobol.Net.Frontend/Preprocessor/ConditionalCompilationProcessor.cs#CompilerDirectiveLine; src/Cobol.Net.Editions/ConstructRegistry.g.cs#display-directive-2023

## What the fixer owes (and the sibling sweep)

Derive each determination from what the code DOES and write it (CONFORMANCE.md §7 rows, DOC-A.1 ids), correct the compile-time arithmetic bullet (System.Decimal = implementor mode; division rounding; current evaluator `CompileTimeExpressionEvaluator.EvalArith`), and make the over-magnitude operand diagnostic cite SR2. Run `scripts/spec/audit_annex_a1.py --check`.

**Rows claimed:** GR-7.3.6.3-2, SR-7.3.6.2-2, GR-7.3.12.4-1, GR-7.3.12.4-3, GR-7.3.12.4-5

## Further documentation defects the batch-15a adjudicators reported (same fill unit)

- `docs/CONFORMANCE.md` (~line 528) says the FLAG-02/FLAG-14 migration diagnostics 'are not yet emitted' — stale: `FlagConformancePass` emits COBOLNET1620/1621 (flag-02-directive, flag-14-directive).
- CONFORMANCE.md (~line 523) and `LeapSecondDirectiveProcessor`'s summary cite the never-report-second-60 determination (§7.3.17.4 GR2) as 'A.1 item 112'; the adjudicator reads it as item 111, which has no DOC-A.1-111 row — re-derive the item number with `cite.py --check A.1` before editing (leap-second-directive).
- A.1 item 117 (listings: whether and when produced) has no DOC-A.1-117 row; the no-listing choice lives only in the recognized-and-ignored-directives prose (listing-directive).
- DOC-A.1-158 calls main-source format auto-detection an 'implementor extension' without a standard-default selection — owned by PB1362.
- `specs/ISO_COBOL.md` §7.3.15.2's figure note contradicts itself (first paragraph: ON optional, omission means ON; second: 'The trailing ON/OFF braces are a separate, required one-of-two selection') — a transcription repair in the PB808 family; PB1374 must render the page before fixing.
