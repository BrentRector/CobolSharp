---
title: "PB137 — ✅ LANDED 2026-08-29 (the ONE cobolWord reservation predicate; BoundCommitRollback identity; SR1/SR2 live; the facility itself stays the documented A.3 non-support). WAS: the COMMIT/ROLLBACK facility's identity is erased at bind (BoundNop — indistinguishable from CONTINUE, so no SR can ever be enforced) and the facility word is ABSORBED by every open operand list at 2023 (`DISPLAY "OK" COMMIT` inside IF fails to compile with COBOLNET1639 where GR1 requires CONTINUE-equivalence); GateMergeInSortMergeProc implements exactly the SR2 ban with a MERGE-only predicate; the three EC-FLOW names are catalogued and never set"
id: PB137
kind: defect
status: landed
severity: MAJOR
area: binding
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB137

Rows: GR-14.9.7.4-1 (DIVERGES) through -5, SR-14.9.7.3-1/-2, findings [COMMIT statement].

(1) The absorption: COMMIT is a cobolWord alternative, so DISPLAY's operand loop, moveReceivingPhrase, addToPhrase and the SUBTRACT/MULTIPLY/DIVIDE twins greedily absorb a following bare COMMIT (PB45's own measured ANTLR rule; the {facilityWord}? predicate cannot rescue a non-left-edge decision), and ReferenceResolver.ReportUnidentified then errors — legal COBOL-2023 rejected; wave_h_facilities_inert.cob writes COMMIT only sentence-initial, the one position that cannot reach the defect (test-ref demoted). docs/CONFORMANCE.md §1's description is wrong in both directions and its EVALUATE claim is PB45-stale. (2) BindUnsupportedFacility returns a payload-free BoundNop — after binding a COMMIT is a CONTINUE, so §14.9.7.3 SR1/SR2 are unenforceable; an identity-bearing bound node is the shared blocker. (3) VersionConformancePass.GateMergeInSortMergeProc's Pass B predicate is `s is BoundMerge` — COMMIT (§14.9.7.3 SR2) and ROLLBACK (§14.9.36.3 SR2) are its unimplemented siblings. (4) EC-FLOW-COMMIT/-ROLLBACK/-APPLY-COMMIT registered, never set. (5) The APPLY COMMIT clause: refuter PROVED it is a LOUD syntax error (COMMIT is a hard token genericClause cannot span) — §4.2.6's named-warning obligation still unmet for the clause, but no silent acceptance; the CLOSE/DELETE lock-release guards stay vacuously true. Whether to IMPLEMENT the facility is the owner's (A.4.3); the absorption fix and bound identity are due regardless.

*(Found by Phase-B batch 8 — §8.8.1 + §14.9.1–14.9.12, 2026-08-29; one adjudicator + one independent refuter per subject. Full forensic evidence: the named rows' `notes` in `tests/version-matrix/traceability-inventory.json`; the raw finding lines in the session scratchpad `pb-batch8/findings.txt`.)*
