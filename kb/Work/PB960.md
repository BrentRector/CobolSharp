---
title: "PB960 — a RECOVERY `PicInfo`'s category is read as though it were an analysis, so every invalid PICTURE draws a real diagnostic AND a second, FALSE one calling the user's numeric item `of category alphanumeric`"
id: PB960
kind: defect
status: landed
severity: MINOR
area: binding/data-division
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
cluster: [PB453, PB959]
spec_refs: ["8.8.1.1", "13.18.40.3"]
inventory_rows: []
closes_rows: []
closes_rows_reason: "no inventory row: the defect was a false SECOND diagnostic on source that is already invalid (process_only); the witnesses are RecoveryCategoryCascadeTests and conformance:negative/pb960-invalid-picture-no-cascade"
tags: [cobolsharp, work, defect]
---

Filed 2026-09-22 by the ORCHESTRATOR from wave 44's group-D leads. **Re-measured here, not inherited** — and the
first attempt measured a THIRTEEN-DAY-STALE `cobol.exe` (built 2026-09-09 against src changed the same morning)
and printed nothing at all, which would have read as "does not reproduce". Rebuilt `CobolSharp.sln -c Debug`
(0 warnings, 0 errors) and re-ran; the transcript below is from the fresh binary.

## Reproduced

`<scratch>/pb960.cob`:

```cobol
       01 W-BAD PIC 9V9V9.
       01 W-N   PIC 9(4) VALUE 0.
       PROCEDURE DIVISION.
           ADD W-BAD TO W-N
```

`cobol pb960.cob --std 2023` → exit 70, TWO diagnostics:

```
(5,8): error COBOLNET1934: invalid PICTURE 9V9V9 — data item 'W-BAD': the symbol 'V' appears 2 times …
                            (ISO §13.18.40.3 SR12 b)                                    ← CORRECT
(8,12): error COBOLNET0844: item 'W-BAD' of category alphanumeric is not a numeric operand …
                            (ISO §8.8.1.1)                                              ← FALSE
```

The user wrote a NUMERIC picture. It is invalid, and COBOLNET1934 says so correctly. COBOLNET0844 then tells them
their item is **alphanumeric**, which is not true of anything they wrote — it is true only of the recovery object
the binder substituted after the first error.

## Mechanism

When `PictureAnalyzer` cannot analyse a picture it yields a RECOVERY `PicInfo` so that binding can continue and
report further real errors. `PicInfo.IsRecovery` exists precisely to say "my category is a placeholder, not an
analysis" — and only four sites in `DataBinder` ask it. Every other reader of `OperandCategory` treats the
recovery object's category as a measured fact, so the first invalid PICTURE in a program seeds a false category
that any later operand check can trip over.

⭐ **This is the same shape as the half PB453 fixed** in wave 44: `SetFormatSelection.KindOf` was one such reader,
and it was repaired by asking the USAGE rather than the recovered category. That fix was correct for its one site
and did not generalise — which is the `two_arm_dispatch` / `scan_all_similar` pattern this project keeps meeting,
seen from the far side: one arm was fixed, and the shape was left in place for every other reader.

## Harm

Not a wrong answer and not a rejection of legal source — the program is genuinely invalid. The harm is to the
DIAGNOSTIC, which is user-facing text that contradicts the user's own source and sends them looking at the wrong
thing. A commercial-quality compiler does not tell someone their `PIC 9V9V9` is alphanumeric.

## Fix shape (design, not yet implemented)

1. **Make the recovery category unaskable.** Reading `OperandCategory` on a recovery `PicInfo` should be
   impossible rather than merely discouraged — return an explicit `Unknown`/`Recovered` category that no rule
   table matches, so a reader must handle it, instead of a plausible-looking `Alphanumeric` that silently flows
   into §8.8.1.1's screen.
2. **Suppress the cascade at the source.** An operand whose `PicInfo.IsRecovery` is true has already produced a
   diagnostic at its declaration; downstream operand checks on that item should be skipped, not re-answered.
3. **Sweep every `OperandCategory` reader** (rule 4 — `src/Cobol.Net.Compiler/Binding/Model/PicInfo.cs#Recovery`
   names the four that ask `IsRecovery`; the sweep is the readers that do NOT), and pin the result with a drift
   test asserting no reader consumes a recovery category unguarded.
4. **A negative golden per arm**, asserting the real diagnostic is present and the false one is ABSENT — the
   absence is the assertion that matters, and it is the one a normal negative fixture would not make.

## Landed 2026-09-22 (wave 45, group F)

**The recovery value's category can no longer be read as an analysis.** `PicInfo.AnalyzedCategory` is `null` for a
recovery profile and `Category` otherwise; `Category` stays the STORAGE shape the doomed emit needs. The §8.5.2.1
readers a syntax-rule screen consults read the analyzed one and meet the nullable type: `IntrinsicArgumentRules.ClassOfItem`
(which every §8.8.1.1 / §15.3 / subscript screen reaches through `ClassOf` / `CandidateClasses` /
`IsArithmeticOperandClass`), `IntrinsicResultType.OperandCategory` (the relation / collating / intrinsic-type reader),
and `ExpressionBinder.ScreenResultant` (the arithmetic-resultant screen, which reads category directly). Each already
had a "not statically decidable → fail open" contract, so a recovered item is not re-diagnosed; the relation screen's
boolean arm (`StatementValidation.CheckRelationalOperands`) was the one reader whose null arm failed CLOSED and it now
fails open like the rest. The PB453 `SetFormatSelection.KindOf` fix stays as it is (it asks the usage, correctly).

Measured on a 30-statement probe (ADD / COMPUTE / resultant / REMAINDER / subscript / SQRT / SET / relation incl.
boolean / EVALUATE / MOVE to edited and boolean / STRING / INSPECT / UNSTRING / INITIALIZE / CALL / LENGTH / NUMVAL /
UPPER-CASE, plus a picture-less USAGE NATIONAL recovery): before, five screens re-diagnosed the item as alphanumeric
(COBOLNET0844 ×4, 1675, 1627, 0844-boolean); after, only the declarations' own COBOLNET1934 / 1658 / 0881 remain.
`RecoveryCategoryCascadeTests` is the drift test (one row per operand context, `Assert.Single`), and
`conformance:negative/pb960-invalid-picture-no-cascade` pins the true diagnostic.
