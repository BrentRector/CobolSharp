---
title: "PB181 — the CALL boundary reads a byte-form Tier-B window through the TEXT reader: CallStringRead→FieldImage→NonTextBytes decodes the window's radix-2 bytes into DISPLAY DIGITS at the wrong width, corrupting BY REFERENCE aliasing, BY CONTENT snapshots and copy-out over REDEFINES-class members"
id: PB181
kind: defect
status: landed
severity: MAJOR
area: codegen
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB181

Found by the Step D design scout (2026-08-30). CallEmitter.CallStringRead routes a class-window place through
OperandText.FieldImage whose NonTextBytes lane re-renders the byte window as display digits — the §14.2.3 GR8
storage-area aliasing then carries the WRONG bytes at the WRONG width across the boundary. The read twin of the
family PB177 catalogs (its B is the write half). FIX: the boundary read of a RedefViewPlace passes the window
verbatim (the storage-area rule — PlaceRenderer.Read IS the window); sweep the BY CONTENT/copy-out/RETURNING
call sites; golden: CALL BY REFERENCE with a REDEFINES-class argument round trip. Related: [[PB177]].

## ✅ LANDED 2026-08-30 — DEVLOG Entry 1404 (Step D order, step 2b)
MEASURED FIRST (the scout's claim and the PB176 skeptic's disagreed): the probe showed 1234 crossing
BY REFERENCE, ADD 1 in the callee, and 2594 coming home (ORD 50/51 — the display characters spliced
raw). The READ half was already correct — the elementary boundary convention carries the DISPLAY image
and the callee decodes it — the defect was the WRITE-BACK: `CallStringWrite` now decodes the returned
display image and re-encodes through the ONE byte-form recipe (the same MOVE/ACCEPT store shape) for an
image-stored / windowed numeric receiver. `2023/pb181_call_tier_b_window` pins the round trip (1235,
bytes 04 D3 via ORD, sibling intact). Wave gate GREEN (Conformance 1343 — the golden and the ACCEPT fact by name · Unit 5032 · characterization 33). Battery #37 accrues (R40 wave + PB179 + this pair).
