---
title: "PB1450 — §8.4.3.9.3 SR7 is never checked: nothing compares the GET property method's RETURNING description with the SET property method's USING description, so mismatched explicit accessors compile and the property temp (modelled on the GET item) is passed to a SET formal of a different description"
id: PB1450
kind: defect
status: open
severity: MINOR
area: binder/oo
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1425", "PB1448", "PB1449"]
spec_refs: ["8.4.3.9.3", "8.4.3.9.4"]
inventory_rows: ["SR-8.4.3.9.3-7", "SR-8.4.3.9.3-6", "GR-8.4.3.9.4-2"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 15c** from lane-3 adjudication batch **d2-15c** (adjudicated on the pinned tree `071ff04ea`; everything that landed since is registrar-only — nothing touches this mechanism unless the body says otherwise).

# PB1450 — OPEN

## The rule (every citation re-run by the registrar through `cite.py --check`)

> The data description of the item specified in the RETURNING phrase of the get property method shall be the same as the data description of the item specified as the USING parameter of the set property method

`python scripts/spec/cite.py --check 8.4.3.9.3 "…"` → `OK  §8.4.3.9.3 7)  (Syntax rules)`

## Finding — `object-property` (under-rejects; rows `SR-8.4.3.9.3-7`, `SR-8.4.3.9.3-6`, `GR-8.4.3.9.4-2`)

**Mechanism.** §8.4.3.9.3 SR7 is never checked: nothing compares the GET property method's RETURNING description with the SET property method's USING description, so mismatched explicit accessors compile and the property temp (modelled on the GET RETURNING item) is passed to a SET formal of a different description

**Repro.** pinned cobol.exe --std 2002 E:/Temp/claude/E--CobolSharp/a700c052-f9e4-4942-84ff-a13c7505d6ec/scratchpad/adj-d2/out/probe/objprop/p11.cob: class CPA11 with METHOD-ID. GET PROPERTY BAL (RETURNING LK-R PIC 9(5)) and METHOD-ID. SET PROPERTY BAL (USING LK-V PIC X(3)); driver `MOVE 5 TO BAL OF A. DISPLAY BAL OF A.` -> compiles clean, prints 00005

**Expected from the spec.** A compile-time diagnostic: SR7 'The data description of the item specified in the RETURNING phrase of the get property method shall be the same as the data description of the item specified as the USING parameter of the set property method.'

**Observed / reasoning.** OoClassTable checks only the §11.7.3 SR6/SR7 header shapes (GET: no USING + one RETURNING; SET: one USING + no RETURNING); no site compares the two descriptions, and ReferenceResolver's `model = get?.Binding!.Returning ?? set!...` silently picks the GET's.

**Code site.** src/Cobol.Net.Compiler/Oo/OoClassTable.cs#Build (accessor shape checks ~:355-:365); src/Cobol.Net.Compiler/Binding/ReferenceResolver.cs#OoTryBindPropertyReference (:140)

## What the fixer owes

Compare the two descriptions in OoClassTable.Build where both accessors of one property are known (reuse the §14.8.2 description comparator, not a new one), with a rule-specific diagnostic; then ReferenceResolver's `get?.Returning ?? set!…` choice is safe. Negative golden p11.
