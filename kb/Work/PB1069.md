---
title: "PB1069 — run-unit state is not scoped to the run unit: `RunUnit.ResetCurrent` resets only Programs (with External and Modules) and Random, so SWITCH settings, the LOCALE state and the report-flow state survive into the next run unit a host starts; and `CobolTable.Scratch<T>.Slot` is a PROCESS-static cell returned by ref, shared by concurrent run units on the EC-BOUND-SUBSCRIPT checking-off path"
id: PB1069
kind: defect
status: open
severity: MINOR
area: runtime/control
wrong_answer: true
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB307"]
spec_refs: ["14.6.1"]
inventory_rows: ["GR-14.6.11-3", "GR-14.6.11-4", "GR-14.6.11-5", "GR-14.6.11-6", "DOC-A.1-168"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-23 by **REGISTRAR #13** from `leads-w53-w57.md` and the train-57 lander report (w56c leads `wf_69a72ced-bc4` / `wf_ccbf74f1-936-3`: the PB307 mechanism on instance state). Code-read on
`c54434a8d`: `src/Cobol.Net.Runtime/Control/RunUnit.cs` `ResetCurrent` (lines ~131–136) against the instance members
`Switches` (:55), `ReportFlow` (:66), `Locale` (:74); `ProgramTable.Reset` covers External and Modules;
`src/Cobol.Net.Runtime/Values/Tables/CobolTable.cs` `Scratch<T>.Slot`.

# PB1069 — OPEN

## Harm
Only a host that runs several run units in one process sees it (the conformance test host does; an embedding host
may). A switch turned ON by run unit 1 reads ON in run unit 2; two run units on two threads that both take an
out-of-range subscript with checking off write the same scratch slot.

## What the fixer owes
Make 'a new run unit' mean a NEW `RunUnit` object (the `RunUnit.Run` shape) on the emitted driver path too, so no
field can be forgotten, and move the scratch slot onto the run unit (or `[ThreadStatic]`). Extend
`RunUnitStateDriftTests` to assert that every mutable member is fresh in the second run unit (reflection over the
members, not a hand list).

## Extended 2026-09-24 by REGISTRAR 14b (adjudication batch d1-14b, pinned @68bbdb00f)
Two execution-batch findings land here. (1) Every class's FACTORY object is a process-lifetime static singleton (`public static readonly <Factory> __Instance = new();`, OoEmitter), outside RunUnit.ResetCurrent, so instance objects, ALLOCATEd storage and dynamic tables/strings reachable from factory data are neither destroyed nor released at run-unit termination and stay visible to the next run unit in the same process (§14.6.11 rules 3, 4, 6). (2) Annex A.1 item 168 (whether the locale is reset at run-unit termination) has no determination row in CONFORMANCE.md §7 (catalog row DOC-A.1-168) — its answer follows from this note's LOCALE-state scoping fix and is owed with it (rule 5).
`cite.py --check 14.6.11 "All instance objects are destroyed"` → `OK  §14.6.11 4)  (Normal run unit termination)`

## Registrar-1 (kb/Work PB1522) — rows adjudicated on the `claude/adj-*` branches

Filed 2026-09-24 by REGISTRAR-1 (cloud, branch `claude/adj-registrar-1`). The adjudicators (tree `0caa7d5`) attributed these rows to this note; this note now claims them in `inventory_rows`.

- `DOC-A.1-168` — adjudicated **DIVERGES** on `claude/adj-w2-a1-s2`; NOT recorded — DOC rows wait on owner decision PB1535.

Adjudicator finding (`adj-w2-a1-s2`, harm silent): Run-unit termination does not reset the locale (RunUnit.ResetCurrent leaves RunUnit.Locale) and the A.1-168 answer is undocumented

- Repro: RunUnit.ResetCurrent resets Programs + Random only
- Expected: §14.6.11 GR5 'the implementor specifies whether the locale is reset to the locale in effect when the run unit was activated'
- Observed: Not a chosen answer; owed with the scoping fix
- Code site: src/Cobol.Net.Runtime/Control/RunUnit.cs#ResetCurrent

Each row's full evidence (rule text with its `cite.py --check` line, probes, determination) is the record's `notes` in `adjudication/registrar-1/batch-{doc,nondoc}.json` on the registrar branch.
