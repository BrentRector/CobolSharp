---
title: "PB1545 — the hexadecimal alphanumeric literal `X\"…\"` is accepted at `--std 85` with no diagnostic: no edition gate and no constructs.json row exist, while its national twin `NX\"…\"` IS gated at 85 (COBOLNET0900) — to be confirmed against the 1985 text, which has no hexadecimal literal"
id: PB1545
kind: defect
status: open
severity: MINOR
area: frontend/grammar
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1393"]
spec_refs: ["8.3.3.2"]
inventory_rows: []
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 23:55 UTC by the **doc-rows-1 INTEGRATOR** (cloud, branch `claude/adj-doc-rows-1`, kb/Work PB1522 step 3) from the §7 determination writers and refuters (`adjudication/doc-rows-1/`). Every citation below was re-run through `python scripts/spec/cite.py --check`, and the repro was re-probed on the Debug build of this branch.

# PB1545 — OPEN

## Finding

`X"4142"` in a VALUE clause and in a DISPLAY compiles and runs identically at `--std 85` and `--std 2002` (scratch `hx85.cob` →
`AB` / `CD` at both). `tests/version-matrix/constructs.json` has no row for the hexadecimal alphanumeric literal, and
`VersionConformancePass` gates only NATLIT/BOOLLIT (recorded as an open question in
`docs/rearchitecture/pb59-mechanism-map-2026-08-09.md`: "If §8.3.3.2 does introduce the hexadecimal-alphanumeric literal at a
specific edition, that is a separate finding about the EXISTING token") — it was never filed. ISO/IEC 1989:1985 defines no
hexadecimal literal (it arrived with ISO/IEC 1989:2002, where `NX"…"` is also gated today), so a program using `X"…"` at
`--std 85` is using a later edition's construct without the COBOLNET0900 every other such construct draws.

⚠ **Confirm before fixing** — `docs/VERSION_CHANGE_REFERENCE.md` covers only 2002→2023, so the 1985 status is not yet written down
anywhere in the repo. If it holds: add the constructs.json row (`introducedIn: 2002`), gate the HEXLIT token at the same site as
NATLIT, and run the edition-gate test sweep (`new-construct` skill) — every golden under `tests/conformance/85/` using `X"` must be
moved or re-derived.

## Sibling to check with it

The class condition `IS alphabet-name` is accepted at `--std 85` (refuter s6 observation) — check whether the 1985 class condition
admits an alphabet-name, and gate it the same way if not.
