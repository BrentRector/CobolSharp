---
title: "PB487 — `dataDescriptionClause` ends in `genericDataClause -> genericClause : IDENTIFIER (IDENTIFIER|literal)*`, a vendor-extension catch-all that swallows ANY word sequence at the end of a data description entry (one root, 2 findings across data-description-entry-p1, usage-clause-p3)"
id: PB487
kind: defect
status: landed
severity: MAJOR
area: frontend/grammar
wrong_answer: false
crashes: true
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.16.2", "13.16.3", "13.18.1.3", "13.18.1.4", "13.18.60.4", "8.5.1.6.3"]
inventory_rows: ["FMT-13.16.2", "SR-13.16.3-8", "SR-13.16.3-12", "SR-13.16.3-13", "SR-13.16.3-17", "SR-13.16.3-18", "FMT-13.18.1.2", "SR-13.18.1.3-1", "GR-13.18.1.4-1", "GR-13.18.1.4-2", "GR-13.18.1.4-3", "GR-13.18.60.4-9"]
tags: [cobolsharp, work, defect]
---

# PB487

## Mechanism — data-description-entry-p1
`dataDescriptionClause` ends in `genericDataClause -> genericClause : IDENTIFIER (IDENTIFIER|literal)*`, a vendor-extension catch-all that swallows ANY word sequence at the end of a data description entry. It makes the Format-1 clause list open-ended, and it is why the ALIGNED clause -- which has no grammar rule of its own anywhere in src/Cobol.Net.Frontend/Grammar -- is accepted and silently inert. Every '...the only other clauses permitted are...' rule of 13.16.3 is unenforceable against a word the parser does not recognize.

### Expected, from the standard
13.16.2 Format 1 is a CLOSED list of clauses, with `[ ALIGNED ]` among them; 13.18.1.4 GR1 requires an ALIGNED item to be aligned on the first bit of the first available BYTE, with implicit filler bits per 8.5.1.6.3 -- so `05 B1 PIC 1(4) BIT. 05 B2 PIC 1(4) BIT ALIGNED.` occupies TWO bytes, not one. 13.16.3 SR13 names ALIGNED's neighbours in the CONSTANT RECORD exclusion, and SR17/SR18 permit only level-number, entry-name, PICTURE, USAGE and VALUE beside ANY LENGTH / DYNAMIC LENGTH -- so an ALIGNED co-clause on those entries shall be diagnosed, and an unrecognized word shall be a syntax error.

```
(the finding quotes no rule sentence verbatim, so there is nothing to --check; the implementer MUST quote §13.16.2 and re-run `cite.py --check` before writing a code comment)
```

### Observed
ALIGNED is reserved from 2002 (ReservedWords.Table.cs:20) but has no lexer token and no parser rule, so it arrives as IDENTIFIER and genericDataClause eats it. FUNCTION LENGTH of the bit group is 1 with and without ALIGNED -- the clause is accepted and does nothing, which is the silent-wrong-layout case the design's loud-guard rule exists to prevent. The same catch-all accepts `WIBBLE WOBBLE`, so the permitted-set rules SR12/SR13/SR17/SR18 can never be complete however carefully their condition lists are maintained. Closing the Format-1 list (and giving ALIGNED its own rule and 13.18.1.3 SR1 check) resolves one FMT row and lifts four SR rows out of PARTIAL at once.

### Repro
cobol.exe E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/h_generic_junk/h_generic_junk.cob        -> exit 0 (also --std 85, probe h_generic_junk_85)
           01  A PIC X(3) WIBBLE WOBBLE.
cobol.exe E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/z_dl_aligned/z_dl_aligned.cob            -> exit 0   (01 A DYNAMIC LENGTH PIC X ALIGNED.)
cobol.exe E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/z_sameas_aligned/z_sameas_aligned.cob    -> exit 0   (01 A SAME AS Z ALIGNED.)
cobol.exe E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/z_cr_aligned/z_cr_aligned.cob            -> exit 0   (01 A CONSTANT RECORD ALIGNED.)
cobol.exe --run E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/r_aligned/r_aligned.cob  vs  r_noaligned.cob  -> both print LEN=1

### Code site
src/Cobol.Net.Frontend/Grammar/Core/CobolData.g4#dataDescriptionClause (line 290, `| genericDataClause`) and #genericDataClause (line 398); src/Cobol.Net.Frontend/Grammar/Core/CobolExpressions.g4#genericClause (line 15)

## Mechanism — usage-clause-p3
usage-p3/F3 — the data-description-entry parse silently DISCARDS an arbitrary run of unrecognized words between the data-name and the clause list, so any clause word the grammar does not know is treated as if it had not been written. Reached here through the COBOL-2023 MESSAGE-TAG usage, whose bare (USAGE-less) spelling 13.18.60.2 makes legal for every usage: `01 M MESSAGE-TAG.` compiles clean and binds an ordinary alphanumeric item. Where the swallowed entry is a GROUP header the program runs with a silently wrong data description; where it is ELEMENTARY the missing PicInfo escapes the binder and the compiler itself fails — an unhandled System.NullReferenceException in MoveEmitter, or a raw Roslyn CS0103 the user cannot act on. The same swallow accepts `01 M FOOBAR.`, `01 M FOOBAR PIC X(3).` and `01 M FOOBAR BAZQUX PIC X(3).`, so the blast radius is every misspelled or unimplemented clause word in the DATA DIVISION, not one usage.

### Expected, from the standard
13.18.60.4 GR9 (cite.py --check OK): "The MESSAGE-TAG clause specifies that a data item is a message-tag data item and contains an implementor-defined value that identifies a message and a server or requestor. The class and category of a message-tag data item are message-tag." MESSAGE-TAG is a COBOL-2023 addition (Annex E.2 item 25 adds it to the reserved words; cite.py --check E.2 OK), 13.18.60.2 prints `[ USAGE IS ]` as optional for every usage in the figure, and A.1 item 212 makes the size and contents a required documentation item. Since the usage is not implemented, the CONFORMING outcome for `01 M MESSAGE-TAG.` at --std 2023 is a named refusal in the 0899 band ("recognized but not yet implemented"), exactly as `01 M USAGE FUNCTION-POINTER.` gets; at --std 85/2002/2014 it is COBOLNET0900. Under NO reading may the entry bind as an alphanumeric item, and under no reading may a MOVE into it crash the compiler. Separately, an unrecognized word in a data description entry shall be a syntax error, never discarded.

```
$ python scripts/spec/cite.py --check 13.18.60.4 "The MESSAGE-TAG clause specifies that a data item is a message-tag data item and contains an implementor-defined value that identifies a mes..."
  OK  §13.18.60.4 9)  (General rules)
```

### Observed
Measured at --std 2023. The prefixed spelling IS refused, if bluntly: `01 M USAGE MESSAGE-TAG.` -> COBOL0001 "no viable alternative at input 'MESSAGE-TAG'" (q13). The bare spelling is not refused at all (q14/q76). The word is genuinely reserved (q74: `01 MESSAGE-TAG PIC X(4).` -> COBOLNET0901), so the funnel that should have named it exists and never sees this position. The generalized probes q77/q78/q79 prove the swallow is a property of the entry parse and not of this word. Mechanism by inspection: CobolData.g4's `dataDescriptionEntry : levelNumber dataName? dataDescriptionBody DOT` and `dataDescriptionClause` contain no alternative that admits a bare cobolWord, and MESSAGE-TAG has no lexer token, so the extra words are consumed by error recovery whose report never reaches the diagnostic stream. The binder even documents the gap it is left with (DataBinder.UsageDeclaration.cs:61: "The MESSAGE-TAG phrase is a 2023 addition ... and has no Usage member yet, so SR14's message-tag arm and SR21 ... are forward obligations").

### Repro
Three files, all under E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/usage-p3, all at --std 2023 with the pinned E:/CobolSharp/.claude/worktrees/lane3-pin-89044aa0/src/Cobol.Net.Cli/bin/Debug/net10.0/cobol.exe:
(a) q76_gr9_group_member... actually q76_gr9_bare_group.cob — `01 M MESSAGE-TAG.` + `05 S PIC X(3).` + `MOVE "ABC" TO S` + `DISPLAY FUNCTION BYTE-LENGTH(M)` -> compiles clean, prints S=ABC and BLM=3 (the MESSAGE-TAG word gone, no diagnostic).
(b) q29_gr9_bare_silent.cob — `01 M MESSAGE-TAG.` + `MOVE "ABCD" TO M` -> the COMPILER crashes: `Unhandled exception: System.NullReferenceException ... at CobolNet.CodeGen.MoveEmitter.ConvertSource ... MoveEmitter.cs:line 287`.
(c) q75_gr9_bare_what.cob — `01 M MESSAGE-TAG.` + `DISPLAY FUNCTION BYTE-LENGTH(M)` -> `error: backend compilation failed ... (43,54): error CS0103: The name 'M' does not exist in the current context`.
The general form: q77_bareword.cob / q78_bareword2.cob / q79_bareword3.cob all compile and run with an invented word (FOOBAR, FOOBAR BAZQUX) after the data-name.

### Code site
src/Cobol.Net.Frontend/Grammar/Core/CobolData.g4#dataDescriptionEntry / #dataDescriptionClause (the swallow) and #usageKeyword + src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4 (no MESSAGE_TAG token); src/Cobol.Net.Compiler/Binding/PictureAnalyzer.cs#ParseUsage (no arm); src/Cobol.Net.Compiler/CodeGen/Verbs/MoveEmitter.cs:287 (the NullReferenceException); the frontend's ANTLR error listener (the suppressed recovery report)

## Sibling sweep the fixer owes
Every bug here is a pattern (CLAUDE.md rule 4). Before closing this note, sweep the sibling arms named in the mechanism above and add the ones that fail to `inventory_rows`.

## Provenance
Lane-3 adjudication batch b3 (subjects: data-description-entry-p1, usage-clause-p3), adjudicated against the compiler pinned at 89044aa0 and RE-PROBED on the registrar's worktree cut from 6e7caa91 (a 1942-program sweep of `out-b3/probe/` plus targeted re-probes); the finding reproduces on today's tree.

### Re-probed by the registrar on this worktree
the swallow is confirmed by a second construct this batch measured independently: the §13.18.1 ALIGNED clause has NO grammar rule (`grep -rn ALIGNED src/Cobol.Net.Frontend/Grammar/` is empty) and `05 B2 PIC 1(3) USAGE BIT ALIGNED.` still compiles clean with rc=0 and no layout effect. REPRODUCES on this worktree.

## Repro programs
`E:\Temp\claude\E--CobolSharp\a9d94900-79fe-4a38-89f0-921976861543\scratchpad\repros\PB487\` — h_generic_junk, r_aligned, usage-p3, z_cr_aligned, z_dl_aligned, z_sameas_aligned. The implementer's opening move is to rerun them against a freshly built `cobol.exe`.

## Landed

**The root was ONE alternative, and closing it was half the work.** `dataDescriptionClause` ended in
`genericDataClause -> genericClause : IDENTIFIER (IDENTIFIER|literal)*`. The 13.16.2 Format-1 general format is a
CLOSED list of 21 optional clause slots -- RENDERED from the printed page (PDF p393 / printed folio 363; the
transcription matches it exactly, `[ ALIGNED ]` included) -- so that alternative admitted words 4.2.2 does not
admit and then DISCARDED them. All three harms in this note are that one harm:

* **ALIGNED** (13.18.1) is a clause of this very format and had NO lexer token and NO parser rule anywhere in the
  frontend, so it arrived as an IDENTIFIER and was eaten. It now has both. 13.18.1.4 GR1 and GR2 are honoured at
  the ONE bit-layout site, and 13.18.1.3 SR1 is adjudicated by a new post-forest pass.
* **The bare `01 M MESSAGE-TAG.`** that 13.18.60.2 makes legal (`[ USAGE IS ]` is optional for every usage) bound
  with no usage at all. MESSAGE-TAG is now a lexer token and a `usageKeyword` arm, refused BY NAME as the
  DECLINED Annex A.3 item 4 data surface (COBOLNET1943) -- it cannot be accept-inert like SEND/RECEIVE, because
  GR9 fixes the class AND category of a message-tag item, so an accepted one would bind as some other class.
* **Every "...the only other clauses permitted are..." rule of 13.16.3** was unenforceable against a word the
  parser never classified.

**The re-architecture the estimate did not contain (CLAUDE.md rule 5).** SR12, SR13, SR17 and SR18 are rules over
an entry's WHOLE clause list, and each was an `||` chain over whichever local decode flags its author remembered.
Each was incomplete, and not by carelessness -- a hand list cannot stay complete as a grammar grows. SR12 could
not see ALIGNED, DYNAMIC LENGTH, GROUP-USAGE, PROPERTY, SELECT WHEN or the validation clauses; SR13 listed five
of its EIGHT excluded clauses; SR17 was blind to nine of the fourteen it excludes, GLOBAL and PROPERTY among them
*because those clauses bind post-build and leave no decode flag to test*. They now read ONE clause-presence SET
(`DataClauseKind` / `DataDescriptionCst.WrittenClauses`, one bit per Format-1 slot, taken from the parse tree
because a syntax rule asks what was SPECIFIED), with the permitted/excluded sets as constants transcribed from
the rules' own sentences and the offending clause NAMED in each message. `DataClauseKindDriftTests` reflects over
the generated parser and fails in BOTH directions, so a clause added to the grammar cannot fall out of the rules
that govern its composition.

**A second, independent crash the catch-all was hiding.** The note attributed the unhandled
`NullReferenceException` at `MoveEmitter.cs:287` to MESSAGE-TAG. It is not MESSAGE-TAG's: `01 M.` followed by
`01 N PIC X(4).` and a `MOVE` -- plain COBOL, no exotic clause in it at all -- crashes identically, with no
diagnostic of any kind. 13.16.3 SR8's REQUIREMENT half ("For any other entry describing an elementary item, a
PICTURE clause shall be specified") was enforced only for USAGE NATIONAL and USAGE BIT, the two usages that had a
`PicPending` mark; every other picture-less elementary entry escaped with a null `PicInfo`. `CheckPictureRequired`
is the closing guard -- COBOLNET0881 over the conformance forest, a recovery `PicInfo` over the composition
forest, so "no elementary item reaches the emitter without a Pic" is an invariant rather than a hope. An
unexpanded TYPE / SAME AS reference is excluded by the rule's own words (it describes nothing); the first cut of
the guard rejected `tests/conformance/2002/typedef_nested_strong.cob` for exactly that.

**The two-arm defect, here three-armed.** 8.5.1.6.3's placement rule was written out THREE times --
`BitLayout.ExtentBits`, `BitLayout.StartBitWithin` and `DataBinder.AssignClassOffsets`, whose comment even said it
was "the SAME pair". Adding GR1's conjunct to some of them would have made an item's OFFSET disagree with its
group's EXTENT and with a REDEFINES view of it, silently. `BitLayout.SharesByteWith` is now the one predicate all
three call. GR2 forced a second split: the per-occurrence WIDTH and the subscript STRIDE were one number at six
call sites and differ for an ALIGNED table, so `BitLayout.StrideBits` is now THE stride and `RunBits` counts
`stride x (n-1) + width` -- never `stride x n`, because 8.5.1.6.3 generates trailing filler only "to advance
alignment ... for the NEXT item".

**Measured, before and after** (all six repro programs, on a build of this worktree):

| program | before | after |
|---|---|---|
| `h_generic_junk` (`01 A PIC X(3) WIBBLE WOBBLE.`) | rc 0, silent, at 2023 AND 85 | COBOLNET1941 naming WIBBLE |
| `r_aligned` (`--run`) | `LEN=1` -- the clause inert | `LEN=2`, the 13.18.1.4 GR1 value |
| `z_cr_aligned` / `z_dl_aligned` / `z_sameas_aligned` | rc 0, silent | COBOLNET1942 (+ 1563 / 1555) |
| `01 M MESSAGE-TAG.` + `MOVE` | compiler CRASH, unhandled NRE | COBOLNET1943 |
| `01 M FOOBAR PIC X(3).` | compiles, runs, prints `M=ABC` | COBOLNET1941 naming FOOBAR |
| `01 M.` + `MOVE` (NOT in the note) | compiler CRASH, unhandled NRE | COBOLNET0881 (13.16.3 SR8) |

**Codes:** COBOLNET1941 (unrecognized data clause), COBOLNET1942 (13.18.1.3 SR1), COBOLNET1943 (MESSAGE-TAG
declined). SR8 reuses the existing COBOLNET0881 usage-clause-compatibility band, whose picture-less-usage arm it
generalizes.

**Rows:** 12 closed, GAP 2726 -> 2714. The five 13.18.1 ALIGNED rows had NO verdict at all and are closed by the
same change; SR-13.16.3-8 was added to this note's `inventory_rows` by the sweep the note asked for.

**Not closed by this change, and deliberately so.** `genericClause` has four other call sites, each its own closed
general format: `genericFileDescriptionClause` (FD/SD), `fileControlClause` (SELECT), the SPECIAL-NAMES paragraph
and the IDENTIFICATION paragraph. All four swallow junk identically -- measured, see the report -- and each needs
its own spec audit of what the grammar does not yet model before its list can be closed without rejecting legal
source. They are separate items, recorded in `docs/rearchitecture/DESIGN-frontend-grammar.md` section 3.10.

### Finished 2026-09-09

The SPLIT checkpoint's four mechanical items, on a tree merged onto current main TWICE (first `e060c8a9`, then
`8aed2b67` after the PB258 golden round landed mid-work). Both merges were verified ROW BY ROW against the
three-way expectation, because a clean text merge of a 4348-row JSON is not evidence of a clean SEMANTIC one:
`scripts` cannot see a lost verdict, and the second merge did conflict, in `tests/conformance/2002/manifest.json`,
where both sides inserted at the same list position (resolved as the union).

1. **The seven negative goldens have their `.err` baselines.** Each is what the compiler PRINTS at EVERY edition
   its `*> reject-at:` header names, and only where that is the code the fixture's own header says it pins:
   COBOLNET1941 (`pb487-unrecognized-data-clause`, 85+), 1942 (`pb487-aligned-non-bit-subject`, 2002+), 1943
   (`pb487-message-tag-usage`, 2023), 0881 (`pb487-picture-required-elementary`, 85+), 1563
   (`pb487-aligned-co-clause-rules`, 2014+), 1549 (`pb487-constant-record-excluded-clauses`, 2014+), 1542
   (`pb487-any-length-excluded-clauses`, 2002+). One line, CRLF, no BOM, matching all 917 existing baselines.
   ⚠ The format pins ONE substring per fixture, and `pb487-aligned-co-clause-rules` legitimately raises three
   diagnostics: 1563 for entry A (SR18), 1555 for entry C (SR12) and 1942 for both. The baseline pins SR18's;
   SR12's band is pinned separately by `SameAsTests`, and SR18's by `DynamicLengthTests`, so neither rule rests
   on this one file.
2. **`Usage.MessageTag` has its drift-table rows** -- the two tables went red the moment the member landed, which
   is the drift tests doing their job. `NumericByteFormDriftTests` gets `(NumericByteForm.None,
   NumericTruncation.DigitCount)`: 13.18.60.4 GR9 makes the class and category of a message-tag data item
   message-tag, so the item is not of class numeric at all and `None` states that rather than defaulting to a lie.
   `IntrinsicArgumentClassDriftTests` gets `"category"`, with the DECLINE argued at the row -- ParseUsage refuses
   the usage BY NAME (COBOLNET1943, an Error, at every edition), so no CONFORMING program can present such an item
   to `ClassOfPlace` and a usage-keyed `CobolClass` arm would be an arm for a class no bound item can legally
   have; if the facility is ever implemented, GR9 requires a class of its own and that row changes with it. That
   test's own doc comment ("USAGE MESSAGE-TAG deliberately has NO Usage member") was stale the moment the member
   landed and now records what actually happened.
3. **A FIGURATIVE-constant VALUE is exempt from the SR8 DIAGNOSTIC, and from the diagnostic only.** SR8 defers to
   SR9 in its own words ("except as indicated in Syntax rule 9"), and SR9 keys the implied PICTURE on a literal
   WITH A LENGTH -- "the length of the literal as specified in 8.3.3, Literals" -- which a figurative constant has
   not got. That this compiler ACCEPTS the figurative case is already settled: the `>>FLAG-14
   VALUE-FIG-CON-LENGTH` option (7.3.15.4 GR4 k -- "A figurative constant specified in the VALUE clause of a data
   item with no specified length shall be flagged") FLAGS it, which presupposes acceptance, and
   `FlagDirectiveTests.Compile_ValueFigConLengthOn_*` pins that green. A closing guard must not silently
   RE-ADJUDICATE a settled reading, so `01 A VALUE SPACE.` (and the `ALL literal-1` form, a figurative constant by
   8.3.3.6) skips the DIAGNOSTIC loop while the recovery `PicInfo` still lands over the composition forest -- the
   crash this note closed stays closed either way. The predicate reads THE compiler's one figurative-word table
   (`FigurativeConstants.KindOf`), so it cannot drift from the emitter's.
   ⛔ **The exemption is NOT "any VALUE clause", and the brief's first formulation (`item.RawValue is null`) was
   MEASURED to be a wrong answer.** `01 B VALUE "AB".` is SR9's own case, and this compiler synthesizes the
   implied PICTURE NOWHERE -- that is kb/Work PB504, inventory row `SR-13.16.3-9` = NOT-IMPLEMENTED, an item
   already in the register. Exempting it bound B as the ONE-CHARACTER recovery item and `DISPLAY B` printed `A`;
   the national and boolean legs did the same, and `01 F VALUE 42.` (outside SR9 altogether) also went quiet.
   Loudly requiring a PICTURE that SR9 says may be omitted is debt PB504 owns and states; silently storing a
   truncated value is not, so the narrow carve-out is what landed. PB504's note records what PB487 changed for
   its repros (a named COBOLNET0881 where a raw Roslyn CS0103 used to leak) and that it stays open.
4. **The gate is GREEN and every attributed red is gone.** Filter
   `~Aligned|~Usage|~Typedef|~Bit|~Redefines|~VersionMatrix|~CorpusRunner|~Nist` (8 terms, all reported live):
   Conformance `Failed: 0, Passed: 4558`; Unit `Failed: 0, Passed: 23086`; Characterization `Failed: 0,
   Passed: 33`. The twelve reds the SPLIT report attributed -- 7 negatives without baselines, 3 drift-table rows,
   2 `FlagDirectiveTests` -- are all gone, and nothing new is red. The 12-row verdict batch re-verified on the
   merged tree unchanged (`rows changed: 0`), GAP 2707 (main) -> 2695 with PB487's twelve.
