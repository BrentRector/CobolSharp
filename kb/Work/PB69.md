---
title: "PB69 — LANDED 2026-08-18 — a saturated FromDouble sentinel consumed as an intrinsic ARGUMENT: MOD(A ** 3, B) answered from Int128.MaxValue"
id: PB69
kind: defect
status: landed
severity: MAJOR
area: runtime/intrinsics
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["15.64.4", "15.4.1", "8.8.1.2"]
tags: [cobolsharp, work, defect]
---

# PB69 — LANDED 2026-08-18 — the sentinel-as-argument residue of the PB13 saturation discipline

> ⚙ **flags:** wrong-answer, silent · found by the PB65/RV-15.64.4-1 landing's edge probe 2026-08-09;
> pre-existing (the old scale-9 arm saturated the same way at a lower threshold), NOT a regression.

**Measured repro** (`--std 2023`, probe `pb65b_edge.cob`, PB65 landing session): with
`A PIC S9(18) VALUE 999999999999999`, `B PIC S9(18) VALUE 1000000007`:
`COMPUTE R18 = FUNCTION MOD(A ** 3, B)` prints **639816141** where the owed value is **980012199**
(python: `999999999999999**3 % 1000000007`), silently — no SIZE ERROR, no EC.

**Mechanism.** `A ** 3` is 45 digits — past the Int128 exact window — so `PowNativeInt` takes its
documented double-approximation fallback (owner decision 2026-08-03: exact while it fits, the double
approximation past it, never a size error merely for outgrowing the carrier), and `FromDouble(9.99e44, 0)`
SATURATES to `Int128.MaxValue`. The PB13 saturation discipline is safe exactly when a receiver's capacity
check sits downstream ("the sentinel stays above the capacity check"); consumed as an intrinsic ARGUMENT
there is no capacity check, so `ModScaled(Int128.MaxValue, B)` answers from the sentinel. The same class
exists for ANY saturated Int128 carrier consumed at a value-semantics site rather than a store.

**Fix design question (record the alternatives, decide at fix time):**
1. Route the literal-exponent arm through `PowNativeIntDec` too (one arm, no sentinel — the overflow case
   carries the double approximation on the SDIDI, and `ModDec` computes an honest approximation). Cost:
   the hot `A ** 2` shape rides CobolDec, and ⚠ MEASURE FIRST whether `CobolDec.From(r, 0)` keeps a
   35–38-digit significand exact — if the SDIDI normalizes to §8.8.1.5.2's 32 digits, this arm LOSES
   exactness the Int128 lane has, and the split must stay.
2. Keep the split; make the FALLBACK Dec-carried by splitting at run time (an Int128-returning body cannot
   express it — needs the renderer to emit a Dec-typed call whose fast path is the exact integer).
3. Sweep the CLASS, not just `**`: enumerate every site where a saturated `FromDouble` result can reach a
   value-semantics consumer (intrinsic argument alignment, comparison operands, subscripts) — the PB13/PB5
   family's remaining quadrant.

### ✅ LANDED 2026-08-18 (DEVLOG 1301) — fix-design alternative #1 + #3, and the class swept

> **The decision:** ONE arm — `PowNativeIntDec` (`PowNativeInt` deleted); the SDIDI carries the exact Int128
> significand while the power fits (`CobolDec.From` keeps it — the ⚠ "measure first" concern is answered: no
> 34-digit rounding on construction) and the owner-decided double approximation past it. The class-sweep (#3):
> a NATIVE arithmetic operation with a Dec operand evaluates on the SDIDI (`CombineCore` — it truncated
> `2 ** -2 + 1` to 1 and did not compile `*`/`/` with a Dec); an intrinsic with a Dec RAW argument routes to its
> SDIDI body under native (`RenderNum`), with `ModDec`/`RemDec` gaining an exact-integer fast path (`MOD(A ** 2, B)`
> stays exact for a 38-digit power); `IntArg` lands at scale 0 from the RAW operand; every Dec→Int128 landing that
> cannot hold the value raises EC-SIZE-OVERFLOW (`CobolDec.ToUnscaledIntermediate`, A.1 item 179 — three places).
> Measured (golden `pb69_native_power_past_the_carrier`): T5/T6 TRUE/FALSE, T9 = 0, T10 = 999999999999998,
> T2/T4/T11 exact, T1 = T3 (the same SDIDI approximation for both spellings — the residue of a 15-significant-digit
> approximation of a 45-digit value mod 1e9 is below its granularity: 0; the exact 980012199 needs 45-digit
> arithmetic no native mode of this compiler has — an approximation of the expression, never a sentinel).
> **Found and fixed on the way: PB83** — `CobolDec.Div` capped its numerator pre-scale at 10^38 while subtracting
> the uncapped scale from the exponent (`100000 / D30` under STANDARD-DECIMAL was 0). Docs: COBOLNET_NUMERIC_DESIGN
> D19, CONFORMANCE.md A.1 179; unit `CobolDecDivisionTests`.

### The rows this note owns

> (none — no inventory row charges this shape; the RV-15.64.4-1 rows close on the PB65 landing, whose
> golden deliberately excludes the past-Int128 cube so the defect cannot be pinned as expected output)

> ⚙ **2026-08-09 PROBE SWEEP (wf_fdd1492c — probe + adversarial refute, every verdict a pasted CLI run + a --check'd citation; full per-row evidence/mechanisms/blockers frozen in `docs/rearchitecture/backlog-probe-sweep-2026-08-09.json`):** all 3 rows LIVE.
