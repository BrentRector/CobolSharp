---
title: "PB1079 — the EXTERNAL file-connector fingerprint (`OoEmitter.EmitExternalDescribes`' `SelectFingerprint`) omits §12.4.5.3 GR1 d) RESERVE, g) COLLATING SEQUENCE, j)/k) the key items' descriptions and relative locations and k)'s SUPPRESS WHEN, so a mismatch in any of them never raises EC-EXTERNAL-FILE-MISMATCH; its doc comment claims COLLATING SEQUENCE and RESERVE are 'identical by construction'"
id: PB1079
kind: defect
status: open
severity: MINOR
area: codegen/oo
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB643", "PB1072", "PB1074"]
spec_refs: ["12.4.5.3", "14.8.4.4"]
inventory_rows: ["GR-12.4.5.3-1"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 04:03 PDT by **REGISTRAR 14a** from adjudication batch d1-14a (pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probes under `scratchpad\adj-d1\out\probe\`). Not fixed by trains 58/59 (their src diff was checked).

# PB1079 — OPEN — the EXTERNAL file-connector fingerprint (`OoEmitter.EmitExternalDescribes`' `SelectFingerprint`) omits §12.4.5.3 GR1 d) RESERVE, g) COLLATING SEQUENCE, j)/k) th

## Rule text (every citation re-run by the registrar)
- `python scripts/spec/cite.py --check 12.4.5.3 "The same value for integer-1 in the RESERVE clause"` → `OK  §12.4.5.3 1) d)  (General rules)`
- `python scripts/spec/cite.py --check 12.4.5.3 "The same specification of COLLATING SEQUENCE clauses"` → `OK  §12.4.5.3 1) g)  (General rules)`

### Adjudicator finding (file-control-entry, harm under-rejects)
- **Mechanism:** external-entry-fingerprint-incomplete: OoEmitter.EmitExternalDescribes' SelectFingerprint omits GR1 d) RESERVE integer-1, g) COLLATING SEQUENCE clauses, j)/k) the record keys' data description entries and relative location, and k)'s SUPPRESS WHEN phrase (keys are compared by NAME only), and its doc comment wrongly claims COLLATING SEQUENCE and RESERVE are 'identical by construction'
- **Repro:** probe/fce/gr1.py: MAIN and a CALLed SUB, each `>>TURN EC-EXTERNAL-FILE-MISMATCH CHECKING ON`, `FD XF IS EXTERNAL`, indexed XF with RECORD KEY XK and ALTERNATE RECORD KEY XA WITH DUPLICATES; SUB's entry differs in ONE item: RESERVE 3 AREAS (gr1_d_reserve) / COLLATING SEQUENCE IS REV (gr1_g_collating) / XK moved to offset 2 (gr1_jk_key_location) / XK PIC X(6) instead of X(4) (gr1_jk_key_size) / SUPPRESS WHEN "XX" on XA (gr1_k_suppress). MAIN: CALL "GR1SUB" ON EXCEPTION DISPLAY FUNCTION EXCEPTION-STATUS.
- **Expected (spec):** §12.4.5.3 GR1: all file control entries referencing one external file connector 'shall have' d) 'The same value for integer-1 in the RESERVE clause', g) 'The same specification of COLLATING SEQUENCE clauses', j)/k) 'The same data description entry for data-name-2 ... as well as their relative location within the associated record ... the same SUPPRESS WHEN phrase'; with checking enabled each case must fail the CALL with EC-EXTERNAL-FILE-MISMATCH (§14.8.4.4), exactly as the fingerprinted items do.
- **Observed:** All five cases print SUB-RAN / NO-CONDITION; the control items a) OPTIONAL, b) ASSIGN, f) ACCESS, k) DUPLICATES and alternate-key count, l) SHARING, m) LOCK MODE each correctly print SUB=[EC-EXTERNAL-FILE-MISMATCH]. The fingerprint is `OPT|ASSIGN|USING|ORG|ACC|KEY=<name>|ALT=<name>:<dups>|SHARE|LOCK`; FileModel already carries FileLevelCollating/KeyLevelCollating and AlternateKeyClause.Suppress, and the key items' resolved DataItems carry size and offset. RESERVE has no carrier at all (open PB643/PB781).
- **Code site:** src/Cobol.Net.Compiler/CodeGen/Verbs/OoEmitter.cs#EmitExternalDescribes (fingerprint lines ~218-228; doc comment line ~192)
- **Clause (adjudicator):** 12.4.5.3 GR1

## What the fixer owes
Fingerprint every GR1 item from the bound FileModel (FileLevelCollating/KeyLevelCollating, AlternateKeyClause.Suppress, the key DataItems' size and offset); RESERVE needs its carrier first (PB643/PB781 — take them in the same fill). Correct the doc comment.

Rows claimed: GR-12.4.5.3-1. Flip `status` and write `closes_rows` in the landing commit (CLAUDE.md rule 8).
