---
title: "PB322 — six required Annex A.1 implementor determinations on the I-O and USE paths are neither made nor documented, and where the code has an implicit answer two hooks give opposite ones"
id: PB322
kind: defect
status: open
severity: MINOR
area: process/traceability
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.27.4", "14.9.49.4", "9.1.13.8", "14.7.9.3", "13.18.43.4"]
inventory_rows: ["GR-14.9.27.4-21", "GR-14.9.27.4-23", "GR-14.9.27.4-24", "GR-14.9.27.4-18", "GR-14.9.32.4-6", "GR-14.9.49.4-7", "GR-14.9.49.4-12", "DOC-A.1-76", "DOC-A.1-77", "DOC-A.1-130", "DOC-A.1-131", "DOC-A.1-103", "DOC-A.1-147"]
tags: [cobolsharp, work, defect]
---

# PB322 — the determinations the rules delegate, and nobody wrote down

**Rows:** `GR-14.9.27.4-21`, `GR-14.9.27.4-23`, `GR-14.9.27.4-24`, `GR-14.9.27.4-18`, `GR-14.9.32.4-6`,
`GR-14.9.49.4-7`, `GR-14.9.49.4-12`. It also explains the DOCUMENTATION half of `GR-14.9.30.4-16` (owned by
**PB329**).

⛔ **`GR-14.9.49.4-7` and `GR-14.9.49.4-12` BECAME THIS NOTE'S ROWS on 2026-09-13**, when PB367b landed the last
behavioural limit either of them carried (GR7 a) / GR12 a) — the SORT/MERGE-invoked USE — by modelling
§14.9.40.4 GR17 and witnessing it). What keeps both rows PARTIAL is now exactly determination **E** below:
GR7 c) and GR12 c) hand the action after a USE procedure with a fatal I-O status to the implementor, and this
implementation's answer is neither single nor written down. Nothing else is open on them; landing E closes both.

## Why one note

Each entry is an element the standard makes the IMPLEMENTOR's to decide and **required** to document, where
COBOL.NET has a de-facto behaviour, no §7 determination, and — in three cases — a written claim that contradicts
the code. That is one mechanism: a rule delegating to the implementor is discharged by writing the answer down
ONCE and implementing it, and none of these was.

## The determinations

**A. File sharing — the default mode when unspecified.** Annex A.1 item 77 and item 131 both make it required
and documented, citing §9.1.3, §9.1.15 and §14.9.27.4 GR23. Nothing appears in `docs/CONFORMANCE.md` outside the
A.4.7 *Claimed* row. The one place a default IS written down states a choice the code does not implement:
`docs/PHASE4_RECONCILIATION.md:1523` records *our documented choice = default sharing NO OTHER (exclusive,
record locks ignored per GR3)*, while `FileSupport.cs`'s own summary says the opposite (*the implementor default
... is outside the sharing subsystem entirely*). Observable: two plain connectors on one physical file both open
'00', and an OPEN OUTPUT over an open connector reports '30' — an OS sharing violation leaking into the I-O
status — where the PHASE4 choice makes both '61'. **PB321's fix cannot be written until this is settled.**

⛔ **`GR-14.9.27.4-23` is THIS note's row as of 2026-09-05** (it moved here from PB316). GR23 has three
sentences and PB316 landed the first two — the SHARING phrase now overrides only the file control entry of the
file-names in ITS OWN repeated group, and a group carrying no phrase keeps the file control entry's sharing
instead of a sibling group's override. The THIRD sentence is determination A above and is all that keeps the
row defective: no sharing mode is *established* for a connector with neither clause nor phrase — it is left
outside the sharing subsystem rather than placed in one of §9.1.15's three modes — so `OPEN INPUT` then
`OPEN OUTPUT` on one physical file answers '30' where every one of the three modes gives Table 19's '61'
(re-probed 2026-09-05). ⚠ Landing A must re-derive the first two lines of
`tests/conformance/2002/pb316_open_group_scope.out`: its retry-scope halves observe a connector with no
SHARING clause, so establishing a real default changes what that connector arbitrates against. The GR20
EQUALITY the golden asserts survives either way; the literal statuses do not.

**B. Which devices allow concurrent access** — A.1 items 52 and 76, the predicate §14.9.27.4 GR21's
*shareable* rests on. Absent.

**C. Deadlock detection and the retry parameters.** §9.1.13.8 item 2 (*the implementor shall specify under what
conditions a deadlock is detected*) plus §14.7.9.3 GR1/GR2 (the retry interval; m, n, the maximum meaningful
timeout; the retry-frequency technique). `OPEN ... RETRY FOREVER` / `FOR n SECONDS` answers '52', which
§9.1.13.8 item 2 licenses only against a SPECIFIED deadlock condition — and which re-routes the raised exception
from EC-I-O-FILE-SHARING to EC-I-O-RECORD-OPERATION under §9.1.13.1's first-digit map. Absent. (The
zero/negative arm of the same machinery is a behavioural defect: **PB346**.)

**D. OPEN OUTPUT and physical page boundaries** — A.1 item 130. §14.9.27.4 GR18's second half says *the
positioning of the output medium with respect to physical page boundaries is implementor-defined* (`--check
14.9.27.4` → **OK**). A behaviour exists (a LINAGE file gets `EvaluateLinage()` + `LinageCounter = 1` at OPEN
OUTPUT completion per §13.18.34 GR6b1/GR7d; a non-LINAGE file starts at stream offset 0) and is nowhere
DEFINED. GR18's first half — the file is created and contains no records — is verified and conformant.

**E. The action after a USE procedure when the I-O status is a fatal EC-I-O condition** — A.1 item 218, and this
one is worse than undocumented: **it is two different behaviours selected by a directive the rule does not
mention.** §14.9.49.4 GR7 c) and GR12 c) both key on the I-O STATUS VALUE — *the implementor determines what
action is taken as described in 9.1.13* (`--check 14.9.49.4` → **OK**) — not on whether checking is enabled.
Measured on the same status '35' and the same declarative shape: with no `>>TURN`, execution continues and
`AFTER-OPEN FS=35` prints (`probe/gr7-handler-end/p2.cob`); with `>>TURN EC-I-O CHECKING ON`, the run unit
terminates abnormally (`probe/gr3-gr5-tiers/p1.cob`). The two branches live in different generated hooks —
`EcEmitter.cs#EmitIoCheckEc` ends with `if (__en && ExceptionCatalog.IsFatalIoStatus(__st)) throw new
CobolFatalException(...)`, while the plain `__IoCheck` (`DispatchEmitter.cs#EmitUseMachinery`) has no fatal arm
at all. `docs/CONFORMANCE.md` carries no A.1 item 218 row.

**F. The implied RECORD clause of a sort-merge description entry.** With no RECORD clause the implicit clause is
implementor-defined (§13.18.43.4 GR5), and if format 1 is implied the size is *the record size of the largest
record description entry in this file description entry* (`--check 13.18.43.4` → **OK**) — the fixed-length sort
file §14.9.40 GR7 presupposes. `SortBinder.BindRelease` instead releases at record-name-1's OWN description size
(`SortBinder.cs:238-240` + `BoundSort.cs:71`), so an SD with `01 SRT-SHORT PIC X(4)` and `01 SRT-LONG PIC X(10)`
and no RECORD clause holds a 4-byte and a 10-byte record for ONE SD, and `SORT ... GIVING` writes a 4-character
record into a `PIC X(10)` FD record (`probe/ref-gr6/w.cob`). That MAY be legitimate latitude — GR5 also lets the
implementor imply format 2, under which 4 bytes is in range and §14.9.32.4 GR6 is vacuous — but the choice is
nowhere DECLARED, and the two comments defending it cite §14.9.40 GR7, a rule about file-name-2 (the USING
file), not about a RELEASE in an input procedure. That mis-citation is **PB320** item 6.

## To close

Settle A as one determination (it gates PB321), then write A-F into `docs/CONFORMANCE.md` §3/§7 in the same
change set, with both USE hooks reading ONE E rather than two independently-evolved defaults, and a witness per
determination so §7 and the code cannot drift apart again.

## Stale row notes 2026-09-05 (PB346 implementer)

The PB322 inventory row still says "DIVERGENCE 2 — the SECONDS and FOREVER arms answer 52 where the conflict is a FILE SHARING conflict … RetryLoop re-checks once and returns FileStatusCode.Deadlock". That divergence was fixed by 0b1c39bc (PB142 cluster E) and `2023/pb142_retry_conflict_class_ec` pins the correction; PB346 measured every RETRY form landing 51 on a record conflict and 62 on a file-sharing one. Re-verdict the row through a batch when this note is next touched — the notes field is stale, not the code.

## Registrar-1 (kb/Work PB1522) — rows adjudicated on the `claude/adj-*` branches

Filed 2026-09-24 by REGISTRAR-1 (cloud, branch `claude/adj-registrar-1`). The adjudicators (tree `0caa7d5`) attributed these rows to this note; this note now claims them in `inventory_rows`.

- `DOC-A.1-52` — adjudicated **DIVERGES** on `claude/adj-a1-w1-s2`; NOT recorded — DOC rows wait on owner decision PB1535.
- `DOC-A.1-76` — adjudicated **DIVERGES** on `claude/adj-a1-w1-s3`; NOT recorded — DOC rows wait on owner decision PB1535.
- `DOC-A.1-77` — adjudicated **DIVERGES** on `claude/adj-a1-w1-s3`; NOT recorded — DOC rows wait on owner decision PB1535.
- `DOC-A.1-130` — adjudicated **DIVERGES** on `claude/adj-w2-a1-s1`; NOT recorded — DOC rows wait on owner decision PB1535.
- `DOC-A.1-131` — adjudicated **DIVERGES** on `claude/adj-w2-a1-s1`; NOT recorded — DOC rows wait on owner decision PB1535.
- `DOC-A.1-218` — adjudicated **DIVERGES** on `claude/adj-w2-a1-s3`; NOT recorded — DOC rows wait on owner decision PB1535.

Adjudicator finding (`adj-a1-w1-s2`, harm silent): a1-register-rows-missing-for-determinations-the-code-makes

- Repro: grep '^| DOC-A.1-(32|40|42|43|44|46|51|52) |' docs/CONFORMANCE.md -> none; python scripts/spec/audit_annex_a1.py lists them undischarged
- Expected: Each item: 'This item is required/conditionally required ... shall be documented in the implementor's user documentation' (§A.1, cite.py OK per row)
- Observed: The code fixes each answer (FillChar values; FindCopybook search order; ORDER TABLE literal-9 set - documented but under §2 row 25, not the §7 key; ToUpperInvariant currency equivalence; empty extra-prohibition set; per-usage representations with 47/74/81/213/217 unregistered; DELETE FILE = one host unlink; every host path treated as concurrently accessible).
- Code site: docs/CONFORMANCE.md §7

Adjudicator finding (`adj-w2-a1-s3`, harm silent): A fixed implementor determination on a REQUIRED + documented A.1 item has no docs/CONFORMANCE.md §7 row (and no prose answer, or a contradicting one)

- Repro: 186: ALPHABET AL IS "Z" "Y" "X"; SYMBOLIC CHARACTERS S4 IS 4 IN AL -> U+0000. 203: PCS=EBCDIC, 88 VALUE "Z" THRU "a" excludes '_' (native includes it). 217: BYTE-LENGTH(PROGRAM-POINTER)=8, non-COBOL target -> NULL. 218: USE AFTER ERROR on status 35 continues; with >>TURN EC-I-O CHECKING ON the run unit terminates.
- Expected: §12.3.7.4 GR7 k4 / §14.7.8 r2 / §13.18.60.4 GR24 / §14.9.49.4 GR7c+GR12c: the implementor defines AND (A.1 186/203/217/218) documents the choice
- Observed: Each behaviour is deterministic in code but no §7 row states it; 186 is contradicted by §2 row 27; 218 is additionally bifurcated on checking state, which the rule does not key on
- Code site: CollatingModel.cs#CodedCharacterSet.CharAt; CollatingModel.cs#CollatingSelection.ForComparison; Runtime/Control/ProgramPointer.cs; EcEmitter.cs#EmitIoCheckEc vs DispatchEmitter.cs#EmitUseMachinery

Adjudicator finding (`adj-a1-w1-s3`, harm silent): Required Annex A.1 determination missing from docs/CONFORMANCE.md while the code fixes a concrete behavior (no '| DOC-A.1-N |' row, no statement)

- Repro: >>DISPLAY "X" 42 / >>DISPLAY "X" UPON LISTING / >>DISPLAY PARAMETER XYZ compile clean at --std 2023 and transfer nothing; PROGRAM-ID. sub-p. registers externalized name 'sub-p', CALL "SUB-P" resolves case-insensitively; three clause-less SELECTs on one path: OPEN INPUT/INPUT/I-O all '00' (ALL-OTHER-like default); every host file arbitrated as shareable
- Expected: §7.3.12.4 GR1/GR3/GR5 b), §8.3.2.2 2) a)/§7.3.9.3 GR2 b), §9.1.15 (default sharing mode; devices allowing concurrent access) each make the answer implementor-defined and Annex A.1 items 53/54/55/68/76/77 require it documented
- Observed: No DOC-A.1-53/54/55/68/76/77 rows; FileRegistry.ImplementorDefaultSharing is null ('UNDETERMINED') yet the arbitration quantifier yields a fixed permissive answer
- Code site: src/Cobol.Net.Frontend/Preprocessor/DirectiveStateRegistry.cs; src/Cobol.Net.Runtime/Control/ProgramTable.cs#_byPath; src/Cobol.Net.Runtime/IO/FileRegistry.cs#ImplementorDefaultSharing

Adjudicator finding (`adj-w2-a1-s1`, harm silent): Annex A.1 determinations that the code already fixes are written nowhere under their DOC-A.1-<n> key (no §7 row; at most design-doc or code-comment prose), so the required user documentation is absent and audit_annex_a1.py counts the item open

- Repro: grep '^| DOC-A.1-<n> ' docs/CONFORMANCE.md for each id -> nothing. Behaviour probes (--std 2023): ls1.cob writes 'AB\r\n' (114: CRLF written; ls2.cob shows LF, lone CR, CRLF and EOF all delimit on READ); nx2.cob NX"414" -> COBOLNET1635 '4-digit groups' (122); int2.cob subscript I/2 with I=5 under >>TURN EC-BOUND -> 'EC-BOUND-SUBSCRIPT ... 2.500000000 is not an integer', unchecked it silently selects element 2, COMP-2 2.0 accepted (124); pg.cob OPEN OUTPUT -> empty file, LINAGE-COUNTER 1, TOP margin at first WRITE, first ADVANCING PAGE emits \f (130); sh.cob three clause-less SELECTs on one path all open 00 in INPUT/INPUT/I-O and I-O/I-O (131). 116/141: ProgramRegistry.CallProgram(CobolArg[]) is the non-COBOL host ABI, undocumented beyond the RETURN half; 120: LocaleState reads CurrentCulture once (DESIGN-locale-facility L3), not in CONFORMANCE.md; 121: __GET_<P>/__SET_<P> only in NamingConvention.
- Expected: Each item's A.1 sentence: 'This item shall be documented in the implementor's user documentation' (cite.py --check A.1 OK for each), with the operative rules §12.4.5.10.3 GR2, §13.7.4 GR3, §8.2.1, §11.7.4 GR1 a), §8.3.3.5.3 SR5, §5.5 3) a), §14.9.27.4 GR18 and GR23, §14.2.3 GR13 (all --check OK). §4.2.5 requires every such element to be documented.
- Observed: The behaviour is settled in code (and for 120 in the locale design doc) but the user documentation - docs/CONFORMANCE.md §7 - carries no row under the item's key, so a user cannot learn the implementor's choice and the traceability anchor docs/CONFORMANCE.md#DOC-A.1-<n> does not resolve.
- Code site: docs/CONFORMANCE.md §7; src/Cobol.Net.Runtime/IO/SequentialConnector.cs#OpenWriter (114, 130); src/Cobol.Net.Runtime/Control/ProgramRegistry.cs#CallProgram (116, 141); src/Cobol.Net.Runtime/Control/LocaleState.cs#LocaleState (120); src/Cobol.Net.Compiler/Oo/NamingConvention.cs#GetAccessorName (121); src/Cobol.Net.Frontend/Common/CobolLiteral.cs#HexGroupViolation (122); src/Cobol.Net.Runtime/Values/Numeric/CobolNum.cs#HasFraction (124); src/Cobol.Net.Runtime/IO/FileRegistry.cs#ImplementorDefaultSharing (131)

Each row's full evidence (rule text with its `cite.py --check` line, probes, determination) is the record's `notes` in `adjudication/registrar-1/batch-{doc,nondoc}.json` on the registrar branch.

## PB1522 step 3 (doc-rows-1 integrator, 2026-09-24 23:55 UTC, branch `claude/adj-doc-rows-1`)

**E, extended — A.1 item 103.** A fatal I-O status with NO FILE STATUS item and NO applicable USE procedure continues the run unit when EC-I-O checking is off (re-probed `/tmp/adj/doc/doc-w1-s4/refute-probe/r103.cob` → `AFTER OPEN` / `AFTER READ`, exit 0). §14.6.13.1.3 leaves that latitude to the implementor, and CLAUDE.md rule 1's precedent — GnuCOBOL 3.2 — TERMINATES: `cobc/codegen.c` 11091-11092 emits `if (!(… & COB_SELECT_FILE_STATUS)) cob_fatal_error (COB_FERROR_FILE);` in the default error handler. No owner decision records a deviation (COBOLNET_DESIGN item 14 itself recommended terminate). DOC-A.1-103 was therefore OMITTED from §7 and is claimed here (PB1512, the host-failure mapping, also claims it). The `FileStatus.LineRecordInvalidChar` ('71') summary, which claimed termination, now states the current behaviour and points here.

**F — A.1 item 147** (the implied RECORD clause of a sort-merge file) was OMITTED from §7 on this note's F defects; claimed here. Items 77, 131 (A) and 218 (E) stay OMITTED and claimed.

## doc-rows-2 landing (2026-09-25)

**DOC-A.1-52** is CONFORMS (all devices allow concurrent access, the item-76 determination) and is released.
**Determination E is DOCUMENTATION ONLY, not a code defect** (doc-rows-2 refuter of item 218): §14.6.13.1.3
rule 3 routes a fatal EC-I-O condition through §9.1.13 and then, absent an implementor override, through rules
4–8, which key on the checking state — so the checked/unchecked split E called 'two behaviours selected by a
directive the rule does not mention' is the standard's own. DOC-A.1-218 now documents it (CONFORMS; test-needed:
the checked arm has no witness) and is released. **E-extended (A.1 item 103) stays here**: a fatal status with no
FILE STATUS and no USE procedure must TERMINATE (DOC-A.1-103, GnuCOBOL codegen.c:11091), and today continues.
**Leads for determination A (items 77, 131)**, all consequences of the landed rows, owed in A's landing:
DOC-A.1-153's example (two clause-less connectors open together, B READs and REWRITEs) is what the code does
TODAY and becomes impossible under item 131 (a second clause-less I-O opener gets '61') — re-derive it with the
golden `2002/pb669_lock_visibility_plain_connector`; golden `2002/pb316_open_group_scope`'s first two lines move;
docs/PHASE4_RECONCILIATION.md:1523 ('default sharing NO OTHER') becomes the INPUT-excepted form;
`FileRegistry.ImplementorDefaultSharing`'s summary, FileLockPosture's 'owner-facing question' remark and
docs/COBOLNET_FILES_DESIGN.md (~l.701 'UNDETERMINED') follow DOC-A.1-77 / -131.
