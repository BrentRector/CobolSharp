---
title: "PB1237 — an FD whose file-name has NO file control entry is silently given a synthesized FileModel with an empty assignment (DataBinder.BindFileSection: 'FD with no matching SELECT — keep a model … it is never opened'); no diagnostic, and I-O verbs on it compile and run"
id: PB1237
kind: defect
status: open
severity: MINOR
area: binding/io
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1238", "PB1077"]
spec_refs: ["13.4.5.3"]
inventory_rows: ["SR-13.4.5.3-1"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14d** from adjudication batch d1-14d (the adjudicators' pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probes under `scratchpad\adj-d1\out13\probe\`). The given repro was re-run once on registrar 14d's Debug build of `071ff04ea` (main after trains 58/59) and reproduces unless stated otherwise.

# PB1237 — OPEN

## Measured

**file-description-entry** — An FD whose file-name has no file control entry is silently given a synthesized FileModel with an empty assignment (DataBinder.BindFileSection 'FD with no matching SELECT — keep a model ... it is never opened'); no diagnostic is issued and I-O verbs on it compile and run against an empty ASSIGN.

- **Repro:** probe/fde/sr1_noselect_use/sr1_noselect_use.cob: FILE-CONTROL has only SELECT G ASSIGN "g.txt"; FILE SECTION has FD F. 01 R PIC X(10). and FD G.; PROCEDURE: OPEN OUTPUT F MOVE "X" TO R WRITE R CLOSE F DISPLAY "OK". -> compiles clean, prints OK; .g.cs: CobolFile.Register("SR1U::F", "", 10, ...).
- **Expected (spec):** §13.4.5.3 SR1 'File-name-1 shall be specified in a file control entry.' — a syntax-rule violation, so the compilation group is non-conforming and the compiler shall reject it (compile-time error) at every edition. Sibling: §13.4.6.3 SR1 states the same for the SD, whose arm in BindFileSection has the same silent fallback.
- **Observed:** Compiles and runs with no diagnostic at --std 2023; the fallback arm has no edition gate, so all four editions. The comment's premise ('it is never opened') is false — OPEN/WRITE/CLOSE reach a connector registered with an empty file name.
- **Code site:** src/Cobol.Net.Compiler/Binding/DataBinder.cs#BindFileSection (the !FilesByName.TryGetValue arm; the SD loop's twin arm)
- **Clause (adjudicator):** 13.4.5.3 SR1

## The rules — every citation re-run through `cite.py --check` by registrar 14d

- `SR-13.4.5.3-1`: `cite.py --check 13.4.5.3 "File-name-1 shall be specified in a file control entry"` → OK  §13.4.5.3 1)  (Syntax rules)

## Rows claimed

- `SR-13.4.5.3-1` — batch 14d verdict DIVERGES

## What the fixer owes (sibling sweep)

The SD loop's twin arm (§13.5 sort-merge file description). PB1077 (registrar 14a) is the mirror image — a SELECT with no FD — in BindFileControl: one pairing check for both directions.
