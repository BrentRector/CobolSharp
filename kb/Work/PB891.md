---
title: "PB891 — the AMBIENT exception-checking flags are SET and RESET, never SAVED and RESTORED: every `EcEmitter` gate emits `ExceptionState.<Flag> = true; try { … } finally { <Flag> = false; }`, so a statement executed INSIDE another statement's guard clears the enclosing statement's enable on the way out and an unguarded inner statement inherits the outer's `true` — against §14.6.13.1.1's 'if checking … is not enabled, the exception condition will not be raised' in both directions, and the correct save-restore shape (`PushAllCheckingOff`/`PopAllChecking`) is in the SAME FILE at `:637`"
id: PB891
kind: defect
status: landed
severity: MAJOR
area: codegen/exceptions
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: [PB841, PB892]
spec_refs: ["14.6.13.1.1", "14.9.28.4"]
inventory_rows: []
closes_rows: []
closes_rows_reason: "no inventory row names the run-time checking scope: its governing rules GR-7.3.25.4-5/-6/-8 are GAP and unclaimed, and their file-name-1 arm has no witness in the corpus, so they are left for an adjudication pass rather than closed on this evidence"
tags: [cobolsharp, work, defect]
---

Filed 2026-09-19 by **REGISTRAR #6** — the lead-filing pass over the implementer, finisher and lander reports
that landed after registrar #5 (PB416, PB426, PB482, PB368, PB391, PB555, PB401, PB403, PB388, PB436, PB408,
PB447, PB419, PB427, PB443, PB396, PB411, PB420, PB425, PB877, PB391-finisher, PB405, PB522, PB502; landing
trains 33-38 and train-pb877). **Every probe below was REBUILT from the source report and RE-RUN on this
worktree's own `dotnet build CobolSharp.sln -c Debug` (0 errors) at main `80dd97e2`**, and **every citation was
re-derived with `python scripts/spec/cite.py --check`** — the command line is printed beside the quotation.
Sources under `<scratch>/reg6/probe/`.

# PB891 — LANDED — a flag with no stack, in a language with nesting

Source: the `PB368` report, "New defects" — filed there as the single lead of that change set.

## The mechanism, read from the emitter

`src/Cobol.Net.Compiler/CodeGen/EcEmitter.cs`:

```
:150   foreach (var g in gates) w.Line($"ExceptionState.{g.Flag} = true;");
:162   w.Line("finally { " + string.Join(" ", gates.Select(g => $"ExceptionState.{g.Flag} = false;")) + " }");

:253   foreach (var g in gates.Where(g => g.Flag is not null)) w.Line($"ExceptionState.{g.Flag} = true;");
:271-2 var reset = gates...Select(g => $"ExceptionState.{g.Flag} = false;")...
       if (reset.Count > 0) w.Line("finally { " + string.Join(" ", reset) + " }");
```

`= false` is not "restore what it was"; it is "assume it was off". Two failures follow, in opposite
directions:

* **an enclosing statement loses its enable.** When a guarded statement's execution runs OTHER statements in
  the same runtime element — a declarative invoked from its `__IoCheck`, an F3 handler range, a debugging
  declarative — the inner gate's `finally` sets the flag `false`, so a LATER raise site of the OUTER
  statement reads "checking off" and the condition is not raised.
* **an unguarded inner statement inherits `true`** and can raise a condition that is not enabled at THAT
  statement.

```
$ python scripts/spec/cite.py --check 14.6.13.1.1 "if checking for an exception condition is not enabled, the exception condition will not be raised"
  OK  §14.6.13.1.1   (General)
```

Enablement is a property of the SOURCE TEXT at the raising statement, so both directions are wrong answers.

## ⛔ The correct shape is seventeen lines away in the same file

```
:637   w.Line("var __ck = ExceptionState.PushAllCheckingOff();   // GR14 implicit PUSH ALL + TURN OFF ALL");
:644   w.Line("finally { ExceptionState.PopAllChecking(__ck); }   // GR14 implicit POP ALL");
```

§14.9.28.4 GR14's implicit PUSH/POP already has a saved-state discipline in `CheckingFlags`. The repair is a
**saved local per gate**, the same shape, applied to `EmitArgOrPlain` and `EmitGatesOrInner`.

## ⛔ What the fixer must expect — this changes generated text for EVERY EC golden

There are ~30 ambient conditions and every one of them goes through these two methods. The diff is wide and
shallow: it is a mechanism, not an EC-FLOW-USE special case, and shrinking it to one condition would leave
the other twenty-nine with the same bug and a test that says otherwise.

## Repro shape (NOT yet measured end to end)

A declarative whose body lies in a `>>TURN … CHECKING OFF` region, invoked from a statement in a CHECKING ON
region; then a second raise site of that outer statement. **This note is filed from the emitted-text
mechanism, which is decisive about what is generated; the behavioural golden is the fixer's first step**, and
it should be written BEFORE the fix so it fails once (memory `green_gates_arent_evidence`).

## Relation to PB841

`kb/Work/PB841` is the same family and a different boundary — the flags leaking across a CALL, because
`ProgramTable.CallProgram` saves the external mask and the PERFORM depth and not `ExceptionEngine._checking`.
PB841 is the CALL boundary, PB891 is the STATEMENT boundary. One implementer takes both: the answer in both
cases is that the ambient checking state is a SAVED-AND-RESTORED stack rather than a flag anyone may clear.

## Landed — 2026-09-22, wave 45 (C), with PB841 and PB893

**Reproduced first, on the worktree's own build.** The note's "not yet measured" repro turned out to be a HANG:
`>>TURN EC-BOUND-SUBSCRIPT CHECKING ON` + `PERFORM VARYING I … UNTIL T (I) > 5` whose body is a checked
`MOVE T (1) TO R` — the MOVE's `finally { BoundSubscriptChecking = false; }` switched the PERFORM's own check
off, `T (4)` in the UNTIL never raised, and the loop ran forever. The inherit direction reproduced too: a MOVE
after `>>TURN … CHECKING OFF` inside an IF whose own line is ON raised (`WRONG HANDLED=EC-BOUND-SUBSCRIPT`).

**Rule.** §7.3.25.4 GR6 (checking "is enabled for the procedure division statements and procedure division
headers that follow in the compilation group") and GR5 (a TURN inside a statement "applies to any succeeding
statement … whether or not that succeeding statement is within the scope of the statement in which the TURN
directive is specified"); §14.6.13.1.1 (not enabled ⇒ not raised). All `cite.py --check` OK.

**Fix — one discipline, not a per-gate patch.** The run-time flags are a SCOPE: a guard saves
(`ExceptionState.SaveChecking`), sets its own flags and restores (`EcEmitter.OpenGateFlags` → `RestoreChecking`);
wherever control reaches OTHER source statements while a guard's flags stand, those start from ALL-OFF
(`EnterCheckingBaseline`: nested statement lists via `EnterNestedStatements`, procedure ranges via the new
`StatementEmitter.EmitProcedureRange` — now the ONE caller of `DispatchCall` — and `__RunUse` for every USE
procedure / F3 handler). `EcState.FlagsStanding` keeps it zero-cost where no guard encloses. The two GR14
push-offs (`__RunF3`, the FINALLY block) were deleted as second realizations of the same window;
`PopAllChecking` became `RestoreChecking`. Drift: `CheckingScopeDriftTests` (no `= false` reset anywhere in
CodeGen — measured to match the two pre-fix sites — one `DispatchCall` caller, every activation boundary, the
emitted shape, and the no-scope control). Golden: `2002/pb891_checking_scope` (four cases) + negative
`pb891-checking-scope-below-2002` (COBOLNET0900 at 85).
