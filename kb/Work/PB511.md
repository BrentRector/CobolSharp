---
title: "PB511 — The EXTERNAL clause's `AS literal-1` phrase has no grammar: `externalClause : IS? EXTERNAL` is the whole rule, so the externalized-name phrase printed in the data-description Format 1 (and in the EXTERNAL clause's own general format) is an ANTLR parse error at every edition that has it."
id: PB511
kind: defect
status: open
severity: MAJOR
area: frontend/grammar
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.16.2"]
inventory_rows: ["FMT-13.16.2"]
tags: [cobolsharp, work, defect]
---

# PB511

## Mechanism — data-description-entry-p1
The EXTERNAL clause's `AS literal-1` phrase has no grammar: `externalClause : IS? EXTERNAL` is the whole rule, so the externalized-name phrase printed in the data-description Format 1 (and in the EXTERNAL clause's own general format) is an ANTLR parse error at every edition that has it.

### Expected, from the standard
ISO 13.16.2 Format 1 prints the slot as `[ IS EXTERNAL [ AS literal-1 ] ]`, and 13.18.22.2 gives the clause's whole general format as `IS EXTERNAL [ AS literal-1 ]`; 13.18.22.3 SR3 constrains literal-1 (alphanumeric or national, not a figurative constant, not zero-length) and 13.18.22.4 GR5/GR6 make it the externalized name. The entry shall COMPILE, and A's externalized name shall be 'EXTNAME'.

```
$ python scripts/spec/cite.py --check 13.16.2 "s whole general format as `IS EXTERNAL [ AS literal-1 ]`; 13.18.22.3 SR3 constrains literal-1 (alphanumeric or national, not a figurative co..."
  FAIL: §13.16.2 exists but does NOT contain 's whole general format as `IS EXTERNAL [ AS literal-1 ]`; 13.18.22.3 SR3 constrains literal-1 (alphanumeric or national, not a figurative constant, not zero-length) and 13.18.22.4 GR5/GR6 make it the externalized name. The entry shall COMPILE, and A'
  ⛔ REGISTRAR: no quotation in this finding validated verbatim against §13.16.2. The MECHANISM below is measured and stands, but the QUOTATION is the finding's paraphrase — quote the clause and re-run `--check` before it reaches a code comment, a golden or the DEVLOG (CLAUDE.md rule 1).
```

### Observed
error COBOL0307: unexpected 'AS'. A period may be missing at the end of the previous sentence. Identical at --std 2002, 2014 and 2023 (at 85 the phrase does not exist, so the rejection there is correct but for the wrong reason -- it is the same missing rule, not an edition gate). Consequence: a conforming program that names its external items -- the normal way to share storage with a non-COBOL runtime element -- cannot be compiled at all.

### Repro
cobol.exe E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/f_external_as/f_external_as.cob  (and --std 2002 / 2014, probe q_ext_as_2002)
       IDENTIFICATION DIVISION.
       PROGRAM-ID. PBFEA.
       DATA DIVISION.
       WORKING-STORAGE SECTION.
       01  A IS EXTERNAL AS 'EXTNAME' PIC X(3).
       PROCEDURE DIVISION.
           STOP RUN.

### Code site
src/Cobol.Net.Frontend/Grammar/Core/CobolData.g4#externalClause (line 329); the binder's consumer is src/Cobol.Net.Compiler/Binding/DataBinder.cs#BindEntry (`clause.Context.externalClause() is not null` -> hasExternal, no literal read) and src/Cobol.Net.Compiler/Binding/DataBinder.Linkage.cs:350

## Sibling sweep the fixer owes
Every bug here is a pattern (CLAUDE.md rule 4). Before closing this note, sweep the sibling arms named in the mechanism above and add the ones that fail to `inventory_rows`.

## Provenance
Lane-3 adjudication batch b3 (subjects: data-description-entry-p1), adjudicated against the compiler pinned at 89044aa0 and RE-PROBED on the registrar's worktree cut from 6e7caa91 (a 1942-program sweep of `out-b3/probe/` plus targeted re-probes); the finding reproduces on today's tree.

## Repro programs
`E:\Temp\claude\E--CobolSharp\a9d94900-79fe-4a38-89f0-921976861543\scratchpad\repros\PB511\` — f_external_as. The implementer's opening move is to rerun them against a freshly built `cobol.exe`.

