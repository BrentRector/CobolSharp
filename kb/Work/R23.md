---
title: "R23 — a CobolDate comment cites Annex A.1 item 171 as documented; the CONFORMANCE.md register does not carry it"
id: R23
kind: defect
status: landed
severity: MINOR
area: process/traceability
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: ["15.80.3"]
tags: [cobolsharp, work, defect]
---

# R23 — a CobolDate comment cites Annex A.1 item 171 as documented; the CONFORMANCE.md register does not carry it

> R00 ledger-audit orphan (2026-08-08): F35 (surviving half) of `PHASE-B-15.32-15.44-findings.md`, probe-verified OPEN
> against the R10-landed tree (`5312996a`). The probe programs are reproducible from the finding text.

MECHANICAL CHECK (2026-08-08): `CobolDate.cs:170` cites "the COBOL.NET documented precision (§15.80.3 r3,
implementor item 171)"; the audit_annex_a1.py register carries items 2, 59, 92, 123, 135, 158, 179, 202,
205–215 — no 171. (The item-87 comment the ledger also named is gone.) A comment asserting a documented
determination the register does not carry is the documentation equivalent of a dead lookup. FIX: either add
the item-171 determination to CONFORMANCE.md §7 (the SECONDS-PAST-MIDNIGHT precision it describes) or
correct the comment; then extend the audit to sweep src/ for `implementor item N` citations against the
register so the next one fails loud.

## LANDED 2026-08-08

A.1 item 171 verified against the standard ("SECONDS-PAST-MIDNIGHT function returned value (precision) —
required, shall be documented") — the COMMENT was right, the REGISTER was missing the row, which §7 now
carries (100 ns / 7 fraction digits, the tick-count-at-scale-7 contract, pinned by
`CobolDateWindowingTests`). The audit gained the sweep the note demanded —
`sweep_source_citations`: every `implementor item N` / `A.1 item N` in `src/Cobol.Net.*` must name a real
A.1 item AND a row §7 discharges, with `--self-test` proving both failure reasons fire. **The sweep's
first real run caught THREE more undischarged citations (six sites)** — items **22** (CHAR-NATIONAL's
ALSO-position choice: the FIRST character defined), **180** (SMALLEST-ALGEBRAIC's float rejection under
native arithmetic), **188** (the ALPHABET UTF-16 BMP-identity correspondence) — each now a §7 row written
from the code's own pinned determination. **En route, the audit also caught the R26 commit's §7 "item
216" row squatting on A.1-216 (USAGE POINTER)**: §7 is keyed to A.1's own numbering, no A.1 item covers
the counter registers' size, and the determination MOVED to §3 (behavior determinations) with the code
and golden comments repointed. §7's header now quotes the fresh audit: 17 of 199 discharged, findings
none, self-test 7/7. Gate: Unit drift 3169/3169 · the counters golden by name · characterization 33/33.
