---
title: "PB256 — The NUMVAL-family scanners take their mode and flag decisions as PARAMETERS and every test supplies them as literals, so the compiler arm that computes them is untested — and where the scanner must choose between two readings of a rule, it chooses in silence"
id: PB256
kind: defect
status: landed
severity: MINOR
area: runtime/intrinsics
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["15.93.4", "15.95.4", "15.69.3"]
inventory_rows: []
tags: [cobolsharp, work, defect]
---

# PB256

## Landed — 2026-09-13

All three rows re-verdicted **PARTIAL → CONFORMS** (`pb256-numval-family-mode-flags-and-exponent-space`
batch; GAP 2561 → 2558), so `inventory_rows` is now empty and the register's back-link is discharged.

**FINDING 1 — the silent choice is now a stated determination, and the figure's reading wins.** A space does
NOT split the exponent's `n`: `TEST-NUMVAL-F("1E+1 2345")` is **6**, not 9. Two independent grounds agree, and
neither was in the record before. (a) §15.69.3 r1's format figure — **rendered from the printed page 898**,
because a general-format diagram was load-bearing — draws `n` as ONE contiguous metavariable ("n is one, two,
three, or four digits representing the exponent") with `[ space-string ]` only *before* and *after* it, so the
space is the legal trailing space-string and the `'2'` after it is outside the format. (b) **§15.95.4 r1 b) 1
governs the case directly and is UNQUALIFIED as to which digit run** — "if one or more spaces are embedded
within a string of numeric characters, the returned value is the position of the first non-space character
following the spaces" — where sub-notes **2, 3 and 4 of that same list each say "of the significand"
explicitly**. The drafters distinguished inside one list, so sub-note 1 reaches the exponent too, and
§15.69.3 r5's "ignored" means *contributes nothing where the figure admits one*, never *elide and join*.
Recorded in `docs/CONFORMANCE.md` §3 and at the exponent loop in `NvfScan`; witnessed by
`conformance:2023/pb256_test_numval_f_spaces`, whose `EIN4 "1E+1 234" = 6` is the **discriminating** case (the
rejected reading answers **0** there — a conforming four-digit exponent).

**FINDING 2 — the compiler arms are measured end to end, in both directions.** The mirror pair is what makes a
golden discriminating: a dead arm passes either half alone. `conformance:2002/pb256_test_numval_mode_flags`
carries three compilation units (no clause · `DECIMAL-POINT IS COMMA` · `OPTIONS. ARITHMETIC IS STANDARD.`) and
`conformance:2014/pb256_test_numval_standard_decimal` runs under `ARITHMETIC IS STANDARD-DECIMAL`, so
`ctx.Data.DecimalPointIsComma → CommaFlag` and `ArithmeticModes.NumvalDigitCap → DigitCapFlag` are exercised
rather than supplied as literals. The NATIONAL edge is measured in both directions too, and `NB (N"12 34") = 4`
proves positions are counted in **national character positions** — a byte-counted image answers 7.

**FINDING 3 — leg c) is adjudicated on the record that carries its text**, with a golden written for it:
`conformance:2023/pb256_test_numval_leg_c_and_national` measures all three NOTE kinds and, because the rule's
value is an arithmetic expression over another function, pins `FUNCTION LENGTH(NSP) = 4` beside `NS = 5` on the
same item so *both operands* of "LENGTH + 1" are observed. The FOURTH, smaller item is discharged with it: the
output-shaped leg-c width evidence is replaced by a direct measurement over a data item.

**THE MECHANISM ITSELF, not just its three instances** (rule 5). `IntrinsicModeFlagCoverageDriftTests` derives
every (function, flag) pair from `IntrinsicRenderer`'s own arms crossed with `IntrinsicCatalog`, and fails until
an **enabled + runnable** golden compiles that function in one source-unit scope specifying the construct that
turns the flag ON. Its failure branch was fired once before being trusted, and its catalog scan asserts it sees
*every* declared row so a new row shape cannot hide a function from it. Measured on landing: the sweep found
**three more holes of the identical shape** outside the note's own rows — NUMVAL-C × `DECIMAL-POINT IS COMMA`,
TEST-NUMVAL-C × `DECIMAL-POINT IS COMMA`, TEST-NUMVAL-C × the standard-decimal cap — all closed here.

Also landed: `conformance:negative/pb256-standard-binary-test-numval-f` (the PB198 decline proven on
TEST-NUMVAL-F's own path, not on the OPTIONS clause in isolation); TEST-NUMVAL-F added to
`conformance:negative/test_validators_below_2002`, the one member of the validator quartet whose COBOLNET1502
window that fixture never named; and the unit test renamed
`DigitCap_FollowsArithmeticMode` → `DigitCapParameter_SelectsTheSubNoteVerdict`, since no arithmetic mode was
ever in scope in it (`RV-15.93.4-L2.2`'s test-ref was repaired in the same batch, verdict untouched).

⚠ **What did NOT hold on today's tree:** the note's "No conformance golden compiles TEST-NUMVAL **or
TEST-NUMVAL-F** under `OPTIONS. ARITHMETIC IS STANDARD-DECIMAL` at all" was true of TEST-NUMVAL and **false of
TEST-NUMVAL-F** — `conformance:2014/pb121_test_numval_f_capacity` landed after the note was written and does
exactly that, and its `digitCap == 34` guard makes it an end-to-end measurement of the arm. The note's
`RV-15.93.4-L2.4` claim that leg c) had no covering test was also slightly stronger than the tree: two of the
NOTE's three kinds were pinned by `intrinsics_test_validators` (N8, N9); the zero-length kind, the data-item
form and the LENGTH arithmetic were the genuinely uncovered parts.

---

**Rows (as claimed while open):** `RV-15.93.4-1`, `RV-15.93.4-L2.4`, `RV-15.95.4-L2.5`

The NUMVAL-family scanners take their mode and flag decisions as PARAMETERS and every test supplies them as literals, so the compiler arm that computes them is untested — and where the scanner must choose between two readings of a rule, it chooses in silence.

ROWS: RV-15.93.4-1 and RV-15.93.4-L2.4 (§15.93.4 r1, TEST-NUMVAL's three legs and its digit-cap sub-notes) and RV-15.95.4-L2.5 (§15.95.4 r1 b item 5, TEST-NUMVAL-F's fifth-exponent-digit position).

⛔ FINDING 1 — A NORMATIVE TENSION RESOLVED WITH NO RECORD, and it changes the answer. §15.69.3 r5 makes embedded spaces IGNORED everywhere outside the significand ('Embedded spaces in argument-1 are ignored except between the first numeric digit and the last digit that precedes a letter E' — --check OK), while r1's figure draws the exponent `n` as a contiguous one-to-four-digit run with [space-string] only before and after it. The two readings give DIFFERENT answers for an exponent written with an interior space, and they land squarely on this rule's 'more than four significant digits' count. `TEST-NUMVAL-F("1E+1 2345")`: the code answers 6 — the digit loop exits at the space with ed=1, the trailing-space skip advances to '2', and `if (i != n) return Pos()` reports it — where the r5 reading makes the exponent 12345, five digits, and §15.95.4 r1b item 5 requires the position of its FIFTH digit, 9. `pendingSpace` implements r5's exception for the significand and NOTHING implements it for the exponent. Nothing in NvfScan, its comments, the design docs or any test records that the choice was made. Either reading may be defensible; an undocumented one is not.

⛔ FINDING 2 — THE COMPILER ARM HAS NO TEST, ON ANY OF THE THREE FUNCTIONS. `TestNumvalScannerTests.CommaMode_SwapsSeparatorRoles` calls `CobolIntrinsics.TestNumval("1,5", commaMode: true)` — the flag is a LITERAL, so it exercises the runtime scan and never `ctx.Data.DecimalPointIsComma → IntrinsicRenderer.CommaFlag → the emitted named argument`. `TestNumvalScannerTests.DigitCap_FollowsArithmeticMode` calls `TestNumval(d40, digitCap: 34)` — the cap arrives as a LITERAL, no arithmetic mode is in scope in a unit test, and the name asserts exactly what the test does not measure (feedback_green_gates_arent_evidence). No conformance golden compiles TEST-NUMVAL or TEST-NUMVAL-F under `OPTIONS. ARITHMETIC IS STANDARD-DECIMAL` at all; the property is proven live only through SIBLINGS (`2023/pb60_numval_standard_decimal`, `2023/numvalf_decimal_comma_and_spaces`). Same for the NATIONAL edge both records flagged: correct by construction (`OperandText.FieldAsString:249-250` hands the scan the UTF-16 image, one char per national position, so 'C'/'R', ' ' and the ASCII digits are the same code units) but unmeasured in both directions.

⛔ FINDING 3 — A RULE LEG ADJUDICATED IN NO RECORD. RV-15.93.4-L2.4's own rule text carries THREE parts — sub-note 4, then 'c) Otherwise, (FUNCTION LENGTH (argument-1) + 1).', then the NOTE enumerating leg c's three error kinds (zero-length, all spaces, 'valid characters but incomplete, such as the string " +."'). Its notes and test-ref address only the digit cap; leg c) was argued on RV-15.93.4-1, whose rule text STOPS at leg b) and does not contain it. Leg c) is therefore adjudicated NOWHERE. (On the refuter's own read leg c) is CORRECT — `NvScan:621` returns n+1 for a digit-free scan that ran off the end, and all three NOTE kinds trace: "" → 1, "    " → 5, " +." → 4 — so this is an unadjudicated part, not a wrong answer. It still needs its record and its golden.)

FOURTH, SMALLER: RV-15.93.4-1's leg-c width evidence was output-shaped and could not discriminate — golden N10's verdict 32 is 32 for ANY width >= 32 — and the property was re-derived on other evidence (`CobolString.Fit` pads or truncates every alphanumeric store to the declared width; `2023/pb33_numval_family_digit_cap`'s TEST-31 = 0 over `S31 PIC X(44)` exercises a padded 12-space tail end to end). The conclusion survives; the cited proof did not, and the row's test-ref should say so.

WHAT IS SETTLED AND MUST NOT BE RE-LITIGATED: RV-15.93.4-L2.4's third ground — that `NumericRenderer.StandardDecimal` also fires for `ArithmeticMode.Standard`, so plain ARITHMETIC IS STANDARD gets a cap no sub-note authorises — is now a stated determination rather than a widening: `ArithmeticModes.NumvalDigitCap` (OptionsModel.cs:81) names the 2002 STANDARD mode at 34 'because it routes to the same SDIDI engine' (PB198). And the standard-binary halves both records price are PB198's recorded decline.

FIX SHAPE: decide the exponent-space reading against §15.69.3 r5 and record it (a comment at the loop plus a CONFORMANCE.md determination if the figure's reading wins); add STANDARD-DECIMAL conformance goldens for TEST-NUMVAL and TEST-NUMVAL-F so the flag-producing arms are measured end to end; and adjudicate §15.93.4 leg c) in the record that carries its text.

VERIFIED LIVE AT HEAD (read-only): NvfScan:714-720 unchanged; CommaFlag/DigitCapFlag are still the compiler-side arms and the two unit tests still pass literals.

## Mechanism

src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Exact.cs#NvScan and #NvfScan take commaMode / digitCap / anycase as parameters; the compiler arms that compute them are src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs#CommaFlag (:792), #AnycaseFlag and #DigitCapFlag (:815-817). The silent choice is NvfScan's exponent run at Exact.cs:714-720 (`while (i < n && char.IsAsciiDigit(text[i])) { if (++ed > 4) return new(Pos(), …); … }`), contiguous by construction, where §15.69.3 r5's embedded-space exception is implemented for the SIGNIFICAND alone via `pendingSpace`

## Lineage

PB33/PB34 (landed) built the digit caps, PB60 (landed) made the validators and the value paths ONE scan, PB198 (landed) made the cap mode-derived through ArithmeticModes.NumvalDigitCap. Each landing tested the scan; none tested the arm that feeds it.

---

> Registered by the **defective-row registration wave** (2026-08-31). Every PARTIAL / NOT-IMPLEMENTED /
> DIVERGES row of the P14 traceability inventory now names a live work note in its `inventory_rows`
> frontmatter, and `DefectiveRowCoverageDriftTests` fails the battery if one stops doing so. The
> `inventory_rows` field is the SSOT for which rows this note owns; when the note lands, those rows must
> be re-verdicted in the same change set or the gate goes red.

> ⚠ **ROW IDS RE-KEYED 2026-09-06 (kb/Work PB698).** The transcription repair returned the outdented sub-items of these rule blocks to their printed level, so the rows this note names moved: `RV-15.95.4-L2.5` -> `RV-15.95.4-1`. `inventory_rows` above is live; the ids in the prose are as they stood when it was written, and the adjudications they carry were migrated by the `pb698-rekey` and `pb698-fold` batches.
