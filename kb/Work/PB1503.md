---
title: "PB1503 — METHOD-ID GET/SET PROPERTY accessor shape is screened on header PRESENCE only: OoClassTable.Build tests HasUsing/HasReturning booleans, never the number of USING formals nor whether the GET returning item / SET formal is ACTIVE-CLASS (§11.7.3 SR6/SR7), and SR5 (no PROPERTY clause on a working-storage item that is also an explicit accessor's property-name) is enforced only as a side-effect of a roster COLLISION, so `PROPERTY WITH NO GET` plus an explicit GET PROPERTY method is accepted"
id: PB1503
kind: defect
status: open
severity: MINOR
area: binder/oo
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1449", "PB1273", "PB1274"]
spec_refs: ["11.7.3", "13.18.42"]
inventory_rows: ["SR-11.7.3-5", "SR-11.7.3-6", "SR-11.7.3-7"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 15d** from lane-3 adjudication batch **15d** (adjudicated on the pinned tree `071ff04ea`; everything that landed since — Registrar 14a/14b/14c — is register-only, so no code change touches this mechanism). Every citation below was re-run by the registrar through `cite.py --check`.

# PB1503 — OPEN

## The rule (every citation re-run through `cite.py --check`)

> 5) If property-name-1 is specified as a data-name in the working-storage section of the containing object definition, the PROPERTY clause shall not be specified in the data description entry of that data-name.

`OK  §11.7.3 5)  (Syntax rules)`

> 6) If the GET phrase is specified, then the method shall have no USING phrase parameters specified in the procedure division header and shall have a single RETURNING phrase. The returning item shall not be an object reference described with the ACTIVE-CLASS phrase.

`OK  §11.7.3 6)  (Syntax rules)`

> 7) If the SET phrase is specified, then the method shall have a single USING parameter specified in the procedure division header and no RETURNING phrase. The USING parameter shall not be an object reference described with the ACTIVE-CLASS phrase.

`OK  §11.7.3 7)  (Syntax rules)`


## Finding — `method-id-paragraph` (under-rejects; rows `SR-11.7.3-6`, `SR-11.7.3-7`)

**Mechanism.** The GET/SET PROPERTY accessor shape (§11.7.3 SR6/SR7) is screened on header PRESENCE only: OoClassTable.Build tests HasUsing/HasReturning booleans, never the number of USING formals, and no site checks whether the accessor's RETURNING item (GET) or USING formal (SET) is an OBJECT REFERENCE ACTIVE-CLASS. The comment promising a formal-count re-check 'at data-bind' has no implementation.

**Repro.** (a) E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d2b\out\probe\method-id-paragraph\p1_set_two_using\p1_set_two_using.cob: CLASS-ID MQ1C / OBJECT / METHOD-ID. SET PROPERTY BAL. / LINKAGE 01 LK-A PIC 9(4). 01 LK-B PIC 9(4). / PROCEDURE DIVISION USING LK-A LK-B.  (b) p2_get_active_class: METHOD-ID. GET PROPERTY ME. / 01 LK-R USAGE OBJECT REFERENCE ACTIVE-CLASS. / PROCEDURE DIVISION RETURNING LK-R.  (c) p3_set_active_class: METHOD-ID. SET PROPERTY PEER. / 01 LK-P USAGE OBJECT REFERENCE ACTIVE-CLASS. / PROCEDURE DIVISION USING LK-P.  Compile with the pinned cobol.exe --std 2023 (and 2002 for a).

**Expected from the spec.** All three are compile-time errors. §11.7.3 SR7: 'the method shall have a single USING parameter specified in the procedure division header and no RETURNING phrase. The USING parameter shall not be an object reference described with the ACTIVE-CLASS phrase.' §11.7.3 SR6: 'The returning item shall not be an object reference described with the ACTIVE-CLASS phrase.'

**Observed / reasoning.** All three compile clean (rc 0, no diagnostic) at 2023; (a) also at 2002. The emitter (OoEmitter.cs ~669) assumes 'the setter's one formal', so (a) silently builds a setter from a two-formal method.

**Code site.** src/Cobol.Net.Compiler/Oo/OoClassTable.cs#Build (the two `method.Accessor == 'G'/'S'` COBOLNET0842 checks); src/Cobol.Net.Compiler/Binding/DataBinder.Oo.cs (method formal/RETURNING bind — where the resolved formal count and ACTIVE-CLASS descriptor are first known)

## Finding — `method-id-paragraph` (under-rejects; rows `SR-11.7.3-5`)

**Mechanism.** §11.7.3 SR5 is enforced only as a by-product of an accessor-roster COLLISION in DataBinder.OoBindPropertyClauses: when the PROPERTY clause says WITH NO GET (or WITH NO SET) it synthesizes no accessor of that kind, so an explicit GET (or SET) PROPERTY method for the same data-name never collides and the illegal pair is accepted.

**Repro.** E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d2b\out\probe\method-id-paragraph\p4_property_noget_plus_explicit_get\p4_property_noget_plus_explicit_get.cob: CLASS-ID MQ4C / OBJECT / WORKING-STORAGE 01 BAL PIC 9(4) PROPERTY WITH NO GET. / METHOD-ID. GET PROPERTY BAL. RETURNING LK-R. — and p4b (PROPERTY WITH NO SET + METHOD-ID. SET PROPERTY BAL.). Compile --std 2002 / 2023.

**Expected from the spec.** Compile-time error: §11.7.3 SR5 'If property-name-1 is specified as a data-name in the working-storage section of the containing object definition, the PROPERTY clause shall not be specified in the data description entry of that data-name.' — unconditional; the NO GET/NO SET phrase does not lift it.

**Observed / reasoning.** Both compile clean (rc 0) at 2002 and 2023. The bare-PROPERTY case is rejected COBOLNET0842 (OoSpineTests.PropertyClause_DuplicateExplicitAccessor_0842), which is why the gap is invisible to the existing test.

**Code site.** src/Cobol.Net.Compiler/Binding/DataBinder.Oo.cs#OoBindPropertyClauses (AddAccessor / TryAddMethod collision is the only SR5 test)

## What the fixer owes

Screen SR6/SR7 on the bound header (formal COUNT and the ACTIVE-CLASS descriptor of the one formal / returning item) at the site the comment promises ('at data-bind'), and SR5 as its own rule: an explicit GET/SET PROPERTY method whose property-name is a working-storage data-name carrying ANY PROPERTY clause (WITH NO GET/SET included) is refused, independent of accessor synthesis. Coordinate with PB1273/PB1274 (the PROPERTY clause's own rules and the synthesis-order defect) — same files, one fill unit. Negatives p1_set_two_using and p4_property_noget_plus_explicit_get.
