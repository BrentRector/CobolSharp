---
title: "PB154 — ✅ LANDED 2026-08-29 (IsFunction both directions; GR7 modeled as CalledSinceCancel + probe-free; the static BASED bridge with index-cell routing; FileCloseIfOpen tolerant + SD-filtered + the tolerant transient close; COBOLNET1696; LOUD containee invariant; A.1 items 18/19; cascade golden; → PB168 registered). WAS: CANCEL: a user-defined FUNCTION is locatable and cancellable (ProgramTable carries no program-vs-function discriminator — CANCEL \"FCTR\" re-initializes a UDF's statics where §8.4.6.3 scopes GR2 to program-names and GR7 requires no action); ProbeSiblingModule runs and CACHES side-effectful probes on the not-located arm; a containee registered before its container silently drops from the GR4 cascade; the seven initial-state actions are one-seventh implemented and action 5 REJECTS legal BASED-in-RECURSIVE source"
id: PB154
kind: defect
status: landed
severity: MAJOR
area: runtime
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB154

Rows: GR-14.9.5.4-2 (DIVERGES), -3/-4/-5/-7/-9/-10/-12, SR-14.9.5.3-1/-2/-3, findings [CANCEL statement]/[cancel-statement].

(1) EmitEntryWrapper registers FUNCTION-ID units with ParentPath null + Recursive true, so ResolveVisible's rule-4 matches a UDF and CancelNode resets its statics — repro: insert `CANCEL "FCTR-P10RW"` into recursive_ws.cob, U2 flips 0002→0001. Tag the node kind. (2) The not-located arm probes sibling DLLs (registering foreign modules, firing staticReset) and caches the MISS so a later CALL's legitimate probe is suppressed — GR7 says no action. (3) ProgramTable.Register drops a containee whose container is unregistered, silently — an assert-worthy invariant. (4) Initial state = §14.6.2.3.2's SEVEN actions; only action 2 exists; action 5's route is DiagnosticCatalog.RecursiveWsPointerBacked REJECTING legal `01 X BASED` in a RECURSIVE program; 6/7 untested. (5) No class screen on identifier-1 (CANCEL WS-NUM no-ops; BindCall's sibling hole); literal kinds + loop-abort → PB130/PB134; prototype alternative blocked on P13. (6) GR5's active-target EC consults no target checking state and A.1 items 18/19 are undischarged; GR9's CloseFiles is unguarded (Close exception mapping → PB140) and closes not-open connectors.

*(Found by Phase-B batch 8 — §8.8.1 + §14.9.1–14.9.12, 2026-08-29; one adjudicator + one independent refuter per subject. Full forensic evidence: the named rows' `notes` in `tests/version-matrix/traceability-inventory.json`; the raw finding lines in the session scratchpad `pb-batch8/findings.txt`.)*

---

## ✅ LANDED 2026-08-29 — wave gate GREEN (Conformance 2426/2426 · Unit 4636/4636 · Characterization 33/33); DEVLOG Entry 1390

All six mechanisms above landed, plus what the landing itself surfaced:

- **The first gate run caught GR7 mis-derived** (NIST IC203A CNCL-TEST-04 "CANCEL UNCALLED PROG"): the
  transient close's "no-op for a never-called unit" claim guarded OPEN-ness via `Require()`, which throws on
  a never-registered name. GR7 is now a MODELED predicate — `Node.CalledSinceCancel`, set at both activation
  sites, cleared by explicit CANCEL and by §14.9.18 GR2's implicit cancel, guarding CancelNode's whole body —
  and `FileRegistry.CloseIfOpen` treats a never-registered name as a fortiori not open.
- **The pre-landing review fleet (73 agents, 4 lenses, 18 adversarially confirmed findings)** caught: the
  §14.9.18 GR2 implicit cancel runs AFTER the activation's finally nulled an INITIAL container's slot, so the
  transient close's ParentInstance lookup THREW on every INITIAL return with a null-slot containee (the
  cancel path now resolves the container tolerantly; golden `2023/pb154_cancel_cascade` pins that return,
  the never-called-containee no-op over an unregistered connector, and the already-canceled no-op); the
  emitted CloseFiles loop was the one file loop without the `IsSortMerge` skip; the BASED arm's `continue`
  dropped INDEXED-BY-cell routing (a silent per-activation index in static WS); `__ResetStatics` iterated a
  HashSet into generated source (now the one source-ordered walk — with the BASED arm FIRST in the chain,
  because a plain `X(n) BASED` is Tier-B and the tier arm swallowed the bridge reset until the second gate
  run measured ADDR=HELD); COBOLNET1681's CALL text promised program-pointer admissibility (CANCEL's
  narrower §14.9.5.3 SR1 owns COBOLNET1696 now); and the A.1 item-19 row's §7.3.4 citation for
  CALL-CONVENTION (→ §7.3.9 — the inventory row carried the same inherited miscitation).
- **Registered:** kb/Work PB168 — the CALL-side connector-lifetime twin (a fresh RECURSIVE activation
  re-registers over a live open connector; also records the host-locking dependence of
  `pb154_cancel_initial`'s REOPEN leg).

Residue, each named at its owner: the ADDRESS-OF-taken static cell (GR-14.9.5.4-3, narrowed reject), the
unobserved reverse-source-ORDER half (GR-14.9.5.4-4), the P13 prototype arm (SR-14.9.5.3-3). Battery #34 is
owed for the since-#33 batch (PB151 + PB154).
