---
title: "PB996 — INHERITED clause numbers in code comments and messages that name the wrong rule: SortEmitter's \"GR16c / MERGE GR13c\" space-fill cite, the COMP-5 container-range cite \"§13.18.60.4 GR12\" in NumericRenderer / OperandText / PicInfo (GR12 is the BINARY-CHAR…DOUBLE rule), and SortBinder.TableSortKeyUnsupported still citing SR14e on paths that are no longer SR14e"
id: PB996
kind: defect
status: open
severity: MINOR
area: docs/citations
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
cluster: ["PB599", "PB388"]
spec_refs: ["14.9.40.4", "13.18.60.4", "14.9.40.3"]
inventory_rows: []
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-22 by **REGISTRAR #12** from `leads-w48-w52.md` (wave 49 group E (PB837) and the train-49 lander). Code-read lead; the fixer re-derives each number with `cite.py --check` against the sentence the code actually implements (CLAUDE.md rule 1 — a citation not `--check`ed is not a citation).

# PB996 — OPEN

## Sites
- `SortEmitter` — the GIVING space-fill comment cites "GR16c / MERGE GR13c"; the GIVING-WRITE cite beside it was already
  corrected to MERGE GR12 b) by PB837's implementer, the space-fill one was not re-derived.
- `NumericRenderer`, `OperandText`, `PicInfo` — the COMP-5 container range cites "§13.18.60.4 GR12"; GR12 governs
  BINARY-CHAR … BINARY-DOUBLE. The owner reference for COMP-5 is `kb/Work/R10`'s derivation.
- `SortBinder.TableSortKeyUnsupported` — the message cites SR14e, but after PB599's split it is reached only from the
  GR21 path and the REDEFINES-view deferral; the message must name the rule of the path that reaches it (or split into
  two descriptors — PB599's two-jobs shape).

## What the fixer owes
Re-derive each with `cite.py --check <clause> "<the sentence the code implements>"`, fix the comment/message, and extend
`audit_code_citations.py`'s known-inherited list if the tool can flag the pattern (a cite whose quoted text is absent).

## Extended 2026-09-24 by REGISTRAR 14c (lane-3 batch d1-14c, pinned tree `68bbdb00f`)

Two more wrong-clause texts (behaviour correct in every probe): SortBinder.SortBindCollating's COBOLNET0898 for a national alphabet in the ALPHANUMERIC slot cites §14.9.40.3 SR2 where SR1 is violated (`cite.py --check 14.9.40.3 "Alphabet-name-1 shall reference an alphabet that defines an alphanumeric collating sequence"` → OK SR1), and its UTF-8/UTF-16 message's list should follow §12.3.7.4 GR7 Table 6 (LOCALE and code-name-2 also reference a collating sequence); the CobolIO.g4 sortCollatingPhrase comment ('COLLATING is required … leniency L5') contradicts the measured §14.9.40.2 figure (COLLATING is optional — the grammar's COLLATING? is right) and the unused CBL3617/CBL3618 descriptors are wrong. Probe c_nat1.

**Finding (`sort-statement-p1`, silent; rows `SR-14.9.40.3-1`, `SR-14.9.40.3-2`, `FMT-14.9.40.2`).** Wrong-clause text in SORT collating diagnostics and grammar comments (the PB996 inherited-citation family)

**Repro.** SPECIAL-NAMES. ALPHABET N-NAT FOR NATIONAL IS NATIVE. `SORT SW ASCENDING K1 COLLATING SEQUENCE IS N-NAT USING IN1 GIVING OU1` -> COBOLNET0898 '... alphabet-name-1 shall reference an alphabet that defines an ALPHANUMERIC collating sequence ... (ISO §14.9.40.3 SR2)' (probe c_nat1).

**Expected from the spec.** The violated rule is §14.9.40.3 SR1 (alphabet-name-1), not SR2. The UTF-8/UTF-16 message's list 'only NATIVE, UCS-4, and literal-phrase national alphabets may collate' should follow §12.3.7.4 GR7 Table 6, which also marks LOCALE and code-name-2 as referencing a collating sequence. The sortCollatingPhrase grammar comment ('COLLATING is required by the ISO SORT/MERGE format ... leniency L5') contradicts the measured §14.9.40.2 figure note, whose required-word list omits COLLATING — the grammar's COLLATING? is correct, the comment and the unused CBL3617/CBL3618 descriptors are wrong.

**Observed / reasoning.** Behavior is correct in every case (the rejections fire, LOCALE national alphabets are accepted — probe c_nloc); only the cited clause / explanatory text is wrong, which sends a reader to the wrong rule. Fold into kb/Work PB996's sweep.

**Code site.** src/Cobol.Net.Compiler/Binding/Procedure/Verbs/SortBinder.cs#SortBindCollating (lines 585-587, 570-573); src/Cobol.Net.Frontend/Grammar/Core/CobolIO.g4#sortCollatingPhrase; src/Cobol.Net.Frontend/Diagnostics/DiagnosticDescriptors.cs (CBL3617/CBL3618)

Also (d1-14c stop-statement dossier gap): `src/Cobol.Net.Runtime/Control/Signals/StopRun.cs:7` cites 'ISO §14.9.43' (measured) — the STRING statement — for STOP RUN (§14.9.42); and an orphaned doc comment in `StatementBinder.cs` says of the STOP status phrase 'The phrase has no runtime effect in this compiler', which PB1178 measures as false.

## Folded from PB1550 (2026-09-24, golden lane #2)

Found 2026-09-24 by the golden-lane-2 misc-p18 writer, confirmed and extended by its refuter (pin f0d582ccd). The
diagnostics fire correctly; their TEXT mis-cites the rule (a comment/citation defect — rule 1).

## Code sites — `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/SortBinder.cs`

- :576-579 alphabet-name-2 not national → cites "§14.9.40.3 SR2"; for MERGE it is §14.9.24.3 SR6.
- :581-584 national alphabet with no collating sequence → cites "§14.9.40.3 SR2" (verb-dependent).
- :596-598 alphabet-name-1 is FOR NATIONAL → cites "§14.9.40.3 SR2", wrong even for SORT (SR1); MERGE §14.9.24.3 SR5.
- :601-602 undeclared alphabet-name → cites "§14.9.40.3 SR1" (verb-dependent).

## Fix shape

The binder is shared, so the clause must come from the VERB being bound (one table keyed by verb, not a second copy
of the messages); `cite.py --check` every resulting clause. Goldens `negative/l1c18-merge-collating-*` stop their
`.err` before the clause text, so they survive the fix.

Also (golden lane #2 misc-p29, SortBinder.cs ~598): the alphabet-name-1-is-national message cites "§14.9.40.3 SR2" for
SORT's SR1 (confirmed), and the UTF-alphabet message's "only NATIVE, UCS-4, and literal-phrase" omits LOCALE and
code-name-2 — fix the message text with the clause.

## Folded from PB1552 (2026-09-24, golden lane #2)

Found 2026-09-24 by the golden-lane-2 misc-p19 writer, confirmed by its refuter (pin f0d582ccd). Same shape as
PB1550 (diagnostic TEXT cites the wrong clause; the check itself fires correctly).

Code sites: `src/Cobol.Net.Compiler/Binding/Validation/StatementValidation.cs:1021` (the message) and the comments at
:956, :958, :1013. Sweep: `audit_code_citations.py` should have caught a clause that fails `cite.py` — find out why it
did not (a message string vs a comment?) and close that hole so the next one is automatic (rule 5).

Also (golden lane #2 misc-p32): the COBOLNET0844 message for a boolean literal in a relation cites "§8.8.4.2.1 F1
SR2/SR3"; the syntax rules are §8.8.4.2.3.

Also (golden lane #2 misc-p34): COBOLNET1557 (SAME AS naming the TYPE subject / an enclosing group, §13.18.57.3 SR1)
cites §13.18.49.3 SR3.
