---
title: "PB1351 — REPLACE / COPY REPLACING compare literal text-words case-insensitively on their RAW spelling (`ApplyReplacements` uses `StringComparison.OrdinalIgnoreCase`, delimiters included): literal CONTENT is case-folded against §7.2.3.4 9) c) 3., quote vs apostrophe delimiters never match (c) 4. a.), and doubled quotation symbols are not collapsed (c) 4. b.) — so `==\"abc\"==` wrongly replaces `\"ABC\"` and `=='X'==` fails to replace `\"X\"`"
id: PB1351
kind: defect
status: open
severity: MAJOR
area: frontend/text-manipulation
wrong_answer: true
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1350", "PB1352", "PB1353"]
spec_refs: ["7.2.3.4", "7.2.4.4"]
inventory_rows: ["GR-7.2.3.4-9", "GR-7.2.3.4-L2.3", "GR-7.2.3.4-L2.4", "GR-7.2.4.4-L2.3", "GR-7.2.4.4-L2.4"]
closes_rows: []
closes_rows_reason: "open — nothing landed yet"
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 15a** from adjudication batch 15a (subjects in §7.2 / §7.3, adjudicator pin `E:\CobolSharp-pins\adj-d2` @ `071ff04ea`; nothing under `src/` landed between the pin and `8bd4c7c9f`, so every measurement below stands). Probe programs: `E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d2\out\probe\<subject>\`.

# PB1351 — OPEN

The comparison half of the text-manipulation pass: once PB1350 forms literal text-words correctly, the comparison must still compare a literal by (delimiter representation-class, un-doubled content, case-SENSITIVE for non-hex alphanumeric/national formats) and every other word case-insensitively with alphanumeric≡national.

## The rule (every citation re-run by the registrar)

> Except when used in the non-hexadecimal formats of alphanumeric and national literals …

`python scripts/spec/cite.py --check 7.2.3.4 "Except when used in the non-hexadecimal formats of alphanumeric and national literals"` → `OK  §7.2.3.4 9) a)  (General rules)`

> each lowercase letter is equivalent to its corresponding uppercase letter …

`python scripts/spec/cite.py --check 7.2.4.4 "each lowercase letter is equivalent to its corresponding uppercase letter"` → `OK  §7.2.4.4 8)  (General rules)`

> The two representations of the quotation symbol match …

`python scripts/spec/cite.py --check 7.2.4.4 "The two representations of the quotation symbol match"` → `OK  §7.2.4.4 8)  (General rules)`

> two contiguous occurrences of the character used as the quotation symbol …

`python scripts/spec/cite.py --check 7.2.3.4 "two contiguous occurrences of the character used as the quotation symbol"` → `OK  §7.2.3.4 9) a)  (General rules)`

## Finding — copy-statement-p2 (wrong-answer)

Literal text-words are compared raw: TokenizeTextWords splits a literal's prefix (X/N/NX/B/BX/Z...) from its quoted body and ends a literal at the first embedded quote, and ApplyReplacements compares every text-word OrdinalIgnoreCase with delimiters included — so literal case is folded, quote vs apostrophe delimiters never match, doubled quotes are not collapsed, and pseudo-text can match INSIDE a literal

**Rows:** GR-7.2.3.4-9, GR-7.2.3.4-L2.3, GR-7.2.3.4-L2.4

**Repro:**

```
p3book.cpy: `01 LV PIC X(3) VALUE "abc".` `01 LQ PIC X(3) VALUE 'xyz'.` `01 LD PIC X(3) VALUE "p""q".`; `COPY p3book REPLACING =="ABC"== BY =="MMM"== =="xyz"== BY =="NNN"== =='p"q'== BY =="OOO"==.` DISPLAY LV LQ LD. p12book.cpy: `01 HX PIC X(1) VALUE X"41".`; `COPY p12book REPLACING =="41"== BY =="42"==.` DISPLAY HX. (E:/Temp/claude/E--CobolSharp/a700c052-f9e4-4942-84ff-a13c7505d6ec/scratchpad/adj-d2/out/probe/copy-p2/p3.cob, p12.cob)
```

**Expected from the spec:** LV = abc (c)3: case is significant inside non-hexadecimal alphanumeric/national literals); LQ = NNN (c)4 a: the two quotation-symbol representations match); LD = OOO (c)4 b: a doubled quote is a single occurrence); HX = A (§7.2.2.5 2): X"41" is ONE text-word including its delimiters, so =="41"== does not match it).

**Observed:** LV = MMM, LQ = xyz, LD = p"q, HX = B. Sibling: the §7.2.4 REPLACE statement goes through the same TokenizeTextWords/ApplyReplacements and inherits all four.

**Code site:** src/Cobol.Net.Frontend/Preprocessor/CopyProcessor.cs#TokenizeTextWords (lines 300-327); src/Cobol.Net.Frontend/Preprocessor/CopyProcessor.cs#ApplyReplacements (line 210, OrdinalIgnoreCase; lines 229/231 partial-word)

## Finding — replace-statement-p2 (wrong-answer)

replace-literal-comparison: ApplyReplacements compares every text-word with StringComparison.OrdinalIgnoreCase on its raw spelling, so literal CONTENT is case-folded and the quote/apostrophe delimiter representations are not equated

**Rows:** GR-7.2.4.4-L2.3, GR-7.2.4.4-L2.4

**Repro:**

```
REPLACE =="abc"== BY =="ZZZ"== =="QQ"== BY =="RR"==. DISPLAY "ABC" DISPLAY 'QQ'
```

**Expected from the spec:** 8) c) 3 excepts non-hex alphanumeric/national literal content from case equivalence, so "abc" does not match "ABC" -> prints ABC; 8) c) 4 a makes the two quotation-symbol representations match, so "QQ" matches 'QQ' -> prints RR.

**Observed:** Pinned compiler prints ZZZ then QQ (out/probe/replace-p2/lit.cob) - both inverted from the spec.

**Code site:** src/Cobol.Net.Frontend/Preprocessor/CopyProcessor.cs#ApplyReplacements line 210 (and the LEADING/TRAILING arm 229-231); shared with COPY REPLACING

## What the fixer owes (and the sibling sweep)

Give `ApplyReplacements` (whole-pseudo-text arm ~line 210 and the LEADING/TRAILING arm ~229-231) ONE text-word equality function implementing §7.2.3.4 9) c) 3.-4. / §7.2.4.4 8) c) 3.-4. and use it in both statements' comparisons (they share the code today — keep it shared). Goldens: case-differing literal NOT replaced; quote/apostrophe-delimited literals matched; `"A""B"` matched by its doubled form; a non-literal word still matched case-insensitively.

**Rows claimed:** GR-7.2.3.4-9, GR-7.2.3.4-L2.3, GR-7.2.3.4-L2.4, GR-7.2.4.4-L2.3, GR-7.2.4.4-L2.4
