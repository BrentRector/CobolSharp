---
title: "PB372 — the Phase-B discovery dossier is CITATION-KEYED, so it is structurally blind to the finding class that dominates adjudication: code that fails to implement a rule cites no clause. Fifteen batch-b1 subjects reported the same seven shapes, and one of them cost a landed row"
id: PB372
kind: analysis
status: open
severity: MINOR
area: tooling
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: []
inventory_rows: []
tags: [cobolsharp, work, analysis]
---

# PB372 — what the dossier could not show, measured across fifteen subjects

> Raised by adjudication batch b1 (OPEN · READ · RELEASE · RETURN · START · UNLOCK · USE, 184 rules across 15
> subject files, 2026-09-02). Every item below was reported INDEPENDENTLY by at least two subjects; the ones
> reported by five or more are marked ⛔. `kb/Work/PB286` (landed) owned the generator's slug defect and is not a
> live home for these; this note is.

## ⛔ 1. A citation regex cannot see an absence, and an absence is what a conformance review is for

The `source.citing` selector matches files that CITE the subject's clause. **Code that fails to implement a rule
cites nothing**, so the largest finding in a subject is routinely invisible to the dossier:

* `SequentialIoBinder.BindRead` never reads `readDirection()` / `readKey()` / `readInvalidKey()` — three dropped
  phrases, four inventory rows (**PB334**).
* `SequentialConnector.cs` implements no `Start` member at all — the whole sequential START arm (**PB352**).
* `SequentialIoEmitter.EmitFileRegistration` drops a record-less FD; `FileRegistry.RetryLoop` lives under
  §14.7.9 (**PB345**, **PB346**).
* `useStatement` is a plain alternative of `CobolParserCore.g4#statement` and `StatementBinder`'s
  `_ => new BoundUnsupported(...)` fallback carries no §14.9.49 citation, yet together they ARE the SR1 defect
  (**PB361**).
* Four of OPEN's nine syntax rules have NO code; establishing that took a hand sweep of every `BoundOpenMode`,
  `ReportNames`, `.Linage`, `EXTEND()` and `REWIND` site in `src/Cobol.Net.*` (**PB318**).

**Two mechanical probes were proposed by the adjudicators and both are buildable.** (a) For each grammar rule the
subject's statement references, name the bind-site token accessor that READS it, or report *no consumer found* —
which surfaces every dropped phrase without any citation. (b) For each rule, extract the nouns its text names
(REPORT clause, LINAGE clause, access mode, NO REWIND) and search for THOSE, not for the clause number.

## ⛔ 2. The tests list does not say which tests can CLOSE a row

Two distinct traps, reported by six subjects:

* **Legacy differentials read exactly like spec tests.** Every method in `KeyedIoDifferentialTests`,
  `UseDeclarativeDifferentialTests`, `FileIoDifferentialTests` and most of `InterProgramFileDifferentialTests`
  routes through `DifferentialGolden.Assert`, whose golden under `tests/differential/` is **baked from a legacy
  run**. Their XML docs quote real §/GR, so the dossier presents them as coverage. They are the ONLY tests
  covering START's GR8 and GR9.
* **A whole project cannot close anything.** `tests/CobolSharp.Tests.Integration/` (SpecFixTests, FileIoSpecTests,
  ConformanceTests, FileIOTests) tests the LEGACY engine and matches no `test-ref` form in
  `inventory-schema.json`. Four subjects lost time reading it, and one nearly recorded a ref from it.

**Fix:** mark every citing test with its ORACLE (spec-derived / differential-baked / legacy-project) and whether
its project is a legal `test-ref` form. The schema already knows the forms.

## ⛔ 3. `determinations` keys on a § citation, so the rows that DECIDE a verdict are missed

`docs/CONFORMANCE.md` §5's Annex A.4 module table is what makes a rule live or vacuous, and it names modules, not
clauses. Missed rows that decided verdicts in this batch: `A.4.7 File sharing and record locking | Claimed`
(makes OPEN SR8, READ GR22 and every UNLOCK rule in-scope), `A.4.3 Commit and rollback | Not claimed` (decides
OPEN SR7, READ SR5, UNLOCK SR2), `A.4.8 | Not claimed` (decides OPEN SR9), `A.4.11 Report Writer | Partial`
(decides USE SR10/SR11), `§A.3 item 40 SOURCE phrase | Not claimed` (decides four START rows), `§2 row 39 READ
PREVIOUS | Claimed`. A §2 row spelling `14.9.27` without the section sign also defeated the prefix regex.

**And a determination the dossier DID return was stale** — `docs/CONFORMANCE.md` line 111, *the only 07 setter is
CLOSE REEL/UNIT*, untrue since PB141 (**PB317**). A determination a verdict may rest on needs a freshness check
against the code it names.

## ⛔ 4. `register` matches on `spec_refs` only, so a note whose subject is a MECHANISM is invisible

Nine subjects reported an empty or near-empty register block that was misleading. Missed live notes: `PB236` and
`PB237` (the `BoundUnsupported`-as-runtime-throw shape, which four batch-b1 subjects then re-found), `PB159` (the
OPEN citation-drift note, which already owned two of the six wrong clause numbers found here), `PB142`, `A2` (the
SR-enforcement census, which already names ASSIGN USING), `PB140`, `PB179`/`PB183`, and `PB144` — whose TITLE
names the missing SOURCE grammar verbatim while its `inventory_rows` list only §14.9.10 rows.

**Fix:** a second selector over note TITLES and BODIES, and a clause-PREFIX match (`14.9.30.4` matching a note
whose `spec_refs` says `14.9.30`) rather than string equality.

## 5. Truncation is silent about WHAT it dropped, and it dropped the load-bearing rule

`grammar.truncated: N` and `source.citing.truncated: 63` appear with no list. Measured casualties: `readKey` and
`retryPhrase` (the two rules READ's Format-2 ordering defect turns on); `recordKeyClause` and
`alternateKeyClause` (which decide whether record-key-name-1 is writable at all); `lockModeClause` (without which
OPEN SR8's consequent cannot be checked); `ProcedureTableBuilder.DeclHandlerEndPc`, whose own doc comment cites
§14.9.49.4 and which carries this batch's widest USE defect. The cap also spends slots on NOISE — the bare
keyword `START` matched `COMMENT_START` and a comment body; `RETURN` matched every `RETURNING` phrase of CALL.

**Fix:** match rule/token NAMES with word boundaries; prefer rules REFERENCED BY the subject's top-level rule
over lexer tokens; make the cap per-file; and always name what was dropped.

## 6. Annex A.1 / A.3 back-references are not harvested, and an unmet documentation obligation is invisible

The standard's own rules carry them — *(14.9.27, OPEN statement, General rule 10)* — so they are mechanically
extractable. Missed in this batch: A.1 item 129 (OPEN GR10), 130 (GR18), 131 (GR23), 52/76/77 (file sharing),
218 (the USE fatal-I-O action), A.3 items 30/33 (NO REWIND), 39 (READ PREVIOUS), 40 (the SOURCE phrase). Without
them a verdict cannot tell an undischarged REQUIRED documentation obligation from a satisfied one — which is
exactly what **PB322** now owns for six of them.

## 7. Four smaller shapes, each reported twice or more

* **`diagnostics` is empty on subjects with live diagnostics**, because the selector keys on `DIAGNOSTICS.md`
  rows carrying the clause rather than on the clause cited at the `Error()` call site. Missed: COBOLNET0862,
  0863, 0864, 0899, 0900, 1512, 1720, 0715, 0716, 0859, 0897.
* **`tests/version-matrix/constructs.json` is invisible** although it is the SOURCE of the
  `ConstructRegistry.g.cs` rows the dossier DOES list — a citation fixed in the generated file is regenerated
  away (**PB320** item 1).
* **The rule extractor glues the NEXT format heading onto the previous rule's text** (`GR-...-3` ends *FORMATS 1
  AND 2*, `GR-...-13` ends *FORMAT 4*), so a reader who trusts the extract mis-assigns every rule's format scope
  by one. It also mis-segments §14.9.30.4 GR21 badly: `L4.3`'s text absorbs thirteen distinct normative rules —
  two divergent and eleven conforming — under ONE inventory row, which no single verdict can carry honestly.
* **The pinned worktree cannot render the PDF** (`scripts/render-spec-page.py` and `clause_page.py` resolve
  `specs-private/` relative to their own tree), and the figure notes cite the printed FOLIO while the script
  takes the PDF page — a 30-page offset that sends a reader to a search page.

## The one that cost something

Three of the four NEEDS-OWNER-DECISION adjudications assert that `inventory-schema.json` carries exactly ONE
derived selector. **It carries seven**, and `commit-and-rollback-only` had already NAMED and considered their
rows. The dossier's inputs were built from a worktree pinned before the A.4 landing, and nothing in the dossier
records WHICH tree it was built from. **Stamping the pin sha into every dossier is one line and would have
prevented all four.** (**PB371** records the consequence.)


## Batch b2 confirms all seven shapes and adds two — 2026-09-04

Twenty more subjects (EVALUATE · EXIT · GO TO · GOBACK · IF · INITIALIZE · MOVE · PERFORM · SEARCH · SET ·
SUBTRACT), **124 `dossier_gaps` reported**, and the distribution is the same one this note already describes.
The counts below are over those 124.

* ⛔ **The grammar list is TRUNCATED and the truncation hides the load-bearing rule — 20 reports.** The sharpest:
  EVALUATE's list was `truncated: 4`, and what it hid was `valueRange : valueOperand (THRU|THROUGH)
  valueOperand ;`, the code site for FOUR of that subject's nineteen rows. GO TO's list was truncated at 33 and
  most of what it DID return is noise from the two-letter keyword `TO` (`addToPhrase`, `setToValueStatement`,
  `screenToClause`) — a keyword-derived selector on `GO`/`TO` is a weak filter that spends its budget on
  coincidences. **Truncation is not a display limit here; it decides which rule the adjudicator can see.**
* ⛔ **`tests.named` counts an EMPTY TEST FILE as coverage — 4 reports, and it is the worst of them.** IF's only
  named test is `M413_OverlenientIfElseTests.cs`, whose three `[Fact]` bodies are empty; SEARCH's is
  `M421_OverlenientSearchTests.cs`, three more empty bodies. A file-name selector cannot tell a written test
  from a stub, so "the subject has a test" is exactly the wrong inference. `kb/Work/PB387` now owns the 45
  vacuous facts themselves; the dossier defect is that it presented them as evidence.
* **The test list cannot tell a SPEC-DERIVED test from a legacy-oracle characterization pin — 7 reports**, and
  that distinction decides every `test-ref` in the batch. INITIALIZE's headline test file asserts through
  `AssertSameAsLegacy → DifferentialGolden`; SEARCH's is five `[Fact]`s that all do. The inventory schema bars
  those from closing a row, so a dossier that surfaces them without saying what they are invites the one
  mistake the schema exists to prevent.
* **No OPEN register note owns any of the subject's findings — 9 reports.** EVALUATE surfaced PB39, PB45 and
  PB47 (all landed) and PB55 (open, but about row granularity); EXIT surfaced none at all, including for the
  highest-harm item in the subject. The selector answers "which notes cite this clause", which after a landing
  is a list of history.
* **The `determinations` list is empty on subjects that have landed determinations — 7 reports.**
* **The `diagnostics` list is keyed on `DIAGNOSTICS.md` rows rather than on the clause at the `Error()` call
  site — 7 reports**, so a subject with live diagnostics shows none.
* **Rule-row granularity: one rule split across `L`-rows by transcription indent — 4 reports** (this note's
  existing item; `PB55` owns it).
* **The pin sha is still not stamped into the dossier — 6 reports**, the item that cost a landed row in b1.

### The two shapes b2 adds

* **The dossier cannot say a cited clause is WRONG, so a live miscitation POLLUTES it.** IF's grammar list
  contains `generateStatement` for exactly one reason: `CobolReportWriter.g4` carries a banner citing IF's
  clause above the GENERATE production. The adjudicator had to notice the pollution by reading the rule.
  ⭐ **This one is now empty by construction** — the citation sweep `9c08e6fe` corrected the grammar banners,
  and both citation audits are at zero on today's tree — but the dossier would not have caught it either way,
  because a citation-keyed selector treats a wrong citation as a true membership fact.
* **The pinned worktree cannot render the PDF and the figure notes cite the printed FOLIO** while
  `render-spec-page.py` takes the PDF page — a 30-page offset that sends a reader to a search page. Reported
  in b1 as a smaller shape; in b2 it blocked figure work on four subjects, because lane 3's defects are
  disproportionately GENERAL-FORMAT defects and the rendered figure is the only authority for those
  (CLAUDE.md rule 1).

## Lane-3 batch b3 — 17 more dossiers measured (2026-09-04)

Every one of the 17 batch-b3 subjects reported dossier gaps, and they fall into FOUR shapes that recur across the whole batch rather than 17 unrelated complaints:

1. **A CITATION-KEYED SELECTOR IS STRUCTURALLY BLIND TO AN ABSENCE.** Where a rule has NO implementation nothing cites it, so the dossier's source list is empty exactly where the GAP is. Named independently by linage-clause (all four syntax rules — three NOT-IMPLEMENTED rows the dossier could not point at), data-description-entry-p1 (SR1/SR2/SR11/SR14, and it also hid the MISSING `ALIGNED` grammar rule and the missing `AS literal-1` phrase) and picture-clause-p1 (no diagnostic code exists for a format-1 PICTURE composition violation, and the absence is the signal).
2. **THE GRAMMAR SELECTOR RETURNS THE WRONG CLAUSE'S RULES.** Four subjects got FD/SD file-description productions instead of `dataDescriptionEntry` / `dataDescriptionClause` / `levelNumber`, because `linageClause`'s comment cited `ISO 13.16`. That citation was REPAIRED by PB159 in the live tree (`ISO 13.18.34`), so the dossier snapshot predates its own fix and a reader trusting it re-reports a landed repair.
3. **THE LINE NUMBERS ARE STALE.** blank-when-zero-clause (`blankWhenZeroClause` at :565 in the dossier, :660-663 in the tree) and linage-clause (DataBinder.cs off by ~25 lines, CobolData.g4 by ~19) both had to re-derive every code-location by grep. A dossier that ships offsets invites exactly the inherited-citation defect CLAUDE.md rule 1 is about.
4. **THE REGISTER AND DIAGNOSTICS SELECTORS RETURN EMPTY WHERE AN OWNER EXISTS.** linage-clause got an empty `register.notes` while EIGHT kb/Work notes match the LINAGE keyword (PB318 and PB322 bear directly on it); blank-when-zero-clause got an empty register while PB260 owns the declined SCREEN-SECTION arm of the same clause; data-description-entry-p2 found PB163's residue paragraph only by hand because it cites §13.18.27.3. The diagnostics selector keys on the ISO field text in docs/DIAGNOSTICS.md, so five of the six codes that actually adjudicate §13.16.3 (COBOLNET1542/1563/0881/1529/1708) were absent.

A fifth, narrower one worth fixing on its own: **the TESTS list does not separate a spec-pinned body from an oracle differential.** blank-when-zero-clause was handed `DecimalPointDifferentialTests.cs:124-137` as a §13.18.8 GR1/GR2 test; that body is `AssertSameAsLegacy`, which by the adjudication rules cannot close a row. A selector that offers a differential as a witness is worse than one that offers nothing.

The per-subject returns, verbatim:

### blank-when-zero-clause
- GRAMMAR list was mostly noise and MISSED the actual defect. The keyword derivation ('BLANK','WHEN','ZERO') pulled six PERFORM/EVALUATE WHEN-phrase rules out of CobolControlFlow.g4 and reported truncated:34, burying the one relevant rule; the lexer rule that carries the FMT-13.18.8.2 over-acceptance - src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4:722, `ZERO : 'ZERO' | 'ZEROS' | 'ZEROES' ;` - never appeared, because the sweep listed parser rules only.
- GRAMMAR line number is STALE: the dossier places blankWhenZeroClause at src/Cobol.Net.Frontend/Grammar/Core/CobolData.g4:565; in the pinned worktree it is at :660-663. Anything citing the dossier's offset without re-deriving it inherits a wrong line.
- SOURCE list is complete for lines that CITE 13.18.8 and therefore invisible to the code that implements these rules WITHOUT citing them - which is where the two defects live: src/Cobol.Net.Compiler/CodeGen/Emit/ConditionRenderer.cs:178-180 (the relation-condition routing GR3 needs and never reaches), plus src/Cobol.Net.Compiler/Binding/DataBinder.cs:1704 and :1743 (the SAME AS / TYPE BlankWhenZero merge and clone), src/Cobol.Net.Compiler/Binding/DataBinder.Reports.cs:431-432 and :536 (the Report Writer arm), src/Cobol.Net.Compiler/CodeGen/DataDivision/ValueInitializer.cs:238-244 and src/Cobol.Net.Compiler/CodeGen/DataDivision/GroupImageCodec.cs:89-93.
- REGISTER and DIAGNOSTICS came back EMPTY, yet two owner-owned items bear directly on this subject and a fixer must not re-litigate either: kb/Work PB260 (Annex A.4.2 screen handling - the SCREEN SECTION arm of THIS clause is declined, COBOLNET1560, witness tests/conformance/negative/a42-screen-blank-when-zero.cob), and the Phase-4a national-data residue (COBOLNET0899 / DiagnosticCatalog.NationalData, emitted at src/Cobol.Net.Compiler/Binding/PictureAnalyzer.cs:252), which is what makes the 13.18.8.3 SR2 national arm unreachable. Neither surfaced: the screen decline registers 13.18.8 inside a C# dictionary literal in ScreenFacility.cs rather than a citation comment, and the national residue cites 13.18.60.3.
- The LEGACY engine is outside the dossier's scope, and it is the only place these two syntax rules are implemented: src/CobolSharp.Compiler/Semantics/DataItemClassifier.cs:118-135 (CBL0802, CBL0804). A dossier scoped to src/Cobol.Net.* cannot show 'the rule exists but on the wrong arm', which is the actual state of SR-13.18.8.3-1/-2.
- TESTS list does not separate spec-pinned bodies from oracle differentials. It names tests/Cobol.Net.Tests.Conformance/DecimalPointDifferentialTests.cs:124-137 as a 13.18.8 GR1/GR2 test, but that method body is AssertSameAsLegacy (DifferentialGolden.Assert) - a legacy differential, which by the adjudication rules cannot close a row. The class's own summary even says 'differential against the legacy oracle EXCEPT where the legacy deviates', so the distinction is per-method and invisible from the dossier.

### data-description-entry-p1
- The DOSSIER's grammar list returned eight FILE-description rules (fileDescriptionEntry, sortMergeDescription*, linageClause, ...) and truncated 12 more; NOT ONE of the rules that actually decide these thirteen rows appeared -- dataDescriptionEntry, levelNumber, dataName, dataDescriptionClause, externalClause, genericDataClause. The citation regex keyed on the literal string 13.16 and the FD/SD rules happen to carry a stale `ISO 13.16` comment on linageClause (LINAGE is 13.18.35, not 13.16), so the selector returned the wrong subsystem entirely. I found the right rules by reading CobolData.g4 directly.
- A selector cannot report an ABSENCE, and four of the thirteen rows (SR1 level-number range, SR2 named-77, SR11 elementary-only, SR14 TYPE composition) have NO implementation and therefore no citing comment -- the dossier is silent exactly where the GAP is. The same blindness hid the missing ALIGNED grammar rule and the missing `AS literal-1` phrase of externalClause: both are things that are not there.
- The DIAGNOSTICS list carried COBOLNET0899/1549/1555/1674 but not COBOLNET1542 (ANY LENGTH, 13.16.3 SR17), COBOLNET1563 (DYNAMIC LENGTH, SR18), COBOLNET0881 (BASED, SR16), COBOLNET1529 (TYPEDEF, SR15) or COBOLNET1708 (the declined validation-clauses group, SR20) -- five of the six codes that actually adjudicate these rules. It is keyed on the ISO field text in docs/DIAGNOSTICS.md, so a code whose ISO field names only the CLAUSE's own subclause (13.18.2 / 13.18.19 / 13.18.5 / 13.18.58) is invisible even though its message quotes 13.16.3.
- The REGISTER returned only PB283 (A.4.14 VALIDATE, open, inventory_rows GR-13.16.4-4 -- a part-3 row). No open note owns any of the thirteen rows here, so all nine findings are new. Note also that PB283's TITLE is now stale on the point that matters to SR-13.16.3-20: it says the eight data-division VALIDATE elements are 'a generic parse error or the misleading COBOLNET0901', but DeclinedFacilityPass now refuses every one of them BY NAME with COBOLNET1708 and the negative goldens exist (tests/conformance/negative/declined-validate-*.cob). Someone should reconcile the note with the pin.
- The TESTS list did not surface tests/conformance/negative/based-level-05.cob (13.16.3 SR16), constant-record-level.cob / external-constant-record-*-at-2023.cob (SR13) or the declined-validate-* family (SR20) -- all of them spec-derived witnesses for rules in this part; they cite the CLAUSE subclause, not 13.16.3.

### data-description-entry-p2
- The dossier's grammar list returned only FD/SD/file-description rules (fileDescriptionEntry, linageClause, ...) and NOT the rules that actually carry this part's subject — `dataDescriptionEntry` (CobolData.g4:212), `dataDescriptionClauses`/`dataDescriptionClause` (:266/:270) and `renamesClause` (:603). Those three decide SR4 (clause ordering), SR8's RENAMES-subject leg and SR22 outright. `truncated: 12` says a cap cut the list; the keyword was derived from the prose word 'description', which matches every *DescriptionEntry rule in the grammar and is a poor selector for this subject.
- The dossier surfaced no register note for §13.16.3 SR6/SR7, yet kb/Work/PB163.md (status: landed) carries a 'Named residue' paragraph that owns part of the SR6 GLOBAL half — 'the 01/77 admission is looser than both SRs ... a clause-site screen belonging to the §13.18.27 checks, not this registration pass'. It was invisible to the citation regex because it cites §13.18.27.3 / §13.18.22.3, never §13.16. A residue paragraph inside a LANDED note is also not a work item any selector can rank; the SR6/SR7 screen currently has no OPEN owner.
- The dossier's `determinations` returned a single CONFORMANCE.md line about the VALIDATE facility. Nothing in it speaks to the eleven rules of this part, and in particular nothing records that §13.16.3 SR9 (the implied PICTURE) is unimplemented — the largest gap found here is undocumented in every determination surface.
- The DIAGNOSTICS rows shown (0899, 1549, 1555, 1674) cover the CONSTANT RECORD, SAME AS, report-item and PICTURE-LOCALE families. There is no diagnostic code anywhere in the catalog for 'an elementary item requires a PICTURE clause' (§13.16.3 SR8's positive half) or for the SR24 condition-name association rules — the absence of a code is itself the signal that the rules were never implemented, and the dossier gives no way to see it.
- No test surface at all was returned for SR3/SR4/SR5/SR22/SR23/SR24. I located conformance:negative/based-redefines-conflict, conformance:negative/constant-record-level, conformance:negative/program-pointer-with-picture and conformance-test:TypedefConditionTests.TwoRecordsQualifiedConditions_CompileClean by searching myself; the citing-tests selector missed all four because none of them writes the string '13.16.3' with the SR number this part needs.

### data-description-entry-p3
- The pre-computed GRAMMAR list is entirely off-target for this subject: all eight rules it returned are FD/SD file-description productions, pulled in because CobolData.g4's linageClause comment cites 'ISO 13.16' -- a STALE citation (13.16 is the data description entry; the LINAGE clause is 13.18.33). The productions that actually implement these three rules -- dataDescriptionEntry, dataDescriptionClause, groupUsageClause, valueClause -- are not in the list at any position. Two more stale 13.16 citations sit in the source list (DataBinder.Reports.cs:520 and DiagnosticCatalog.cs:618/COBOLNET0899 'A printable report item has no PICTURE clause', ISO 13.16) and one in VersionConformancePass.cs:423. Worth a sweep: 'ISO 13.16' used where 13.18.x was meant.
- The dossier's TESTS list returned nothing for these three rules, but two spec-derived goldens cover GR1 and GR2 exactly -- tests/conformance/2023/pb79_group_usage_bit.cob (SUB-LEN=4) and tests/conformance/2023/pb79_group_usage_national.cob (M2-LEN=2). They were invisible to the citation regex because their headers cite 13.18.29 / 15.50.4, the rules they were written for, and never 13.16.4. A rule implied by one clause and PINNED by a test written for another is systematically unreachable by this dossier's derivation.
- kb/Work PB203 (OPEN) already owns the crash reproduced here at probe/ddep3/a.cob and p18red.cob -- a GROUP-USAGE BIT group as a REDEFINES member emits uncompilable C# (CS0103 on every subordinate bit leaf). Deliberately NOT re-reported. New datum for that note if useful: it fires for a FLAT bit-group view as well as a nested one, and its national twin (REDEFINES + GROUP-USAGE NATIONAL, flat or nested) compiles and runs clean -- so the defect is specific to the bit substrate, not to REDEFINES over a group-usage view.
- No register note owns the level-88 defects in findings F2/F3/F4 (searched kb/Work for level-88, condition-name, 13.16.4, CopyEntryDescription and CloneItem). Nor does any note own the TYPE / SAME AS GROUP-USAGE drop in F1: PB79 (landed) built the clause, PB217 (landed) touched a GROUP-USAGE NATIONAL operand, and the only open 13.18.49/13.18.57 note is PB175, which is a semgrep-baseline process item.
- Adjacent staged residues met while probing, all loud and all outside 13.16.4's rows, none with a register owner found: (a) a THROUGH range over a NATIONAL conditional variable is COBOLNET0899 'recognized but not yet implemented (Phase 4a residue)' citing 13.18.63 SR31 -- that is a leg of GR3's 'range of values' that belongs to the VALUE clause's own rows; (b) SET condition-name TO FALSE throws NotImplementedCobolFeatureException at RUN TIME, after a clean compile, citing 14.9.39 SR7; (c) the `VALUE ... WHEN SET TO FALSE` phrase, which the compiler's own runtime message calls COBOL-2002+, compiles with no COBOLNET0900 introduction gate at --std 85 (probe p12c88.cob) -- I did not derive that 85 boundary from the 2023 text, so it is reported as an observation, not an adjudicated edition finding; (d) national data in a FILE SECTION record is COBOLNET0899 (Phase 4a, single-byte record codec), which blocked the FILE-SECTION leg of the GR2 probe -- LINKAGE was used instead.
- 13.16.3 SR24's row (a sibling part of this subject) has a measured defect I could not record because the row is not mine: an orphan `88 ORPHAN VALUE 1.` written where no conditional variable precedes it is silently discarded by DataBinder.cs ~L640's `if (stack.Count > 0)` guard and the program compiles clean, exit 0 (probe p15neg.cob). SR24 requires that condition-name entries 'shall immediately follow the entry describing the item with which the condition-name is associated'.
- Inventory cross-check, no gap found: 13.16.3 carries 24 numbered syntax rules in the spec against 23 in this batch's clause map, and 13.16.4 carries 4 general rules against 3 -- both differences are already-recorded rows (SR-13.16.3-19 CONFORMS; GR-13.16.4-4 DOCUMENTED-NON-SUPPORT, owned by PB283, the declined A.4.14 VALIDATE facility). Verified directly against tests/version-matrix/traceability-inventory.json.

### linage-clause
- register.notes was EMPTY, and eight kb/Work notes match the LINAGE keyword in the pinned tree (PB123, PB159, PB168, PB318, PB322, PB325, PB372, R26). Two of them bear directly on this subject: PB318 (open) owns four unimplemented OPEN syntax rules including the LINAGE conjunct of 14.9.27.3 SR2 and names the version-conformance pass - which already holds CheckWriteEopLinage for 14.9.51 SR19 - as the host for checks of exactly the shape SR-13.18.34.3-3 needs; PB322 (open) owns the OPEN-time determination whose behaviour it describes as 'a LINAGE file gets EvaluateLinage() + LinageCounter = 1 at OPEN OUTPUT completion per 13.18.34 GR6b1/GR7d'. The selector missed them because it keys on the clause citation, not on the construct keyword the notes actually use.
- diagnostics.entries was EMPTY, yet three diagnostics are of record for this subject and were found by reading: COBOLNET0863 and COBOLNET0864 (ExpressionBinder.LinageFileOf - the LINAGE-COUNTER qualification rules) and COBOLNET0899 (the generic receiving-shape catch-all that LINAGE-COUNTER falls into). A diagnostics selector that keyed on the CONSTRUCT rather than the clause number would have surfaced all three.
- The dossier is citation-keyed and is therefore structurally blind to the dominant finding of this subject: NONE of the four syntax rules has an implementation, so no file in the tree cites 13.18.34.3 and the dossier's source list contains nothing about them. This is precisely the shape PB372 (open) reports; this subject is another independent instance of it, and the cost here was three NOT-IMPLEMENTED rows that only a from-scratch search plus probes could find.
- The dossier's grammar entry quotes the linageClause comment as '(ISO 13.16)'. The pinned tree reads '(ISO 13.18.34)' at CobolData.g4:150 - PB159's citation-repair sweep landed that exact line (its note lists 'LINAGE 13.16 -> 13.18.34'). The dossier snapshot predates the repair, so a reader trusting it would re-report a landed fix.
- Line numbers in the dossier's source list are stale by ~25 lines for DataBinder.cs (dossier 996 / 1069 / 1154; pinned tree 1021 / 1094 / 1179) and by ~19 for CobolData.g4 (dossier 131/138/142/146; pinned tree 151/158/162/166). Every code-location in this return was re-derived by grep against the pinned worktree.
- source.citing was truncated by 8 lines and tests.citing by 2. The truncated source set matters: the sites that decide four of the six findings - SequentialConnector.cs:105 (_top/_bottom), :409 (Advance), ExpressionBinder.cs:662-697 (ResolveReceiving) and DataBinder.cs:1103-1105 (the first-word-only operand capture) - are absent from the list either because they were cut or because they cite no clause at all.
- The dossier's tests.named list is accurate but its silence hid the most useful fact about the test corpus: LinageConformanceTests' own class comment states that the observable surface it checks is 'the LINAGE-COUNTER register and the END-OF-PAGE branches, DISPLAYed to stdout'. No test in the tree inspects the physical stream of a LINAGE file, which is why a whole-clause defect (LINES AT TOP / LINES AT BOTTOM producing nothing) sits under a green suite.

### picture-clause-p1
- source.citing returned 40 lines over 48 files (capped at 200) but source.named surfaced exactly ONE file, PictureAnalyzer.cs. The sites that decide three of this part's rows are invisible to the clause-citation regex: DataBinder.cs:2667 (the `pic.DigitPositions > 0` guard - the SR14 lower-bound hole) cites 13.18.40.3 only six lines earlier in a comment, and Model/DataItem.cs:347/350 (IsGroup/IsElementary - the SR1 mechanism) cites nothing at all. A rule can also be invisible because its enforcement site DOES NOT EXIST, which is the case for SR1 and for every composition rule here; the selector cannot distinguish that from 'not searched'.
- register.notes returned nine notes, of which eight are LANDED and the ninth (PB283) is about VALIDATE. NO OPEN kb/Work note owns any 13.18.40.3 composition rule, SR1, or SR14's lower bound - the ten defective rows in this part are unowned and need new notes. (Cross-checked by grepping kb/Work for 13.18.40.3 and for 'precedence': PB13, PB155, PB175, PB275, PB3, PB39, PB66, PB156, PB158, PB195, PB370, PB46, PB91 - none of them.)
- tests.named listed 15 files but not the two suites that actually cover rows in this part: WideNumericTests (SR14's upper bound, at every edition) and LoudGuardTests / DataSkeletonEditionTests (SR2's membership half). Both were found only by grepping for the DIAGNOSTIC CODES (COBOLNET0801/0802/0808). A test that names the diagnostic rather than the clause is invisible to a clause-citation selector - which means the dossier systematically under-reports coverage for any rule whose test is written against a diagnostic code.
- diagnostics.entries returned four codes (1658, 1664, 1673, 1674), none of which is the diagnostic these rows need. There is NO diagnostic code allocated for a format-1 PICTURE composition violation (Table 10 / SR16-SR22 / SR24) and none for SR1 (PICTURE at a non-elementary level); the fix for findings 1 and 2 must allocate them.
- The subject's whole-subject clause map covers 13.18.40.3 (29 rows), .4 (12) and .5 (8) - but NOT 13.18.40.6, the Precedence rules, which is where SR2 delegates the entire notion of an 'allowable combination' and which supplies Table 10, Table 11, the 'es' precedence sentence and the comma/period interchange. A normative table carrying the authority for at least eleven adjudicated rows has no inventory row of its own in this subject.

### picture-clause-p2
- The dossier's `named` list held exactly ONE file (src/Cobol.Net.Compiler/Binding/PictureAnalyzer.cs), but four of this part's fourteen rules are decided somewhere else, and the clause-citation regex could not see any of them. (a) SR5 (PIC is an abbreviation for PICTURE) lives entirely in the LEXER -- CobolLexer.g4:622 `PIC : ('PIC' | 'PICTURE') -> pushMode(PICMODE) ;` -- which cites no clause. (b) SR7's whole mechanism is the PICMODE PIC_STRING trailing-','/';' trim (the W3 cure, DEVLOG 596, VERSION_CHANGE_REFERENCE row 7.14), also uncited; a reader trusting the dossier would have returned NOT-IMPLEMENTED on a rule that CONFORMS. (c) SR9's class half is decided (by omission) in DataBinder.DecodeEditLiteral, which cites 13.18.40.2, not .3. (d) SR6's constant-name leg is DataBinder.Constants.ExpandPicConstants, whose comment cites 13.18.40.2 and 13.10.3 SR2 -- again outside the .3 regex.
- The dossier's `grammar` section listed pictureClause / pictureLocalePhrase / editingPhrase / EDITING / dynamicLengthClause but NOT the PIC lexer token and NOT the PIC_STRING PICMODE rule, which are the two grammar objects that decide SR5 and SR7. A grammar dossier for a PICTURE subject that omits PICMODE is missing the half of the grammar that does the work.
- The `diagnostics` section returned only COBOLNET1658 / 1664 / 1673 / 1674 -- the float-edited and format-2 band. Every diagnostic actually load-bearing for THIS part is absent from it: COBOLNET1591-1596 (the SR8-SR12 EDITING band), COBOLNET1565/1566 (the NO SIGN / SR31 pair), COBOLNET0808 (the SR2 whitelist and the two-currency-symbols check that carries SR28), and COBOLNET0881 (the USAGE x PICTURE arm that carries SR30). The selector keyed on the 13.18.40 citation in DIAGNOSTICS.md's `iso` column, and those four rows cite 13.18.60 / 12.3.7 instead.
- The `register` section returned ten notes, nine of them landed, and NONE of them owns the missing 13.18.40.6 precedence/placement table walk -- even though PictureAnalyzer.cs:152-155 names it in the source as 'queued (adversarial-review minor, DEVLOG 595)'. I checked kb/Work directly: grep for 'Table 10' returns only PB66 (a float-edited note) and grep for 'precedence' returns only expression-precedence notes (PB156/PB158/PB195/PB370/PB46/PB91). Four of this part's rules (SR25/26/27/29) are unowned work that no register entry has ever carried.
- Rule-text harvest artifact, not a code issue: the text handed to me for SR-13.18.40.3-7 ends with the stray heading 'FORMAT 1' and SR-13.18.40.3-31 ends with 'FORMAT 2' -- the harvester glued the following section heading onto the last rule of each block. Harmless here (both rules' substance is intact and I verified each against the spec region), but a catalog that feeds rule text into diagnostics or goldens would carry the artifact forward.
- docs/COBOLNET_DATA_MODEL_DESIGN.md D21 lists 'SR4 (63 characters)' among the load-bearing anchors for the PICTURE model. No dossier section surfaces that claim, and the code has never implemented it -- a design doc asserting a rule the compiler does not have is the PB299 failure shape.

### picture-clause-p3
- The dossier's source.citing list was CAPPED (truncated: 200, 48 files, 40 lines shown) and source.named held one file (PictureAnalyzer.cs). The single most load-bearing file for the eight 13.18.40.5 editing rules - src/Cobol.Net.Runtime/Values/Numeric/CobolEdit.cs, which IS the format-1 editor (simple/special/fixed/floating insertion and both zero-suppression forms) and cites 13.18.40.4/.5 twenty times - fell past the cap and had to be found by hand. Three of the five findings live in that file. A cap that drops the implementing file makes an editing-rule dossier look thinner than the code is.
- The register selector returned no note for THREE open GAPs these rules run into, all of which are real work with no kb/Work owner: (a) the P14 PICTURE EDITING render GAP (multi-character literal / floating character-1, COBOLNET0899) - it lives only in a code comment and in two green tests that pin it; (b) the Phase-4a national / national-edited residue (COBOLNET0899) - tracked only inside plan section 0's 14g.5 sub-step ledger; (c) the missing 13.18.40.6 precedence-table validation - conceded in a PictureAnalyzer comment as 'queued (adversarial-review minor, DEVLOG 595)'. Under CLAUDE.md rule 8 each should be a kb/Work note; as things stand no work register can rank them.
- tests.citing returned 29 files but no SPEC-DERIVED golden for editing rules 3-7: the coverage over CobolEdit.Format is NumericEditedDifferentialTests / DecimalPointDifferentialTests / PictureScalingDifferentialTests, all legacy differentials, which the inventory schema itself disqualifies. That is why two silent wrong-answer defects (findings 1 and 2) survived - the legacy engine treats the same symbol sets the same way, so the differential is green on both.
- The diagnostics dossier listed COBOLNET1658/1664/1673/1674 (float-edited form, locale-name, format-2, SIGN-with-locale) but none of the codes these twelve GRs and eight editing rules actually fire: COBOLNET0808 (invalid picture symbol/combination), 0899 (staged not-implemented), 0900 (edition introduction gate), 0881 (usage/picture compatibility) and 1591-1596 (the EDITING SR8-SR12 family). The clause-citation regex found the codes whose catalog entry names 13.18.40, not the codes the clause's rules raise.

### sign-clause
- The dossier's grammar list gave signClause at CobolData.g4:551; in the PINNED worktree it is at :647 (and the dataDescriptionClause alternative at :276). Every line reference in this adjudication was re-derived from the pinned tree, not taken from the dossier.
- The dossier's grammar list was TRUNCATED (truncated: 1) and its keyword 'SIGN' pulled in five unrelated rules (noSignPhrase, currencySignClause, binarySign, functionCall/reservedIntrinsicArgFn, functionName). It MISSED the two sites that actually decide this subject: src/Cobol.Net.Frontend/Grammar/Core/CobolReportWriter.g4:94 and CobolScreen.g4:68, the other two general formats that admit signClause. Both were found by grepping signClause directly.
- The dossier's source list did not surface the CODE-SET enforcement of SR3 -- DataBinder.cs:3065-3082 cites §13.18.13.3, not §13.18.52, so the citation regex could not see it, and the single most harmful finding in this subject lives there. Nor did it surface CobolNum.cs:544-581 / :663-709, which implement GR5 and GR6 outright and carry no §13.18.52 citation on the lines that matter.
- register.notes and diagnostics.entries were both EMPTY, and both were wrong to be: COBOLNET1658 (the one SR1 shape that is enforced), COBOLNET1672 (SR3), COBOLNET1560 (the declined screen arm, owned by kb/Work PB260) and COBOLNET0899 (the national-numeric stage that makes SR2's `national` half unreachable) are all live in this subject.
- tests.named listed 28 files matched on the word SIGN, of which 22 concern the SIGN CONDITION (§8.8.4.7), the CURRENCY SIGN clause (§12.3.7) or the SIGNED/UNSIGNED usage phrase -- none of them this clause. The four goldens that actually exercise §13.18.52 (pb188_table_value_image_width, l1_usage_display_character_size, v59_length_agrees, pb9_reserved_intrinsic_names) were found by grepping the SIGN CLAUSE spellings over tests/**/*.cob.
- NOT A GAP BUT A MEASURED ABSENCE worth recording: no test anywhere in the corpus declares a GROUP-level SIGN clause, so GR1's inheritance, GR2's nested-group precedence and GR3's elementary precedence -- the three rules that make this clause more than a per-item flag -- have no test coverage at all.

### synchronized-clause
- The dossier's DETERMINATIONS list carried only docs/CONFORMANCE.md DOC-A.1-195 and did not show that the two OTHER Annex A.1 items cross-referenced from this clause have NO determination row at all: A.1 item 197 (SYNCHRONIZED positioning when neither RIGHT nor LEFT is specified - REQUIRED documentation, cites 13.18.55 GR3) and A.1 item 196 (how records of a file are handled - optional, cites GR11). Both exist in docs/rearchitecture/spec-rule-catalog.json (lines 39920 and 39931); neither appears in docs/CONFORMANCE.md, which carries 47 DOC- rows. Their content survives only inside DOC-A.1-195's paragraph, whose own heading is GR9. Two inventory rows (DOC-A.1-196, DOC-A.1-197) therefore look like clean gaps that are really 'answered in the wrong row'.
- The dossier's TESTS list is misleading in one direction that matters: tests/CobolSharp.Tests.Integration/DataDivisionSpecTests.cs:356+ carries two [Fact]s named Synchronized_TwoByteComp_AddsSlackByte_AffectsGroupLength and Synchronized_FourByteComp_AlignsGroupLength that ASSERT SLACK BYTES ARE INSERTED (SYNCLEN=0005 for X + S9(4) COMP SYNC + X, against PLAINLEN=0004). They are NOT tests of the compiler under adjudication - that project references src/CobolSharp.Compiler (the LEGACY oracle) - and their expectations are the exact contradiction of the shipped Cobol.Net determination (DOC-A.1-195, no alignment; measured here as 0004). They are therefore unusable as test-refs, and they mean the opt-in legacy differential (COBOLSHARP_LEGACY_DIFFERENTIAL=1) will disagree on every SYNCHRONIZED program by construction. Worth a line in the differential's known-divergence list if one exists.
- The DIAGNOSTICS section of the dossier was empty, but COBOLNET0900 (via ConstructRegistry / Constructs.SyncOnGroup2023) is this clause's only diagnostic and is central to SR-13.18.55.3-1's edition split; COBOLNET1702 (13.18.63.3 SR14) and COBOLNET1549 (13.16.3 SR13) are the two other codes that read DataItem.Synchronized. A citation-regex dossier cannot see them because they cite the CONSUMING clause, not 13.18.55.
- GR9 a) - 'the format on the external media of records or groups containing elementary items whose data description contains the SYNCHRONIZED clause' - is a REQUIRED, documented determination with NO shipped witness of any kind. It was measured here for the first time (probe syq_gr9a_media writes two SEQUENTIAL files differing only in the clause and gets byte-identical 41 00 01 5a), but nothing in the corpus would catch a regression. The shipped golden conformance:2002/l1_byte_length_implicit_filler measures GR9 b) only, through FUNCTION BYTE-LENGTH.
- No shipped spec-derived test writes the abbreviation SYNC (SR-13.18.55.3-2), nor SYNCHRONIZED LEFT or SYNCHRONIZED RIGHT (GR4/GR5), nor SYNCHRONIZED inside an OCCURS table (GR8), nor SYNCHRONIZED beside a SIGN clause (GR7). All were verified here by probe. Four of the fourteen rows close on reading + probe rather than on a corpus witness, and one added leg in tests/conformance/2002/l1_byte_length_implicit_filler.cob (the abbreviation, LEFT, RIGHT and a table occurrence) would close all four on a shipped test.

### usage-clause-p1
- THE DOSSIER'S GRAMMAR SNAPSHOT IS STALE relative to the pinned worktree and would have produced two wrong verdicts if trusted. It shows `usageClause : (USAGE IS?)? usageKeyword binarySign? noSignPhrase?` and a `usageKeyword` with no dataPointerUsage arm; the pin (src/Cobol.Net.Frontend/Grammar/Core/CobolData.g4:452-454, :494-524, :530-533) has `... noSignPhrase? floatFormatPhrase*` and `dataPointerUsage : POINTER (TO cobolWord)?`. kb/Work PB174 (the per-item endianness phrase) and PB153 (the restricted data-pointer, which is SR18's whole subject) have both LANDED since the snapshot.
- THE REGISTER SNAPSHOT LISTS kb/Work PB183 AS `status: open` with the title '13.18.60.3 SR14 is enforced NOWHERE'. At the pin, kb/Work/PB183.md carries `status: landed` and `inventory_rows: [SR-13.18.60.3-4, SR-13.18.60.3-14, SR-13.18.60.3-15]`, and src/Cobol.Net.Compiler/Binding/DataBinder.UsageDeclaration.cs implements all three with six negative goldens and two positive ones. Three of this part's twenty rows would have been mis-adjudicated from the dossier alone.
- kb/Work/PB213.md's FRONTMATTER AND BODY DISAGREE at the pin: `status: open`, while the body's first line reads 'Found and CLOSED by the PB169-PB172 burn-down cluster (2026-08-31)' and names the goldens and the unit test that pin it. Both of its repros -- `COMPUTE N = IDX + 1` and `MOVE E(IDX) TO R` -- reject under strict at this pin (probes u_sr10_arith, u_sr10_sub). The status field wants flipping; until it is, `work.py next` ranks a closed defect.
- THIS SUBJECT HAS NO FMT ROW. part.whole-subject-clause-map is {13.18.60.3: 20, 13.18.60.4: 29} -- 13.18.60.2, the general format, is not in the inventory for this subject at all. Three general-format gaps measured here therefore have no adjudicated home: the MESSAGE-TAG alternative is absent from usageKeyword (findings F7), and the OBJECT REFERENCE alternative is missing ACTIVE-CLASS, interface-name-1 and the `[ONLY]` tail (finding F9). Each of them makes a 13.18.60.3 syntax rule unadjudicable (SR21, SR16) or partly unverifiable (SR4, SR14).
- THE DETERMINATIONS LIST SURFACED ONLY GR-LEVEL REPRESENTATION ROWS (CONFORMANCE.md A.1 items 205-211, 215 and the 13.18.60.4 GR4/GR5/GR11/GR12 documentation). Nothing in it bears on any 13.18.60.3 SYNTAX rule -- which is silence, not evidence: no A.1 item is required for a syntax rule. Recorded so the next reader does not take the empty intersection as a finding.
- THE tests/named LIST MISSED THE ACTUAL COVERING TESTS for four of this part's rows. It lists UsageDataEditionTests, BinaryUsageDataTests, pb95_bare_usage_keywords and the pb79 group-usage fixtures, but not tests/conformance/negative/pb183-usage-{pointer-in-group,objref-in-group,progptr-in-group,pointer-in-weak-typedef,pointer-same-as,pointer-group-usage,objref-file-section,index-constant-record}, not tests/conformance/2023/pb183_usage_{index_in_group_ok,pointer_placement_ok}, not tests/conformance/negative/pb153-pointer-to-type-without-typedef and not tests/conformance/2023/pb153_restricted_pointer -- i.e. every spec-derived witness for SR4, SR14, SR15 and SR18. They were found by direct search of tests/conformance/negative and tests/conformance/2023, not from the dossier.
- A DEFECT FOUND HERE BELONGS TO A CLAUSE OUTSIDE THIS SUBJECT AND HAS NO ROW IN THIS BATCH: 13.18.57.3 SR8 (the TYPE-clause twin of SR15) is unenforced -- `FD F. 01 R TYPE OT.` over a WORKING-STORAGE `01 OT IS TYPEDEF USAGE OBJECT REFERENCE.` compiles clean (finding F8). Likewise 14.9.25.3 SR1's class-pointer arm (finding F5) and 14.9.20.3 SR1's class-index arm (finding F6). They are recorded as findings against the SR rows that led to them, but the owning rows are in other subjects.

### usage-clause-p2
- DOSSIER GRAMMAR IS STALE AND WOULD HAVE PRODUCED A FALSE FINDING. The dossier's `usageClause` text is `(USAGE IS?)? usageKeyword binarySign? noSignPhrase?`; the PINNED tree at Core/CobolData.g4:451 reads `(USAGE IS?)? usageKeyword binarySign? noSignPhrase? floatFormatPhrase*`, and the fragment list omits floatFormatPhrase, encodingPhrase, endiannessPhrase and dataPointerUsage (usageKeyword in the pin also carries `dataPointerUsage`, not a bare POINTER token). Adjudicating GR19/GR20 from the dossier alone would have reported the per-item endianness-phrase as unparseable, which is false.
- DOSSIER REGISTER STATUSES ARE STALE FOR TWO NOTES. It lists PB174 (the per-item endianness-phrase) and PB183 (the 13.18.60.3 SR14 screen) as `status: open`; both read `status: landed` in the pin's kb/Work/PB174.md and kb/Work/PB183.md, and both are demonstrably landed in the pinned compiler (probes p18/p29/p30 for PB174, COBOLNET1724 in probes p28/p37/p39 for PB183). PB213, PB215 and PB306 are genuinely open and were referenced rather than re-reported.
- DOSSIER TESTS LIST MISSED THE CLOSING EVIDENCE FOR HALF THIS PART. Its `tests.citing` list is truncated at 69 and its `tests.named` list does not carry tests/conformance/2014/float_binary.cob, tests/conformance/2023/pb174_float_item_endianness.cob, tests/conformance/2023/pb174_float_item_endianness_default.cob, tests/conformance/2023/pb164_index_group_image.cob, tests/conformance/negative/pb148-display-index-item.cob or tests/Cobol.Net.Tests.Conformance/FloatFamilyTests.cs - i.e. the covering tests for GR10, GR14, GR15, GR16-18 and GR19. They were found by searching the tree directly.
- ⛔ BATCH-WIDE PROCESS DEFECT, NOT ABOUT THIS SUBJECT: the sibling checkpoints already written into this same out-b3 directory (adjudicate-picture-clause-p2/p3, adjudicate-data-description-entry-p1/p2/p3, adjudicate-sign-clause, adjudicate-blank-when-zero-clause) record `editions` as PROSE - e.g. "85/2002/2014/2023 (the usage-national arm exists only from COBOL-2002 ...)". scripts/spec/record_verdicts.py:116-118 splits `editions` on ',' and rejects any token not in the schema's {85,2002,2014,2023}, PER RECORD, so every one of those rows will be dropped from the merge and the GAP will not move for them. This part's checkpoint uses the strict comma form and carries the edition prose in `notes` instead. The registrar should re-shape the sibling files before merging and check the GAP delta against the expected row count.
- EDITION DERIVATION IS ONLY PARTLY SPEC-BACKED, AND kb/Work A1 (status: open) OWNS THAT. Annex E of the in-repo 2023 standard covers 2014->2023 only, so it confirms exactly one introduction in this part: E.3.2 item 5 for PACKED-DECIMAL WITH NO SIGN (2023). The 2002 introductions (BINARY-CHAR/-SHORT/-LONG/-DOUBLE, FLOAT-SHORT/-LONG/-EXTENDED, OBJECT REFERENCE) and the 2014 ones (FLOAT-BINARY-*, FLOAT-DECIMAL-*, the endianness- and encoding-phrases) are NOT derivable from this document; the `editions` fields record the conventional introductions, which the compiler's gates agree with (probes p44-p48 at 85/2002/2014), and A1 is the open analysis note that must settle them from a historical standard.
- NO kb/Work NOTE OWNS EITHER NEW DEFECT CLUSTER. Searching the register for the group-usage inheritance shape returns only PB79 and PB183 (both landed, both about other rules), and searching for ACTIVE-CLASS returns PB285, PB365 and PB366 (about A.4.10's optional OO items, USE Format 4 operand resolution and factory declarative selection). The three findings in findings-usage-clause-p2.json need new notes.
- ONE ADJACENT SPEC OBSERVATION, OUTSIDE THIS PART'S ROWS: 13.16.3 SR8's picture-less usage list names float-short/-long/-extended but NOT float-binary-32/-64/-128 or float-decimal-16/-34, so a literal reading makes a PICTURE clause REQUIRED on a standard-float item. The compiler treats the whole float family as picture-less and refuses a PICTURE with COBOLNET1521, which is the only coherent reading beside GR14-GR18's pinned formats and is what the surveyed implementations do - but the row that owns SR8 is in the data-description-entry subject, not here, and should record the reading rather than inherit it silently.

### usage-clause-p3
- The dossier's `determinations` list quotes docs/CONFORMANCE.md line 838 as `DOC-A.1-209 | **USAGE BIT clause — alignment and representation of data**, §13.18.60.4 GR5 + §8.5.1.6.3`. In the PINNED tree that row is at line 978 and reads `DOC-A.1-209 | **USAGE DISPLAY — size and representation of characters**, §13.18.60.4 GR7` — the misfiling was corrected between the dossier build (2026-09-02) and the pin (89044aa0), and the spec's own A.1 confirms 209 is USAGE DISPLAY with no A.1 item existing for USAGE BIT at all. Anyone reading the dossier alone would have adjudicated GR5 and GR7 against the wrong determination rows.
- `source.citing` reported 39 files / 40 lines with 217 TRUNCATED, and `tests.citing` 42 files with 69 truncated — so more than 80% of the citing sites were invisible. Everything load-bearing for this part was found by reading the code directly: DataItem.ElementaryByteWidth / IsCharacterImage / ImageWidth (the GR8 two-width split), SetBinder.BindSetPointer (the GR23 third arm), CobolParserCore.g4 setEntryStatement (the GR24 vendor spelling), and CobolData.g4 dataDescriptionEntry (the GR9 swallow). None of the four is in the dossier.
- The dossier's `register` carries 7 open notes, none of which owns any defect found here. It does NOT carry PB153 (landed — the restricted-pointer model whose third arm F4 names), PB188 (landed — the exact wrong-width twin of F2), PB231 or PB327 (open — the national BASED and FILE channels), all of which had to be found by grepping kb/Work directly. A register selector keyed on the clause number cannot see a note whose title names the mechanism instead.
- `diagnostics` listed 9 entries and none of the codes that actually fire on these rules: COBOLNET0869 (the pointer-operand band carrying every restricted-pointer screen), COBOLNET0813, COBOLNET0826, COBOLNET0881, COBOLNET0900, COBOL0001/COBOL0201/COBOL0307. The 0899 rows it did carry were accurate.
- No entry anywhere in the dossier points at Annex A.1 as the place a USAGE general rule's implementor obligation is enumerated, although six of this part's ten GRs end in a 'shall specify / defines the precise effect' sentence whose A.1 item is REQUIRED and whose DOC row is missing (210, 212, 213, 214, 216, 217). The A.1 <-> GR mapping is mechanically derivable from the spec ('(13.18.60, USAGE clause, General rule N)' is printed on every item) and would be worth adding to the dossier builder.

### value-clause-p1
- THE DOSSIER NEVER NAMED THE SITE THAT DECIDES FOUR OF THESE ROWS. `source.citing` truncated 106 lines and returned only comment hits; the enforcement bodies -- DataBinder.cs#ValidateTableValues (:3205-3243, the SR18/SR20/SR21/SR22 site), #ValidateValueCategory (:1249-1341, the SR10 site) and #BindCondition (:2103-2158, the SR24/SR26 site) -- were reached only by reading DataBinder.cs directly. Its silence was not evidence.
- SR25's HOME IS INVISIBLE TO A CLAUSE-CITATION SELECTOR BY CONSTRUCTION. The block where the rule belongs, DataBinder.cs:715-734, cites 13.18.15.3 SR1, 13.16.3 SR13 and 13.18.40.3 SR32 -- three OTHER clauses -- so no regex over '13.18.63' could ever surface it. A rule that is missing from a block is invisible to a dossier keyed on the rule's own number; only reading the block's neighbours finds it.
- NO DIAGNOSTICS ROW FOR THE FORMAT-2 SUBSCRIPT BAND. The dossier's DIAGNOSTICS list (9 entries) has nothing for COBOLNET1585/1586/1587/1588, which ARE the enforcement for the Format-1 single-literal rule and for SR20/SR21/SR22; those codes are raised as bare string literals in DataBinder.cs rather than through DiagnosticCatalog descriptors, so they carry no catalogued ISO reference to be found by.
- EDITION ATTRIBUTION IS NOT DERIVABLE FROM THIS SPEC FOR THREE CONSTRUCTS. Annex E lists changes from the previous standard only, and carries no item for the introduction edition of the Format-2 (table) VALUE clause, of the CONSTANT RECORD clause, or of the OCCURS DYNAMIC phrase. The 2002 column on SR16-SR21 and SR25, and the 2014 column on SR22/SR23, are therefore taken from the CONSTRUCT's own gate in the compiler (VersionConformancePass.ParseArm.VisitValueClause / VisitConstantRecordClause; 13.18.38 Format 4) and NOT derived from the standard -- flagged rather than asserted. Annex E DOES settle SR11's edition: E.3.3 item 43 and E.2 item 29 make the numeric-literal route for a numeric-edited item a 2023 introduction.
- TWO OPEN REGISTER NOTES OWN PART OF WHAT THIS PART MEASURED BUT CARRY NO INVENTORY ROWS. kb/Work PB206 (SR13 sentence 1 + the group-SIZE limit) and PB208 (the Format-2 literal-screen bypass) both have `inventory-rows: []`, so neither can close SR-13.18.63.3-13 or -10/-16 by reference; findings F6 and F7 extend them with the directions and the measured wrong answers they do not name (the NATIONAL-group direction; SR10 losing the same way SR2 does). PB260 and PB283 DO carry rows, but the rows they name (SR-13.18.63.3-15, -37) are outside this part.
- CROSS-PART DEPENDENCY THE MAP DOES NOT SHOW. Finding F10 is anchored on SR-13.18.63.3-11 (this part) but the row it primarily bites is SR-13.18.63.3-5, which the clause map assigns to a sibling part; the reconciler should route it there as well. Likewise SR16 pulls in SR15, whose carrier (the screen section) is an owner-declined module, so this part's SR16 verdict is capped by a decision recorded outside it.
- NO SPEC-DERIVED WITNESS EXISTS FOR SIX ROWS AND ONE GREEN TEST ACTIVELY HIDES FOUR. SR12, SR16, SR17, SR18, SR19, SR23, SR25 and SR26 all return an empty test-ref; ValueFormat2Tests.MultiDimension_StagedGap0899 is GREEN and pins the COBOLNET0899 STAGE that stands in for SR18/SR20-count/SR22-subordinate/SR23, so from the test side those four look covered. It is deliberately NOT recorded as a witness for any of them.

### value-clause-p2
- ⛔ THREE OF THIS PART'S SIXTEEN ROWS WERE ALREADY CLOSED BEFORE THE BATCH WAS BUILT. tests/version-matrix/traceability-inventory.json carries SR-13.18.63.3-36, -38 and -39 with verdict DOCUMENTED-NON-SUPPORT, state OK, code-location DeclinedFacilityPass.cs#VisitValidateValidPhrase and test-ref conformance:negative/declined-validate-value-format5 -- signed by the owner's declined-facility decision (kb/Work PB375 / PB198, docs/CONFORMANCE.md 4 item 3). The input file's rule list is derived from the rule CATALOG and is not filtered against the inventory's closed rows, so 19% of this part re-adjudicated settled work. The three records returned here restate the existing closure and must not overwrite it.
- The `grammar` block is capped (`truncated: 20`) and what it DID return is keyword noise: usingByValue, callByValue, HIGH_VALUE, valueOfClause and initializeCategoryToValue all matched on the word VALUE and none of them is the VALUE clause. `valueItem` -- the production that decides whether a THRU range may appear at a non-88 level, and therefore the whole of SR33's defect (finding F3) -- is absent from the returned list; it was found by reading src/Cobol.Net.Frontend/Grammar/Core/CobolData.g4:641-644 directly.
- The `source.citing` block is capped at 40 of 146 lines (`truncated: 106`), and NONE of this part's three decisive code sites survived the cut although all three carry a 13.18.63 citation: DataBinder.cs:2458-2472 (the only SR9 enforcement site), DataBinder.cs:2120-2130 (the only SR29 and national-THROUGH site) and ReportWriterEmitter.cs:129-159 (the Format-4 editing defect). A verdict taken from the returned list alone would have read NOT-IMPLEMENTED on SR9 and SR29, both of which are implemented.
- NOTHING in the dossier reaches the report-section (Format 4) VALUE lane, which owns three of this part's rows (SR30, SR34, SR35). DataBinder.Reports.cs appears only at lines 160 and 439, and the one ReportWriterEmitter.cs line returned (:125) is cited for 13.18.53.4, not for the VALUE path. All three rows were opened by reading the report binder and emitter from scratch.
- NO REGISTER NOTE OWNS THE FORMAT-3 MODEL GAP. The three open VALUE notes -- PB206 (group-level VALUE size limits), PB207 (bit-packed group VALUE area) and PB208 (Format 2 table VALUE dropped for an image-stored leaf) -- are all about group-level or Format-2 VALUE. Neither alphabet-name-1 nor the WHEN SET TO FALSE literal-4 has an owner, and they are the cause of three rows here (SR27, SR31, SR32). PB206 DOES already own the SR5 sentence-3 leg measured in this part (the national-group size truncation), which is why that one is referenced rather than re-filed.
- A 4.2.16 DOCUMENTATION OBLIGATION WITH NO STATEMENT. 14.7.8 rule 2 says in terms "When there is no IN alphabet-name phrase specified for the range, the collating sequence is defined by the implementor" (cite --check OK), and this compiler's choice -- the PROGRAM COLLATING SEQUENCE, measured at c32_prog_collate -- is a real implementor definition. grep over docs/CONFORMANCE.md for `14.7.8`, `THROUGH phrase` and `collating sequence is defined by the implementor` returns NOTHING, and the dossier's `determinations` block returned exactly one row (DOC-A.1-82, the floating-point exponent range). Recorded as a gap rather than a finding because I did not sweep CONFORMANCE.md exhaustively -- but if there is no such row, SR32 carries an undocumented latitude as well as an unimplemented leg.
- THE DIAGNOSTICS BLOCK CARRIES A CITATION THIS PART CONTRADICTS. docs/DIAGNOSTICS.md:34 attributes the `national-through-range` decline to "ISO 13.18.63 SR31", and DiagnosticCatalog.cs:486 repeats the string. SR31 is about alphabet-name-1 and PRESUPPOSES that national THROUGH ranges are legal -- see finding F8. A dossier row is a map entry, not evidence, and this one is wrong.

### value-clause-p3
- The dossier's `source` selector is a §13.18.63 citation regex, so it cannot see code that realizes a §13.18.63 general rule but cites the STATEMENT clause instead. Two of this part's four wrong-answer mechanisms live in exactly such files and appear nowhere in the dossier: src/Cobol.Net.Compiler/CodeGen/Verbs/SetEmitter.cs (EmitSet — the SET condition-name TO TRUE store, cites §14.9.39) and src/Cobol.Net.Compiler/Binding/Procedure/Verbs/SetBinder.cs (the TO FALSE refusal, cites §14.9.39 SR7). A GR whose effect is delivered by another clause's verb is structurally invisible to this derivation.
- Same shape, third mechanism: src/Cobol.Net.Compiler/Binding/Procedure/Verbs/InitializeBinder.cs is absent from the dossier (it cites §14.9.20), yet it is the ONLY place §13.18.63.4 GR2, GR3, GR4 and therefore GR11 are observable at all — those rules say the VALUE takes effect during INITIALIZE, so the INITIALIZE binder IS their implementation site.
- src/Cobol.Net.Compiler/Binding/Model/Condition88.cs and DataBinder.BindCondition (DataBinder.cs:2105) carry the Format 3 VALUE set — the subject of GR17-GR20 — and neither is in the dossier's `source` or `named` lists; BindCondition appears only obliquely through unrelated §13.18.63 comments elsewhere in the same 3000-line file.
- The dossier's `diagnostics` section lists 8 entries with truncated:0, i.e. it reports itself complete — but four shipped, user-visible diagnostic codes in this subject have NO row in docs/DIAGNOSTICS.md at all and so could not appear: COBOLNET1586 and COBOLNET1587 (the Format 2 FROM/TO subscript screens, cited to §13.18.63.3 SR20/SR21), COBOLNET1588 (the SR22 no-TO-on-an-unbounded-dynamic screen) and COBOLNET1528 (the GR16 staged capacity derivation). All four were observed firing in this adjudication. That is a docs gap under CLAUDE.md rule 6, and it is worth a note of its own.
- The dossier's `register` list is accurate about what it returned but says nothing about the largest hole found here: NO kb/Work note covers `SET condition-name TO FALSE` / the `WHEN SET TO FALSE` phrase (verified by grepping kb/Work directly), and none covers the discarded `IN alphabet-name-1` phrase either. Both needed to be found by reading the binder, not by reading the dossier.

### value-clause-p4
- The code that actually realizes GR4's 'during the execution of an INITIALIZE statement' occasion -- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/InitializeBinder.cs (InitializeSender at :212, the pointer arm at :158) -- cites 14.9.20 and never 13.18.63, so the dossier's clause-citation regex could not return it. It is the site of this part's largest wrong-answer finding.
- The Format-4 implementation site -- src/Cobol.Net.Compiler/CodeGen/Verbs/ReportWriterEmitter.cs:128-160 FieldImage, which composes every report-section VALUE -- carries no 13.18.63.4 citation of its own (only the :125 doc comment naming 13.18.53.4 GR1 was returned), so GR21/GR22/GR23's implementation was invisible to the selector.
- src/Cobol.Net.Frontend/Grammar/Core/CobolWords.g4:106 (`| VALID`, unguarded) carries no clause citation at all and is not a named grammar rule, so neither the grammar list nor the citation regex could surface it -- yet it is what defeats the Format-5 decline.
- Two spec-derived goldens that DO cite this clause were absent from tests.citing (which reported 22 files and 7 truncated names): tests/conformance/2014/external_constant_record.cob (13.18.63 GR4a) and tests/conformance/2023/pb152_options_initialize_background.cob (13.18.63.4 GR4 c). tests/conformance/2002/table_value_occurs.cob -- the one covering golden this part cites -- appeared only in tests.named, not in tests.citing, even though its first line quotes 'ISO 13.18.63.4 GR 9'.
- The determinations list returned exactly one line (DOC-A.1-82, floating-point literal exponents), which is unrelated to any rule in this part; the GR4c-licensed initial-content determination that IS recorded (COBOLNET_DATA_MODEL_DESIGN D23 / CONFORMANCE.md, quoted in the traceability inventory's own notes for the OPTIONS INITIALIZE rows) never surfaced.
- No register note in the dossier owns the report-group OCCURS refusal (COBOLNET0899 at DataBinder.Reports.cs:434-437) or the multiple LINE refusal beside it, and a kb/Work grep found none -- the two rules in this part that depend on that vehicle (GR21's GR9 import, GR22's OCCURS-DEPENDING arm) had no visible owner until this pass.

## Registrar 14a — dossier gaps
**Batch d1-14a dossier gaps (23 subjects, environment division + file control):** the citation-keyed selector missed,
again, the sites that matter most — every one of these was found only by reading: `DataBinder.ResolveKeyCollating`
(cites §12.4.5.7.4 in prose), `FixedFileAttributes.Fingerprint`, `OoDriver.OoReparentClassData/OoReparentFactoryData/
BindInterfaceData`, `DataBinder.Switches.ResolveProgramCollating` and `SwitchBindSymbolic/LiteralPhraseOperand`
(rule ids only inside message strings), `PictureAnalyzer`'s currency-symbol resolution, `PrototypeUnitRules`, the
runtime collation carriers and the record codec, `DataBinder.InheritConfiguration`. Keyword matching added noise
instead (SAME → sameAsClause/callByContent; LOCK/MODE → paddingCharacterClause; 'general' → *_general_format tests;
'control' → Report Writer CONTROL). Rules with NO enforcement site (§12.4.5.8.3 SR1-SR4, §12.4.5.2 SR2-SR4,
§12.4.6.4.3 SR2-SR10, §12.4.5.5.2 SR1, §12.3.5.4) are invisible to a citation regex by construction — the dossier
should say 'no citing site' explicitly rather than list neighbours. Also: no derived-verdict selector covers Annex
A.3 item 40 (PB1099 Q1), and DIAGNOSTICS.md lists COBOLNET1584 as a Warning though it is emitted as an Error (PB1074).

## Extended 2026-09-24 by REGISTRAR 14b (adjudication batch d1-14b)
Batch d1-14b's 23 subjects reported 97 dossier gaps; the same citation-keyed blindness dominates: code that implements a rule WITHOUT citing it (PictureAnalyzer's scale arms for §14.6.8.2 r3; SortEmitter's FILL for §14.9.24.4 GR2/GR13; CobolDynTable.FromCurrentImage for §14.6.9.2) and code that FAILS to implement a rule (IsBareInspectOperand, CobolTable.Occ(string), the ReportWriter CODE staging, EC-RANGE-INSPECT-SIZE, EC-DATA-INCOMPATIBLE rules 5/6, the >>PROPAGATE processor) never surfaced; register notes that own a mechanism under a DIFFERENT clause (PB546 under 13.18.60.4, PB428 under 8.4.3.4, PB994/PB995/PB1035 under 14.9.40) were missed; the citing-source list was truncated at 333 lines; and one listed golden (`tests/conformance/2023/goback_raising_unchecked_activator.g.cs`) does not exist in the pinned tree (a stale entry). The dossier should key a register note by the MECHANISM's code site as well as its clause.

## Batch d1-14c (registrar 14c, 2026-09-24) — the same blindness, 22 subjects

The d1-14c adjudicators reported dossier gaps in 22 of 22 subjects, and they are this note's shape again: sites that
implement a rule WITHOUT citing its clause are invisible to the citation regex. Measured instances: the MCS
RECEIVE/SEND binding arm (`StatementBinder` `BindUnsupportedFacility`, cites no §14.9.31/§14.9.38); the lexer
subscript-trigger set (`CobolLexer.g4` OnDefaultLParen / `CobolLexerWordSet.g.cs`) that decides whether `RETRY (N)`
parses; `UdfBinder.UdfReturningResidue` (the FUNCTION-ID RETURNING lane); `IndexedConnector.IsSuppressed` and the
SUPPRESS WHEN decode (cite §12.4.5.6.4, not §14.9.35/§14.9.51); `DataBinder.BindIoControl` (drops SAME AREA, cites
no §14.9.40); `StatementEmitter.Visit(BoundCommitRollback)` (cites PB137, not §14.9.36); `ProgramTable.RunMain` (the
§14.6.11 CloseAll STOP reaches); the scope-terminator rules (§14.5.3, implemented STRUCTURALLY by the grammar and
cited nowhere). A second recurring gap: the dossier's test lists do not separate `AssertSameAsLegacy` differentials
(which never close a row) from spec-derived tests (string, unstring, rewrite subjects). A third: the register list
misses open notes whose rows omit the subject's row (PB812, PB259, PB397, PB1036, PB374, PB863, PB387) — a register
search by CODE SITE, not only by `inventory_rows`, would have surfaced them.

## Batch d1-14d dossier gaps (appended 2026-09-24 by REGISTRAR 14d)

The same citation-keyed blindness, measured again on 23 data-division subjects. Sites that implement a rule WITHOUT citing it were missed every time: MaterializeImpliedRecord (the mechanism of PB1219), ProgramEmitter's LOCAL-STORAGE re-init loop and OoEmitter.PtrActivationSeed (PB1214), DataBinder.ConformanceForest (PB1212), InitialStateBackground.FillFor / PicInfo.DefaultInitializer (PB1233), ReportWriter.TerminateCore (cited only as §14.9.46.4, although §13.18.16.4 GR5 governs it — PB1235), DataItem.DisplayTextWidth and ReportWriter.NewLine (COLUMN GR3/GR10). Truncation cut the ANY LENGTH SR1 check site (PB1210). Grammar hits for 'CONTROL' were FILE-CONTROL / I-O-CONTROL noise. Two gaps are TOOLING defects rather than dossier gaps and belong to their owners: `audit_grammar_optional_words.py` (PB332/PB695) did not flag constantEntryBody's REQUIRED `AS` although §13.10.2 does not underline it (PB1225); and `COBOLNET_REPORT_WRITER_DESIGN.md` §5 records COLUMN SR4/SR5/SR7/SR8a/SR10 and the EC-REPORT-* seams as 'not enforced' in design-doc PROSE outside the register (now PB1221/PB1222/PB1223 — the design doc should point at them). Dossier determinations lists were empty or unrelated for BLOCK CONTAINS (CONFORMANCE.md A.3 row 5 decides GR1-GR4) and CODE (A.4.11 understates the stage: EVERY CODE clause is staged, not only cross-program CODE — PB1218).

## Batch d1-14e dossier gaps (REGISTRAR 14e, 2026-09-24)

The adjudicators of batch 14e (22 subjects: OCCURS through WORKING-STORAGE) reported these gaps in their dossiers, verbatim:

- **occurs-clause-p1**: The dossier's citing-source list carries no site for SR1 a), SR4, SR6, SR8, SR9, SR18, SR19, SR21 or SR33. Own searches of src confirmed that no enforcement exists; that absence is the finding, not a dossier miss.
- **occurs-clause-p1**: The occursStepPhrase grammar comment claims the binder refuses STEP outside a report group entry. The probe fmt_step_ws refutes it: the comment is a stale claim in the dossier's own grammar excerpt.
- **occurs-clause-p1**: No negative golden pins COBOLNET0854, 0856, 0857 or 1523, and none pins the CAPACITY-register receiving refusal, so SR22 and SR32 are CONFORMS with an empty test-ref.
- **occurs-clause-p1**: docs/CONFORMANCE.md has no row for SR29's implementor maximum. Annex A.1 does not list it, but the rule itself requires the implementor to specify one.
- **occurs-clause-p1**: Editions for 85/2002 are asserted from each facility's introduction; the 2023 text's Annex E covers only 2014->2023 and lists no OCCURS change.
- **occurs-clause-p2**: The dossier lists no code for §13.18.38.4 GR9 (RECORD VARYING + Format 2). It is implemented without citing the clause, through SequentialIoEmitter.VaryingLengthArg's null (GR13b) arm plus the CobolTable.OdoExtent image slice.
- **occurs-clause-p2**: The dossier's diagnostics list does not include COBOLNET1523 (the CAPACITY register as a receiving operand, §13.18.38.3 SR30–32 / GR15), and no test or .err file references it.
- **occurs-clause-p2**: The dossier gives no pointer that EC-BOUND-ODO has a single raise site (CobolTable.OdoExtent). The EcEmitter/EcBinder lines it cites read as if the whole of GR7 is wired.
- **occurs-clause-p2**: CobolDynTable.RefSending's doc still says 'EC-BOUND-SUBSCRIPT under CHECKING ON is a later wiring'. No kb/Work note owns that residue (FINDING F4).
- **occurs-clause-p2**: For the part-1 SR7 adjudicator: MOVE/ADD/COMPUTE/INITIALIZE into an index-name are refused with a false COBOLNET1639 "'IX' is not defined". ACCEPT gets the correct COBOLNET1637.
- **occurs-clause-p2**: EC-BOUND-SET (GR17's explicit-SET consequence, §14.9.39.4 GR30) has no raise site. Already owned by open kb/Work PB460, not re-reported.
- **page-clause**: Annex E carries no PAGE-clause entry, so the introducing edition of LAST CONTROL HEADING, the DE/CH abbreviations and COLS/COLUMNS cannot be derived from the spec; the editions field records all four editions for every row. If the version matrix wants these phrases gated below 2002, that needs a spec source other than Annex E (owner/VERSION_CHANGE_REFERENCE question).
- **page-clause**: No kb/Work note owns LAST CONTROL HEADING / FIRST DE / LAST CH / LAST DE (PB1059's body instructs its fixer to file it; CONFORMANCE.md A.4.11 lists it as 'tracked' with no note) -- finding 1 is register-ready for that.
- **page-clause**: PB1058 names §13.18.39.3 as PAGE's range rule but measures only the Int32 overflow crash; SR5's 10000..2^31-1 band compiling silently is not recorded anywhere -- finding 2.
- **page-clause**: The dossier's IntegerOperandPass citation of '§13.18.39.3 SR6' covers only the greater-than-zero half; the ascending-order half has no citing code at all (invisible to a citation selector).
- **page-clause**: §13.18.35.3 SR6c (absolute lines within the per-TYPE region) is unscreened; that row belongs to the LINE clause adjudication, and finding 3 is filed here only because the PAGE GR1/GR2/GR4 constraint halves depend on it -- deduplicate against the LINE-clause output.
- **page-clause**: No spec-derived golden asserts the §13.18.39.4 GR3 defaults (every conformance PAGE clause either writes the phrases or does not observe the defaulted ones); GR3 is CONFORMS by reading + run probes gr3d/gr3e but test-needed.
- **physical-subdivisions-of-a-report**: No source or test cites §13.8.6.2.3; the sum-counter permission is implemented under its §13.18.54.4 GR5/GR12 twin (ReferenceResolver.SumCounterFor) and the prohibition only implicitly (non-sum report items never enter the name space), surfacing as the generic COBOLNET1639 'not defined ... declare the item' rather than a diagnostic naming §13.8.6.2.3.
- **physical-subdivisions-of-a-report**: Adjacent, out of scope, seen while probing: a SUM addend naming a report-section item (data-name-1, rolled total, §13.18.54.3 SR4 / §13.18.54.4 GR6) is staged COBOLNET0899 'not yet implemented' (DataBinder.Reports.cs ~L2746); a subscripted SOURCE operand (SOURCE WS-A(1)) is staged COBOLNET0899 'not yet implemented'; SUM WS-A over an unsubscripted WS table draws COBOLNET2270 whose message lists 'a SUM addend' among the exempt contexts (§8.4.2.3.3 SR5 g exempts only a data-name addend, so the rejection may be right but the message contradicts itself). This search found no kb/Work note owning the two staged-loud items; the registrar should check.
- **physical-subdivisions-of-a-report**: Latitude note: the sum counter register is USAGE BINARY, so FUNCTION LENGTH(sum-counter) = 2 for PIC 9999 and 4 for PIC 9(7)V99, and a BY REFERENCE method argument must be declared binary; §13.18.54.4 GR1 makes the counter a conceptual numeric item with no stated usage - a candidate CONFORMANCE.md determination.
- **present-when-clause**: Dossier did not surface that the §13.18.35.3 SR6 a/b/d/e and §13.18.14.3 SR7/SR8 'subject to a different PRESENT WHEN clause' rules are unenforced and EC-REPORT-LINE-OVERLAP/COLUMN-OVERLAP/PAGE-WIDTH are catalogued but never raised — owned by the LINE/COLUMN rows, makes GR3 b/c/e/f unverifiable.
- **present-when-clause**: No existing test exercises GR2 level-01 absence, non-elementary absence 'irrespective' of subordinate clauses, or GR3a/GR3d (page-fit with absent lines); probe pwa.cob shows they behave per spec but they are untested.
- **property-clause**: The dossier's register list shows only PB805 (landed); no open kb/Work note owns any §13.18.42 row, so all four findings are new.
- **property-clause**: No spec-derived conformance test exists for the FINAL phrase, SR2-SR6, or the WITH-less `PROPERTY NO GET` spelling; the only covering tests are OoSpineTests.PropertyReference_SendingWithNoGet_0843 / _ReceivingWithNoSet_0843 (suppression halves of GR1/GR2).
- **property-clause**: The citing source does not cite SR1, SR3, SR5 or SR6 anywhere -- consistent with their absence (dossier silence matched the code here).
- **property-clause**: DataBinder.Oo.cs OoBindPropertyClauses doc comment says 'SR checks are the 0842 family', which overstates coverage: only SR2 (own OCCURS) and SR4 are implemented.
- **property-clause**: Method WORKING-STORAGE is refused at 2023 (COBOLNET0902), so SR1's method-WS leg was not probed at 2002/2014.
- **record-clause**: docs/COBOLNET_FILES_DESIGN.md (~L2108) says 'RECORD IS VARYING (13.18.43) is a 2002 introduction ... at 85 the VARYING phrase is rejected'. The pinned compiler accepts VARYING at --std 85, which is correct because the 1985 RECORD clause had the VARYING format. The design doc is stale and could mislead an edition-gating change.
- **record-clause**: No code or doc cites GR-13.18.43.4-3 (the bytes-regardless-of-character-type rule). The national miscount in MinRecordSize/MaxRecordSize went unnoticed because the RecordClauseRules drift test measures the rule table, not the byte arithmetic.
- **record-clause**: No spec-derived test pins GR12 across its verbs (DELETE/START/RELEASE/failed READ/RETURN), GR7 at both bounds, or GR18. These are test-needed, not defects.
- **record-clause**: kb/Work PB322 determination F (the SD implied RECORD clause) bears on GR-13.18.43.4-5 but does not claim that row in inventory_rows.
- **record-clause**: Dossier grammar/source lists were truncated (24 grammar and 112 source entries). The Format-1 defect is in BindRecordClause's early return, which the dossier did show.
- **redefines-clause**: The dossier's diagnostics list shows no Cobol.Net screen for SR1, SR2, SR3, SR13 or SR15, and there is none: the frontend DiagnosticDescriptors CBL0808/CBL0806/CBL3111-3114/CBL3117 matched by the keyword are LEGACY CobolSharp.Compiler sites (SemanticBuilder/SymbolValidator/DataItemClassifier), not Cobol.Net - a reader of the dossier could mistake them for live screens.
- **redefines-clause**: DeferredShape.NestedClassBacking (REDEFINES class inside an OCCURS) has no owning kb/Work note; PB1056 records the census gap generally. The new finding nested-class-backing should become that owner.
- **redefines-clause**: SR-13.18.44.3-9 is owned by open PB550 (under §13.18.63.3 SR12/SR16) but PB550's inventory_rows do not list SR-13.18.44.3-9 - add it there.
- **redefines-clause**: The landed PB183 golden conformance:2023/pb183_redefines_in_strong_typedef_ok pins behavior this adjudication finds non-conforming under §13.18.44.3 SR14 (its derivation cites §13.18.57.3 SR4 only).
- **redefines-clause**: Diagnostic code COBOLNET1542 is used both for the SR16 ANY LENGTH REDEFINES screen and the §13.18.2.3 SR2 ANY LENGTH placement rule (seen in probe sr16_anylen.cob) - one code, two rules.
- **redefines-clause**: Rules SR4, SR7, SR10 of §13.18.44.3 are not in this input file (another batch) though the implementing code (COBOLNET1654/1656) shares ResolveRedefines.
- **renames-clause**: The dossier input omits SR-13.18.45.3-4 (data-name-2/-3 in the same record, not the same data-name). Its 'shall not be the same data-name' half was not probed here.
- **renames-clause**: No citing source implements SR3's OCCURS bar, or SR5, SR6, SR8 (non-strong arms), SR10 or SR11. The dossier's citing lines cover SR4 and SR7 only, and SR8 is cited only as an explanation in VariableLengthCompatibility/MoveEmitter, never as a screen.
- **renames-clause**: No negative golden exists for any §13.18.45.3 syntax rule except SR4-shaped pb93-renames-undefined-operand; SR7 (COBOLNET2024 on a RENAMES operand) is enforced but untested.
- **renames-clause**: The legacy SymbolValidatorTests CBL0813 cites SR11 for the legacy engine only. It is not a COBOL.NET test.
- **report-clause**: The dossier lists no register note for SR-13.18.46.3-1/-2 (unchecked FD-side report-name existence/uniqueness); new finding F1.
- **report-clause**: DataBinder.Reports.cs ResolveReports comment cites '13.18.46.4 GR1; 13.14' for the exactly-one-REPORT-clause requirement, which is actually 13.18.46.3 SR2 - a mis-attributed citation.
- **report-description-entry**: Dossier grammar list omits CobolData.g4#reportClause and the FD REPORT-name capture that SR1 depends on; the SR1 enforcement lives in DataBinder.Reports.cs#ResolveReports (found by reading, cited only as §13.14 prose).
- **report-description-entry**: No citing source names §13.14.3 SR1/SR2 or §13.14.4 GR1 by rule id; the GLOBAL semantics are cited as §13.18.27 in InheritGlobalReport.
- **report-description-entry**: The ReportCodeClause COBOLNET0899 stage has no kb/Work owner (PB1056 census).
- **report-group-description-entry**: The dossier's citation regex could not see the missing screens: SR5 (TYPE placement), SR9 as a subordination rule, and SR12 for column-less entries cite no clause because no code implements them. The same blind spot is recorded in kb/Work PB372.
- **report-group-description-entry**: Unfiled lead outside this subject, from the §13.18.53 SOURCE clause: `05 S1 COLUMN 1 PIC 99 SUM WN.` followed by `05 COLUMN 5 PIC 99 SOURCE S1.` in the same CF FINAL group draws COBOLNET0899 "SOURCE 'S1' does not resolve to a data item (ISO §13.18.53.3 SR4)". §13.18.53.3 SR4 expressly admits 'a sum counter defined in the current report' as identifier-1, so legal source is rejected (probe rgde/sumsrc2 and sumsrc). No kb/Work note matched it in a keyword search.
- **report-group-description-entry**: Diagnostic misfiling (quality, not a verdict change): DiagnosticCatalog ReportGroupBefore01 (SR4), ReportColumnWithoutLine (SR9) and ReportItemMissingPicture (SR12) are declared on the shared COBOLNET0899 'recognized but not implemented' code, so these illegal-source refusals read as staged features. ReportColumnWithoutLine also cites §13.18.14 where the rule is §13.15.3 SR9.
- **report-group-description-entry**: JUSTIFIED §13.18.32.3 SR3 (category) is unenforced in both the data-division and report arms (probe rgde/justnum, rgde/wsbwz). PB507 owns the BLANK WHEN ZERO sibling and mentions the JUSTIFIED combination. A keyword search found no note clearly owning the SR3 category rule, so the registrar should confirm whether PB507 or PB512 claims it before filing a new one.
- **report-group-description-entry**: SR-13.15.3-7 depends on the open analysis kb/Work PB1046, which asks whether report-section names are referenceable from the procedure division. The row is left PARTIAL until that is adjudicated.
- **report-section**: The dossier's citing-source list shows no code for §13.8.2: the reportSection/reportDescriptionEntry productions cite §13.14/§13.15 (the pre-2023 clause numbers for RD and report group entries) rather than §13.8.x, so the regex misses them.
- **report-section**: The interface half of §13.8.3 SR1 is enforced only by the grammar's omission of a data division in interfaceDefinition (generic COBOL0001), with no citation; invisible to the dossier.
- **report-section**: No register note owns the constant-entry-in-report-section or empty-RD defects; the constant-entry adjudication in this run found half (a) independently.
- **same-as-clause**: Spec ambiguity worth an owner look (not recorded as a defect): an elementary data-name-1 inside a GROUP-USAGE NATIONAL group is national only because the group implies it (§13.18.29.3 SR3 'explicitly or implicitly described as usage national'). §13.18.49.4 GR3 transfers a USAGE clause only from an alphanumeric or strongly-typed group, and GR4 only when the subject is a group, so the compiler gives the SAME AS subject a DISPLAY description (probe p24: S SAME AS a PIC 9(3) member gives BYTE-LENGTH 3; q02: 4 against E's 8). The literal reading supports this; whether 'the data description identified by data-name-1' (GR1) includes the implied USAGE NATIONAL is not settled.
- **same-as-clause**: GR-13.18.49.4-4 cannot fire: SR7 limits data-name-1 to an elementary or level-1 item, so it is never both subordinate to a group and a group itself. Judged CONFORMS on that reasoning, and the compiler has no transfer code for it.
- **same-as-clause**: The SR4 violation is rejected by the SR3 ancestor walk on the cloned SAME AS. COBOLNET1557's message cites SR3, not SR4. This is cosmetic.
- **same-as-clause**: Every SAME AS rejection that returns before the copy also emits a cascade COBOLNET0881 (PICTURE required) on the subject that was never copied. This is diagnostic noise, not a conformance defect.
- **same-as-clause**: Rows that pass by probe but have no spec-derived test: SR2's level-88 half, SR4, SR6, SR7's section half, and GR2c (levels above 49) together with the copied 88s.
- **same-as-clause**: The dossier leaves out SR9 (§13.18.49.3 rule 9) from this rule set. It was probed anyway: q04 (SIGN) and q05 (GROUP-USAGE) both get COBOLNET1555, which is the PB889 fix (landed).
- **sort-merge-file-description-entry**: The dossier did not surface the FD/SD SELECT-presence question at all (no code cites §13.4.6.3 SR1 or §13.4.5.3 SR1) -- the rule is unimplemented and uncited.
- **sort-merge-file-description-entry**: The dossier did not surface FileModel.MaxRecordSize/MinRecordSize or RecordClauseRules as the implementers of §13.4.6.4 GR1 (they cite §13.18.43, not §13.4.6).
- **sort-merge-file-description-entry**: No spec-derived test pins §13.4.6.3 SR4 (SD record-name in WRITE/REWRITE; FROM/INTO permitted).
- **source-clause**: The dossier's citing-source list has no line for the missing MOVE-validity screen (SR2/SR8): code that omits a rule cites no clause (the PB372 shape) - found only by probing and by grepping MoveTable16 callers.
- **source-clause**: The dossier did not surface that sum counters are invisible to the identifier-1 arm (LookupQualified) while ReportSectionNameIn admits them for expressions - a two-arm dispatch found by probe p4 vs p7.
- **source-clause**: No kb/Work note owns the COBOLNET0899 ReportSourceSubscripted staging (DIAGNOSTICS.md row report-source-subscripted) nor the rolled-total SUM staging that makes GR3's SUM exception unreachable.
- **source-clause**: Editions of the SOURCES spelling / multi-operand form (2002) are not derivable from the 2023 text (Annex E covers 2014->2023 only); taken from docs/VERSION_CHANGE_REFERENCE row 130d's caveat.
- **source-clause**: GR4 sentence 3 (suppressed items still take operands) is not observable with ODO (suppression is always trailing) and PRESENT WHEN suppresses every repetition of an entry; verified by reading the ordinal-indexed emitter only.
- **sum-clause**: No open kb/Work note owns the staged-loud data-name-1 / cross-report SUM / cross-report UPON rows (SR4, GR6, GR7a/b, GR8, GR11) — the dossier's register list shows only landed notes.
- **sum-clause**: EC-REPORT-SUM-SIZE / size error indicator has no raise site and no owning note (PB326 and PB482 mention it only as a seam).
- **sum-clause**: Editions below 2023 for the OF word, arithmetic-expression-1 addend and SUM ROUNDED cannot be derived from the 2023 text (Annex E records no SUM change); the compiler accepts them at --std 85.
- **sum-clause**: Dossier did not surface BindSumClause (DataBinder.Reports.cs L2373), where SR3 is enforced and SR2 would belong.
- **sum-clause**: GR10's OCCURS DEPENDING leg depends on whether a repeating SUM entry has per-occurrence counters (GR8 a) implies so); the engine keeps one counter per entry — contestable reading, recorded as PARTIAL without a finding.
- **type-clause-p1**: specs/ISO_COBOL.md §13.18.57.2 carries NEITHER general-format diagram (the heading, 'Format 1 (type-name):', then the §13.18.57.3 anchor): a transcription loss; cite.py --check 13.18.57.2 'TYPE TO type-name-1' fails for that reason. The rule catalog's FMT text (from the rendered page) is intact.
- **type-clause-p1**: The dossier's grammar entry for reportTypeClause did not flag that reportGroupType lacks ON/FOR and OR PAGE; no register note owns that FMT gap (new finding 1).
- **type-clause-p1**: The dossier register listed PB183/PB205/PB836/PB889 (all landed) but not open PB545, which owns SR-13.18.57.3-8 with an empty inventory_rows — a note that claims no rows is invisible to the citation-keyed selector (the PB372 shape).
- **type-clause-p1**: ReportWriter.AddGroup's doc says SR13/SR14 are 'diagnosed at bind' — a stale claim; no binder screen exists.
- **type-clause-p1**: Editions of the ON/FOR words, the OR PAGE phrase and the SR11 omission are not derivable from the 2023 text (VERSION_CHANGE_REFERENCE row 130d leaves Report Writer's 2002/2014 status open); the code gates none of them.
- **type-clause-p1**: SR11/SR12/SR13/SR14/SR15/SR16 and SR9 have no spec-derived tests; SR1's TYPE-side shape has none either.
- **type-clause-p1**: Side observation outside this part: sr13_two_rh prints differently at --std 2023 vs 2002/85 (a leading blank line at 2002/85); the source is illegal so not adjudicated here.
- **type-clause-p2**: The dossier's grammar entry for typeClause/reportTypeClause does not show that reportGroupType lacks OR PAGE and ON/FOR; no kb/Work note owns OR PAGE (searched kb/Work).
- **type-clause-p2**: 13.18.35.3 SR6 c) -- the only compile-time consumer of the GR7/GR8 limits -- cites no clause in code and is unenforced; invisible to a citation-keyed dossier (PB372 shape).
- **type-clause-p2**: DataItem.IsBased's DescriptionCopy reason cites 13.18.57.4 GR4 to justify NOT copying BASED -- a real clause answering a different question (GR4 covers only a subject that writes its own BASED).
- **type-clause-p2**: The SynthesizeImpliedPictures/ExpandTypes pass order (BindPipeline.cs) is not cited under 13.18.57.4 GR3's implicit-PICTURE sentence, so the dossier did not surface it.
- **type-clause-p2**: LAST CONTROL HEADING phrase has no grammar surface and no owning kb/Work note (already recorded by the page-clause adjudication); GR8 d) depends on it.
- **typedef-clause**: The dossier's register list was empty, yet PB608 (COBOLNET1535 defined twice) touches this clause's RENAMES stage; no note owns the RENAMES rejection itself.
- **typedef-clause**: Nothing in the dossier pointed at BinderDriver's contained-unit GLOBAL inheritance loop -- code that fails to implement GR3's GLOBAL scope cites no clause (the PB372 blind-spot shape).
- **typedef-clause**: No spec-derived test exists for SR2 (COBOLNET1530 recursion) or for the positive IS TYPEDEF spelling; the dossier's test list contains only characterization coverage for these.
- **typedef-clause**: GR3's BASED divergence was found by the sibling type-clause-p2 adjudication (GR-13.18.57.4-1), not by this dossier; reproduced here.
- **varying-clause**: Dossier did not surface DiagnosticCatalog.ReportSourceSubscripted (COBOLNET0899, DataBinder.Reports ~2358) which blocks the GR4 NOTE subscript use; it cites §13.18.53, not §13.18.64.
- **varying-clause**: No kb/Work note owns the report VARYING rows; EC-REPORT-VARYING's missing raise site is mentioned only in landed PB326's prose.
- **varying-clause**: Group-entry VARYING (the counter on a non-printable repeating entry) has no code at all - invisible to a citation-keyed dossier.
- **varying-clause**: The ReportVaryingModel doc comment says the counter steps 'once per repetition of the entry's multiple COLUMN clause' and the SR1 comment says 'the multiple LINE clause still stages loud' - both stale against PB565's landed OCCURS/vertical vehicles.
- **working-storage-section**: The dossier did not surface the COBOLNET0899 RecursiveContainedWs stage as unowned: its only register reference is PB168 (landed), and PB1028/PB1056 (open deferral-census notes) do not name it.
- **working-storage-section**: The dossier's citing-tests list missed conformance:2002/oo_factory (factory WS one copy per class), conformance:2023/pb154_cancel_cascade (INITIAL program WS reinitialized), and the pb152_options_initialize_* goldens (the §11.9.10 route of GR3), because they cite 13.5.4 by no clause number.
- **working-storage-section**: VCR 130e's edition boundary for method WS (2002/2014 legal) is marked provisional, and Annex E.2's silence contradicts it. No in-repo 2002/2014 text can settle it.
