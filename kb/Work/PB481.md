---
title: "PB481 — a REFERENCE-MODIFIED file key is silently accepted: `RECORD KEY IS IX-KEY(1:3)` compiles clean and keys on all six characters, because `DataBinder.KeyReference` keeps only the base name and qualifiers for RECORD KEY, ALTERNATE RECORD KEY and RELATIVE KEY (which also drops the qualifiers) — §12.4.5.12's format writes data-name-1, and §8.4.3.3.3's NOTE says reference modification is not permitted there"
id: PB481
kind: defect
status: landed
severity: MINOR
area: binder/file-control
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["12.4.5.12", "12.4.5.6", "12.4.5.13", "8.4.3.3.3", "8.4.2.3.3"]
inventory_rows: []
closes_rows: []
closes_rows_reason: "The rule this closes is §8.4.3.3.3's NOTE over the key-naming positions, and its inventory row (SR-8.4.3.3.3-5) speaks for EVERY data-name-n position in the standard, so one sweep of the key clauses cannot re-verdict it; the witnesses are the four pb481 negatives and a witness-only record on SR-14.9.30.3-11 (batch w58j-pb524-pb481)."
tags: [cobolsharp, work, defect]
---

# PB481 — LANDED — the same helper PB205 fixed for CONTROL, at its three remaining callers

Found 2026-09-04 by the `PB205` implementer while sweeping every caller of `DataBinder.KeyReference`; PB205
fixed the CONTROL / TYPE / RESET ON arms (where §13.18.16.3 SR4 permits a reference modification and the
binder must KEEP it) and measured that the FILE-CONTROL arms have the opposite defect: the format does not
permit one and the binder silently DROPS it. Re-derived on this tree for this note.

## The mechanism

`src/Cobol.Net.Compiler/Binding/DataBinder.cs` — `KeyReference(...)` returns `(Base, Quals)` from a
`dataReference` and discards everything else on the operand. Its callers: RECORD KEY (`:818`), ALTERNATE
RECORD KEY (`:821`), and RELATIVE KEY (`:827`, which keeps `.Base` only — the qualifiers are dropped too).
So `RECORD KEY IS IX-KEY(1:3)` binds as `IX-KEY`, compiles with no diagnostic, and the file is keyed on the
whole six-character item: a program written to key on three characters gets a different file organisation
than it wrote, silently. A subscripted spelling is the same shape (§8.4.2.3.3 SR2 leaves no legal one, since a
key item cannot be a table element — §12.4.5.12.3).

## The rule

```
python scripts/spec/cite.py --check 12.4.5.12.1 "The RECORD KEY clause specifies the prime record key"
OK  §12.4.5.12.1
python scripts/spec/cite.py --check 8.4.3.3.3 "Because the references to data items are restricted to identifiers, where data-name-n is used in a general format or syntax rule, then reference-modification is not permitted."
OK  §8.4.3.3.3 5)  (the NOTE)
```

§12.4.5.12's general format (and §12.4.5.6's ALTERNATE RECORD KEY, §12.4.5.13's RELATIVE KEY) writes
`data-name-1`, not `identifier-1`; §8.4.3.3.3's NOTE is the general statement that a data-name slot admits no
reference modification. The conforming posture is a compile-time rejection naming the clause.

## Fix shape

`KeyReference` becomes a screen, not a projection: it reads the operand's suffixes through
`ReferenceResolver.ReadOperandSuffixes` (PB205's lexical reader) and rejects a reference modification or a
subscript with a named diagnostic citing the clause; RELATIVE KEY keeps its qualifiers. One helper, three
callers, one negative golden per clause (`*> reject-at: 85 2002 2014 2023`), and the existing positive
keyed-file goldens prove nothing legal is refused. The helper's doc comment on main already states the
hazard (PB205 wrote it); the screen is what is owed.

## Landed 2026-09-23 (wave 58 group J) — the data-name-n screen reads the one suffix reader, and the key arms that bypassed it take it

**Re-measured first.** The three FILE-CONTROL arms the title names no longer compiled clean: kb/Work PB489 had
routed RECORD KEY, ALTERNATE RECORD KEY and RELATIVE KEY through `ClauseDataName`, whose shape screen refuses a
reference-modifier. But the sweep this note owes found FOUR defects still live on `c54434a8d`:

1. **The refusal said the wrong thing.** `RECORD KEY IS IX-KEY(1:3)` drew COBOLNET2024 *"is written with a
   subscript"*: `ScreenClauseOperandShape` walked the parse tree itself and counted every `subscriptPart` as a
   subscript, but a ref-mod written straight after a data-name arrives in the SUBSCRIPT-mode captured group (a
   depth-0 colon), not in `refModPart`. It now reads `ReferenceResolver.ReadOperandSuffixes`, the one reader
   that knows both carriers — every clause that uses the screen (LINAGE, FILE STATUS, the keys, ASSIGN USING,
   RECORD DEPENDING, OCCURS DEPENDING/KEY/CAPACITY, RENAMES) now names the shape correctly.
2. **A second, false verdict.** RECORD KEY and ALTERNATE RECORD KEY resolve through their own SR2 selection
   (`InRecords`), which never asked `_refusedClauseOperands`, so the refused `IX-KEY(1:3)` also drew COBOLNET0863
   "references nothing described". It now joins the ambiguous names in the set `FileControlKeyRules.Screen` skips
   (renamed `refusedOperands`). RELATIVE KEY never had the cascade — it already went through `ResolveClauseOperand`.
3. **SORT / MERGE keys — silent wrong answer.** `SORT E ON ASCENDING KEY K(4:3)` (Format 2) printed
   `AAACCC BBBZZZ CCCAAA` — sorted on all of K — and the file-format SORT/MERGE KEY `S-KEY(1:3)` compiled clean.
   §14.9.40.2 (both formats) and §14.9.24.2 print `KEY { data-name-1 }`; the screen is now
   `DataBinder.ScreenDataNameShape` (static over the EditionContext) and `SortBinder` calls it on both arms.
4. **READ … KEY IS IX-KEY(1:3) — compiled clean.** §14.9.30.3 SR11 is an identity over data items, like START's
   SR5/SR6 which kb/Work PB602 moved to `Place.DenotedItem`; the READ arm still asked `Place.Item`. It asks
   `DenotedItem` now and draws COBOLNET0864.

Citations re-run: `cite.py --check 8.4.3.3.3 "where data-name-n is used in a general format or syntax rule, then
reference-modification is not permitted"` → OK §8.4.3.3.3 5) (the NOTE); `--check 14.9.40.3 "Key data names shall
not be subscripted."` → OK §14.9.40.3 14) b); `--check 14.9.30.3 "shall be specified in the RECORD KEY clause or an
ALTERNATE RECORD KEY clause associated with file-name-1"` → OK SR11; `--check 8.4.3.3.4 "Reference modification
creates a unique data item that is a subset of the data item referenced by identifier-1"` → OK GR5. The general
formats (§12.4.5.12.2, §14.9.40.2, §14.9.24.2) are figures cite.py cannot match; read on the page.

Goldens: `negative/pb481-record-key-refmod` (2024, all editions), `pb481-sort-key-refmod` (2024, all),
`pb481-table-sort-key-refmod` (2024, 2002+ — the table SORT is a 2002 introduction), `pb481-read-key-refmod`
(0864, all). START was already refused (PB602, SR6/SR5) and is unchanged.