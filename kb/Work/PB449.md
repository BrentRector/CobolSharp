---
title: "PB449 — the SET format is selected from the FIRST receiving operand alone, so the SAME two operands give a correct SR23 diagnostic in one order and a run-time crash in the other, and a statement no printed format admits is executed as Format 1/2 arithmetic"
id: PB449
kind: defect
status: landed
severity: MAJOR
area: binder/set
wrong_answer: false
crashes: true
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.39.2", "14.9.39.3", "14.9.39.4"]
inventory_rows: ["SR-14.9.39.3-21", "SR-14.9.39.3-23"]
tags: [cobolsharp, work, defect]
---

# PB449 — the SET format is selected from the FIRST receiving operand alone

Lane-3 adjudication batch b2, group g7-set-a. **Every repro below was re-run on HEAD `89044aa0`** (the
adjudication ran on the pinned `5fe593a0`); all still reproduce.

## 1. THE MECHANISM

`SET` has seventeen printed general formats and several of them share one token shape. The binder therefore
*sniffs* the format from an operand's category — and it sniffs **`drefs[0]` and nothing else**.

`PtrBinder.TryBindSetUpDown` (`src/Cobol.Net.Compiler/Binding/Procedure/Verbs/PtrBinder.cs:257-282`) decides
whether a `SET … UP/DOWN BY` statement is Format 10 (data-pointer arithmetic) or Format 2 (index arithmetic):

    if (host.Expr.IndexFieldOf(drefs[0]) is not null) return null;                       // :263
    if (ctx.Refs.Probe(drefs[0]) is not { } sniff
        || sniff.Item.Pic?.Category is not PicCategory.Pointer) return null;             // :264-265

If that one operand is not a data-pointer the method returns `null` and the **whole statement** — including any
data-pointer operands written later in the list — falls through to `SetBinder.BindSetUpDown`
(`SetBinder.cs:366-379`), whose `SetTargetOf` (`SetBinder.cs:444-447`) accepts anything `ResolveReceiving` can
resolve. When the pointer *is* written first, the SR23 screen at `PtrBinder.cs:274-279` runs over
`drefs.Skip(1)` and produces a correct, self-citing `COBOLNET0869`. Same rule, same two operands, two verdicts —
decided by source order, which is not a rule variable anywhere in §14.9.39.

The TO direction has the identical shape at `SetBinder.cs:337-353`: the re-route reads `tds[0]` **or** the
sender (`t0`/`s0` at :340-341), so a Format-9 statement whose *first* receiver and sender are both numeric while
a *later* receiver is a program-pointer escapes routing entirely and SR21 is never asked.

**This is the shape the file's own comment says was fixed one level down.** `PtrBinder.cs:269-271` records
[[PB221]] verbatim: "Committing the probe's Place made `SET P(XE) Q(XE) UP BY 4` diagnose COBOLNET0844 for Q and
not for P: one statement, one rule, two verdicts, decided by which operand the format sniff happened to read."
That landing made the first operand share the others' **resolution**. It did not make the format **selection**
consider more than the first operand, and the same sentence still describes what is left.

There is also **no residual arm**: nothing says "no printed format admits this statement". A receiving list that
matches no format is simply executed as Format 1/2 arithmetic.

## 2. THE RULES, QUOTED, WITH THEIR `--check` LINES

    python scripts/spec/cite.py --check 14.9.39.3 "Identifier-9 shall be of category data-pointer"
    OK  §14.9.39.3 23)  (Syntax rules)
        23\) Identifier-9 shall be of category data-pointer.

    python scripts/spec/cite.py --check 14.9.39.3 "Identifier-7 shall reference a data item of category program-pointer"
    OK  §14.9.39.3 21)  (Syntax rules)
        21\) Identifier-7 shall reference a data item of category program-pointer. Identifier-8 shall be of
             category program-pointer.

    python scripts/spec/cite.py --check 14.9.39.3 "Identifier-1 shall reference a data item of class index or an integer data item"
    OK  §14.9.39.3 1)  (Syntax rules)
        1\) Identifier-1 shall reference a data item of class index or an integer data item.

§14.9.39.2 (read whole, not inherited): Format 2's receiving operand is `{ index-name-3 } …`; Format 10's is
`{ identifier-9 } …`; Format 14's is a CAPACITY register; Format 9's is `{ identifier-7 } …`. **No format admits
a mixed data-pointer / numeric receiving list, and none admits a bare `PIC 9(4)` under UP BY.**

## 3. EXPECTED vs OBSERVED — RE-RUN ON HEAD 89044aa0

Compiler `src/Cobol.Net.Cli/bin/Debug/net10.0/cobol.exe`, probes from
`.../lane3/out-b2/probe/set1/`, all at `--std 2023 --run` (identical at `--std 2002`):

    cobol g5_mixed_upby.cob --std 2023 --run          # SET P1 WS-N UP BY 4 — pointer FIRST
      error COBOLNET0869: 'WS-N': a SET UP/DOWN BY receiver mixed with data-pointers
      (ISO §14.9.39 SR23) shall be a USAGE POINTER data item          ← correct, and cites SR23

    cobol h6_rev_mixed_upby.cob --std 2023 --run      # SET WS-N P1 UP BY 4 — the SAME two, swapped
      (compiles with ZERO diagnostics)
      Unhandled exception. CobolNet.Runtime.NotImplementedCobolFeatureException: … arithmetic into a
      non-fixed-point target 'P1'

    cobol h7_rev_mixed_to.cob --std 2023 --run        # SET WS-N P1 TO 4 — the TO twin
      (clean compile) → same NotImplementedCobolFeatureException on 'P1'

    cobol g2_num_upby.cob --std 2023 --run            # SET WS-N UP BY 4 over 01 WS-N PIC 9(4) VALUE 1
      N=[0005]                                        ← no format admits this; it is executed

And the SR21 leg the refuter found, re-measured here on HEAD (`probe-g7-set-a/p449/z1_pp_late_recv.cob`,
`01 PP1 USAGE PROGRAM-POINTER. 01 WS-N PIC 9(4). 01 WS-M PIC 9(4).` + `SET WS-N PP1 TO WS-M`):

    (compiles with ZERO diagnostics)
    Unhandled exception. … arithmetic into a non-fixed-point target 'PP1'

Both operands are identifiers, so Format 9 matches syntactically, and Format 9 is the **only** format whose
receiving operand may be a program-pointer. SR21 is violated twice over and never asked, because neither `t0`
nor `s0` is a program-pointer.

**Expected:** each of these statements shall be diagnosed, and diagnosed *identically whichever operand is
written first*. `g2_num_upby` shall be rejected rather than executed.

## 4. THE CODE SITE AND THE SHAPE OF THE REPAIR

| arm | file:line | what it reads |
|---|---|---|
| UP/DOWN BY format sniff | `PtrBinder.cs:263-265` | `drefs[0]` only |
| the SR23 screen it gates | `PtrBinder.cs:274-279` | `drefs.Skip(1)` |
| TO-direction re-routes (F4 / F9 / F5) | `SetBinder.cs:340-352` | `tds[0]` and the sender only |
| permissive fall-through | `SetBinder.cs:354-360`, `:373-378` → `SetTargetOf` `:444-447` | anything resolvable |

⛔ **The repair is not a second screen; it is to move the selection off `[0]`** (CLAUDE.md rule 5). Format
selection shall be computed from the **whole receiving list plus the sender** — classify every operand once,
then pick the single format whose printed receiving brace admits *all* of them — and there shall be a terminal
arm that reports "no SET format admits this receiving list" rather than falling into Format 1/2. A scalar
`category-of-operand-0` is standing in for what §14.9.39.2 makes a **set-valued** question, which is exactly
`model_the_rule_shape_not_one_case`. Pair it with a drift test that compiles each mixed list **in both orders**
and asserts the two diagnostics are the same.

## 5. THE SIBLING SWEEP THE FIXER MUST DO

* `SetBinder.cs:340-352` — all three TO re-routes (Pointer / ProgramPointer / ObjectReference) ride the same
  `t0 || s0` sniff. Fixing only the UP/DOWN arm repeats [[PB221]]'s mistake one level up.
* `SetBinder.cs:59-70` (`setObjectReferenceStatement` dispatch) — `sorCat` is `Probe(sor.dataReference(0))`,
  the same first-operand sniff for the NULL/SELF/SUPER sender route.
* `DynTryBindSetCapacity` (`SetBinder.cs:387-405`) already reads `targets[0]` but *does* screen `Count > 1`
  (COBOLNET1524) — it is the one arm with a residual check, and it is the model.
* `DynTrySetSize` (Format 16 bare form) — same peek idiom, unchecked list.
* The Format-9 data-pointer near-miss: `SET PP1 TO DP1` is refused by the **data-pointer** arm, blaming an
  operand that conforms to SR21, and its message names a format ordinal that is condition-setting in 2023, not
  pointer assignment. That wrong-ordinal sweep is **[[PB388]]**'s (`SetBinder.cs:194/:204/:217`,
  `BoundTree.cs:744`) — do not re-tabulate it here; land it in the same change set.

## 6. ROWS

* **`SR-14.9.39.3-21`** — PARTIAL. On every statement the compiler *does* route to Format 9, both sentences are
  enforced and the diagnostic names SR21; the verdict rests on `SET WS-N PP1 TO WS-M` dodging the routing
  entirely (measured above, HEAD).
* **`SR-14.9.39.3-23`** — PARTIAL. The screen exists, is complete and cites itself; the verdict rests on the
  measured order-dependence (`g5_mixed_upby` vs `h6_rev_mixed_upby`, same two operands, opposite verdicts).

No spec-derived test covers either rejection arm. `tests/Cobol.Net.Tests.Unit/CobolPtrTests.cs` exercises the
GR18/GR19/GR20 value rules on the runtime carrier, not the operand category.

## LANDED — 2026-09-19 (wave 39, group SET-FORMATS)

**The repair is the one the note asked for: the selection moved off `[0]`.**
`src/Cobol.Net.Compiler/Binding/Procedure/Verbs/SetFormatSelection.cs` is now the ONE place a SET general
format is chosen. It classifies EVERY receiving operand once (a pure R30 probe — the capacity register first,
then an index-name, then the item's own description), then picks the single row of a printed-brace table whose
receiving brace admits all of them. When no row does, it picks the NEAREST row — the first, in specificity
order, that admits ANY of them — and that format's own syntax rule refuses the operands it does not admit, by
name. When nothing matches at all there IS no format: `COBOLNET2112`, the terminal arm §14.9.39.2 needed and did
not have.

**The sender no longer selects anything.** §14.9.39.2 writes a format's identity into its RECEIVING brace; the
sending brace is then constrained by that format's own rules. The `t0 || s0` sniff at `SetBinder.cs:340-352` is
deleted, and so is `PtrBinder.TryBindSetUpDown`'s `drefs[0]` peek — that method is now `BindSetUpDown`, entered
only once Format 10 is chosen, and its SR23 screen runs over EVERY operand including the first (so the first
operand also gets the §8.8.1.1 position screen the others always had — the other half of what [[PB221]] found).
The `setObjectReferenceStatement` dispatch's `sorCat` first-operand sniff is gone the same way.

**Measured on this worktree's own build, all at `--std 2023`:**

    g5_mixed_upby.cob     SET P1 WS-N UP BY 4  -> COBOLNET0869 on WS-N, citing SR23
    h6_rev_mixed_upby.cob SET WS-N P1 UP BY 4  -> COBOLNET0869 on WS-N, citing SR23   (was: run-time abort)
    h7_rev_mixed_to.cob   SET WS-N P1 TO 4     -> COBOLNET0869 on WS-N, citing SR17   (was: run-time abort)
    z1_pp_late_recv.cob   SET WS-N PP1 TO WS-M -> COBOLNET0869 on WS-N, citing SR21   (was: run-time abort)
    g2_num_upby.cob       SET WS-N UP BY 4     -> COBOLNET2112                        (was: N=[0005])

**The sibling sweep the note asked for, done:** all three TO re-routes, the `setObjectReferenceStatement`
dispatch, `DynTryBindSetCapacity` (now `BindSetCapacity`) and `DynTrySetSize` (now folded into `BindSetSize`)
ride the ONE selector; none of them peeks any more. `SetEmitter.AugmentSetTarget`'s comment claiming "the edition
validator will diagnose" a numeric SET UP/DOWN was FALSE and is corrected — COBOLNET2112 is what makes it true.

**Rows:** `SR-14.9.39.3-21` and `SR-14.9.39.3-23` are `CONFORMS`.
**Drift test:** `SetFormatSelectionDriftTests` pins that within one direction no kind is admitted by two rows,
that every classifiable kind has a row, and that a receiving list and its reverse select the same format.
**Code allocated:** COBOLNET2112.

## WAVE-41 FINISHER — the selector's one exception, and the row the merge moved

Dropped from train 40 and re-landed. The selection itself is unchanged; `SelectForTo` adds the ONE case the
receiving list cannot answer (a sender of a category Format 1's sending brace cannot hold — see [[PB456]]), as a
table column with its own drift facts rather than an arm. Two merge-level repairs came with it: `SR-14.9.39.3-29`
is re-pointed from `SetBinder.cs#DynTryBindSetCapacity` — the method this cluster folds into `BindSetCapacity` —
to its new home, in a BATCH, since that stale code-location is the composition red train 40's lander measured on
the merged tree and neither cluster's own gate could see; and `BindSetCapacity` now carries BOTH intents, this
cluster's Format-14 selection plus SR30 literal screen AND [[PB457]]'s "the target NAMES a register but breaks a
reference rule" arm, which landed on main while this cluster was held back.
