---
title: "PB837 — a SORT/MERGE USING transfer reports its retrieval failures to the USE declarative with the IMPLICIT CLOSE's I-O status: SortEmitter.EmitInputFile emits the read loop, then GR12 c)'s CLOSE, then one EmitUseHook, so a retrieval that fails '30' followed by a successful CLOSE ('00') runs NO declarative at all, though §14.9.40.4 GR12 b) makes that retrieval an as-if READ"
id: PB837
kind: defect
status: landed
severity: MAJOR
area: codegen/io
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.40.4", "14.9.24.4", "14.9.49.4"]
inventory_rows: []
closes_rows: []
closes_rows_reason: "the rows this rule family owns (GR-14.9.40.4-12/-15, GR-14.9.24.4-7/-12) stay GAP: each rule also carries the implicit-transfer TERMINATION sentences (fatal OPEN, MERGE GR7a/GR12a/GR12b), a separate mechanism reported as a lead by the wave-49 implementer, so no row can be verdicted whole by this landing"
tags: [cobolsharp, work, defect]
---

Filed 2026-09-13 by REGISTRAR #4 (the lead-filing pass over the train 27-33 implementer, finisher and lander reports). Every probe below was REBUILT from the source report and RE-RUN on this worktree's own `dotnet build CobolSharp.sln -c Debug`, first at main `b5ca78fb` and RE-RUN unchanged at `e13ef810` (train 32's head, after PB415/PB394/PB355/PB526/PB646 landed); every citation was re-derived with `python scripts/spec/cite.py --check` and the command line is printed beside the quotation.

# PB837 — LANDED 2026-09-22 — each as-if I-O statement of a SORT/MERGE transfer offers its OWN status to its OWN USE hook

`SortEmitter.EmitInputFile` now stores the FILE STATUS and runs the USE hook for the retrieval that ended the READ loop
(atEndHandled — the as-if READ carries AT END) BEFORE the implicit CLOSE, and gives the CLOSE its own hook, the shape an
explicit CLOSE has. The sibling arm the sweep named, `EmitGivingFile`, had the same one-hook-after-CLOSE shape: each
as-if WRITE now hooks its own unsuccessful status inside the loop, and the GR15 / MERGE GR12 boundary paragraph
("On the first attempt to write outside the externally defined boundaries of the file") ends the file's processing
through `IoStatusClass.WriteBoundary` ('24' §14.9.51.4 GR33 b / '34' GR20). MEASURED on the worktree build: a USING
file whose implicit OPEN fails '61' (nonfatal, the transfer continues) went 61 → 42 and now goes 61 → 47 → 42; a
LINE SEQUENTIAL GIVING file refusing one record '71' ran NO declarative and now runs one. Witness:
`tests/conformance/2023/pb837_sort_implicit_io_status`; the three pb714 SORT/MERGE sharing goldens gained the
retrieval's '47' / the writes' '48' lines their own rules require. Left for the registrar: the termination rules
(SORT GR15 fatal OPEN, MERGE GR7 a / GR12 a / GR12 b) — a different mechanism.

# PB837 — WAS OPEN — the USE hook reads the CLOSE's status, not the retrieval's

Source: the `PB344` finisher reports (both the dropped branch's and `PB344-finisher2`, item (a)); named again by
the train-29 and train-31 landers. Re-confirmed AT THE SITE on this tree — the code comment there now states it
as a defect rather than as a justification.

## Observed, at the site (`src/Cobol.Net.Compiler/CodeGen/Verbs/SortEmitter.cs#EmitInputFile`, main `b5ca78fb`)

```
144:  w.Line($"{RuntimeApi.FileClose(f)};   // implicit CLOSE (GR12c / GR7c)");
145:  seqIo.EmitStoreFileStatus(input);
148:  // ⛔ The status this hook reads is the implicit CLOSE's, not the retrieval's - GR12 c)'s CLOSE was
152:  return seqIo.EmitUseHook(input, atEndHandled: true, notNormalLabel: endLabel) | terminable;
```

`__IoCheck` returns on the '0' first character of the CLOSE's '00', so the declarative the failed retrieval owes
never runs.

## The rules, quoted

```
$ python scripts/spec/cite.py --check 14.9.40.4 "as if a READ statement"
  OK  §14.9.40.4 12)  (General rules)
      b) The logical records are obtained and released to the sort operation. Each record is obtained as if a READ statement with the NEXT phrase, the IGNORING LOCK phrase, and the AT END phrase had been executed.
$ python scripts/spec/cite.py --check 14.9.49.4 "unless an AT END or INVALID KEY phrase takes precedence"
  OK  §14.9.49.4 6)  (General rules)
```

The as-if READ is a READ, so its unsuccessful completion owes the file's USE declarative exactly as an explicit
READ does; only the AT END condition is pre-handled by GR12 b)'s own AT END phrase.

## Repro shape (not run — it needs a mid-transfer host failure)

`SORT S-FILE ... USING F` with a `USE AFTER STANDARD ERROR PROCEDURE ON INPUT` declarative, where F's physical
file is made unreadable after the OPEN and before the last record. Expected: the declarative runs with the
retrieval's status ('30'). Observed shape, by construction from the emitted code: the declarative is offered the
CLOSE's '00' and does not run. ⚠ The fixer owes the measured probe — this note is a code-read plus the emitter's
own comment, not a run.

## Code site and the shape of the fix

`SortEmitter.EmitInputFile`: hook the retrieval INSIDE the read loop (status stored per retrieval, `isRead:
true`), and give the implicit CLOSE its own hook after it, the way an explicit CLOSE gets one. MERGE's GR7 b)
twin is the same code path and the same fix.

## Sibling sweep the fixer owes

* `EmitGivingFile` (SORT GR15 / MERGE GR12) — the WRITE side has the same one-hook-after-CLOSE shape.
* Every `EmitUseHook(... atEndHandled: true)` call site: `atEndHandled` suppresses the AT END arm, and the
  question each site must answer is which STATUS the hook is reading.
* `~Nist` is the wave-local filter term that covers this class (five implementer reports named 25 filter terms
  and none of them selected `NistDifferentialTests`).
