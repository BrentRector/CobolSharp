---
title: "PB1505 — CLASS-ID syntax rules on the INHERITS object-class-name are checked only for classes in the class table: a PARAMETERIZED class definition (USING form, kept out of the table as an expansion skeleton) that inherits from itself or from a FINAL class compiles silently; the non-parameterized self/cycle rejection cites 'ISO §11.3.2' (the general format) instead of §11.3.3 SR3/SR4 and shares COBOLNET0820 with duplicate-class-definition; and SR6's first sentence is exercised by nothing"
id: PB1505
kind: defect
status: open
severity: MINOR
area: binder/oo
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1020", "PB745", "PB1502"]
spec_refs: ["11.3.3"]
inventory_rows: ["SR-11.3.3-3", "SR-11.3.3-5", "SR-11.3.3-6"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 15d** from lane-3 adjudication batch **15d** (adjudicated on the pinned tree `071ff04ea`; everything that landed since — Registrar 14a/14b/14c — is register-only, so no code change touches this mechanism). Every citation below was re-run by the registrar through `cite.py --check`.

# PB1505 — OPEN

## The rule (every citation re-run through `cite.py --check`)

> 3) Object-class-name-2 shall not be the name of the class declared by this class definition.

`OK  §11.3.3 3)  (Syntax rules)`

> 4) Object-class-name-2 shall not inherit from object-class-name-1 directly or indirectly.

`OK  §11.3.3 4)  (Syntax rules)`

> 5) Object-class-name-2 shall not be the name of a class defined with the FINAL clause.

`OK  §11.3.3 5)  (Syntax rules)`

> 6) If two or more different methods with the same name are inherited, none of them may be specified with the FINAL clause. If the same method is inherited from one superclass through two or more intermediate superclasses, it may be specified with the FINAL clause.

`OK  §11.3.3 6)  (Syntax rules)`


## Finding — `class-id-paragraph` (silent; rows `SR-11.3.3-3`, `SR-11.3.3-4`)

**Mechanism.** inherits-cycle-citation: the self-inheritance and inheritance-cycle rejection (COBOLNET0820) cites 'ISO §11.3.2' - the general format, which states neither rule - instead of §11.3.3 SR3 (self) and SR4 (direct/indirect cycle), reports the self case as a 'cycle', and shares code 0820 with duplicate-class-definition

**Repro.** `CLASS-ID. CSELF INHERITS FROM CSELF.` (REPOSITORY CLASS CSELF), or CYA INHERITS CYB / CYB INHERITS CYA, compiled with the pinned cobol.exe.

**Expected from the spec.** Rejection is correct; the diagnostic should cite §11.3.3 SR3 'Object-class-name-2 shall not be the name of the class declared by this class definition' for the self case and §11.3.3 SR4 'Object-class-name-2 shall not inherit from object-class-name-1 directly or indirectly' for the cycle (both cite.py --check OK).

**Observed / reasoning.** selfinh.cob(2,18): error COBOLNET0820: class 'CSELF': the INHERITS chain is cyclic through 'CSELF' (ISO §11.3.2 - a class shall not inherit from itself directly or indirectly). Same text for cycle.cob. A real clause answering a different question (the general format) is carried into a user-visible message and hides the site from the citation-keyed dossier; no test pins either case.

**Code site.** src/Cobol.Net.Compiler/Oo/OoClassTable.cs#OoClassTable.Build (the inheritance-CYCLE loop after base resolution)

## Row `SR-11.3.3-3` — PARTIAL (editions 2002, 2014, 2023)

Rule: 'Object-class-name-2 shall not be the name of the class declared by this class definition.' (cite.py --check 11.3.3 -> OK §11.3.3 3)). Enforced by the inheritance-cycle loop in OoClassTable.Build (COBOLNET0820). Probe out/probe/class-id-paragraph/selfinh.cob `CLASS-ID. CSELF INHERITS FROM CSELF.` (REPOSITORY CLASS CSELF) -> error COBOLNET0820 at (2,18) 'the INHERITS chain is cyclic through CSELF (ISO §11.3.2 - a class shall not inherit from itself directly or indirectly)'. The source is rejected, as required. The diagnostic cites §11.3.2 (the general format) instead of §11.3.3 SR3 (finding inherits-cycle-citation). No covering test found. || REFUTED: PARTIAL — Self-inheritance is caught only by the cycle walk over table classes. A parameterized class definition that names itself in INHERITS is never in the table, so it compiles silently (the same skeleton gap as SR5). The non-parameterized case is rejected, but its diagnostic cites §11.3.2, not §11.3.3 SR3. There is no covering test.

## Row `SR-11.3.3-5` — PARTIAL (editions 2002, 2014, 2023)

Rule: 'Object-class-name-2 shall not be the name of a class defined with the FINAL clause.' (cite.py --check 11.3.3 -> OK §11.3.3 5)). Base-link loop: baseSym.IsFinal -> COBOLNET0839. Probe out/probe/class-id-paragraph/final.cob -> COBOLNET0839 citing 'ISO §11.3.3 SR5 / §11.3.4 GR3' at 2002/2014/2023; both `FINAL` and `IS FINAL` spellings set IsFinal. OoSpineTests.OverrideFinal_0838_0839 third arm asserts 0839 for a FINAL base (spec-derived). || REFUTED: PARTIAL — The FINAL-base check sits only in the OoClassTable.Build base-link loop, and a parameterized (USING) class definition never enters the class table (OoExpansion keeps skeletons out). So a parameterized class definition whose INHERITS names a FINAL class compiles cleanly unless something expands it. SR5 is a syntax rule on every class definition, and the §11.3.2 format includes the USING form. Non-parameterized definitions and expansions are rejected correctly.

## Row `SR-11.3.3-6` — PARTIAL (editions 2002, 2014, 2023)

Rule: 'If two or more different methods with the same name are inherited, none of them may be specified with the FINAL clause. If the same method is inherited from one superclass through two or more intermediate superclasses, it may be specified with the FINAL clause.' (cite.py --check 11.3.3 -> OK §11.3.3 6)). SECOND sentence VERIFIED: probe out/probe/class-id-paragraph/chain.cob - CHA defines `METHOD-ID. M1 FINAL.`, CHD inherits it through intermediates CHB and CHC; compiles clean and INVOKE of M1 on a CHD prints A-M1-FINAL (permitted, as required). FIRST sentence: two different same-named methods can be inherited only through multiple class inheritance (a linear chain carries one resolved method per name), which the compiler refuses outright with COBOLNET0849 (Annex A.4.10 item 1, NOT claimed in docs/CONFORMANCE.md §5; PB285, PB745). So the first-sentence check is never exercised - satisfied only vacuously by the declined facility; DerivedVerdictDriftTests deliberately keeps this row out of the oo-optional-items-only selector because of its second sentence. Two halves, one verified -> PARTIAL. No covering test for the permitted linear-chain FINAL case.

## What the fixer owes

Run the §11.3.3 SR3/SR4/SR5 checks on EVERY class definition — including the parameterized skeletons OoExpansion keeps out of OoClassTable — at the point the definition is bound, not only in Build's base-link loop. Split COBOLNET0820: self-inheritance (SR3) and cycles (SR4) get their own descriptor citing §11.3.3, distinct from duplicate class definition. SR6: its second sentence (FINAL through intermediate superclasses) is verified by probe chain.cob but pinned by no test — add the golden; its first sentence is reachable only through multiple inheritance (A.4.10 item 1, declined — COBOLNET0849, PB745), so say so in the row's test-ref once the owner's declined-module disposition covers it. Negatives: parameterized CSELF, parameterized class INHERITS a FINAL class.
