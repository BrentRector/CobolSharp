---
title: "PB810 — a sequential READ whose I-O status is set to '46' takes NEITHER the AT END nor the NOT AT END branch: §14.9.30.4 GR21 ends 'execution proceeds as indicated in General rule 24' and GR24 c) transfers control to imperative-statement-1, but the emitter branches only on the '1x' family and writes its reading down as a citation to §9.1.14 — a rule about a DIFFERENT condition. GR24 a) also sets '10', which contradicts GR21's own '46', so this is an ADJUDICATION and not an obvious defect; both rows read CONFORMS today"
id: PB810
kind: adjudication
status: open
severity: MAJOR
area: io
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.30.4", "9.1.14", "9.1.13.4"]
inventory_rows: ["GR-14.9.30.4-21", "GR-14.9.30.4-24"]
tags: [cobolsharp, work, adjudication]
---

# PB810 — OPEN — what "proceeds as indicated in General rule 24" means when the status is '46'

Filed 2026-09-09 by the registrar (second pass) from the **PB343 implementer's report** §12 (a) —
`scratchpad/reports/PB343-report.md` — which measured it while landing PB343 and did not open a note.
**Re-probed on this worktree's own `dotnet build CobolSharp.sln -c Debug` (exit 0) at `d3ba5795`.**

⚠ **The harm flags are set for the reading that makes this a defect.** If the adjudication upholds the current
behaviour (below), `wrong_answer` and `silent` are to be cleared in the same change set that records the
determination. They are set rather than cleared because an item that IS a wrong answer must not be ranked as if
it were not while the question is open (`feedback_measure_the_selectors_complement`).

## The measurement

`registrar2/probe/relprev2.cob` (the PB343 implementer's own repro, copied verbatim from
`scratchpad/PB343/relprev2.cob`), an empty RELATIVE file, `OPEN INPUT`, then two
`READ … PREVIOUS … AT END … NOT AT END` statements:

```
$ cobol.exe relprev2.cob --std 2023 --run
S1-OK FS=00 RK=0005 REC=EEEE
S2-OK FS=00 RK=0005 REC=EEEE
E1-AT-END ES=10
E2-AFTER ES=46          <-- neither imperative ran
```

The first READ ends at end ('10') and takes the AT END imperative. The second READ — the one whose predecessor
was unsuccessful — sets '46' and falls straight through to the statement after the READ, with the record area
undefined (§14.9.30.4 GR18, NOTE 6). Nothing is reported.

## The rule, quoted, with the `--check` lines actually run

```
$ python scripts/spec/cite.py --check 14.9.30.4 "the I-O status is set to '46' and execution proceeds as indicated in General rule 24"
OK  §14.9.30.4 21)  (General rules)
    21) For a sequential READ statement, if the previous READ or START statement for the file connector was
        unsuccessful, then the READ statement is unsuccessful and the I-O status is set to '46' and execution
        proceeds as i…
```

GR24, verbatim from `specs/ISO_COBOL.md`:

> 24) If, during the execution of the READ statement, the at end condition exists, the following occurs in the
> order specified:
> a) The I-O status of the file connector associated with file-name-1 is set to '10' to indicate the at end
> condition, and, if enabled, the EC-I-O-AT-END exception condition is set to exist.
> b) The file position indicator is set to indicate that no next or previous logical record exists.
> c) If the AT END phrase is specified in the READ statement causing the condition, control is transferred to
> imperative-statement-1. …
> d) If the AT END phrase is not specified in the input-output statement, any applicable at end exception
> processing statements are executed. …

## The two readings, and why this is an adjudication

**Reading A — GR21 means all of GR24.** Then the AT END imperative must run, and the compiler is wrong today.
The cost of Reading A is GR24 a): it would overwrite GR21's own '46' with '10', so the status the standard just
required would not be observable — and the probe above shows `ES=46` is exactly what a program reads.

**Reading B — GR21 means GR24's control transfer only (c) and d)), not its status assignment.** Then the AT END
imperative must still run; only a)'s '10' is displaced by GR21's '46'. **Note that Reading B does NOT license
the current behaviour either.**

**Reading C — the compiler's, unwritten as such.** `KeyedIoEmitter` branches to AT END only for the '1x' family
and treats every other unsuccessful status as exception processing. The comment at the site says so:

> "'1x' (10/14, the at-end family, §9.1.13.4) → AT END imperative; any other unsuccessful status takes NO branch
> (exception processing, §9.1.14 final rule item 1)"

That is a citation to a rule about a **different condition** — §9.1.14 governs the invalid key condition, not
the at end condition — which is the exact shape `feedback_a_real_clause_can_answer_a_different_question`
describes and the shape that produced PB343 itself. Whoever adjudicates must decide between A and B and record
the determination where the code is, with a `--check`ed citation.

## Code site

- `src/Cobol.Net.Compiler/CodeGen/Verbs/KeyedIoEmitter.cs:169-190` — the '1x' test and the comment above.
- the sequential twin in `src/Cobol.Net.Compiler/CodeGen/Verbs/SequentialIoEmitter.cs` — **the second arm**
  (`feedback_two_arm_dispatch`): the relative organization reaches the same decision through its own emitter, so
  a fix applied to one leaves the other. The probe above runs a RELATIVE file, so the arm it measured is not
  necessarily the arm a reader of `KeyedIoEmitter` would fix first.

## Inventory

`GR-14.9.30.4-21` and `GR-14.9.30.4-24` both read **CONFORMS / state OK** today. **No verdict is changed by this
note** — a verdict flipped on a reading is the cheap claim the inventory gate exists to refuse ([[PB258]]). What
this note records is that both verdicts are *conditional on Reading C*, which is nowhere written down and is
supported by a citation to the wrong clause. If the adjudication lands on A or B, both rows move.

## Sibling sweep the adjudicator owes

Every other status the standard routes through GR24 or its WRITE/REWRITE/START analogues: the same "'1x' or
nothing" test decides them all. Enumerate the unsuccessful statuses each verb can set, and say for each which
branch the standard requires — one table, not one `if` per site.
