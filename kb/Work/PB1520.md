---
title: "PB1520 — Annex A.1 item 121 (the ACTUAL method-name used when the PROPERTY phrase is specified — required AND documented) has no DOC-A.1-121 determination in docs/CONFORMANCE.md: the accessor names `__GET_<P>` / `__SET_<P>` are stated only in a code comment (NamingConvention.cs), so the row DOC-A.1-121 is GAP with no owner"
id: PB1520
kind: analysis
status: open
severity: MINOR
area: docs/conformance
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
cluster: ["PB1503", "PB1449"]
spec_refs: ["A.1", "11.7.4"]
inventory_rows: ["DOC-A.1-121"]
closes_rows: []
tags: [cobolsharp, work, analysis]
---

Filed 2026-09-24 by **REGISTRAR 15d** from lane-3 adjudication batch **15d** (adjudicated on the pinned tree `071ff04ea`; everything that landed since — Registrar 14a/14b/14c — is register-only, so no code change touches this mechanism). Every citation below was re-run by the registrar through `cite.py --check`.

# PB1520 — OPEN

## The rule (every citation re-run through `cite.py --check`)

> 121) METHOD-ID paragraph (actual method-name used when PROPERTY phrase is specified). This item is required. This item shall be documented in the implementor's user documentation.

`OK  §A.1 121)  (Implementor-defined language element list)`


Lead from the batch-15d method-id-paragraph adjudicator's dossier gaps, confirmed by the registrar: `grep DOC-A.1-121 docs/CONFORMANCE.md` finds nothing; the inventory row `DOC-A.1-121` carries no verdict; the name is built by `src/Cobol.Net.Compiler/Oo/NamingConvention.cs` line 42 (`"__GET_" + DataItem.Sanitize(propertyName).ToUpperInvariant()`) and its SET twin.

## What the fixer owes

Write the DOC-A.1-121 row in docs/CONFORMANCE.md stating the actual method-name of a get/set property method (`__GET_<P>` / `__SET_<P>`, P the sanitized upper-cased property-name) and what that name means for a caller outside COBOL (the CLR method name) and for a COBOL INVOKE of that literal name; pin it with a golden that INVOKEs the accessor by its documented name (or a negative, if the determination is that the name is not invocable), and record the row with the golden as its test-ref.
