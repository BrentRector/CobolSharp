---
title: "PB1183 — the UNSTRING grammar repeats the INTO KEYWORD (`unstringIntoPhrase+` over `INTO unstringIntoTarget+`) where §14.9.48.2 has ONE INTO followed by a repeated receiver group, so `UNSTRING S DELIMITED BY ',' INTO A INTO B` compiles and runs at every edition"
id: PB1183
kind: defect
status: open
severity: MINOR
area: frontend/grammar
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1182", "PB387"]
spec_refs: ["14.9.48.2"]
inventory_rows: ["FMT-14.9.48.2"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14c** from lane-3 adjudication batch **d1-14c** (adjudicated on the pinned tree `68bbdb00f`; trains 58/59 checked — nothing that landed since touches this mechanism unless the body says otherwise).

# PB1183 — OPEN

## Finding — `unstring-statement-p1` (under-rejects; rows `FMT-14.9.48.2`)

**Mechanism.** The grammar repeats the INTO keyword (`unstringIntoPhrase+` over `INTO unstringIntoTarget+`) where the general format has ONE INTO followed by a repeated receiver group, so `INTO A INTO B` is accepted

**Repro.** 01 S PIC X(12) VALUE "AB,CD5EF". 01 A PIC X(4). 01 B PIC X(4).  UNSTRING S DELIMITED BY "," INTO A INTO B.  -> compiles clean and runs A=[AB  ] B=[CD5E] at --std 85/2002/2014/2023.

**Expected from the spec.** A syntax error: §14.9.48.2 writes `INTO { identifier-4 [ DELIMITER IN identifier-5 ] [ COUNT IN identifier-6 ] } …` — the ellipsis repeats the braced receiver group, not the INTO keyword.

**Observed / reasoning.** CobolIO.g4 `unstringStatement : UNSTRING strUnstrSender unstringDelimiterPhrase? unstringIntoPhrase+ ...` and `unstringIntoPhrase : INTO unstringIntoTarget+`; BindUnstring iterates every unstringIntoPhrase without comment. Fix is `unstringIntoPhrase` (no +). The legacy Overlenient M417 tests are empty bodies (kb/Work PB387).

**Code site.** src/Cobol.Net.Frontend/Grammar/Core/CobolIO.g4#unstringStatement

## What the fixer owes

CobolIO.g4#unstringStatement: `unstringIntoPhrase` (no +); BindUnstring's per-phrase loop collapses. Grammar changes are pre-authorized; gate + doc-sync in the same commit. The legacy Overlenient M417 bodies are empty (PB387). Negative at 85.
