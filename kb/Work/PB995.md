---
title: "PB995 — SORT SR11 (and MERGE's twin) — the GIVING file's record sizes against the SD — is enforced nowhere: SD records of 5 characters with a GIVING file `RECORD VARYING IN SIZE FROM 1 TO 2` compiles clean"
id: PB995
kind: defect
status: open
severity: MINOR
area: binder/sort
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB994", "PB993"]
spec_refs: ["14.9.40.3", "14.9.24.3"]
inventory_rows: ["SR-14.9.40.3-11", "SR-14.9.24.3-3", "SR-14.9.24.3-12", "SR-14.9.40.3-5"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-22 by **REGISTRAR #12** from `leads-w48-w52.md` (wave 49 group E, PB837/PB186 implementer). Re-probed on the registrar's own `dotnet build CobolSharp.sln -c Debug` (0 warnings, 0 errors) at main `37f19e028`; every citation below re-run through `python scripts/spec/cite.py --check` on this tree.

# PB995 — OPEN

## Repro — measured
`scratchpad/reg12/q08_sr11.cob`: `SD SF. 01 SR PIC X(5).` / `FD OF1 RECORD VARYING IN SIZE FROM 1 TO 2 CHARACTERS.` /
`SORT SF … USING IF1 GIVING OF1` → compiles clean, prints `COMPILED`.

## The rule
`OK  §14.9.40.3 11)` — "If the file description entry for file-name-3 describes fixed-length records, the file description
entry for file-name-1 shall not describe a record that is larger than the record described for file-name-3." The rule's
first sentence (variable-length file-name-3: file-name-1's records neither smaller than the smallest nor larger than
the largest) is the arm this probe breaks.

## What the fixer owes
One size-range screen in `SortBinder` over every GIVING file (and MERGE's GIVING, whose SR carries the same text — find
its number with `cite.py --find`), using the FD's record-size range the file model already computes. Negative goldens
for both sentences.

## Extended 2026-09-24 by REGISTRAR 14b (adjudication batch d1-14b, pinned @68bbdb00f)
MERGE's twins (SR12 GIVING, and SR3 the USING half that no note owned): neither USING-vs-SD nor SD-vs-GIVING record sizes are checked for MERGE or SORT (`SortBinder.SortMapIoFiles`, `BindMerge`). This note now owns both directions for both verbs.
`cite.py --check 14.9.24.3 "shall describe neither records smaller than the smallest record nor larger than the largest record described for file-name-1"` → `OK  §14.9.24.3 3)  (Syntax rules)`

## Extended 2026-09-24 by REGISTRAR 14c (lane-3 batch d1-14c, pinned tree `68bbdb00f`)

SR5 is SR11's twin in the other direction — the USING file's record sizes against the SD (`cite.py --check 14.9.40.3 "shall describe neither records smaller than the smallest record"` → OK §14.9.40.3 5)): SD VARYING 3 TO 5 with a USING record X(1) or X(9), and a fixed SD with a larger USING record, all compile rc 0 (s5vsmall, s5vbig, s5big); SR11 re-measured at the pin (s11 rc 0). The ONE size-range screen this note prescribes runs over every USING file as well as every GIVING file; MERGE §14.9.24.3 has the USING twin too.

**Finding (`sort-statement-p1`, under-rejects; rows `SR-14.9.40.3-5`, `SR-14.9.40.3-11`).** The USING-file record-size screen (SR5) is missing — the twin of the GIVING screen PB995 owns (SR11), and PB995 names only SR11

**Repro.** SD SW RECORD VARYING IN SIZE FROM 3 TO 5 CHARACTERS (01 SR X(3), 01 SR5 X(5)); FD IN1 01 I1 PIC X(1); `SORT SW ASCENDING SR USING IN1 GIVING OU1` -> --std 2023 rc 0 (probe s5vsmall); X(9) rc 0 (s5vbig); fixed-size SD with a larger USING record rc 0 (s5big). SR11 re-measured at the pin: SD X(5), GIVING OU1 VARYING 1 TO 2 and OU2 fixed X(3) -> rc 0 (s11).

**Expected from the spec.** §14.9.40.3 SR5: with a variable-length SD, file-name-2 shall describe neither records smaller than the smallest nor larger than the largest SD record; with a fixed-length SD, no file-name-2 record larger than the SD record. Each probe is a compile-time rejection.

**Observed / reasoning.** SortBinder.SortMapIoFiles checks only IsSortMerge and IsSequential; no code compares FileModel record-size ranges between the SD and a USING or GIVING file. kb/Work PB995 (open) prescribes 'one size-range screen in SortBinder over every GIVING file' — the same screen, run in the other direction over every USING file, closes SR5; add SR-14.9.40.3-5 to PB995's inventory_rows (and MERGE's USING twin in §14.9.24.3).

**Code site.** src/Cobol.Net.Compiler/Binding/Procedure/Verbs/SortBinder.cs#SortMapIoFiles (line 601)
