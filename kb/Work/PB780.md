---
title: "PB780 — `SR-13.10.3-2` carries NO verdict and cannot honestly get one from a sample: it is a WHOLE-LANGUAGE admission rule (a constant-name stands anywhere a general format specifies a literal), so its verdict is a statement about EVERY literal position in EVERY format — nine binder files satisfy it and at least two positions violate it (`EDITING`'s literal-1/-2/-3 per [[PB778]]; `VALUE … WHEN SET TO FALSE IS literal-4` until [[PB732]] lands), and the only verdict this row can carry above PARTIAL is one a GENERATED check defends"
id: PB780
kind: adjudication
status: open
severity: MINOR
area: spec/inventory
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: ["13.10.3", "13.10.4", "8.3.3.6.3"]
inventory_rows: ["SR-13.10.3-2"]
tags: [cobolsharp, work, adjudication, traceability]
---

# PB780 — OPEN — a rule whose verdict is a claim about the whole grammar

Filed 2026-09-06 by the registrar from the PB732 implementer's report (§"New defects" (d)), which named this as an
"adjudication candidate, not a defect of its own". Everything below was re-measured on main at `9fad13e5`.

## The row, as it stands on main

`tests/version-matrix/traceability-inventory.json`, read programmatically:

```json
{ "rule-id": "SR-13.10.3-2", "section": "13.10.3", "kind": "SR", "ordinal": 2,
  "subject": "Constant entry", "rule-digest": "97b3e0478ff5",
  "verdict": "", "code-location": "", "test-ref": "", "derivation": "",
  "editions": "", "notes": "", "state": "GAP" }
```

Blank in every column. It is one of the 2780 GAP rows and has never been adjudicated.

## The rule

```
python scripts/spec/cite.py --check 13.10.3 "constant-name-1 may be used anywhere that a format specifies a literal"
  OK  §13.10.3 2)  (Syntax rules)
      "Except in a compiler directive, constant-name-1 may be used anywhere that a format specifies a literal of
       the class and category of constant-name-1. If constant-name-1 is an integer, it may also be used to
       specify re…"
python scripts/spec/cite.py --check 13.10.4 "constant-name-1"
  OK  §13.10.4 1)  (General rules)
      "If literal-1 or compilation-variable-name-1 is specified, the effect of specifying constant-name-1 in
       other than this entry is as if literal-1 or the text represented by compilation-variable-name-1 were
       written where…"
```

⛔ **The two are different rules and this row is only the first of them.** §13.10.3 SR2 is ADMISSION — *where* a
constant-name may be written. §13.10.4 GR1 is SUBSTITUTION — *what it means* once written. [[PB614]] records
about eighteen code comments that attribute the substitution to SR2; an adjudication of this row that cites the
substitution behaviour as its evidence would be the nineteenth. **This row's evidence is about ADMISSION: does the
grammar let the word be written there at all.**

## Why the row's shape is the finding

SR2 is not a rule about one construct. Its subject is *"anywhere that a format specifies a literal"* — so a
CONFORMS verdict on it asserts something about **every literal position in every general format in the standard**,
and the two sibling admissions (§8.3.3.6.3 SR1 for figurative constants, `--check` OK; §8.3.3.6.2 Format 7 for
symbolic-characters) ride the same positions. That is a claim no hand-enumerated sample can support:
`feedback_measure_the_selectors_complement` — a survey is evidence about what it looked at, never about what it
did not. It is also the exact shape CLAUDE.md rule 5 names: where a hand-maintained list would go, a structure
belongs.

## The evidence, both directions, measured on main

**Satisfied.** Nine source files carry the §13.10.3 / §13.10.4 substitution and are the mechanism that makes the
common positions work (`grep -rn "13.10.3 SR2\|13.10.4 GR1" --include=*.cs src/`, files de-duplicated):

```
src/Cobol.Net.Compiler/Binding/DataBinder.Constants.cs      src/Cobol.Net.Compiler/Binding/ReferenceResolver.cs
src/Cobol.Net.Compiler/Binding/DataBinder.cs                src/Cobol.Net.Compiler/Binding/Procedure/ExpressionBinder.cs
src/Cobol.Net.Compiler/Binding/DataBinder.Odo.cs            src/Cobol.Net.Compiler/Binding/Procedure/Verbs/CallBinder.cs
src/Cobol.Net.Frontend/Cst/DataDescriptionCst.cs            src/Cobol.Net.Compiler/Binding/Procedure/Verbs/ControlFlowBinder.cs
                                                            src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs
```

The PB732 implementer additionally probed the procedure division rather than assuming it —
`MOVE K TO A. MOVE KT TO B. IF A = K` over two constant entries prints `A=5 B=HI` / `EQ` — so the statement
operands substitute, and the data division's VALUE, OCCURS and PICTURE positions do too.

**Violated.** Two positions, both filed:

| position | note | state on main |
|---|---|---|
| `EDITING character-1 { IS literal-1 \| FOR { NEGATIVE IS literal-2 \| POSITIVE IS literal-3 } }` — all three literals | [[PB778]] | OPEN. `CobolData.g4:451/455/456` spell them as the narrow `literal` rule; a constant-name is `COBOL0001` + `COBOL0309`. |
| `VALUE … [ WHEN SET TO FALSE IS literal-4 ]` | [[PB732]] | `CobolData.g4:665` is still the narrow `literal` on main; PB732's train-19 landing widens it to `valueClauseOperand`. |

⚠ And the complement is unmeasured. A LINE-scoped heuristic over the whole grammar (`.g4` lines with a bare
`literal` and no `dataReference`/`identifier`/`arithmeticExpression`/`cobolWord`/`unaryExpression`/`functionCall`/
`valueClauseOperand` sibling on the same line) returns **29 lines across 7 files** — an upper bound, not a defect
count, because an alternative's sibling frequently sits on the next line. Nobody has done the rule-level pass.

## What verdict the row can carry, and when

1. **Today: not CONFORMS, and not blank either.** The honest verdict is `PARTIAL` with the two violating positions
   named in `notes` and `code-location` pointing at `CobolExpressions.g4:494` (the narrow rule) beside the nine
   files above — the row's state stays GAP, so the denominator does not move on an unfinished claim.
2. **After PB732 lands (train 19)** one violating position closes; the row is still PARTIAL, because PB778's three
   remain.
3. **After PB778 + PB568 land together** the two known holes are closed and the row becomes claimable — but
   `CONFORMS` at that moment would still be a claim about positions nobody enumerated. ⭐ **The verdict this row
   should eventually carry is one a GENERATED check defends**: the rule-level twin of the heuristic above, run as
   a drift test — *every `.g4` operand written as bare `literal` where the printed general format names a literal
   is a defect* — with a cited exemption list for the positions where the format writes something other than
   `literal` (`character-1` is the exemplar, per [[PB568]] and §13.18.40.4 GR14). Then `test-ref` names the drift
   test rather than a sample of programs, and the CONFORMS is defended against the next grammar edit rather than
   against the last one. `feedback_green_gates_arent_evidence`: prove the check red on `CobolData.g4:451` before
   the fix lands, and green after.
4. **`editions`**: the EDITING phrase is a COBOL-2023 introduction (`SR-13.18.40.3-8`'s adjudication records
   Annex E.3.3 item 19), but §13.10.3 itself and the VALUE positions apply from 2002 — the constant entry is not a
   2023 feature. So the row's editions are `2002,2014,2023` for the clause and the 2023-only EDITING hole is a
   position inside it, not a per-edition split of the rule.

## Links

- [[PB778]] · [[PB732]] · [[PB568]] — the three notes whose landings this row's verdict depends on.
- [[PB614]] — the SR2-vs-GR1 attribution defect; read it before writing this row's `notes`.
- [[PB55]] — the other open note about what the inventory's denominator counts, for the same "the rule's shape
  decides what a verdict can mean" reason.
