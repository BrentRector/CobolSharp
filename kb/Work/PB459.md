---
title: "PB459 — every SET amount is narrowed by a bare (long) cast with no integrality test, and an index-name reaches SET as a bare long with no table, so a fractional amount moves the index anyway, a negative amount DESTROYS live table occurrences, and EC-RANGE-INDEX has no raise site at all"
id: PB459
kind: defect
status: landed
severity: MAJOR
area: codegen/set
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.39.4", "13.18.38.4", "A.1"]
inventory_rows: [GR-14.9.39.4-1, GR-14.9.39.4-29, GR-14.9.39.4-3, GR-14.9.39.4-4, GR-14.9.39.4-L2.1, GR-14.9.39.4-L2.2, GR-14.9.39.4-L2.3]
tags: [cobolsharp, work, defect]
---

# PB459 — one guard the standard states four times, written once

**Rows:** `GR-14.9.39.4-1`, `-3`, `-4`, `-29`, `-L2.1`, `-L2.2`, `-L2.3`. From the b2 adjudicators
`set-statement-p4` and `set-statement-p5`. **Every repro below was re-run on the HEAD `cobol.exe`
(base 89044aa0)**; all reproduce unchanged from the 5fe593a0 pin.

## THE MECHANISM — part 1: the integrality test that exists on exactly one arm

§14.9.39.4 states the same guard once per amount-taking format, each time with the same three consequents
(condition set to exist · the SET unsuccessful · the receiving operand unchanged), differing only in the
exception-condition NAME:

| rule | format | condition |
| --- | --- | --- |
| GR2 a) 1. a (row `-L2.1`) | Format 1, `SET index-name TO arithmetic-expression-1` | EC-BOUND-SUBSCRIPT |
| GR3 (row `-3`) | Format 2, `SET index-name UP/DOWN BY arithmetic-expression-2` | EC-BOUND-SUBSCRIPT |
| GR19 | Format 10, the data-pointer amount | EC-SIZE-ADDRESS |
| GR29 (row `-29`) | Format 14, the dynamic-capacity amount | EC-BOUND-SUBSCRIPT (nonnegative INTEGER) |

**GR19 is implemented, and implemented well.** `PtrEmitter.cs:82-101` deliberately does NOT collapse the amount
to a `long` before the runtime sees it — it splits on `x.Real` / `x.Scale == 0` and keeps the fraction alive, so
`CobolPtr.UpByScaled`'s `scaledBy % pow != 0` (`CobolPtr.cs:104`) and `UpByReal`'s `by != Math.Truncate(by)`
(`CobolPtr.cs:154`) can raise the named condition and return the operand unchanged. kb/Work PB151 was the
landing that stopped a `(long)(double)` truncation from bypassing it.

**The other three arms discard exactly that shape.** Each renders one truncating cast and stores the result:

```
SetEmitter.cs:28   long __set… = (long)(NumericRenderer.Align(num.Render(s.Value,  …), 0));   // EmitSetTo
SetEmitter.cs:86   long __set… = (long)(NumericRenderer.Align(num.Render(s.Amount, …), 0));   // EmitSetUpDown
SetEmitter.cs:96   long __cap… = (long)(NumericRenderer.Align(num.Render(s.Amount, …), 0));   // EmitSetCapacity
```

and the only screen downstream is `CobolDynTable.cs:134  long target = n < _min ? _min : n;`. **The asymmetry
IS the bug** — one dispatch, four arms, one finished (`feedback_two_arm_dispatch`, the project's most
reproducible defect shape).

## THE MECHANISM — part 2: an index-name is a bare `long` with no table

GR1 exists so the rest of the clause can say *that table*: GR2 a) 1. b, GR2 a) 3. b and GR4 a) all turn on
whether a value is "outside the limit specified in General rule 2 of 13.18.38, OCCURS clause", and GR2 a) 3. a
distinguishes an index-name "associated with the same table" from one that is not.

The association IS built — `DataBinder.cs:2781-2789` does `item.IndexNames.Add(idxName)` on the OCCURS-bearing
item and `SearchBinder` reads it — but **only in the table→index direction**. `DataBinder.IndexFields`
(`:136`) is `name → C# cell` and carries nothing else, and `SetBinder.cs:435-438 SetTargetOf` hands the emitter
`SetIndexTarget(ix.IndexField)`: a `long` with no owner. So no SET receiver can ask which table it indexes or
what that table's bound is, and every range obligation in Formats 1 and 2 has nowhere to live.

The consequence is total and mechanically checkable:

```
$ grep -rn 'EC-RANGE-INDEX' src/ --include=*.cs
src/Cobol.Net.Compiler/CodeGen/Verbs/SetEmitter.cs:23          (a comment conceding it)
src/Cobol.Net.Compiler/Validation/FlagConformancePass.cs:49,503,506,522,533,535   (a >>FLAG-02 e flagger)
src/Cobol.Net.Frontend/Preprocessor/FlagDirective.cs:67        (the directive description)
src/Cobol.Net.Runtime/Exceptions/ExceptionCatalog.cs:162       L3("EC-RANGE-INDEX", EcFatality.Fatal)
```

**Four hits, none of them a raise.** The `FlagConformancePass` site is the trap worth naming: it reads
`_turn.Enabled("EC-RANGE-INDEX", …)` and flags the SET, so the condition looks wired to anyone grepping for the
name, while it can never occur (`feedback_green_gates_arent_evidence` in its purest form).

`SetEmitter.cs:22-24` states the position honestly — "Range checking (EC-RANGE-INDEX) awaits the EC model —
COBOL-85 has no exception conditions, so the unchecked store IS the 85 semantics". That is right for `--std 85`
and wrong for the other three editions this compiler must ship.

⛔ **The premise is easy to get wrong and must not be:** the limit the four arms cite is the IMPLEMENTOR'S index
range, not the table's bounds. Every one of them ends "…even if that occurrence is not a valid occurrence within
this table", so an index pointing past its table is LEGAL and must not raise. Writing a table-bounds check here
would reject legal programs.

## THE RULES, QUOTED, EACH `--check`ed ON THIS TREE

* `--check 14.9.39.4 "Index-names are associated with a given table by being specified in the INDEXED BY phrase
  of the OCCURS clause for that table."` → **OK §14.9.39.4 1) (General rules)**.
* `--check 14.9.39.4 "If the value of arithmetic-expression-1 does not result in an integer"` →
  **OK §14.9.39.4 2) (General rules)** — this is GR2 a) 1. a, row `-L2.1`; its three consequents follow in the
  clause as EC-BOUND-SUBSCRIPT set to exist, the SET unsuccessful, the receiving operand unchanged.
* `--check 14.9.39.4 "Otherwise, if the value of the arithmetic-expression-1 is outside the limit specified in
  General rule 2 of 13.18.38"` → **OK §14.9.39.4 2) (General rules)** — GR2 a) 1. b, the EC-RANGE-INDEX arm.
* `--check 14.9.39.4 "The value of arithmetic-expression-2 is determined once at the beginning of the execution
  of the statement. If arithmetic-expression-2 does not evaluate to an integer"` →
  **OK §14.9.39.4 3) (General rules)**.
* `--check 14.9.39.4 "incremented (UP BY) or decremented (DOWN BY) by the result of the evaluation of
  arithmetic-expression-2 is outside the limit specified in General rule 2 of 13.18.38"` →
  **OK §14.9.39.4 4) (General rules)**.
* `--check 14.9.39.4 "If arithmetic-expression-4 does not evaluate to a nonnegative integer, the
  EC-BOUND-SUBSCRIPT exception condition is set to exist and the execution of the SET statement is
  unsuccessful."` → **OK §14.9.39.4 29) (General rules)**.
* `--check 13.18.38.4 "The implementor shall specify the rules for the range of values allowed in the index
  defined by index-name-1."` → **OK §13.18.38.4 2) (General rules)** — and the same rule requires that range to
  include at least occurrence numbers (1 − integer-2) through (2 × integer-2), and ends: "If the execution of one
  of these statements creates a value for the index that is outside the range of the values allowed by the
  implementor: a) the EC-RANGE-INDEX exception condition is set to exist, and b) the value of the index is
  undefined unless the value of the index is specified by the rules of that statement."
* `--check A.1 "OCCURS clause (range of values allowed in the index)"` → **OK §A.1 128)** — required, and
  required to be DOCUMENTED. That documentation gap is PB460's row; named here because it is the bound these
  four rules cite.

## EXPECTED vs OBSERVED — re-run on HEAD

Probes: the two `set-p5` ones are the adjudicator's own, copied to `scratchpad/probe-g8-set-b/p5/`; the `p4`
ones I re-wrote from the finding's listing into `scratchpad/probe-g8-set-b/p4/` because that slug's sources were
not in the b2 probe tree.

```
$ cobol.exe p4gr3turn.cob --std 2023 --run
   05 E PIC 9(2) OCCURS 5 TIMES INDEXED BY IX.   01 A PIC 9V9 VALUE 1.5.
   >>TURN EC-BOUND-SUBSCRIPT CHECKING ON  /  SET IX TO 2.  SET IX UP BY A.  SET N TO IX.
IX-AFTER=0003          <-- GR3: expected IX still 2, and EC-BOUND-SUBSCRIPT (Fatal, Table 13) taken

$ cobol.exe l21a-frac-ec.cob --std 2023 --run
   >>TURN EC-BOUND-SUBSCRIPT CHECKING ON  /  SET IDX TO 1.  SET IDX TO WS-F  (WS-F PIC 9V9 VALUE 2.5)
AFTER=[B]  OCC=0002    <-- GR2a1a: expected AFTER=[A], OCC unchanged at 1, condition raised
                            identical with checking OFF, so the TURN state changes nothing — the test does
                            not exist in either state

$ cobol.exe p4gr29neg.cob --std 2014 --run
   05 D PIC 9(2) OCCURS DYNAMIC CAPACITY IN CAP.   01 A PIC S9(4) VALUE -3.   01 B PIC 9V9 VALUE 3.5.
CAP1=0000000005        SET CAP TO 5.
CAP2=0000000000        SET CAP TO A.   <-- a NEGATIVE amount DESTROYED five live occurrences
CAP3=0000000003        SET CAP TO B.   <-- a FRACTIONAL amount shrank 5 to 3
                       GR29: both are "the execution of the SET statement is unsuccessful" — capacity stays 5

$ cobol.exe p4gr1assoc.cob --std 2023 --run
   T1 has 5 occurrences INDEXED BY IX1;  T2 has 9 INDEXED BY IX2.
   >>TURN EC-RANGE-INDEX CHECKING ON  /  SET IX2 TO 8.  SET IX1 TO IX2.  SET N TO IX1.
IX1=0008               <-- taken silently; nothing can even ask which table IX1 indexes
   then  SET IX1 TO 3.  MOVE 77 TO E2(IX1).
E2-3=77                <-- an index-name used as a subscript of a table it is not associated with,
                            no diagnostic at compile time either

$ cobol.exe gr4-overflow.cob --std 2023 --run
   >>TURN EC-RANGE-INDEX CHECKING ON  /  SET IDX TO 1.  SET IDX UP BY 9223372036854775800.  SET IDX UP BY 100.
OCC=5715  EC[                               ]
                       <-- the low four digits of −9223372036854775715: the index wrapped through the
                            64-bit boundary and the program carried on with a NEGATIVE occurrence number,
                            EXCEPTION-STATUS 31 blanks
```

**Expected.** GR3 / GR2a1a / GR29: the receiving operand is unchanged and the named condition is set to exist —
EC-BOUND-SUBSCRIPT is Fatal in Table 13, so under an enabling `>>TURN` the run unit takes the condition (a USE
declarative may RESUME) instead of printing a moved value. GR29 in particular: GR30's minimum-capacity clamp
CANNOT stand in for it — the clamp is the rule for a legal new capacity below the OCCURS minimum, and GR29
rejects the operand *before* any new capacity is computed. GR4 a) + §13.18.38.4 GR2: the second `UP BY` cannot
be represented, so the fatal EC-RANGE-INDEX is raised and IDX stays at 9223372036854775801.

**What is deliberately NOT charged.** The §3.5 model in which an index IS its 1-based occurrence number makes
GR2 a) 3. a's "same table ⇒ content placed unchanged" and 3. c's "different table ⇒ the corresponding occurrence
number" the SAME machine operation, so the VALUE half of Formats 1 and 2 is correct and needs no association at
all — which is exactly why the missing reverse map went unnoticed for so long. It is the RANGE half that has
nowhere to live.

## THE CODE SITES AND THE SHAPE OF THE REPAIR

| site | arm |
| --- | --- |
| `CodeGen/Verbs/SetEmitter.cs:25-30` `EmitSetTo`, `:83-88` `EmitSetUpDown`, `:94-105` `EmitSetCapacity` | the three truncating casts |
| `CodeGen/Verbs/SetEmitter.cs:127-147` `StoreSetTarget`, `:152-172` `AugmentSetTarget` | the ONE store/augment pair PERFORM VARYING and SEARCH also ride |
| `Runtime/Values/Tables/CobolDynTable.cs:122-138` `SetCapacity` (+ `:140`,`:143` UpBy/DownBy) | clamps, never rejects |
| `Binding/DataBinder.cs:136` `IndexFields`, `:2781-2789` the one-directional registration | the missing index→table edge |
| `Binding/Procedure/Verbs/SetBinder.cs:435-438` `SetTargetOf` | mints `SetIndexTarget(cell)` with no owner |
| `Runtime/Exceptions/ExceptionState.cs:409` `SubscriptError` | the raiser that already exists and is never called from here |
| the model done right, for contrast: `CodeGen/Verbs/PtrEmitter.cs:75-102` + `Runtime/Control/CobolPtr.cs:96-112` | |

⛔ **Two structural repairs, not five inline `%` tests** (CLAUDE.md rule 5):

1. **ONE amount landing.** A `SetAmount` that returns (value, isIntegral, isNegative) and lets each format name
   its own exception condition, consumed by `EmitSetTo`, `EmitSetUpDown`, `EmitSetCapacity` and (already
   correct) the pointer arm — and by PB458's literal-alternative screen on the compile-time side. Written as
   two more hand-rolled tests, Format 16's arithmetic-expression-5 (whose sign test the code DOES carry, in
   `CobolDynString.SetSize`) becomes the fourth hand-written copy.
2. **Give the index-name symbol its table.** `SetIndexTarget(cell, table)` carrying the OCCURS bound and its
   ODO / dynamic-capacity variability, so GR2 a) 1. b, GR2 a) 3. a/b/c and GR4 a) all become ONE guarded store.
   The check is against the IMPLEMENTOR range (§13.18.38.4 GR2), which for a `long` cell means the arithmetic
   must detect 64-bit overflow — not a table-bounds test. And the chosen range must be DOCUMENTED (§A.1 128;
   PB460's row) or the check is unverifiable by a user.

Pair both with a drift test so "automatic" stays true: a test that fails if a new `SET`-family emitter path
renders a bare `(long)` narrowing of an amount.

## THE SIBLING SWEEP

* `StoreSetTarget` / `AugmentSetTarget` are shared with **PERFORM VARYING** (§14.9.28 GR13) and **SEARCH** —
  both inherit the missing integrality test and the missing range check. Fixing SET without them leaves the same
  hole one verb over.
* Every `--std` edition: the unchecked store IS COBOL-85's semantics, so the new raises must be gated to
  2002/2014/2023 and must not fire at `--std 85` (`feedback_edition_gate_sweep`).
* `FlagConformancePass.cs:503-535` should be re-read once the raise exists — a flagger for a condition that
  cannot occur is a false green, and it must keep flagging the right statements once the condition is real.
* `ExceptionCatalog.cs:162` (EC-RANGE-INDEX) is not the only catalog row with no raise site; sweep the catalog
  for siblings (EC-BOUND-SET is one — PB460; EC-RANGE-PTR is another — PB460/PB465).
* GR29 sits inside Annex A.4.4, which `docs/CONFORMANCE.md` §5 marks **Claimed** — see PB460.

## ROWS

* `GR-14.9.39.4-1` — the association premise; built one-directionally, so every rule that consumes it is
  unreachable. Verdict rests on the measured cross-table probe and on the four-hit `EC-RANGE-INDEX` grep.
* `GR-14.9.39.4-3` — Format 2's integrality test: no code site; measured (`IX-AFTER=0003`).
* `GR-14.9.39.4-4` — Format 2's range consequent: no raise site; measured 64-bit wrap (`OCC=5715`).
* `GR-14.9.39.4-29` — Format 14's nonnegative-integer test: no code site; measured twice (negative destroys
  occurrences, fractional shrinks).
* `GR-14.9.39.4-L2.1` — Format 1's integrality test: no code site; measured (`AFTER=[B] OCC=0002`), identical
  with checking on and off.
* `GR-14.9.39.4-L2.2`, `-L2.3` — Format 1's range consequents for identifier-2 and for a cross-table
  index-name-2: the VALUE half is correct by the §3.5 identity, the RANGE half has no raise site, same root as
  `-1` and `-4`. Verdict rests on reading the model plus the `-4` overflow measurement; I did NOT write a
  separate probe per sub-rule, because they share one missing mechanism and one fix.

## Landed — 2026-09-12

All seven rows are `CONFORMS` and the GAP moved 2589 -> 2582 (batch `pb459-set-index-amount-landing`).

**The guard is written ONCE.** `src/Cobol.Net.Runtime/Values/Tables/CobolIndex.cs` is the one landing:
`TryAmount` (the exact scaled `Int128` lane, where the divisibility test IS the integrality test),
`TryAmountReal` (the native-float lane, the `CobolPtr.UpByReal` shape from PB151), and `Augment` (GR4 a), the
sum formed in `Int128` so the 64-bit boundary is a value the runtime can see). `SetAmountRule` is the only
thing that varies per format, so GR2 a) 1. a, GR3 and GR29 share one test and each names its own condition.
Every SET-family amount reaches it through `SetEmitter.LandAmount`, and the statement's whole effect sits
inside the landing's success leg, so "the execution of the SET statement is unsuccessful" leaves EVERY
receiving operand unchanged rather than only the ones after the first.

**EC-RANGE-INDEX became a real condition.** It had no raise site at all; it now has the five declarations the
`CheckingFlags` recipe names (flag field, engine property, engine helper, static shim, `FatalAmbientGates` row)
PLUS the piece the note did not anticipate: an **ambient gate in `EcBinder.EcWrap`**, without which `>>TURN
EC-RANGE-INDEX CHECKING ON` wrapped nothing and the runtime flag stayed false — the raise fired into a
disabled check and the probe still printed the lenient answer. It is deliberately AMBIENT, not a per-node
`QueryFor` case: §13.18.38.4 GR2 names three statements and `SetEmitter`'s store/augment pair alone is ridden
by three verbs, so a node-kind list would be the EC-DATA-INCOMPATIBLE (PB230) defect written again.

**The siblings were swept.** `AugmentSetTarget`'s two index arms became `CobolIndex.Augment`, which is the SET
Format-2 guard and the **PERFORM VARYING** guard in one edit (§13.18.38.4 GR2 names PERFORM VARYING); the
amount stays `Int128` into the runtime because a VARYING BY operand is an integer data item of up to 31 digits
(§14.9.28.3 SR4 a). `ControlFlowEmitter.InitVaryingTarget` lands its FROM value for the same reason — the note
and the first checkpoint's STATUS both said `StoreSetTarget` needed no guard, which was true of `EmitSetTo`
and NOT of PERFORM VARYING's initialization. **SEARCH is satisfied by construction and that was measured, not
assumed**: §14.9.37.4 GR4's initial-index guard leaves the scan only when the index is within the table's
occurrence count, so its `+= 1` cannot reach the boundary. One frame below, `CobolDynTable` now computes the
new capacity in `Int128` and tests it against the implementor maximum BEFORE narrowing — a request of
5 000 000 000 used to WRAP through an `(int)` cast into a small VALID capacity instead of raising GR30's
EC-BOUND-TABLE-LIMIT, and `_count ± n` could overflow the `long` before the call was made.

**Two of the note's premises did not survive implementation, and the corrections are the better design.**

1. **No table on `SetIndexTarget`.** The note's structural repair #2 would have been a dead lookup: the limit
   GR2 a) 1. b / 2. a / 3. b and GR4 a) cite is the IMPLEMENTOR's index range (§13.18.38.4 GR2), not the
   table's bounds — the note says so itself in its own warning — and this model's index cell is a `long`, so
   the range is table-independent and the check is a carrier test. Writing a table-bounds test there would
   have rejected legal source, because every one of those arms ends "even if that occurrence is not a valid
   occurrence within this table". The range is now DOCUMENTED, which §13.18.38.4 GR2 and A.1 item 128 both
   require: `docs/CONFORMANCE.md` §7 row `DOC-A.1-128` (the row PB460 was to open — the determination is
   written and `audit_annex_a1.py --check` is at zero findings; PB460 still owns its verdict).
2. **Row `-1`'s real consumer is a COMPILE-TIME rule.** GR1's association is consumed on the reference side by
   §8.4.2.3.3 SR4 — "Index-name-1 shall correspond to a data description entry in the hierarchy of the table
   being referenced that contains an INDEXED BY phrase specifying that index-name" — which was measured
   missing (`MOVE 77 TO E2 (IX1)`, IX1 being another table's index, compiled clean and wrote through the wrong
   occurrence number). `ReferenceResolver.ScreenIndexNameAssociation` now applies it as **COBOLNET1961**, in
   both dialect lanes: the reference has a computable occurrence number, so a leniency would silently read or
   write the wrong element. The rule is applied EXACTLY as written — the index-name must be in the referenced
   item's hierarchy, not on the dimension the subscript position selects — because reading the stronger
   restriction in would reject source the standard admits.

**Drift test.** `SetIndexAmountLandingDriftTests` (4 facts, each proven red by mutation before being trusted):
every SET-family amount site lands and none narrows with a bare `(long)(Align(...))`; the augment is formed
wide through `IndexAugment` on both index arms; every caller of the shared index store lands first AND no
third file calls the shared pair; and the spec population is closed — §14.9.39.4 states the integrality guard
in exactly the four rules the test enumerates.

**Goldens.** `2023/pb459_set_index_amount_guards` (checking OFF — the lenient consequents, including the
`UP BY 2.0` control that proves the test is on the VALUE not the scale, and the `SET IX TO -3` control that
keeps the guard from becoming a table-bounds test), `2023/pb459_set_index_amount_ec` (both conditions through
USE declaratives), `2002/pb459_set_index_range_ec` (the edition leg — the mechanism arrives in 2002),
`2014/pb459_set_capacity_amount_guards` (GR29), `85/pb459_set_index_overflow_unchanged` (no >>TURN exists at
85, so the lenient half is what remains; the witness is arithmetic because an 85 literal and PICTURE are
limited to 18 digits), and `negative/pb459-index-name-not-in-table` (SR4, `reject-at` all four editions, with
the legal cross-table `SET IX1 TO IX2` as its in-file control). Every expected value is derived in the
program's own header from the rule text.

**Diagnostic codes used:** COBOLNET1961. 1962 and 1963 were allocated and are UNUSED — do not reallocate
without checking `DiagnosticCatalog`.
