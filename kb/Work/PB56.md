---
title: "PB56 — LANDED 2026-08-09 — the Dec-carrier body: the §15.4.1 r1 EAE evaluates ON the SDIDI, arguments never quantized"
id: PB56
kind: defect
status: landed
severity: MAJOR
area: runtime/intrinsics
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: [15.4.1, 15.81.4, 8.8.1.5]
tags: [cobolsharp, work, defect]
---

# PB56 — LANDED — the Dec-carrier body replaced the truncating SDIDI landing: the §15.4.1 r1 EAE evaluates ON the SDIDI and arguments are never quantized

> ⚙ **flags:** wrong-answer, silent

> **Found by the batch-6 SIGN adjudication (RV-15.81.4-1, 2026-08-08), derived by reading — the mechanism
> is the DOCUMENTED PB32-interim approximation at the ONE landing.** `IntrinsicRenderer.Landed` converts a
> standard-decimal intermediate (SDIDI) argument via `DecToUnscaled` at working scale `max(receiver scale,
> 6)`, truncating, BEFORE the intrinsic body sees the operand. §15.4.1 r1 — "the returned value shall equal
> the value of the equivalent arithmetic expression" — makes the truncation a wrong answer wherever the
> operand's true value is nonzero below the working scale.
>
> **FAILING SHAPE (SIGN instance):**
> ```
> OPTIONS. ARITHMETIC IS STANDARD-DECIMAL.
> 01 A PIC S9V9(9) VALUE 0.000000001.  01 I PIC S9.
> COMPUTE I = FUNCTION SIGN(A - 0)
> ```
> The SDIDI value 1e-9 is exact and > 0; §15.81.4 r1a selects EAE = +1; **actual 0** (trunc at scale 6).
> The same shape under NATIVE arithmetic is inside §15.4.1's implementor-defined intermediate latitude and
> is NOT a divergence — the defect is STANDARD-DECIMAL-specific, where §8.8.1.5 fixes the intermediate.
>
> ⛔ **FAMILY-WIDE, NOT SIGN-SPECIFIC** — every exact-family intrinsic fed a Dec operand through `Landed`
> shares it (the landing is the ONE choke point PB32 built, which is why the fix belongs there and only
> there). The recorded end state at the landing's own remark: a **Dec-carrier body** — the operand crosses
> into the intrinsic AS a Dec value, no unscaled quantization at all — rather than a wider working scale
> (any fixed ws is the same defect at a smaller epsilon; PB13's receiver-blind scale is the precedent).
> **The sweep when fixing:** enumerate every `Landed` caller and probe each with a sub-working-scale
> operand; add the family drift guard beside `IntrinsicCarrierAgreementDriftTests`.
> Fix lands with: spec-derived golden (the SIGN shape + at least one sibling function), re-verdict of
> RV-15.81.4-1 and any triage-confirmed siblings.

### The rows this note owns (triage 2026-08-09; re-verdict on landing)

> RV-15.62.4-1, RV-15.20.4-1, RV-15.55.4-1, RV-15.56.4-1, RV-15.34.4-1, RV-15.27.3-3
> RV-15.73.3-3, RV-15.9.4-1, RV-15.74.4-1, RV-15.17.4-1, RV-15.8.4-1, RV-15.35.4-1
> RV-15.64.4-1, RV-15.75.4-1, RV-15.69.4-2, RV-15.69.4-3, RV-15.59.4-1, RV-15.13.4-1
> (RV-15.10.4-1 and RV-15.11.4-1 fix here, re-verdict on PB65; RV-15.81.4-1 is the found instance.)
> The design draft: scratchpad pb56-design-draft.md — folds into COBOLNET_NUMERIC_DESIGN.md D3 with the fix.

### ✅ LANDED 2026-08-09 — CobolIntrinsics.Dec.cs + IntrinsicRenderer.RenderDec

> **The fix is the design draft executed whole**: under a standard mode, a Dec/float-bearing exact-family
> call dispatches to `RenderDec` BEFORE any landing — arguments lift exactly (§8.8.1.5.1/.2 r1, via the
> RAW operand, a new `RawArg`/`DecArg` lane), the EAE evaluates on `CobolDec`, and the result lands at the
> receiver ONCE. All-fixed lists stay on the exact Int128 family (D3's documented equivalence, now pinned
> by the `CobolIntrinsicsDecTests` agreement theory). **The sweep found TWO sibling arms of the same
> landing defect**: the R18 Exp/Exp10 arms round-tripped their exponent through the truncating landing
> (`DecOperand(Arg(...))`), and `Dbl` — the float family's ENTIRE argument intake — landed before
> converting, so `SQRT(4e-18)` probed as 0. Both fixed at their one site. `RenderFloat`'s standard-mode
> tail now CONVERTS the prose family's binary64 result in (`CobolDec.FromDouble`) instead of quantizing,
> which also removed the double-rounding artifact (ASIN(1) = …326 by one final transfer, not …327 by two).
> **COBOLNET0899 RETIRED**: ANNUITY / PRESENT-VALUE / VARIANCE / STANDARD-DEVIATION evaluate their
> inexact-division EAEs in SDIDI form; the descriptor and the negative fixture
> `arith-standard-intrinsic-staged` are gone, replaced by the positive golden.
> **VERIFIED**: golden `pb56_dec_carrier_intrinsics` (16 lines, every value EAE-derived) ·
> `CobolIntrinsicsDecTests` 14/14 · FULL Conformance 4,308/4,308 · intrinsic unit filters 49/49 ·
> SpecTraceability 10/10. **11 rows re-verdicted CONFORMS+tested** (SIGN, the SDIDI lane, ANNUITY, PV,
> EXP, ACOS, LOG, LOG10, ASIN, ATAN, COS — each with its probe line named in the row note).
> **RESIDUE REASSIGNED TO PB65** (different mechanisms, probe-and-close): RV-15.17.4-1 (two-arm receiver
> context), RV-15.27.3-3/RV-15.73.3-3/RV-15.35.4-1 (same-site claims needing their own probes),
> RV-15.64.4-1 (Power's receiver-bearing arm), RV-15.75.4-1 (RANDOM's wrap), RV-15.59.4-1 (the unchecked
> Rescale widen, already PB65's headline), RV-15.13.4-1 (the AsInt bridge). RV-15.69.4-2/-3 land with
> PB60's parser rewrite as noted.
