---
title: "PB1097 — the DOC-A.1-73 ASSIGN USING content rule is enforced at OPEN only: a sort-merge file's own `ASSIGN USING WS-N` is never associated (a sort-merge file is never registered as a host connector, so `FileConnector.Associate` is never called), and SORT with WS-N = SPACES or X\"01\" runs to completion where the determination says the statement is unsuccessful"
id: PB1097
kind: defect
status: open
severity: MINOR
area: runtime/sort
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB674", "PB746", "PB749"]
spec_refs: ["12.4.5.3"]
inventory_rows: ["GR-12.4.5.3-4"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 04:03 PDT by **REGISTRAR 14a** from adjudication batch d1-14a (pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probes under `scratchpad\adj-d1\out\probe\`). Not fixed by trains 58/59 (their src diff was checked).

# PB1097 — OPEN — the DOC-A.1-73 ASSIGN USING content rule is enforced at OPEN only: a sort-merge file's own `ASSIGN USING WS-N` is never associated (a sort-merge file is never r

## Rule text (every citation re-run by the registrar)
- `python scripts/spec/cite.py --check 12.4.5.3 "When the USING phrase is specified, the meaning and rules for the allowable content of the data item referenced by data-name-1 are defined by the implementor"` → `OK  §12.4.5.3 4)  (General rules)`

## The refuter's measurement (row GR-12.4.5.3-4, batch 14a)
DOC-A.1-73: 'The association is made at every OPEN, SORT and MERGE'; content empty after space removal, or carrying a character below U+0020, 'names no physical file: the association cannot be made, so the statement is unsuccessful'. The OPEN arm honours it (`FileConnector.Associate`: `spec.Trim(' ')`, `ch < ' '` → '31'), and a SORT GIVING file does through its implicit OPEN. The sort-merge file ITSELF does not: SD SELECT … ASSIGN USING WS-N with WS-N = SPACES or X"01" → SORT completes, records returned, no exception status.

## What the fixer owes
Associate the sort-merge file's ASSIGN USING content at SORT/MERGE time through the same `Associate` rule (one rule, one place), making the statement unsuccessful per DOC-A.1-73; or amend DOC-A.1-73 to say what the sort-merge file does. Golden: SORT with an all-spaces USING name.

Rows claimed: GR-12.4.5.3-4. Flip `status` and write `closes_rows` in the landing commit (CLAUDE.md rule 8).
