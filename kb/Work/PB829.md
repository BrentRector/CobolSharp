---
title: "PB829 — The `genericClause` catch-all (`IDENTIFIER (IDENTIFIER|literal)*`) is wired into SIX closed general formats — FD, SD, the file control entry, I-O-CONTROL, SPECIAL-NAMES, the configuration-section paragraph list and the IDENTIFICATION paragraph list — and every one of them swallows `WIBBLE WOBBLE` silently at strict `--std 2023`. ⛔ PB487's sibling sweep counted FOUR and missed `ioControlClause`'s inline alternative; measured here at seven general formats over six sites"
id: PB829
kind: defect
status: half
severity: MAJOR
area: frontend/grammar
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["4.2.2", "13.4.5.2", "13.4.6.2", "12.4.5.1", "12.4.6.2", "12.3.7.2"]
inventory_rows: ["FMT-13.4.5.2", "FMT-13.4.6.2", "FMT-12.4.5.1", "FMT-12.4.6.2", "FMT-12.3.7.2", "FMT-12.3.2", "FMT-11.2.1", "FMT-13.16.2"]
tags: [cobolsharp, work, defect, grammar]
---

# PB829 — HALF — the catch-all is closed at all EIGHT formats; FOUR rows stay defective for reasons OUTSIDE it

Filed 2026-09-09 by the registrar (third pass) from the **PB487 implementer's report**, "New defects found
outside my mechanism" item (c) (`scratchpad/reports/PB487-report.md`). **Every row of the table below was
measured here**, on this worktree's own `dotnet build CobolSharp.sln -c Debug` (exit 0) — not copied from the
report, and the re-measurement changed the count. Measured first at `094739b3` and **re-measured unchanged at
`35f86874`** after train 26 landed; PB487's own catch-all is now closed and every other one still swallows.

## ⛔ Why ONE note and not six — the question the brief asked to be answered explicitly

Six notes or one? **One**, because they share more than a shape: they share **one grammar rule**
(`CobolExpressions.g4:15`, whose own comment says *"Shared across all grammars — one rule, one source of
truth"*), **one fix operation** (model the format's real clause list, then replace the catch-all with a NAMED
diagnostic for the residue), and **one trap** (closing a list without first auditing what the grammar does not
yet model rejects legal source — the trap ALIGNED sprang in PB487). Under the owner's
`feedback_fill_by_rows_per_mechanism` an implementer slot is filled per MECHANISM, so six notes would compete
for one slot while hiding that closing the second catch-all is the same operation as the first.

What is NOT shared is the **model gap** — each format lacks different clauses — so each catch-all gets its own
section below with its own inventory row, and a fixer may land them one section per commit.

⚠ **`computerAttributes` is deliberately NOT here.** `SOURCE-COMPUTER. IBM-370 WIBBLE WOBBLE.` is swallowed by a
different mechanism — a `~DOT` token SINK, not `genericClause` — and it contradicts two rows already verdicted
CONFORMS. That is **`kb/Work/PB830`**, filed the same day. PB487's report attributed it to
`CobolSpecialNames.g4:226`, which is not where it happens.

## The rule that makes this a defect

```
$ python scripts/spec/cite.py --check 4.2.2 "An implementation shall provide a warning mechanism that optionally may be invoked by the user at compile time to indicate violations of the general formats and the explicit syntax rules of standard COBOL."
  OK  §4.2.2   (Acceptance of standard language elements)
```

Each of the seven formats below is CLOSED — it enumerates its clauses and admits nothing else — so a word run
the format does not define is a violation of a general format, and §4.2.2 obliges the compiler to be able to
indicate it. Today none of them can: the residue is not merely unflagged, it is **not represented at all**.

## Measured — six sites, seven general formats, all silent

`grep -rn "genericClause" src/ --include=*.g4 | grep -v Generated | grep -v obj/` on `main` at `094739b3`:

| # | grammar site | consumer | general format | FMT row (state today) | probe | result |
|---|---|---|---|---|---|---|
| 1 | `CobolData.g4:179` `genericFileDescriptionClause` | `fileDescriptionClause` (`:75`) | **§13.4.5.2** File description entry | `FMT-13.4.5.2` (GAP) | `p7_fd.cob` — `FD  F WIBBLE WOBBLE.` | compiles + runs, `FD-GENERIC=[OK ]` |
| 2 | the same rule | `sortMergeDescriptionClause` (`:52`) | **§13.4.6.2** Sort-merge file description entry | `FMT-13.4.6.2` (GAP) | (same rule, SD arm) | ⚠ one catch-all, TWO formats |
| 3 | `CobolIO.g4:242` `vendorFileControlClause` | `fileControlClause` (`:78`) | **§12.4.5.1** File control entry | `FMT-12.4.5.1` (GAP) | `p7_select.cob` — `SELECT F ASSIGN TO "f.dat" WIBBLE WOBBLE.` | compiles + runs, `SELECT-GENERIC=[OK ]` |
| 4 | `CobolIO.g4:263` **inline `genericClause` alternative** of `ioControlClause` | I-O-CONTROL paragraph | **§12.4.6.2** I-O-CONTROL paragraph | `FMT-12.4.6.2` (GAP) | `p7_iocontrol.cob` — `I-O-CONTROL.` / `WIBBLE WOBBLE.` | compiles + runs, `IOCONTROL-GENERIC=[OK ]` |
| 5 | `CobolSpecialNames.g4:46` inline `genericClause DOT?` | `specialNamesParagraph` | **§12.3.7.2** SPECIAL-NAMES paragraph | `FMT-12.3.7.2` (GAP) | `p7_conf.cob` | compiles + runs, `CONF-GENERIC=[OK ]` |
| 6 | `CobolSpecialNames.g4:225` `vendorConfigurationParagraph` | `configurationParagraph` (`CobolParserCore.g4:381`) | **§12.3.2** Configuration section | `FMT-12.3.2` (GAP) | `p7_conf.cob` | (a whole PARAGRAPH fallback, not a clause) |
| 7 | `CobolParserCore.g4:354` `genericIdentificationParagraph` | `identificationParagraph` (`:191`) | **§11.2.1** Identification division structure | `FMT-11.2.1` (GAP) | `p7_conf.cob` — `WIBBLE. WOBBLE.` | compiles + runs |

## ⛔ The seventh consumer is now the TEMPLATE — copy it, do not re-invent it

PB487's own catch-all **landed closed with train 26** and is on `main` at `35f86874`. It did NOT delete the
`genericClause` reference; it turned it into an **error production**:

```antlr
// CobolData.g4:327 — inside dataDescriptionClause
    | unrecognizedDataClause   // ⛔ LAST — the error production above; refused BY NAME at bind (COBOLNET1941)
// CobolData.g4:344
unrecognizedDataClause
    : genericClause
    ;
```

That is the shape each of the six sections above wants: **recognize the word run so the parser does not die on
it, then refuse it BY NAME at bind with a diagnostic that quotes the words and the §.** It keeps recovery (the
rest of the entry still parses, so the user gets one good error instead of a cascade) and it removes the
silence. Read `CobolData.g4:288-345` before starting — the comment block there records why the alternative must
be LAST and what the ALIGNED trap was.

⛔ **PB487's sweep undercounted.** Its report lists *"four other call sites"* and names FD, FILE-CONTROL,
SPECIAL-NAMES, SOURCE-COMPUTER and the ID paragraph. Measured here: `ioControlClause`'s **inline** alternative
(#4) was missed — it is not a named `xxxClause : genericClause` wrapper, so a grep read by rule NAME skips it —
and SOURCE-COMPUTER is not a `genericClause` site at all (PB830). `feedback_scan_all_similar`: the sweep found
the shape and then enumerated it by the wrong key.

## The rule, and the shape of each fix

```antlr
// CobolExpressions.g4:15 — "Generic clause pattern for vendor/extension hooks.
//                           Shared across all grammars — one rule, one source of truth."
genericClause
    : IDENTIFIER (IDENTIFIER | literal)*
    ;
```

`IDENTIFIER (IDENTIFIER|literal)*` matches any word run, so wherever it sits in an alternative list it is a
total sink for everything the named alternatives missed. Per section, the fixer owes, IN THIS ORDER:

1. **Audit the format first** — render the printed general-format page (`scripts/render-spec-page.py`;
   `feedback_spec_diagrams_render_pdf` — the OCR'd diagrams were systematically lossy toward falsely-restrictive
   syntax) and list every clause the standard defines, including the ones only some editions have.
2. **Model what is missing**, gated per edition where the standard gates it
   (`feedback_edition_gate_sweep`: gating a construct breaks everything compiling it below that edition).
3. **Only then** replace the catch-all with a named diagnostic carrying the clause text and the §, so the
   residue is REPORTED rather than absorbed.

Doing (3) before (1) and (2) rejects legal source silently accepted today, which is a strictly worse defect than
the one this note describes. That ordering is the whole reason PB487 declined to sweep these.

## Sibling sweep — where else a total sink hides

`genericClause` is the named one; the shape is "an alternative that matches an unbounded token run". The two
others found on `main` are `computerAttributes` (`CobolParserCore.g4:497`, `({!objectComputerClauseAhead()}?
~DOT)+` → **PB830**) and the COMMENT-ENTRY family — `dateCompiledContent` / `securityContent` (`~DOT+`,
`CobolParserCore.g4:330,340`) and `remarksContent` (`(IDENTIFIER | STRINGLIT)+`, `:349`). Those last are
**correct and must not be closed**: a comment-entry's content is arbitrary text by definition, so an unbounded
token run is what the standard describes there. That is the distinction a fixer needs — a sink over a
COMMENT-ENTRY is right; a sink over a CLAUSE LIST is this note. Whoever takes it re-runs
`grep -nE "~DOT|IDENTIFIER \(IDENTIFIER" src/Cobol.Net.Frontend/Grammar/**/*.g4` and classifies every hit into
those two piles before touching anything.

## Why `silent` + `under_rejects` and not `rejects_legal_source`

The compiler accepts MORE than the standard defines, never less, so no legal program is refused. The harm is
that a typo inside a closed format — a misspelled clause word, a clause written in the wrong paragraph — is
absorbed without a word and its intended effect never happens. That is the under-rejection half of §4.2.2, and
it is silent by construction.

## Status and rows

All seven `inventory_rows` are **GAP with no verdict today**, so this note claims them without changing any
verdict: **no `record_verdicts` batch, GAP delta 0.** Each row earns its verdict when its section is audited and
closed — and a row may only reach CONFORMS with a witness that a non-clause word run is DIAGNOSED, not merely
that the legal clauses parse (`feedback_green_gates_arent_evidence`; PB830 is what that omission looks like
after it ships).

`docs/rearchitecture/DESIGN-frontend-grammar.md` §3.10 records this pattern — ⚠ that section arrives with
**train 26** and is not on `main` at `094739b3`; when it lands, its list is the five-site one from PB487's
report and **this note's table (six sites, seven formats) supersedes it**.

## Probe programs

`E:\Temp\claude\E--CobolSharp\f781220f-0e84-4a73-8282-aebebefeff2b\scratchpad\registrar3\probe\` —
`p7_fd.cob` (`PBR829A`), `p7_select.cob` (`PBR829B`), `p7_iocontrol.cob` (`PBR829C`), `p7_conf.cob`
(`PBR829D` — ID paragraph, SOURCE-COMPUTER and SPECIAL-NAMES in one program). Run them against a freshly built
`cobol.exe` first; each must become a DIAGNOSTIC, and the legal-source controls in each format must stay legal.

## Landed — the MECHANISM, in full; the note stays HALF because four of its rows are still defective

**The mechanism is closed at every site, and it is closed ONCE rather than eight times.** `genericClause`
(`Core/CobolExpressions.g4`) is now referenced from exactly one place — `unrecognizedClause`, THE error
production of the grammar — and every one of the six alternative lists spells `| unrecognizedClause`, LAST. The
five named wrappers are gone (`genericFileDescriptionClause`, `vendorFileControlClause`,
`vendorConfigurationParagraph`, `genericIdentificationParagraph` and PB487's own `unrecognizedDataClause`), and so
is the inline alternative of `ioControlClause` this note was filed to add to the count.

**⛔ THE COUNT WAS SEVEN FORMATS OVER SIX SITES AND IT IS EIGHT OVER SEVEN.** §13.16.2 belongs in the table: PB487
closed it with a per-format production and a `DataBinder.BindEntry` arm, which is a SECOND mechanism for the same
rule, and leaving it there would have been `feedback_one_mechanism_per_job` with the two mechanisms landed a
fortnight apart. Folding it in also CLOSED a hole its binder home had: `BindEntry` is not reached for every
written entry — the level-66 and level-88 paths return before it — so `88 CN WIBBLE VALUE 1.` dropped the word in
silence, MEASURED on the pre-change build where only COBOLNET1747 fired. The message and code COBOLNET1941 are
byte-identical, so `conformance:negative/pb487-unrecognized-data-clause` passes unchanged.

**The shape.** One grammar production · one table (`Frontend/Cst/ClosedFormats.cs`, keyed on the PARENT context
type, carrying the §, the subject, the noun and the code) · one pass (`Compiler/Validation/ClosedFormatPass.cs`,
a sibling of `DeclinedFacilityPass` run from `BinderDriver`, a parse-tree walk because four of the eight formats
bind nothing at all) · one drift guard (`ClosedFormatDriftTests`, which reads the `.g4` files AND reflects over
the generated parser, in both directions). Adding a closed format is `| unrecognizedClause` plus a table row, and
forgetting the row fails the build — proven, not claimed: both halves of the grammar fact were made to FAIL once
against a temporary second sink.

**A PARAGRAPH list is not a CLAUSE list.** §12.3.2 and §11.2.1 list paragraphs, so they get their own code,
COBOLNET1971; calling an unrecognized paragraph header a "clause" sends the reader to the wrong subclause.
Codes: **COBOLNET1970** (clause-list formats), **COBOLNET1971** (paragraph-list formats); §13.16.2 keeps
**COBOLNET1941**. COBOLNET1972 was allocated to this note and NOT used — it returns to the pool.

**Rows: 3 closed, GAP 2561 → 2558, and FOUR left PARTIAL ON PURPOSE.** The audit the note demanded — render the
printed page BEFORE closing the list — is what found them, and every one was MEASURED identically on `main` at
`1840778e`, so each predates this change and none of them is caused by it:

| row | verdict | why |
|---|---|---|
| `FMT-13.4.6.2` SD | CONFORMS | one clause, modelled; residue diagnosed |
| `FMT-12.4.6.2` I-O-CONTROL | CONFORMS | both clauses modelled; residue diagnosed |
| `FMT-12.3.2` CONFIGURATION SECTION | CONFORMS | four paragraphs, all modelled; residue diagnosed |
| `FMT-13.16.2` data description entry | CONFORMS (re-verdict) | was already closed; the code-location moved |
| `FMT-13.4.5.2` FD | PARTIAL | `fileGlobalExternalClause : IS? (GLOBAL\|EXTERNAL)` has no `[ AS literal-1 ]`, so `FD F IS EXTERNAL AS "X".` is refused with COBOLNET0901 |
| `FMT-12.4.5.1` file control entry | PARTIAL | `assignClause` takes ONE `assignTarget`, but the printed format writes `{device-name-1\|literal-1} …` — `ASSIGN TO "a" "b"` is refused with COBOL0308 |
| `FMT-12.3.7.2` SPECIAL-NAMES | PARTIAL | the `dynamic-length-structure-clause` is not modelled at all — `DYNAMIC LENGTH STRUCTURE DLS1 IS PREFIXED.` is COBOL0001 |
| `FMT-11.2.1` IDENTIFICATION DIVISION | PARTIAL | the header is printed in BRACKETS and is therefore OPTIONAL; `PROGRAM-ID. X.` with no `IDENTIFICATION DIVISION.` is COBOL0001 |

**⛔ THE NOTE'S ROW 5 PREMISE IS WRONG, and the correction matters more than the row.** `SPECIAL-NAMES. WIBBLE
WOBBLE.` is NOT swallowed by the catch-all and is NOT a §12.3.7.2 general-format violation.
`implementorSwitchEntry` is `cobolWord (IS? cobolWord)?`, it precedes the catch-all, and IS is un-underlined in
the printed format — so a bare two-word entry is SHAPE-LEGAL as `device-name-1 [IS] mnemonic-name-3` and ANTLR
takes it first. Measured: `SPECIAL-NAMES. WIBBLE "WOBBLE".` (a literal, which no arm of the format admits) does
reach the catch-all and is now COBOLNET1970, while the two-word form still compiles. What refuses the two-word
form is §12.3.7.3 SR8 — "The implementor shall specify the names that are available for switch-name-1,
feature-name-1, and device-name-1" (`cite.py --check` OK) — an unenforced SEMANTIC rule over an
implementor-defined name set, which `DataBinder.Switches.cs` registers without checking. That is a different
mechanism and a different row (`SR-12.3.7.3-8`), reported for the registrar.

**Goldens.** Seven negatives (`pb829-fd-…`, `-sd-`, `-file-control-`, `-io-control-`, `-special-names-`,
`-configuration-`, `-identification-unrecognized-…`), each `*> reject-at: 85 2002 2014 2023`, plus ONE positive,
`conformance:2023/pb829_closed_formats_legal`, which writes a LEGAL clause or paragraph in every newly-closed
format and runs. The positive is not decoration: closing a general-format list against the lossy OCR diagram
REJECTS LEGAL SOURCE, which is strictly worse than the silence it replaces, so every list was rendered from the
printed page first and the positive is the standing witness that the rendering was right.

**Docs.** `docs/rearchitecture/DESIGN-frontend-grammar.md` §3.10 is rewritten from PB487's five-site "not closed
by this change" paragraph to the current eight-format table, the four-part shape, the ordering a fixer owes, and
what is deliberately NOT this mechanism (the COMMENT-ENTRY sinks, which are correct; `computerAttributes` →
PB830; SR8; the dynamic-length-structure gap). `docs/DIAGNOSTICS.md` regenerated.

**⛔ WHY THE NOTE IS `half` AND NOT `landed`.** The MECHANISM is complete at every one of the eight formats and
nothing of it is deferred. Four of the rows this note claims are nevertheless verdicted PARTIAL, and a note may
not go terminal while it holds a defective row — `DefectiveRowCoverageDriftTests` is the gate, and it went RED on
the first attempt to flip this to `landed`, which is the gate doing exactly what it was built for. Each of the
four is a DIFFERENT mechanism found by the audit this note demanded (render the printed page BEFORE closing the
list), each was MEASURED IDENTICALLY on `main` at `1840778e`, and each is written up as a note-ready paragraph in
the implementer report for the registrar to split out:

1. §13.4.5.2 — `fileGlobalExternalClause` has no `[ AS literal-1 ]` (`FD F IS EXTERNAL AS "X".` → COBOLNET0901).
2. §12.4.5.1 — `assignClause` takes ONE target where the printed format writes `{device-name-1|literal-1} …`.
3. §12.3.7.2 — the `dynamic-length-structure-clause` is not modelled at all; and §12.3.7.3 SR8 is unenforced.
4. §11.2.1 — the BRACKETED (therefore optional) `IDENTIFICATION DIVISION.` header is required.

When those four land, this note goes terminal with them.

**Two things the NIST corpus had been hiding behind the catch-all, both fixed here.** Closing a list is exactly
what makes an under-modelled sibling visible, and both were found by the gate rather than by reading:

* **`remarksContent` was the odd one of SIX comment-entry bodies.** `authorContent`, `installationContent`,
  `dateWrittenContent`, `dateCompiledContent` and `securityContent` are all `~DOT+` with a terminating DOT;
  `remarksContent` alone was `(IDENTIFIER | STRINGLIT)+` with NO terminating DOT, so any comment-entry word that
  happens to be a lexer token (`REMARKS. CCVS-STYLE REMARK TEXT.` — TEXT is one) ended the content early and the
  REST of the entry fell through to the catch-all. It is now `~DOT+` like its five siblings.
* **Three NIST X-cards the catch-all had been eating.** `XXXXX069` ("REPLACE WITH ADDITIONAL INFO (*OPT G ONLY)",
  an implementor's own FD clause) and `XXXXX053` (the implementor's own I-O-CONTROL entry) name constructs
  COBOL.NET does not provide, so `NistPreprocessor` now DELETES those cards — the CCVS-documented action for an
  optional card an implementor does not supply, and the same treatment kb/Work PB487 gave `XXXXX086-088` when it
  closed §13.16.2.

## Appended 2026-09-13 by REGISTRAR #4 — a lead that belongs to this note's mechanism

Re-measured on this worktree's own `dotnet build CobolSharp.sln -c Debug` at main `b5ca78fb`; every citation below was re-derived with `cite.py --check`.

### The four unmodelled general-format fragments this note's rows still carry (measured again)

PB829's landed mechanism was the `genericClause` catch-all; these four are the residue that keeps
`FMT-13.4.5.2`, `FMT-12.4.5.1`, `FMT-12.3.7.2` and `FMT-11.2.1` at PARTIAL. All four re-measured on this tree,
`--std 2023`:

| probe | source | result |
|---|---|---|
| `p24_fd_external_as.cob` | `FD F IS EXTERNAL AS "QBEXT".` | `COBOLNET0901: 'AS' is a reserved word ... cannot be used as a user-defined word` |
| `p25_assign_multi.cob` | `SELECT F ASSIGN TO "a.dat" "b.dat"` | `COBOL0308: unexpected '"b.dat"'. A data-name is expected here, not a literal.` |
| `p23_dynamic_length_structure.cob` | `DYNAMIC LENGTH STRUCTURE DLS1 IS PREFIXED.` | `COBOL0001: unexpected 'DYNAMIC'` |
| `p26_no_id_division.cob` | a source unit beginning `PROGRAM-ID. X.` | `COBOL0001: unexpected 'PROGRAM-ID'` |

All four are **rejects_legal_source** against the RENDERED general formats (PB829 measured them on the printed
folios: §13.4.5.2 Formats 1-3 print `[ IS EXTERNAL [ AS literal-1 ] ]`; §12.4.5.1 Formats 1-4 print
`ASSIGN { TO { device-name-1 | literal-1 } ... }` with the ellipsis on the inner brace pair; §12.3.7.2 lists
`[ dynamic-length-structure-clause ]`; §11.2.1 prints `[ IDENTIFICATION DIVISION. ]` in BRACKETS).

Code sites: `CobolData.g4#fileGlobalExternalClause` (the phrase rule `CobolParserCore.g4#externalizedNamePhrase`
already exists); `CobolIO.g4#assignClause` / `#assignTarget`; `CobolSpecialNames.g4#specialNameEntry` (with
`CobolData.g4#dynamicLengthClause` already parsing the reference §13.18.19.3 SR2 binds it to);
`CobolParserCore.g4#identificationDivision`.

⚠ §11.2.1's optional header is EDITION-GATED: X3.23-1985 required it, so making it optional is a 2002+
relaxation needing the `feedback_edition_gate_sweep` treatment plus a VERSION-MATRIX row. The other three are
not edition questions.

⚠ `FMT-12.3.7.2` is ALSO held by §12.3.7.3 SR8 (the unspecified implementor name set) — kb/Work **PB862**, an
adjudication. Landing this fragment does not close that row on its own.
