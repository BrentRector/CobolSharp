---
title: "PB1397 — six REQUIRED Annex A.1 character-set determinations the compiler already MAKES are written nowhere in docs/CONFORMANCE.md: item 25/26 (source-text encoding: per-file BOM detection, default UTF-8, several encodings per compilation group — CompilationInputs.ReadAllText; ⚠ the silent U+FFFD replacement of invalid bytes is a DEFECT, not a determination to write — see the step-3 section), item 32 (the one-and-only coded values of SPACE/QUOTE/ZERO, space fill and editing characters), item 44 (characters prohibited as a currency symbol — the empty set), item 74 (representation of ZERO/SPACE/QUOTE in both sets) and item 95 (hex-character-sequence with no corresponding character)"
id: PB1397
kind: defect
status: open
severity: MINOR
area: docs/conformance
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
cluster: ["PB1092", "PB160"]
spec_refs: ["8.1.2", "8.1.3.2", "8.3.3.2.4", "8.3.3.6.4"]
inventory_rows: ["GR-8.1.2-3", "GR-8.1.3.2-1", "GR-8.1.3.2-2", "GR-8.3.3.2.4-5", "GR-8.3.3.6.4-1", "DOC-A.1-25"]
closes_rows: []
tags: [cobolsharp, work, defect]
---
Filed 2026-09-24 by **REGISTRAR 15b** from lane-3 adjudication batch **15b** (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d2` @`071ff04ea`). Not re-probed by the registrar (MANDATORY-PRACTICES R2: every lead carries its repro and code site); origin/main since the pin carries only registrar landings (no src change), so nothing landed has touched this mechanism.

# PB1397 — OPEN

## The rule (every citation re-run by the registrar through `cite.py --check`)

**GR-8.1.2-3**

> The number of bytes used to represent a character of the national coded character …

`python scripts/spec/cite.py --check 8.1.2 "The number of bytes used to represent a character of the national coded character"` → `OK  §8.1.2 3)  (Computer's coded character set)`

**GR-8.1.3.2-1**

> The implementor shall define a mapping of each basic letter, basic digit, basic special …

`python scripts/spec/cite.py --check 8.1.3.2 "The implementor shall define a mapping of each basic letter, basic digit, basic special"` → `OK  §8.1.3.2 1)  (General rules)`

**GR-8.3.3.2.4-5**

> The implementor defines the result of specifying a hex-character-sequence-1 for which no corresponding character …

`python scripts/spec/cite.py --check 8.3.3.2.4 "The implementor defines the result of specifying a hex-character-sequence-1 for which no corresponding character"` → `OK  §8.3.3.2.4 5)  (General rules)`

## Finding — `character-sets` (silent; rows `GR-8.1.2-3`)

**Mechanism.** Two required-and-documented §8.1.2 implementor specifications have no documentation: the one-and-only-one coded character values for SPACE/QUOTE/ZERO, space fill and the editing characters in both the alphanumeric and national sets (Annex A.1 item 32), and the implementor-defined additional characters prohibited as a currency symbol (A.1 item 44, whose implemented answer is the empty set - DataBinder.ValidateCurrencyChar enforces only SR22/SR27). Bits-per-byte (A.1 item 12) is stated only inside the USAGE BIT prose, with no DOC-A.1-12 register row.

**Repro.** grep docs/ for a coded-character-value statement for SPACE/QUOTE/ZERO/'B'/'0' or a currency-symbol prohibition beyond §12.3.7.3 SR22/SR27: none; no DOC-A.1-12/-32/-44 rows exist in docs/CONFORMANCE.md.

**Expected from the spec.** §8.1.2 (cite.py OK): "The implementor shall specify one and only one alphanumeric coded character value associated with each of the following COBOL elements" (and the national twin), and "the implementor shall specify any such additional characters that are prohibited from use as a currency symbol"; Annex A.1 items 12, 32 and 44 are each required and shall be documented in the implementor's user documentation.

**Observed / reasoning.** The behaviour is consistent (' ' U+0020, '0' U+0030, quote U+0022 in both classes; no extra currency prohibitions) but the documentation obligation is unmet, so the conformance claim for these items is unevidenced.

**Code site.** docs/CONFORMANCE.md (missing DOC-A.1-12, DOC-A.1-32, DOC-A.1-44 rows); src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs#DataBinder.ValidateCurrencyChar

## Finding — `cobol-character-repertoire` (silent; rows `GR-8.1.3.2-1`, `GR-8.1.3.2-2`)

**Mechanism.** The source-text encoding determination (per-file BOM detection, default UTF-8, invalid bytes silently replaced by U+FFFD, several encodings allowed in one compilation group) exists only in CompilationInputs.ReadAllText and is not documented anywhere in docs/CONFORMANCE.md, so the required A.1 item 25 (and the conditionally required item 26) have no determination

**Repro.** (1) A fixed-form source saved as Latin-1 containing DISPLAY "caf<0xE9>". -> pinned cobol.exe --run prints bytes 63 61 66 EF BF BD with no diagnostic. (2) A UTF-8 source (no BOM) that COPYs a copybook saved UTF-16LE with BOM containing 01 CPY-ITEM PIC X(3) VALUE "é1". -> compiles and prints 'é1'.

**Expected from the spec.** 8.1.3.2 GR1 and Annex A.1 item 25 require the implementor to define AND document the mapping of the repertoire to its coded character set(s) (and substitute graphics, here none); GR2 and A.1 item 26 require specifying the method that distinguishes encodings when more than one is permitted in a compilation group. The user documentation must state: source decoded per file by byte-order mark (UTF-8/UTF-16LE/BE/UTF-32), UTF-8 when none; what happens to bytes invalid in that encoding.

**Observed / reasoning.** grep of docs/CONFORMANCE.md for source encoding, UTF-8 source, byte-order mark, 'repertoire (encoding' finds nothing; no DOC-A.1-25/-26 row. The behavior itself is a legitimate implementor choice, but the silent U+FFFD replacement of a non-UTF-8 byte in a literal changes program output without any indication -- the determination should either document it or make an undecodable byte a diagnostic (a strict UTF8Encoding(throwOnInvalidBytes: true)).

**Code site.** src/Cobol.Net.Frontend/Pipeline/CompilationInputs.cs#CompilationInputs.ReadAllText (File.ReadAllText); src/Cobol.Net.Frontend/Preprocessor/CopyProcessor.cs (~line 430)

## Measured (rows this note claims beyond the findings above)

### `GR-8.3.3.2.4-5` — PARTIAL (adjudicator + refuter record, verbatim)

cite.py --check 8.3.3.2.4 "The implementor defines the result of specifying a hex-character-sequence-1 for which no corresponding character" OK. BEHAVIOR: with two digits per alphanumeric character (SR6, HexGroupViolation) every sequence 00-FF maps to U+0000-U+00FF, all of which are characters of the UTF-16 alphanumeric repertoire, so no 'no corresponding character' case arises; characters occupy 16 bits (a multiple of four), so the second sentence's mapping is not triggered. Probe p1/p10: ORD(X"00")=1, ORD(X"C1")=194, X"FF"/X"80" round-trip to file bytes. UNVERIFIED HALF: the rule requires the implementor to DEFINE these results, and Annex A.1 item 95 (required, documented) has no row in docs/CONFORMANCE.md's A.1 documentation table (searched: no DOC-A.1-95 / 'hexadecimal' determination beyond the 11.9.10.3 one); the definition exists only implicitly in DecodeHex. The documentation row itself is rule DOC-A.1-95/96 (not this adjudication's), so no finding is filed here -- see dossier_gaps.

### `GR-8.3.3.6.4-1` — PARTIAL (adjudicator + refuter record, verbatim)

Rule: 'When a figurative constant is used in a context requiring national characters, the figurative constant represents a national character value ... the character value representation of the figurative constant ZERO (ZEROS, ZEROES), SPACE (SPACES), and QUOTE (QUOTES) is the value of the character '0', space, and '"', respectively ... The implementor shall specify the unique representation of ZERO, SPACE, and QUOTE in the computer's alphanumeric and national coded character sets.' (cite.py --check 8.3.3.6.4 -> OK 1)). BEHAVIOUR HALF CONFORMS: FillChar maps Z->'0', S->' ', Q->'"' for every category, and the category (national vs alphanumeric) comes from the associated operand (EmitCore/OperandText cite GR1). Probe probe/figurative-constant-values/p1.cob: MOVE ZERO/SPACE/QUOTE TO PIC X(3) and PIC N(3) -> '000|000', '[   |   ]', '"""|"""'; DISPLAY QUOTE ZERO SPACE -> '"|0| '. DOCUMENTATION HALF MISSING: the last sentence is Annex A.1 item 74 (cite.py --check A.1 -> OK 74) 'required ... shall be documented'), and docs/CONFORMANCE.md carries no DOC-A.1-74 row (its A.1 table jumps 73 -> 82); the representation is stated nowhere user-facing. The DOC-A.1-74 catalog row owns that debt; this row is PARTIAL until it lands (one half verified).

## What the fixer owes

Write DOC-A.1-25, -26, -32, -44, -74 and -95 rows in docs/CONFORMANCE.md from the code that makes each determination (CompilationInputs.ReadAllText; FigurativeConstants.FillChar; DataBinder.ValidateCurrencyChar; DecodeHex / HexGroupViolation), each with a pinning test; then re-adjudicate the five rows.

## Registrar-1 (kb/Work PB1522) — rows adjudicated on the `claude/adj-*` branches

Filed 2026-09-24 by REGISTRAR-1 (cloud, branch `claude/adj-registrar-1`). The adjudicators (tree `0caa7d5`) attributed these rows to this note; this note now claims them in `inventory_rows`.

- `DOC-A.1-25` — adjudicated **DIVERGES** on `claude/adj-a1-w1-s1`; NOT recorded — DOC rows wait on owner decision PB1535.
- `DOC-A.1-26` — adjudicated **DIVERGES** on `claude/adj-a1-w1-s1`; NOT recorded — DOC rows wait on owner decision PB1535.
- `DOC-A.1-32` — adjudicated **DIVERGES** on `claude/adj-a1-w1-s2`; NOT recorded — DOC rows wait on owner decision PB1535.
- `DOC-A.1-44` — adjudicated **DIVERGES** on `claude/adj-a1-w1-s2`; NOT recorded — DOC rows wait on owner decision PB1535.
- `DOC-A.1-95` — adjudicated **DIVERGES** on `claude/adj-a1-w1-s4`; NOT recorded — DOC rows wait on owner decision PB1535.

Adjudicator finding (`adj-a1-w1-s4`, harm silent): A fixed implementor determination exists in code (or in §3 prose) but has no '| DOC-A.1-N |' register row, so the traceability inventory cannot see it (missing/misfiled A.1 documentation)

- Repro: python scripts/spec/audit_annex_a1.py -> no row for 94/95/97/101/111; probes: GOBACK WITH NORMAL STATUS 300 -> exit 44; X"80" -> ORD 129; LENGTH(NX"D800")=1; INVOKE of REPOSITORY CLASS SB AS "System.Text.StringBuilder" -> COBOLNET0813/0823
- Expected: §4.2.5 + each A.1 item: 'This item shall be documented in the implementor's user documentation' — a determination row for exactly this item
- Observed: 94: substance in §3 STOP/GOBACK bullet labelled items 192/193; 111: substance in §3 directives bullet labelled item 112 (wrong item); 95/97/101: stated nowhere
- Code site: docs/CONFORMANCE.md §7; src/Cobol.Net.Frontend/Common/CobolLiteral.cs#DecodeHex/#DecodeHexGroups; src/Cobol.Net.Compiler/Binding/Procedure/Verbs/OoBinder.cs

Adjudicator finding (`adj-a1-w1-s2`, harm silent): a1-register-rows-missing-for-determinations-the-code-makes

- Repro: grep '^| DOC-A.1-(32|40|42|43|44|46|51|52) |' docs/CONFORMANCE.md -> none; python scripts/spec/audit_annex_a1.py lists them undischarged
- Expected: Each item: 'This item is required/conditionally required ... shall be documented in the implementor's user documentation' (§A.1, cite.py OK per row)
- Observed: The code fixes each answer (FillChar values; FindCopybook search order; ORDER TABLE literal-9 set - documented but under §2 row 25, not the §7 key; ToUpperInvariant currency equivalence; empty extra-prohibition set; per-usage representations with 47/74/81/213/217 unregistered; DELETE FILE = one host unlink; every host path treated as concurrently accessible).
- Code site: docs/CONFORMANCE.md §7

Adjudicator finding (`adj-a1-w1-s1`, harm silent): Annex A.1 determinations that the code already fixes are written nowhere under their DOC-A.1-<n> key (or only incidentally, off-key)

- Repro: grep -n 'DOC-A.1-<n>' docs/CONFORMANCE.md returns nothing for each item. Probes: ord.cob (group BYTE-LENGTH 3 = no filler, so the group is coincident with its first item; ORD("A")=66 = code point + 1); hx2.cob (X"414" -> COBOLNET1635, so 2 digits per character); call17.cob (a failed CALL has no other effect: RETURNING and USING items unchanged); chr.cob (CHAR on a locale tie returns the lowest-coded member); rep.cob ('§' accepted in a REPLACE text-word); enc.cob (per-file BOM, UTF-8 default, U+FFFD substitution, UTF-16 copybook in a UTF-8 group).
- Expected: Each item: 'This item is required [or provided]. This item shall be documented in the implementor's user documentation.'
- Observed: The behavior is deterministic and the code fixes it, but no determination row exists. Item 9's only statement cites §8.3.3.2.3 SR5 instead of SR6. Item 16's substance sits in §3 prose and in DOC-A.1-19 and does not name the EC-PROGRAM-NOT-FOUND outcome. The DOC-A.1-22 row documents the CHAR-NATIONAL twin of item 21 but not CHAR.
- Code site: docs/CONFORMANCE.md §7

Adjudicator finding (`adj-a1-w1-s1`, harm silent): Undecodable source bytes are silently replaced by U+FFFD, which changes literal content without a diagnostic

- Repro: A UTF-8 (no BOM) source whose literal "A<0xE9>B" DISPLAYs A EF BF BD B, with no warning
- Expected: §8.1.3.2 GR1: the implementor defines and documents the mapping; the substitution must at least be documented.
- Observed: File.ReadAllText's default decoder replaces the byte; nothing is documented.
- Code site: src/Cobol.Net.Frontend/Pipeline/CompilationInputs.cs#ReadAllText

Each row's full evidence (rule text with its `cite.py --check` line, probes, determination) is the record's `notes` in `adjudication/registrar-1/batch-{doc,nondoc}.json` on the registrar branch.

## PB1522 step 3 (doc-rows-1 integrator, 2026-09-24 23:55 UTC, branch `claude/adj-doc-rows-1`)

Rows **26, 32, 44, 74 and 95** are written in `docs/CONFORMANCE.md` §7 and recorded CONFORMS (26, 32, 44, 74, 95 still test-needed); their claims leave this note. **Item 25 was OMITTED**, and this note's title is amended accordingly: the silent replacement of a byte sequence invalid in the detected encoding with U+FFFD (`CompilationInputs.ReadAllText` → `File.ReadAllText`) changes a literal's content without a diagnostic, and CLAUDE.md rule 1's precedent for this latitude — GnuCOBOL 3.2 — passes source bytes through unchanged (cobc runs no iconv on source text). So the U+FFFD substitution is a defect to FIX (pass the bytes through, or a strict decoder with a diagnostic naming the offset), not a determination to document. DOC-A.1-25 stays claimed here until then.

## doc-rows-2 landing (2026-09-25)

**The fix is now DETERMINED (doc-rows-2 item 25, refuter + orchestrator):** a source or library file with no
byte-order mark that is not well-formed UTF-8 is read as ISO/IEC 8859-1 (every byte the character with that code
point) WITH a warning naming the file; a file whose byte-order mark names an encoding its content is not
well-formed in (invalid UTF-8 after EF BB BF, a lone surrogate in UTF-16, a UTF-32 unit beyond U+10FFFF) is a
compile-time ERROR naming the file and the byte offset — no fallback for a marked file. No source character is
ever replaced by U+FFFD. DOC-A.1-25 and DOC-A.1-26 state it; the 'pass-through or strict decoder' alternatives in
this note's body are superseded. Code site: `CompilationInputs.ReadAllText` (`File.ReadAllText`).
