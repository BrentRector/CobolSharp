---
title: "PB1506 — the standard class BASE (§16.1 'A standard class BASE shall be provided by the implementation') is not provided: `CLASS-ID. X INHERITS FROM BASE` with `REPOSITORY. CLASS BASE.` is refused COBOLNET0821 'no such a class is defined in this compilation group', and neither BaseFactoryInterface nor BaseInterface (§16.2) exists"
id: PB1506
kind: defect
status: landed
severity: MAJOR
area: binder/oo
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1086"]
spec_refs: ["16.1", "16.2"]
inventory_rows: []
closes_rows: ["GR-16.2.1.2-1", "GR-16.2.2.2-1"]
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 15d** from lane-3 adjudication batch **15d** (adjudicated on the pinned tree `071ff04ea`; everything that landed since — Registrar 14a/14b/14c — is register-only, so no code change touches this mechanism). Every citation below was re-run by the registrar through `cite.py --check`.

# PB1506 — LANDED (wave 59, with PB1548)

## The rule (every citation re-run through `cite.py --check`)

> A standard class BASE shall be provided by the implementation. It may be used as the root of a class hierarchy to provide standard object life-cycle function.

`OK  §16.1   (General)`


UNFILED LEAD from the batch-15d refuter of `GR-9.3.6-L2.2` (objects-p1), re-run by the registrar on its own Debug build of `8bd4c7c9f`:

```
       CLASS-ID. BSX INHERITS FROM BASE.
       ... REPOSITORY. CLASS BASE.
       OBJECT. PROCEDURE DIVISION. METHOD-ID. HELLO. ... END CLASS BSX.
       PROGRAM-ID. BSMAIN. ... INVOKE BSX "NEW" RETURNING R. INVOKE R "HELLO".
```

`cobol base1.cob --std 2023 --run` → `base1.cob(2,36): error COBOLNET0821: class 'BSX': INHERITS FROM 'BASE': no such a class is defined in this compilation group (ISO §11.3.3 SR2)`. Probe: `E:\Temp\claude\E--CobolSharp\f49c94e8-2693-47b6-90b2-d1c7475a4bf6\scratchpad\p15d\base1.cob`. Code site: `src/Cobol.Net.Compiler/Oo/OoClassTable.cs#Build` (base resolution → COBOLNET0821) and `src/Cobol.Net.Compiler/Oo/OoNameResolution.cs#Lookup` (per-compilation-group table, no predefined class). The inventory rows of §16.2 (`GR-16.2.1.2-1`, `GR-16.2.1.2-2`, `GR-16.2.2.2-1`, …) are GAP and unadjudicated; the implementer claims them on landing.

## What the fixer owes

Provide BASE as a predefined class available to every compilation group through the REPOSITORY (the external-repository gap is PB1086; BASE must resolve without it): its factory NEW and the object life-cycle methods of §16.2 (BaseFactoryInterface / BaseInterface), implemented per the §16.2 method specifications — the NOTE allows it not to be written in COBOL, so a runtime class is fine. Classes that do not name BASE keep today's implicit root. Goldens: a class inheriting BASE using NEW and the §16.2 methods; conformance of a user override to BaseInterface.

## Registrar-1 (kb/Work PB1522) — rows adjudicated on the `claude/adj-*` branches

Filed 2026-09-24 by REGISTRAR-1 (cloud, branch `claude/adj-registrar-1`). The adjudicators (tree `0caa7d5`) attributed these rows to this note; this note now claims them in `inventory_rows`.

- `GR-16.2.1.2-1` — adjudicated **PARTIAL** on `claude/adj-w2-misc-s3`; recorded.
- `GR-16.2.2.2-1` — adjudicated **NOT-IMPLEMENTED** on `claude/adj-w2-misc-s3`; recorded.

Adjudicator finding (`adj-w2-misc-s3`, harm rejects-legal-source): base-class-and-factoryobject-absent

- Repro: probe/base1.cob: CLASS-ID. BSX INHERITS FROM BASE. REPOSITORY. CLASS BASE. -> COBOLNET0821 at --std 2023; probe/new1.cob: INVOKE B1 "FactoryObject" RETURNING F1 -> COBOLNET0825.
- Expected: §16.1 'A standard class BASE shall be provided by the implementation'; §16.2.2.2 GR1 FactoryObject returns the factory of the object's class.
- Observed: ALREADY OWNED - kb/Work PB1506 (open). Re-measured on 0caa7d5, unchanged. Listed only so the registrar can attach the two rows; do not file again.
- Code site: src/Cobol.Net.Compiler/Oo/OoClassTable.cs#Build; src/Cobol.Net.Compiler/Oo/OoNameResolution.cs#Lookup

Each row's full evidence (rule text with its `cite.py --check` line, probes, determination) is the record's `notes` in `adjudication/registrar-1/batch-{doc,nondoc}.json` on the registrar branch.

## Landed (wave 59, implementer J — with PB1548)

The standard class BASE is provided: `src/Cobol.Net.Compiler/Oo/OoStandardClasses.cs` (the symbol, New in its factory
roster and FactoryObject in its instance roster, each with its section 16.2 signature) resolved through `OoClassTable.Find` when the
REPOSITORY declares `CLASS BASE`; runtime `src/Cobol.Net.Runtime/Control/BASE.cs` (`BASE` / `BASE__FACTORY`). Every
receiver form binds through `OoBinder.OoBindStandardInvoke`; a class that does not inherit BASE has no New
(COBOLNET2448). Witnesses: `conformance:2002/pb1548_standard_class_base`, `StandardClassBaseTests`,
`StandardClassBaseNewResourceTests`. Classes that do not name BASE are NOT given an implicit root with New (the "Fix
shape" above suggested keeping it; PB1548 determined that SR3 forbids it).
