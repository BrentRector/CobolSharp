---
title: "PB838 — both citation audits are structurally blind to a PARAPHRASED citation - they check only verbatim-shaped quotations - so a clause number attached to a claim about the standard propagates through code comments, design docs and the DEVLOG with every gate at zero findings; nine sites carried a §13.4.2 the standard does not contain, and the surviving MoveEmitter GR5/GR6c spellings are the same hole"
id: PB838
kind: defect
status: landed
severity: MAJOR
area: scripts/spec
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
cluster: [PB900, PB388, PB946]
spec_refs: ["13.18.33.4", "14.9.25.4"]
inventory_rows: []
closes_rows: []
closes_rows_reason: "an audit arm and seven citations in diagnostic strings carry no traceability row; no rule verdict moved"
tags: [cobolsharp, work, defect]
---

Filed 2026-09-13 by REGISTRAR #4 (the lead-filing pass over the train 27-33 implementer, finisher and lander reports). Every probe below was REBUILT from the source report and RE-RUN on this worktree's own `dotnet build CobolSharp.sln -c Debug`, first at main `b5ca78fb` and RE-RUN unchanged at `e13ef810` (train 32's head, after PB415/PB394/PB355/PB526/PB646 landed); every citation was re-derived with `python scripts/spec/cite.py --check` and the command line is printed beside the quotation.

# PB838 — LANDED — the audits see quotations, and the failure mode is a claim with no quotation

<!-- audit-doc-citations: names-misfilings - this note PAIRS clauses with the wrong text on purpose;
     that pairing is the defect it records. -->

## LANDED — 2026-09-21 (wave 42 group G, worktree `wf_031fc86a-7dd-6`, base main `4da7ba02d`)

**The check that landed is NOT the one the note's "Shape of the fix" section proposes, and the difference was
measured rather than argued.** That section asks for an `UNQUOTED CLAIM` class — "does this clause REFERENCE
carry any `--check`able quotation at all in its vicinity". Measured on this tree, that population is essentially
the whole corpus: `audit_code_citations` alone scans 4529 files, and the commonest citation in the repository is
a statement-header comment with no quotation by design (`// MOVE (§14.9.25)`). A check that fires on thousands
of correct sites cannot gate anything and trains the reader to skip it — the module's own lesson from the
SUBJECT check's first draft.

**What the note's SECOND half asks for is decidable, and it is what landed** (registrar #5's addition, the
paragraph headed *"A citation that omits the SUBCLASSIFYING subclause is invisible"*): *any `§`-reference in a
STRING LITERAL that reaches a diagnostic must resolve to a clause the citation checker can address — it must
carry the subclause, and the rule number it names must exist in that subclause.* That is mechanically decidable
from the literal alone, needs no quotable fragment, and is the population with the highest cost per error.

### The arms

`scripts/spec/audit_code_citations.py`, scoped to C# string literals under `src/` (`citation_corpus.code_files`):

* **`DIAG-NO-RULE` — GATES.** A rule KIND with no ordinal: `(ISO §14.9.18 SR)`. It names no rule, so it can be
  neither confirmed nor refuted and `cite.py` has nothing to resolve. **THE SCOPE IS THE WHOLE CHECK**, and it
  was measured before it was chosen: the same shape is **2683 sites tree-wide** in prose (`§13.5.3 SR 1`, the
  inventory row id `SR-14.9.28.3-2`, "the 1561-1563 SR band") and **5 inside message strings**. A citation in a
  comment sits beside the derivation that justifies it; a citation in a message is all the user gets.
* **`DIAG-UNQUALIFIED` — MEASURED, not gating.** The clause carries no rule block of that kind of its own while
  a child does: `§14.9.39 SR17`, where the syntax rules are §14.9.39.3. The ordinal arms resolve that through
  `_alias`, which keys a block under both spellings because this repository's PROSE cites it both ways — right
  there, wrong in a message. **160 sites**, so it joins `RULE`/`SUBITEM` in the reported backlog and gates under
  `--check-all`. The audit's backlog headline now tallies per family instead of describing all three as
  rule-ordinal findings.
* **The RANGE question is deliberately NOT re-implemented here.** `RULE`/`SUBITEM` already run over every line
  of every file, message strings included, and report **zero** out-of-range ordinals inside diagnostic strings —
  measured, not assumed. A second implementation would be a second home for one rule.

### ⛔ THE ARM'S FIRST RUN ACCUSED TWO CORRECT CITATIONS, and reading its output is what caught it

After the five real sites were repaired, `DIAG-NO-RULE` still reported `OoBinder.cs:645` (`§14.9.23.3 SR 10`)
and `ConstructRegistry.g.cs:119` (`§13.5.3 SR 1`, generated from `constructs.json`). **Both are correct
citations** — the ordinal is written with a SPACE. The regex now excludes `(?!\s*\d)` and the space-separated
form is a silent self-test case. An arm is evidence about what it RETURNED, never about what it dropped
(`feedback_measure_the_selectors_complement`), and a finding is a claim about a line.

### The seven repairs, each `cite.py --check`ed before it was written

| site | was | is | why |
| --- | --- | --- | --- |
| `DataBinder.cs:4251` | `ISO §13.18.5 SR` | `ISO §13.16.3 SR3` | ⛔ **WRONG CLAUSE.** §13.18.5 is the BASED clause; its two SRs are about class object and dynamic-length items. The prohibition is the data description entry's. |
| `DataBinder.Odo.cs:179` | `ISO §13.18.44 SR` | `ISO §13.18.44.3 SR5` | "Neither the original definition nor the redefinition shall include an occurs-depending table" |
| `DataBinder.Oo.cs:665` | `ISO §13.18.42 SR` | `ISO §13.18.42.3 SR2` | "The PROPERTY clause shall not be specified for data items subject to an OCCURS clause." |
| `CallEmitter.cs:788` | `ISO §14.9.18 SR` | `ISO §14.9.18.4 GR2` | ⛔ **WRONG KIND.** §14.9.18.3 has no rule about RETURNING at all; the rule is a GENERAL one. |
| `CallEmitter.cs:875/876` (emitted C#) | `§14.9.18 GR` | `§14.9.18.4 GR1b` | "an exception condition is raised in the activating runtime element" |
| `ReportWriterBinder.cs:95` | `ISO §14.9.21 SR1` | `ISO §14.9.21.3 SR1` | the ordinal was right; the subclause was missing |
| `OoClassTable.cs:475` | `ISO §11.8 SR` | the `citation` parameter | ⛔ **A TWO-ARM DISPATCH WITH ONE ARM FIXED.** |

The last one is the sibling this note did not know it had. `CaptureImplements` is ALREADY given its paragraph's
own citation — `ISO §11.8.3 SR1` for OBJECT, `ISO §11.4.3 SR1` for FACTORY — and threads it into the RESOLUTION
arm; the DUPLICATE arm beside it hard-coded §11.8, so a duplicate IMPLEMENTS in a **FACTORY** paragraph was
reported against the **OBJECT** paragraph's clause. One citation, one parameter
(`feedback_two_arm_dispatch` — this repository's most reproducible defect shape).

**Witness:** `tests/conformance/negative/based-redefines-conflict.err` now carries the whole message including
`(ISO §13.16.3 SR3)`, so a re-inheritance of §13.18.5 fails by name through the corpus runner; the arms
themselves are pinned by `DIAG_SELF_TEST` (two firing defects, three silent calibrations) and by
`CitationAuditSelfTestDriftTests` (Unit), which asserts the case NAMES.

### Still open, and named here rather than silently dropped

* The 160 `DIAG-UNQUALIFIED` sites are a SWEEP, not a defect of this note — one derivation per site, exactly
  like the `RULE`/`SUBITEM` backlog they now print beside. Filed as a lead in the implementer report.
* The MoveEmitter `§14.9.25.4 GR5`/`GR6c` spellings this note lists as survivors are a RULE-ORDINAL question,
  not a resolvability one; they belong to that same sweep.
* **Is a duplicate IMPLEMENTS illegal at all?** §11.8.3 has two syntax rules and neither forbids naming an
  interface twice; the general format is `IMPLEMENTS { interface-name-1 } …`. COBOLNET0840's duplicate arm may
  be an OVER-REJECTION. That is a semantic question, out of this wave's scope, and is filed as a lead.


Source: the `PB355` implementer report, "New defects" item 1; the same hole is named independently by the
`PB366` report (item b, a "RESOLVED" line in a deep-dive that paraphrases a clause) and the `PB492` report
(item A, `MoveEmitter`'s §14.9.25.4 "GR5"/"GR6c"). CLAUDE.md rule 1 names this exact failure mode — a citation
is INHERITED rather than invented.

## The hole, demonstrated

```
$ python scripts/spec/cite.py --check 13.4.2 "each of these record description entries is a description of the same storage area"
  FAIL: §13.4.2 exists but does NOT contain ...
$ python scripts/spec/cite.py --check 13.18.33.4 "Multiple level 1 entries subordinate to a FD or SD entry represent implicit redefinitions of the same area"
  OK  §13.18.33.4 3)  (General rules)
```

Nine code and doc sites asserted "multi-01 FDs share one area (ISO §13.4.2)" while **both audits sat at zero
findings**, because none of the nine carried a quotable fragment: each was a clause number followed by an
em-dash-introduced assertion. PB355 repaired those nine. What is not repaired is the audit.

## Known survivors, for the fixer's first pass

* `src/Cobol.Net.Compiler/CodeGen/Verbs/MoveEmitter.cs` — "§14.9.25.4 GR5" for the editing path and "GR6c" for
  JUSTIFIED. GR5 is de-editing only (`cite.py --check 14.9.25.4 "De-editing takes place only when the sending
  operand is a numeric-edited data item"` -> `OK §14.9.25.4 5)`); the editing rule is GR6 and the alignment rule
  GR6 a). The same spellings recur in `AcceptDisplayEmitter`, `StringEmitter` and `PicInfo`.
* `src/CobolSharp.Compiler/Semantics/DataItemClassifier.cs:189` cites "ISO §13.18.63 GR10" for what is SR10
  (`cite.py --check 13.18.63.3 "If the category of the subject of the entry is boolean, literals in the VALUE
  clause shall be boolean literals."` -> `OK §13.18.63.3 10) (Syntax rules)`). Legacy oracle, slated for deletion
  at P15 — listed for completeness, not for urgency.

## Shape of the fix (structural, not a list)

`scripts/spec/audit_code_citations.py` and `audit_doc_citations.py` today answer "does this quotation appear in
this clause". They need a second question: "does this clause REFERENCE carry any `--check`able quotation at all
in its vicinity". A reference followed by an assertion and no quotation is a finding of its own class
(`UNQUOTED CLAIM`), reported separately from a wrong quotation so the two are not confused. Pair it with a
drift test that plants one unquoted claim and asserts the audit reports it — the gate must fail once before its
silence is evidence (`feedback_green_gates_arent_evidence`).

## Harm

`process_only` in the sense that no user program misbehaves — but this is the mechanism by which a wrong rule
reaches goldens and code, which is how PB355's nine sites and two CA10 citations happened. Severity MAJOR for
that reason.

## Sibling sweep the fixer owes

* Count the "RESOLVED" / "holds structurally" claims in `docs/rearchitecture/DESIGN-*.md` that carry a clause
  number and no quotation — PB366's report asks for exactly this measurement.
* `kb/Work/` note bodies are in the same population (the register is where an adjudicator reads a rule).

## A SECOND blindness of the same auditor, measured 2026-09-13 by REGISTRAR #5

From the `PB390` implementer report. This note's subject is a citation attached to a PARAPHRASE; the same
auditor is blind to a second shape, and that shape is live in SHIPPED DIAGNOSTIC TEXT — the one place a wrong
clause number reaches a user directly.

**A citation that omits the SUBCLASSIFYING subclause is invisible.** `audit_code_citations.py` keys on a
`§N.N.N.N` reference beside a quotable fragment. These three are in diagnostic strings a user reads:

```
src/Cobol.Net.Compiler/Binding/Procedure/Verbs/SearchBinder.cs:29
    "SEARCH table '{tableName}' without INDEXED BY (ISO §14.9.37 SR1)"
src/Cobol.Net.Compiler/Binding/Procedure/Verbs/ReportWriterBinder.cs:65
    "INITIATE '{...}' — not a report description entry (ISO §14.9.21 SR1)"
src/Cobol.Net.Compiler/Binding/Procedure/Verbs/EvaluateBinder.cs:55
    "EVALUATE: more WHEN objects than subjects (ISO §14.9.13 SR)"
```

`§14.9.37` is the SEARCH statement's clause; its syntax rules are `§14.9.37.3`. `§14.9.13 SR` names no rule at
all. The audit reports zero findings on all three.

**And the first of them is WRONG, not merely unqualified:**

```
$ python scripts/spec/cite.py --check 14.9.37.3 "The data description of identifier-1 shall contain an OCCURS clause with an INDEXED phrase"
  OK  §14.9.37.3 2)  (Syntax rules)
$ python scripts/spec/cite.py --check 14.9.37.3 "Identifier-1 shall not be reference-modified."
  OK  §14.9.37.3 1)  (Syntax rules)
```

The message says SR1. SR1 is the reference-modification rule. The INDEXED-phrase rule is SR2. A user who looks
it up reads a rule about something else — which is exactly the harm this note is about, one step closer to the
user than a code comment.

**What the auditor should gain, stated as a rule rather than as three fixes:** any `§`-reference in a STRING
LITERAL that reaches `DiagnosticCatalog` or a `BoundUnsupported` message must resolve to a clause the citation
checker can address — i.e. it must carry the subclause, and the rule number it names must exist in that
subclause. That is mechanically decidable from the literal alone (the clause hierarchy is in
`specs/ISO_COBOL.md`), needs no quotable fragment, and it is the population with the highest cost per error.
`kb/Work/PB390` carries the eight sites and their correct rule numbers; this note carries the auditor.
