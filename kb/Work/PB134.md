---
title: "PB134 — ✅ LANDED 2026-08-29 (one Format-2/Format-1 discipline over four verbs; DELETE FILE Format 2 whole; END-ACCEPT/END-DISPLAY gates live; the DIVIDE crash is COBOLNET1689). WAS: statement grammars reject or mis-parse legal source across ADD/DELETE FILE/ACCEPT/DIVIDE: ADD's TO arm never got receivingOperand so `ADD 1 TO 2 GIVING C` is a parse error (and a function-identifier sending operand too, held open by a GREEN drift test that over-extends SR1 to sending roles); DELETE FILE lacks Format 2's repetition, OVERRIDE and the optional ON; END-ACCEPT exists only as a lexer token; DIVIDE's bare-literal INTO crashes the compiler and extra operands are silently dropped"
id: PB134
kind: defect
status: landed
severity: MAJOR
area: grammar
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB134

Rows: FMT-14.9.2.2, FMT-14.9.10.2, FMT-14.9.1.2, FMT-14.9.12.2, GR-14.9.10.4-12, SR-14.9.10.3-4.

(a) `addToPhrase : TO receivingArithmeticOperand+` admits no literal though Format 2 prints `TO {identifier-2 | literal-2}`; the production for it (receivingOperand) already exists and SUBTRACT/MULTIPLY use it — AND ArithmeticSendingOperandDriftTests.ReceivingOperandRules_DoNotAdmitAFunctionIdentifier PINS the defect in place (its §8.4.3.2.3 SR1 justification is right for receiving roles, wrong for ADD-TO/MULTIPLY-BY/SUBTRACT-FROM sending roles — the production must split before the drift pin can pass). (b) deleteFileStatement is `DELETE FILE fileName` — no `{file-name-1}…` repetition (GR12), no [OVERRIDE] (OVERRIDE binds AS the file-name), and deleteFileOnException requires the optional ON (the arithmeticOnSizeError repair's missed sibling). (c) END_ACCEPT is referenced by NO parser rule — `ACCEPT X END-ACCEPT` is a syntax error at 2002+ while both gate consumers are structurally dead (constructs.json `pending`; VersionConformancePass's own comment says it fires zero times). Also END-DISPLAY has NO gate at all — accepted at 85 (the end-accept twin is registered and checked; asymmetry). (d) `DIVIDE 2 INTO 10.` throws InvalidOperationException through the CLI (targets.Max on empty); `DIVIDE A INTO B C GIVING D` silently drops C; `DIVIDE A INTO B ROUNDED GIVING C` silently drops the ROUNDED; three more non-format shapes reach run-time louds. (e) CANCEL/CALL national+hex literal targets: PB130's (d) — fix together at the literal-target helper.

*(Found by Phase-B batch 8 — §8.8.1 + §14.9.1–14.9.12, 2026-08-29; one adjudicator + one independent refuter per subject. Full forensic evidence: the named rows' `notes` in `tests/version-matrix/traceability-inventory.json`; the raw finding lines in the session scratchpad `pb-batch8/findings.txt`.)*
