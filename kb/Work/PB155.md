---
title: "PB155 — ✅ LANDED 2026-08-29 (EditMask:null guard dropped AND the screen widened to the R29 index-window contexts; STRINGLIT/HEXLIT + non-ZERO figurative → bind-time 0844; InComposite admits numeric-edited via StoredShapeOf with the P-split anchor fix; UNSTRING SR2 as ONE predicate for all four identifiers; → PB169-PB172 registered). WAS: the arithmetic SENDING screen's holes: an ALPHANUMERIC-EDITED or NATIONAL-EDITED operand slips NonNumericOperandKind's `EditMask: null` guard and is silently digit-decoded under STRICT (the --permissive leniency applied unconditionally); STRINGLIT/HEXLIT fall through to BoundNumLiteral and surface as Roslyn errors (the PB94 VALUE-clause fix's arithmetic sibling); CheckComposite counts E-form literals the §14.7.7 r2b exclusion removes (spurious COBOLNET0805 on legal ADD/DIVIDE) while dropping numeric-edited resultants it must count"
id: PB155
kind: defect
status: landed
severity: MAJOR
area: binding
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB155

Rows: SR-14.9.2.3-1 (DIVERGES)/-2/-3, SR-14.9.12.3-1/-3/-4, findings [ADD statement]/[divide-statement].

(1) NonNumericOperandKind's guard `{ Category: Alphanumeric or National, EditMask: null }` — PictureAnalyzer builds alphanumeric-edited as Alphanumeric WITH EditMask (no AlphanumericEdited member), so `ADD <PIC XXBXX> TO N` binds silently and FieldNumCore digit-decodes; drop the EditMask condition (the de-editing MOVE grant is §14.9.25.4 GR6d1's alone). (2) STRINGLIT/HEXLIT: CheckLiteral returns the quoted text; IntLiteralX emits `"ABC"L` — wrong stage; screen at NumLiteral. (3) CheckComposite: the BoundNumLiteral arm counts E-form digits (r2b excludes floating-point literals) — `ADD 1.5E+3 TO F(9(28))` rejects legal source — and InComposite requires Category==Numeric so a numeric-edited identifier-3 (SR2-admitted) contributes nothing — both directions wrong in one method.

*(Found by Phase-B batch 8 — §8.8.1 + §14.9.1–14.9.12, 2026-08-29; one adjudicator + one independent refuter per subject. Full forensic evidence: the named rows' `notes` in `tests/version-matrix/traceability-inventory.json`; the raw finding lines in the session scratchpad `pb-batch8/findings.txt`.)*
---

## ✅ LANDED 2026-08-29 — wave gate GREEN; DEVLOG Entry 1391

The three mechanisms landed as described. What the landing itself corrected or added:

- **The batch-8 "DIVIDE SR1 receiving half unenforced" claim was STALE** — it read ResolveReceiving alone
  and missed PB128's ScreenResultant right behind it (`DIVIDE 2 INTO <PIC X>` was already COBOLNET1675;
  probed, now pinned DIVIDE-shaped by pb155-divide-into-alpha).
- **A fourth defect shape**: a non-ZERO FIGURATIVE arithmetic operand was a bare BoundExprError with no
  diagnostic — a runtime NotImplemented on illegal source; now bind-time 0844 (pb155-divide-figurative).
- **The review fleet (73 agents, 15 confirmed findings)**: the §8.8.1.1 screen was BYPASSED for
  OperandContext.ArithmeticIndexWindow (R29 flipped eleven call sites claiming "identical screening" —
  `SET IX TO <edited>` digit-decoded under STRICT; the DATA-ITEM guard widened, pb155-set-to-edited,
  while the INTRINSIC guard's widening was gate-measured rejecting six NIST IF programs' legal sole
  function comparands and reverted → the two-regime conflation is [[PB172]]; the same gate exposed
  pb26's UNSTRING leg carrying an SR2-illegal numeric LOG10 delimiter — now FUNCTION CHAR(0),
  §15.15.3 AR2, .out unchanged);
  the REMAINDER golden leg could not fail (composite is maxInt+maxFrac, not a sum — Q is 9V9 now);
  UNSTRING SR2 names identifier-1/-2/-3/-5 and only the sender was screened (Sr2OffendingCategory is the
  ONE predicate; pb155-unstring-numeric-delimiter; the PB88 row's overbroad CONFORMS re-recorded);
  StoredShapeOf's P-split anchored on 9/Z/* only, so `PIC $$$$PP` read its trailing P as LEADING — the
  anchor fix landed in BOTH CobolEdit.FractionDigits and PictureAnalyzer with CobolEditMaskGeometryTests
  (14 facts) pinning it.
- **Registered, not crammed**: PB169 (STOP/GOBACK STATUS literal-1's non-numeric form — legal COBOL now
  rejected under a miscited §8.8.1.1; the fleet proved no regression), PB170 (a SIMPLE subscript bypasses
  the screen at the resolver), PB171 (a bare non-numeric literal sign-condition operand degrades to 0).
- **Shaped but unreachable** (Phase 4a residue #2 — a national-edited ITEM is 0899 at declaration; no
  fixture on a loud stage): STRING INTO SR5's national-edited arm, UNSTRING SR4's national-arm EditMask
  screen, Sr2OffendingCategory's national-edited reporting.
- The first composite fixture drafted a 33-digit-position edited PICTURE — itself illegal (§8.3.1.2); the
  compiler's own cap caught it, and the landed fixture uses a LEGAL 31-position Z9.9(29).

Rows: SR-14.9.2.3-1/-3 DIVERGES → CONFORMS · SR-14.9.12.3-1/-3/-4 PARTIAL → CONFORMS ·
SR-14.9.48.3-2 re-recorded (honest evidence). GAP 3672 → 3667. Battery #34 is owed for the since-#33
batch (PB151 + PB154 + PB155).
