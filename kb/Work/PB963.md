---
title: "PB963 — the >>TURN file-name-1 arm and GR5's 'does not apply to any phrase of that statement' have NO witness: GR-7.3.25.4-5, -6 and -8 are GAP and unclaimed, although two probes show the file-name-1 ON and OFF arms behaving to the rule on main"
id: PB963
kind: analysis
status: open
severity: MINOR
area: runtime/exceptions
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: [PB841, PB891, PB941]
spec_refs: ["7.3.25.4", "14.6.13.1.3"]
inventory_rows: ["GR-7.3.25.4-5", "GR-7.3.25.4-6", "GR-7.3.25.4-8"]
closes_rows: []
tags: [cobolsharp, work, analysis]
---

Filed 2026-09-22 by REGISTRAR #10 from the wave-45 group-C lead (PB891 implementer report). Group C's report says
these three rows stay GAP and unclaimed after its batch, and no other note names them.
`grep -l "GR-7.3.25.4" kb/Work/*.md` on main finds none. **This is a MEASUREMENT gap, not an observed defect.** The
probes below, run on this registrar's own build of main `f503888da`, behave to the rule.

## Rules (each `cite.py --check`ed)

- `--check 7.3.25.4 "If specified within a statement, the TURN directive does not apply to any phrase of that statement."` → `OK §7.3.25.4 5)`
- `--check 7.3.25.4 "if file-name-1 is specified, checking is enabled only for exception conditions associated with that file-name"` → `OK §7.3.25.4 6)`
- `--check 7.3.25.4 "if file-name-1 is specified, checking is disabled only for exception conditions associated with that file-name"` → `OK §7.3.25.4 8)`
- `--check 14.6.13.1.3 "If execution of the declarative completes normally the execution of the run unit is terminated abnormally"` → `OK §14.6.13.1.3 5)` (why the OFF probe below ends in an abnormal termination, correctly)

## Probes (scratchpad/reg10)

`turnfn.cob`: two SEQUENTIAL files, both with FILE STATUS, both assigned to missing files.
`>>TURN EC-I-O F1 CHECKING ON`, then `OPEN INPUT F2`, then `OPEN INPUT F1`:

```
F2 S=35 EC=[                               ]        ← F2 not checked: no EC raised
abnormal run-unit termination: EC-I-O-PERMANENT-ERROR (fatal): I-O status 35 on RGTURNF::F1   ← F1 checked
```

`turnfn2.cob`: `>>TURN EC-I-O CHECKING ON` then `>>TURN EC-I-O F1 CHECKING OFF`, with a USE AFTER STANDARD ERROR
declarative on F2:

```
F1 S=35 EC=[                               ]        ← F1 turned off by file-name
DECL F2 EC=[EC-I-O-PERMANENT-ERROR         ]         ← F2 still checked, declarative runs
abnormal run-unit termination: EC-I-O-PERMANENT-ERROR (fatal): … RGTURNG::F2   ← §14.6.13.1.3 5)
```

## What the golden round must do

1. Turn the two probes into goldens (2002, where >>TURN is introduced). Termination is the expected output, so use a
   declarative with RESUME or a nonfatal EC-I-O condition such as EC-I-O-AT-END to keep the goldens running to
   completion. Add the level-2 × file-name-1 case of GR3's second sentence as well.
2. GR5 first sentence: a `>>TURN` written BETWEEN phrases of one statement (for example after `READ F1` and before
   its `AT END`, or inside a SEARCH's WHEN) must not change checking for that statement's own phrases. It must
   change checking for the statements inside the phrase and after it. Nothing has ever measured this. The
   EcBinder `TurnState` capture point and the `__IoCheckEc` masks are the code sites. If it misbehaves, this note
   becomes a defect (re-grade it, do not open a second note).
3. Record GR-7.3.25.4-5/-6/-8 with those goldens as witnesses.
