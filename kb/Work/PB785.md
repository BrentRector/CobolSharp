---
title: "PB785 — OWNER DECISION (2026-09-06, never re-ask): `docs/rearchitecture/evidence/*.json` is FROZEN EVIDENCE — an agent's recorded output, never edited to stay current — and a claim the tree has since REFUTED is not repaired but MARKED: a `superseded_by` object (`note` · `devlog` · `date` · `why`) as the FIRST key of the object holding it, under a top-of-file `_frozen` banner naming the convention, with `scripts/spec/audit_evidence_supersession.py --check` as the drift gate beside the two citation audits and a `docs/DOC_INDEX.md` row for the directory"
id: PB785
kind: decision
status: landed
severity: MINOR
area: docs
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: ["5.2.6.4", "14.9.51.2"]
inventory_rows: []
tags: [cobolsharp, work, decision, docs, process]
---

# PB785 — LANDED — a research artefact that tells the next scout not to look

Filed 2026-09-06 by the registrar from the PB712 finisher's report (`PB712-report.md` §"New defects" (b)), which
found the sentence, deliberately did **not** edit it, and escalated the question of what the file *is*.
**Re-measured here against `origin/main` at `07cbb2ad`**; two of the dispatch's premises did not hold and are
corrected below.

## The two lines, verbatim, with their numbers

`docs/rearchitecture/evidence/PHASE-13-grammar-batch-research.json`, line **81** (the `currentState.summary`
string; the relevant tail):

> SEPARATE FINDING (asked for explicitly): C6-A (WRITE BEFORE AND AFTER ADVANCING) IS FULLY LANDED end-to-end —
> grammar `writeBeforeAfter : writeAdvancePhrase writeAdvancePhrase?`, the ParseArm gate, the construct row +
> generated registry, the RuntimeApi/CobolFile/FileRegistry shim, and the conformance golden all exist. **C6-A
> must NOT be re-implemented**; only C6-B remains.

Line **106** (an `evidence` array entry):

> `src/Cobol.Net.Frontend/Grammar/Core/CobolIO.g4:357-366 — writeBeforeAfter/writeAdvancePhrase (C6-A grammar, LANDED)`

## ⛔ Premise corrections — what is and is not false TODAY

**(1) The grammar text quoted on line 81 is still ACCURATE on main; the JUDGMENT around it is what PB712
refuted.** `src/Cobol.Net.Frontend/Grammar/Core/CobolIO.g4:547-549` on `07cbb2ad`:

```
writeBeforeAfter
    : writeAdvancePhrase writeAdvancePhrase?
    ;
```

PB712's fix — which deletes `writeAdvancePhrase` and spells the rule
`(BEFORE AFTER? | AFTER BEFORE?) ADVANCING? ( PAGE | operand (LINE|LINES)? )` — is on the **train-20** branch and
is **not landed**. So the falsity on main is not yet in the quoted rule; it is in *"IS FULLY LANDED"* and
*"must NOT be re-implemented"*, said about a rule that rejects the one legal spelling of the 2023 combination
(`WRITE R BEFORE AFTER ADVANCING 4 LINES`) and accepts a two-operand form the standard never prints. The rule the
judgment contradicts is checked: `cite.py --check 5.2.6.4 "one or more of the alternatives contained within the
choice indicators shall be specified"` → **OK §5.2.6.4** (*"Choice indicators are a pair of bars, |, that enclose
a portion of a general format. When enclosed by braces, one or more of the alternatives contained within the
choice indicators shall be specified…"*), over `cite.py --check 14.9.51.2 "General format"` → **OK §14.9.51.2**
(WRITE's General formats). ⚠ When train 20 lands, line 81 becomes false in the flat sense too — the rule it
quotes will not exist.

**(2) The scout doc does NOT yet carry the ⛔ refutation banner.** The dispatch says it does.
`grep -n "⛔\|refut" docs/rearchitecture/PHASE-13-wave-c-scout.md` on main returns **nothing**; the doc is 728
lines and its entry A (`:408`, `**Spec sections:** A (WRITE BEFORE+AFTER)`) stands unmarked. That banner is also
train-20 cargo. **So today BOTH artefacts assert the refuted design**, and this note is the only place the
refutation is visible on main.

**(3) The line reference on 106 is independently stale.** `CobolIO.g4:357-366` on main is `closeStatement` /
`closeFilePhrase` / `closeOption`. `writeBeforeAfter` is at `:547`. A reader who follows the evidence entry lands
on CLOSE — which is the ordinary decay of a line-numbered reference in a file nobody re-points, and is worth
recording because it shows the artefact is *already* being read as a stale snapshot by the tree itself.

## The bare question for the owner

> **Is `docs/rearchitecture/evidence/*.json` frozen evidence — a record of what an agent said, needing a
> machine-readable "superseded by" marker — or a document that CLAUDE.md rule 6 makes current?**

## The default, if the owner does not answer — and the convention it follows

Per `feedback_owner_decision_protocol` (follow the surveyed convention; ask only on significant considerations),
the default is **FROZEN, plus a marker**, because three independent parts of this repo already treat the
directory that way:

1. ⭐ **`scripts/spec/citation_corpus.py:19-25` names this exact directory in a `FROZEN` tuple** — a MECHANICAL
   commitment, not prose: *"Directories that hold FROZEN evidence — an agent's raw output, recorded as it was
   said. Repairing a citation inside one would rewrite the record of what was found, which is the opposite of
   what these files are for. (The findings they contain are actioned through `kb/Work/`, never by editing the
   transcript.)"* Both citation audits skip `docs/rearchitecture/evidence` because of it. Under the alternative
   answer (rule 6 makes it current) that exclusion would itself be a defect.
2. **`docs/DOC_INDEX.md:74`** already classifies the one evidence file it indexes exactly so — and shows the
   house form of the marker, which is a POINTER: *"⛔ Frozen evidence: never edited to stay current. The CURRENT
   process is `.claude/skills/workstream/SKILL.md`; the six questions still owed the owner are `kb/Work/PB468`."*
3. **The banner precedent** the dispatch names: `docs/ISO2023_CONFORMANCE_PLAN.md:3` and
   `docs/PHASE4_RECONCILIATION.md:3` both open with *"⚠ **FROZEN HISTORICAL LEDGER (2026-07-19):** … Live state =
   the plan §0 + …"* — date, freeze, and the pointer to what IS live, in that order.

⛔ **What the default does NOT cover, and why this is a question at all:** the house marker is a Markdown banner,
and a `.json` file has nowhere to put one. So adopting the default still requires choosing a machine-readable
form — a top-level `"_frozen"` / `"_superseded_by"` object carrying the date, the reason and the pointers (for
this file: `kb/Work/PB712`, and `docs/rearchitecture/PHASE-13-wave-c-scout.md`'s refutation once train 20 lands),
which the schema-free evidence JSONs can all take. That is a shape decision the owner may want, which is why the
question is asked rather than silently applied.

## ⛔ What a future scout reads first — measured, not assumed

This is the part that decides whether a marker in the JSON is enough:

- `docs/DOC_INDEX.md` indexes **neither** file. `grep -n "grammar-batch-research\|wave-c-scout" docs/DOC_INDEX.md`
  → no hits; the only `evidence/` entry in the whole index is `PROCESS-REVIEW-2026-09-04.md` (`:74`).
- So nothing routes a reader from one to the other. A scout arrives by grep or by listing
  `docs/rearchitecture/`, and a keyword-dense JSON full of `LANDED` verdicts is at least as likely a hit as a
  728-line prose scout doc — **which is exactly how PB712 was built**: the refuted design was read out of the
  research artefact and implemented as fact.

Therefore, whichever way the owner answers, the marker has to live **inside the JSON**, and
`docs/rearchitecture/evidence/` needs a row of its own in `docs/DOC_INDEX.md` saying what the directory is. A
banner added only to the prose scout doc does not reach the reader who never opens it.

## Fix shape once answered

- **If FROZEN (the default):** add the machine-readable marker object to
  `PHASE-13-grammar-batch-research.json` naming PB712 and the scout doc; add the `docs/rearchitecture/evidence/`
  row to `docs/DOC_INDEX.md` stating the freeze and where live state lives; leave every line of the body
  untouched — the record is the point.
- **If CURRENT under rule 6:** lines 81 and 106 are edited to today's truth in the same change set as train 20,
  `citation_corpus.py`'s `FROZEN` tuple loses this directory, and both citation audits start scanning ~12 files
  of recorded agent output — expect findings, and expect them to be corrections of what agents *said*.
- Either way, ⛔ **do not do half of it**: an artefact that is edited sometimes is the worst of both, because a
  reader can no longer tell whether an un-updated sentence is a record or a claim.

## ⚖ Owner decision 2026-09-06 — FROZEN, and the marker is machine-readable

> **`docs/rearchitecture/evidence/*.json` is FROZEN evidence, and a refuted claim in it carries a
> machine-readable superseded-by marker.**

The default was taken, and with it the shape question the default could not answer. The record is never edited
to stay current — so a claim the tree has since refuted is not repaired, it is **marked**, and the marker is
data rather than prose because the artefact is data.

## ✅ Landed 2026-09-06 — the marker, the banner, and the gate that keeps them true

⚠ **Both conditions the premise corrections above predicted have since occurred, and were re-measured here.**
Train 20 landed (DEVLOG 1564): `writeAdvancePhrase` is gone from the grammar, so line 81 is now false in the
flat sense too — the rule it quotes does not exist — and the ⛔ refutation banner the corrections said was
missing does sit on entry A of `docs/rearchitecture/PHASE-13-wave-c-scout.md:432` today. Read §"Premise
corrections" as the measurement taken when this note was filed on `07cbb2ad`, not as current state.

**The marker.** `superseded_by`, an object carrying `note` · `devlog` · `date` · `why`, placed as
the **FIRST key of the object holding the refuted claim** — JSON preserves key order in the file, so a
reader going top-down meets the refutation before the claim. Both of this note's measured lines (81, the
`currentState.summary` judgment; 106, the `evidence` entry naming `CobolIO.g4:357-366`) live in the one object
`specs[0].spec.currentState`, so **one marker** covers both, and its `why` names them individually rather than
condemning the object wholesale — the C6-B findings around them (the unlanded `SUPPRESS WHEN` phrase) are
untouched and say so.

**The banner.** Every `*.json` in the directory (7 files) gained a top-level `_frozen` string stating what the
directory is, that findings are actioned through `kb/Work/`, that `citation_corpus.py` names it in `FROZEN`, and
— naming the key, so the convention is discoverable from any file that has it — that a refuted claim
carries `superseded_by`. `ledger-trend.json` carries a variant worded for what it actually is (an append-only
series of measurements appended by `gen_ledger.py`; `write_trend()` reads the existing document and replaces
only `points`, so the banner survives regeneration — verified by reading the function, not assumed).

⛔ **Every edit was TEXTUAL and byte-preserving** — a `json.load`/`json.dump` round trip would have
reformatted every line of a frozen record and made the diff the whole file. **13 inserted lines across 7 files,
zero lines changed**, CRLF intact, re-parsed as JSON afterwards.

**The sweep, measured rather than assumed.** The two lines this note names were not taken on trust; the whole
directory was swept for the same shape:

- Grammar rule names that no longer exist: `writeAdvancePhrase` and **nothing else** — two occurrences,
  both in `specs[0].spec.currentState`, both now under the marker.
- `path:line` citations pointing at a file that no longer exists: **zero of 225**.
- Bare paths (no line number) that do not exist: **122 — and every one is a PROPOSAL**
  (`tests/conformance/2023/alt_key_suppress.cob`, a golden the research asked for and nobody wrote). That
  measurement is why the gate checks only LINE-ANCHORED citations: a line number can only have been written
  about a file that existed. Checking bare paths would have been 122 false reds on day one.
- Line-coordinate drift — a rule that still exists but has MOVED: **10**, e.g. `alternateKeyClause` cited
  at `CobolIO.g4:146-149` and living at `:205-209` today, and `writeBeforeAfter` cited at `:357-366` (which is
  `closeStatement`) and living at `:574`. These are **reported and never gated**: nothing about them is refuted,
  only their coordinates decayed, and gating would force ten markers that say something false or force
  re-pointing the record — the one thing the freeze forbids. The `_frozen` banner states the true and
  permanent fact instead: *resolve the NAME, never the line number*.

**The drift check** — `scripts/spec/audit_evidence_supersession.py`, wired into `scripts/build-local.sh`,
`scripts/build-local.ps1` and `scripts/battery.sh` PHASE -1, beside the two citation audits and for the same
reason: they ask whether a citation of the STANDARD is right, this one asks whether a frozen file's citation of
the TREE is still true. It needs no submodule. Four gated checks (`banner`, `path`, `rule`, `marker`), the
population asserted (zero files or zero citations is a refusal, exit 2), and the guarded directory read back
against `citation_corpus.FROZEN` rather than copied — if the citation audits ever stop skipping the
directory, this audit refuses instead of going on guarding a record they have started editing.

⚠ **Two false-accusation shapes were measured and designed out**, both found by running the first draft:

1. *Every camelCase word in a string that mentions a `.g4` file* accused four correct sites — `popMode` (an
   ANTLR lexer command), `nameSlot`/`subscriptTrigger` (JSON field names quoted in prose) and, the instructive
   one, `editingClause` in *"dataDescriptionClause alternatives; no editingClause"* — a NEGATIVE claim that
   is still true. A rule name is now read only where the text puts it in a declaration position: adjacent to a
   `<file>.g4:<line>` citation, or in a written-out production.
2. *Every written-out production* accused five more — `mcsFacilityStatement`, `commitStatement`,
   `rollbackStatement`, `authorContent`, plus `introducedIn: 2023`, a JSON field. Two fixes: a production needs
   a SPACE before its colon (which a quoted JSON key never has), and only the BODY of a production whose HEAD
   is a real rule is checked — a head that exists makes the line a statement about how that rule reads
   today; a head that does not is a sketch the grammar has no opinion about. This arm is what reaches line 81,
   which cites no file at all and would otherwise be invisible to the gate that exists for it.

**Proven to fail, by construction.** `--self-test` runs every gated check twice — once on a document built
to break it, once on the same document with the marker — plus the drift arm, and passes. And the live gate
was made to fail once for real: with the marker removed from the tree,

```
7 frozen evidence JSON(s) · 225 line-anchored citation(s) · 46 grammar-rule claim(s) · 0 superseded_by marker(s)
⛔ 2 UNMARKED CLAIM(S) THE TREE CONTRADICTS — these are defects

  docs/rearchitecture/evidence/PHASE-13-grammar-batch-research.json:82 [rule] specs[0].spec.currentState.summary
     writes the production `writeBeforeAfter : …` with `writeAdvancePhrase` in its body — `writeBeforeAfter` is a real rule, so this is a claim about how it reads today, and `writeAdvancePhrase` is not in the grammar

  docs/rearchitecture/evidence/PHASE-13-grammar-batch-research.json:107 [rule] specs[0].spec.currentState.evidence[23]
     cites grammar rule `writeAdvancePhrase` at src/Cobol.Net.Frontend/Grammar/Core/CobolIO.g4:357 — no rule of that name is in the grammar today
```

exit **1** — and with the marker restored, the verdict line on the clean tree:

```
7 frozen evidence JSON(s) · 226 line-anchored citation(s) · 48 grammar-rule claim(s) · 1 superseded_by marker(s)
⛔ 0 UNMARKED CLAIM(S) THE TREE CONTRADICTS — these are defects

— 10 line-coordinate drift(s): a claimed rule that still exists but has MOVED. Reported, never gating — the frozen record cannot be re-pointed, and the _frozen banner says so.
```

exit **0**. Both citation audits still zero (`0 finding(s)` over 3,701 files; `0 MISFILED` over 373 citations);
`work.py check` → 834 items, all well-formed.

**The docs.** `docs/DOC_INDEX.md` gains a row for `docs/rearchitecture/evidence/` itself — the gap this note
measured, since the index routed a reader to neither of the two artefacts — stating the freeze, the marker
convention and the coordinate caveat, plus a GATE row for the audit; the `citation_corpus.py` row now names the
audit that holds the other end of its bargain.

⚠ **What did NOT change, deliberately.** Not one line of the body of any evidence file was edited — the
refuted sentence on line 81 still reads exactly as the agent said it, which is the entire point of the freeze.
`docs/rearchitecture/spec-reconciliation/` (the other `FROZEN` directory) is out of scope and stays so: 456 files
there carry FOUR line-anchored citations into the tree between them, so the drift this audit measures is not the
drift that directory can suffer, and banner-stamping 456 frozen records to say so would be the larger edit.
There is no CI citation step to add this to — the two citation audits are deliberately absent from CI
because the checkout is `submodules: false` (`build-local.sh:26-28`), and this audit inherits their placement
rather than opening a new CI job of its own.

## Links

- [[PB712]] — the defect whose implementation was misdirected by line 81, and whose fix falsifies the quoted
  grammar rule when train 20 lands. ⭐ Whoever lands train 20 should re-read this note: the banner it carries for
  the scout doc is half the fix, and the JSON is the other half.
- [[PB468]] — the other open owner-question note; scoped to the 2026-09-04 process re-evaluation, so this
  question does not belong inside it.
- [[PB762]] — the sibling shape one register over: forensic prose no gate reads, going stale under a rename.
