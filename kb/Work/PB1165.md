---
title: "PB1165 — an ACTIVE-CLASS returning item is compared as an ACTIVE-CLASS sender of the DECLARING class: §14.8.3.3 rule 2's re-description of the sender from the invocation form (object-class-name → ONLY, a typed reference → its description) is not implemented, so `INVOKE D 'MK' RETURNING D2` with D, D2 typed RID2 (a subclass of the declaring RIB2) is refused COBOLNET0828 SR12 b)2."
id: PB1165
kind: defect
status: open
severity: MINOR
area: binder/oo
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1164", "PB1166"]
spec_refs: ["14.8.3.3", "14.9.39.3"]
inventory_rows: ["GR-14.8.3.3-2", "GR-9.3.6-L3.6"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14c** from lane-3 adjudication batch **d1-14c** (adjudicated on the pinned tree `68bbdb00f`; trains 58/59 checked — nothing that landed since touches this mechanism unless the body says otherwise).

# PB1165 — OPEN

## The rule (every citation re-run by the registrar through `cite.py --check`)

> 1) If the returning item in the activated element is not described with an ACTIVE-CLASS phrase, the conformance rules are the same as if a SET statement were performed … (rule 2 gives the ACTIVE-CLASS case, re-describing the sender from the invocation form)

`python scripts/spec/cite.py --check 14.8.3.3 "…"` → `OK  §14.8.3.3 1)  (Elementary items)`

## Finding — `returning-items` (rejects-legal-source; rows `GR-14.8.3.3-2`)

**Mechanism.** An ACTIVE-CLASS returning item is compared as an ACTIVE-CLASS sender of the declaring class; §14.8.3.3 rule 2's re-description of the sender from the invocation form (class-name -> ONLY, other reference -> its description) is not implemented

**Repro.** RIB2 instance method MK 'LINKAGE SECTION. 01 R USAGE OBJECT REFERENCE ACTIVE-CLASS. PROCEDURE DIVISION RETURNING R.' 'SET R TO SELF'; RID2 INHERITS FROM RIB2; main '01 D USAGE OBJECT REFERENCE RID2. 01 D2 USAGE OBJECT REFERENCE RID2.' 'INVOKE RID2 "NEW" RETURNING D' 'INVOKE D "MK" RETURNING D2' 'INVOKE D2 "WHO"' (p2.cob). Also RIB3 FACTORY method MAKE returning ACTIVE-CLASS, 'INVOKE RID3 "MAKE" RETURNING DO' with DO 'OBJECT REFERENCE RID3 ONLY' (p3.cob).

**Expected from the spec.** Rule 2 b) 4.: invoked with the RID2-typed reference D, the sender is described RID2, and SET D2 TO it is §14.9.39.3 SR12 a)2. -> compiles, prints DERIVED. Rule 2 b) 1.: invoked with object-class-name RID3, the sender is 'RID3 ONLY' (FACTORY absent as in the returning item) -> SR12 a)1. admits the RID3 ONLY receiver -> compiles, prints DERIVED.

**Observed / reasoning.** p2: COBOLNET0828 'the class containing the ACTIVE-CLASS sender (RIB2) is not RID2 or one of its subclasses (ISO §14.9.39.3 SR12 b)2.)'. p3: COBOLNET0828 'the receiver is described with the ONLY phrase, so an ACTIVE-CLASS sender is not permitted (SR12 b)1.)'. OoBindResolvedInvoke passes m.Binding.Returning.Pic unchanged to ObjectRefAssignmentMismatch; the invocation site (class-name / SELF / SUPER / typed reference) is known there but never used to derive the sending description. The C# projection must also cast the declaring-class-typed result to the invoking description.

**Code site.** src/Cobol.Net.Compiler/Binding/Procedure/Verbs/OoBinder.cs#OoBindResolvedInvoke (§14.8.3.3 rule-1 block near :533)

## What the fixer owes

In OoBinder.OoBindResolvedInvoke (the §14.8.3.3 rule-1 block near :533) derive the SENDING description from the invocation site (class-name / SELF / SUPER / typed reference) per rule 2 b) 1.-4. and the FACTORY sentence before calling ObjectRefAssignmentMismatch, and have the C# projection cast the declaring-class-typed result to the invoking description. Goldens p2.cob (DERIVED) and p3.cob (FACTORY MAKE into `RID3 ONLY`).

## Extended 2026-09-24 by REGISTRAR 15d (batch 15d, pinned `071ff04ea`)

Rows added to `inventory_rows`: `GR-9.3.6-L3.6`.

Seen from §9.3.6 match rule 6 (batch 15d objects-p2): an inherited factory/instance method returning ACTIVE-CLASS invoked on the SUBCLASS cannot deliver into a subclass-typed receiver — the same missing §14.8.3.3 rule-2 substitution.

### Finding — `objects-p2` (rejects-legal-source; rows `GR-9.3.6-L3.6`)

**Mechanism.** An ACTIVE-CLASS RETURNING item is typed as its DECLARING class at the INVOKE site instead of by §14.8.3.3 rule 2's invocation-dependent substitution, so an inherited method returning ACTIVE-CLASS cannot deliver into a receiver typed as the subclass it was invoked on

**Repro.** CLASS-ID BA; factory method MK `01 LA USAGE OBJECT REFERENCE ACTIVE-CLASS. PROCEDURE DIVISION RETURNING LA. INVOKE SELF "NEW" RETURNING LA.`; instance method MA `RETURNING LA2 (ACTIVE-CLASS)` with `SET LA2 TO SELF`. CLASS-ID SB INHERITS FROM BA. Caller `01 OS USAGE OBJECT REFERENCE SB`: (S5) INVOKE SB "MK" RETURNING OS; (S7) INVOKE SB "NEW" RETURNING OS, INVOKE OS "MA" RETURNING OS (probe/objects-p2/gen2.py).

**Expected from the spec.** §14.8.3.3 2): with an ACTIVE-CLASS returning item the SET is performed in the activating element with the sender described by the INVOCATION - b) 1. invoked with object-class-name SB => `OBJECT REFERENCE SB ONLY`; b) 4. invoked through OS => OS's own description (SB). Both conform to the SB receiver (§14.9.39.3 SR12 a)), so §9.3.6 match rule 6 is satisfied and both programs compile and run.

**Observed / reasoning.** Both refused COBOLNET0828: 'the class containing the ACTIVE-CLASS sender (BAS5) is not SBS5 or one of its subclasses (ISO §14.9.39.3 SR12 b)2.) (ISO §14.8.3.3 returning-item conformance)'. OoBindResolvedInvoke hands the method's declared returning PicInfo straight to ObjectRefAssignmentMismatch, which treats ACTIVE-CLASS as 'the containing class' (the SET-statement reading for an item written in the same class), never applying rule 2's substitution. Invoked on its own class (S8 INVOKE BA "MK" RETURNING OB) it works, which is why no existing golden (pb496_active_class_positions) sees it. No kb/Work note owns it.

**Code site.** src/Cobol.Net.Compiler/Binding/Procedure/Verbs/OoBinder.cs#OoBindResolvedInvoke (line ~537, the RETURNING conformance call); src/Cobol.Net.Compiler/Oo/OoConformance.cs#ObjectRefAssignmentMismatch
