---
title: "PB1278 — two REQUIRED Annex A.1 determinations of the RECORD clause are not written down in docs/CONFORMANCE.md: item 148 (§13.18.43.4 GR17 — Format 3 fixed or variable; the code answers 'variable') and item 146 (GR2 — the calculations relating record-area size to physical record size)"
id: PB1278
kind: defect
status: open
severity: MINOR
area: docs/conformance
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
cluster: ["PB1276"]
spec_refs: ["13.18.43.4"]
inventory_rows: ["GR-13.18.43.4-17", "GR-13.18.43.4-2"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14e** from adjudication batch d1-14e (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probe sources under the batch's `out13/probe/<subject>/`). Checked against main 071ff04ea: trains 58/59 did not touch this mechanism.

# PB1278 — OPEN

## The rules (every quotation run through `cite.py --check` by the registrar)

- `python scripts/spec/cite.py --check 13.18.43.4 "It is implementor defined whether format 3 of the RECORD clause produces fixed-length records or variable-length records"` → `OK  §13.18.43.4 17)  (General rules)`
- ⚠ NOT validated verbatim (the adjudicator's paraphrase or a cross-clause quote — re-quote before it reaches code, a golden or the DEVLOG): "This item is required. This item shall be documented in the implementor"

## Measured

### Finding record-clause — 13.18.43.4 GR17, GR2; Annex A.1 items 146, 148 (silent; rows GR-13.18.43.4-17, GR-13.18.43.4-2)

**Mechanism.** Two required Annex A.1 determinations of the RECORD clause are not written down. Item 148 (GR17: whether Format 3 produces fixed or variable records) has a real answer in the code (variable: VaryingRecordInfo with VaryingClause:false) but no row in docs/CONFORMANCE.md. Item 146 (GR2: how to derive a record's size on the storage medium) is covered only in passing by DOC-A.1-151's 4-byte length prefix, with no stated calculation for fixed and variable files or for the CODE-SET and FORMAT factors GR2 names.

**Repro.** grep docs/CONFORMANCE.md for 'DOC-A.1-146', 'DOC-A.1-148', 'fixed-or-variable', 'Format 3': no determination. Behaviour: out13/probe/record-clause/p3.cob, FD F3 RECORD CONTAINS 5 TO 20 CHARACTERS with 5- and 10-byte records, writes two length-prefixed frames of 5 and 10 bytes (23-byte file), so the variable choice is in force but undocumented.

**Expected, from the standard.** Annex A.1 items 146 and 148: 'This item is required. This item shall be documented in the implementor's user documentation.' (cite.py --check A.1 OK for both.)

**Observed.** Item 147 (the implicit clause) is documented by D-FRA (iv). Items 146 and 148 are not documented; 148's answer can only be recovered by reading DataBinder.BindRecordClause.

**Code site.** docs/CONFORMANCE.md (missing DOC-A.1-146 / DOC-A.1-148 rows); behaviour at src/Cobol.Net.Compiler/Binding/DataBinder.cs#BindRecordClause

## What the fixer owes

Re-probe on your own build first (MANDATORY-PRACTICES I3). Fix the mechanism at the code sites above — one rule, one place (the `cluster:` notes share these files; take them as one fill unit). One positive golden at the introducing edition and one negative below it / per violated rule, each derived from the quoted rule. Sweep the sibling arms named in the findings (CLAUDE.md rule 4). On landing flip `status`, write `closes_rows` from `inventory_rows`, and re-verdict each row with `record_verdicts.py`.
