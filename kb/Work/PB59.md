---
title: "PB59 — LANDED 2026-08-09 — CONVERT/BASECONVERT/DISPLAY-OF/NATIONAL-OF: the Repertoire/hex subsystem, rebuilt family by family to 28 of 28 rows"
id: PB59
kind: defect
status: landed
severity: MAJOR
area: runtime/intrinsics
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["15.19.3", "15.19.4", "15.12.3", "15.12.4", "15.15.3", "15.15.4", "15.18.3", "15.18.4", "15.26.3", "15.26.4", "15.66.3", "15.66.4", "A.1"]
tags: [cobolsharp, work, defect]
---

# PB59 — LANDED — CONVERT/BASECONVERT/DISPLAY-OF/NATIONAL-OF: the Repertoire/hex subsystem, rebuilt family by family

> ✅ **CLOSED 2026-08-09 — ALL 28 ROWS CONFORMS.** Seven families landed across one day (DEVLOG
> 1263–1283): the item-33 total identity + the UTF-8 device · the value-path trims + item 93 · CHAR on
> the one collation reader · the numeric-literal admission · CONVERT's positional parse + the r4–r7
> usage screens (5a) + the ANY raw-storage channel + the r2/r4 padding (5b) · the width totalization +
> the X'' lexer twin · the CONCAT alphabetic rider (7a) + the OperandCategory totalization (7b) · the
> BASECONVERT screen set (COBOLNET1642, the note's last member). Bonus closures outside the 28:
> AR-15.26.3-1, SR-14.9.25.3-10 (with [[PB72]]). Successor notes: [[PB68]] [[PB69]] [[PB70]] [[PB71]]
> [[PB72]] (landed) [[PB73]].

> ⚙ **flags (historical):** wrong-answer, silent · **28 inventory rows** (triage 2026-08-09, probe+refute
> confirmed; the per-row mechanisms live in the triage record and each row's inventory notes)

> **One subsystem, three mechanism families** (`CobolIntrinsics.Text` + `BindConvert` +
> `IntrinsicBinder.CheckRepertoireArgs`):
> 1. ✅ **LANDED 2026-08-09 — the item-33 TOTAL IDENTITY (7 rows → CONFORMS: RV-15.26.4-1/-3,
>    RV-15.66.4-1/-2/-3, RV-15.19.4-1/-3).** `Repertoire` is the identity both directions; the 0xFF cut,
>    the dead-arm `sub`, and the `Sub()` raise are gone. The determination is DECLARED: the Annex A.1
>    item-33 row in `docs/CONFORMANCE.md` §7 (audit 20/199, `--check` clean), item 188 reconciled, the
>    D-N4 twin design-doc copies rewritten together. The standard display device now writes UTF-8
>    (item 59; `ProgramTable.RunMain`) and the test harness decodes UTF-8 (`ProcessObservation`), so wide
>    characters survive end-to-end — golden `pb59_repertoire_identity` (ORD-353 witnesses both sides,
>    round trips, argument-2 inert), `national_intrinsics` re-derived, the EC test retargeted POSITIVELY
>    at the surviving ByteSub raise. ⚠ Residue stays open under family 2/3 rows: the 8-bit BYTE
>    serialization's '?'+EC on the HEX legs (RV-15.19.4-2 — §15.19.4 r2 authorizes no substitution).
> 2. **The value paths trim/normalize before parsing** — ✅ **the TRIM half LANDED 2026-08-09 (4 rows →
>    CONFORMS: AR-15.12.3-2/-3, AR-15.19.3-1/-4):** `TrimEnd(' ')` only (a leading space is content), the
>    digitless guard counts DIGITS not the accumulator, the runtime §15.19.3 r1 zero-length screen sits at
>    Convert's top beside its bind-time literal twin, the §15.4 8,191-position returned-value maximum is
>    DECLARED (Annex A.1 item 93, audit 21/199) and enforced in BaseConvert, and lowercase a–f are settled
>    by derivation (r2's "basic-letters A to F" + §8.1.3.1 Table 1 = capitals only; CONVERT's r4
>    "hexadecimal digits" deliberately keeps both cases). STILL OPEN in this family: no bit-level padding
>    step before `ToHex` (r2/r4 — family 5's CONVERT rework). ✅ **The CHAR half LANDED 2026-08-09
>    (3 rows → CONFORMS: RV-15.15.4-1/-2, AR-15.15.3-2):** `AlphanumericCollation` mirrors the national
>    twin (RepByPos + NextFree; Weight/CharAt exact inverses), `AlphabetBind` keeps the source order it
>    used to discard with the ALSO advance guarded, `__COLLATE` emits the object, and comparison paths
>    deliberately keep the raw table (the `: c` tail is ORDER-EQUIVALENT — proof note at
>    `CobolString.Weight`). Golden `pb59_char_representative`; PB3.md's scope sentence corrected.
> 3. **CONVERT and CONCAT have no bind-time screens at all**, and `BindConvert` harvests keyword-shaped
>    arguments position-blind. ✅ **The numeric-literal ADMISSION half LANDED 2026-08-09 (RV-15.12.4-1,
>    RV-15.18.4-1 → CONFORMS; AR-15.12.3-1 leg (a)):** the renderer's string channel gained the
>    per-function admitting entry (`StrNum`/the admitting `StrArgList` — §15.12.3 r1's unsigned-integer
>    literal, §15.18.3 r1's class-numeric list; the default stays LOUD so NUMVAL's compile-time-rejection
>    rows are not silently widened), the PB49 drift pin moved WITH the fix (the conditional arm + a
>    caller-set guard proven in the failing direction), and the folded-nested-numeric producer shape is
>    in the golden `pb59_numeric_literal_argument`. ✅ **The CONVERT half of the screens LANDED 2026-08-09
>    as family 5a (below)**; AR-15.12.3-1 legs (b)–(f) — the BASECONVERT screen set — remain open.
> 5a. ✅ **CONVERT's positional parse + the r4/r5/r6/r7 bind screens LANDED 2026-08-09 (3 rows →
>    CONFORMS: FMT-15.19.2, AR-15.19.3-5, AR-15.19.3-6; AR-15.19.3-7 leg (a) enforced, row open on (b)):**
>    `BindConvert` walks §15.19.2 BY POSITION (slot 0 = argument-1 ALWAYS an operand, so a data item named
>    NAT/ANUM/HEX/BYTE — §8.10 context-sensitive — compiles; a misplaced operand rejects 1514), MinArgs
>    2→3 on the catalog row, and the screens key on REPRESENTATION via the NEW shared
>    `IntrinsicArgumentRules.StaticUsageOf` (r4 display|national; r5 display — its NOTE cuts by what the
>    storage HOLDS, so numeric/edited DISPLAY items are ADMITTED, the frozen apply plan's class screen was
>    corrected at apply; r6 national; r7 the usage exclusion — the pre-fix probe hex-dumped a
>    ManagedPointer's .NET TYPE NAME). BASECONVERT's §15.12.3 r1 display-or-national rule reuses the same
>    axis when its screen set lands. Landing the r6 screen surfaced `argument_rule_equivalences_batch1`'s
>    own NAT legs riding a PIC X item — the golden corrected to PIC NN, output byte-identical.
> 5b. ✅ **The ANY raw-storage channel + the §15.19.4 r2/r4 padding LANDED 2026-08-09 (4 rows →
>    CONFORMS: AR-15.19.3-7, RV-15.19.4-2, RV-15.19.4-4, RV-15.19.4-5 — family 5 is COMPLETE, 26 of the
>    28 rows CONFORMS):** ANY reads RAW STORAGE through the ONE new storage channel
>    (`OperandText.AsStorageImage`, char==byte — COMP → radix-2 bytes (258→0102, was 30323538-shaped
>    display digits), zoned → stored characters, BIT → `CobolBits.Pack` (whose trailing zero-fill IS
>    r2's pad), national → the ONE `CobolBits.NatBytes` UTF-16BE serializer shared with the runtime NAT
>    arm, group → AsImage(); float/COMP-5 stage LOUD via IsImageCapable); the bind-time ANY→NAT remap +
>    `IsNationalOperand` are DELETED (one mechanism); r4's 16-bit pad is the odd-byte zero append —
>    `ANUM NAT HEX` of "A" = **4100** (the map's golden, per DEVLOG 1277's corrected derivation),
>    `B"101" ANY NAT HEX` = A000, NOTE 3 c's E0 and NOTE 3 b's X"41" both ruled defective NOTES (the
>    rules' arithmetic decides; RV-15.19.4-5's note carries the ruling). The runtime signature is
>    UNCHANGED — the packed byte image made the map's bit-count companion unnecessary (the analysis is
>    at the runtime's dstHex leg). RV-15.19.4-2(c) DECIDED: the partial 1-byte serialization substitutes
>    '?'+EC-DATA-CONVERSION as the r1/r3-analogous documented determination (CONFORMANCE.md item-33 row
>    tail).
> 7a. ✅ **The ALPHABETIC result rider LANDED 2026-08-09 (RV-15.18.4-3 → CONFORMS — 27 of the 28 rows;
>    landed WITH [[PB72]]'s Table-16 axis completion, one landing over the same edit sites):**
>    `BoundIntrinsicCall.ResultIsAlphabetic` — a RIDER, never a `PicCategory.Alphabetic` (the PIC A
>    fold's written-down decisions hold) — set at the ONE generic construction site when every CONCAT
>    argument is provably class alphabetic (a non-ref-mod PIC A field or a rider-carrying nested
>    CONCAT; §8.4.3.3.4 GR2/GR6 break the chain through a view), consumed at Table 16's sender
>    position. `MOVE FUNCTION CONCAT(picA picA) TO boolean` rejects 0819; plain/mixed/ref-mod forms
>    stay admitted (golden `pb72_table16_admitted_cells`, negative `pb59-concat-alphabetic-to-boolean`).
> 7b. ✅ **The `OperandCategory` TOTALIZATION LANDED 2026-08-09 (AR-15.26.3-1 → CONFORMS — a row
>    outside the 28 this note owns, closed as the totalization's own defect):** the ONE §8.5.2.1
>    classifier now answers groups (alphanumeric — §8.5.2.10 reserves national for the staged
>    GROUP-USAGE), ALL literals (their literal's category, GR9) and ref-mod views (the §8.4.3.3.4 GR6
>    rewrites through `RefModPlace.CategoryOf` — the reader's own doc CITED GR6 while skipping GR6c),
>    and `IsStringOperand`'s field arm reads the same classifier so MAX/MIN's TYPE resolution and BODY
>    choice can never split again. Measured before/after: `MAX(N9(1:1) "A")` answered **0** (wrong —
>    the slice is class alphanumeric, "A" wins) and `MAX(G1 G2)` **crashed at run time** ("no numeric
>    render recipe" — the halves disagreed); `DISPLAY-OF(group)` and `DISPLAY-OF(ALL "A")` failed the
>    r1 screen open, now 1546 (negatives); `NATIONAL-OF(group)` and `DISPLAY-OF(SPACE)` stay admitted
>    (golden `pb59_operand_category_total` — MR/MG/NG/SP). Residue on the inventory note: the
>    national-edited leg rides the 0899 staging.
>    STILL OPEN on this note: **AR-15.12.3-1 legs (b)–(f)** — the BASECONVERT screen set, on the landed
>    StaticUsageOf axis — PB59's last member.
> 4. ✅ **The WIDTH/lexer family LANDED 2026-08-09 (3 rows → CONFORMS: AR-15.26.3-2, AR-15.66.3-2,
>    AR-15.66.3-3):** `KnownWidth` is TOTAL over the static shapes (group/ALL/figurative/literal-refmod
>    arms; null only for genuinely-runtime widths — dynamic-length, dynamic-capacity, ODO, ANY LENGTH,
>    computed), guarded by `KnownWidthTotalityDriftTests` (proven in the failing direction); the
>    `X''` apostrophe zero-length hex literal lexes via the shared `HEX_BODY` fragment (§8.3.3.2.2
>    Format 2 RENDERED — both delimiters, sequence optional; SUB_HEXLIT measured-unnecessary and the
>    decision recorded at the fragment); four negative fixtures pin the RULE ORDINAL in their .err
>    substrings. BONUS render-don't-reason catch: `HexVal`'s lowercase arm deleted (§8.3.3.1 defines
>    hexadecimal digits over the CAPITAL basic letters; the PB59-2 comment's term-based defense was
>    refuted by the printed page and corrected). The CLASS-half (`OperandCategory`) totalization is
>    deliberately deferred to family 7 — it moves MAX/MIN result-type resolution, measured with the
>    ALPHABETIC channel. Incidental finds registered: [[PB70]] (group-refmod MOVE loud stage),
>    [[PB71]] (`ALL N"…"` parse gap).
> Silent wrong answers on legal source (conversions produce wrong bytes/characters) — top harm band with
> PB56/PB60/PB65.

### The rows this note owns (re-verdict on landing)

> AR-15.12.3-1, AR-15.12.3-2, AR-15.12.3-3, AR-15.15.3-2, AR-15.19.3-1, AR-15.19.3-4
> AR-15.19.3-5, AR-15.19.3-6, AR-15.19.3-7, FMT-15.19.2, RV-15.12.4-1, RV-15.15.4-1
> RV-15.15.4-2, RV-15.18.4-1, RV-15.18.4-3, RV-15.19.4-1, RV-15.19.4-2, RV-15.19.4-3
> RV-15.19.4-4, RV-15.19.4-5, AR-15.26.3-2, AR-15.66.3-2, AR-15.66.3-3, RV-15.26.4-1
> RV-15.26.4-3, RV-15.66.4-1, RV-15.66.4-2, RV-15.66.4-3
