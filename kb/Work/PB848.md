---
title: "PB848 — the word TO is UNSTRESSED in all three §13.18.60.2 pointer phrases, so `USAGE POINTER type-name`, `USAGE FUNCTION-POINTER prototype` and `USAGE PROGRAM-POINTER prototype` are conforming spellings the grammar refuses - and the audit that should have found the family, audit_grammar_optional_words.py, scores 0 candidates over 320 general formats because its heuristic cannot see the KEYWORD-unstressed-word-operand shape"
id: PB848
kind: defect
status: open
severity: MAJOR
area: frontend/grammar
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.18.60.2", "5.2.3"]
inventory_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-13 by REGISTRAR #4 (the lead-filing pass over the train 27-33 implementer, finisher and lander reports). Every probe below was REBUILT from the source report and RE-RUN on this worktree's own `dotnet build CobolSharp.sln -c Debug`, first at main `b5ca78fb` and RE-RUN unchanged at `e13ef810` (train 32's head, after PB415/PB394/PB355/PB526/PB646 landed); every citation was re-derived with `python scripts/spec/cite.py --check` and the command line is printed beside the quotation.

# PB848 — OPEN — an optional word the grammar requires, and the audit that cannot see its shape

Source: the `PB452` implementer and finisher reports (item 2 / item (2)), measured there on the RENDERED folio
503 — the underline rule sits under `POINTER` / `FUNCTION-POINTER` / `PROGRAM-POINTER` only, so by §5.2.3 the
`TO` is an optional word (`feedback_underlining_not_bracketing`; the rendering is the authority, not the OCR).

## Repro — re-run on this tree

`scratchpad/reg4/probe/p30_pointer_unstressed_to.cob` (PROGRAM-ID `R4P30`), `--std 2023`:

```cobol
       01 REC-T IS TYPEDEF.
          05 A PIC X(2).
       01 P USAGE POINTER REC-T.
```
```
(7,27): error COBOLNET1941: 'REC-T' is not a clause of the data description entry for 'P' - the ISO §13.16.2
        Format 1 general format lists the clauses that may be specified, and it is a closed list
```

## The rule

```
$ python scripts/spec/cite.py --check 5.2.3 "optional word"
  OK  §5.2.3   (Optional words)
```

§5.2.3: an optional word may be written or omitted without changing the meaning. The measurement that makes
`TO` one is the UNDERLINING on the printed page of §13.18.60.2 — re-render it (`python
scripts/render-spec-page.py 503`) before writing the code comment, per CLAUDE.md rule 1.

## Code sites

`src/Cobol.Net.Frontend/Grammar/Core/CobolData.g4:595-611` — `dataPointerUsage`, `functionPointerUsage`,
`programPointerUsage`, each requiring the word.

## ⚠ The carrier of the defect is the AUDIT

`python scripts/spec/audit_grammar_optional_words.py` measures 320 general formats and reports **0 candidates**:
its heuristic cannot see the `KEYWORD <unstressed word> operand` shape, so fixing the three grammar rules
without fixing the audit leaves the whole family unmeasurable. kb/Work PB695 is the optional-word family's
landed repair of a DIFFERENT blindness (PB332's), which is why this is a new note and not an append: the shape
it misses is not the shape PB695 taught it to see.

## Sibling sweep the fixer owes

* Run the repaired audit and triage every candidate it then reports — that list is the actual scope.
* The same three phrases in the SCREEN and REPORT usage positions, and `USAGE OBJECT REFERENCE` (which has its
  own optional words).
* An edition-gate sweep: FUNCTION-POINTER is 2002+, so the relaxation must not leak below its edition.
