---
title: "PB141 — ✅ LANDED 2026-08-29 (NoRewind kind + '07'; REEL in FLAG-02; GR5 raise site; __IoCheck returns the RESUME pc; onExceptionHandled + the missing QueryFor arm; GR20/21 standard defect adjudicated). WAS: CLOSE semantics: WITH NO REWIND has no BoundCloseKind member so it reports '00' where Table 14 requires '07' (the compiler already WARNS about the behavior it does not produce); FlagConformancePass deliberately breaks the REEL/UNIT spelling equivalence; FOR REMOVAL is parsed and discarded; EC-REPORT-NOT-TERMINATED is never raised though ReportWriter._active holds the state; __IoCheck discards __RunUse's returned pc so RESUME AT procedure-name is swallowed on the non-EC USE arm of every I-O verb"
id: PB141
kind: defect
status: landed
severity: MAJOR
area: codegen
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB141

Rows: GR-14.9.6.4-3/-L2.2/-4/-5, SR-14.9.6.3-2, FMT-14.9.6.2, GR-14.9.10.4-20, findings [CLOSE statement]/[close-statement]/[delete-statement-p2].

(1) BindClose's ternary folds NO REWIND into Normal — add BoundCloseKind.NoRewind and the '07' path (FLAG-02 already predicts it). (2) VisitCloseStatement tests UNIT and NO REWIND but not REEL, with a comment electing to break SR2's equivalence — one-word fix; the pattern (a second consumer of one parse node deciding against a spelling equivalence) is the finding. (3) opt.REMOVAL() has zero callers. (4) EC-REPORT-NOT-TERMINATED: registered, never raised; EmitClose never consults file.ReportNames though EmitFileRegistration reads it eight lines up — one field access wide. (5) DispatchEmitter's __IoCheck emits `case F: __RunUse(i, s, e); return;` discarding the int — RESUME AT procedure-name never transfers on the Format-1 USE path (RESUME NEXT STATEMENT survives only because discarding -2 happens to mean fall-through). (6) The DELETE-side sibling: EmitDeleteFile SKIPS EmitUseHook entirely under ON EXCEPTION so the level-3 EC is never set (stale EXCEPTION-STATUS in the handler) — the INVALID KEY arm in the same file does it right with a suppression flag; add onExceptionHandled to __IoCheckEc instead of skipping. Unit/reel/volume remains an unmodelled category whose vacuity determination (GR3c, a REQUIRED documented item) is a code comment, not a CONFORMANCE.md row.

*(Found by Phase-B batch 8 — §8.8.1 + §14.9.1–14.9.12, 2026-08-29; one adjudicator + one independent refuter per subject. Full forensic evidence: the named rows' `notes` in `tests/version-matrix/traceability-inventory.json`; the raw finding lines in the session scratchpad `pb-batch8/findings.txt`.)*
