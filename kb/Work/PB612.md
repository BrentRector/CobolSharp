---
title: "PB612 — adjudication-lane follow-up from PB237's landing: the rows for §12.3.8.3 SR1/SR2/SR15, §12.3.8.4 GR2 (PROGRAM half) and GR10, §8.4.6.8, and part of FMT-12.3.8.2 are now implemented and witnessed by `conformance:2002/pb237_program_prototype` and the two `pb237-repository-program-*` negatives, but sit unadjudicated with no note owning them — free GAP closures with the evidence already on disk"
id: PB612
kind: analysis
status: open
severity: MINOR
area: process/traceability
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: ["12.3.8.3", "12.3.8.4", "8.4.6.8", "12.3.8.2"]
inventory_rows: []
tags: [cobolsharp, work, analysis]
---

# PB612 — OPEN — evidence on disk, rows still GAP

Found 2026-09-05 by the `PB237` implementer (report §"New defects", item 4), which recorded verdicts only for
its own `inventory_rows` per the brief and left these for the adjudication lane. Re-derived on this tree.

## The rows

`tests/version-matrix/traceability-inventory.json` rows for: §12.3.8.3 SR1 (repeated specifiers shall be
identical — `pb237-repository-program-inconsistent`, COBOLNET1761), SR2 (literal class —
`pb237-repository-program-literal-class`), SR15 (a program-specifier naming a containing program is ignored —
realized by `BinderDriver.BuildProgramDefinitionTable`); §12.3.8.4 GR2 for the PROGRAM specifier's literal-3
(the `SCALED` line of `pb237_program_prototype` calls through the externalized name) and GR10 a)/c)
(`BuildProgramDefinitionTable`, the run-unit registry as the external repository); §8.4.6.8's containing-program
spelling (`CONTAINER-BLOCKED` line); `FMT-12.3.8.2` partially (the PROGRAM alternative — the other four `AS`
phrases are `PB611`).

## What closes it

One `record_verdicts.py` batch from an adjudicator who reads each rule against the landed code and the golden
(the code sites and test-refs are named in `reports/PB237-report.md` and in `kb/Work/PB237`'s Landed section),
with `FMT-12.3.8.2` recorded PARTIAL naming PB611 for its residue. No compiler change. Fold into the next lane-3
batch that touches §12, or the next golden lander's verdict pass.
