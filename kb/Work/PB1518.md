---
title: "PB1518 — OWNER DECISION: batch 15d excluded GR-9.1.13.7-5 (I-O status '45', no record description entry selected with the FORMAT clause or the CODE-SET clause) because its only producers are the declined modules — record-description SELECTION exists only through SELECT WHEN (Annex A.4.8) or the FORMAT clause — so '45' has no producer and cannot be observed; does the row take the declined-module disposition (DOCUMENTED-NON-SUPPORT jointly with the modules), or stay live?"
id: PB1518
kind: decision
status: open
severity: MINOR
area: process/register
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
cluster: ["PB1255", "PB1198"]
spec_refs: ["9.1.13.7", "13.18.13.4"]
inventory_rows: []
closes_rows: []
tags: [cobolsharp, work, decision]
---

Filed 2026-09-24 by **REGISTRAR 15d** from lane-3 adjudication batch **15d** (adjudicated on the pinned tree `071ff04ea`; everything that landed since — Registrar 14a/14b/14c — is register-only, so no code change touches this mechanism). Every citation below was re-run by the registrar through `cite.py --check`.

# PB1518 — OPEN

## The rule (every citation re-run through `cite.py --check`)

> 5) I-O status = 45. … no record description entry was selected for processing with the FORMAT clause or the CODE-SET clause …

`OK  §9.1.13.7 5)  (Logic error condition with unsuccessful completion)`


## Row `GR-9.1.13.7-5` — NEEDS-OWNER-DECISION (editions 2002/2014/2023 (FORMAT and SELECT WHEN clauses))

§9.1.13.7 5) '45' record identification failure 'no record description entry was selected for processing with the FORMAT clause or the CODE-SET clause' (cite.py --check OK §9.1.13.7 5)). No producer: FileStatusCode defines no '45' and a search of src/Cobol.Net.Runtime and src/Cobol.Net.Compiler finds none. Reachability: record-description SELECTION exists only through SELECT WHEN (§13.18.13.4 GR4, cite.py --check OK §13.18.13.4 4): 'a record description entry is selected by evaluation of those SELECT WHEN clauses') or the FORMAT clause; CODE-SET without SELECT WHEN converts every record with one alphabet (GR3) and selects nothing. Both FORMAT and SELECT WHEN are Annex A.4.8, declared Not claimed in docs/CONFORMANCE.md §5 and refused by COBOLNET1705 at every edition (PB281 landed), so status 45 is unreachable in accepted source. But the derived declined-module selector deliberately EXCLUDES this row (tests/Cobol.Net.Tests.Unit/DerivedVerdictDriftTests.cs:427 Assert.DoesNotContain("GR-9.1.13.7-5") and kb/Work PB281's note: 'wants a one-row owner adjudication' because its text names CODE-SET, which is supported). No derived selector covers it, so this is the owner's call: fold it into the A.4.8 declined set (it is reachable only through that module) or require a producer.

## What the fixer owes

OWNER: the same shape as PB1255 (CODE-SET §13.18.13.4 GR4, conditioned on the declined SELECT WHEN / FILE … FROM modules) — answer both together. DNS → record GR-9.1.13.7-5 DOCUMENTED-NON-SUPPORT citing the declined modules' CONFORMANCE.md rows; live → an implementation note for the '45' producer when either module is claimed.
