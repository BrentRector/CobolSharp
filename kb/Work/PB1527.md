---
title: "PB1527 — an object reference inside a STRONG typed group contradicts the §7 DOC-A.1-214 determination: MOVE gives the group an image with 8 SPACE positions for the reference (BYTE-LENGTH 11 for PIC X(3) + one reference), and STRING of the same group compiles clean and aborts at run time"
id: PB1527
kind: defect
status: open
severity: MAJOR
area: codegen
wrong_answer: true
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB244", "PB1116"]
spec_refs: ["13.18.60.4", "A.1"]
inventory_rows: ["DOC-A.1-214", "DOC-A.1-217"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 22:46 UTC by **REGISTRAR-1** (cloud, branch `claude/adj-registrar-1`, kb/Work PB1522 step 2) from the lane-3 adjudication of the last 208 verdict-less inventory rows (wave 1 `claude/adj-cal-1` + `claude/adj-a1-w1-s1..s4`, wave 2 `claude/adj-w2-*`, all pinned at `0caa7d5`). Every citation below was re-run by the registrar through `python scripts/spec/cite.py --check`.

# PB1527 — OPEN

## The rule and the determination

> 22) … a) The amount of storage allocated … is not necessarily the same for every object reference.

`cite.py --check 13.18.60.4 "not necessarily the same for every object reference"` → OK §13.18.60.4 22). Annex A.1 item 214
requires the amount to be documented, and docs/CONFORMANCE.md §7 row `DOC-A.1-214` documents it: one .NET managed
reference, 8 bytes, which **contributes no character positions to any group image**.

## Repro (re-probed by the registrar, Debug build of `0caa7d5`, `--std 2023`)

Probes `/tmp/adj/adj-w2-a1-s3/probe/or2.cob`, `or3.cob`, `or4.cob`:

```cobol
       01 TG TYPEDEF STRONG.
          05 A PIC X(3).
          05 R USAGE OBJECT REFERENCE ORC.
       01 G TYPE TG.
       01 XX PIC X(20).
       ...
           MOVE "ABC" TO A. MOVE G TO XX.
```

| Probe | Observed |
|---|---|
| `or2` | `FUNCTION BYTE-LENGTH(G)` = `00011`; `MOVE G TO XX` → `[ABC                 ]`, positions 4, 11 and 12 all SPACE — the reference contributed 8 space positions |
| `or4` | `MOVE G TO XJ` (JUSTIFIED) → `[   ABC        ]` — the 11-position image, right-justified |
| `or3` | `STRING G DELIMITED BY SIZE INTO XX` compiles clean, then `NotImplementedCobolFeatureException: … whole-group image of 'G' with a pointer/object-class leaf (no character image) — the Tier-C byte island …` |

So the documented determination ("no character positions") and the code (8 space positions in MOVE and BYTE-LENGTH; a run-time
abort in STRING) disagree, and they disagree with EACH OTHER inside the code. Either the §7 row or the image composer is wrong;
rule 1 decides by §13.18.60.4 + §8.5 group semantics, and the result is ONE group-image rule every consumer (MOVE, STRING,
DISPLAY, BYTE-LENGTH, comparison) reads.

## Code sites

The STRUCT-SCOPED group image composer (the Tier-C "no whole-group character image" path — see PB244 for the DISPLAY twin
and PB1116 for the INVOKE twin) and the MOVE group-sender path that pads the reference leaf with spaces.

## Owns

`DOC-A.1-214` (adjudicated **DIVERGES** on branch `claude/adj-w2-a1-s3`; not recorded — DOC rows wait on the PB1535 decision).

## PB1522 step 3 (doc-rows-1 integrator, 2026-09-24 23:55 UTC, branch `claude/adj-doc-rows-1`)

**Sibling: POINTER and PROGRAM-POINTER inside a STRONG typed group** (A.1 item 217, OMITTED from §7). Re-probed `/tmp/adj/doc/doc-w1-s6/probe/p217/pp2.cob` and `fp.cob` (`--std 2023`): `01 TG TYPEDEF STRONG. 05 A PIC X(3). 05 GP USAGE PROGRAM-POINTER.` → `FUNCTION BYTE-LENGTH(G)` = 12 and `MOVE G TO XJ` gives `[ABC        Z        ]` (8 spaces for the pointer); `05 P USAGE POINTER` the same. §7 DOC-A.1-210 documents POINTER / PROGRAM-POINTER as managed slots of image width 0, so the group image contradicts the register exactly as the object reference does for DOC-A.1-214. One mechanism (`TierCIsland` gives a managed leaf of a STRONG group an 8-byte image); this note now claims DOC-A.1-217. DOC-A.1-214's existing row stays (it states the intended behaviour) and its verdict stays unrecorded until the fix.
