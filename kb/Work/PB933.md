---
title: "PB933 — INITIALIZE carries a SIXTH private reading of §8.3.3.6.2 whose figurative word map disagrees with the shared one (NULL/NULLS → 'N' where `FigurativeConstants.KindOf` says 'L'), and the PRECONDITION for all of them is that a level-88 VALUE operand is RAW PARSE TEXT in the bound tree (`Condition88.Values[].Low`), so three CodeGen sites re-parse COBOL during emission"
id: PB933
kind: defect
status: open
severity: MINOR
area: binding
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
cluster: [PB880, PB418, PB461]
spec_refs: ["8.3.3.6.3", "13.18.63.4"]
inventory_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-20 by **REGISTRAR #7** — the lead-filing pass over the wave-39 implementer reports, the wave-40 PB246 report, the wave-41 finisher and implementer reports (PB449-finisher, PB511-finisher, PB566-finisher, PB245, PB560, PB412), the train-40 and train-42 lander reports, and DEVLOG 1617-1621. **Every probe below was rebuilt from the source report and RE-RUN on this worktree's own `dotnet build CobolSharp.sln -c Debug` (0 warnings, 0 errors) at main `c701786d5`**, and **every citation was re-derived with `python scripts/spec/cite.py --check`** — the command line is printed beside the quotation. Probe sources under `<scratch>/reg7/probe/`.

# PB933 — OPEN — the copy that was not folded, and the reason copies are possible

Source: the `PB461` report, "New defects" items 1 and 2. They are ONE note because the second is the
first's precondition, which is what makes the fix a single change of shape rather than a deletion.

## Arm 1 — the sixth reading

`src/Cobol.Net.Compiler/Binding/Procedure/Verbs/InitializeBinder.cs:795-827`
(`InitializeValueOperand`) has its own ALL strip and its own figurative word map, and that map sends
`NULL` / `NULLS` to `'N'` where the shared `FigurativeConstants.KindOf` sends it to `'L'`.

**PB461** (landed) folded three emitter copies into one recipe. This is the copy it did not fold, and the
reason is recorded in the drift test itself: the INITIALIZE binder was in flight in the same wave. The
note's fix is therefore known and small — delete `InitializeFigurativeKind`, call `Classify`, remove the
drift test's exemption.

⚠ **The divergence is not yet shown to be OBSERVABLE, and this note does not pretend it is.** Reading the
two maps side by side is the repro the report gives; whether a program can reach the `'N'`/`'L'`
disagreement through INITIALIZE REPLACING is a probe the fixer runs FIRST
(`feedback_reachability_is_measured_not_deduced`). If it is reachable it is a wrong answer and this note's
severity moves; if it is not, the fold still lands, because a divergent private copy of a rule is how the
next wrong answer is built.

```
$ python scripts/spec/cite.py --check 8.3.3.6.3 "without the ALL phrase"
OK  §8.3.3.6.3 1)  (Syntax rules)
    a) If the literal is restricted to a numeric literal, the only figurative constant permitted is ZERO
       (ZEROS, ZEROES) without the ALL phrase.
```

## Arm 2 — why there were ever six

`Condition88.Values[].Low` is **the parse text**. The bound tree carries the characters the user typed, so
three CodeGen sites re-parse COBOL during emission — and every site that needs to know what a level-88
VALUE MEANS has to write its own reading of §8.3.3.6.2. That is the precondition for PB461 existing, and
for this sixth copy existing, and for the seventh.

```
$ python scripts/spec/cite.py --check 13.18.63.4 "condition-name"
OK  §13.18.63.4 19)  (General rules)
    19) The characteristics of a condition-name are implicitly those of its conditional variable.
```

A VALUE is a value — GR19 makes its characteristics those of the conditional variable — and a bound tree
that stores it as text has thrown that away and asked every consumer to recover it.

## The code sites

* `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/InitializeBinder.cs:795-827` (arm 1)
* `src/Cobol.Net.Compiler/Binding/Model/Condition88.cs` + `SetBinder.BindSetCondition` (arm 2)

## The fix shape

`InitializeValueOperand` already returns a `BoundOperand` — it is the shape `Condition88` should copy. Make
the level-88 VALUE operand a bound operand at bind time and the three emission-time re-parses have nothing
to re-parse; then the figurative reading has exactly one site because there is exactly one place the
question can be asked. CLAUDE.md rule 5: the restructuring IS the fix, and deleting the sixth map without
it leaves the seventh possible.

## Rows

None claimed — no row records "how many copies of this rule exist". The evidence for the fix is the drift
test that counts the readers, in the shape
`ConditionValueRecipeDriftTests.TheNumericEditedValueImage_HasExactlyTwoReaders` already uses.
