---
title: "PB771 — the RELATIVE and INDEXED organizations establish NO §9.1.15 file lock against other RUN UNITS: they hold no long-lived host handle at all between OPEN and CLOSE — the whole store is read at OPEN and rewritten at CLOSE through `HostFile.OpenAuxiliary`, fixed at `FileShare.ReadWrite` — so `SHARING WITH NO OTHER` on an INDEXED or RELATIVE file protects nothing from another process, and the CLOSE then truncates and rewrites the physical file from a snapshot taken at OPEN, discarding whatever the other run unit wrote, on '00' from both sides"
id: PB771
kind: defect
status: landed
severity: MAJOR
area: runtime/io
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["9.1.15", "14.9.27.4"]
inventory_rows: ["GR-9.1.15-1", "GR-9.1.15-2", "GR-9.1.15-3"]
closes_rows: ["GR-9.1.15-1", "GR-9.1.15-3"]
tags: [cobolsharp, work, defect, files, file-sharing]
---

# PB771 — LANDED — two of the four organizations never took a file lock, in any sharing mode

Found 2026-09-06 by the [[PB740]] finisher (report, "New defects (b)", which says *"Worth its own note"*).
Verified against main (`9fad13e5`) and against PB740's branch (`397595e5`, landing train 18) by reading both
trees. ⛔ **Reachability is argued from the code, not measured** — see the reproduction status.

## ⛔ Reproduction status — read this before designing the fix

- **Nothing was compiled or run for this note.** It is a reading of both trees. `feedback_reachability_is_measured_not_deduced`
  says a reachability claim is a probe to run; the probe is named below and is owed before the fix is designed.
- **The probe.** Two COBOL programs (or one program and one shell writer) over the SAME `ASSIGN`ed path,
  `ORGANIZATION IS INDEXED` (and again `RELATIVE`), the first with `SHARING WITH NO OTHER`: run unit A OPENs
  I-O and holds it; run unit B OPENs the same file, WRITEs a record, CLOSEs; A then READs, WRITEs one record of
  its own and CLOSEs; read the physical file. §9.1.15 1) requires B's OPEN to be **unsuccessful**. The two
  predictions to record are (i) B's OPEN status and (ii) whether B's record survives A's CLOSE.
- The sequential organization's twin of this measurement WAS taken — PB740's report records
  *"`SHARING WITH NO OTHER`, external process appends mid-open → SUCCEEDED, file ends `SEEDXXXX`"* before its fix
  and REFUSED after — so the shape of the experiment is established; only the keyed arm is unrun.

## What the standard requires — every clause re-derived with `cite.py --check`, none inherited

- `--check 9.1.15 "The sharing with no other mode specifies exclusive access to a physical file"` →
  **OK §9.1.15 1)**. *Exclusive access* is the whole of the requirement, and item 1) goes on: *"If the OPEN
  statement is successful, subsequent requests to open the physical file through other file connectors before
  this file connector is closed will be unsuccessful."*
- `--check 9.1.15 "The successful opening of a file establishes a file lock for the applicable sharing rules,
  thereby preventing other run units from opening that file with incompatible sharing rules"` → **OK §9.1.15 3)**.
  This is the sentence this note is about, and it is the only one in the standard that names OTHER RUN UNITS as
  the thing being prevented.
- `--check 9.1.15 "Multiple paths of access may exist in the same runtime element"` → **OK §9.1.15 3)**; the
  sentence ends *"…, contained elements, separate runtime elements within the same run unit, or runtime elements
  in different run units."* The last clause is what makes an in-process registry insufficient by itself.
- `--check 9.1.15 "Before access to a shared physical file is allowed through an OPEN statement, the sharing mode
  and the open mode of that OPEN statement shall be allowed by all other file connectors that are currently
  associated with the physical file"` → **OK §9.1.15** (unnumbered). This is the OTHER audience — the file
  connectors of THIS run unit, Table 19's gate — and it is the one the compiler does implement, for all four
  organizations, in `FileRegistry`.
- `--check 14.9.27.4 "The SHARING phrase specifies the level of sharing permitted for the physical file
  associated with file-name-1 and specifies the operations that may be performed on the physical file through
  other file connectors sharing the physical file"` → **OK §14.9.27.4 22)**.

⛔ **Premise correction, since the dispatch asked for "the OPEN statement's file-lock sentence".** §14.9.27
carries NO sentence containing "file lock" — measured by scanning the whole of §14.9.27.4. GR22 and GR23 point at
§9.1.15 and stop there. So **§9.1.15 3) is the only home of the file-lock rule**, and a citation of §14.9.27 for
it would be `feedback_a_real_clause_can_answer_a_different_question`.

## The mechanism on main (`9fad13e5`)

1. `src/Cobol.Net.Runtime/IO/FileSupport.cs:264-266` —
   `public static FileStream OpenAuxiliary(string hostPath, FileMode mode, FileAccess access) =>
   new(hostPath, mode, access, FileShare.ReadWrite, 4096, FileOptions.SequentialScan);`
   No share parameter: the role IS the posture, and the role is *"a SHORT-LIVED stream the runtime opens for its
   own bookkeeping"* (`:255-263`). It is `ReadWrite` for a good and cited reason — [[PB713]]: the path may already
   be held by a connector of this run unit and a narrower request would manufacture an I/O failure the COBOL
   statement never asked for.
2. **`RelativeConnector` and `IndexedConnector` reach the physical file ONLY through that role.**
   `RelativeConnector.cs:119/127/139` and `:478` call `RecordFraming.WriteStore(HostPath, …)`; `:467` calls
   `RecordFraming.ReadStore(HostPath)`. `IndexedConnector.cs:202/211/224/264/785` do the same.
   `RecordFraming.cs:51` and `:76` are both `using var fs = HostFile.OpenAuxiliary(...)` — opened, used, closed.
3. **Neither class holds a stream at all.** `RelativeConnector`'s fields are a `RelativeStore _st` and scalars
   (`:17-35`); `IndexedConnector`'s are its record map; `KeyedConnector` declares no `Stream`. So **between a
   successful OPEN and its CLOSE there is no host handle in existence for these two organizations**, and a host
   handle is the only thing that can say anything to another process.
4. **The arbitration that DOES exist is in-process only.** `FileRegistry` is the Table-19 arbiter and is reached
   through a static registry ([[PB603]] records `CobolFile._reg` as static and its run-unit consequences). It
   answers §9.1.15's first audience — the file connectors of this run unit — and cannot answer the second.
5. **By contrast the sequential organization does hold one**: `SequentialConnector.cs:359/365/398/420` open through
   `HostFile.OpenConnectorStream` / `OpenAppendStream` and keep `_reader`/`_writer` for the life of the OPEN.
   Whether its POSTURE is right is a different defect and is [[PB740]]'s (on main the posture is the
   `SharedStreams` boolean at `FileConnector.cs:168`, which is inverted for `SHARING WITH NO OTHER`); what
   matters here is that the sequential organization has a handle to be right or wrong about and the keyed ones
   have none.

## The wrong answer is bigger than "unprotected", and this is why the note is MAJOR

An absent lock alone would be a missing refusal — an under-rejection. But the keyed store model turns it into a
**silent data loss in both directions**, because the store is a snapshot:

- `ReadStore` at OPEN takes the whole physical file into memory. Every subsequent READ answers from that
  snapshot, so records another run unit writes after our OPEN are invisible to us — a READ that reports `'00'`
  and delivers superseded content, which is [[PB753]]'s harm shape arriving by a different route.
- `WriteStore` at CLOSE opens `FileMode.Create` and rewrites the file from our snapshot plus our own changes
  (`RelativeConnector.cs:478`, `IndexedConnector.cs:264`). **Everything the other run unit wrote is truncated
  away**, and both run units report `'00'` at every step.

Which is why `wrong_answer: true` and `silent: true` and not merely `under_rejects`. ⚠ Both halves follow from
reading the store model and neither is measured; the probe above measures the second one directly.

## PB740's branch (`397595e5`, landing train 18) — what changes and what does not

- **Changes:** the sequential posture becomes a DERIVATION — `src/Cobol.Net.Runtime/IO/Sharing/FileLockPosture.cs`
  (`OfSharingMode` reads §9.1.15 1)/2)/3) literally, `For` widens it by what Table 19 has already admitted),
  applied in one place by `FileRegistry.SyncHostPostures`/`PostureOf`; `HostFile.OpenConnectorStream` takes a
  `FileShare` (`FileSupport.cs:251`) instead of the boolean, `OpenConnectorWriteStream` (`:293`) replaces
  `OpenAppendStream`, `SharedStreams` is deleted, and `FileConnector.Reposture` is virtual.
- **Does NOT change:** `OpenAuxiliary` is byte-for-byte the same on the branch (`FileSupport.cs:265-266`,
  `FileShare.ReadWrite`, no parameter), and `RelativeConnector`/`IndexedConnector` still reach the file only
  through `RecordFraming.ReadStore`/`WriteStore`. **This note is true on both trees.**
- The branch DOCUMENTS the gap rather than closing it — `docs/rearchitecture/DESIGN-runtime-library.md:279-283`:
  *"Only the sequential organization holds a long-lived host handle; the keyed and relative connectors load and
  persist their store through `OpenAuxiliary` and hold none, so the base implementation is the whole of their
  obligation — **and, as a consequence, they establish no file lock against other run units at all.**"*
  That sentence is this note's charter, and it is correct that PB740 was not the change set to fix it in.

## ⛔ The inventory rows, and the verdict that is about to stand over this

`GR-9.1.15-1`, `GR-9.1.15-2`, `GR-9.1.15-3` are `state: GAP` with an empty verdict on main.
**PB740's verdict batch flips all three to CONFORMS**, on evidence taken entirely from the SEQUENTIAL
organization (`pb740_clauseless_share`, `FileLockPostureDriftTests`). When train 18 lands, a CONFORMS verdict
will stand over a rule that two of the four organizations do not implement at all —
`feedback_green_test_can_hold_a_gap_open`, and exactly the shape the burn-down cannot see.

They are deliberately **not** claimed in `inventory_rows:` — on main they are unverdicted rather than defectively
verdicted, and `DefectiveRowCoverageDriftTests` reads a claim on an unverdicted row as noise (the reasoning
[[PB770]] used for the same situation). ⭐ **What is owed instead:** when this note is worked, either re-verdict
`GR-9.1.15-1/-2/-3` down from CONFORMS and claim them here, or record on those rows that the claim is scoped to
the sequential organization. Whoever lands PB740 should be told; that decision belongs to the change set that
lands the fix, not to this filing.

## The fix shape

The rule to write down once is **"a successful OPEN holds, for the life of that OPEN, a host handle whose share
posture is the file lock §9.1.15 3) requires"** — today it is a property of one organization's implementation
detail. Two admissible structures:

1. ⭐ **The keyed connectors take and hold their own handle.** `OpenCore` opens the physical file once with the
   derived posture, reads the store THROUGH that handle, keeps it for the life of the OPEN, writes the store back
   through it at CLOSE, and releases it there. It reuses PB740's derivation unchanged (`FileLockPosture.For`),
   makes `Reposture` meaningful for these organizations instead of a no-op base call, and removes the two
   `OpenAuxiliary` round trips. ⚠ It must not re-open [[PB713]]: the handle has to be the SAME one the store is
   read and written through, or the second open collides with the first.
2. **The registry holds it on their behalf** — one host handle per physical path in
   `Sharing/PhysicalFileTable`, taken on the first successful OPEN of that path and released with the last CLOSE,
   postured by `FileLockPosture.For`. This puts the lock exactly where the sharing state already lives, covers a
   fifth organization for free, and keeps the connectors' store model untouched. ⚠ It has to interact correctly
   with the sequential organization's OWN handle on the same path (the run unit's effective lock is the
   INTERSECTION of its handles' postures — PB740's design says so), which is the argument for doing 2 for the
   keyed organizations only, or for doing it for all four and deleting the per-connector posture.
   ⛔ Choosing between 1 and 2 is a design decision that belongs in `DESIGN-runtime-library.md` in the same
   change set (CLAUDE.md rule 2 and rule 5).

Whichever shape: **the drift test is the deliverable that keeps it true** — no organization under
`src/Cobol.Net.Runtime/IO` may complete an OPEN without a live host handle carrying its derived posture, measured
through the connectors rather than asserted about them, in the manner of `FileLockPostureDriftTests`. Pair it with
the cross-process probe as a golden for at least `SHARING WITH NO OTHER` × {INDEXED, RELATIVE}, and keep PB740's
sequential legs green as the control.

## Sweep

- **It is uniform across sharing modes and across both keyed organizations**, so there is no second arm hiding:
  `NO OTHER`, `READ ONLY`, `ALL OTHER` and the undetermined default all produce the same absent lock.
  `feedback_scan_all_similar` — the sweep here found the population is exactly two organizations, and both are
  named.
- **Not this note, checked and excluded deliberately:** [[PB740]] (the sequential organization's posture, and
  inverted rather than absent), [[PB753]] (a stale read-ahead INSIDE one run unit, sequential only), [[PB754]]
  (`OPEN OUTPUT` clearing a store other connectors of THIS run unit are attached to), [[PB603]] (the implicit
  CLOSE bypassing the sharing registry). All four are about the SAME run unit; this is the only one about
  another.
- ⛔ **The undetermined-default question is the owner's and is not re-asked here.** PB740's standing owner
  question 20 — whether a connector that wrote no SHARING and no LOCK MODE clause should hold an OS handle that
  protects its file from other processes at all — governs one row of the answer for the sequential organization.
  This note is about the modes where §9.1.15 leaves nothing undetermined, `SHARING WITH NO OTHER` first among
  them, so it does not depend on that answer.

## Append — 2026-09-09 — the measurement instrument now exists ([[PB795]], landed)

Filed by the registrar from the PB795 implementer's report (§"New defects", N2). ⚠ **The report proposed a new
note; it is the same mechanism this one already owns**, so it is recorded here rather than opened again. Every
fact below was re-derived on main at `d497f3c7`.

**What is new is not the defect — it is that the defect is now cheap to ASSERT.** [[PB795]] landed
`tests/_shared/HostCapability.cs`, whose `OutsiderCan(path, FileAccess)` (`:189-199`) answers *"can a process
outside this run unit open the file for read / for write?"* as a MEASUREMENT with three classified outcomes
(`Enforced`, `BypassedByThisIdentity`, `ToolFailed`), all three proven to fire. Until now the deliverable this
note asks for — *"no organization under `src/Cobol.Net.Runtime/IO` may complete an OPEN without a live host
handle carrying its derived posture, measured through the connectors rather than asserted about them"* — needed
its instrument built first. It does not any more: `FileLockPostureDriftTests` (PB740's class) already carries
the fixture, and the keyed leg is `OutsiderCan` on a file a `SHARING WITH NO OTHER` INDEXED connector has open.

⛔ **Write that assertion RED first.** A drift test added over a lock that does not exist will pass or fail for
reasons unrelated to the posture unless the host capability itself was measured — that is precisely PB795's own
subject (two tests asserting a host capability neither had measured), and repeating it here would be the same
defect one level up.

**The code sites, re-verified on main:**

- `src/Cobol.Net.Runtime/IO/FileSupport.cs:278` — `HostFile.OpenAuxiliary` is **unconditionally**
  `FileShare.ReadWrite`, and its own doc comment says why it must be (a second handle of THIS run unit must not
  manufacture a refusal the COBOL statement never asked for — [[PB713]]). ⛔ **That reasoning is sound and it is
  also exactly what makes the keyed organizations lockless against OTHER run units**: the only handle they ever
  hold is this one.
- `src/Cobol.Net.Runtime/IO/FileRegistry.cs:941` — `SyncHostPostures` reaches only connectors that hold a
  long-lived handle, so for RELATIVE and INDEXED it has nothing to posture.
- ⚠ **[[PB802]] moved the store's shape but not this property.** The framed store now carries a header
  (organization, record type, record sizes, key table) and `FixedFileAttributes` is its codec — the sidecar is
  gone — but `RecordFraming.WriteStore` / `ReadStore` still open through `HostFile.OpenAuxiliary`
  (`RecordFraming.cs:122`, `:156`, `:183`, `:334`), share `ReadWrite`, for the whole-store read at OPEN and the
  whole-store rewrite at CLOSE. The lock is absent for the same reason it always was.

**The design doc already records the property and asserts it nowhere** — `DESIGN-runtime-library.md:291`,
*"and, as a consequence, they establish no file lock against other run units at all"*. `cite.py --check 9.1.15 "The successful opening
of a file establishes a file lock"` → **OK, §9.1.15 3)**. A program writing `SHARING WITH NO OTHER` on an
INDEXED file gets §9.1.15 1)'s *"exclusive access"* honoured inside the run unit by Table 19 and **not at all**
outside it, with no diagnostic: `silent`, unchanged, and now measurable in one line.

`inventory_rows` is unchanged — `GR-9.1.15-1`, `-2` and `-3` are already claimed by this note.

## Landed

**Both halves of this note were RE-PROBED on this worktree's own build before anything was changed, and both
reproduced — the note's own reproduction warning is discharged.** The instrument [[PB795]] landed is what made it
one line:

- `FileLockPostureDriftTests.TheFileLockIsTheSharingMode_MeasuredFromOutsideTheConnector` gained an ORGANIZATION
  axis and went **4 red of 9** on main — `NoOther`×{Indexed, Relative} (an outsider could READ a file declared
  *exclusive*) and `ClauseLess`×{Indexed, Relative} (an outsider could WRITE one). All three sequential arms were
  green, which is PB740's fix standing as the control.
- The data loss, measured with a throwaway probe: `A OPEN I-O SHARING WITH NO OTHER = '00'`; another run unit's
  4-byte append while A held it open **SUCCEEDED**; `A CLOSE = '00'`; the physical file went
  46 → 50 → **46** bytes (indexed) and 28 → 32 → **28** (relative), and the other run unit's bytes were **gone**.
  Prediction (i) and prediction (ii) of the note's probe, both confirmed.

**The fix is the note's starred shape 1, and the design decision is recorded in
`docs/rearchitecture/DESIGN-runtime-library.md` and `docs/COBOLNET_FILES_DESIGN.md` D14 in the same change set.**
Shape 2 (a registry-held handle per physical path) was rejected for `feedback_one_mechanism_per_job`: the
sequential organization already holds its own handle, so a registry handle beside it would be a SECOND answer to
"what is this run unit's file lock" and the two would have to be intersected by hand. Shape 1 makes the invariant
uniform — *every* connector holds a live handle carrying its derived posture — which is the property a drift test
can state.

- **`KeyedConnector` owns the handle, once, for both organizations.** `TakeFileLock(create)` on each `OpenCore`
  arm that has a physical file (the absent-OPTIONAL INPUT arm takes none — §9.1.15 establishes a lock on a
  PHYSICAL FILE and there is none), `ReleaseFileLock()` in each `CloseCore`'s `finally` (§9.1.15, *"The file lock
  is removed by an explicit or implicit CLOSE statement executed for that file connector"* — `--check` OK), and
  `Reposture` to rebuild it.
- **The store travels THROUGH that handle.** `RecordFraming.ReadStore`/`WriteStore` now take a `Stream` instead
  of a path — a second handle for the load or the persist would ask the host for access the connector's own
  `FileShare.None` forbids, which is [[PB713]]'s defect re-opened by its own cure. `WriteStore` truncates
  (`SetLength(0)`) as part of the rewrite instead of relying on a `FileMode.Create` at open, because the handle
  outlives every persist and `Reposture` rebuilds it mid-open. Both compose/parse the whole store in ONE
  `Read`/`Write`, which also makes them independent of the posture-decided buffer size and removes two syscalls
  per frame that the previous shape paid.
- **`OpenAuxiliary` keeps its `FileShare.ReadWrite` and its [[PB713]] reasoning intact** — what changed is that
  the keyed store's load and persist LEFT its role list. Everything still on it (the shared `OPEN EXTEND`
  write-base measurement, the §14.9.27.4 GR10 store-header read, the varying framing's parse check) runs BEFORE
  the connector's own handle for that OPEN exists, which is what makes the permissiveness free.
- **⭐ THE RE-ARCHITECTURE THE ESTIMATE DID NOT CONTAIN (CLAUDE.md rule 5): the widening had to learn the
  ACCESS.** `FileLockPosture.For` took open MODES and mapped them through `AccessOf`; a keyed connector's handle
  needs `ReadWrite` in every writable mode, because the store is rewritten WHOLE and §14.9.51.4 GR29 a)'s release
  number is *"one greater than the highest relative record number existing in the physical file"* — a fact of
  the records already there. A sibling widened by the mode-derived floor admits the write, the host refuses the
  read, and the OPEN the standard ALLOWED answers '30'. So `FileConnector.HostAccess(mode)` is where a connector
  states what it takes, `FileLockPosture.For` takes ACCESSES, and `FileRegistry.OtherAdmittedAccesses` asks each
  connector rather than guessing from `st.Open`. **This was measured, not foreseen**: the first build of the fix
  went red on 24 cells of `TheHostHandleNeverVetoesWhatTable19Permits`.
- **And one rule was found written in two places.** The first build was also wrong about the rebuild ORDER, and
  the sequential connector had already derived the right answer in a comment of its own: a handle holding WRITE
  access is itself what refuses its replacement, so it must be released first, with a fallback re-open at the
  posture it had. That is now `FileConnector.RebuildMustReleaseFirst`, derived from `HostAccess`, and
  `SequentialConnector.Reposture` reads it in place of its `Mode == FileOpenMode.IO` spelling — the same
  condition, now stated as what it means (`feedback_one_rule_one_place`).

**Sweep.** `FileConnector.AbandonOpen` is the sibling the note did not ask for: §9.1.15 establishes a lock on
*"The SUCCESSFUL opening of a file"*, and `Close()` answers '42' for a connector that is not open, so a handle
taken by a body that then failed would be held until the process ended — a file lock no COBOL statement could
remove. The keyed override releases; `SequentialConnector` overrides it too (no arm reaches that state there
today, and the override is what keeps that true of arms added later). `RelativeConnector.Load` and
`IndexedConnector.Load` also lost their second `HostFile.Probe` per OPEN: the handle's existence is that answer.

**Goldens.** `tests/conformance/{2002,2023}/pb771_keyed_file_lock.{cob,out}` (`PB771K02` / `PB771KEY`), four legs
over RELATIVE and INDEXED: the clause-less INPUT+EXTEND pair with GR29 a)'s record number moved back into the
RELATIVE KEY item (`L1-K=003`), the `SHARING WITH NO OTHER` '61' control (§9.1.13.9 1) a)) for each organization,
and the clause-less INPUT+I-O pair at DYNAMIC access (§14.9.27.4 GR8 Table 20 lists WRITE under the I-O column
for random and dynamic). Every expected value was derived from the spec before the run and matched line for line.
⛔ **A file lock is not observable from inside one run unit** — it names no requester — so what the corpus
certifies is the half a new handle could silently have broken: the gate INSIDE the run unit is still the file
connectors and Table 19. The lock itself is measured from a handle OUTSIDE the connector, by the drift class.

**Guards.** `EveryOrganizationHoldsALiveFileLockWhileOpen` is the deliverable the note asked for — *no
organization completes an OPEN without a live host handle carrying its derived posture* — measured THROUGH the
connectors with `HostCapability.OutsiderCan`, over every (organization × open mode), plus the give-back at the
CLOSE. `EveryOrganisationsHandleAccessLiesInThatBand` pins the premise the symbolic theorems now quantify over (a
handle takes at least the open mode's floor and at most that floor plus READ — never WRITE on an INPUT handle,
because §14.9.27.4 GR16 and §9.1.13.6 item 6 a) make write capability a question asked of every mode BUT input)
and turns a FOURTH organization RED instead of letting it inherit the omission.
`TheHostHandleNeverVetoesWhatTable19Permits` carries the organization axis too — 768 cells.

**Rows.** `GR-9.1.15-1` and `GR-9.1.15-3` PARTIAL → **CONFORMS** (GAP 2561 → 2559). `GR-9.1.15-2` stays
**PARTIAL**, and its scope note is rewritten to name the ONE residue that is left, which is the HOST and not the
organization: [[PB795]]'s measured determination that a `FileShare.Read` file lock cannot refuse a writer on a
host whose sharing is one advisory `flock` with two states, so §9.1.15 2)'s *"restricts concurrent access … to
input mode"* has no expression on Linux or macOS. That residue is the same for all four organizations and is
recorded in `DESIGN-runtime-library.md`; it is not this note's.

**Gate.** `pwsh scripts/build-local.ps1 -Filter "~FileLockPosture|~SharedExtend|~SharedRead|~KeyedIo|~Sharing|~OpenTable19|~FileAuthority|~Drift|~CorpusRunner|~SpecTraceabilityInventory"` — verdicts in the report.
