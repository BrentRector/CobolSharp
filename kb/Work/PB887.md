---
title: "PB887 — ACCEPT's transfer is written as a HAND LIST OF RECEIVER CATEGORIES where §14.9.1.4 GR6 says 'according to the rules for the MOVE statement', so `AcceptDisplayEmitter` carries a second copy of `MoveEmitter.ConvertSource` — and the copy is missing an arm: `ACCEPT F` into a `USAGE COMP-2` receiver aborts the run unit with a NotImplementedCobolFeatureException, although §14.9.1.4 GR1 makes the device conversion OURS to define and §14.9.1.3 SR1 excludes only index / message-tag / object / pointer"
id: PB887
kind: defect
status: open
severity: MAJOR
area: codegen/accept
wrong_answer: false
crashes: true
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: [PB160]
spec_refs: ["14.9.1.3", "14.9.1.4"]
inventory_rows: ["GR-14.9.1.4-6"]
tags: [cobolsharp, work, defect]
---

Filed 2026-09-19 by **REGISTRAR #6** — the lead-filing pass over the implementer, finisher and lander reports
that landed after registrar #5 (PB416, PB426, PB482, PB368, PB391, PB555, PB401, PB403, PB388, PB436, PB408,
PB447, PB419, PB427, PB443, PB396, PB411, PB420, PB425, PB877, PB391-finisher, PB405, PB522, PB502; landing
trains 33-38 and train-pb877). **Every probe below was REBUILT from the source report and RE-RUN on this
worktree's own `dotnet build CobolSharp.sln -c Debug` (0 errors) at main `80dd97e2`**, and **every citation was
re-derived with `python scripts/spec/cite.py --check`** — the command line is printed beside the quotation.
Sources under `<scratch>/reg6/probe/`.

# PB887 — OPEN — the arm list a general rule says should not exist, and the arm it is missing

Source: the `PB420` report, "New defects" items 1 and 2. The report asked for **ONE note for the pair**,
because both live in the arm list GR6 says should not exist; this is it.

## Arm A — the measured abort

`<scratch>/reg6/probe/p32_accept_float.cob` (PROGRAM-ID `R6P32`), `--std 2023 --run`:

```cobol
01 F USAGE COMP-2.
PROCEDURE DIVISION.
    ACCEPT F
```

```
Unhandled exception. CobolNet.Runtime.NotImplementedCobolFeatureException: ... ACCEPT into floating-point
receiver 'F' (COMP-1/COMP-2 device conversion, deferred)
rc=127
```

The identical statement into `PIC 9(8)` reads the device fine.

```
$ python scripts/spec/cite.py --check 14.9.1.4 "Any conversion of data required between the device and the data item referenced by identifier-1 is defined by the implementor"
  OK  §14.9.1.4 1)  (General rules)
      1) The ACCEPT statement causes the transfer of data from the device. This data replaces the content of
      the data item referenced by identifier-1. Any conversion of data required between the device and the
      data item referenced by identifier-1 is defined by the implementor.
```

The conversion is OURS to define, and §14.9.1.3 SR1 excludes only index, message-tag, object and pointer — a
float receiver is legal source. **The mechanical blocker is that the device window is
`DataItem.DisplayTextWidth`, which is `pic.Digits + sign` for a numeric receiver, and a float PICTURE has no
digit positions.** Closing it therefore needs a §7 CONFORMANCE determination (transfer width + the accepted
character syntax), so it is an ADJUDICATION-FIRST item — the determination is published in
`docs/CONFORMANCE.md` §7 with a drift test reading the number back, the way PB463's A.1 item 62 was.

## Arm B — why the arm could be missing for years

```
$ python scripts/spec/cite.py --check 14.9.1.4 "according to the rules for the MOVE statement"
  OK  §14.9.1.4 6)  (General rules)
      6) The ACCEPT statement causes the information requested to be transferred to the data item specified
      by identifier-2 according to the rules for the MOVE statement. DATE, DAY, DAY-OF-WEEK, and TIME
      reference the current ...
```

`src/Cobol.Net.Compiler/CodeGen/Verbs/AcceptDisplayEmitter.cs#EmitAcceptTemporal` writes its OWN numeric /
numeric-edited / edited-character / alphanumeric / national / float arms — a second copy of
`src/Cobol.Net.Compiler/CodeGen/Verbs/MoveEmitter.cs#ConvertSource` (memory `one_rule_one_place`). It is why
the float arm could be absent here while MOVE's was live, and why PB420's fix had to be made twice.

**The structural repair is a synthetic sending operand for the GR7-GR12 conceptual item, so the temporal
store becomes `move.Emit`.** PB420 deliberately did not do it because that touches the MOVE *sending* seam,
which was PB425's subject in flight. PB425's cluster was DROPPED by train 38 and `kb/Work/PB425` is still
`open`, so the seam is free — but the fixer should check whether PB425 is in flight again before starting,
and coordinate rather than race.

## Rows

`GR-14.9.1.4-6` is CONFORMS and had no claimer; claimed here, because the measurement above contradicts it
(a second copy of the MOVE rules, one arm short, is not "according to the rules for the MOVE statement").
`GR-14.9.1.4-1`/`-2` are `PB160`'s — the Annex A.1 DOCUMENTATION rows for exactly this determination, which
is why `cluster:` names it: the §7 row PB887's adjudication produces is the row PB160 is waiting for.
