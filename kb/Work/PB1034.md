---
title: "PB1034 — relation conditions accept `NOT >=`, `NOT <=`, `NOT GREATER THAN OR EQUAL TO`, `NOT LESS THAN OR EQUAL TO` and `EQUAL THAN`, which §8.8.4.2.2 Format 1 does not print: `IF A NOT >= B` compiles and runs at every edition (PB333 fixed only START)"
id: PB1034
kind: defect
status: open
severity: MINOR
area: binding
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB333"]
spec_refs: ["8.8.4.2.2"]
inventory_rows: ["FMT-8.8.4.2.2", "FMT-8.7.5.1", "SR-8.7.5.2-2"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-23 by **REGISTRAR #13** from `leads-w53-w57.md` and the train-57 lander report (w56a lead). Re-probed on registrar #13's own Debug build of `c54434a8d` (BelowNormal).

# PB1034 — OPEN

## Measured
`scratchpad` probe `a01ifn.cob` (reproduced in PB1033): `IF A NOT >= B DISPLAY "LT"` compiles with no diagnostic
and prints `LT`.

## The rule
`cite.py --check 8.8.4.2.2 "Figure notes (relation condition Format 1 (General-relation) syntax diagram)"` → OK. The
rendered format prints `IS [ NOT ] GREATER THAN`, `>`, `LESS THAN`, `<`, `EQUAL TO`, `=` and then
`IS GREATER THAN OR EQUAL TO` / `>=` / `IS LESS THAN OR EQUAL TO` / `<=` / `<>` **without** the `[ NOT ]` bracket, and
there is no `EQUAL THAN` alternative. PB333 derived this for START and built the membership test
`ConditionBinder.InGeneralRelationFormat`; its landing section names this relation-condition half as a registrar lead.

## Decision already taken (no owner question)
The lander asked for a dialect decision. None is needed: CLAUDE.md rule 1 lets the GnuCOBOL/IBM precedence settle
implementor LATITUDE only, never a vendor extension, and vendor-dialect constructs are PARKED by owner decision 2
(`tests/version-matrix/vendor-constructs.json`). So strict mode rejects the spelling; `--permissive` is the
removed-construct migration lane and does not cover it either.

## What the fixer owes
Ask `InGeneralRelationFormat` of the WRITTEN operator in every relation-condition binding (IF, EVALUATE partial
expressions, PERFORM UNTIL, SEARCH WHEN, abbreviated combined relations) before `MapOperator` folds the spelling —
one call site, the relation binder, not one per verb. Golden: a negative per spelling at 85–2023; run the external
corpus to learn whether any corpus program uses the vendor spelling (record the count in the landing).

## Extended 2026-09-24 by REGISTRAR 15c (lane-3 batch d2-15c, pinned tree `071ff04ea`)

Registrar 15c: the relational-operators adjudication measured the same superset from §8.7.5.1's side (the complete printed operator set) and adds the reach: abbreviated combined relations (`A > B AND NOT >= C`) and the >>IF evaluator (`>>IF X NOT >= 3`, CompileTimeExpressionEvaluator's duplicated MapOperator) accept the five spellings too. ConditionBinder.InGeneralRelationFormat already encodes the right exclusion and its only caller is KeyedIoBinder (START) — the fix is one screen at the comparisonOperator node for EVERY consumer, plus a drift test.

## The rule (every citation re-run by the registrar through `cite.py --check`)

> Comparisons are defined for

`python scripts/spec/cite.py --check 8.8.4.2.1 "…"` → `OK  §8.8.4.2.1   (General)`

### Finding — `relational-operators` (under-rejects; rows `FMT-8.7.5.1`, `SR-8.7.5.2-2`)

**Mechanism.** comparisonOperator superset unscreened outside START: the one grammar rule shared by every condition admits five spellings outside §8.7.5.1 (IS NOT >=, IS NOT <=, NOT GREATER THAN OR EQUAL TO, NOT LESS THAN OR EQUAL TO, EQUAL THAN) and the only screen for them, ConditionBinder.InGeneralRelationFormat, is asked by the START KEY phrase alone (kb/Work PB333) - IF / EVALUATE / PERFORM UNTIL / SEARCH WHEN / abbreviated combined relations / >>IF accept them silently at strict mode in every edition

**Repro.** probe/relop/relneg.cob: 01 A PIC 9 VALUE 5. 01 B PIC 9 VALUE 3. ... IF A IS NOT >= B ... / IF A NOT <= B ... / IF A NOT GREATER THAN OR EQUAL TO B ... / IF A NOT LESS THAN OR EQUAL TO B ... / IF A EQUAL THAN B ...; also relabb.cob 'IF A > B AND NOT >= C' and relcce.cob '>>IF X NOT >= 3'. cobol relneg.cob --std 2023 --run (and --std 85).

**Expected from the spec.** §8.7.5.1 prints the complete operator set: Format 1 GREATER THAN, >, LESS THAN, <, EQUAL TO, =; Format 2 GREATER THAN OR EQUAL TO, >=, NOT LESS THAN, NOT <, LESS THAN OR EQUAL TO, <=, NOT GREATER THAN, NOT >, NOT EQUAL TO, NOT =, <> (IS/THAN/TO optional). §8.8.4.2.2 Format 1 brackets [NOT] on the first six alternatives only, and §8.8.4.12.4's NOTE states 'NOT cannot immediately precede an extended-relational operator'. None of the five extra spellings is a relational operator, so each program is non-conforming source: a strict-mode compile error (warning under --permissive) citing §8.7.5.1 / §8.8.4.2.2.

**Observed / reasoning.** All five compile with no diagnostic at strict --std 2023 and --std 85 and run (N1..N5 = F T F T F, i.e. MapOperator silently inverts the OR-EQUAL operator); 'A > B AND NOT >= C' prints X3 T; '>>IF X NOT >= 3' takes the ELSE branch. InGeneralRelationFormat already encodes exactly the right exclusion ('[NOT] is not printed on the OR-EQUAL four', 'EQUAL TO, never EQUAL THAN') and its doc comment names comparisonOperator a SUPERSET, but grep shows its only caller is KeyedIoBinder (START). The positive half is sound: every printed spelling evaluates correctly (relpos.cob). Root-cause fix: one screen at the comparisonOperator node for EVERY consumer (a validation pass visiting ComparisonOperatorContext, or the grammar narrowed to the printed set with the START-specific not-equal exclusion kept in KeyedIoBinder), plus a drift test; the >>IF evaluator's duplicated MapOperator (CompileTimeExpressionEvaluator) should ride the same classification.

**Code site.** src/Cobol.Net.Frontend/Grammar/Core/CobolExpressions.g4#comparisonOperator (lines 312-336: IS? NOT GTEQUAL, IS? NOT LTEQUAL, IS? EQUAL (TO|THAN)?, IS? NOT GREATER THAN? OR EQUAL TO?, IS? NOT LESS THAN? OR EQUAL TO?); src/Cobol.Net.Compiler/Binding/Procedure/Verbs/ConditionBinder.cs#InGeneralRelationFormat (sole caller KeyedIoBinder.cs:314)
