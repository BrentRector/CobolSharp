---
title: "PB1427 — NULL is an IDENTIFIER (§8.4.3.1.2 Format 8, predefined address) restricted to pointer contexts by §8.4.3.10.3 SR1, but the grammar carries NULL_ as an arm of figurativeConstant (CobolExpressions.g4:776), so NULL is accepted in a relation against an alphanumeric item and runs as LOW-VALUE"
id: PB1427
kind: defect
status: open
severity: MAJOR
area: frontend/grammar
wrong_answer: false
crashes: true
silent: true
rejects_legal_source: true
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB939", "PB933", "PB1137", "PB1136", "PB1468"]
spec_refs: ["8.4.3.1.3", "8.4.3.10.3"]
inventory_rows: ["SR-8.4.3.1.3-9", "SR-8.4.3.10.3-1", "GR-8.4.3.10.4-1", "GR-8.4.3.10.4-3", "FMT-8.4.3.10.2", "SR-8.8.4.2.3-2", "GR-8.8.4.2.1-4"]
closes_rows: []
tags: [cobolsharp, work, defect]
---
Filed 2026-09-24 by **REGISTRAR 15b** from lane-3 adjudication batch **15b** (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d2` @`071ff04ea`). Not re-probed by the registrar (MANDATORY-PRACTICES R2: every lead carries its repro and code site); origin/main since the pin carries only registrar landings (no src change), so nothing landed has touched this mechanism.

# PB1427 — OPEN

## The rule (every citation re-run by the registrar through `cite.py --check`)

**SR-8.4.3.1.3-9**

> Predefined-address NULL is defined in 8.4.3.10, NULL address pointer and message tag content. …

`python scripts/spec/cite.py --check 8.4.3.1.3 "Predefined-address NULL is defined in 8.4.3.10, NULL address pointer and message tag content."` → `OK  §8.4.3.1.3 9)  (Syntax rules)`

## Measured (rows this note claims beyond the findings above)

### `SR-8.4.3.1.3-9` — PARTIAL (adjudicator + refuter record, verbatim)

SR9 (cite.py --check 8.4.3.1.3 9) OK): Format 8 is the 8.4.3.10 predefined address NULL. Probe p1.cob: SET P TO NULL then IF P = NULL -> TRUE; pb399 golden (cites 8.4.3.10) covers NULL as a relation operand; conformance:negative/pb148-display-null pins a NULL misuse. Probes under out/probe/ident with the pinned cobol.exe. || REFUTED: PARTIAL — 8.4.3.1.2 makes NULL an IDENTIFIER (Format 8, predefined-address), and 8.4.3.10.3 SR1 restricts it to pointer contexts. The grammar carries NULL_ as an arm of figurativeConstant (CobolExpressions.g4:776) and of objectReference, so NULL behaves as a figurative literal. It is accepted in a relation against an alphanumeric item and runs as LOW-VALUE. The pointer SET/IF half does work.

## What the fixer owes

Remove NULL from figurativeConstant; bind NULL only as the predefined address (pointer / object-reference contexts); sweep PB939's VALUE NULL and PB933's INITIALIZE map.

## Folded 2026-09-24 by REGISTRAR 15c from its draft PB1427 (lane-3 batch d2-15c, pinned tree `071ff04ea`)

**Why here.** NULL carried as a figurative-constant arm instead of the identifier §8.4.3.1.2 Format 8 makes it — 15c adds the legal NULL slots it breaks (INITIALIZE REPLACING BY NULL, prototype CALL BY VALUE / BY CONTENT NULL; PB1427's draft) and the full list of illegal contexts it runs as LOW-VALUE plus the `boolean = SPACE` fail-open in the same relation checkpoint (PB1427's draft).

**The draft's title.** NULL is carried by the grammar only as an arm of `figurativeConstant` (plus two SET-specific arms), never as the identifier §8.4.3.1.2 Format 8 makes it, so the legal NULL slots that admit an identifier but not a literal refuse it or mis-carry it: `INITIALIZE P REPLACING DATA-POINTER DATA BY NULL` → COBOLNET1982, `CALL proto USING BY VALUE NULL` → COBOLNET1762, and `BY CONTENT NULL` compiles and ABORTS the run unit against a pointer formal

### The rule (every citation re-run by the registrar through `cite.py --check`)

> it may be used only as a sending operand in an INITIALIZE or a SET statement; as an argument in a program-prototype format CALL statement

`python scripts/spec/cite.py --check 8.4.3.10.3 "…"` → `OK  §8.4.3.10.3 1) a)  (Syntax rules)`

> references a data item of category data-pointer that contains the null address

`python scripts/spec/cite.py --check 8.4.3.10.4 "…"` → `OK  §8.4.3.10.4 1)  (General rules)`

### Finding — `null-address-pointer-and-message-tag-content` (rejects-legal-source; rows `SR-8.4.3.10.3-1`, `GR-8.4.3.10.4-1`, `GR-8.4.3.10.4-3`, `FMT-8.4.3.10.2`)

**Mechanism.** NULL-as-literal: the predefined address NULL is carried in the grammar only as an arm of `figurativeConstant` (and two SET-specific arms), never as the identifier §8.4.3.1.2 Format 8 makes it, so every slot that admits an IDENTIFIER but not a literal sees NULL as literal-1/literal-2 -- INITIALIZE REPLACING <pointer-category> BY NULL is rejected by the §14.9.20.3 SR3 screen, CALL <prototype> USING BY VALUE NULL is rejected by the §14.9.4.3 SR23 screen, and CALL <prototype> USING BY CONTENT NULL is bound as an alphanumeric literal carrier and aborts at run time against a pointer or program-pointer formal

**Repro.** (1) `01 P USAGE POINTER.` ... `INITIALIZE P REPLACING DATA-POINTER DATA BY NULL` -> COBOLNET1982 at 2002/2023. (2) Prototype `PROGRAM-ID. PNQ IS PROTOTYPE.` LINKAGE `01 L-P USAGE POINTER.` `PROCEDURE DIVISION USING BY VALUE L-P.`; caller REPOSITORY `PROGRAM PNQ.` `CALL PNQ USING BY VALUE NULL` -> COBOLNET1762 'literal-2 shall be a NUMERIC literal'. (3) Same with a BY REFERENCE formal and `CALL PNT USING BY CONTENT NULL`, implementation `IF L-P = NULL DISPLAY "GOT NULL" ELSE DISPLAY "GOT ADDR"` -> compiles, run aborts 'CALL argument #1: its carrier (ManagedPointer`1) cannot be read through a pointer or object-reference formal ... EC-PROGRAM-ARG-MISMATCH'; identical with a USAGE PROGRAM-POINTER formal. Probe sources: E:/Temp/claude/E--CobolSharp/a700c052-f9e4-4942-84ff-a13c7505d6ec/scratchpad/adj-d2/out/probe/null/{NULLOK.cob,PROTOV.cob,PROTOC3.cob,PROTOPP.cob}.

**Expected from the spec.** (1) compiles; P holds the null address afterwards (SR3 satisfied: NULL is identifier-2; SR4: SET P TO NULL is valid). (2) compiles (identifier-4 of class pointer, §14.9.4.3 SR22) and the callee prints GOT NULL. (3) callee prints GOT NULL for both the data-pointer and the program-pointer formal.

**Observed / reasoning.** §8.4.3.1.2 Format 8 (predefined-address) `NULL` is an identifier format (cite.py OK) and §8.4.3.10.3 SR1 a) admits it 'as a sending operand in an INITIALIZE or a SET statement; as an argument in a program-prototype format CALL statement' (OK). §8.4.3.10.4 GR1/GR3: associated with a data-/program-pointer it 'references a data item of category data-pointer/program-pointer that contains the null address' (OK). StatementValidation.CheckInitializeReplacingSetCategoryIdentifier and CallBinder.Sr23LiteralIsNumeric receive NULL through the nonNumericLiteral/figurativeConstant arm and apply the literal rule; the BY CONTENT arm renders it as a character literal carrier the runtime cannot read through a pointer formal. Sweep siblings: the same figurative arm reaches function-prototype activation arguments and INVOKE/inline-invocation arguments (both admitted by SR1 a), not probed), and the §8.4.3.7 NULL object reference under INITIALIZE REPLACING OBJECT-REFERENCE DATA BY NULL rides the same 1982 screen.

**Code site.** src/Cobol.Net.Frontend/Grammar/Core/CobolExpressions.g4#figurativeConstant (NULL_ arm); src/Cobol.Net.Compiler/Binding/Validation/StatementValidation.cs#CheckInitializeReplacingSetCategoryIdentifier; src/Cobol.Net.Compiler/Binding/Procedure/Verbs/CallBinder.cs#Sr23LiteralIsNumeric + the BY CONTENT literal arm; src/Cobol.Net.Runtime/Control/CallAbi.cs

### What the fixer owes

Give NULL ONE operand model — an identifier of class pointer (Format 8) / class object (§8.4.3.7, Format 6) — produced in one place, so every slot that admits an identifier gets it automatically and the per-statement literal screens (StatementValidation.CheckInitializeReplacingSetCategoryIdentifier, CallBinder.Sr23LiteralIsNumeric, the BY CONTENT literal carrier) stop seeing it as literal-1/literal-2. Sweep the other SR1 a) slots: function-prototype activation arguments and INVOKE / inline-invocation arguments (PB1137 owns the INVOKE argument lane for the object-reference reading), and INITIALIZE REPLACING OBJECT-REFERENCE DATA BY NULL. Land with PB1427 (the same operand model is what lets the context screen be ONE rule). Goldens: the three repros, callee printing GOT NULL for data-pointer and program-pointer formals.

## Folded 2026-09-24 by REGISTRAR 15c from its draft PB1427 (lane-3 batch d2-15c, pinned tree `071ff04ea`)

**Why here.** NULL carried as a figurative-constant arm instead of the identifier §8.4.3.1.2 Format 8 makes it — 15c adds the legal NULL slots it breaks (INITIALIZE REPLACING BY NULL, prototype CALL BY VALUE / BY CONTENT NULL; PB1427's draft) and the full list of illegal contexts it runs as LOW-VALUE plus the `boolean = SPACE` fail-open in the same relation checkpoint (PB1427's draft).

**The draft's title.** NULL binds to BoundFigurative('N'), which every generic consumer (FigurativeConstants, InspectBinder, the relation renderer, IntrinsicArgumentRules) reads as LOW-VALUE, so outside a handful of per-statement screens NULL is ACCEPTED AND RUN where §8.4.3.10.3 SR1 a) forbids it — `IF X = NULL`, `IF N = NULL`, `EVALUATE X WHEN NULL`, `INSPECT … FOR ALL NULL`, `STRING NULL …` (writes a NUL byte), `FUNCTION LENGTH(NULL)` — and the relation checkpoint also lets `boolean = SPACE` through because it counts any null-category operand as boolean-compatible

### The rule (every citation re-run by the registrar through `cite.py --check`)

> pointer-or-object-reference relation condition

`python scripts/spec/cite.py --check 8.4.3.10.3 "…"` → `OK  §8.4.3.10.3 1) a)  (Syntax rules)`

> All identifiers shall be of class alphabetic, alphanumeric, index, national, or numeric or shall be strongly-typed group items

`python scripts/spec/cite.py --check 8.8.4.2.3 "…"` → `OK  §8.8.4.2.3 2)  (Syntax rules)`

### Finding — `null-address-pointer-and-message-tag-content` (under-rejects; rows `SR-8.4.3.10.3-1`)

**Mechanism.** NULL-as-LOW-VALUE: outside the handful of per-statement screens (MOVE 0809, DISPLAY/STOP/STATUS, Format-1 CALL 0899, SET 0867, relation 0869 only when the OTHER operand is a pointer, VALUE -- PB939), the NULL figurative arm binds to BoundFigurative('N') which every generic consumer (FigurativeConstants, InspectBinder, the relation renderer, IntrinsicArgumentRules' class-any functions) reads as LOW-VALUE, so NULL is accepted and RUN in contexts §8.4.3.10.3 SR1 a) does not list

**Repro.** `01 X PIC X(4) VALUE "ABCD". 01 N PIC 9(4) VALUE 5. 01 T PIC 9(4) VALUE 0.` then each alone: `IF X = NULL DISPLAY "EQ" ELSE DISPLAY "NE" END-IF` -> runs 'NE'; `IF N = NULL ...` -> 'NE'; `EVALUATE X WHEN NULL ...` -> 'W OTHER'; `INSPECT X TALLYING T FOR ALL NULL` -> T=0000; `STRING NULL DELIMITED SIZE INTO X` -> writes a NUL byte into X; `COMPUTE N = FUNCTION LENGTH(NULL)` -> 0001. All at --std 2023 (and 2002). Probes: E:/Temp/claude/E--CobolSharp/a700c052-f9e4-4942-84ff-a13c7505d6ec/scratchpad/adj-d2/out/probe/null/{IFX,IFN,EVALX,INSP,STRG,LEN}.cob.

**Expected from the spec.** Each is a compile-time error: §8.4.3.10.3 SR1 a) confines NULL (class pointer) to an INITIALIZE/SET sending operand, a program-prototype CALL / function-prototype activation / method-invocation argument, or a pointer-or-object-reference relation condition; an alphanumeric or numeric comparand, an INSPECT or STRING operand and an intrinsic-function argument are none of these (§8.4.3.2.2 distinguishes intrinsic-function-name-1 from function-prototype-name-1).

**Observed / reasoning.** cite.py --check 8.4.3.10.3 'it may be used only as a sending operand in an INITIALIZE or a SET statement; as an argument in a program-prototype format CALL statement' -> OK; '... or in a pointer-or-object-reference relation condition' -> OK. §8.4.3.10.1 'NULL is a predefined address of class pointer or a predefined content of class message-tag' -> OK: it is not one of the §8.3.3.6.2 figurative formats. The screens are per-statement lists (one rule written in many places); the structural fix is one context screen keyed on the SR1 enumeration, applied where BoundFigurative('N') is produced or consumed. PB939 (open) owns only the VALUE-clause arm; this mechanism is otherwise unfiled.

**Code site.** src/Cobol.Net.Compiler/Binding/Procedure/ExpressionBinder.cs:318 (BoundFigurative('N')); src/Cobol.Net.Compiler/CodeGen/Roslyn/FigurativeConstants.cs:70/104-134 ('N' -> LOW-VALUE); src/Cobol.Net.Compiler/Binding/Procedure/Verbs/InspectBinder.cs:386; src/Cobol.Net.Compiler/Binding/Validation/StatementValidation.cs:1545 (relation IsNull only screens pointer-vs-other); src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:391

### Finding — `simple-relation-conditions-p1` (under-rejects; rows `SR-8.8.4.2.3-2`, `GR-8.8.4.2.1-4`)

**Mechanism.** F4 — the relation checkpoint fails OPEN on operands the class classifier returns null for: NULL takes the OTHER operand's class in CheckFormat3Relation, so NULL against a numeric/alphanumeric operand makes neither side Format-3 and nothing screens it; and BoolCompatible counts any null-category operand (every non-ZERO figurative) as boolean-compatible, so `boolean = SPACE` passes

**Repro.** 01 NI PIC 9(3) VALUE 12. 01 X1 PIC X(5). 01 B1 PIC 1(4) USAGE BIT VALUE B"1010".  IF NI = NULL DISPLAY "Y" ELSE DISPLAY "N" END-IF / IF X1 = NULL … / IF B1 = SPACE …  -> each compiles clean at 2023 and prints N (probe srel1/c_num_null, c_an_null, c_b_space).

**Expected from the spec.** §8.8.4.2.3 SR2: 'All identifiers shall be of class alphabetic, alphanumeric, index, national, or numeric or shall be strongly-typed group items' and §8.4.3.10.3 SR1 a): NULL (class pointer) 'may be used only … in a pointer-or-object-reference relation condition' — IF NI = NULL and IF X1 = NULL must be refused. B1 is class boolean, excluded from Format 1 by SR2, and in Format 2 a boolean-expression's figurative may only be ZERO or ALL boolean-literal (§8.8.2) — IF B1 = SPACE must be refused (as COMPUTE's boolean operand already is, negative pb1029-boolean-figurative-space).

**Observed / reasoning.** No diagnostic; each relation is silently FALSE. The fail-open is deliberate for error-recovery nulls (PB960) but a figurative SPACE and NULL are not recovery profiles — they are classifiable operands the classifier returns null for.

**Code site.** src/Cobol.Net.Compiler/Binding/Validation/StatementValidation.cs#CheckFormat3Relation (lc/rc for IsNull take the other side's class; early return when neither is Format-3); src/Cobol.Net.Compiler/Binding/Validation/StatementValidation.cs#CheckRelationalOperands (BoolCompatible: OperandCategory is Boolean or null)

### What the fixer owes

One context screen keyed on the §8.4.3.10.3 SR1 a) enumeration, applied where the NULL operand is produced or consumed — never a seventh per-statement list (the existing screens MOVE 0809, DISPLAY/STOP/STATUS, Format-1 CALL 0899, SET 0867, relation 0869, VALUE PB939 collapse into it). In CheckFormat3Relation, NULL must not borrow the OTHER operand's class; in CheckRelationalOperands, BoolCompatible must not treat a classifiable figurative (SPACE, QUOTE, HIGH-VALUE …) as boolean — the PB960 fail-open is for error-recovery nulls only. Negative goldens for each repro (IF X/N = NULL, EVALUATE WHEN NULL, INSPECT, STRING, LENGTH(NULL), IF B1 = SPACE).
