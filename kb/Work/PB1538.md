---
title: "PB1538 — the >>DISPLAY directive transfers NOTHING: with no source listing §7.3.12.4 GR2 leaves the result to the implementor, and CLAUDE.md rule 1 settles that latitude by GnuCOBOL 3.2, which writes the operands to the compiler's standard output at compile time; COBOL.NET consumes the line silently"
id: PB1538
kind: defect
status: open
severity: MINOR
area: frontend/directives
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB807", "PB1369", "PB1533"]
spec_refs: ["7.3.12.4"]
inventory_rows: ["DOC-A.1-53", "DOC-A.1-55"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 23:55 UTC by the **doc-rows-1 INTEGRATOR** (cloud, branch `claude/adj-doc-rows-1`, kb/Work PB1522 step 3) from the §7 determination writers and refuters (`adjudication/doc-rows-1/`). Every citation below was re-run through `python scripts/spec/cite.py --check`, and the repro was re-probed on the Debug build of this branch.

# PB1538 — OPEN

## The rule

> 2) If the compiler does not produce a source listing, the result of the DISPLAY directive is defined by the implementor, otherwise the data transfer shall take place irrespective of whether it is suppressed by the LISTING directive.

`cite.py --check 7.3.12.4 "If the compiler does not produce a source listing, the result of the DISPLAY directive is defined by the implementor"` → OK §7.3.12.4 2).
A.1 item 53 (conversion of the data transferred) and item 55 (how a PARAMETER value is obtained from the operating environment)
are REQUIRED and documented; neither can be written while the directive transfers nothing.

## Precedent (CLAUDE.md rule 1: ISO silent → GnuCOBOL)

GnuCOBOL 3.2 `cobc/pplex.l` `display_finish` (l.2711-2721) `puts()` the collected operands to the compiler's standard output at
compile time (collection l.242-248, 721-734; testsuite `syn_misc.at` 6797-6829: `>>DISPLAY "OK"` prints `OK` when compiling).

## Repro (`/tmp/adj/doc/doc-w1-s2/refute-probe/r53.cob`; re-probed, `--std 2023`)

```cobol
      >>DISPLAY "HELLO-COMPILE" 42
      >>DISPLAY PARAMETER XYZ
      >>DISPLAY "DEV" UPON LISTING
```

Observed: the compile prints nothing (the run prints only the program's own `RUN`). Expected (precedent): `HELLO-COMPILE 42` (and
the other operands) on the compiler's standard output. The directive is consumed by `DirectiveStateRegistry` with no transfer
("NONE HELD").

## To close

Transfer the operands to the compiler's standard output (a diagnostic-sink "note" line is the natural carrier), decide the PARAMETER
lookup with kb/Work PB1533 (the >>DEFINE PARAMETER environment lookup), then write DOC-A.1-53 and DOC-A.1-55 (their row text is
PB1369's deliverable) and remove the ⚠ from CONFORMANCE.md §3's "Recognized-and-ignored compiler directives" bullet. The operand
syntax screen is PB807's.
