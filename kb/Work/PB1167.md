---
title: "PB1167 — an ANY LENGTH returning item of a contained program or a method is refused at DECLARATION (COBOLNET0899 'recognized (ISO §13.18.2.3 SR3b) but not yet implemented' — 'the ANY-LENGTH-RETURNING wave', which no register note owned), so §14.8.3.3 rule 5's length relaxation and rule 4's conforming pairing are unreachable and the comparator's written anyLengthFormal arm and OoEmitter's width-fit delivery are dead code"
id: PB1167
kind: defect
status: open
severity: MINOR
area: binder/data-linkage
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1164", "PB1166", "PB1210", "PB165"]
spec_refs: ["14.8.3.3", "13.18.2.3", "13.18.2.4"]
inventory_rows: ["GR-14.8.3.3-L3.4", "GR-14.8.3.3-L3.5", "SR-13.18.2.3-3", "GR-13.18.2.4-1", "GR-8.5.4-3"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14c** from lane-3 adjudication batch **d1-14c** (adjudicated on the pinned tree `68bbdb00f`; trains 58/59 checked — nothing that landed since touches this mechanism unless the body says otherwise).

# PB1167 — OPEN

## The rule (every citation re-run by the registrar through `cite.py --check`)

> 4) If the receiving operand is described with the ANY LENGTH clause, the sending operand shall also be described with the ANY LENGTH clause.

`python scripts/spec/cite.py --check 14.8.3.3 "…"` → `OK  §14.8.3.3 4)  (Elementary items)`

## Finding — `returning-items` (rejects-legal-source; rows `GR-14.8.3.3-L3.5`, `GR-14.8.3.3-L3.4`)

**Mechanism.** An ANY LENGTH returning item (contained program or method) is refused at declaration (COBOLNET0899, 'the ANY-LENGTH-RETURNING wave'), so rule 5's relaxation and rule 4's conforming pairing are unreachable; no register note owns the wave

**Repro.** Class RIC7 method GETV 'LINKAGE SECTION. 01 R PIC X ANY LENGTH. PROCEDURE DIVISION RETURNING R.' 'DISPLAY "MLEN=" FUNCTION LENGTH(R)' 'MOVE "ABCDEFGH" TO R'; main 'INVOKE O "GETV" RETURNING R5' with R5 PIC X(5); and the same as a nested program NR7 called 'CALL "NR7" AS NESTED RETURNING C5' (p7.cob).

**Expected from the spec.** §13.18.2.3 SR3 b) permits ANY LENGTH on a contained program's or method's returning item; §14.8.3.3 rule 5 makes its length match the receiver's; §13.18.2.4 GR1 b) gives n = 5. Output MLEN=5, [ABCDE], NLEN=5, [ABCDE].

**Observed / reasoning.** COBOLNET0899 on both declarations ('recognized (ISO §13.18.2.3 SR3b) but not yet implemented'), then COBOLNET0828/1736 'length mismatch (formal X(1), argument X(5))'. The comparator's anyLengthFormal relaxation and OoEmitter's width-fit delivery are written but dead. Rule 4's prohibition (ANY LENGTH receiver, fixed sender) IS enforced (p8.cob -> COBOLNET1736 citing rule 4).

**Code site.** src/Cobol.Net.Compiler/Binding/DataBinder.Linkage.cs#AnyLengthValidateUnit; src/Cobol.Net.Compiler/Binding/DataBinder.Oo.cs#OoBindMethodData

## What the fixer owes

Lift the COBOLNET0899 staging in DataBinder.Linkage.AnyLengthValidateUnit / DataBinder.Oo.OoBindMethodData for a contained program's or method's RETURNING item (§13.18.2.3 SR3 b)), give the returning item its length from the receiver (§13.18.2.4 GR1 b)), and wire the existing comparator relaxation and OoEmitter width-fit. Keep rule 4's prohibition (p8.cob, already enforced). Golden p7.cob: MLEN=5 [ABCDE] NLEN=5 [ABCDE].

## Extended 2026-09-24 by REGISTRAR 14d (folded from PB1211 — the same mechanism)

Registrar 14c's PB1167 already owns the COBOLNET0899 'ANY-LENGTH-RETURNING wave' staging of an ANY LENGTH returning item; PB1211 measured the same stage from the §13.18.2.3 SR3 b) / §13.18.2.4 GR1 side.

PB1211 as filed: PB1211 — ANY LENGTH on a PROCEDURE DIVISION RETURNING item is staged loud COBOLNET0899 ('the ANY-LENGTH-RETURNING wave'), so the legal §13.18.2.3 SR3 b) leg and the returning-item half of §13.18.2.4 GR1 are unimplemented

Filed 2026-09-24 by **REGISTRAR 14d** from adjudication batch d1-14d (the adjudicators' pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probes under `scratchpad\adj-d1\out13\probe\`). The given repro was re-run once on registrar 14d's Debug build of `071ff04ea` (main after trains 58/59) and reproduces unless stated otherwise.

# PB1211 — OPEN

## Measured

**any-length-clause** — ANY LENGTH on a PROCEDURE DIVISION RETURNING item (contained program and method) is staged loud as RecognizedNotImplemented COBOLNET0899, so the legal 13.18.2.3 SR3b leg and the returning-item half of 13.18.2.4 GR1 are unimplemented

- **Repro:** p2.cob: outer program `01 A3 PIC XXX VALUE "XYZ". CALL "ALP2SUB" RETURNING A3. DISPLAY "A3=" A3.`; contained ALP2SUB `LINKAGE SECTION. 01 L PIC X ANY LENGTH. PROCEDURE DIVISION RETURNING L. MOVE "ABCDEF" TO L. EXIT PROGRAM.` -> COBOLNET0899 'ANY LENGTH on the PROCEDURE DIVISION RETURNING item is recognized ... but not yet implemented'. Method arm: p8.cob METHOD-ID RET with `PROCEDURE DIVISION RETURNING R`, R PIC X ANY LENGTH -> same 0899.
- **Expected (spec):** 13.18.2.3 SR3 b) permits the returning item for a contained program or method; 13.18.2.4 GR1 b) makes L n=3 repetitions of X (the length of the activator's returning item A3), so MOVE "ABCDEF" TO L stores ABC and the caller prints A3=ABC; GR1 a) makes L zero-length when the activator's returning item is zero-length. Only an INLINE method invocation forbids an ANY LENGTH returning item (8.4.3.4.3 SR4).
- **Observed:** DataBinder.Linkage.cs AnyLengthValidateUnit (`else if (returning && !formal) Edition.Error(DiagnosticCatalog.AnyLengthReturning, ...)`) and DataBinder.Oo.cs OoBindMethodData (same for m.Binding.Returning). The catalog marks it RecognizedNotImplemented ('the ANY-LENGTH-RETURNING wave'); no open kb/Work note owns it - a new defect note is needed. The return crossing must carry the activator's receiving-item length into the callee (CallAbi width = -1 mode for the returning slot), and the inline-invocation SR4 rejection (COBOLNET2140) must remain.
- **Code site:** src/Cobol.Net.Compiler/Binding/DataBinder.Linkage.cs#AnyLengthValidateUnit; src/Cobol.Net.Compiler/Binding/DataBinder.Oo.cs#OoBindMethodData; src/Cobol.Net.Runtime/Control/CallAbi.cs
- **Clause (adjudicator):** 13.18.2.3 SR3 b); 13.18.2.4 GR1

## The rules — every citation re-run through `cite.py --check` by registrar 14d

- `SR-13.18.2.3-3`: `cite.py --check 13.18.2.3 "If the source element containing the ANY LENGTH clause is a contained program or is a"` → OK  §13.18.2.3 3)  (Syntax rules)
- `GR-13.18.2.4-1`: `cite.py --check 13.18.2.4 "The ANY LENGTH clause specifies that the subject of the entry shall be"` → OK  §13.18.2.4 1)  (General rule)

## Rows claimed

- `SR-13.18.2.3-3` — batch 14d verdict DIVERGES
- `GR-13.18.2.4-1` — batch 14d verdict PARTIAL

## What the fixer owes (sibling sweep)

Both activation channels: a contained/separate program's RETURNING and a method's RETURNING (DataBinder.Oo.cs#OoBindMethodData); the CallAbi return path must carry the returning item's run-time length.

## Extended by REGISTRAR 15b (batch 15b, pin @`071ff04ea`) — rows `GR-8.5.4-3`

§8.5.4 item 3's zero-length RETURNING-item half is unreachable because the ANY LENGTH returning item is refused at declaration; the argument half conforms

**GR-8.5.4-3**

> A data item defined with the ANY LENGTH clause corresponding to an argument or …

`python scripts/spec/cite.py --check 8.5.4 "A data item defined with the ANY LENGTH clause corresponding to an argument or"` → `OK  §8.5.4 3)  (Zero-length items)`

#### `GR-8.5.4-3` — PARTIAL (adjudicator + refuter record, verbatim)

Stem: `OK  §8.5.4` "A zero-length item is a data item or a literal whose minimum length is zero and whose length at runtime is zero." The compiler holds the stem's description half as ONE recurrence (DataItem.MinimumLengthIsZero) and the runtime half as the emitted current-length test; consumers read it (MoveClassifier.ZeroLengthItemRoute / MoveEmitter for §14.9.25.4 GR1, CobolClass for §8.8.4.4.4 GR1, STRING/UNSTRING/INSPECT runtime length). Item 3: MinimumLengthIsZero answers true for IsAnyLength and ZeroLengthItemRoute routes an ANY LENGTH elementary sender through its runtime length test. Probe p3: contained program with `LINKAGE 01 P PIC X ANY LENGTH`, CALL ... USING BY REFERENCE D (DYNAMIC LENGTH, current length 0) -> in the callee FUNCTION LENGTH(P)=0, IS ALPHABETIC false, MOVE P TO X(4)=spaces, INSPECT tally 0; control CALL USING S ("XYZ") -> L=3, ALPHABETIC true, [XYZ ]. No existing spec-derived golden passes a zero-length argument to an ANY LENGTH item (unit Items3And4 pins only the description half) -> test-needed. || REFUTED: PARTIAL — Item 3 has two halves: an ANY LENGTH item corresponding to a zero-length ARGUMENT or to a zero-length RETURNING ITEM (OK §8.5.4 3); §13.18.2.3 SR3b, OK, makes the returning item a legal ANY LENGTH subject in a contained program/method). The adjudicator verified only the argument half. The returning half is refused outright: a contained program with `LINKAGE 01 P PIC X ANY LENGTH` + `PROCEDURE DIVISION RETURNING P` is rejected with COBOLNET0899 'ANY LENGTH on the PROCEDURE DIVISION RETURNING item is recognized ... but not yet implemented (the ANY-LENGTH-RETURNING wave)'. One half verified, the other not implemented => PARTIAL. No kb/Work note found owning the ANY-LENGTH-RETURNING stage (grep of kb/Work for ANY-LENGTH-RETURNING found none) -- the registrar should file one (rejects legal source).
