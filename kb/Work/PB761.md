---
title: "PB761 — the COBOLNET0899 `NationalThroughRange` stage declines legal source on a citation that does not carry the obligation: `DataBinder.BindCondition` (`:2593-2597`) and `DiagnosticCatalog.NationalThroughRange` (`:654-657`) both cite §13.18.63 SR31, but SR31 governs the `IN alphabet-name-1` phrase — WHEN it may be written and WHICH KIND of alphabet it shall reference — and never makes a national THROUGH range unimplementable; SR29 bans THROUGH for a boolean subject ONLY, and SR32 delegates the range to §14.7.8, so `88 X VALUE N\"A\" THROUGH N\"C\"` on a national conditional variable is conforming source the compiler refuses. And the stage looks dischargeable: `__COLLATE_NAT` has been live since PB678 and `ConditionRenderer` already renders the national arm at `:72`"
id: PB761
kind: analysis
status: open
severity: MINOR
area: binding/data-division
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.18.63.3", "14.7.8"]
inventory_rows: []
tags: [cobolsharp, work, analysis, diagnostics, national, condition-name]
---

# PB761 — NATIONAL THROUGH — a decline whose citation answers a different question

Found 2026-09-06 in the PB728 implementer's report (`New defects (b)`), filed as **analysis** rather than a
straight defect because it carries two claims that resolve differently: the citation is misfiled (a fact,
verified below) and the stage is dischargeable (a strong reading that needs one build to confirm).
Verified against main `3d75a747`.

## ⛔ Harm flag — a deliberate departure from the filing brief, with the reason

The brief allocated this note `process-only`. **It is not process-only, and the flag is set from what was
verified.** §13.18.63.3 SR29 bans the THROUGH phrase for a **boolean** subject and for no other category; SR31
and SR32 both discuss national THROUGH literals as an ordinary case; so a national THROUGH range is **conforming
source**, and `Edition.Error(...)` at `DataBinder.cs:2594` refuses to compile it. That is
`rejects_legal_source: true` by the register's own definition, and marking it process-only would keep it out of
`work.py next` — the exact disappearance `feedback_measure_the_selectors_complement` records as having hidden
nine harmful items for months. The severity label stays **MINOR** (the construct is niche and the refusal is
LOUD — no wrong answer can escape it); the register ranks on what the defect DOES, not on the label, and this
one stops a conforming program from compiling.

## The two sites, verbatim on main

`src/Cobol.Net.Editions/Diagnostics/DiagnosticCatalog.cs:654-657`:

```csharp
public static readonly DiagnosticDescriptor NationalThroughRange = new(
    NotImplemented, "national-through-range", EditionSeverity.Error,
    "A condition-name THROUGH range over a national conditional variable is not yet implemented.",
    "ISO §13.18.63 SR31", RecognizedNotImplemented);
```

`src/Cobol.Net.Compiler/Binding/DataBinder.cs:2587-2597`, inside `BindCondition`:

```csharp
// §13.18.63 SR29: THROUGH shall not be specified for a boolean conditional
// variable (0898). A national THROUGH range is spec-legal but orders under a
// NATIONAL alphabet (SR31) — recognized, staged (0899).
if (parent.Pic is { Category: PicCategory.Boolean })
    Edition.Error("COBOLNET0898", …);
else if (parent.Pic is { Category: PicCategory.National })
    Edition.Error(DiagnosticCatalog.NationalThroughRange, $"condition-name '{name}': a THROUGH range over "
        + "a national conditional variable (ordered by the national collating "
        + "sequence) is recognized but not yet implemented (Phase 4a residue) — "
        + "(ISO §13.18.63 SR31)");
```

⭐ **The code comment already concedes the point**: *"A national THROUGH range is **spec-legal**"*. The decline
is an implementation-capacity statement wearing a syntax rule's citation.

## ⛔ Two premise corrections against main, both of which the report could not have known

1. **The descriptor does NOT yet name the GROUP shape.** PB728's report states it extended
   `NationalThroughRange`'s description to cover a `GROUP-USAGE NATIONAL` subject as the honest interim. On main
   the description is still the plain one-liner quoted above. **PB728's landing is not on main** — measured:
   `tests/conformance/2023/` has no `pb728_*` golden, `ConditionNameSubjectClassDriftTests.cs` and
   `CollatingComparisonClassDriftTests.cs` do not exist, the semgrep rule
   `cobolnet-condition-category-from-raw-picture` is not in `scripts/semgrep/`, and `kb/Work/PB728.md`,
   `PB741.md` and `PB575.md` all still read `status: open`. The interim is **in flight, not landed**.
2. **Both guards read `parent.Pic`, not `OperandPic`** (`:2590`, `:2593`) — so today the stage does not even
   fire on the group shape, because `Pic` is null for every group (`DataItem.cs:88`). That is **[[PB575]]**'s
   half of PB728, also in flight. Whatever this note becomes, it must be applied to the ONE reader, or it
   re-creates the asymmetry PB728 is landing to remove.

## What the standard prints — every clause re-derived with `cite.py --check`, none inherited

- `--check 13.18.63.3 "Alphabet-name-1 may be specified only when the literals specified in the THROUGH phrase
  are of class alphanumeric or national"` → **OK §13.18.63.3 31)**. The full rule reads:

  > 31) Alphabet-name-1 may be specified only when the literals specified in the THROUGH phrase are of class
  > alphanumeric or national. If literal-2 and literal-3 or literal-5 and literal-6 are of class national,
  > alphabet-name-1 shall reference an alphabet that defines a national collating sequence; otherwise,
  > alphabet-name-1 shall reference an alphabet that defines an alphanumeric collating sequence.

  ⛔ **This is a rule about the `IN alphabet-name-1` PHRASE** — when it may be written, and which kind of
  alphabet it must name. It says nothing about implementability, and it is not even reachable in the probe that
  triggers the diagnostic (`nat88c.cob` writes no `IN` phrase at all). It is a real clause answering a different
  question — `feedback_a_real_clause_can_answer_a_different_question` exactly, and the reason it survived is
  that `--check` PASSES on it: the text is genuinely there. **A citation that verifies is not thereby the right
  citation.**
  ⭐ Note the inversion: SR31 is the strongest evidence AGAINST the decline it is cited for. It contemplates
  national THROUGH literals as an ordinary, supported case and legislates the alphabet they order under.
- `--check 13.18.63.3 "If the category of the subject of the entry is boolean, the THROUGH phrase shall not be
  specified"` → **OK §13.18.63.3 29)**. **The only categorical THROUGH prohibition in the clause, and it names
  boolean alone** — which is what makes the national form legal source rather than merely undiscussed.
- `--check 13.18.63.3 "The manner of determining the range of values specified by the THROUGH phrase is
  described in 14.7.8"` → **OK §13.18.63.3 32)**. **This is the rule the stage actually stands in front of**:
  the obligation the compiler is declining to meet is §14.7.8's range determination for a national subject, and
  SR32 is the pointer to it. If the stage survives, it is SR32 + §14.7.8 that it should cite — never SR31.

## Is it dischargeable? The evidence, and the one thing that must still be built

**For:**

- `__COLLATE_NAT` and the `NationalCollation` machinery have been live since **[[PB678]]**.
- `ConditionRenderer.cs:72` already renders the national arm of a THROUGH range —
  `IsNationalOperand(n.Left) || IsNationalOperand(n.Lo) ? ctx.NatCollateArg : ctx.CollateArg` — routed through
  `RuntimeApi.ThruMember` (`:73-74`), which is §14.7.8's inverted-range / EC-RANGE-INVALID behaviour. The
  renderer is waiting for source the binder refuses to hand it.
- PB728's report reached the same conclusion independently from its own build.

**Against, or at least still open:**

- ⛔ The report's own reservation was *"discharging it properly pulls in SR31's `IN alphabet-name-1` phrase,
  whose grammar support I did not measure."* **That reservation is now answered, and the answer is [[PB555]]**
  (open): the grammar **does** parse the phrase (`CobolData.g4:621-624`), and `Condition88`
  (`Condition88.cs:11-25`) does not MODEL it — its whole state is `Name`, `Parent` and
  `List<(string Low, string? High)> Values` — so `BindCondition` **discards** alphabet-name-1 and literal-4 and
  every downstream consumer is structurally unable to see them. So it is a **model** gap, not a grammar gap, and
  PB555 owns it together with SR27's literal-4 screen as ONE restructuring.
- ⭐ **Therefore the discharge SPLITS cleanly, and does not need PB555 to start.** A national THROUGH range with
  **no** `IN` phrase orders under the national PROGRAM collating sequence, which `ctx.NatCollateArg` already
  supplies — that half can be discharged on its own. The `IN alphabet-name-1` half arrives with PB555's
  restructuring and not before; declining *that* form is what a staged diagnostic is legitimately for, and it
  would cite SR31 **correctly**, for the first time.

## The fix shape

**Two admissible outcomes, and the note is filed as analysis precisely because one build decides between them.**

1. ⭐ **Discharge the no-alphabet half.** Delete the `else if (parent.Pic is { Category: National })` arm, ship
   the golden below, and let the existing renderer path carry it. `RecognizedNotImplemented` stages are debt by
   CLAUDE.md rule 3 and `feedback_no_deferral_default`; this one's cost is a conforming program that will not
   compile, and its stated reason ("Phase 4a residue") is four phases stale.
2. **If a build shows the renderer path is not in fact reachable for a level-88 national subject**, keep the
   stage and **re-cite it honestly** — §13.18.63.3 SR32 → §14.7.8 for the obligation, narrowed in scope to the
   `IN alphabet-name-1` form and cross-referenced to [[PB555]] as the blocker. ⛔ Do not leave SR31 on it either
   way: a wrong citation on a LOUD diagnostic is a wrong citation a **user** reads.

**In both outcomes** the guard moves from `parent.Pic` to `parent.OperandPic` so the group and elementary shapes
agree ([[PB575]]/[[PB728]]), and the diagnostic's `spec_ref` string is corrected in
`DiagnosticCatalog.cs` with `docs/DIAGNOSTICS.md` **regenerated** via `pwsh scripts/gen-diagnostics-doc.ps1`
rather than hand-edited.

## Goldens

⛔ **PB728's implementer deliberately declined to pin the 0899 stage as a negative golden** — *"a green test on a
'recognized but not implemented' decline reads as a decision and holds the gap open"*
(`feedback_green_test_can_hold_a_gap_open`). That judgment stands and this note does not overturn it. So:

- **Outcome 1** ships a POSITIVE 2023 conformance case: a national conditional variable, a
  `VALUE N"A" THROUGH N"C"` range, a member and a non-member, under an explicit
  `ALPHABET NAT-D FOR NATIONAL` in `PROGRAM COLLATING SEQUENCE` so the national sequence is demonstrably the one
  used — expected values derived from §14.7.8 rule 2 and §8.8.4.2.9/.10, with the alphabet chosen so the native
  UTF-16 ordering gives the OPPOSITE answer (otherwise the leg passes for the wrong reason). Plus the
  elementary/group pair, which must agree.
- **Outcome 2** ships the narrowed negative only for the `IN alphabet-name-1` form, whose decline is then a true
  statement about PB555.

## Sweep

- **The sibling arm is correct and stays.** `COBOLNET0898` at `:2591` cites §13.18.63 SR29 and SR29 is exactly
  the rule it enforces — the two-arm dispatch here has one right arm and one wrong one
  (`feedback_two_arm_dispatch`); the wrong one is the national arm, and only it.
- **Rows deliberately NOT claimed** (`inventory_rows: []`), because they are already owned and a second claimant
  makes a half-covered row read as done: `SR-13.18.63.3-31` (DIVERGES) is claimed by [[PB498]], [[PB503]] and
  [[PB555]]; `SR-13.18.63.3-32` (PARTIAL) by [[PB555]]. `SR-13.18.63.3-29` is PARTIAL and `SR-13.18.63.3-26`
  NOT-IMPLEMENTED, both code-located at `DataBinder.cs#BindCondition` — the same method — so whoever discharges
  this stage should re-record them in the same batch.
- **Landing order.** [[PB728]] + [[PB741]] first (they are in flight and rethread the very site at
  `ConditionRenderer.cs:72` this discharge depends on, and PB728's interim makes the group and elementary shapes
  agree before either is discharged), then this note's half, then [[PB555]] for the alphabet phrase. Discharging
  before PB728 lands would discharge the ELEMENTARY leg while the group leg silently compiled to the wrong
  collation — a worse asymmetry than the one being fixed, which is the same argument PB728 makes for landing
  PB575 with it.
- **Not this note:** [[PB552]] is the *reversed* range (SR26) at the same site and is an UNDER-rejection; it
  records the national leg as "unreachable meanwhile (a national THROUGH range is staged COBOLNET0899)" — i.e.
  PB552's national coverage is BLOCKED BY this stage, which is one more reason to discharge it.
