---
title: "PB1509 — the report SUM accumulation is emitted through a C# `(long)` cast — not the native Int128 the ReportWriterEmitter comment claims and not SDIDI under ARITHMETIC IS STANDARD-DECIMAL — so a 20-digit addend wraps modulo 2^64 into a wrong printed total with no size error, breaking the SUM arm of §11.9.5.2 GR1 and GR3"
id: PB1509
kind: defect
status: open
severity: MAJOR
area: codegen/report
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1271", "PB1295"]
spec_refs: ["11.9.5.2", "13.18.54.4", "8.8.1.3"]
inventory_rows: ["GR-11.9.5.2-1", "GR-11.9.5.2-3"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 15d** from lane-3 adjudication batch **15d** (adjudicated on the pinned tree `071ff04ea`; everything that landed since — Registrar 14a/14b/14c — is register-only, so no code change touches this mechanism). Every citation below was re-run by the registrar through `cite.py --check`.

# PB1509 — OPEN

## The rule (every citation re-run through `cite.py --check`)

> 1) If the NATIVE phrase is specified, the techniques used in handling arithmetic expressions and intrinsic functions shall be those specified by the implementor …

`OK  §11.9.5.2 1)  (General rules)`

> 3) If the STANDARD-DECIMAL phrase is specified, the techniques used in handling arithmetic expressions, arithmetic statements, the SUM clause, and integer and numeric functions shall be as described for standard-decimal arithmetic …

`OK  §11.9.5.2 3)  (General rules)`

> [GR3: each accumulation is consistent with ADD … ON SIZE ERROR]

`OK  §13.18.54.4 3)  (General rules) — checked on the fragment `ON SIZE ERROR``


## Row `GR-11.9.5.2-1` — PARTIAL (editions 2002, 2014, 2023 (the NATIVE phrase exists from 2002))

Rule (cite.py --check 11.9.5.2 "If the NATIVE phrase is specified, the techniques used in handling arithmetic expressions and intrinsic functions shall be those specified by the implementor" -> OK §11.9.5.2 1)): NATIVE selects implementor techniques for expressions/functions and §8.8.1.3 native arithmetic for statements and SUM (cite --check 8.8.1.3 "Native arithmetic is in effect when the ARITHMETIC IS NATIVE clause is specified in the OPTIONS paragraph" -> OK). Code: ArithmeticOf maps NATIVE -> ArithmeticMode.Native, the same value as the absent-clause default; IsDecimalEngine(Native)=false keeps the scaled-Int128 / binary64-float native lane documented as DOC-A.1-123. Probed: explicit NATIVE is byte-identical to no clause (p_native vs p_none, SQRT(2) = binary64 value); in a nested probe (probe/arithmetic-clause/p_nest.cob) a contained program writing ARITHMETIC IS NATIVE under a STANDARD-DECIMAL container runs native (IN2 = ...145474621858738), and ITS contained program inherits native (IN3) - §11.9.4 GR1 override. ADD whose composite spans 38 digits is refused under NATIVE (COBOLNET0805, §14.7.7 2) a) native-only cap, cite --check OK) and accepted under STANDARD-DECIMAL - the mode really selects the statement technique. Per-function native defects (kb/Work PB621 SUM/RANGE/MEAN scale, PB1041 SIN/COS/TAN) belong to their own §15 rows, not this dispatch rule. Test: conformance:2023/l1_native_arithmetic_intermediate writes ARITHMETIC IS NATIVE explicitly and asserts the §14.7.7 2) a) 31-digit boundary identities (MUL31, MULSC, ADD31) - spec-computed values. || REFUTED: PARTIAL — GR1 makes NATIVE govern four things: expressions, intrinsic functions, arithmetic statements and the SUM clause, the last two by the documented §8.8.1.3 technique. The expression, statement and function arms hold. The SUM arm does not follow the documented technique. DOC-A.1-123 documents the native intermediate as a scaled Int128, but the report SUM accumulation is emitted as a C# (long) cast. A 20-digit addend therefore wraps modulo 2^64 into a wrong printed total, with no size error. That contradicts §13.18.54.4 GR3, under which each accumulation is consistent with ADD ... ON SIZE ERROR, and the 20-digit counter holds the true value, so no size error is owed. The adjudicator never probed the SUM arm of GR1.

## Row `GR-11.9.5.2-3` — PARTIAL (editions 2014, 2023 (STANDARD-DECIMAL phrase introduced 2014))

Rule (cite --check 11.9.5.2 "If the STANDARD-DECIMAL phrase is specified, the techniques used in handling arithmetic expressions, arithmetic statements, the SUM clause, and integer and numeric functions shall be as described for standard-decimal arithmetic" -> OK §11.9.5.2 3); the technique itself is §8.8.1.5.1, cite --find confirms 'Standard-decimal arithmetic is a method of evaluating ...' is in §8.8.1.5.1). Code: ArithmeticOf -> StandardDecimal; IsDecimalEngine(StandardDecimal)=true is the ONE switch NumericRenderer.StandardDecimal reads, so expressions, statements and intrinsic functions route to the CobolDec SDIDI engine. Verified per arm with the pinned compiler: EXPRESSION - COMPUTE R = FUNCTION SQRT(2) gives the 34-digit value 1.414213562373095048801688724209 (native gives the binary64 value); STATEMENT - probe/arithmetic-clause/p_add_STA.cob: A PIC 9(20) VALUE 12345678901234567890, B PIC V9(18) VALUE .999999999999999999, ADD A B GIVING R (R PIC 9(20)V9(10)) and ADD B TO R both give 12345678901234567891.0000000000 = the exact 38-digit sum rounded to 34 digits (default INTERMEDIATE ROUNDING NEAREST-AWAY-FROM-ZERO) then truncated - spec-derived, and native refuses the same statement (0805); FUNCTIONS - SQRT above (the §15.84.4 34-digit rule); INHERITANCE - p_nest.cob contained program with no clause inherits STANDARD-DECIMAL (§11.9.4 GR1). SUM CLAUSE - not routed; ReportWriterEmitter documents (and I re-derived) that a SUM accumulation is one aligned addition of fixed-point values (§13.18.54.3 addends are never float) whose exact result fits both Int128 and a 34-digit SDIDI, so the engines are digit-identical for every reachable SUM. Per-function SDIDI defects are owned by their own notes/rows (kb/Work PB1041 SIN/COS/TAN narrowing, PB624 NumvalF arm, PB228 ADD-under-SD golden gap for §14.9.2.4). Test-ref left empty: no single spec-derived test covers the dispatch across its arms - conformance:2014/pb116_sqrt_standard_decimal covers functions, conformance:2014/arithmetic_standard_decimal covers COMPUTE but its header cites '8.8.1.4' (standard-BINARY) for standard-decimal and 'COBOLNET0804 below 2014' (the gate is 0900), so it is not a clean spec-derived closer. || REFUTED: PARTIAL — The rule has four arms (expressions, statements, the SUM clause, integer/numeric functions) and the SUM arm is not standard-decimal: under ARITHMETIC IS STANDARD-DECIMAL the report SUM counter accumulates through a C# (long) cast, not SDIDI and not the Int128 the ReportWriterEmitter comment claims, so a 20-digit addend wraps modulo 2^64. SDIDI (34 digits) represents …

## What the fixer owes

Route the SUM accumulation through the SAME arithmetic lane an `ADD addend TO counter ON SIZE ERROR` statement uses under the unit's ARITHMETIC mode (native: the documented DOC-A.1-123 Int128 intermediate; standard-decimal: SDIDI), never a narrowing cast; the counter-scale alignment can span 38 digits, which the comment's premise ignores. Correct the emitter comment. Golden: a 20-digit SUM under NATIVE and under STANDARD-DECIMAL, plus a counter-overflow leg showing the size-error behaviour of §13.18.54.4 GR3. Sweep every `(long)` in ReportWriterEmitter's numeric paths (PB1271 owns the repeating-counter shape — same file).
