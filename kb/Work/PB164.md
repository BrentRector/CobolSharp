---
title: "PB164 — the group-image predicate excludes leaf kinds whose byte forms are already pinned or pinnable: COMP-5/BINARY-CHAR..DOUBLE leaves have ByteForm.Binary and StorageWidth yet IsImageCapable rejects them (a group containing one cannot cross CALL, MOVE as a group, or REDEFINES-window — loud on conforming source); FLOAT leaves need an IEEE byte-form pin; USAGE INDEX in a crossing group is a documented-loud posture needing an owner decision"
id: PB164
kind: defect
status: open
severity: MAJOR
area: codegen
wrong_answer: false
crashes: true
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.18.60.4", "14.8.2.2", "15.50.4"]
tags: [cobolsharp, work, defect]
---

# PB164

Re-homed from PB133's mechanism (7): `CallEmitter.ArgText` loud-stages a CALL argument group whose leaves
are not `IsImageCapable`, and `OperandText`/`PlaceRenderer`/`RecordStructEmitter` hold the same Tier-C
island posture for MOVE/REDEFINES/records — conforming source throws at RUN time.

**The COMP-5 half is pure predicate drift**: `PicInfo.ByteForm`'s own doc (written after V59) says COMP-5
and BINARY-CHAR..DOUBLE "are excluded from the IMAGE by their BinaryCapacity discipline … never by their
representation: stating the form here is what makes admitting them a WIDTH question, not a representation
question" — their `ByteForm = Binary` and `StorageWidth` are pinned, `CobolNum.FormatImage`'s Binary lane
renders full container values through the Int128/UInt128 bits contract, and `IsImageCapable`'s leaf arm
(`Usage.Display or Usage.Binary or Usage.Packed`) simply never widened. The landing must move in LOCKSTEP
across every consumer — `DataItem.IsImageCapable` + `ElementaryImageWidth`, `PhysicalModel`'s own usage
filter (a second copy of the predicate — the [[feedback_two_arm_dispatch]] shape), `GroupImageCodec`,
`OperandText`, `PlaceRenderer`, `SortBinder`, `OoBinder`'s group-conformance arm — with a
`BYTE-LENGTH`/image drift test pinning the widened set.

**The FLOAT half needs a new pin**: COMP-1/COMP-2 leaves have no `NumericByteForm` member — an IEEE 754
big-endian 4/8-byte image form (`Ieee32`/`Ieee64`) with FormatImage/ParseImage lanes over the float
carriers (which do not ride the Int128 lane), plus the same consumer sweep.

**The INDEX half is an owner decision**: `NumericByteForm.None`'s doc deliberately makes a codec handed a
USAGE INDEX item reject loudly (§13.18.60.4 GR10 — an occurrence-number carrier only SET may reference).
A group CONTAINING one that crosses CALL BY REFERENCE is conforming source; pinning index bytes (e.g.
8-byte binary occurrence number) versus keeping the documented loud is a §4.2.16 documentation choice —
ask as a bare question when this lands.

---

## ✅ WAVE 1 LANDED 2026-08-29 (the COMP-5/BINARY-CHAR..DOUBLE predicate half) — DEVLOG Entry 1394

`PicInfo.HasImageByteForm` is THE ONE image predicate, DERIVED from the ByteForm table (`Category Numeric ∧
¬IsFloat ∧ ByteForm ≠ None`) — the four drifted copies of the hand-rolled usage union
(`DataItem.IsImageCapable`, `PhysicalModel`'s numLeaf filter, `OperandText`'s raw-storage arm, the
DataBinder Tier-B window mark) all read it now, so a usage added to ByteForm (the float IEEE forms next)
widens every consumer in one place. MEASURED working end to end: a COMP-5 + BINARY-SHORT group crosses
MOVE-as-group and CALL BY REFERENCE (`2023/pb164_comp5_group_image` — M=[0513 00258] round-trip, A=[0514]
through the shared record area); `ImagePredicateDriftTests` (18 facts) pins the widened set and the
StorageWidth image widths. THE REVIEW FLEET (78 agents, 18 confirmed) then caught the FIFTH drifted copy —
PhysicalModel's carrier-name list `"long" or "Int128"` left an UNSIGNED wide COMP-5/BINARY-DOUBLE leaf
(ulong/UInt128 carriers) out of the codec, emitting UNCOMPILABLE generated C# for any group declaring one —
fixed by deriving numLeaf from the predicate plus bit-identical unsigned FormatImage/StoreImage lanes in
CobolNum (the golden's W leg runs a 9(10) COMP-5 ulong carrier through the round trip); caught the golden's
MOVE leg riding the memberwise fast path (green BEFORE the fix — the discriminating legs now force
AsImage/FromImage through an alphanumeric intermediary, §14.9.25.4 GR4, and the CALL leg writes BOTH leaves,
§14.2.3 GR8); caught THREE stale truth-texts (TierCIsland's default "COMP-5" blame, A.1 item 208's
"excluded from character images" sentence, the ONE-WIDTH test's own retired enumeration — a sixth copy);
and my golden-header citations, now cite.py-validated. SIDE EFFECT MEASURED: the DISPLAY leg for a COMP-5
leaf CLOSED itself — the group image exists, so DISPLAY transfers it verbatim (raw bytes, the GnuCOBOL
behavior; `TierCRejectionTests.DisplayComp5Group_RendersVerbatimBytes` pins it byte-level, and the island
lock's fixture moved to the TRUE remaining boundary, a COMP-1 leaf). GR-14.9.11.4-4's residue narrowed.
MEASURED still open, unchanged scope: **REDEFINES of a binary leaf** remains the
Tier-C island (a runtime loud, probed: "float/COMP-5/BINARY-*/INDEX REDEFINES of 'NUM' (Tier-C byte path)"
— the class TIERING, not the predicate, gates it; the mixed-usage REDEFINES codec is this note's remaining
codegen half), the FLOAT IEEE pin, the INDEX owner decision, and the remaining DISPLAY legs below
(float/INDEX groups; the variable-length-group format).

## ✅ WAVE 2 LANDED 2026-08-29 (the FLOAT IEEE byte-form pin + the FLOAT-BINARY endianness axis) — DEVLOG Entry 1395

`NumericByteForm.Ieee32`/`Ieee64` — the IEEE 754 binary32/64 interchange FORMATS, 4/8 bytes (§13.18.60.4
GR14/GR15 pin the FLOAT-BINARY formats; GR13/GR21 leave COMP-1/-2/FLOAT-SHORT/-LONG/-EXTENDED to the
implementor — one encoding serves both; FLOAT-EXTENDED = binary64, the documented GR13 subset nesting) —
with BIT-REINTERPRETING lanes (`FormatImageFloat`/`ParseImageFloat`/float-double `StoreImage`; DISTINCTLY
NAMED because FormatImage overloads on float made every integer call site CS0121-ambiguous), the codec's
float encode/decode arms, and `HasImageByteForm` losing its interim `!IsFloat` conjunct exactly as its doc
promised. THE PROFILE-EMISSION FILTER was one more drifted copy (a float leaf in a group referenced `_P_n`
the emitter never declared — CS0103 in generated code; caught by the probe). `2023/pb164_float_group_image`
round-trips COMP-1/COMP-2 through an alphanumeric intermediary BIT-EXACT and writes through CALL BY
REFERENCE; the island lock moved to its FINAL boundary (USAGE INDEX — the one remaining exclusion, pending
the owner decision). A REVIEW FLEET (84 agents, 18 confirmed) measured the landing; its findings closed
under the owner directive "make the long-term supportability change, not a quick and dirty hack":

- **The FORMAT/BYTE-ORDER citation split (fleet, real-clause-different-question family):** GR14/GR15 pin
  only the interchange FORMAT; byte order is §13.18.60.4 GR19 (HIGH-ORDER-LEFT/RIGHT = big/little-endian)
  via §11.9.8 for the STANDARD usages — and §11.9.8.3 SR1 makes the no-clause default the implementor's
  REQUIRED-documented choice (A.1 item 48; the resume note's "SR3 implies HIGH-ORDER-RIGHT default" was a
  misreading — SR3 only applies when the clause SAYS HIGH-ORDER-RIGHT). Landed as the full axis, not a
  loud: `PicInfo.FloatItem(usage, Options.FloatBinaryEndianness)` is THE ONE application of the
  implied-phrase rule (standard usages only, GR19c) → `PicInfo.FloatLittleEndian` → `ProfileInitializer`
  (stated only when true — big-endian profiles byte-identical to before) → `NumProfile.FloatLittleEndian`
  → the lanes. `OptionsModel.FloatBinaryEndianness`'s zero-consumer state is retired.
  `2023/pb164_float_hor_image` proves the plumbing end to end with FUNCTION ORD byte reads (a round trip
  is SELF-INVERSE and cannot see byte order); CONFORMANCE.md row 48 documents the default; inventory
  FMT-11.9.8.2 + SR-11.9.8.3-1/-2/-3 → CONFORMS (GAP 3663 → 3659). The missing PER-ITEM
  endianness-phrase on the USAGE clause (§13.18.60.2) is registered as [[PB174]] and merges into the same
  factory argument when the grammar carries it.
- **The SORT float key (fleet 3-0, the wave's real catch) — implemented WHOLE, superseding the recorded
  stage-loud plan:** the recorded decision's premise ("the NumericKey column cannot be flipped on — its
  ParseImage throws NoByteImage on Ieee profiles") dissolved once `KeyColumns.Build` dispatches on the
  profile's ByteForm: an Ieee32/Ieee64 key decodes through `ParseImageFloat` into a `double[]` column and
  compares ALGEBRAICALLY (§14.9.40.4 GR8 → §8.8.4.2.4; raw big-endian IEEE bytes would order every
  negative after every positive). `SortBinder` drops the `IsFloat:false` conjunct; `2023/pb164_float_sort_key`
  releases negatives on both sides of zero out of order and pins the algebraic order. Smaller than the
  stage would have been, and no residue.
- **The self-inverse-golden gap (fleet):** `FloatImageLaneTests` pins the lanes at BYTE level against the
  standard, not against their own inverse — 1.5f = 3F C0 00 00, −2.25 = C0 02 00 00 00 00 00 00, the
  little-endian reversals as literal bytes, the Ieee32 narrowing, and the wave-1 unsigned ulong lane's
  full-container big-endian bytes.
- **The drift-table re-derivation:** `NumericByteFormDriftTests` rows Float..FloatBinary64 → Ieee32/Ieee64
  (FloatBinary128/FloatDecimal16/-34 stay None — COBOLNET1564 rejects them at ParseUsage); `hasOwnBytes`
  gains the Ieee forms; the "no profile is emitted for floats" comment rewritten; new
  `FloatBinaryEndianness_ReachesOnlyStandardBinaryFloatProfiles` theory pins the one-place derivation.
- **ONE-WIDTH widened to floats:** `ImageWidthIsStorageWidthTests` enumerates the float family (its doc
  had itself drifted); `DataItem.ElementaryByteWidth`'s duplicated float 4/8 literals now delegate to
  `PicInfo.StorageWidth` like the binary siblings (only the rejected 128/decimal forms keep literals).
- **The stale-wording sweep (eleven sites + two user-facing):** every 'float/COMP-5/INDEX' island
  formulation now blames USAGE INDEX alone (RecordStructEmitter, OperandText ×2, CallEmitter,
  PlaceRenderer ×2, MoveEmitter ×2, SequentialIoEmitter ×2, StringEmitter, NumericRenderer, DataBinder
  ComputeTier, PicInfo.ByteForm doc, TierCIsland class doc); `TierCIsland.Reason`'s default leafKind reads
  "USAGE INDEX" (grammatical, precise); SortBinder's SD-record message + `SortRecordOf` doc corrected; and
  `OoConformance`'s two hand-rolled island strings (stale twice over) now CALL `TierCIsland.Reason`.
- **Rejected by refuters, not re-fixed:** ImageInitOf float seeding, the Ieee32-via-double reinterpret
  concern, non-finite coverage, the "unreachable" StoreImage overloads, DISPLAY-of-float-group pinning.

Wave-local gate GREEN (Conformance 1739 · Unit 5027 incl. the new pins · characterization 33; the corpus
population lists all four pb164 goldens by name); inventory reference gate 10/10. NO other verdict rows
change (GR-14.9.11.4-4 was updated in wave 1). **Battery #35 accrues: PB157 + PB164 w1 + w2.**

**Remaining, unchanged scope:** the mixed-usage REDEFINES codec (the class TIERING gates it — the live
Tier-C runtime loud), the USAGE INDEX owner decision (ask when PB164 lands whole), the variable-length-group
display format, and the DISPLAY legs below (INDEX groups; floats closed with this wave).

## ✅ VARIABLE-LENGTH-GROUP DISPLAY LANDED 2026-08-30 (the PB148-consolidated half (b) + (c)) — DEVLOG Entry 1398

§14.9.11.4 GR7's implementor-defined format IS the A.1 item 57 determination (CONFORMANCE.md row 57):
members' images in declaration order, each dynamic member at its CURRENT extent — a dynamic-capacity
table every occurrence at current capacity (`CobolDynTable.CurrentImage`), a DYNAMIC LENGTH item its
current content, a nested SCALAR variable-length group recursively — every FIXED member contributing
exactly the record-image member law (`GroupImageCodec.AsImageOf`, the ONE recipe). Emitted as a
`CurrentImage()` sibling of `AsImage()` gated on `GroupImageCodec.CurrentExtentImageCapable`; the
struct-method gate keys on the new `DataItem.ElementImageCapable` (the IsImageCapable body minus the
dynamic axis — split, not duplicated) so a dynamic table's ELEMENT struct carries its per-occurrence
image. Wired into the DISPLAY operand path ONLY (GR7 is a DISPLAY determination — the shared
WRITE/RELEASE/compare sender keeps the loud). `TierCIsland.Reason` now DERIVES its default offender
(half (c)): imageless-leaf first, then the dynamic mechanism, promising nothing about DISPLAY.

**THE REVIEW FLEET (31 agents; 11 confirmed → FIVE distinct defects, all fixed in-change; 3 killed):**
- CRITICAL (refuters reproduced): the nested-group arm discriminated by `Occurs is null` — TRUE for a
  Format-4 dynamic table too — re-admitting the dynamic-table-with-runtime-length-element shape arm 3
  rejects, and emitting UNCOMPILABLE C# on legal source that never referenced the group. Now
  `!IsDynamicTable ∧ Occurs is null`.
- MAJOR: an OCCURS DEPENDING member rendered at MAXIMUM occurrences where the LENGTH sum counts the
  CURRENT count. EXCLUDED (loud) — architecturally forced, not convenience: `CurrentImage()` is a
  struct instance method and data-name-1 may live outside the group; the LENGTH sum reads it through
  the operand's access path, a mechanism a struct method does not have. Row 57 documents the exclusion.
- MAJOR: a NATIONAL member falsified "LENGTH(G) == displayed width by construction" (LENGTH counts 2
  bytes/position, the image displays 1 char). The CLAIM was wrong, not the display: row 57, both code
  docs and the inventory note now state the character-position geometry with the sanctioned D-N1/D-N3
  divergence, and the golden gained a national member so L = width + 2 is OBSERVED at both extents.
- MAJOR+MINOR (both blame directions): `Reason` blamed the dynamic member when an INDEX leaf was the
  real blocker (false remedy), blamed INDEX for an item that IS a dynamic table
  (HasVariableLengthSubordinate walks children only), and promised "DISPLAY alone renders…" inside
  DISPLAY's own failure message. Now: imageless-leaf test first (`HasImagelessLeaf` — the leaf half of
  ElementImageCapable), the item's own dynamic axes checked, no promise.
- Killed (≥1 refute): the bit-run gate concern, an ODO-at-max duplicate, the OdoGroupPlace bypass claim.

MEASURED: `2023/pb164_vlg_display` (fixed prefix + dynamic table + DYNAMIC LENGTH + NATIONAL member)
matches spec-derived output at two extents, CLI probes green on the fleet's repro shapes. Wave gate
GREEN ×2 (Conformance 1333 · Unit 5027 · characterization 33; the FIRST run went RED on pb62 — a
fixed-OCCURS nested variable-length group the recursion admitted — and the fix for it produced the
fleet's CRITICAL, a lesson in discriminator choice). GR-14.9.11.4-7 NOT-IMPLEMENTED → PARTIAL (residues
named and loud); GR-14.9.11.4-4/-6 narrowed to USAGE INDEX only. Registered: [[PB176]] — the probes
found the ODO-plus-dynamic group-sender emitting `AsImage()` on a struct that never gets one (CS1061 on
legal source), PROVEN PRE-EXISTING at `4006248f` by stash/build/probe/pop. Also observed, PB164-family
residue: DISPLAY of a SUBSCRIPTED dynamic-table ELEMENT is loud though its element struct now has an
image (the sender arm keys on the table item) — closes with the INDEX/display legs. Battery #36 accrues
(PB168 + this).

**PB164 remaining: the mixed-usage REDEFINES codec, the USAGE INDEX owner decision (with display legs
(a)/(d) and the element-display gap), [[PB176]] adjacent.**

**Consolidated from PB148 (2026-08-29):** the DISPLAY-side legs of the same island — (a) `DISPLAY G`
over a group with an INDEX leaf throws the Tier-C loud on legal source (GR-14.9.11.4-4's
PARTIAL residue; the float leg closed with wave 2); (b) the VARIABLE-LENGTH-GROUP display format
(§14.9.11.4 GR7 + Annex A.1 item 57 — a REQUIRED documented determination; GR-14.9.11.4-7 NOT-IMPLEMENTED)
— the natural format is the fixed prefix plus each dynamic leaf at its CURRENT length / dynamic table at
current capacity, the same sum `IntrinsicBinder.VariableLengthGroupSum` computes for FUNCTION LENGTH under
§15.50.4 r7; add the A.1 item-57 CONFORMANCE.md row WITH the format; (c) TierCIsland.Reason's default
leafKind string blames 'USAGE INDEX' even for a variable-length group — fix the message with the
mechanism; (d) the GR-14.9.11.4-6 sum-of-sizes residue for such operands closes with (a)/(b).
