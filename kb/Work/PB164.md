---
title: "PB164 — LANDED WHOLE 2026-08-31 — the group-image predicate excluded leaf kinds whose byte forms were already pinned or pinnable: COMP-5/BINARY-CHAR..DOUBLE leaves had ByteForm.Binary and StorageWidth yet IsImageCapable rejected them (a group containing one could not cross CALL, MOVE as a group, or REDEFINES-window — loud on conforming source); FLOAT leaves needed an IEEE byte-form pin; USAGE INDEX needed an owner decision (R40); and the last half — the 'mixed-usage REDEFINES codec' — turned out not to exist: V59 had already made Tier-B windows byte-form, so the fix was to DELETE the rejecting arm and complete its lanes"
id: PB164
kind: defect
status: landed
severity: MAJOR
area: codegen
wrong_answer: false
crashes: true
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.18.60.4", "13.18.44.4", "14.8.2.2", "15.50.4"]
tags: [cobolsharp, work, defect]
---

# PB164

Re-homed from PB133's mechanism (7): `CallEmitter.ArgText` loud-stages a CALL argument group whose leaves
are not `IsImageCapable`, and `OperandText`/`PlaceRenderer`/`RecordStructEmitter` hold the same Tier-C
island posture for MOVE/REDEFINES/records — conforming source throws at RUN time.

**The COMP-5 half is pure predicate drift**: `PicInfo.ByteForm`'s own doc (written after V59) says COMP-5
and BINARY-CHAR..DOUBLE "are excluded from the IMAGE by their BinaryCapacity discipline … never by their
representation: stating the form here is what makes admitting them a WIDTH question, not a representation
question" — their `ByteForm = Binary` and `StorageWidth` are pinned, `CobolNum.FormatImage`'s Binary lane
renders full container values through the Int128/UInt128 bits contract, and `IsImageCapable`'s leaf arm
(`Usage.Display or Usage.Binary or Usage.Packed`) simply never widened. The landing must move in LOCKSTEP
across every consumer — `DataItem.IsImageCapable` + `ElementaryImageWidth`, `PhysicalModel`'s own usage
filter (a second copy of the predicate — the [[feedback_two_arm_dispatch]] shape), `GroupImageCodec`,
`OperandText`, `PlaceRenderer`, `SortBinder`, `OoBinder`'s group-conformance arm — with a
`BYTE-LENGTH`/image drift test pinning the widened set.

**The FLOAT half needs a new pin**: COMP-1/COMP-2 leaves have no `NumericByteForm` member — an IEEE 754
big-endian 4/8-byte image form (`Ieee32`/`Ieee64`) with FormatImage/ParseImage lanes over the float
carriers (which do not ride the Int128 lane), plus the same consumer sweep.

**The INDEX half is an owner decision**: `NumericByteForm.None`'s doc deliberately makes a codec handed a
USAGE INDEX item reject loudly (§13.18.60.4 GR10 — an occurrence-number carrier only SET may reference).
A group CONTAINING one that crosses CALL BY REFERENCE is conforming source; pinning index bytes (e.g.
8-byte binary occurrence number) versus keeping the documented loud is a §4.2.16 documentation choice —
ask as a bare question when this lands.

---

## ✅ WAVE 1 LANDED 2026-08-29 (the COMP-5/BINARY-CHAR..DOUBLE predicate half) — DEVLOG Entry 1394

`PicInfo.HasImageByteForm` is THE ONE image predicate, DERIVED from the ByteForm table (`Category Numeric ∧
¬IsFloat ∧ ByteForm ≠ None`) — the four drifted copies of the hand-rolled usage union
(`DataItem.IsImageCapable`, `PhysicalModel`'s numLeaf filter, `OperandText`'s raw-storage arm, the
DataBinder Tier-B window mark) all read it now, so a usage added to ByteForm (the float IEEE forms next)
widens every consumer in one place. MEASURED working end to end: a COMP-5 + BINARY-SHORT group crosses
MOVE-as-group and CALL BY REFERENCE (`2023/pb164_comp5_group_image` — M=[0513 00258] round-trip, A=[0514]
through the shared record area); `ImagePredicateDriftTests` (18 facts) pins the widened set and the
StorageWidth image widths. THE REVIEW FLEET (78 agents, 18 confirmed) then caught the FIFTH drifted copy —
PhysicalModel's carrier-name list `"long" or "Int128"` left an UNSIGNED wide COMP-5/BINARY-DOUBLE leaf
(ulong/UInt128 carriers) out of the codec, emitting UNCOMPILABLE generated C# for any group declaring one —
fixed by deriving numLeaf from the predicate plus bit-identical unsigned FormatImage/StoreImage lanes in
CobolNum (the golden's W leg runs a 9(10) COMP-5 ulong carrier through the round trip); caught the golden's
MOVE leg riding the memberwise fast path (green BEFORE the fix — the discriminating legs now force
AsImage/FromImage through an alphanumeric intermediary, §14.9.25.4 GR4, and the CALL leg writes BOTH leaves,
§14.2.3 GR8); caught THREE stale truth-texts (TierCIsland's default "COMP-5" blame, A.1 item 208's
"excluded from character images" sentence, the ONE-WIDTH test's own retired enumeration — a sixth copy);
and my golden-header citations, now cite.py-validated. SIDE EFFECT MEASURED: the DISPLAY leg for a COMP-5
leaf CLOSED itself — the group image exists, so DISPLAY transfers it verbatim (raw bytes, the GnuCOBOL
behavior; `TierCRejectionTests.DisplayComp5Group_RendersVerbatimBytes` pins it byte-level, and the island
lock's fixture moved to the TRUE remaining boundary, a COMP-1 leaf). GR-14.9.11.4-4's residue narrowed.
MEASURED still open, unchanged scope: **REDEFINES of a binary leaf** remains the
Tier-C island (a runtime loud, probed: "float/COMP-5/BINARY-*/INDEX REDEFINES of 'NUM' (Tier-C byte path)"
— the class TIERING, not the predicate, gates it; the mixed-usage REDEFINES codec is this note's remaining
codegen half), the FLOAT IEEE pin, the INDEX owner decision, and the remaining DISPLAY legs below
(float/INDEX groups; the variable-length-group format).

## ✅ WAVE 2 LANDED 2026-08-29 (the FLOAT IEEE byte-form pin + the FLOAT-BINARY endianness axis) — DEVLOG Entry 1395

`NumericByteForm.Ieee32`/`Ieee64` — the IEEE 754 binary32/64 interchange FORMATS, 4/8 bytes (§13.18.60.4
GR14/GR15 pin the FLOAT-BINARY formats; GR13/GR21 leave COMP-1/-2/FLOAT-SHORT/-LONG/-EXTENDED to the
implementor — one encoding serves both; FLOAT-EXTENDED = binary64, the documented GR13 subset nesting) —
with BIT-REINTERPRETING lanes (`FormatImageFloat`/`ParseImageFloat`/float-double `StoreImage`; DISTINCTLY
NAMED because FormatImage overloads on float made every integer call site CS0121-ambiguous), the codec's
float encode/decode arms, and `HasImageByteForm` losing its interim `!IsFloat` conjunct exactly as its doc
promised. THE PROFILE-EMISSION FILTER was one more drifted copy (a float leaf in a group referenced `_P_n`
the emitter never declared — CS0103 in generated code; caught by the probe). `2023/pb164_float_group_image`
round-trips COMP-1/COMP-2 through an alphanumeric intermediary BIT-EXACT and writes through CALL BY
REFERENCE; the island lock moved to its FINAL boundary (USAGE INDEX — the one remaining exclusion, pending
the owner decision). A REVIEW FLEET (84 agents, 18 confirmed) measured the landing; its findings closed
under the owner directive "make the long-term supportability change, not a quick and dirty hack":

- **The FORMAT/BYTE-ORDER citation split (fleet, real-clause-different-question family):** GR14/GR15 pin
  only the interchange FORMAT; byte order is §13.18.60.4 GR19 (HIGH-ORDER-LEFT/RIGHT = big/little-endian)
  via §11.9.8 for the STANDARD usages — and §11.9.8.3 SR1 makes the no-clause default the implementor's
  REQUIRED-documented choice (A.1 item 48; the resume note's "SR3 implies HIGH-ORDER-RIGHT default" was a
  misreading — SR3 only applies when the clause SAYS HIGH-ORDER-RIGHT). Landed as the full axis, not a
  loud: `PicInfo.FloatItem(usage, Options.FloatBinaryEndianness)` is THE ONE application of the
  implied-phrase rule (standard usages only, GR19c) → `PicInfo.FloatLittleEndian` → `ProfileInitializer`
  (stated only when true — big-endian profiles byte-identical to before) → `NumProfile.FloatLittleEndian`
  → the lanes. `OptionsModel.FloatBinaryEndianness`'s zero-consumer state is retired.
  `2023/pb164_float_hor_image` proves the plumbing end to end with FUNCTION ORD byte reads (a round trip
  is SELF-INVERSE and cannot see byte order); CONFORMANCE.md row 48 documents the default; inventory
  FMT-11.9.8.2 + SR-11.9.8.3-1/-2/-3 → CONFORMS (GAP 3663 → 3659). The missing PER-ITEM
  endianness-phrase on the USAGE clause (§13.18.60.2) is registered as [[PB174]] and merges into the same
  factory argument when the grammar carries it.
- **The SORT float key (fleet 3-0, the wave's real catch) — implemented WHOLE, superseding the recorded
  stage-loud plan:** the recorded decision's premise ("the NumericKey column cannot be flipped on — its
  ParseImage throws NoByteImage on Ieee profiles") dissolved once `KeyColumns.Build` dispatches on the
  profile's ByteForm: an Ieee32/Ieee64 key decodes through `ParseImageFloat` into a `double[]` column and
  compares ALGEBRAICALLY (§14.9.40.4 GR8 → §8.8.4.2.4; raw big-endian IEEE bytes would order every
  negative after every positive). `SortBinder` drops the `IsFloat:false` conjunct; `2023/pb164_float_sort_key`
  releases negatives on both sides of zero out of order and pins the algebraic order. Smaller than the
  stage would have been, and no residue.
- **The self-inverse-golden gap (fleet):** `FloatImageLaneTests` pins the lanes at BYTE level against the
  standard, not against their own inverse — 1.5f = 3F C0 00 00, −2.25 = C0 02 00 00 00 00 00 00, the
  little-endian reversals as literal bytes, the Ieee32 narrowing, and the wave-1 unsigned ulong lane's
  full-container big-endian bytes.
- **The drift-table re-derivation:** `NumericByteFormDriftTests` rows Float..FloatBinary64 → Ieee32/Ieee64
  (FloatBinary128/FloatDecimal16/-34 stay None — COBOLNET1564 rejects them at ParseUsage); `hasOwnBytes`
  gains the Ieee forms; the "no profile is emitted for floats" comment rewritten; new
  `FloatBinaryEndianness_ReachesOnlyStandardBinaryFloatProfiles` theory pins the one-place derivation.
- **ONE-WIDTH widened to floats:** `ImageWidthIsStorageWidthTests` enumerates the float family (its doc
  had itself drifted); `DataItem.ElementaryByteWidth`'s duplicated float 4/8 literals now delegate to
  `PicInfo.StorageWidth` like the binary siblings (only the rejected 128/decimal forms keep literals).
- **The stale-wording sweep (eleven sites + two user-facing):** every 'float/COMP-5/INDEX' island
  formulation now blames USAGE INDEX alone (RecordStructEmitter, OperandText ×2, CallEmitter,
  PlaceRenderer ×2, MoveEmitter ×2, SequentialIoEmitter ×2, StringEmitter, NumericRenderer, DataBinder
  ComputeTier, PicInfo.ByteForm doc, TierCIsland class doc); `TierCIsland.Reason`'s default leafKind reads
  "USAGE INDEX" (grammatical, precise); SortBinder's SD-record message + `SortRecordOf` doc corrected; and
  `OoConformance`'s two hand-rolled island strings (stale twice over) now CALL `TierCIsland.Reason`.
- **Rejected by refuters, not re-fixed:** ImageInitOf float seeding, the Ieee32-via-double reinterpret
  concern, non-finite coverage, the "unreachable" StoreImage overloads, DISPLAY-of-float-group pinning.

Wave-local gate GREEN (Conformance 1739 · Unit 5027 incl. the new pins · characterization 33; the corpus
population lists all four pb164 goldens by name); inventory reference gate 10/10. NO other verdict rows
change (GR-14.9.11.4-4 was updated in wave 1). **Battery #35 accrues: PB157 + PB164 w1 + w2.**

**Remaining, unchanged scope:** the mixed-usage REDEFINES codec (the class TIERING gates it — the live
Tier-C runtime loud), the USAGE INDEX owner decision (ask when PB164 lands whole), the variable-length-group
display format, and the DISPLAY legs below (INDEX groups; floats closed with this wave).

## ✅ VARIABLE-LENGTH-GROUP DISPLAY LANDED 2026-08-30 (the PB148-consolidated half (b) + (c)) — DEVLOG Entry 1398

§14.9.11.4 GR7's implementor-defined format IS the A.1 item 57 determination (CONFORMANCE.md row 57):
members' images in declaration order, each dynamic member at its CURRENT extent — a dynamic-capacity
table every occurrence at current capacity (`CobolDynTable.CurrentImage`), a DYNAMIC LENGTH item its
current content, a nested SCALAR variable-length group recursively — every FIXED member contributing
exactly the record-image member law (`GroupImageCodec.AsImageOf`, the ONE recipe). Emitted as a
`CurrentImage()` sibling of `AsImage()` gated on `GroupImageCodec.CurrentExtentImageCapable`; the
struct-method gate keys on the new `DataItem.ElementImageCapable` (the IsImageCapable body minus the
dynamic axis — split, not duplicated) so a dynamic table's ELEMENT struct carries its per-occurrence
image. Wired into the DISPLAY operand path ONLY (GR7 is a DISPLAY determination — the shared
WRITE/RELEASE/compare sender keeps the loud). `TierCIsland.Reason` now DERIVES its default offender
(half (c)): imageless-leaf first, then the dynamic mechanism, promising nothing about DISPLAY.

**THE REVIEW FLEET (31 agents; 11 confirmed → FIVE distinct defects, all fixed in-change; 3 killed):**
- CRITICAL (refuters reproduced): the nested-group arm discriminated by `Occurs is null` — TRUE for a
  Format-4 dynamic table too — re-admitting the dynamic-table-with-runtime-length-element shape arm 3
  rejects, and emitting UNCOMPILABLE C# on legal source that never referenced the group. Now
  `!IsDynamicTable ∧ Occurs is null`.
- MAJOR: an OCCURS DEPENDING member rendered at MAXIMUM occurrences where the LENGTH sum counts the
  CURRENT count. EXCLUDED (loud) — architecturally forced, not convenience: `CurrentImage()` is a
  struct instance method and data-name-1 may live outside the group; the LENGTH sum reads it through
  the operand's access path, a mechanism a struct method does not have. Row 57 documents the exclusion.
- MAJOR: a NATIONAL member falsified "LENGTH(G) == displayed width by construction" (LENGTH counts 2
  bytes/position, the image displays 1 char). The CLAIM was wrong, not the display: row 57, both code
  docs and the inventory note now state the character-position geometry with the sanctioned D-N1/D-N3
  divergence, and the golden gained a national member so L = width + 2 is OBSERVED at both extents.
- MAJOR+MINOR (both blame directions): `Reason` blamed the dynamic member when an INDEX leaf was the
  real blocker (false remedy), blamed INDEX for an item that IS a dynamic table
  (HasVariableLengthSubordinate walks children only), and promised "DISPLAY alone renders…" inside
  DISPLAY's own failure message. Now: imageless-leaf test first (`HasImagelessLeaf` — the leaf half of
  ElementImageCapable), the item's own dynamic axes checked, no promise.
- Killed (≥1 refute): the bit-run gate concern, an ODO-at-max duplicate, the OdoGroupPlace bypass claim.

MEASURED: `2023/pb164_vlg_display` (fixed prefix + dynamic table + DYNAMIC LENGTH + NATIONAL member)
matches spec-derived output at two extents, CLI probes green on the fleet's repro shapes. Wave gate
GREEN ×2 (Conformance 1333 · Unit 5027 · characterization 33; the FIRST run went RED on pb62 — a
fixed-OCCURS nested variable-length group the recursion admitted — and the fix for it produced the
fleet's CRITICAL, a lesson in discriminator choice). GR-14.9.11.4-7 NOT-IMPLEMENTED → PARTIAL (residues
named and loud); GR-14.9.11.4-4/-6 narrowed to USAGE INDEX only. Registered: [[PB176]] — the probes
found the ODO-plus-dynamic group-sender emitting `AsImage()` on a struct that never gets one (CS1061 on
legal source), PROVEN PRE-EXISTING at `4006248f` by stash/build/probe/pop. Also observed, PB164-family
residue: DISPLAY of a SUBSCRIPTED dynamic-table ELEMENT is loud though its element struct now has an
image (the sender arm keys on the table item) — closes with the INDEX/display legs. Battery #36 accrues
(PB168 + this).

**PB164 remaining: the mixed-usage REDEFINES codec, the USAGE INDEX owner decision (with display legs
(a)/(d) and the element-display gap), [[PB176]] adjacent.**

## ✅ THE INDEX HALF LANDED 2026-08-30 (owner decision R40) — DEVLOG Entry 1401

The R40 decision (pin an 8-byte BIG-ENDIAN two's-complement binary occurrence-number image; A.1 item 211
rewritten) landed as TWO TABLE ROWS — `PicInfo.ByteForm(Index) = Binary`, `StorageWidth(Index) = 8` —
and THE ONE derived predicate did the rest: `HasImageByteForm` admits INDEX-leaf groups to
images/records/CALL/MOVE/DISPLAY with ZERO consumer edits, the wave-1 design's payoff. §13.18.60.3
SR10's reference restriction on the ITEM is untouched. `2023/pb164_index_group_image` pins the crossing
(ORD byte discrimination; observation only through SR10's permitted references — SET + relation; CALL BY
REFERENCE write-through); the rejection lock flipped (island fixture → the variable-length group;
ACCEPT/STRING as bind-screen pins; `DisplayIndexGroup_RendersVerbatimBytes` byte-level).

**THE REVIEW FLEET (57 agents; 11 confirmed → FOUR distinct defects, all fixed in-change; 7 killed):**
- **The sign lane (CRITICAL family, refuters reproduced):** `IndexItem` was `Signed: false` over the
  signed `long` carrier — FormatBinaryImage's unsigned arm encoded |value| and ParseBinaryImage
  zero-extended, so the codec was NOT an involution and a group MOVE of HIGH-VALUES silently rewrote
  the index window where §14.9.25.4 GR4 requires a representation copy. `Signed: true` IS the
  two's-complement pin; the byte facts gained the negative row (−1 = FF×8, and back).
- **The overclaim (MAJOR, 0 refutes):** "no leaf kind excludes a group any more" was FALSE — the
  POINTER/PROGRAM-POINTER/FUNCTION-POINTER/OBJECT-REFERENCE categories have no character image and R40
  closed only the NUMERIC kinds. Every absolute wording corrected (including R40.md's own first-cut
  claim); SortBinder's SD message now DERIVES the mechanism via TierCIsland.Reason (a fixed
  "VARIABLE-LENGTH" string had misdiagnosed a pointer-leafed record — the wrong-cause defect a third
  time); the lock gained the pointer-arm facts; and rows GR-14.9.11.4-4/-6 were HONESTLY REVERTED to
  PARTIAL with the pointer-leaf-group residue named (GAP back to 3659 — a newly NAMED island member
  found by review, not by a user).
- **Round-2 stale sites (MINOR):** five more pre-R40 comments swept, one of them the ByteForm
  property's own doc twelve lines above the new row.
- **The citation split (MINOR, cite.py-verified):** the reference restriction is §13.18.60.3 SR10 — and
  its permitted set is BROADER than the first cut said (intrinsic/method arguments, the USING phrases
  of PD header/CALL/INVOKE) — while §13.18.60.4 GR10 is the class/representation rule the pin rests
  on. Corrected in R40.md, item 211, the golden header, and every comment.
Wave gate GREEN (Conformance 1347 — the golden and the pointer/R40 lock facts by name · Unit 5029 — the drift tables, the negative byte row, ONE-WIDTH at 8 · characterization 33). Battery #37 accrues (this).

**PB164 remaining: the Step D WIDENING (the 2026-08-30 four-reader design scout INVERTED the premise —
V59 already made Tier-B windows byte-form; there is no byte codec to build). The re-based plan is
DESIGN-data-model §2.3 Step D: Tier-D first ([[PB179]] — the spec-required SR12/SR14 pointer/object arm),
the live Tier-B lane defects ([[PB180]] ACCEPT's width bug, [[PB181]] the CALL text-reader corruption),
then dissolve ComputeTier arm 1 with the float/unsigned-wide/Length-0 lanes completed, the second gate
(ForceStringCanonical) widened deliberately, NATIONAL rejected with the corrected reason, and the truth
sweep incl. [[PB182]] (the phantom §8.8.4.1.1, 18 sites). Plus the subscripted-dynamic-element display
gap. The island's P1 boundary: variable-length groups + pointer/object-class leaves.**

## ✅ THE LAST HALF LANDED 2026-08-31 — arm 1 dissolved, and the premise inverted (DEVLOG Entry 1406)

**THE NOTE CLOSES WHOLE.** What every earlier section called "the mixed-usage REDEFINES codec — PB164's
remaining half" did not need to be built. The 2026-08-30 four-reader design scout INVERTED the premise: V59 had
already made Tier-B REDEFINES windows byte-form, so there was no byte codec missing — there was a *rejecting arm*
standing in front of one that already worked, and a handful of emit LANES that had never been asked to serve a
windowed float / unsigned-wide / index item. The work was therefore a DELETION plus lane completion, not a
codec. `RedefinesTier.ByteCanonical` and `StorageForm.TierCWindow` are GONE (zero fan-out, confirmed by build),
`ComputeTier`'s float/COMP-5/BINARY-*/INDEX reject is GONE, and the second gate (`ForceStringCanonical`, the
EXTERNAL/BASED/ADDRESS-OF path) now admits every `HasImageByteForm` numeric plus display-form booleans.

**THE LANES, each pinned:** `NumericRenderer`'s windowed-float arm ordered BEFORE the native float arm, plus the
windowed unsigned-wide/long arms (`ParseImageU`/`ParseImageU128` new in `CobolNum` + `RuntimeApi`);
`ArithmeticEmitter` + `MoveEmitter` float-receiver stores re-encoding through `NumFormatImageFloat` when
`StoreAsImage`; `ValueInitializer` + `GroupImageCodec` float seeds through the ONE `RawValueAsFloat` recipe;
`StorageFormPass`'s canonical promotion widened to NativeInt/NativeFloat/IndexCell; and `SetEmitter`'s TWO
windowed-index arms (`StoreSetTarget` **and** `AugmentSetTarget` — the second found only by the golden probe,
CS1503 raw-long-into-window, the [[feedback_two_arm_dispatch]] shape yet again). NATIONAL is rejected with the
honest RESIDUE-11 reason; USAGE BIT stays out on packing; pointer/object stay out on having no image at all.

**THE ONE BEHAVIORAL FLIP, and it earned a POSITIVE golden.** The first gate run went RED on exactly one
fixture — the negative `pb151-based-comp-leaf`, which pinned the OLD posture (a BASED record with a COMP leaf
draws COBOLNET1695). The second-gate widening makes that shape WORK, so the fixture was pinning a rejection of
legal source: §13.18.5.3 SR1/SR2 restrict a BASED subject only to "shall not be of class object" and "shall not
be a dynamic-length elementary item or a variable-length group", and §14.9.3.3 SR1 asks only for the BASED
clause. It was DELETED and replaced by `2023/stepd_based_comp_leaf`, which allocates a COMP + COMP-3 + COMP-1
record, windows the same cell through a second BASED `PIC X(8)` entry and reads all eight bytes with
`FUNCTION ORD` (FF FE · 12 3D · 3F C0 00 00), then writes bytes back through the X view and proves the codec is
an involution. COBOLNET1695 kept its residue — and its WITNESS: the new negative fixture
`pb164-based-national-leaf` (a BASED record with a `PIC N(4)` leaf), because without it the diagnostic had none
at all and the residue boundary could have moved unobserved. [[PB151]]'s leg (3) is annotated as superseded.

**GOLDENS:** `2023/stepd_mixed_usage_redefines` (the mixed-usage window read through its X view with
`FUNCTION ORD` — COMP-5 `FF FF`, COMP-1 `3F C0 00 00`, the R40 INDEX occurrence at byte 14, the unsigned-wide
9(10) at byte 18 — then write-through `"AB"` decoding 16706 and displaying its R13 picture-digit image 6706,
IDENTICAL to a native COMP-5); `2023/stepd_fd_multi01_comp` (the 507-construct latent multi-01 FD implicit
class, first-run match); `2023/stepd_based_comp_leaf`. All three in the manifest.

### THE REVIEW FLEET — the full owed round

**25 confirmed findings collapsed to 11 distinct defects, all fixed in-change; 12 killed on refutation.** Five
probes were written and all five reproduced before anything was changed. Refuter corrections were treated as
binding — several findings were fixed at a DIFFERENT site than the finder named, and two had their severity
corrected downward by refuters rather than being taken at face value.

- **F1 (CRITICAL, silent wrong answer) — a windowed float DISPLAYed its raw IEEE bytes** instead of its value.
  Root cause was `OperandText.NonTextBytes`'s `IsFloat: false` conjunct, with the LIVE arm one function away
  (`RedefViewPlace` — `PlaceForItem` gives EVERY StringCanonical member one, canonical included) and the
  `StoreAsImage` arm as a second copy: fixed at the root so both arms serve floats, rendering per the A.1 item 56
  determination through the `ParseImageFloat` lane. The finder's MOVE/compare/INSPECT triggers were REFUTED and
  deliberately NOT "fixed". Rider (C18, refuter-corrected): the same function decoded a windowed UNSIGNED-wide
  COMP-5 through the SIGNED lane — the U twin added, matching the native sibling.
- **F2 (MAJOR) — `AsStorageImage` over a windowed float emitted `FormatImageFloat(<string>)`, CS1503.** Reachable
  only through `FUNCTION CONVERT` source-format ANY (the refuters killed the BYTE-LENGTH/LENGTH callers, which
  fold at bind); governing clause corrected to §15.19.3 r7. Pinned by the golden's CV leg against a native
  control — both print `3FC00000`.
- **F3 (CRITICAL) — `INITIALIZE` into a windowed float emitted `SpliceInto(…, 0f)`, CS1503.** No `StoreAsImage`
  arm existed in `InitializeEmitter`; the non-float windowed kinds were checked in the same pass (two-arm rule).
  Pinned by the golden's IZ leg.
- **F11 (the wave's real catch — a WRONG ANSWER in code this wave itself added).** The finder filed a MINOR
  call-site redundancy; its second half asked whether `FormatImage`/`ParseImageU128` round-trip a 16-byte
  UNSIGNED item at or above 2^127. They did not. `FormatBinaryImage` funnelled `FormatImage(UInt128)` through the
  SIGNED lane, whose §14.9.25.4 GR6b absolute-value rule then NEGATED a reinterpreted bit pattern — so an
  all-`X"FF"` 16-byte window read back as `…0001` instead of 2^128−1, and the re-encode did not return the bytes
  it was given. Fixed with a `BinaryBytes` helper that keeps the unsigned lane unsigned, and pinned at the three
  values that discriminate it: 2^128−1, 2^127 and 2^127+1. The golden's UW/UB legs observe both directions.
- **F4 (MINOR — severity corrected DOWN by refuters) — one seeder, not three.** The EXTERNAL cell seed wrote
  characters over byte-form windows; refuters established this is NOT a wrong answer (ISO mandates no initial
  value for a plain EXTERNAL item — §11.9.10.4 GR7, §14.6.2.3.3 "undefined"), so it landed as a
  one-mechanism-per-job consolidation: `CallInitialImage` now routes through `GroupImageCodec.ImageInitOf`, and a
  THIRD copy of the same seeding (`PtrEmitter.cs:48`) was deleted. `ImageInitOf` with `useValues` is THE seeder.
- **F8 (MAJOR, pre-existing, and in the wave's declared scope) — the hand-rolled DISPLAY-union sweep.**
  `UdfBinder`'s group-RETURNING screen still hand-rolled `{ Category: Numeric, IsFloat: false, Usage: Display }`
  and drew COBOLNET1510 on a COMP-5 group leaf — false since V59 and a rejection of legal source (§14.2.2 SR5
  places no category restriction on a RETURNING item). PROBED first (`FUNCTION PBF8MK` returning such a group
  drew 1510; after widening the screen to `DataItem.ElementImageCapable` it returns `N=[0009] T=[ABC]`). The
  elementary-INDEX RETURNING reject (§13.18.60.3 SR10 permits USING, not RETURNING) and the pointer/object
  refusals are KEPT — spec, not drift. **Then every remaining copy of that shape was classified, and the
  classification is now ENFORCED**: `DisplayUsageUnionDriftTests` inventories all 21 occurrences across 9 files
  with per-file counts and the reason beside each, so a new copy cannot appear and a removed one forces a
  decision. Verdicts: 7 SPEC-REQUIRED (§8.4.3.3.3 SR1's reference-modification restriction ×5 — MEASURED:
  `MOVE NB(1:1)` over a COMP-5 item draws COBOLNET1647 citing it — and §14.9.48.3 SR4's UNSTRING receiver);
  10 LOAD-BEARING, where the question really is "is this zoned DISPLAY?" (storage form, usage inheritance, the
  Tier-B character-vs-byte pipeline split, a report field's printable image, MoveBinder's figurative fill);
  3 OUT-OF-WAVE, registered ([[PB187]] ×2 for the OO crossing, [[PB177]] D for the STRING SR1 guard); and
  1 OPEN QUESTION deliberately left alone and registered instead — [[PB184]], `GroupValueSlicer`.
- **F5 — the stale-prose family, including a LOST WITNESS.** COBOLNET1695's title rewritten to the residue truth
  and `docs/DIAGNOSTICS.md` REGENERATED (never hand-edited); COBOLNET0869's deleted "COMP/float/index leaf" cause
  dropped; `DataBinder.Ptr` and `DataBinder.Linkage` comments corrected. The witness half is the important one:
  deleting `pb151-based-comp-leaf` left COBOLNET1695 with NO fixture at all, which is how a diagnostic quietly
  stops being tested — hence `pb164-based-national-leaf`.
- **F6 — the design docs still described `ByteCanonical`/`TierCWindow` as LIVE.** Swept the AS-BUILT sites only,
  deliberately leaving §2.3's plan text (which PRESCRIBES the deletion) intact: DESIGN-data-model,
  COBOLNET_DESIGN, COBOLNET_REDEFINES_DESIGN, COBOLNET_DATA_MODEL_DESIGN, kb/IR Data Flow, and the retired
  `kb/Reference/Compiler/TierCWindow.md`; in code, `RedefinesModel`'s stale reason-table entries and
  `NumericRenderer`'s group-arm truth-texts. `DataItem.IsCharacterImage`'s doc was EXONERATED by a refuter and
  left alone.
- **F7 — CONFORMANCE.md items 207 + 211** still said "a REDEFINES view over one remains staged loud (PB164's
  remaining half)". Rewritten to the landed truth: the item's window IS its storage (§13.18.44.4 GR1/GR2), so a
  windowed float / INDEX item behaves exactly as its natively-carried twin. Item 208 gained the same sentence for
  COMP/COMP-5 at the landing, witnessed by `stepd_mixed_usage_redefines`.
- **F9 — inventory notes** for GR-14.9.3.4-4 and -7 still asserted the superseded COBOLNET1695 boundary;
  rewritten. Row -1 was CLEAN (refuter-corrected) and left untouched.
- **F10 — a citation split, twice.** The golden's MOVE-leg claim is §14.9.25.4 **GR6d** (a numeric receiver), not
  GR6a (alphanumeric/national receivers); the SAME misattribution sat in [[R13]] and both were fixed and
  `cite.py --check`ed. One finder's corroborating "GR4 is correct" was FABRICATED — no GR4 citation exists in
  that golden — and was ignored rather than acted on.
- **KILLED (12, no action):** all four SortEmitter windowed-key findings (unreachable — `SortBinder` rejects
  classed keys; the one true half is preserved as [[PB186]] rather than lost), the STRING INTO screen-skip as
  filed, the `stepd_fd_multi01` header charge, national-boolean CellCapable, the numeric-USAGE-NATIONAL escape,
  the `RawValueAsFloat` literal-guard divergence, the CellCapable drift-pin hypothetical, and the UnifyCrossing
  OO crossing (out of scope — registered as [[PB187]] instead of fixed under a sweep).

### The gate, and what accrues

Wave-local gate GREEN on the landed tree:
`Conformance 1429/1429 · Unit 5035/5035 · Characterization 33/33`
(filter `~Corpus|~Negative|~Redefines|~TierC|~Record|~Move|~Call|~Image|~Physical|~Storage|~Drift|~V59|~External|~Based|~Accept|~Set`).
**Battery #38 accrues: this wave (PB164 arm-1 + the fleet fixes F1–F11).**

Verdicts at the landing: no row FLIPS — GAP stays 3659, honestly. `GR-14.9.3.4-3` was re-adjudicated and
DELIBERATELY held at PARTIAL (GR3 asks for "the number of bytes required to hold an item as described by
data-name-1", and for a NATIONAL / USAGE BIT / pointer-class leaf no such number is computed at all — that is a
GR3-shaped residue, not someone else's); `-9`'s residue was re-stated as the pointer-class leaf specifically
rather than "PB164's cell model"; `GR-14.9.11.4-4` gained [[PB189]] as a second named residue. The R40 precedent
governed: a named residue keeps PARTIAL.

**Registered on the way out (rule 8 — notes before DEVLOG):** [[PB184]] (the group-VALUE distributor),
[[PB185]] (the build hook's third self-block mode), [[PB186]] (the latent unsigned sort-key decode),
[[PB187]] (the OO crossing), [[PB188]] (the CCVS-leniency seed width), [[PB189]] (the subscripted
dynamic-element DISPLAY gap, promoted out of this note's prose), [[PB190]] (the stale REVERSE inventory row),
and [[PB177]] gained its fourth screen (D). The owner-decision survey items are NOT registered here — the next
wave owns them.

**The island's FINAL boundary, and it is small:** variable-length groups (the composable ones display; the rest
are named and loud) and the pointer/object-class leaves, which have no character image to give.

**Consolidated from PB148 (2026-08-29):** the DISPLAY-side legs of the same island — (a) `DISPLAY G`
over a group with an INDEX leaf throws the Tier-C loud on legal source (GR-14.9.11.4-4's
PARTIAL residue; the float leg closed with wave 2); (b) the VARIABLE-LENGTH-GROUP display format
(§14.9.11.4 GR7 + Annex A.1 item 57 — a REQUIRED documented determination; GR-14.9.11.4-7 NOT-IMPLEMENTED)
— the natural format is the fixed prefix plus each dynamic leaf at its CURRENT length / dynamic table at
current capacity, the same sum `IntrinsicBinder.VariableLengthGroupSum` computes for FUNCTION LENGTH under
§15.50.4 r7; add the A.1 item-57 CONFORMANCE.md row WITH the format; (c) TierCIsland.Reason's default
leafKind string blames 'USAGE INDEX' even for a variable-length group — fix the message with the
mechanism; (d) the GR-14.9.11.4-6 sum-of-sizes residue for such operands closes with (a)/(b).

## APPENDIX — the F8 hand-rolled DISPLAY-union sweep, occurrence by occurrence

The review fleet found `UdfBinder.UdfReturningResidue`'s GROUP-RETURNING screen still hand-rolling
`{ Category: Numeric, IsFloat: false, Usage: Display }` and rejecting a COMP-5 group leaf as having "no shared
character image across the activation boundary" — false since V59, and it rejects conforming source (§14.2.2 SR5
places NO category restriction on a RETURNING item). PROBED first: `FUNCTION PBF8MK` returning a group with a
`PIC 9(4) COMP-5` leaf drew COBOLNET1510; after widening the screen to `DataItem.ElementImageCapable` it returns
`N=[0009] T=[ABC]`. The elementary INDEX reject (§13.18.60.3 SR10 permits USING, not RETURNING) and the
pointer/object refusals are KEPT — those are spec, not drift.

**Every remaining copy of that shape was then classified; NONE is silent.** The inventory is enforced by
`tests/Cobol.Net.Tests.Unit/DisplayUsageUnionDriftTests.cs` (21 occurrences across 9 files, per-file counts with
the reason beside each), so a new copy cannot appear and a removed one forces a decision. Summary of verdicts:

- **SPEC-REQUIRED (7 occurrences)** — `ReferenceResolver` ×4 and `MoveBinder:274`: §8.4.3.3.3 SR1 admits
  reference modification of "a numeric data item of usage display or national" ONLY. MEASURED: `MOVE NB(1:1)`
  over a COMP-5 item draws COBOLNET1647 citing that rule. `StringUnstringBinder:345`: §14.9.48.3 SR4 restricts an
  UNSTRING numeric receiver the same way.
- **LOAD-BEARING, the question really is "is this zoned DISPLAY?" (10)** — `DataBinder:521`/`StorageFormPass:76`
  (which leaves are STORED AS characters; a byte-form leaf keeps its native carrier and encodes on demand),
  `DataBinder:1923`/`:3262` (USAGE INHERITANCE — Usage.Display as a VALUE), `DataBinder:3584` (the Tier-B
  character-vs-byte pipeline split — BOTH arms present), `DataBinder.Reports:539` (a report field composes a
  printable character image), `MoveBinder:136` (a non-digit figurative fill deposits fill CHARACTERS into the
  receiver's cells; a binary carrier has none).
- **OUT-OF-WAVE, registered elsewhere (3)** — `StorageFormPass:123` + `OoEmitter:335` are the OO crossing
  (now [[PB187]]); `StringEmitter:155` is the §14.9.43.3 SR1 guard (now [[PB177]] D).
- **⚠ ONE OPEN QUESTION, deliberately NOT changed (2)** — `GroupValueSlicer:48`/`:58`. §13.18.63.4 GR5 initializes a
  group VALUE's area "without consideration for the individual elementary or group items contained within this
  group" (--check OK; the shorter paraphrase this note first carried is in NO clause of the standard) — which
  would deposit the literal's BYTES into byte-form leaves; today they are undistributable and take the
  member-wise default.
  MEASURED: `01 GV VALUE "40537". 05 GB PIC 9 COMP-5 OCCURS 5.` leaves every `GB` zero. That is a separate
  §13.18.63.4 GR5 derivation with real blast radius, not predicate drift, so this wave records it rather than changing
  it under a sweep — registered as [[PB184]]. Its stale in-code reason ("their character image is the Tier-C
  byte boundary") and its mis-paraphrased citation were corrected at the landing.
