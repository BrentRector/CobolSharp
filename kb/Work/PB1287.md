---
title: "PB1287 — the report-section grammar misses two entry shapes: `reportGroupName : cobolWord` has no FILLER alternative (§13.18.20 Format 3 is a parse error in the report section, §13.15.3 SR2), and `reportDescriptionEntry : RD … DOT reportGroupEntry*` has no constant-entry arm and admits an RD followed by NO report group entry, where §13.8.2 prints the group entries with a brace ellipsis"
id: PB1287
kind: defect
status: open
severity: MINOR
area: frontend/grammar
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1129", "PB1288"]
spec_refs: ["13.15.3", "13.15.2", "13.8.2"]
inventory_rows: ["FMT-13.15.2", "SR-13.15.3-2", "FMT-13.8.2"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14e** from adjudication batch d1-14e (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probe sources under the batch's `out13/probe/<subject>/`). Checked against main 071ff04ea: trains 58/59 did not touch this mechanism.

# PB1287 — OPEN

## The rules (every quotation run through `cite.py --check` by the registrar)

- `python scripts/spec/cite.py --check 13.15.3 "If the entry-name clause is specified, the data-name format or filler format shall be specified"` → `OK  §13.15.3 2)  (Syntax rules)`
- `python scripts/spec/cite.py --check 13.15.3 "If the entry-name clause is specified, the data-name format or filler format shall be specified"` → `OK  §13.15.3 2)  (Syntax rules)`
- `python scripts/spec/cite.py --check 13.18.20.3 "If the entry-name clause is specified in a data description entry or a report group description entry, either data-name-1 or FILLER shall be specified"` → `OK  §13.18.20.3 3)  (Syntax rules)`
- `python scripts/spec/cite.py --check 13.18.20.4 "The word FILLER may be used to name a data, report, or screen item"` → `OK  §13.18.20.4 1)  (General rule)`

## Measured

### Finding report-group-description-entry — §13.15.3 SR2; §13.18.20.3 SR3; §13.18.20.4 GR1 (rejects-legal-source; rows FMT-13.15.2, SR-13.15.3-2)

**Mechanism.** report-group-entry-filler-format: the report group entry's entry-name clause is modelled as `reportGroupName : cobolWord`, which has no FILLER alternative, so the §13.18.20 Format 3 (filler) spelling of the entry-name clause is a parse error in the report section at every edition

**Repro.** RD R1 PAGE LIMIT 20 LINES. 01 D1 TYPE DE. 03 LINE 1. 05 FILLER COLUMN 1 PIC X SOURCE WA. + INITIATE/GENERATE D1/TERMINATE. Compile with --std 85, 2002 or 2023 (probe E:/Temp/claude/E--CobolSharp/a700c052-f9e4-4942-84ff-a13c7505d6ec/scratchpad/adj-d1/out13/probe/rgde/filler/filler.cob)

**Expected, from the standard.** Compiles and prints `A` in column 1. §13.15.3 SR2: 'If the entry-name clause is specified, the data-name format or filler format shall be specified.' §13.18.20.3 SR3: 'If the entry-name clause is specified in a data description entry or a report group description entry, either data-name-1 or FILLER shall be specified.' §13.18.20.4 GR1: 'The word FILLER may be used to name a data, report, or screen item.'

**Observed.** error COBOL0307: unexpected 'FILLER'. A period may be missing at the end of the previous sentence. (rc 65, all three editions probed). The data-division entry-name path admits FILLER (CobolData.g4:258). The report path does not: the two-arm shape.

**Code site.** src/Cobol.Net.Frontend/Grammar/Core/CobolReportWriter.g4#reportGroupName (`: cobolWord`)

### Finding report-section — §13.8.2 general format; §13.8.4 (one or more report group description entries); §13.10 (rejects-legal-source; rows FMT-13.8.2)

**Mechanism.** F-REPORT-SECTION-SHAPE — reportDescriptionEntry is `RD … DOT reportGroupEntry*`: it has no constant-entry arm (a constant entry after an RD is a parse/reserved-word error) and its `*` admits an RD followed by nothing, where §13.8.2's brace ellipsis and §13.8.4 require one or more entries

**Repro.** (a) REPORT SECTION. RD R-1 PAGE LIMIT IS 10 LINES. 01 KC CONSTANT AS 7. 01 DET-1 TYPE DE LINE PLUS 1. 03 COLUMN 1 PIC 9 VALUE KC. … DISPLAY "KC=" KC. (probe/report-section/c1.cob, --std 2023 or 2002). (b) REPORT SECTION. RD R-1 PAGE LIMIT IS 10 LINES. followed directly by PROCEDURE DIVISION (probe/report-section/c3.cob, any edition).

**Expected, from the standard.** (a) clean compile; report line holds 7; KC=7 displayed (§13.8.2 admits constant-entry in the brace group after report-description-entry, 2002+). (b) a diagnostic: the brace group carries an ellipsis (at least one entry) and §13.8.4 requires an RD to be followed by one or more report group description entries.

**Observed.** (a) c1.cob(12,14): error COBOLNET0901: 'CONSTANT' is a reserved word in COBOL-2023 and cannot be used as a user-defined word (ISO §8.9) — same at 2002. (b) c3.cob compiles and runs (NOGROUPS) at 2023 and 85 with no diagnostic. This duplicates the constant-entry adjudication's F-REPORT-SECTION for half (a); file ONE note covering both halves, since the fix is the one reportDescriptionEntry production (admit constantEntry in the repeated group, require at least one entry) plus the binder routing report-section constants into the constant table.

**Code site.** src/Cobol.Net.Frontend/Grammar/Core/CobolReportWriter.g4#reportDescriptionEntry (line ~27)

## What the fixer owes

Re-probe on your own build first (MANDATORY-PRACTICES I3). Fix the mechanism at the code sites above — one rule, one place (the `cluster:` notes share these files; take them as one fill unit). One positive golden at the introducing edition and one negative below it / per violated rule, each derived from the quoted rule. Sweep the sibling arms named in the findings (CLAUDE.md rule 4). On landing flip `status`, write `closes_rows` from `inventory_rows`, and re-verdict each row with `record_verdicts.py`.
