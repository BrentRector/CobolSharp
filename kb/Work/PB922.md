---
title: "PB922 — EXCEPTION-OBJECT outside a SET statement is reported as UNDEFINED — `MOVE U TO EXCEPTION-OBJECT` draws COBOLNET1639 'is not defined' plus COBOLNET0901 'is a reserved word' — where §8.4.3.6.3 SR1 declares the name and says only that it shall not be a RECEIVING operand: two diagnostics that contradict each other and the standard, and two inventory rows that are blank because nothing asks the question"
id: PB922
kind: defect
status: open
severity: MAJOR
area: binding
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: []
spec_refs: ["8.4.3.6.3", "8.9"]
inventory_rows: ["SR-8.4.3.6.3-1", "SR-8.4.3.6.3-2"]
tags: [cobolsharp, work, defect]
---

Filed 2026-09-20 by **REGISTRAR #7** — the lead-filing pass over the wave-39 implementer reports (PB425, PB392, PB399, PB404, PB449, PB413, PB432, PB457, PB897, PB461, PB465, PB428, PB554, PB491, PB882, PB511, PB566), the wave-40 report (PB246), the wave-41 finisher and implementer reports (PB449-finisher, PB511-finisher, PB566-finisher, PB245, PB560, PB412), the train-40 and train-42 lander reports, and DEVLOG 1617-1621. **Every probe below was rebuilt from the source report and RE-RUN on this worktree's own `dotnet build CobolSharp.sln -c Debug` (0 warnings, 0 errors) at main `c701786d5`**, and **every citation was re-derived with `python scripts/spec/cite.py --check`** — the command line is printed beside the quotation. Probe sources under `<scratch>/reg7/probe/`.

# PB922 — OPEN — the compiler says a name the standard declares does not exist

Source: the `PB449-finisher` report, "New defects" items 1 and 3. Re-measured here, both statements.

## The measurement

`<scratch>/reg7/probe/p_excobj_move.cob`, `--std 2023`:

```
p_excobj_move.cob(7,12): error COBOLNET1639: 'EXCEPTION-OBJECT' is not defined — no declaration in this
source element gives the name 'EXCEPTION-OBJECT', so the statement's reference identifies no resource
(ISO §8.4.2.1 …). Check the spelling, or declare the item.
p_excobj_move.cob(7,12): error COBOLNET0809: a MOVE operand shall not be of class object …
p_excobj_move.cob(7,22): error COBOLNET0901: 'EXCEPTION-OBJECT' is a reserved word in COBOL-2023 and cannot
be used as a user-defined word (ISO §8.9)
```

`<scratch>/reg7/probe/z_excobj_add.cob`, `ADD 1 TO EXCEPTION-OBJECT`, the same pair (1639 + 0901).

Read the two together: *"the name is not defined — declare it"* and *"the name is reserved — you may not
declare it"*. The user is given an instruction the next diagnostic forbids. And the advice is wrong
twice over, because the correct answer is neither: the name IS defined, by the standard, and the statement
is illegal for a third reason entirely.

## The rule

```
$ python scripts/spec/cite.py --check 8.4.3.6.3 "EXCEPTION-OBJECT"
OK  §8.4.3.6.3 1)  (Syntax rules)
    1) EXCEPTION-OBJECT shall not be specified as a receiving operand.
```

SR1 is a rule about **every** receiving operand in the language, not about SET. It presupposes the name is
known everywhere — which is the point: EXCEPTION-OBJECT is a predefined object reference (§8.4.3.6), so
`MOVE U TO EXCEPTION-OBJECT` is refused BY SR1, with SR1's words, and `MOVE EXCEPTION-OBJECT TO U` (a
SENDING use) is legal source that today gets the same false "not defined".

```
$ python scripts/spec/cite.py --check 8.9 "reserved word"
OK  §8.9   (Reserved words)
```

COBOLNET0901 is the reserved-word funnel firing because the general resolver did not recognise the name —
it is a SYMPTOM of the same hole, not a second defect.

## The code sites

* the general receiving path — `src/Cobol.Net.Compiler/Binding/ReferenceResolver.cs#ResolveImpl` reports
  the name as undefined; **no caller asks the §8.4.3.6 question.** The one-place test
  `OoBinder.OoIsExceptionObject` now exists (the PB449 finisher created it for SET) and is what the general
  path should call.
* ⛔ **A THIRD spelling test** sits in `src/Cobol.Net.Compiler/Binding/Validation/VersionConformancePass.cs`
  (~`:2478`), asking the §8.9 question — is this occurrence the predefined register, so the reserved-word
  funnel must not fire — against a `string`. Three places that decide what EXCEPTION-OBJECT is
  (`OoIsExceptionObject`, the resolver's silence, and this string test) is
  `feedback_one_rule_one_place`: the extraction is the fix, and the fix is that the RESOLVER knows the
  predefined registers, so every caller inherits it.

## Rows

**`SR-8.4.3.6.3-1`** and **`SR-8.4.3.6.3-2`** are both GAP with **no claimer, no code location and no
test-ref** — blank because nothing in the compiler asks the question. This mechanism closes both, and they
are claimed here.

## The sibling sweep

The other predefined registers and their spellings: `LINAGE-COUNTER`, `LINE-COUNTER`, `PAGE-COUNTER`,
`RETURN-CODE`, the CAPACITY registers (**PB457**, landed — its qualified-reference arm now works, verified
today) and `ADDRESS OF`. For each, ask whether a reference OUTSIDE the one statement that motivated it
resolves, or produces the same false "is not defined". That is the same defect class train 39's DEVLOG
named for CAPACITY, and it is why this one is MAJOR rather than cosmetic.
