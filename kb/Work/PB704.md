---
title: "PB704 — `SORT/MERGE … WITH DUPLICATES IN ORDER` is REJECTED at every edition ≥ 2002: `sortDuplicatesPhrase : WITH? DUPLICATES IN? cobolWord?` spells the required word ORDER as a `cobolWord` (\"not a lexer token\"), and the reserved-word screen then refuses the matched word as a user-defined one (COBOLNET0901, ORDER reserved since 2002) — the ONLY phrase that demands §14.9.40.4 GR3's defined tie order is unusable, so the workaround is a program whose result the standard leaves undefined; sweep every general-format position where a reserved word is spelled as `cobolWord`"
id: PB704
kind: defect
status: open
severity: MAJOR
area: frontend
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.40.2", "14.9.24.2", "8.9"]
inventory_rows: []
tags: [cobolsharp, work, defect]
---

# PB704 — OPEN — a required word that the grammar treats as a name

Found 2026-09-05 by the `PB678` implementer (report §"New defects" N1) while writing its SORT goldens: any SORT
with the phrase, at `--std 2002/2014/2023`, draws `error COBOLNET0901: 'ORDER' is a reserved word in COBOL-2002 and
cannot be used as a user-defined word (ISO §8.9)`. It compiles at `--std 85`, where ORDER is not yet reserved.

## The rule

§14.9.40.2 (`cite.py --check 14.9.40.2 "DUPLICATES"` → OK; the format prints `[ WITH DUPLICATES IN ORDER ]`, DUPLICATES
underlined) and MERGE's §14.9.24.2 twin make ORDER a required word of the phrase; §8.9 (`--check 8.9 "ORDER"` → OK)
reserves it from 2002. A reserved word appearing in its own general format is not a user-defined word. The phrase is
the only way a program can demand §14.9.40.4 GR3's release-order tie-break over GR4's undefined order.

## The mechanism

`src/Cobol.Net.Frontend/Grammar/Core/CobolIO.g4` ≈725: `sortDuplicatesPhrase : WITH? DUPLICATES IN? cobolWord?` with
the comment "cobolWord matches ORDER (not a lexer token)"; it parses, then the reserved-word screen refuses the
matched `cobolWord`. Same family as [[PB693]] (a reserved word admitted to the name slot unguarded — there the word IS
a token; here it is not one at all) and [[PB655]]/[[PB657]] (the reserved-word funnel's reach).

## Fix

An ORDER token in this position (the lexer/`cobol-words.json` row with the 2002 reservation, `reservationGated` so
the 85 lane keeps accepting it as a name elsewhere — PB693's drift test then covers it), or an exemption at the
screen for a word matched inside its own format; the token is the honest shape. Sweep (rule 4): every other
general-format position where a RESERVED word is spelled as `cobolWord` in the grammar — `grep -n "cobolWord" src/
Cobol.Net.Frontend/Grammar/Core/*.g4` against §8.9's list — and pin each with a positive golden at 2002+ and the 85
behaviour measured. A positive golden with `WITH DUPLICATES IN ORDER` whose expected order is GR3's (records equal on
every key in release order), at 2002 and 2023.

## Registrar verification 2026-09-06 — measured at three editions, and the corpus already contains the spelling

Train 14's lander re-collected this from the [[PB678]] implementer report (§N1); verified against main at
`0db46983` and folded here rather than filed again (CLAUDE.md rule 8).

**The grammar is unchanged** — `src/Cobol.Net.Frontend/Grammar/Core/CobolIO.g4:737-739`,
`sortDuplicatesPhrase : WITH? DUPLICATES IN? cobolWord?` with the comment *"cobolWord matches ORDER (not a lexer
token)"*.

**Measured per edition** (compiler on main; `…/scratchpad/reg14/sortdup.cob`, an SD + `SORT S ON ASCENDING KEY
S-KEY WITH DUPLICATES IN ORDER USING I GIVING O`):

| `--std` | result |
|---|---|
| 85 | **clean** |
| 2002 | `sortdup.cob(24,35): error COBOLNET0901: 'ORDER' is a reserved word in COBOL-2002 and cannot be used as a user-defined word (ISO 8.9)` |
| 2023 | the same, naming COBOL-2023 |

Column 35 lands on `ORDER`, which positively identifies the reserved-word screen refusing the word the
`cobolWord` matched — the mechanism this note states, now measured at three editions rather than one. Citations
re-run, not inherited: `--check 14.9.40.2 "DUPLICATES"` → **OK §14.9.40.2**, printing `[ WITH <u>DUPLICATES</u> IN
ORDER ]`; `--check 14.9.40.4 "DUPLICATES"` → **OK §14.9.40.4 3)** (the defined tie order the phrase buys);
`--check 8.9 "reserved word"` → **OK §8.9**; `ReservedWords.Table.cs:310` encodes ORDER as `(85:false, 2002:true,
2014:true, 2023:true)` — *"added 2002"*.

**⛔ A free, externally-authored witness this note did not have.** `tests/nist/programs/ST127A.cob:257` writes
`WITH DUPLICATES IN ORDER` — the only occurrence of the phrase anywhere in the tree. It passes today **only**
because the NIST leg runs at `--std 85` (`--nist` defaults the edition to 85), where ORDER is not yet reserved. So
the corpus already contains a program the compiler would refuse at every edition a user is likely to target, and
no gate can see it: `feedback_measure_the_selectors_complement` — the NIST leg is evidence about `--std 85` and
about nothing else. **The fix should add ST127A's SORT to its evidence**; it is proof from outside the project
that this spelling is what real COBOL writes.

**Retired ids (recorded once, in [[PB726]]):** PB727 was minted for this finding by the train-14 registrar and is
BURNED — never reassign it. Its content is this section.
