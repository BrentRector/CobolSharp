---
title: "PB1033 — an integer operand is narrowed to C# `int` at EMIT time, so a value beyond 2^31 wraps: `GO TO P1 P2 DEPENDING ON G` with `G PIC 9(10) VALUE 4294967297` goes to P1 where §14.9.17.4 GR2 falls through, and a reference-modifier start of 4294967297 reads position 1 with EC-BOUND-REF-MOD checking ON instead of raising it"
id: PB1033
kind: defect
status: open
severity: MAJOR
area: codegen
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: []
spec_refs: ["14.9.17.4", "8.4.3.3.4", "14.9.47.4", "14.7.9.3"]
inventory_rows: ["GR-14.9.17.4-2", "GR-14.7.9.3-1", "GR-14.7.9.3-2", "GR-14.7.9.3-4", "GR-14.9.43.4-4"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-23 by **REGISTRAR #13** from `leads-w53-w57.md` and the train-57 lander report (w56a lead, `wf_69a72ced-bc4` / `wf_ccbf74f1-936-1`). Re-probed on registrar #13's own Debug build of `c54434a8d` (BelowNormal).

# PB1033 — OPEN

## Measured
```cobol
       IDENTIFICATION DIVISION.
       PROGRAM-ID. W56IFN.
       DATA DIVISION.
       WORKING-STORAGE SECTION.
       01 A PIC 9 VALUE 1.
       01 B PIC 9 VALUE 2.
       01 G PIC 9(10) VALUE 4294967297.
       PROCEDURE DIVISION.
           IF A NOT >= B DISPLAY "LT" END-IF
           GO TO P1 P2 DEPENDING ON G
           DISPLAY "FELL-THROUGH"
           STOP RUN.
       P1.
           DISPLAY "WENT-P1"
           STOP RUN.
       P2.
           DISPLAY "WENT-P2"
           STOP RUN.
```

prints `LT` then **`WENT-P1`** (the `NOT >=` half is PB1034). And
```cobol
       >>TURN EC-BOUND-REF-MOD CHECKING ON
       IDENTIFICATION DIVISION.
       PROGRAM-ID. B01RM.
       DATA DIVISION.
       WORKING-STORAGE SECTION.
       01 X PIC X(5) VALUE "ABCDE".
       01 S PIC 9(10) VALUE 4294967297.
       PROCEDURE DIVISION.
           DISPLAY "RM=[" X(S:1) "]"
           STOP RUN.
```

prints **`RM=[A]`** — no EC-BOUND-REF-MOD, although checking is on and position 4294967297 is outside `X`.

## The rules
- `cite.py --check 14.9.17.4 "control is transferred to procedure-name-1, etc., depending on the value of identifier-1 being 1, 2"` → `OK §14.9.17.4 2)`. A value that is not 1…n transfers nowhere.
- `cite.py --check 8.4.3.3.4 "EC-BOUND-REF-MOD"` → `OK §8.4.3.3.4 5) c)`: an out-of-range leftmost position or length raises the condition.

## The mechanism
The one integer landing (`NumericRenderer.Align(num.AsNum(…), 0)`, PB86) yields a wide value, and every consumer then
casts it with `(int)(…)`:
- `CodeGen/Verbs/ControlFlowEmitter.cs:42` — `int __dep = (int)(…)` for the GO TO DEPENDING selector.
- `CodeGen/Roslyn/RuntimeApi.cs` `RefModStart` / `RefModLength` — `(int)(start)`, `(int)(length)`.
- `CodeGen/Verbs/SequentialIoEmitter.cs` `LinesExpr` (WRITE ADVANCING count, ~:824/:828), `RetryTimes` / `RetrySeconds`
  (~:386/:391), `LinageOpExpr` (~:303).
- `CodeGen/Verbs/AcceptDisplayEmitter.cs:95` — the ref-modified ACCEPT width.

## What the fixer owes
One range-checked narrowing helper in the runtime (`CobolInt.ToIndex(long/Int128)` or equivalent) that maps an
out-of-range value to the rule's out-of-range outcome (fall through; EC-BOUND-REF-MOD; the ADVANCING/RETRY/LINAGE
rule's own answer) instead of a wrapped `int`, and a semgrep or drift rule that bans a bare `$"(int)({…})"` around a
rendered numeric operand in `CodeGen/`. Sweep every `(int)(` in `CodeGen/` (the grep above found 20+). Row
GR-14.9.17.4-2 is CONFORMS on main and this contradicts it: re-verdict with the golden.

## Extended 2026-09-24 by REGISTRAR 14c (lane-3 batch d1-14c, pinned tree `68bbdb00f`)

Two more arms of the same emit-time narrowing. (1) RETRY (§14.7.9.3 GR1/GR2/GR4 a)): SequentialIoEmitter.RetryTimes / RetrySeconds emit `(int)(...)` — a VARIABLE wraps (WN = 4294967297 → one re-attempt, '62'; a value in [2^31, 2^32) wraps negative and GR4 a) wrongly suppresses every retry) and a LITERAL above int.MaxValue CRASHES THE BACKEND (CS0221 'Constant value 4294967297 cannot be converted to a int', both TIMES and SECONDS) — legal source that never compiles. GR1 wants a saturating count, GR2 a clamp. (2) STRING POINTER (§14.9.43.4 GR4/GR6/GR8): StringEmitter.EmitString reads identifier-4 with an unchecked `(long)` cast; a PIC 9(20)..9(31) pointer (legal at 2002+) of 2^64+3 wraps to 3, writes at position 3, runs NOT ON OVERFLOW and writes the pointer back as 5, where the rules require overflow, no transfer and the pointer unchanged; UNSTRING builds its pointer the same way (StringEmitter ~110). One range-checked narrowing helper for every emit-time integer operand is the shape that makes the next arm automatic.

**Finding (`retry-phrase`, crashes; rows `GR-14.7.9.3-1`, `GR-14.7.9.3-2`, `GR-14.7.9.3-4`).** RETRY amounts are narrowed to C# int at emit (`(int)(...)`) — wraps a variable and crashes the backend on a literal (extends open kb/Work PB1033 with the literal-crash symptom)

**Repro.** (a) DELETE FILE F RETRY 4294967297 TIMES.  or  RETRY FOR 4294967297 SECONDS  -> cobol --std 2023. (b) 01 WN PIC 9(10) VALUE 4294967297. DELETE FILE F RETRY WN TIMES. run against an external holder that releases after 1.5 s (out/probe/retry-phrase/wrapv.cob).

**Expected from the spec.** §14.7.9.3 GR1: arithmetic-expression-1 (4294967297) re-attempts; with the holder releasing after 1.5 s the statement succeeds '00' (as `RETRY 100000000 TIMES` does in the same probe). GR2: a SECONDS value above the maximum meaningful value is clamped — legal source, must compile. GR4a screens 'the result of the evaluation of the arithmetic expression', not a wrapped value.

**Observed / reasoning.** (a) backend compilation failed: CS0221 'Constant value 4294967297 cannot be converted to a int' for both TIMES and SECONDS. (b) emitted `(int)(WN)` = 1 -> one re-attempt -> '62' in 51 ms, file not deleted. A value in [2^31, 2^32) wraps negative and GR4a wrongly suppresses all retries. PB1033 (open) already names RetryTimes/RetrySeconds for the variable wrap; the literal CS0221 crash is not in its body and should be added there, with a range-checked narrowing that saturates (GR1 count / GR2 clamp).

**Code site.** src/Cobol.Net.Compiler/CodeGen/Verbs/SequentialIoEmitter.cs#RetryTimes / #RetrySeconds

**Row `GR-14.9.43.4-4` (DIVERGES) — adjudicator / refuter evidence.** GR4: identifier-4 shall be > 0 at the start of execution - a requirement on the program whose run-time consequence is GR8 (a pointer < 1 before a character move is an overflow). The emitter initializes the working pointer from the POINTER item's VALUE (NumericRenderer.Align(AsNum(p0),0)) and the kernel checks pointer < 1 before each move. Probes gr8.cob D (P=-3 -> D-OV, receiver untouched, P unchanged '0L'); the SpecPinned test (P=0 -> OVERFLOW, receiver ABCDEF, P 0000) derives its expected output from GR8/GR6/GR7. Edge: with P<1 and zero characters to move no overflow arises - consistent with GR8's 'before each move of a character'. || REFUTED: DIVERGES — StringEmitter.EmitString reads identifier-4's starting value with an UNCHECKED C# (long) cast of the aligned decimal (`long __strPtrN = (long)(PHI);`). A POINTER item allowed by SR7 (a PIC 9(20)..9(31) integer; legal at 2002+) whose value is above long.MaxValue wraps. The rule requires the value at the start of execution to be the value used. With value 18446744073709551619 (2^64+3) the pointer wraps to 3: the characters are written at position 3, NOT ON OVERFLOW runs, and the pointer is written back as 5. GR4 + GR8 require an overflow with no transfer (the value is greater than the 10 positions of OUT1), and GR6 requires the pointer to be left unchanged. The existing SpecPinned test covers only P=0. UNSTRING (StringEmitter line ~110) builds its pointer the same way and is a sibling.
