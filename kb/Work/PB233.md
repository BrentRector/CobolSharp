---
title: "PB233 — a CALL written with only NOT ON EXCEPTION silently swallows a failed activation, and the same unfiltered catch routes EC-FUNCTION-NOT-FOUND into the ON EXCEPTION phrase that §14.9.4.4 GR3h excludes"
id: PB233
kind: defect
status: landed
severity: MAJOR
area: codegen
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.4.4"]
inventory_rows: ["GR-14.9.4.4-L2.1", "GR-14.9.4.4-L2.2", "GR-14.9.4.4-L2.3"]
tags: [cobolsharp, work, defect]
---

# PB233

**Rows:** `GR-14.9.4.4-L2.1`, `GR-14.9.4.4-L2.2`, `GR-14.9.4.4-L2.3`

a CALL written with only NOT ON EXCEPTION silently swallows a failed activation, and the same unfiltered catch routes EC-FUNCTION-NOT-FOUND into the ON EXCEPTION phrase that §14.9.4.4 GR3h excludes.

Rows: GR-14.9.4.4-L2.1 (§14.9.4.4 3h item 1), -L2.2 (3h item 2), -L2.3 (3h item 3 plus the whole of 3i). ONE emitted catch arm, three rules.

MECHANISM, re-verified live at HEAD. EmitCall computes `hasPhrase = c.OnException is not null || c.NotOnException is not null` (:75), emits `try { <invocation> }`, then the name-filtered `catch (CobolCallException __ce) when (<EnabledProgramNames test>)` (:87 → EmitProgramEcCatch :223-233), and only THEN, when hasPhrase, the UNFILTERED `catch (CobolCallException) { __callErr<id> = true; }` (:88-89). The catch ORDER is right (a filtered arm must precede an unfiltered one or it is dead) and the phrase dispatch that follows is right (`if (__callErr) { on } else { notAlso }` with both phrases; `if (!__callErr) { not }` with only NOT ON; falling through to the end of the statement). Two things about the unfiltered arm are not.

DEFECT 1 — A NOT-ON-ONLY CALL SWALLOWS THE FAILURE (L2.3 half (a), the `silent` flag). Because hasPhrase is true when only NOT ON EXCEPTION is written, the catch is emitted; `c.OnException` is null so no `if (__callErr) { on }` block is written; the NOT body is correctly skipped — and execution then falls to the end of the CALL with no diagnostic, no status and no termination. The identical CALL WITHOUT the NOT ON phrase takes the `!hasPhrase && ecProg.Count == 0` path (:76), emits the bare invocation, and the CobolCallException reaches ProgramTable.RunMain's `catch (CobolCallException cx) { AbnormalTermination(cx.Message) }` — this implementation's documented §14.6.13.1.3 #8 disposition. GR3h has no ON EXCEPTION phrase in that source, so item 2 or item 3 governs and neither permits the failure to be discarded: a phrase that is not a party to the rule changes its outcome. Fix: gate the catch on `c.OnException is not null`, not on hasPhrase.

DEFECT 2 — THE ARM IS FAMILY-UNFILTERED (L2.1 half (i), L2.2 half (B), the `wrong_answer` flag). GR3h item 1 admits only the EC-PROGRAM-* and EC-EXTERNAL-* families into imperative-statement-1, and carries NO checking-enabled qualifier (unlike items 2 and 3) — EnabledProgramNames() already selects exactly those two prefixes one line above, so the rule's own partition is expressed in the file. But ProgramTable.CallProgram also raises a CobolCallException with `notFoundEc == "EC-FUNCTION-NOT-FOUND"` for a user-defined-function locate miss, and that can happen while EVALUATING an argument inside the same try; being a CobolCallException it lands in the unfiltered arm and runs imperative-statement-1. GR3h item 2's second disjunct — 'or if the exception condition is not one of the EC-PROGRAM exception conditions', which requires such a condition to reach the applicable exception processing statements EVEN WHEN a phrase is present — therefore has no implementation at all. Checked and excluded: conditions of other families raised while evaluating arguments (EC-BOUND-SUBSCRIPT and the like) are not CobolCallException and correctly propagate to the enclosing statement's own EC wrapper, so the divergence is scoped to the CobolCallException carrier; and EC-PROGRAM-RESOURCES / EC-PROGRAM-ARG-MISMATCH can reach neither arm, having no raise site beyond their ExceptionCatalog rows (lines 151/158) and being absent from EcBinder.ProgramNames.

WHAT IS ALREADY RIGHT AND MUST NOT REGRESS: the filtered arm sets ExceptionState.Set(__ce.EcName, true) per §14.6.13.1.1, runs the §14.9.49 Format-3 declarative selection, honours `__r >= 0` as RESUME AT and re-throws CobolFatalException when `__r != -2` (§14.6.13.1.3 #5/#7), with RESUME NEXT (-2) falling to the end of the CALL — pinned by conformance:2023/ec_program_ptr_null (>>TURN EC-PROGRAM-PTR-NULL CHECKING ON, USE AFTER EXCEPTION CONDITION, RESUME AT NEXT STATEMENT; .out HANDLED-PTR-NULL / AFTER) and by ExceptionConditionConformanceTests.CallNotFound_Enabled_DeclarativeResumes / CallNotFound_Enabled_NoHandler_FatalTerminates (AssertSpec / AssertFatal, spec-pinned, not legacy-baked). 3i's NOT ON dispatch and EmitPropagationPickup (the activator-side pickup of a callee-staged GOBACK/EXIT PROGRAM … RAISING, honouring RESUME per §14.6.13.1.3 #6) are pinned by conformance:2023/ec_external_format_conflict.

WHAT WOULD CLOSE IT: `c.OnException is not null` on the phrase catch; a family filter (EC-PROGRAM-*/EC-EXTERNAL-* only) on it; routing a non-EC-PROGRAM CobolCallException to EcDispatchExpr regardless of the phrase; and goldens for (a) a NOT-ON-only failing CALL that must terminate the run unit and (b) a UDF-miss raised while evaluating an argument under an ON EXCEPTION phrase.

SPEC REFS as carried by the rows: §14.9.4.4 GR3h items 1/2/3 and GR3i; §14.6.13.1.1; §14.6.13.1.3 #5/#6/#7/#8; §14.9.49 Format 3; Annex E.2 item 1 bullet 3 (the ON OVERFLOW removal — the only CALL entry in Annex E).

## Landed

**2026-09-05 — landed whole; all three rows re-verdicted PARTIAL → CONFORMS (GAP 2992 → 2989).**

Both defects reproduced on a fresh build before any edit, and a THIRD instance of the same mechanism turned
up while probing: `CALL "MID" ON EXCEPTION …` where MID is successfully activated and ITS inner CALL fails
ran the OUTER program's imperative-statement-1 and the run unit continued (exit 0), which §14.9.4.4 GR3i
forbids — "If the program was successfully called … the ON EXCEPTION phrase, if specified, is ignored." That
third case is what showed the root cause is not the arm's FILTER but the arm's SCOPE: the emitted `try` spans
the callee's entire execution, and the CALL site has no way to tell an activation failure from an exception
escaping the callee. So the fix is structural, in three parts.

1. **The activation boundary became explicit.** `ProgramTable.CallProgram` marks the new
   `CobolCallException.ControlTransferred` on anything escaping `inst.Call`, and every emitted arm filters on
   `!ControlTransferred`. The mark is monotone, so a failure deep in a call chain is an activation failure for
   exactly one CALL site and post-transfer for every site above it. For "escaped from `Call`" to BE the
   GR3g test, §14.9.4.4 GR3e's external-conformance check had to move out of the callee body — it is an
   activation-ATTEMPT step that GR3e places before GR3g's transfer — so it is now
   `ICobolProgram.DescribeExternals()` (a default no-op; overridden only by a unit that describes externals),
   which the boundary calls before `Call`. `conformance:2023/ec_external_format_conflict` proves the move
   kept EC-EXTERNAL attributable to the CALL.
2. **Three arms, one per fact GR3h routes on**, replacing the two that conflated them:
   the enabled EC-PROGRAM/EC-EXTERNAL arm (status set, then the phrase or the declaratives — item 1 or item
   2's first disjunct), the enabled non-EC-PROGRAM carrier arm (status set, then the declaratives ALWAYS —
   item 2's SECOND disjunct, which had no implementation), and the un-enabled family arm (the phrase only,
   no status set — item 1 carries no checking-enabled qualifier while §14.6.13.1.1 does). The phrase arms are
   gated on `c.OnException is not null`, never on the NOT phrase: §14.6.13.1.3 #1 admits only "a conditional
   phrase without the NOT phrase". Every catching arm now sets the failure flag, so a declarative that
   RESUMEs NEXT cannot fall into the NOT ON body.
3. **The binder half of defect 2.** `EcBinder.QueryFor(BoundCallProgram)` queried only
   ProgramNames + ExternalNames, so `>>TURN EC-FUNCTION-NOT-FOUND CHECKING ON` produced no `BoundEcChecked`
   wrapper at all and the emitter had nothing to work from. A user-FUNCTION activation (`IsFunction`) now
   also queries `FunctionActivationNames` — §8.4.3.2.4 GR6b → GR6f, "any declarative … associated with that
   exception condition is executed". This is the sibling arm the note's "no implementation at all" was
   really about.

SWEEP. The shared arm emitter has two callers — `EmitCall` and `EmitCancel`. Both got the
`!ControlTransferred` filter; CANCEL keeps `byPhrase: false` because §14.9.5.2 gives it no conditional phrase
and its only condition is raised outside any activation, so the filter is vacuously true there.

WHAT DID NOT HOLD ON TODAY'S TREE. The note's stated repro for defect 2 — "a UDF-miss raised while EVALUATING
an argument inside the same try" — does not reproduce: `UdfBinder.UdfWrapCalls` hoists a function activation
into a `BoundSequence` step BEFORE the CALL, so it never executes inside the CALL's try. The defect is real,
but its live carrier is a UDF miss inside the CALLEE (measured), which is the GR3i scope defect above.

STRUCTURE + DRIFT TEST. `CobolCallException.IsProgramOrExternal` is now the ONE place GR3h item 1's family
partition is written (the emitter's compile-time split and the emitted runtime filter both ask it), and
`CobolCallException.CarriedNames` the one place the carriable name set is. `CallExceptionCarrierDriftTests`
re-derives that set from the carrier files and fails on a stale OR a missing entry — its failure branch was
fired once before the list was restored.

GOLDENS. `tests/conformance/2023/pb233_call_exception_partition.{cob,out}` (enabled) pins the four
clean-running arms; the three abnormal-termination arms are
`ExceptionConditionConformanceTests.CallNotOnExceptionOnly_ActivationFails_TerminatesLikeAPhraselessCall`,
`.CallOnException_IsIgnoredWhenTheFailureIsTheCalleesOwn` and
`.CallOnException_IsNotEnteredByFunctionNotFound`, with
`.CallNotOnExceptionOnly_Enabled_RunsTheDeclarativeAndSkipsTheNotPhrase` and
`.FunctionNotFound_Enabled_RunsTheDeclarative` on the declarative arms.

## Mechanism

src/Cobol.Net.Compiler/CodeGen/Verbs/CallEmitter.cs#EmitCall — `bool hasPhrase = c.OnException is not null || c.NotOnException is not null;` (:75) and the unfiltered `if (hasPhrase) w.Line($"catch (CobolCallException) {{ __callErr{id} = true; }}")` (:88-89), emitted after the correctly-ordered name-filtered EmitProgramEcCatch arm (:87, :223-233)

## Lineage

phase-B orphan (batch 8, §14.9.1–14.9.12). Adjacent and already landed: PB133 wave C1 moved EC-PROGRAM-ARG-OMITTED off this catch (CallAbi.cs's Omitted<T> doc records that 'the old carrier threw CobolCallException unconditionally, which the CALL SITE's catch arm treated as an ACTIVATION failure'), so the omitted-formal half of L2.1(ii)/L2.3(b) is discharged and only the two defects below remain.

---

> Registered by the **defective-row registration wave** (2026-08-31). Every PARTIAL / NOT-IMPLEMENTED /
> DIVERGES row of the P14 traceability inventory now names a live work note in its `inventory_rows`
> frontmatter, and `DefectiveRowCoverageDriftTests` fails the battery if one stops doing so. The
> `inventory_rows` field is the SSOT for which rows this note owns; when the note lands, those rows must
> be re-verdicted in the same change set or the gate goes red.
