---
title: "PB157 — ✅ LANDED 2026-08-29 (the boolean channel's raw-Pic readers → OperandPic; Refs.Probe made the PURE predicate its R30 doc claims; BoundBoolAll.IsAllLiteral un-conflates ZERO from the ALL literal in SR3 AND 8.8.2 rules 4/5 with a replicating positionless store; reroute probes span the receiver list + fail loud; the discriminating mixed-width golden; → PB173 registered). WAS: COMPUTE's boolean family mis-models bit groups and the F1→F2 reroute: a GROUP-USAGE BIT receiver is REJECTED (§13.18.29.1 says treat as elementary usage-bit; the sending side of the same statement accepts it — the binder reads raw Pic where OperandPic is THE ONE reader, the PB79 conversion that never reached six COMPUTE sites); Gr3Width counts a bit group as 0 so the GR3 store width is wrong (CobolBool.Resize truncates 6→3); the reroute's failure arm builds BoundBoolError with NO diagnostic (compiles, throws — the PB68 sweep's sixth site); SR3's ALL-literal ban over-rejects figurative ZERO on the PB51 residue arm and is untested in its actual prohibition"
id: PB157
kind: defect
status: landed
severity: MAJOR
area: binding
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB157

Rows: SR-14.9.8.3-2 (DIVERGES), SR-14.9.8.3-3 (DIVERGES), GR-14.9.8.4-3 (DIVERGES), GR-14.9.8.4-1/-2, FMT-14.9.8.2, findings [COMPUTE statement].

One conversion closes most of it: the six raw-Pic readers (BuildComputeBoolean's receiver test, Gr3Width, BoolExprAllLengthOne, IsBooleanValueOperand, EmitComputeBoolean's width, BindCompute's two reroute probes) move to OperandPic — pb79_group_usage_bit has NO COMPUTE statement, so add the pins. The reroute probes must widen from computeStore(0) to the whole receiver list (mixed `COMPUTE N B = 1` silently louds). SR3: test the PARSE TREE for a written Format-6 ALL <boolean-literal> (as SoleFigurativeZero does) independent of receiver category — the spec decides PB51's left-open reading against the current reject of `COMPUTE B = ZERO`; and nothing anywhere pins the ACTUAL `= ALL B"1"` rejection. GR1/GR2/GR3 test debt: standard-decimal sole-operand arm, the multi-receiver partial-commit (→ PB129's golden shape), a non-uniform-width Format-2 case, multi-target Format 2 + Format-2 END-COMPUTE (never once written).

*(Found by Phase-B batch 8 — §8.8.1 + §14.9.1–14.9.12, 2026-08-29; one adjudicator + one independent refuter per subject. Full forensic evidence: the named rows' `notes` in `tests/version-matrix/traceability-inventory.json`; the raw finding lines in the session scratchpad `pb-batch8/findings.txt`.)*
---

## ✅ LANDED 2026-08-29 — wave gate GREEN; DEVLOG Entry 1393

As scoped, plus what the review fleet (100 agents, 21 confirmed findings) reshaped:

- **Refs.Probe was never pure** — the widened receiver probe exposed the OO property hook registering a
  temp + pending op and the D18 materializer BINDING fragments under a probe (an orphan op made
  OoWrapPropertyOps prepend a GET §8.4.3.9.4 GR2 forbids, or reject a WITH NO GET property). Fixed at the
  INVARIANT: the `_probing` flag threads ResolveImpl; the property hook answers the MODEL item; the
  materializer and the ref-mod/ambiguity diagnostics gate on commit. Every Probe caller is pure now.
- **The SR3 fix was one-arm-of-five**: `ALL ZERO` (Format 1's optional-ALL figurative) and `B-NOT ZERO`
  still rejected, and §8.8.2 rules 4/5 carried the same ZERO/ALL conflation; the SR1a citation was scoped
  to numeric-literal positions. BoundBoolAll.IsAllLiteral (set at the ONE Format-6 site, cleared by the
  B-NOT fold) replaced the parse-tree side channel in every consumer, and a sole positionless RHS
  replicates to the receiver width (§8.3.3.6.4 GR2).
- **The first golden could not fail** (uniform 6-wide items — Gr3Width 0 passed every leg): the W8 leg
  mixes widths and discriminates (§8.8.2 rule 9; 11011000 vs the bug's 11000000).
- **Staged, registered**: a ref-mod slice OF a bit group — newly admissible via rm.Category, but the
  boolean channel sizes in BITS over the packed BYTE substrate — is COBOLNET0899 at the COMPUTE receiver
  and excluded from the boolean-valued classifications; kb/Work PB173 owns the bit-position slice model
  (plus PlaceRenderer's raw-Pic ref-mod pad and the pre-existing MOVE surface).
- My LENGTH §15.50.4 r1 arm change was a NO-OP with a false justification (LengthPositions already answers
  a bit group's positions — pb79 proves it) — reverted. The third stale `{is2002()}?` copy (.g4) corrected.

Residue at its owners: PB173 (bit-group slices), GR-14.6.13.2-1/-2 (incompatible-data wiring),
RefModLen's non-literal length fallback (named in GR-14.9.8.4-3's PARTIAL). Battery #35 accrues from PB157.
