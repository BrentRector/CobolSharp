---
title: "PB980 — UNSTRING's all-or-nothing NATIONAL rule is unenforced: `UNSTRING S DELIMITED BY \",\" INTO NR` with `S PIC X(4)` (alphanumeric) and `NR PIC N(3)` (national) compiles clean and runs at strict `--std 2023` (`NR=[AB ]`), although §14.9.48.3 SR3 says that if any of identifier-1…-5 or literal-1/-2 is of category national, all shall be"
id: PB980
kind: defect
status: landed
severity: MINOR
area: binding/string
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: [PB979]
spec_refs: ["14.9.48.3", "14.9.22.3", "14.9.43.3"]
inventory_rows: []
closes_rows: ["SR-14.9.48.3-3", "SR-14.9.22.3-4"]
tags: [cobolsharp, work, defect]
---

Filed 2026-09-22 by **REGISTRAR #11**, found while re-measuring PB887's finisher lead 1 (which asked for the national
and float INTO receivers to be probed before filing). **Measured on this registrar's own build of main `6871b6e10`**
(0/0).

## Reproduced

- `reg11/u888.cob`: `01 S PIC X(4) VALUE "AB,7".` `01 NR PIC N(3).` `UNSTRING S DELIMITED BY "," INTO NR` → compiles
  clean, prints `NR=[AB ]`. Expected: a compile-time diagnostic (SR3).
- `reg11/u889.cob` (control): the same with `S PIC N(4) VALUE N"AB,7"` and `DELIMITED BY N","` — conforming — runs
  `NR=[AB ]`.
- `reg11/u887.cob` (control): a COMP-2 receiver is refused `COBOLNET1626 … (ISO §14.9.48.3 SR4)` — correct; the
  float arm of PB887's lead cannot arise.

## Rule

- `python scripts/spec/cite.py --check 14.9.48.3 "If any of identifier-1, identifier-2, identifier-3, identifier-4, identifier-5, literal-1, or literal-2 are of category national, then all shall be of category national"` → OK §14.9.48.3 3)

## What the fixer owes

The check sits with SR4's receiver screen in the UNSTRING binder: collect the category of every operand SR3 names
(sending item, each delimiter literal / identifier, each receiver, DELIMITER IN, COUNT IN is numeric and outside the
list) and refuse a mix with one named diagnostic. Sweep STRING's sibling rule (§14.9.43.3) and INSPECT's (§14.9.22.3)
for the same all-national obligation; one negative per verb.

## Landed 2026-09-22 (wave 51 group B)

Re-probed first: `u888` compiled clean and ran `NR=[AB ]`; `u889` (all national) ran `NR=[AB ]`. The rule shape is
shared by three verbs, so it is ONE predicate now — `AllOrNothingClass.Violated` — asked by STRING SR1's second
sentence (its private `Sr1MixesNational` tally is deleted), UNSTRING SR3 (new) and INSPECT SR4 (new — the sweep found
it unenforced: `INSPECT XS REPLACING ALL "," BY N";"` ran `XS=[AB;C]`, and a boolean identifier-1 tallied for "1"
compiled clean). All three report the new **COBOLNET2306** (`pb664-string-national-mix.err` moves from 1626).
INSPECT records each operand's class as `InspectCharOperand` binds it, so every phrase position is covered; SR4
triggers on an elementary item or literal only, and a figurative constant never mixes.

⚖ DETERMINATION D-UN3 (`docs/CONFORMANCE.md` §3): a NUMERIC identifier-4 answers SR3 by its usage — SR4 pairs usage
national with the national operands — because read by category alone SR3 would leave SR4's usage-national numeric
arm unusable. Witnessed by `conformance:2002/pb979_unstring_any_length_national` (national sender into two
usage-national numeric receivers).

Negatives `pb980-unstring-national-mix`, `pb980-inspect-national-mix`. Rows SR-14.9.48.3-3 and SR-14.9.22.3-4
re-verdicted CONFORMS; SR-14.9.43.3-1's `Sr1MixesNational` witness retired to `AllOrNothingClass`.
