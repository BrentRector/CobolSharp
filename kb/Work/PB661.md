---
title: "PB661 — §8.3.2.2 carries three IMPLEMENTOR obligations `docs/CONFORMANCE.md` does not answer (grep for `8.3.2.2`, `externalized` and `formation and mapping` return nothing): (a) whether extended letters may appear in externalized user-defined words, (b) the formation and mapping rules of AS-phrase names, (c) the mapping when no AS phrase is written — PB303 made all three answerable for the first time (the literal's characters verbatim, compared ordinally; the user-defined word verbatim, compared case-insensitively) and `GR-8.3.2.2-L2.2` plus the `-L4.*`/`-L5.*` CALL-CONVENTION rows stay GAP until the §7 determinations are written"
id: PB661
kind: analysis
status: open
severity: MINOR
area: docs/conformance
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: ["8.3.2.2"]
inventory_rows: ["DOC-A.1-219"]
tags: [cobolsharp, work, analysis]
---

# PB661 — OPEN — three determinations the compiler now makes and the document does not state

Found 2026-09-05 by the `PB303` implementer (report §"New defects" D-2). The two-name model PB303 landed fixes the
answers in code — an externalized name is the AS literal's characters verbatim, compared ordinally; with no AS
phrase it is the user-defined word verbatim, compared case-insensitively — but §8.3.2.2's obligations on the
implementor (`OK §8.3.2.2 2)` for the externalization sentence; the three implementor sentences are re-checked when
the rows are written) are answered nowhere in `docs/CONFORMANCE.md`, and the inventory rows that close only on a
documented determination stay open.

## The work (a CONFORMANCE-register job, one change set)

1. Find the three sentences in §8.3.2.2 with `cite.py --find "extended letters"`, `--find "formation"`,
   `--find "mapping"` and validate each with `--check`.
2. Write the three §7 rows (A.1 items — find their numbers with `audit_annex_a1.py` / the A.1 table), each with the
   determination PB303's design D7 states and the goldens PB303 landed as the `Pinned by` witnesses
   (`2002/pb303_as_externalized_name`, `2023/pb303_as_module_name_and_invoke`, the case-sensitivity leg to be added if
   none of the twelve pins it — measure).
3. Re-verdict `GR-8.3.2.2-L2.2` and the `-L4.*` / `-L5.*` rows on the determinations; `AnnexA1RegisterDriftTests`
   and `audit_annex_a1.py --check` hold the rows.

## Registrar-1 (kb/Work PB1522) — rows adjudicated on the `claude/adj-*` branches

Filed 2026-09-24 by REGISTRAR-1 (cloud, branch `claude/adj-registrar-1`). The adjudicators (tree `0caa7d5`) attributed these rows to this note; this note now claims them in `inventory_rows`.

- `DOC-A.1-219` — adjudicated **DIVERGES** on `claude/adj-w2-a1-s3`; NOT recorded — DOC rows wait on owner decision PB1535.

Adjudicator finding (`adj-w2-a1-s3`, harm rejects-legal-source): The lexer's COBOL-word character class is ASCII-only, so no extended letter can appear in any user-defined word, while §5 claims A.4.6 Extended letters; item 219's answer is undocumented

- Repro: PROGRAM-ID. PRÖG. / 01 ÄB PIC X(3) EXTERNAL. / 01 ñame PIC X(3). -> COBOL0307 unexpected 'Ö'/'Ä'/'ñ' at --std 2002 and 2023
- Expected: §8.1.2 rule 4 + Annex B: extended letters permitted in user-defined words; §8.3.2.2: implementor specifies (documents) whether they may appear in EXTERNALIZED words
- Observed: All user-defined words refuse extended letters; nothing documents item 219
- Code site: src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4 (NAME_BODY/NAME_TAIL)

Each row's full evidence (rule text with its `cite.py --check` line, probes, determination) is the record's `notes` in `adjudication/registrar-1/batch-{doc,nondoc}.json` on the registrar branch.
