---
title: "PB1210 — the ANY LENGTH §13.18.2.3 SR1 picture check is a closed list of six spellings (`X`, `N`, `1`, `X(1)`, `N(1)`, `1(1)`), so a one-occurrence picture written `PIC X(01)` / `N(001)` / `X(constant-name)` is refused COBOLNET1542 while the DYNAMIC LENGTH twin in the same file accepts every count-1 spelling"
id: PB1210
kind: defect
status: open
severity: MINOR
area: binding/data-division
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB514", "PB1211", "PB1167"]
spec_refs: ["13.18.2.3", "13.18.40.3"]
inventory_rows: ["SR-13.18.2.3-1"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14d** from adjudication batch d1-14d (the adjudicators' pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probes under `scratchpad\adj-d1\out13\probe\`). The given repro was re-run once on registrar 14d's Debug build of `071ff04ea` (main after trains 58/59) and reproduces unless stated otherwise.

# PB1210 — OPEN

## Measured

**any-length-clause** — ANY LENGTH SR1 PICTURE check is a closed list of six literal spellings (X, N, 1, X(1), N(1), 1(1)) instead of the count-1 shape its DYNAMIC LENGTH sibling uses, so a one-occurrence picture written X(01) / N(001) / X(constant-name=1) is refused as not 'one instance'

- **Repro:** Contained program ALP1SUB with `LINKAGE SECTION. 01 L PIC X(01) ANY LENGTH. PROCEDURE DIVISION USING L.` called with PIC XXX and PIC X(8) arguments (probe/any-length-clause/p1.cob) -> COBOLNET1542 'requires a PICTURE whose character-string is exactly one instance ... PICTURE X(01)'.
- **Expected (spec):** 13.18.40.3 SR6: the parenthesized integer 'indicates the number of consecutive occurrences of the symbol'; X(01) is one occurrence of X, satisfying 13.18.2.3 SR1. The program compiles and prints LEN=03 VAL=XYZ then LEN=08 VAL=WXYZABCD (13.18.2.4 GR1b).
- **Observed:** DataBinder.cs ANY LENGTH shape check: `norm is not ("X" or "N" or "1" or "X(1)" or "N(1)" or "1(1)")`. The DYNAMIC LENGTH check ~40 lines below already uses ^[XN](\(0*1\))?$ with a comment that 'a count of 1 in ANY spelling is still one instance'. Two-arm dispatch: one arm generalized, the other not. Fix shape: ONE shared 'single-occurrence picture' predicate (symbol set as parameter), ideally over the parsed PICTURE (symbol + resolved count, so a constant-name count is covered too), used by both clauses, with a drift test.
- **Code site:** src/Cobol.Net.Compiler/Binding/DataBinder.cs (ANY LENGTH declaration-shape validation, SR1 arm)
- **Clause (adjudicator):** 13.18.2.3 SR1; 13.18.40.3 SR6

## The rules — every citation re-run through `cite.py --check` by registrar 14d

- `SR-13.18.2.3-1`: `cite.py --check 13.18.2.3 "A PICTURE clause shall be specified for the subject of the entry"` → OK  §13.18.2.3 1)  (Syntax rules)

## Rows claimed

- `SR-13.18.2.3-1` — batch 14d verdict DIVERGES

## What the fixer owes (sibling sweep)

ONE shared single-occurrence predicate over the PARSED picture (symbol + resolved repetition count, so a constant-name count is covered) used by ANY LENGTH SR1 and DYNAMIC LENGTH SR1 (§13.18.19.3), with a drift test that both clauses call it; PB514 owns the sibling permitted-set lists in the same file.
