---
title: "PB989 — §12.3.8.4 GR10 a) / GR11 a) take a program or function prototype's details from a definition \"specified PREVIOUSLY in the same compilation group\", but BuildProgramDetailsTable and the user-function table ignore source order, so a definition that FOLLOWS the REPOSITORY entry is used as if it preceded it"
id: PB989
kind: defect
status: open
severity: MINOR
area: binder/repository
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB894"]
spec_refs: ["12.3.8.4"]
inventory_rows: ["GR-12.3.8.4-10", "GR-12.3.8.4-11", "GR-11.5.4-2"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-22 by **REGISTRAR #12** from `leads-w48-w52.md` (wave 48 group U, worktree wf_ffdefe6a-659-4). Code-read lead; the observable difference is compile-time only (which details are checked), so the fixer measures it with a negative golden whose ONLY defect is a mismatch against a FOLLOWING definition. Every citation below re-run through `cite.py --check` on this tree.

# PB989 — OPEN

## The rule
`OK  §12.3.8.4 10) a)` — "if the externalized name of the program prototype is the externalized name of a program
definition specified previously in the same compilation group, the details are taken from that program definition …;
otherwise," b) takes them from a program-prototype definition in the group, then the external repository. GR11 a) is the
same sentence for function prototypes.

## Expected vs observed
A REPOSITORY `PROGRAM P` entry whose program definition comes LATER in the compilation group must take its details from
b)/c) (a prototype definition, or the repository), not from the later definition. The details tables are built once
over the whole group, order-blind.

## Code site
`BuildProgramDetailsTable` and the user-function details table (binder) — keyed by name only.

## What the fixer owes
Record each definition's position in the compilation group and resolve GR10/GR11 a) against definitions strictly
before the referencing source element; fall through to b)/c) otherwise. Measure with a two-program group where the
callee follows the caller and its USING list disagrees with a prototype definition that precedes both.

## Extended 2026-09-24 by REGISTRAR 15d (batch 15d, pinned `071ff04ea`)

Rows added to `inventory_rows`: `GR-11.5.4-2`.

Batch 15d (function-id-paragraph): BuildUserFunctionTable merges a function PROTOTYPE and a function DEFINITION by WORD (`if (defs.ContainsKey(name)) continue;  // GR11a`), but §12.3.8.4 GR11 a) matches by EXTERNALIZED name and only a definition specified PREVIOUSLY — so a prototype `FIDDEF AS "NOPE"` is replaced by a following definition FIDDEF (externalized FIDDEF) and the call activates the wrong function where EC-FUNCTION-NOT-FOUND is owed. Same table, same source-order defect plus the key.

## The rule (every citation re-run through `cite.py --check`)

> However, literal-1, if specified, is the name of the function prototype that is externalized to the operating environment.

`OK  §11.5.4 2)  (General rules)`

### Row `GR-11.5.4-2` — PARTIAL (editions 2002, 2014, 2023 (Format 2 function prototype, COBOL-2002 introduction; not in COBOL-85))

cite.py --check 11.5.4 "However, literal-1, if specified, is the name of the function prototype that is externalized to the operating environment." -> OK §11.5.4 2). Two halves: (a) function-prototype-name-1 names the prototype (the word a FUNCTION reference / REPOSITORY FUNCTION entry resolves), (b) literal-1 is the externalized name. Code: MakeUnit sets externalized = literal-1 for pid OR fid AS phrase (prototype included); BuildUserFunctionTable keys a lone prototype by u.Name and records p.ExternalizedName in UserFunctionSignature.Externalized, which the emitter uses as the activation key. Both halves probed: p1 prototype FIDPA AS "FIDREAL" + REPOSITORY FUNCTION FIDPA + FUNCTION FIDPA(7) activates the definition FUNCTION-ID. FIDDEF AS "FIDREAL" (different word, same externalized name) -> P=000049 at 2002 and 2023 (word resolves = half a; activation by literal = half b). Axis flip p3: same program with the prototype's AS removed -> EC-FUNCTION-NOT-FOUND for 'FIDPC', proving the activation really keys on literal-1 and not coincidence. No existing golden pins a prototype carrying AS literal-1 (udf_prototype has none; pb303_as_self_prototype_call is the PROGRAM-ID arm), so test-ref empty (test-needed). || REFUTED: PARTIAL — Half (b), literal-1 as the prototype's externalized name, is dropped whenever a function DEFINITION with the same WORD exists in the compilation group. BuildUserFunctionTable merges prototype and definition by word ('if (defs.ContainsKey(name)) continue;  // GR11a'), but §12.3.8.4 GR11 a) matches by EXTERNALIZED name and only for a definition specified PREVIOUSLY. So a prototype FIDDEF AS "NOPE" is replaced by a following definition FIDDEF, whose externalized name is FIDDEF, and the call activates the wrong function when it should target "NOPE" (EC-FUNCTION-NOT-FOUND). The adjudicator's p1/p3 probes only used a prototype word different from the definition word, so they never reached this arm.
