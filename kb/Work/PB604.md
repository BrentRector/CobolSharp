---
title: "PB604 — two adjudication-lane follow-ups from PB235's CLOSE landing: row GR-14.9.27.4-19's CONFORMS prose places every physical file in §14.9.6.4 GR2 category (b) 'single-unit' while the determination now written in docs/CONFORMANCE.md §7 (A.1 item 24) and the '07' the compiler reports force category (a) 'non-unit' (its verdict stands a fortiori, its wording and its stale ⚠ note do not); and DOC-A.1-24's inventory row is still blank although §7 now carries the determination and `audit_annex_a1.py --check` accepts it — a discharged documentation obligation the burn-down cannot see"
id: PB604
kind: analysis
status: open
severity: MINOR
area: process/traceability
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: ["14.9.6.4", "14.9.27.4", "9.1.13.2"]
inventory_rows: []
tags: [cobolsharp, work, analysis]
---

# PB604 — OPEN — a determination written down makes two rows read differently

Found 2026-09-05 by the `PB235` implementer (report §"New defects", items 2 and 3), which wrote Annex A.1 item
24's determination — the physical-file category placement: sequential files in GR2 category (a) Non-unit,
relative and indexed in (d) — into `docs/CONFORMANCE.md` §7 and left the two rows below to the adjudication
lane, since neither is in PB235's `inventory_rows`. Re-derived on this tree for this note.

## 1. `GR-14.9.27.4-19` — right verdict, wrong ground

The row is CONFORMS and its notes read "COBOL.NET's storage medium is single-unit mass storage … every physical
file has exactly ONE reel/unit", which is GR2 category **(b)** ("A sequential file that is entirely contained on
one unit"). Under (b), Table 14's `CLOSE WITH NO REWIND` cell is b,c — no symbol g, hence no '07' — and
`CLOSE UNIT` is e,f, a rewind. The compiler reports '07' for both, which only the **(a) Non-unit** column
produces (`cite.py --check 9.1.13.2 "references a physical file on a non-reel/unit medium"` → OK §9.1.13.2 6)),
and §7 A.1 item 24 now states (a). The row's conclusion holds a fortiori under (a); its prose and its ⚠ note
("docs/CONFORMANCE.md §2 row 33-34 states the opposite" — no longer true, §2 was rewritten by PB235) should be
re-derived and re-recorded through `record_verdicts.py` with the determination cited.

## 2. `DOC-A.1-24` — discharged, not recorded

The row is `{"verdict": "", "code-location": "", "state": "GAP"}`. §7 now carries the determination
(`DOC-A.1-24`), `audit_annex_a1.py --check` accepts it (42 → 43 required items documented), and the evidence
exists: `unit:CloseTable14Tests.Category_PlacesEverySupportedConnectorKind`,
`conformance:85/pb235_same_record_area_close`, `conformance:2002/pb235_close_unit_locks`,
`conformance:85/pb141_close_forms`. A DOC row also pays the kind's anchor and observability costs (a
`code-location` under `src/Cobol.Net.` — `FileConnector.Category` / `CloseTable14.cs` qualify), so an
adjudicator records it CONFORMS with those refs and the row closes.

Both are one `record_verdicts.py` batch for the next lane-3 registrar or golden lander touching §14.9; neither
needs a compiler change.
