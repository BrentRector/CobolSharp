---
title: "PB742 — §12.4.5.2 SR8/SR9 are unenforced: a Format-1 key clause (RECORD KEY / ALTERNATE RECORD KEY) on a NON-indexed file compiles clean and the key is then silently ignored, and because §12.4.5.10.3 GR6 makes an OMITTED ORGANIZATION clause record sequential, the shape that hits it is the ordinary `SELECT F ASSIGN … RECORD KEY IS K.` with no ORGANIZATION clause at all — MEASURED on main"
id: PB742
kind: defect
status: landed
severity: MINOR
area: binder
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["12.4.5.2", "12.4.5.10.3"]
inventory_rows: ["SR-12.4.5.2-8", "SR-12.4.5.2-9"]
tags: [cobolsharp, work, defect, files, file-control-entry]
---

# PB742 — LANDED — the file control entry's FORMAT rules are the ones nobody wrote down

Reported 2026-09-06 by the `PB699` fix-lane implementer (report
`…\a9d94900…\scratchpad\reports\PB699-report.md`, "New defects", item 1), re-measured here rather than
inherited.

## Measured on main

Probes in `E:\Temp\claude\E--CobolSharp\49b17747-ce58-4386-8c49-ecb2ce644f8d\scratchpad\pb740\`, run against
the checked-in `src/Cobol.Net.Cli/bin/Debug/net10.0/cobol.exe` (**no rebuild**; built 2026-09-06 03:11 PDT, whose
newest commit was `c34f01ad`). PB699's own fix has **not** landed — `src/Cobol.Net.Compiler/Binding/
FileControlKeyRules.cs` does not exist on main (`89555b09`) — so the binary and main agree about this code.

| probe | file control entry | result |
|---|---|---|
| `pb742a.cob` | `SELECT SQF ASSIGN TO "…" ORGANIZATION IS SEQUENTIAL RECORD KEY IS SQ-KEY` | **compiles clean**, runs, `OPEN=35` |
| `pb742b.cob` | `SELECT SQF ASSIGN TO "…" RECORD KEY IS SQ-KEY` — *no ORGANIZATION clause at all* | **compiles clean**, runs, `OPEN=35` |

Zero diagnostics of any severity in both. (`'35'` is only the absent input file; it is the proof that binding and
emission both completed — the point is that no compile-time diagnostic named the illegal clause.) The second row
is the one that matters: it is not a contrived spelling, it is the shape a program written without an
ORGANIZATION clause has, and §12.4.5.10.3 GR6 is what puts it on the wrong side of SR8.

## What the standard prints

Both rules re-derived with `cite.py --check`; neither clause number was taken from the report:

- `--check 12.4.5.2 "Format 1 shall be specified only for an indexed file"` → **OK §12.4.5.2 8)** —
  *"Format 1 shall be specified only for an indexed file. The associated file description entry shall not be a
  sort-merge file description entry."* Format 1 of §12.4.5.1 is the file control entry format carrying RECORD KEY
  and ALTERNATE RECORD KEY.
- `--check 12.4.5.2 "Format 2 shall be specified only for a relative file"` → **OK §12.4.5.2 9)** — the twin, for
  Format 2 and RELATIVE KEY. It is in the same note because a rule set with one member enforced is the shape
  where the missing member is hardest to see (`feedback_model_the_rule_shape_not_one_case`), and because
  `KeyedValidateFile` today enforces §12.4.5.13.3's RELATIVE KEY rules **only after** checking
  `file.Organization != FileOrganization.Relative` — i.e. it *skips* the very entry SR9 forbids.
- `--check 12.4.5.10.3 "When the ORGANIZATION clause is not specified, sequential organization with the RECORD
  SEQUENTIAL phrase is implied"` → **OK §12.4.5.10.3 6)**. This is why the common shape is caught: the compiler
  already knows it (`DataBinder.cs:891` quotes GR6 verbatim, and `:918` renders *"record sequential by default,
  the ORGANIZATION clause being omitted (ISO §12.4.5.10.3 GR6)"* into a diagnostic), so nothing has to be
  inferred — the organization is bound to record sequential and the key clause is bound beside it, and the two
  facts are never compared.

## Why it is harm and not tidiness

`under_rejects` + `silent`. The user's key semantics are not *misapplied*, they are **absent**: the RECORD KEY
name resolves, is stored on the `FileModel`, and then nothing reads it, because every key consumer is on a keyed
path a sequential file never enters. A program written against a mistaken belief that `RECORD KEY` makes a file
keyed compiles, runs, and reads its records in arrival order with no indication whatever that half its file
control entry was inert. It is MINOR only because the mistake is a program's own, and no correct program's
output changes.

## The code, at current line numbers on `89555b09`

There is **no** site that compares a key clause against the organization. The two places that could:

- `DataBinder.cs:854` captures `RECORD KEY` unconditionally
  (`(file.RecordKeyName, file.RecordKeyQualifiers) = KeyReference(rk);`) with no organization test, and
  `DataBinder.ResolveFiles` resolves it at `:1420` the same way.
- `KeyedIoBinder.KeyedValidateFile` (`src/Cobol.Net.Compiler/Binding/Procedure/Verbs/KeyedIoBinder.cs:407`)
  holds every key-clause syntax rule the compiler does enforce — §12.4.5.12.3 SR1 / §12.4.5.6.3 SR1 (no OCCURS),
  §12.4.5.13.3 SR1/SR2/SR3 — and it is reached **only from a keyed verb**, which is PB699's whole subject. A
  sequential file never has one, so even a rule written there would not fire for this defect. **SR8/SR9 are
  entry rules and have to be enforced where the entry is bound.**

## The fix shape

⛔ **Land it as two more rows in `FileControlKeyRules`, not as two `if`s.** PB699's fix (train 16 row 6, in
flight — `status: open` on `kb/Work/PB699.md`, `FileControlKeyRules.cs` not yet on main) moves the file control
entry's key rules out of `KeyedValidateFile` into a table evaluated when the ENTRY is bound, keyed today on the
clause and the file's organization. SR8 and SR9 are the same shape one column over — a row that fires on the
CLAUSE's presence and the organization's *disagreement* rather than on a keyed verb — so once that table exists
this is two rows and a diagnostic message, and the generalization the report describes ("keyed on the CLAUSE's
presence rather than on the file's organization — a small generalization of the table's `Organization` column")
is the whole of the work. **This note is therefore blocked in practice on PB699 landing**, not by a
`blocked_by` dependency but by the fact that landing it first would build the second mechanism PB699 exists to
remove (`feedback_one_mechanism_per_job`). Diagnostic code: take it from `pwsh scripts/session-probe.ps1`'s
next-free, never a guessed number — and the message must name the *organization* the entry actually has,
including the "by default, the ORGANIZATION clause being omitted" case, or the common shape produces a
diagnostic the user cannot act on.

⛔ **Sweep the external corpus BEFORE it lands.** This rule newly REJECTS source that compiles today, and the
population it rejects is exactly the sloppy-but-widespread `SELECT … RECORD KEY` with no ORGANIZATION clause.
`feedback_edition_gate_sweep`'s reasoning applies unchanged even though this is not an edition gate: run the
GnuCOBOL differential and the external corpus (`tests/external/gnucobol/tests/`) and count the newly-refused
programs before the commit, not after. If a real population is hit, the answer is a dialect-strictness decision
(the version × strictness model — `docs/COBOLNET_DESIGN.md:183`, *"(version × strictness) model applies
throughout"*), not a quiet deferral — CLAUDE.md rule 3.

**Rows claimed:** `SR-12.4.5.2-8` and `SR-12.4.5.2-9`, both GAP today. They are claimed together because one
screen closes both and a note claiming only SR8 would leave a rule whose enforcement site had already been
written (`feedback_a_dead_lookup_is_also_unverified`).

## Sweep

- **The rest of §12.4.5.2's format rules.** SR11 is already known to the binder — `DataBinder.cs:898-899` quotes
  it verbatim, and `--check 12.4.5.2 "Format 3 shall be specified only for a sequential file or a report file"`
  → **OK §12.4.5.2 11)** — though it is quoted there to DERIVE where LINE SEQUENTIAL sits, not enforced; SR7 is
  genuinely enforced (`DataBinder.cs:1443-1475`, COBOLNET1811). SR8 and SR9 are the two members of that family
  with no site at all — the *dead* members of a set whose other members are live, which is the shape this
  repository has been bitten by repeatedly.
- **The `SORT-MERGE` half of both rules** — *"The associated file description entry shall not be a sort-merge
  file description entry"* is a second obligation inside SR8 and SR9, over the SD rather than the organization.
  Read the whole printed rule before writing the row; do not close SR8 on its first sentence.
## Landed 2026-09-06

Reproduced first on this worktree's own `cobol.exe`, built on PB699's branch (train 16 row 6) merged onto
`0a24276f`. The note's two shapes held exactly, and probing the rest of the rule family found **three more**
before choosing where the rows go:

| probe | file control entry | before | after |
| --- | --- | --- | --- |
| a | `ORGANIZATION IS SEQUENTIAL` + `RECORD KEY` | silent | COBOLNET0863 §12.4.5.2 SR8 |
| b | **no ORGANIZATION clause** + `RECORD KEY` | silent | SR8, naming the GR6 omission |
| c | `ORGANIZATION IS SEQUENTIAL` + `RELATIVE KEY` | silent | COBOLNET0863 §12.4.5.2 SR9 |
| d | **SD** + `RECORD KEY` | silent | COBOLNET1900 §12.4.5.2 SR8 sentence 2 |
| e | **SD** + `ORGANIZATION IS INDEXED`, no key clause | silent | COBOLNET1900 |
| f | `ORGANIZATION IS RELATIVE` + `RECORD KEY` | silent | SR8 |
| g | `ORGANIZATION IS LINE SEQUENTIAL` + `ALTERNATE RECORD KEY` | silent | SR8 |
| h | `ORGANIZATION IS SEQUENTIAL` + file-level `COLLATING SEQUENCE` | COBOLNET1582 citing §12.4.5.7.1 | COBOLNET1582 citing §12.4.5.2 SR8 |

**The estimate was wrong, and the correction is the fix.** The note said "once that table exists this is two rows
and a diagnostic message". It is seven rows, a new column TYPE, a new role and a new code, because SR8 and SR9
cannot be expressed in the table PB699 shipped:

1. **The `Organization` column had to become a SET.** It was one `FileOrganization` meaning two different things
   at once — the format that CARRIES a clause, and the files a rule is IN FORCE over. Those coincide for every
   rule stated *inside* a format and are OPPOSITE here. `FileKinds` is a flags enum with one member per
   §12.4.5.10.3 organization plus `SortMerge`; SR8's rows are screened on `AnyOrganization & ~Indexed`. A scalar
   where the rules name a SET is `feedback_model_the_rule_shape_not_one_case` exactly.
2. **The SECOND sentence of both rules needed a subject that is not an operand.** *"The associated file
   description entry shall not be a sort-merge file description entry"* is broken by probe **e**, which writes no
   key clause at all — the format is specified by the ORGANIZATION clause alone. `FileKeyRole.Entry` contributes
   exactly one operand with a null name, so a rule about the ENTRY is the same kind of row as a rule about an
   operand and there is still no second arm.
3. **`Screen`'s `if (file.IsSortMerge) return;` had to go.** PB699 wrote it with a comment observing that an SD
   writing `ORGANIZATION INDEXED` breaks SR8 — a rule the table did not then carry. It does now, so the blanket
   veto would have silenced exactly the two rows written for the case. The sort-merge kind is a member of the
   set instead, and `FileControlKeyRuleDriftTests.EverySortMergeRow_QuotesTheSortMergeSentenceOfSection12452`
   asserts both directions (every sort-merge row quotes that sentence; no other row carries the kind).

**Diagnostic codes.** COBOLNET1900 for sentence 2 of both rules — ONE code, because it is literally one sentence
printed twice, and a code of its own because its subject is the file description entry and its remedy is a
different edit than a key clause's. Sentence 1 rides COBOLNET0863, whose subject already includes §12.4.5.2
(SR10 ships under it). COBOLNET1901 stays FREE.

**The COLLATING SEQUENCE operand stays where it is, with its citation repaired.** §12.4.5.1 prints the
collating-sequence-clause in Format 1 and in no other format, so it is a third operand of SR8 — and it was
already refused, by `DataBinder.ResolveFileCollating` (COBOLNET1582), citing §12.4.5.7.1, the clause's
descriptive General paragraph, which states no obligation. It is not moved into the table because the same test
is also the guard that stops the rest of that method: splitting a guard from its report would leave the
resolution running on an entry it has already refused. The inventory row `SR-12.4.5.2-8` names both sites, and
the class doc says so out loud rather than leaving a second unlabelled copy of one rule.

**Over-rejection sweep — the note's ⛔, and it is ZERO.** `scratchpad/PB742/sweep.py` over `tests/` (which
contains the 459-program NIST corpus): **2208 files, 1611 file control entries, 230 RECORD KEY / 29 ALTERNATE
RECORD KEY / 94 RELATIVE KEY clauses and 263 `ORGANIZATION INDEXED`** — **0** SR8 hits, **0** SR9 hits, **0**
numeric indexed keys. Positive control: the same script finds 5 / 1 / 2 hits in the probe directory, so it can
fire. No NIST program writes a key clause outside its format, so no edition or dialect decision arises.
⚠ `tests/external/` holds only `gnucobol-verdict-baseline.tsv` (verdicts, no sources): the GPL corpus is absent
from a fresh worktree and the differential was NOT run here — it belongs to the batch gate.

**Rows:** `SR-12.4.5.2-8` and `SR-12.4.5.2-9` → CONFORMS. **GAP 2788 → 2784 of 4,348** with PB743's two.

Goldens: `conformance:negative/pb742-record-key-on-sequential-file`, `-record-key-no-organization-clause`,
`-alternate-key-on-line-sequential-file`, `-relative-key-on-sequential-file`,
`-indexed-format-on-sort-merge-entry`, `-relative-format-on-sort-merge-entry`,
`-collating-sequence-on-sequential-file` (all `reject-at: 85 2002 2014 2023`), and
`conformance:85/pb742_legal_entry_formats_85`, the over-rejection twin — a sequential file with no key clause, a
group prime key, an alternate key, a relative key in WORKING-STORAGE, and a LEGAL Format-4 sort-merge entry that
is actually SORTed through, which is the half the deleted early return used to protect by construction.
