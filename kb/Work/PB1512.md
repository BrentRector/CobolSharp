---
title: "PB1512 — a host I/O failure inside a sequential READ or WRITE is never mapped to an I-O status: SequentialConnector.Read/Write (and the StreamReader/StreamWriter they drive) have no catch and neither FileRegistry nor CobolFile wraps the verb, so a System.IO.IOException escapes the generated statement and kills the run unit where §9.1.13.6 1) requires '30' — the OPEN, CLOSE and DELETE FILE arms of the same dispatch already map to '30'/'37'/'61'"
id: PB1512
kind: defect
status: open
severity: MAJOR
area: runtime/io
wrong_answer: false
crashes: true
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1192", "PB603"]
spec_refs: ["9.1.13.6", "9.1.13.1"]
inventory_rows: ["GR-9.1.13.6-1", "DOC-A.1-103"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 15d** from lane-3 adjudication batch **15d** (adjudicated on the pinned tree `071ff04ea`; everything that landed since — Registrar 14a/14b/14c — is register-only, so no code change touches this mechanism). Every citation below was re-run by the registrar through `cite.py --check`.

# PB1512 — OPEN

## The rule (every citation re-run through `cite.py --check`)

> 1) I-O status = 30. A permanent error exists and no further information is available concerning the input-output operation.

`OK  §9.1.13.6 1)  (Permanent error condition with unsuccessful completion)`


## Finding — `i-o-status-p1` (crashes; rows `GR-9.1.13.6-1`)

**Mechanism.** Host I/O failures inside the sequential READ/WRITE bodies are never mapped to an I-O status: SequentialConnector.Read/Write (and the StreamReader/StreamWriter they drive) have no catch, and neither FileRegistry nor CobolFile wraps the verb, so a System.IO.IOException escapes the generated statement and kills the run unit. The OPEN (FileConnector.Open), CLOSE (FileConnector.Close, PB140), DELETE FILE (ForDeleteFileFailure) arms all map to '30'/'37'/'61' - the READ/WRITE arms of the same dispatch were never fixed.

**Repro.** out/probe/iostat-30/p30.cob: SELECT F ASSIGN TO "p30.dat" ORGANIZATION SEQUENTIAL FILE STATUS ST; FD F 01 FR PIC X(4). OPEN INPUT F (-> 00), ACCEPT W (pause), meanwhile another process opens p30.dat (FileAccess.Read, FileShare.ReadWrite) and calls FileStream.Lock(0,12); then READ F AT END CONTINUE END-READ; DISPLAY ST. Observed: 'Unhandled exception. System.IO.IOException: The process cannot access the file because another process has locked a portion of the file' at System.IO.RandomAccess.ReadAtOffset, process exit -532462766, nothing displayed.

**Expected from the spec.** §9.1.13.6 1): the READ is unsuccessful with I-O status '30' ('A permanent error exists and no further information is available'), set before any exception processing (§9.1.13.1); the FILE STATUS item receives '30', applicable USE procedures run, and then the implementor's documented fatal-status action (A.1 item 103) applies - never an unhandled .NET exception.

**Observed / reasoning.** MEASURED for READ with the pinned compiler. WRITE: by reading - SequentialConnector.Write buffers through StreamWriter and ReleaseRecord calls _writer.Flush() with no catch, so a device-full or locked-region failure during WRITE takes the same path (not measured). Keyed organizations persist at OPEN/CLOSE, which are mapped.

**Code site.** src/Cobol.Net.Runtime/IO/SequentialConnector.cs#Read; src/Cobol.Net.Runtime/IO/SequentialConnector.cs#Write; src/Cobol.Net.Runtime/IO/SequentialConnector.cs#ReleaseRecord; contrast src/Cobol.Net.Runtime/IO/FileConnector.cs#Open (catch IOException -> '30') and #Close

## What the fixer owes

Wrap every connector verb (READ, WRITE, REWRITE, DELETE, START on every organization — sweep, this is the two-arm shape) at ONE dispatch seam so a host IOException becomes '30' (or the more specific status a documented determination assigns — PB1192's medium-exhaustion → '34' boundary is the WRITE sibling) and the §9.1.13 exception path (EC-I-O-PERMANENT-ERROR, USE/WHEN, implicit fatal) runs instead of a .NET crash. Golden: p30 (a file truncated by another process between OPEN and READ); a unit test injecting an IOException through a fake stream for WRITE.

## Registrar-1 (kb/Work PB1522) — rows adjudicated on the `claude/adj-*` branches

Filed 2026-09-24 by REGISTRAR-1 (cloud, branch `claude/adj-registrar-1`). The adjudicators (tree `0caa7d5`) attributed these rows to this note; this note now claims them in `inventory_rows`.

- `DOC-A.1-103` — adjudicated **DIVERGES** on `claude/adj-a1-w1-s4`; NOT recorded — DOC rows wait on owner decision PB1535.

Adjudicator finding (`adj-a1-w1-s4`, harm silent): The fatal-I-O-status action (item 103) is made in code (continue with checking off; abnormal termination exit 1 with EC-I-O checking on) but documented only as a parenthetical in the SORT/MERGE bullet, and FileStatus.cs's '71' summary states the opposite of the default behaviour

- Repro: f2.cob: SELECT F ASSIGN "nonexist-zz.dat" (no FILE STATUS, no USE); OPEN INPUT F; READ F -> continues, exit 0. f6.cob: LINE SEQUENTIAL WRITE of X"41014243" with no FILE STATUS -> status 71 (f7 shows 71), continues, exit 0. With >>TURN EC-I-O CHECKING ON: 'abnormal run-unit termination: EC-I-O-PERMANENT-ERROR (fatal): I-O status 35', exit 1
- Expected: §9.1.13.1: 'the implementor determines what action is taken ... may either continue or terminate' + A.1 103 required + documented
- Observed: No DOC-A.1-103 row; FileStatus.LineRecordInvalidChar doc comment says 'a program without a FILE STATUS item or a USE declarative terminates here rather than writing a corrupt line' — false in a default build
- Code site: src/Cobol.Net.Runtime/IO/FileStatus.cs#LineRecordInvalidChar; src/Cobol.Net.Runtime/Exceptions/ExceptionCatalog.cs#IsFatalIoStatus; docs/CONFORMANCE.md §7

**A second host-IOException escape, not re-verified (refuter of DOC-A.1-24, `adj-a1-w1-s1`).** An INDEXED `OPEN OUTPUT` on a nearly full device crashes the run unit with an unhandled `IOException` thrown from `KeyedConnector` `AbandonOpen` → `ReleaseFileLock`, outside OPEN's '30' mapping (probes `/tmp/adj/refute/d24/p1..p5.cob`, a tmpfs filled to capacity). §9.1.13.6 1) makes an OS-level failure the permanent error '30' (`cite.py --check 9.1.13.6 "30"` → OK §9.1.13.6 1)). Same mechanism as this note (a host failure not mapped to an I-O status), different verb and organization — the fixer's sibling sweep must cover every connector's OPEN/abandon path. Item 103 (the fatal-I-O-status action) is filed here for its documentation half: FileStatus.cs's '71' summary states the opposite of the default behaviour.

Each row's full evidence (rule text with its `cite.py --check` line, probes, determination) is the record's `notes` in `adjudication/registrar-1/batch-{doc,nondoc}.json` on the registrar branch.
