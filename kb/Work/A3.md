---
title: "A3 — NUMERIC-SEMANTICS DEPTH AUDIT"
id: A3
kind: analysis
status: landed
severity: MAJOR
area: analysis
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: [8, 8.8.1, 8.8.1.2, 8.8.1.4.2, 8.8.1.4.4, 8.8.1.5.2]
tags: [cobolsharp, work, analysis]
---

# A3 — NUMERIC-SEMANTICS DEPTH AUDIT

- NUMERIC-SEMANTICS DEPTH AUDIT — the intermediate-results model vs §8.8.1 end-to-end: 8 ROUNDED modes × ops, native-vs-standard intermediates, size-error boundaries, long/Int128 crossovers, float↔fixed
- Arithmetic is COBOL's heart; wave-grown tests ≠ a model-level audit; numeric bugs silently corrupt data
- A verified conformance map of the numeric engine + hand-derived oracle values; GAP rows into the inventory
- **CAMPAIGN A** — inside P14 after Step 0 (consumes/extends inventory rows); REQUIRED before the P15 oracle deletion
- **PENDING — ✅ UNBLOCKED 2026-08-03.** ⚙ **The denominator gap that blocked it is CLOSED for this audit's subject:** §8.8.1's five rule-bearing clauses were adjudicated and admitted (21 GR rules; denominator 3,861 → 3,882), so A3 can now record findings against rows that exist. **Run it.** ⚠ One of the 21 is already CONFORMS (GR-8.8.1.2-6, exponentiation, measured across all four editions and both reachable arithmetic modes) — the other 20 are open GAP rows and are A3's starting worklist. The history, for context: this audit's subject had **ZERO rows in the rule catalog**. Every §8 section the catalog holds has the `8.x.y.2/.3/.4` shape because the extractor keys on the *General format* / *Syntax rules* / *General rules* headings, and §8.8.1's rules are numbered `1)`…`n)` under ordinary prose headings. **21 top-level rules + 5 lettered sub-rules were missing** (§8.8.1.2 7+3 · §8.8.1.4.2 3 · §8.8.1.4.4 4+2 · §8.8.1.5.2 3 · §8.8.1.5.4 4), and all 21 are now in the catalog. Running A3 against a denominator that omits its own subject would produce a burn-down that cannot reach the rules it is auditing — and D13 makes zero GAP the definition of v1.0, so those rules could never BE a GAP. That is fixed (PB29's content-keyed detector + the admission above). ⚠ PB28 is the proof it was not academic: §8.8.1.2 r6a/r6c WERE violated on the native path — `0 ** 0` returned 1 and `-2 ** 0.5` returned 0, neither raising — and both are fixed and pinned as of 2026-08-03.
