---
title: "PB502 — The `IN alphabet-name-1` phrase of a condition-name VALUE range is recognized by the grammar and then dropped on the floor (one root, 3 findings across data-description-entry-p3, value-clause-p1, value-clause-p3)"
id: PB502
kind: defect
status: open
severity: MAJOR
area: frontend/grammar
wrong_answer: true
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.16.4", "13.18.63.2", "13.18.63.3", "13.18.63.4"]
inventory_rows: ["GR-13.16.4-3", "FMT-13.18.63.2", "SR-13.18.63.3-26", "GR-13.18.63.4-18"]
tags: [cobolsharp, work, defect]
---

# PB502

## Mechanism — data-description-entry-p3
The `IN alphabet-name-1` phrase of a condition-name VALUE range is recognized by the grammar and then dropped on the floor. CobolData.g4's valueClause carries `(IN IDENTIFIER)?` at the end of the condition-name arm, but BindCondition reads only `range.valueClauseOperand(0)` and `(1)` and stores the pair as raw text; no alphabet is recorded on Condition88, and RenderCondition88 renders the membership test with the program's default collating argument. A range written IN a non-native alphabet is therefore evaluated in the native sequence -- silently, and in BOTH directions (a range that should be non-empty tests false, and one that should be empty tests true).

### Expected, from the standard
13.18.63.4 GR18: 'The manner of determining the range of values specified by the THROUGH phrase is described in 14.7.8, THROUGH phrase.' 14.7.8 rule 2: 'When the IN alphabet-name phrase is specified, the collating sequence used for range evaluation is the collating sequence defined by that alphabet ... The range of values includes all values collating at the starting value and all successive ascending values in the applicable collating sequence up to and including all values collating at the ending value. When the value of literal-1 is greater than the value of literal-2 in the collating sequence in effect at runtime, the EC-RANGE-INVALID exception condition is set to exist, and ... execution proceeds as if the range of values were empty.' Under ALPHA-REV the characters Z..A occupy ordinals 1..26, so P is 11th, M is 14th and D is 23rd. p19 ("P" THRU "D") is the ascending range 11..23, which contains M -> A-RNG=T (observed F). p20 ("D" THRU "P") is inverted, 23 down to 11 -> EC-RANGE-INVALID and an empty range -> A-RNG=F (observed T). Both answers are wrong, in opposite directions.

```
$ python scripts/spec/cite.py --check 13.16.4 "The manner of determining the range of values specified by the THROUGH phrase is described in 14.7.8, THROUGH phrase"
  FAIL: §13.16.4 exists but does NOT contain 'The manner of determining the range of values specified by the THROUGH phrase is described in 14.7.8, THROUGH phrase'
$ python scripts/spec/cite.py --check 13.18.63.4 "The manner of determining the range of values specified by the THROUGH phrase is described in 14.7.8, THROUGH phrase"
  OK  §13.18.63.4 18)  (General rules)
```

### Observed
Read: `(IN IDENTIFIER)?` at CobolData.g4 valueClause L623 with no listener; BindCondition's range arm keeps only the two operand texts; Condition88 has no alphabet member; RenderCondition88 passes ctx.CollateArg / ctx.NatCollateArg, i.e. the program-level sequence. Measured: the three probe runs above, whose control isolates the phrase as the only difference. NOTE ON OWNERSHIP: the primary rows are 13.18.63.4 GR18 and 14.7.8 (and 13.18.63.3 SR31, the unchecked syntax rule on which alphabets are legal here), all outside this subject; GR-13.16.4-3 is listed because 'range of values associated with the condition-name' is the constituent this defect corrupts, and it is one of the reasons that row is recorded PARTIAL rather than CONFORMS.

### Repro
Pinned cobol.exe --std 2023 --run.
E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/ddep3/p19alpha.cob and p20alpha.cob, identical apart from the range endpoints:
  SPECIAL-NAMES. ALPHABET ALPHA-REV IS "Z" THRU "A".
  01 WS-A PIC X VALUE "M".
     88 A-RNG VALUE "P" THRU "D" IN ALPHA-REV.      *> p19  -> observed A-RNG=F
     88 A-RNG VALUE "D" THRU "P" IN ALPHA-REV.      *> p20  -> observed A-RNG=T
control p21alpha.cob, the same entry with the IN phrase removed (`VALUE "D" THRU "P"`) -> A-RNG=T, byte-identical to p20: the alphabet makes no difference to the answer, which is the whole point.

### Code site
src/Cobol.Net.Frontend/Grammar/Core/CobolData.g4#valueClause L623 (`(IN IDENTIFIER)?`, parsed and unread); src/Cobol.Net.Compiler/Binding/DataBinder.cs#BindCondition (~L2117-2141, the range arm); src/Cobol.Net.Compiler/Binding/Model/Condition88.cs (no alphabet field); src/Cobol.Net.Compiler/CodeGen/Emit/ConditionRenderer.cs#RenderMembershipTest (~L400, the collate argument). A parsed-but-unread grammar slot is the reproducible shape here -- worth a mechanical check that every optional grammar sub-rule has at least one reader.

## Mechanism — value-clause-p1
value-p1-F2-IN-alphabet-name-1-parsed-into-a-bare-token-and-never-read-by-the-binder

### Expected, from the standard
With `ALPHABET ALPH1 IS "C" "B" "A"` the named sequence orders C < B < A, so `88 CN VALUE "C" THRU "A" IN ALPH1.` covers C, B and A, and a conditional variable holding "B" shall make the condition TRUE. The range shall be determined under alphabet-name-1, not under the native sequence.

```
$ python scripts/spec/cite.py --check 13.18.63.2 "IN ALPH1.` covers C, B and A, and a conditional variable holding"
  FAIL: §13.18.63.2 exists but does NOT contain 'IN ALPH1.` covers C, B and A, and a conditional variable holding'
$ python scripts/spec/cite.py --check 13.18.63.2 "` the named sequence orders C < B < A, so `88 CN VALUE"
  FAIL: §13.18.63.2 exists but does NOT contain '` the named sequence orders C < B < A, so `88 CN VALUE'
  ⛔ REGISTRAR: no quotation in this finding validated verbatim against §13.18.63.2. The MECHANISM below is measured and stands, but the QUOTATION is the finding's paraphrase — quote the clause and re-run `--check` before it reaches a code comment, a golden or the DEVLOG (CLAUDE.md rule 1).
```

### Observed
FALSE -- the range is evaluated under the native sequence, where "C" THRU "A" is empty. MEASURED at the pin: the program prints OUT (probe d_in_alpha_range). This is not a missing facility: the SAME alphabet under `OBJECT-COMPUTER. X PROGRAM COLLATING SEQUENCE IS ALPH1.` makes `IF "B" < "A"` take its true branch (probe vcp1f/alph.cob prints B-LT-A), so the collating machinery is live and only the VALUE clause's operand is dropped at the parse->bind boundary. SILENT: no diagnostic at any edition. THIS BLOCKS SR26 TOO -- SR26 b's antecedent is that the runtime collating sequence is KNOWN, and the phrase that makes it known is the one discarded; a landing that adds SR26's comparison without also reading alphabet-name-1 would reject `88 C VALUE "C" THRU "A" IN ALPH1.` as a reversed range although it conforms. The two must land together. (For contrast, the OTHER discarded-looking Format-3 operand is NOT silent: `WHEN SET TO FALSE IS literal-4` parses and `SET CN TO FALSE` throws CobolNet.Runtime.NotImplementedCobolFeatureException at run time -- probe e_set_false_literal4 -- so it is a loud staged GAP, not a wrong answer.)

### Repro
Pinned worktree E:/CobolSharp/.claude/worktrees/lane3-pin-89044aa0 (89044aa0); its own cobol.exe at src/Cobol.Net.Cli/bin/Debug/net10.0/cobol.exe. Write:
       IDENTIFICATION DIVISION.
       PROGRAM-ID. DA1.
       ENVIRONMENT DIVISION.
       CONFIGURATION SECTION.
       SPECIAL-NAMES.
           ALPHABET ALPH1 IS "C" "B" "A".
       DATA DIVISION.
       WORKING-STORAGE SECTION.
       01 CV PIC X.
       88 CN VALUE "C" THRU "A" IN ALPH1.
       PROCEDURE DIVISION.
           MOVE "B" TO CV.
           IF CN DISPLAY "IN" ELSE DISPLAY "OUT" END-IF.
           STOP RUN.
then `cobol.exe da1.cob --std 2023 --run` -> prints OUT; expected IN. Probe tree: scratchpad/lane3/out-b3/probe/vcp1d/d_in_alpha_range/.

### Code site
src/Cobol.Net.Frontend/Grammar/Core/CobolData.g4:623 -- `(IN IDENTIFIER)?`, an unlabelled bare token with no rule of its own; src/Cobol.Net.Compiler/Binding/DataBinder.cs#BindCondition:2110-2158 -- the sole Format-3 consumer, which iterates `value.valueItem()` and never touches `value.IN()` or the IDENTIFIER beside it, so nothing downstream can recover the name. Contrast src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs (the alphabet/locale resolver that already exists) and the working PROGRAM COLLATING SEQUENCE path.

## Mechanism — value-clause-p3
The Format 3 `IN alphabet-name-1` phrase is parsed and then discarded. The grammar carries it (`(IN IDENTIFIER)?` at CobolData.g4:539) but `DataBinder.BindCondition` walks only `value.valueItem()`, `Condition88` has no alphabet member, and `ConditionRenderer.RenderMembershipTest` takes `ctx.CollateArg` — the PROGRAM collating sequence — unconditionally. So the range is evaluated in the wrong sequence, and because nothing resolves the name, an alphabet-name that was never declared is also accepted.

### Expected, from the standard
§13.18.63.4 GR18 defers to §14.7.8: "When the IN alphabet-name phrase is specified, the collating sequence used for range evaluation is the collating sequence defined by that alphabet", and "The range of values includes all values collating at the starting value and all successive ascending values in the applicable collating sequence up to and including all values collating at the ending value." In REV the ordinals are Z=1, Y=2, X=3, so "Z" THRU "X" is ascending and contains "Y" — Y-TRUE. The observed Y-FALSE is the answer for the NATIVE sequence, where "Z" > "X" makes the range inverted and (per the same clause) empty. Separately, §13.18.63.3 SR31 — "Alphabet-name-1 may be specified only when the literals specified in the THROUGH phrase are of class alphanumeric or national ... alphabet-name-1 shall reference an alphabet that defines an alphanumeric collating sequence" — requires the name to be resolved and class-checked, so NOSUCHALPHABET must be a diagnostic.

```
$ python scripts/spec/cite.py --check 13.18.63.4 "The range of values includes all values collating at the starting value and all successive ascending values in the applicable collating sequ..."
  FAIL: §13.18.63.4 exists but does NOT contain 'The range of values includes all values collating at the starting value and all successive ascending values in the applicable collating sequence up to and including all values collating at the ending value'
$ python scripts/spec/cite.py --check 14.7.8 "The range of values includes all values collating at the starting value and all successive ascending values in the applicable collating sequ..."
  OK  §14.7.8 2)  (THROUGH phrase)
```

### Observed
Measured, with a control that flips the axis the subject holds fixed (feedback_probe_the_shape_the_subject_hides): naming the SAME alphabet through PROGRAM COLLATING SEQUENCE proves the collating machinery, the alphabet table and the range renderer all work, so the only missing link is the binder never carrying the per-VALUE-clause alphabet. FIX SHAPE: give Condition88 an alphabet-name member filled in BindCondition, resolve it through the existing alphabet resolver (with the SR31 class check and the undeclared-name diagnostic), and have RenderMembershipTest choose that alphabet's collate argument over ctx.CollateArg — the per-range collate argument is already a parameter of the emitted CobolString.Compare / ThruMember calls, so no new runtime surface is needed. Note the rest of GR18 CONFORMS: numeric ranges (§14.7.8 rule 1) and the native-sequence inverted-range EC-RANGE-INVALID sentence both measured correct (probes gr18_numeric, gr18_ec). A national THROUGH range is separately staged loud (COBOLNET0899 at DataBinder.cs:2124, cited to SR31) and should land in the same change set, since it is the same missing carrier seen from the national side.

### Repro
E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/vcp3/gr18_inalpha2.cob at `--std 2023`: `SPECIAL-NAMES. ALPHABET REV IS "Z" "Y" "X" ... "A".` and `01 V PIC X. 88 V-ZX VALUE "Z" THRU "X" IN REV.`; `MOVE "Y" TO V` then `IF V-ZX` prints Y-FALSE (expected Y-TRUE). CONTROL E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/vcp3/ctl_alphabet.cob: the identical ALPHABET named in `OBJECT-COMPUTER. XX PROGRAM COLLATING SEQUENCE IS REV.` prints A-GE-B and turns the same `88 V-ZX VALUE "Z" THRU "X"` TRUE for "Y" — so REV is honoured everywhere except in the VALUE clause's own IN phrase. UNDER-REJECTION half, E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/vcp3/gr18_inparse.cob: `88 V-ZX VALUE "Z" THRU "X" IN NOSUCHALPHABET.` compiles clean, no diagnostic.

### Code site
src/Cobol.Net.Compiler/Binding/DataBinder.cs:2105-2156 (BindCondition), src/Cobol.Net.Compiler/Binding/Model/Condition88.cs:11-25, src/Cobol.Net.Compiler/CodeGen/Emit/ConditionRenderer.cs:410-419, src/Cobol.Net.Frontend/Grammar/Core/CobolData.g4:539

## Sibling sweep the fixer owes
Every bug here is a pattern (CLAUDE.md rule 4). Before closing this note, sweep the sibling arms named in the mechanism above and add the ones that fail to `inventory_rows`.

## Provenance
Lane-3 adjudication batch b3 (subjects: data-description-entry-p3, value-clause-p1, value-clause-p3), adjudicated against the compiler pinned at 89044aa0 and RE-PROBED on the registrar's worktree cut from 6e7caa91 (a 1942-program sweep of `out-b3/probe/` plus targeted re-probes); the finding reproduces on today's tree.

## Repro programs
`E:\Temp\claude\E--CobolSharp\a9d94900-79fe-4a38-89f0-921976861543\scratchpad\repros\PB502\` — ddep3, vcp1d, vcp3. The implementer's opening move is to rerun them against a freshly built `cobol.exe`.

