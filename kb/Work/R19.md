---
title: "R19 — a reserved phrase word as an intrinsic argument compiles with zero diagnostics and throws at run time"
id: R19
kind: defect
status: open
severity: MINOR
area: intrinsics
wrong_answer: false
crashes: true
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["4.2.2", "8.9"]
tags: [cobolsharp, work, defect]
---

# R19 — a reserved phrase word as an intrinsic argument compiles with zero diagnostics and throws at run time

> R00 ledger-audit orphan (2026-08-08): F18 of `PHASE-B-15.32-15.44-findings.md`, probe-verified OPEN
> against the R10-landed tree (`5312996a`). The probe programs are reproducible from the finding text.

PROBE (2026-08-08): `COMPUTE R = FUNCTION EXP10(LEADING)` — zero diagnostics, then
`NotImplementedCobolFeatureException: intrinsic argument 'LEADING'`. IntrinsicBinder#BindArgOperand's
fnArgPhraseWord arm returns `BoundOperandError` with NO ctx.Edition.Error call, one line below the OMITTED
arm that does report (COBOLNET1544). Generic to every catalogued function that does not take the phrase word.
FIX: report at the arm (once, not per function); the NonNumericOperand `?? BoundOperandError` fallback on the
next line is the same shape and gets the same treatment.
