---
title: "PB506 — THE REPORT-SECTION (FORMAT 4) VALUE LANE HOLDS ONE OPERAND AND ROUTES IT THROUGH THE SOURCE CLAUSE'S IMPLICIT MOVE (one root, 2 findings across value-clause-p2, value-clause-p4)"
id: PB506
kind: defect
status: landed
severity: MAJOR
area: codegen/verbs
wrong_answer: true
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.18.63.3", "13.18.63.4", "13.15.4", "13.18.53.3", "13.18.53.4"]
inventory_rows: ["SR-13.18.63.3-35", "SR-13.18.63.3-34", "GR-13.18.63.4-23"]
closes_rows: ["GR-13.18.63.4-23", "SR-13.18.63.3-34", "SR-13.18.63.3-35"]
tags: [cobolsharp, work, defect]
---

# PB506

## Mechanism — value-clause-p2
THE REPORT-SECTION (FORMAT 4) VALUE LANE HOLDS ONE OPERAND AND ROUTES IT THROUGH THE SOURCE CLAUSE'S IMPLICIT MOVE. Two defects, one lane. (1) src/Cobol.Net.Compiler/Binding/DataBinder.Reports.cs:161 declares `FieldValueSource(string Raw)` -- a single operand -- built at :543 from :440's `ExtractValue(value)`, which returns the first valueItem's GetText. The Format-4 operand LIST is never modelled, so no count can be compared with the repetition count (SR35) and no operand after the first can be distributed (13.18.63.4 GR23). (2) src/Cobol.Net.Compiler/CodeGen/Verbs/ReportWriterEmitter.cs:129-159 FieldImage turns that FieldValueSource into `move.ConvertSource(source, f.PrintItem)` -- the conversion path of the SOURCE clause's implicit MOVE -- under a comment citing "ISO 13.18.53.4 GR1; a VALUE item per 13.18.63". 13.18.53.4 GR1 is about identifier-1 of a SOURCE clause and says nothing about a VALUE literal: a real clause answering a different question. A MOVE edits; a VALUE initialization does not, so an alphanumeric-edited printable item gets its literal EDITED.

### Expected, from the standard
SR35 (13.18.63.3): "If the VALUE clause has more than one operand, the entry shall be a repeating entry or shall be subordinate to a repeating entry. The number of operands of the VALUE clause shall be equal to the number of repetitions of the repeating entry or the same number multiplied by the number of repetitions of any number of successive repeating entries at higher levels than the repeating entry." (cite --check OK) 13.18.63.4 GR23 fixes the behaviour of the legal case: "If the VALUE clause has more than one operand, successive operands are assigned to successive repeating printable items, horizontally and then vertically, as applicable, in that hierarchy. If no further operands remain, assignment begins again from the first operand." (cite --check OK) -- so v35b shall print `AAA BBB CCC`, and v35c (3 repetitions, 2 operands) and v35a (a non-repeating entry with 3 operands) shall be diagnosed. SR34 makes SR11 apply to Format 4, and SR11's second sentence is "Editing characters in a picture character-string for an alphanumeric-edited or national-edited data item do not cause editing of the initial value when the data item is initialized." (cite --check OK); 13.18.63.4 GR21 delegates GR7, "Each literal is aligned in the associated data item in accordance with 14.6.8 ... except that initialization is not affected by a JUSTIFIED clause and no editing takes place." (both cite --check OK) -- so `PIC XXBXX VALUE "AB CD"` shall print `AB CD`, not `AB  C`.

```
$ python scripts/spec/cite.py --check 13.18.63.3 "If the VALUE clause has more than one operand, the entry shall be a repeating entry or shall be subordinate to a repeating entry. The number..."
  OK  §13.18.63.3 35)  (Syntax rules)
```

### Observed
MEASURED at --std 2023. v35b is LEGAL SOURCE and prints the first operand three times: the second and third literals never reach the model, so the wrong answer is silent and the illegal-count case is indistinguishable from the legal one. The editing defect is localized to the report lane by the working-storage control (w11a), which stores `AB CD` unedited -- the divergence is FieldImage's MOVE routing, not the VALUE decoder. The pre-2002 legs could not be measured: the multiple-COLUMN vehicle is introduction-gated to 2002 (COBOLNET0900) and the OCCURS vehicle for a repeating report entry is refused COBOLNET0899 (d35_report_occurs), so multiple COLUMN is today the only reachable repeating shape and it is the one that answers wrongly. The working-storage lane rejects its analogous shape (a bare multi-literal Format-1 VALUE, COBOLNET1585, w1a) -- the report lane simply has no counterpart.

### Repro
cd E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/value-clause-p2 && E:/CobolSharp/.claude/worktrees/lane3-pin-89044aa0/src/Cobol.Net.Cli/bin/Debug/net10.0/cobol.exe v35b_report_multicol_ok.cob --run --std 2023 -o v35b.dll
   source (LEGAL -- a repeating entry with a matching operand count):
           RD R1. 01 DT TYPE DETAIL. 05 LINE 1.
              10 COLUMN 1 5 9 PIC XXX VALUE "AAA" "BBB" "CCC".
   MEASURED: rc=0; the report file v35b.txt contains `AAA AAA AAA`.
Illegal count accepted: v35c_report_multicol_count (3 columns, 2 operands) rc=0, same `AAA AAA AAA`, no diagnostic.
Non-repeating entry with 3 operands: v35a_report_multi_value rc=0, prints `AAA`.
Editing defect: E:/CobolSharp/.claude/worktrees/lane3-pin-89044aa0/src/Cobol.Net.Cli/bin/Debug/net10.0/cobol.exe v34a_report_value_edited.cob --run --std 2023 -o v34a.dll
   source: 10 COL 1 PIC ZZ9 VALUE 12.   10 COL 10 PIC XXBXX VALUE "AB CD".
   MEASURED: rc=0; v34a.txt line = ` 12      AB  C`.
   CONTROL in working-storage: E:/CobolSharp/.claude/worktrees/lane3-pin-89044aa0/src/Cobol.Net.Cli/bin/Debug/net10.0/cobol.exe w11a_ws_alnum_edited.cob --run --std 2023 -o w11a.dll
   -> `AE=[AB CD]` and `NE=[ 12]`, i.e. the Format-1 lane is correct.

### Code site
src/Cobol.Net.Compiler/Binding/DataBinder.Reports.cs:161 (FieldValueSource -- one operand), :439-440 and :543-544 (built from ExtractValue); src/Cobol.Net.Compiler/CodeGen/Verbs/ReportWriterEmitter.cs:129-159 (FieldImage -> move.ConvertSource) and :162-172 (ValueOperand)

## Mechanism — value-clause-p4
A Format-4 VALUE clause with more than one operand is collapsed to a single string before it ever reaches the report model, and that one string is then emitted into every repetition of the entry. DataBinder.ExtractValue takes `value.valueItem().FirstOrDefault()` and, for an ordinary literal list, returns `item.GetText()` -- the concatenated token text of ALL the operands, quote characters included. The report binder calls that same helper (DataBinder.Reports.cs:440) and stores the result in `FieldValueSource(string Raw)`, a model record with room for exactly one scalar, so there is nowhere for a per-repetition operand list to live even if ExtractValue were fixed. The emitter then renders that one value once per COLUMN in the entry's column list. The Format-1 sibling of the very same glue IS caught -- DataBinder.cs:2404-2409 raises COBOLNET1585 when a data-item VALUE has more than one operand -- so the check exists and the report arm was simply never given it: the classic two-arm dispatch. Because 13.18.63.3 SR35 already requires a multi-operand entry to be a repeating entry (its own row is already recorded DIVERGES in the sibling part), the correct shape is a per-repetition operand list on the field model, cycled by GR23's own wrap-around sentence -- the structure that makes the next repetition vehicle (report-group OCCURS, multiple LINE) automatic rather than a second special case.

### Expected, from the standard
13.18.63.4 GR23: 'If the VALUE clause has more than one operand, successive operands are assigned to successive repeating printable items, horizontally and then vertically, as applicable, in that hierarchy. If no further operands remain, assignment begins again from the first operand.' (a) columns 1/6/11 = 'AAA' / 'BBB' / 'CCC'. (b) columns 1/6/11 = 'AAA' / 'BBB' / 'AAA'. (c) is not conforming source under 13.18.63.3 SR35 (a multi-operand VALUE requires a repeating entry) and should be rejected, never stored.

```
$ python scripts/spec/cite.py --check 13.18.63.4 "If the VALUE clause has more than one operand, successive operands are assigned to successive repeating printable items, horizontally and th..."
  OK  §13.18.63.4 23)  (General rules)
```

### Observed
MEASURED, compiling CLEAN at STRICT --std 2023 with no diagnostic: (a) prints 'AAA  AAA  AAA' -- the second and third operands are silently discarded and the first is replicated. (b) prints 'AAA  AAA  AAA'. (c) prints 'XX"YY' -- the raw text `"XX""YY"` was decoded with the doubled quote read as an escaped quote, which is what proves the collapse is a GetText() glue rather than a first-operand selection; in the X(3) cases the glued 'AAA"BB"C' is simply truncated to 'AAA' by the PICTURE, which is why they read as a replication.

### Repro
Pinned cobol.exe --std 2023 --run. (a) `03 COLUMNS ARE 1 6 11 PIC X(3) VALUE "AAA" "BBB" "CCC".` in a TYPE DE group -> probe/gr23rw. (b) fewer operands than repetitions: `... VALUE "AAA" "BBB".` -> probe/gr23wrap. (c) the glue made visible: `03 COLUMN 20 PIC X(12) VALUE "XX" "YY".` -> probe/gr23glue. Repeated at --std 2002/2014 in probe/edrw2002, probe/edrw2014.

### Code site
src/Cobol.Net.Compiler/Binding/DataBinder.cs:2975-2992 (ExtractValue); src/Cobol.Net.Compiler/Binding/DataBinder.Reports.cs:439-440 and :161 (FieldValueSource(string Raw)) and :543; src/Cobol.Net.Compiler/CodeGen/Verbs/ReportWriterEmitter.cs:134. The caught sibling: src/Cobol.Net.Compiler/Binding/DataBinder.cs:2404-2409 (COBOLNET1585).

## Sibling sweep the fixer owes
Every bug here is a pattern (CLAUDE.md rule 4). Before closing this note, sweep the sibling arms named in the mechanism above and add the ones that fail to `inventory_rows`.

## Provenance
Lane-3 adjudication batch b3 (subjects: value-clause-p2, value-clause-p4), adjudicated against the compiler pinned at 89044aa0 and RE-PROBED on the registrar's worktree cut from 6e7caa91 (a 1942-program sweep of `out-b3/probe/` plus targeted re-probes); the finding reproduces on today's tree.

## Repro programs
`E:\Temp\claude\E--CobolSharp\a9d94900-79fe-4a38-89f0-921976861543\scratchpad\repros\PB506\` — edrw2002, edrw2014., gr23glue., gr23rw., gr23wrap., value-clause-p2. The implementer's opening move is to rerun them against a freshly built `cobol.exe`.

## 2026-09-13 — LANDED (both mechanisms, and the SOURCE-clause sibling arm)

**The root cause was ONE model decision and ONE routing, and each of them had a twin the standard writes out
twice.** `ReportFieldModel` held a single `ReportFieldSource` where ISO defines an operand LIST distributed over
the entry's repetitions, and `ReportWriterEmitter.FieldImage` pushed a format-4 VALUE operand through the SOURCE
clause's implicit MOVE. Both are now fixed at the one place the rule belongs, and both sibling arms with them.

**(1) THE OPERAND LIST.** `ReportFieldModel.Source` became `ReportFieldModel.Sources` — one entry per written
operand, a single-operand clause being a one-element list — with `SourceAt(rep)` the ONE per-repetition reader
(§13.18.63.4 GR23 / §13.18.53.4 GR4: successive operands to successive repeating printable items, wrapping to
the first when none remain). The index is the repetition ORDINAL, never a count of the items actually placed,
which makes GR23's last sentence ("If any of the printable items are suppressed … operands are nevertheless
assigned to them") true by construction. `DataBinder.ExtractValueOperandList` reads every format-4 operand
through the SAME literal-position reader (`RawValueOperandText`) the format-1 single-operand path uses, instead
of `ExtractValue`'s multi-operand tail — whose `item.GetText()` glue was the mechanism: `VALUE "XX" "YY"`
reached the emitter as the raw text `"XX""YY"` and the decoder read the doubled quote as an escaped one, so a
single-column entry printed `XX"YY` and an `X(3)` entry printed the truncated `AAA` three times.
`ExtractValue` is unchanged — its glue is exactly what its format-1 caller wants, since that caller REJECTS the
shape (COBOLNET1585).

**(2) A VALUE IS AN INITIALIZATION, NOT A MOVE.** `FieldImage`'s `FieldValueSource` arm now returns
`DataEmitter.ValueImageOf` → `ValueInitializer.InitializerFrom`, the ONE §13.18.63 recipe the working-storage
lane uses; `InitializerFor` is now `InitializerFrom(item, item.ValueAt(subs))`, so there is one body and two
entry points rather than two implementations. That single routing discharges three separately-measured wrong
answers at once, because §13.18.63.4 GR21→GR7/GR8 and §13.18.63.3 SR34→SR11 exclude precisely the three
transforms a MOVE applies: `PIC XXBXX VALUE "AB CD"` printed `AB  C` and now prints `AB CD`;
`PIC X(5) JUSTIFIED RIGHT VALUE "AB"` printed `   AB` and now prints `AB   `; `PIC ZZZ9 BLANK WHEN ZERO
VALUE "0000"` printed four spaces and now prints `0000`. The 2023 golden prints each report item beside its
IDENTICAL working-storage twin, so the two lanes cannot drift apart unseen. `ReportWriterEmitter.ValueOperand`
— a second, divergent raw-text→BoundOperand decode chain that existed only to feed `ConvertSource` — is
DELETED, with a comment standing where it was.

**(3) THE SIBLING SWEEP: the SOURCE clause carries the SAME TWO RULES.** §13.18.53.3 SR6 is §13.18.63.3 SR35
verbatim for SOURCE and §13.18.53.4 GR4 is §13.18.63.4 GR23's twin — the two-arm shape this repo reproduces most often — and
the SOURCE arm was worse than wrong: `reportSourceClause : SOURCE IS? dataReference` admitted ONE operand and
no `SOURCES` spelling, so `03 COLUMNS ARE 1 6 PIC X(3) SOURCES ARE WS-A WS-B.` was a raw parse error
(COBOL0307) on conforming source and SR6 had nothing to screen. The grammar is now
`(SOURCE | SOURCES) (IS | ARE)? dataReference+`, `SOURCES` is a lexer token and a `cobol-words.json` row (a
user word below 2002, §8.9-funnel-gated at and above it), the operands ride the same `Sources` list, and the
2002 introduction gate is the construct row `report-multi-source-2002` (`VisitReportSourceClause`).

**The SR35/SR6 screen is ONE reader.** `ScreenRepeatingOperandCount` computes the admissible operand counts as
the PREFIX PRODUCTS of the repetition chain `RepetitionChain` reads off the report-entry scope stack — the
entry's own count per §13.15.4 GR3 (`EntryRepetitions`) then each enclosing entry's, the scope stack having
grown a `Reps` field for exactly this. A non-repeating frame contributes a factor of 1, so sentence 1 ("shall be
a repeating entry or shall be subordinate to a repeating entry") is exactly "the full product is 1" and needs no
separate case, and the rule generalizes without edit when the report-group OCCURS or multiple LINE vehicles land
— which is why the chain is a structure and not a hand-written `columns.Count` test. Each clause keeps its own
diagnostic so the reader is sent to the subclause actually written: **COBOLNET2012** (VALUE) and **COBOLNET2013**
(SOURCE).

**Goldens.** `conformance:2023/pb506_report_value_operand_list` is the content proof — the VALUE list, the SOURCE
list, and all four VALUE-initialization shapes, each beside its working-storage twin.
`conformance:2002/pb506_report_operand_lists_2002` and `conformance:2014/pb506_report_operand_lists_2014` are
the acceptance proofs at the other two editions the repetition vehicle exists at; they do NOT read the report
file back because that needs `ORGANIZATION IS LINE SEQUENTIAL`, itself a COBOL-2023 introduction (the kb/Work
PB688 precedent). Three negatives — `negative/pb506-report-value-not-repeating`,
`negative/pb506-report-value-operand-count`, `negative/pb506-report-source-operand-count` — reject at all four
editions. `ReportOperandListDriftTests` asserts GR23's wrap-around DIRECTLY on the model, because that sentence
has no reachable COBOL source while the higher-level repetition vehicles stage loud (a rule with no population
is a rule nothing contradicts), and keeps both the MOVE out of the VALUE lane and the operand list to one
indexer.

**Rows re-verdicted** (batch `pb506-report-operand-list`, GAP 2533 → 2530): SR-13.18.63.3-35,
SR-13.18.63.3-34 and GR-13.18.63.4-23 all DIVERGES → CONFORMS.

**⚠ GR-13.18.63.4-21 is NOT this note's row and was NOT flipped, but this landing discharges its three measured
divergences.** That row (owned by kb/Work PB562 + PB565) records the JUSTIFIED, the alphanumeric-edited and the
BLANK-WHEN-ZERO legs of GR21's imports as DIVERGES on exactly the mechanism fixed here, and all three are now
MEASURED correct and pinned by `conformance:2023/pb506_report_value_operand_list`. Its remaining residue is
GR21's GR9 leg (a report-group OCCURS, COBOLNET0899) and its GR1 float leg (no float printable item — a
non-DISPLAY printable item is refused). Its owner should re-verdict it.

**Two things in the note did NOT hold on today's tree, both in the fixer's favour.** (a) The note's repro
directory (`…/repros/PB506/`) was empty and the lane-3 probe tree was gone, so the repros were rebuilt from the
note's quoted source shapes; every one of them reproduced exactly as recorded. (b) The note says the SR11/GR7
editing defect is "localized to the report lane by the working-storage control" — true, and the same control
proved the JUSTIFIED and BLANK-WHEN-ZERO legs, which the note does not mention (they are PB562/PB565's).

