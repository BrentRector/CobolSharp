---
title: "PB876 — the NATIONAL leg of §13.18.63.4 GR7 and GR8 has no population at any edition: a national-edited PICTURE (PIC N with B, 0 or /) is staged loud on COBOLNET0899, so the one category each rule's second exception is about cannot be built"
id: PB876
kind: defect
status: open
severity: MINOR
area: binding/data-division
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.18.63.4", "13.18.63.3"]
inventory_rows: ["GR-13.18.63.4-7", "GR-13.18.63.4-8"]
tags: [cobolsharp, work, defect]
---

# PB876

## Mechanism — the missing VEHICLE, not a missing rule

`GR-13.18.63.4-7` and `GR-13.18.63.4-8` are both recorded PARTIAL, and both records say the same thing about
why: the rule's NATIONAL leg has no vehicle. A national-edited item — `PICTURE N` with a `B`, `0` or `/`
insertion character — draws

```
COBOLNET0899: national-edited data (PICTURE N with B 0 /) is recognized but not yet implemented
```

so the population each rule's second exception is about cannot be constructed, at any edition. Both rules are
otherwise MEASURED and correct in Format 1:

- **GR7**, "Each literal is aligned in the associated data item in accordance with 14.6.8, Alignment and
  transfer of data into data items, except that initialization is not affected by a JUSTIFIED clause and no
  editing takes place." The JUSTIFIED exception is byte-measured (`PIC X(5) JUSTIFIED RIGHT VALUE "AB"` is
  byte-identical to its unjustified twin, at all four editions), and the no-editing exception is measured
  against a MOVE control into the identical picture (`PIC XXBXX VALUE "ABCDE"` gives `ABCDE` where the MOVE
  gives `AB CD`). What is unmeasurable is the same exception on a NATIONAL-edited picture.
- **GR8**, "When a numeric-edited data description includes the BLANK WHEN ZERO clause and the VALUE clause uses
  either an alphanumeric or national literal, the BLANK WHEN ZERO clause has no effect on initialization." The
  ALPHANUMERIC leg is measured with its contrasts (`PIC ZZZ9 BLANK WHEN ZERO VALUE "0000"` prints `0000`, while
  `VALUE ZERO` and `VALUE 0` print spaces and `VALUE 12` prints `  12`). The NATIONAL leg needs a
  national-class numeric-edited item, which is the same refused vehicle.

## Provenance — rehomed from PB562 by the train-34 lander (2026-09-13)

These two rows travelled on `kb/Work/PB562`, whose own mechanism was the FORMAT-4 (report-section) import of
GR7/GR8 through the SOURCE clause's implicit-MOVE path. Both row records already said, verbatim, that the
format-4 divergence "is recorded on GR-13.18.63.4-21's row, not here". `kb/Work/PB506` landed that fix in train
33 and the train-34 lander re-measured it green on the merged tree, so PB562 is `landed` and these two rows —
defective for a cause PB562 never owned — needed a live home rather than an orphaned verdict. This note is that
home; no verdict was changed in the move.

## What closes it

The national-edited PICTURE vehicle itself (the COBOLNET0899 stage). When it exists, re-probe both legs —
`PIC NNBNN USAGE NATIONAL VALUE N"ABCDE"` for GR7's no-editing exception, and a national-class numeric-edited
item with `BLANK WHEN ZERO` and a national VALUE literal for GR8 — and re-verdict both rows with the probes as
witnesses. Until then the two rows are honestly PARTIAL, not CONFORMS.

## Sibling sweep the fixer owes

Every rule whose text names the national-edited category is in the same position. `kb/Work/PB554`
(`ValidateValueCategory` has no NationalEdited arm) and `kb/Work/PB533` (the PICTURE EDITING phrase's literal
identity) sit next to this one; sweep for other rows recorded PARTIAL with "no vehicle" naming COBOLNET0899
national-edited before closing.
