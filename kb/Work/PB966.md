---
title: "PB966 — MOVE CORRESPONDING refuses a level-66 THROUGH alias with COBOLNET1757 ('described with level-number 66, so it is not a group data item'), but §13.18.45.4 GR2 makes a THROUGH alias an alphanumeric GROUP item and §14.9.25.3 SR12 asks only for group data items — legal source rejected, and §14.7.6's correspondence over an alias's included items needs a recorded determination"
id: PB966
kind: defect
status: landed
severity: MAJOR
area: binding/statements
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: [PB907, PB965, PB392]
spec_refs: ["14.9.25.3", "13.18.45.4", "14.7.6"]
inventory_rows: []
closes_rows: ["GR-14.7.6-1"]
tags: [cobolsharp, work, defect]
---

Filed 2026-09-22 by REGISTRAR #10 from the wave-45 group-F lead (PB572 implementer report). The implementer called it
the same root as PB907 (a THROUGH alias not treated as the group §13.18.45.4 GR2 makes it). PB907 fixes only the
MOVE-into-variable-length-group arm and lands in train 46, so this is a SIBLING arm, not an extension of PB907.
**Re-run on this registrar's own build of main `f503888da`.**

## Reproduced

`scratchpad/reg10/pcorr.cob`:

```cobol
       01 SRC.
          05 S1 PIC X(3) VALUE "ABC".
          05 S2 PIC X(3) VALUE "DEF".
       66 SALIAS RENAMES S1 THROUGH S2.
       01 DST.
          05 S1 PIC X(3).
          05 S2 PIC X(3).
           MOVE CORRESPONDING SALIAS TO DST
```

```
pcorr.cob(13,12): error COBOLNET1757: MOVE CORRESPONDING operand 'SALIAS' is described with level-number 66, so it
is not a group data item — the operands shall be group data items (ISO §14.9.25.3 SR12)
```

## Rules (each `cite.py --check`ed)

- `--check 14.9.25.3 "Identifier-3 and identifier-4 shall specify group data items and shall not be reference-modified."` → `OK §14.9.25.3 12)`
- `--check 13.18.45.4 "When the THROUGH phrase is specified, data-name-1 defines an alphanumeric group item that includes all elementary items"` → `OK §13.18.45.4 2)`
- `--check 14.7.6 "have the same data-name and the same qualifiers, if any, up to, but not including, D1 and D2"` → `OK §14.7.6 1)`

SR12 is satisfied: SALIAS IS an alphanumeric group item. §14.7.6's opening sentence also admits it ("D1 and D2 are
identifiers that refer to alphanumeric group items, …"). Nothing excludes level 66. §14.7.6 4) excludes a
corresponding item that CONTAINS a RENAMES clause, which is not the same thing as D1 being one.

## The determination the fixer must record (§14.7.6 1) over an alias)

The items "in D1" are the elementary items GR2 says the alias "includes", but their qualifier chains run through the
REDEFINED record (`S1 OF SRC`), not through SALIAS. That leaves "the same qualifiers … up to, but not including, D1"
undefined for them. The proposed reading, which follows GR2's own wording: an alias includes ELEMENTARY items only
(no intermediate groups), so each included item's qualifier list relative to D1 is EMPTY, and it corresponds by
data-name alone. An included item whose name is not unique within the alias falls to §14.7.6 6). Survey the model
implementations (IBM Enterprise COBOL, Micro Focus, GnuCOBOL) per the owner's protocol. Record the result in
CONFORMANCE.md §7 if the survey agrees, or as a `kind: decision` note if it splits.

## Sibling sweep

The same COBOLNET1757 screen guards ADD/SUBTRACT CORRESPONDING (§14.9.2 / §14.9.44 share §14.7.6). Check whether any
other statement's "shall be a group data item" test keys on the level-number rather than on the item being a group:
INITIALIZE, a group relation condition, a group MOVE sender, and the CALL argument screen that PB965 depends on.
**One predicate, "is this identifier a group item", used by every screen.** PB392 made CORRESPONDING's SR6 key on
group KINDS. Extend that predicate so it knows about THROUGH aliases, and do not add a second test beside it.

## Landed 2026-09-22 (wave 48, group Z)

**Re-measured first** on the implementer's own build of main `ec095d120`: the note's `pcorr.cob` still drew
COBOLNET1757 "described with level-number 66, so it is not a group data item". Every citation above was re-run
through `cite.py --check` and each returned OK (§14.9.25.3 12), §13.18.45.4 2), §14.7.6 1)).

**The screen.** `StatementValidation.CheckCorrespondingGroupOperand` refused every level-66 operand by name,
including under MOVE's SR12, which names no level number. It now asks the level-66 question only where the rule
asks it (the arithmetic SR6 rows, `ExcludesLevel66`), and asks it of `Place.DenotedItem`. Every other operand is
judged by `ItemCategory.GroupKindsOf`, which already answered `Alphanumeric` for a THROUGH alias (PB907). No second
predicate was added.

**The matcher: the arm the screen had hidden.** Admitting the alias exposed the matcher. `CorrMatch` walked
`DataItem.Children`, and an alias has none. `CorrespondingBinder.CorrMembers` is now the ONE answer to "which data
items are in D" for both operands. For a group it is the eligible children. For a THROUGH alias it is the elementary
items GR2 names (`RenamesInfo.IncludedElementaryItems`, declaration order, the alias's MEMBERSHIP as distinct from
its storage `Span`). `CorrAccess` gained an alias arm, in which each member resolves by its own unsubscripted
reference. ⚠ DETERMINATION **D-CORR66** (docs/CONFORMANCE.md §3): an included item stands directly in the alias
with no qualifier between, so it corresponds by name with a first-level item of the other operand. Rule 6 takes a
name included twice. Rules 4 and 5 still apply inside the window.

**Siblings, measured.** (1) `ADD CORRESPONDING GALIAS TO DST`, with `66 GALIAS RENAMES SG` (no THROUGH), COMPILED
before this change. The level-66 test read `Place.Item`, which for GR1's alias is SG itself. It now draws
COBOLNET1757 SR6. (2) `INITIALIZE GALIAS` compiled and initialized SG, against §14.9.20.3 SR5 ("shall not contain a
RENAMES clause"), for the same reason. `CheckInitializeTargetRenames` now takes the Place and asks the denoted
entry. The relation condition `IF SALIAS = G2` and the group MOVE (PB907) were probed and are correct. The CALL
argument screen is PB965's own note.

Goldens: `conformance:85/pb966_move_corresponding_through_alias`, plus the negatives
`pb966-add-corr-level66-alias` and `pb966-initialize-level66-alias`. Rows: GR-14.7.6-1 GAP → CONFORMS, with
witnesses added to SR-14.9.25.3-12, SR-14.9.2.3-6 and SR-14.9.20.3-5.
