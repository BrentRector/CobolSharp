---
title: "PB195 — CheckLiteral still screens an OPERAND-position floating-point literal against binary64 under native arithmetic, so `IF 1.0E+400 > X` is REJECTED with COBOLNET1661 while the identical source compiles under ARITHMETIC IS STANDARD-DECIMAL — legal source refused on a bound PB99 already moved off"
id: PB195
kind: defect
status: landed
severity: MAJOR
area: arithmetic
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["8.3.3.3.3", "8.8.1.3", "14.9.2.4"]
tags: [cobolsharp, work, defect]
---

# PB195 — the native operand lane keeps a binary64 literal screen it no longer earns

## Measured, both arms, 2026-08-31

Same source, one line, `--std 2023`:

| Mode | Result |
|---|---|
| native (default) | `error COBOLNET1661: floating-point numeric literal '1.0E+400': its value lies outside the implementor-defined exponent range for a floating-point operand — the IEEE binary64 range` |
| `OPTIONS. ARITHMETIC IS STANDARD-DECIMAL.` | compiles; prints `BIG` |

```cobol
IF 1.0E+400 > X DISPLAY "BIG" ELSE DISPLAY "SMALL" END-IF.
```

Nothing about the literal changes between the two runs. The comparison is a relation condition, not an
arithmetic statement, and the literal is an OPERAND — the position PB99 moved onto the exact
§8.3.3.3.3 decimal value.

## The site

`ExpressionBinder.CheckLiteral` (the `else` arm):

```csharp
else if (!NumericLiteral.FitsBinaryFloat(text))
    ctx.Edition.Error(DiagnosticCatalog.FloatingLiteral, … "the IEEE binary64 range, about 4.9E-324 to 1.8E+308" …);
```

The comment above it justifies the bound with D16 — "a floating-point literal is a binary64 operand" — which is
the premise PB99 narrowed: the *arithmetic* lane keeps binary64 precedence for float items and receivers, but
the literal's own VALUE is its exact §8.3.3.3.3 value. Screening the value against the carrier of a lane it need
not enter rejects legal source, and the rejection is a hard compile error, so there is no workaround short of
rewriting the program.

## Relationship to the D-B landing

**This is the same code change as plan item D-B** ("float literal is its EXACT §8.3.3.3.3 value everywhere"),
whose implementation half is: make `CheckLiteral`'s decimal128 range arm unconditional and **delete the
`FitsBinaryFloat` arm**, alongside collapsing `LiteralNum`/`LiteralOperandNum` in `NumericRenderer`. This note
exists so the defect is in the register in its own right — a defect recorded only inside a design item is a
defect the work register cannot rank — and it **closes with the D-B landing**, not separately. If D-B is
re-scoped or deferred, this note stands on its own measurement.

Goldens ride with D-B: the `pb99-floating-literal-range` negative case is the one that currently pins the wrong
bound and must be updated in the same change set, or the fix will look like a regression.

## FIXED 2026-08-31 with the D-B landing, as this note said it would be

Re-measured on THIS tree first — `IF 1.0E+400 > X` under native arithmetic at `--std 2023` reproduced
`COBOLNET1661` verbatim at HEAD `6cd228b6`, and prints `BIG` after the fix.

`ExpressionBinder.CheckLiteral`'s `FitsBinaryFloat` arm is deleted and the decimal128 range arm is now
unconditional: the literal is its exact §8.3.3.3.3 rule-5 value on the SDIDI lane in every position and every
mode ([[PB156]]), so the implementor-defined range §8.3.3.3.3 r3 asks for is that ONE carrier's. The mode test
that used to select between the two arms is gone with them, which incidentally removed the fourth copy of the
mode set [[PB194]] is about.

**The merge is real and is recorded in both notes**: this defect and D-B are the same code change, exactly as
the note predicted, and it closes with the D-B landing rather than separately. What did NOT merge is the
goldens — the negative `pb99-floating-literal-range` had to be re-split, because it mixed two different rules in
one file. Its VALUE-clause arms keep the binary64/binary32 bound (§13.18.63.3 SR2 — "within the range indicated
by the USAGE clause" is about the ITEM, not the literal); its three statement arms moved to `1.0E+9999` /
`1.0E-9999`, which are past decimal128 and are the widest a legal literal can be, since SR3 caps the exponent at
four digits. The accepting direction — `1.0E+400` in a relation under native arithmetic — is pinned positively
by `2023/pb156_float_literal_exact_native`, so the fix cannot be silently reverted by re-tightening the screen.
