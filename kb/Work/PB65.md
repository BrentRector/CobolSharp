---
title: "PB65 — OPEN (NEW 2026-08-09) — single-site chokepoints: the unchecked Rescale widening wrap, YearToYyyy's in-band omitted-ness, MinScaled val"
id: PB65
kind: defect
status: open
severity: MAJOR
area: runtime/intrinsics
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB65 — OPEN — single-site chokepoints: the unchecked Rescale widening wrap, YearToYyyy's in-band omitted-ness, MinScaled value-vs-index, and the remaining probe finds

> ⚙ **flags:** wrong-answer, silent · **24 inventory rows** (triage 2026-08-09, probe+refute confirmed; the
> per-row mechanisms live in the triage record and each row's inventory notes)

> The bag of confirmed single-mechanism defects, each small and independently fixable — one note so the
> register carries items, not dust:
> - ✅ **LANDED 2026-08-09 — the Rescale wrap and value-vs-index members (5 rows closed).**
>   Comparisons ride the exact non-widening `CobolNum.Compare` (sign split + the overflow-means-greater
>   magnitude trick the unsigned lane always used); `Align`'s widening is escape-CHECKED
>   (`RescaleEscape` → the size-error condition at the D1 boundary, never a wrap); MAX/MIN/ORD-MAX/
>   ORD-MIN are selection-by-index over unaligned (value, scale) pairs (`CobolIntrinsics.Select.cs`),
>   the selected value rescaling with store semantics into a known receiver and staying LOUD
>   receiverless. Golden `pb65_selection_alignment` (BIGV 9(24) vs SMLV 9V9(15) — 39 aligned digits:
>   IF/MIN/MAX/ORD all exact); `CobolNumCompareScaledTests` 9/9; FULL Conformance 4,309/4,309, Unit
>   4,122/4,122, characterization 33/33 (one snapshot re-baked for the new emit; the bare-access guard
>   passed by routing the new emits through RuntimeApi, not by a baseline bump).
>   ⚠ **Documented residue**: a 39-digit-escape selection consumed through a receiverless MOVE raises
>   EC-SIZE rather than answering (MOVE renders sources under ReceiverContext.None by design; threading
>   the receiver there is its own measured change) — loud, never wrong.
> - ✅ **LANDED 2026-08-09 — the date-windowing members (5 rows closed).** Omitted-ness is the ARITY:
>   two-arg overloads are the omitted form (clock-year default per r5), three-arg overloads validate
>   argument-3 AS WRITTEN — an explicit 0 now takes the r4 EC path instead of silently windowing against
>   the execution year — and errored delegations return the documented default WHOLE (DayToYyyyddd no
>   longer manufactures nnn onto an errored core). Golden `pb65_date_windowing` (4 derived lines);
>   CobolDate unit family 36/36 through the overload split.
> - ✅ **LANDED 2026-08-09 — BOOLEAN-OF-INTEGER's argument-1 crosses the WIDE (Int128) bridge (RV-15.13.4-1
>   D1).** The runtime body takes Int128 with the bit walk covering the carrier (bit 126; negatives never
>   reach the walk), and the emitter's `ArgIntWide`/`AsIntWide` is the one wide integer-argument bridge —
>   carrier-total, mirroring `AsInt` arm-for-arm (Real → `IntegerArgWideReal` at the FromDouble saturation
>   constant; Dec before the scale test; U through the R10 Widen funnel; scale-0 RAW). Swept §15: argument-1
>   here is the ONE integer argument whose domain exceeds `long`. Golden `pb65_boolean_wide` (2⁶², 2⁶³−1,
>   2⁶³, 2⁶⁴+5 at 70 positions + the Annex D.10 truncation control). The row STAYS DIVERGES on its D2/D3
>   class-boolean layers, split to [[PB68]].
> - ✅ **LANDED 2026-08-09 (commits `f24c6f0a` + `42745537` — ⚠ recorded here a day late: neither commit
>   updated this note, a rule-8 omission repaired 2026-08-09 with the wide-bridge landing).** The members
>   those commits closed, each re-verified in source before this bullet was written: `Random(long seed)`
>   rejects a negative seed via EC-ARGUMENT-FUNCTION with the wide-seed mask documented; PresentValue and
>   Annuity carry their domain guards at one raise site per rule, both carriers; the §7.3.17 r5 seconds
>   bound is centralized and cited (`SecondsOutOfStandardForm`) and COMBINED-DATETIME — the numeric
>   consumer — is guarded, closing the string-paths-only asymmetry; `CobolDate.Analyze` returns through a
>   wide out-parameter accumulator; `IntegerOfBoolean` accumulates on Int128 with the loud D1 escape.
>   Goldens `pb65_domain_guards` + `pb65_wide_values`.
> - **Repository intrinsic sets are consulted with the wrong case/collation** (FMT-15.43.2/15.58.2), the
>   grammar's `functionArgument` lacks boolean-expression forms (FMT-15.45.2), ASIN/ATAN's FromDouble
>   nearest-vs-truncation rows (RV-15.10.4-1/15.11.4-1) ride the PB56 working-scale seam and are listed
>   there for the fix but re-verdict here, and RV-15.75.4-3 is the doc cross-link row for item 144.
> - **FMT-15.7.2**: IntrinsicCatalog's ABS row declares IntroducedIn 2014 — verify the window against
>   the VCR editions evidence and fix the row + its edition-gate sweep if wrong (a one-cell fix with the
>   edition_gate_sweep discipline).
> - **RV-15.38.4-2**: an A.1-register documentation discharge (the FORMATTED-DATETIME accuracy item) —
>   doc-only, the item-112 shape.
> Each member: cite, fix at the one site, sweep its siblings, golden, re-verdict its rows.

### The rows this note owns (re-verdict on landing)

> RV-15.63.4-1, RV-15.71.4-2, RV-15.72.4-2, RV-15.63.4-2, RV-15.72.4-3, AR-15.23.3-4
> AR-15.23.3-6, AR-15.25.3-4, AR-15.25.3-6, RV-15.25.4-1, AR-15.75.3-2, AR-15.74.3-2
> AR-15.9.3-2, AR-15.17.3-1, AR-15.17.3-2, RV-15.79.4-1, AR-15.79.3-4, RV-15.45.4-1
> FMT-15.43.2, FMT-15.58.2, FMT-15.45.2, RV-15.10.4-1, RV-15.11.4-1, RV-15.75.4-3
> FMT-15.7.2, RV-15.38.4-2
> RV-15.17.4-1, RV-15.27.3-3, RV-15.73.3-3, RV-15.35.4-1, RV-15.64.4-1, RV-15.75.4-1
> RV-15.59.4-1, RV-15.13.4-1 (PB56 residue — probe-and-close or fix at their own single sites)
