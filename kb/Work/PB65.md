---
title: "PB65 — LANDED 2026-08-18 — single-site chokepoints (the Rescale widening wrap, YearToYyyy omitted-ness, MinScaled value-vs-index, the mixed-carrier selection, the REPOSITORY rule-5 screen, the boolean-expression argument, LEAP-SECOND, two A.1 rows) — 34 rows OK"
id: PB65
kind: defect
status: landed
severity: MAJOR
area: runtime/intrinsics
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB65 — LANDED 2026-08-18 — single-site chokepoints: every member landed, all 34 owned rows OK

> ⚙ **flags:** wrong-answer, silent · **24 inventory rows** (triage 2026-08-09, probe+refute confirmed; the
> per-row mechanisms live in the triage record and each row's inventory notes)

> The bag of confirmed single-mechanism defects, each small and independently fixable — one note so the
> register carries items, not dust:
> - ✅ **LANDED 2026-08-09 — the Rescale wrap and value-vs-index members (5 rows closed).**
>   Comparisons ride the exact non-widening `CobolNum.Compare` (sign split + the overflow-means-greater
>   magnitude trick the unsigned lane always used); `Align`'s widening is escape-CHECKED
>   (`RescaleEscape` → the size-error condition at the D1 boundary, never a wrap); MAX/MIN/ORD-MAX/
>   ORD-MIN are selection-by-index over unaligned (value, scale) pairs (`CobolIntrinsics.Select.cs`),
>   the selected value rescaling with store semantics into a known receiver and staying LOUD
>   receiverless. Golden `pb65_selection_alignment` (BIGV 9(24) vs SMLV 9V9(15) — 39 aligned digits:
>   IF/MIN/MAX/ORD all exact); `CobolNumCompareScaledTests` 9/9; FULL Conformance 4,309/4,309, Unit
>   4,122/4,122, characterization 33/33 (one snapshot re-baked for the new emit; the bare-access guard
>   passed by routing the new emits through RuntimeApi, not by a baseline bump).
>   ⚠ **Documented residue**: a 39-digit-escape selection consumed through a receiverless MOVE raises
>   EC-SIZE rather than answering (MOVE renders sources under ReceiverContext.None by design; threading
>   the receiver there is its own measured change) — loud, never wrong.
> - ✅ **LANDED 2026-08-09 — the date-windowing members (5 rows closed).** Omitted-ness is the ARITY:
>   two-arg overloads are the omitted form (clock-year default per r5), three-arg overloads validate
>   argument-3 AS WRITTEN — an explicit 0 now takes the r4 EC path instead of silently windowing against
>   the execution year — and errored delegations return the documented default WHOLE (DayToYyyyddd no
>   longer manufactures nnn onto an errored core). Golden `pb65_date_windowing` (4 derived lines);
>   CobolDate unit family 36/36 through the overload split.
> - ✅ **LANDED 2026-08-09 — BOOLEAN-OF-INTEGER's argument-1 crosses the WIDE (Int128) bridge (RV-15.13.4-1
>   D1).** The runtime body takes Int128 with the bit walk covering the carrier (bit 126; negatives never
>   reach the walk), and the emitter's `ArgIntWide`/`AsIntWide` is the one wide integer-argument bridge —
>   carrier-total, mirroring `AsInt` arm-for-arm (Real → `IntegerArgWideReal` at the FromDouble saturation
>   constant; Dec before the scale test; U through the R10 Widen funnel; scale-0 RAW). Swept §15: argument-1
>   here is the ONE integer argument whose domain exceeds `long`. Golden `pb65_boolean_wide` (2⁶², 2⁶³−1,
>   2⁶³, 2⁶⁴+5 at 70 positions + the Annex D.10 truncation control). The row STAYS DIVERGES on its D2/D3
>   class-boolean layers, split to [[PB68]].
> - ✅ **LANDED 2026-08-09 (commits `f24c6f0a` + `42745537` — ⚠ recorded here a day late: neither commit
>   updated this note, a rule-8 omission repaired 2026-08-09 with the wide-bridge landing).** The members
>   those commits closed, each re-verified in source before this bullet was written: `Random(long seed)`
>   rejects a negative seed via EC-ARGUMENT-FUNCTION with the wide-seed mask documented; PresentValue and
>   Annuity carry their domain guards at one raise site per rule, both carriers; the §7.3.17 r5 seconds
>   bound is centralized and cited (`SecondsOutOfStandardForm`) and COMBINED-DATETIME — the numeric
>   consumer — is guarded, closing the string-paths-only asymmetry; `CobolDate.Analyze` returns through a
>   wide out-parameter accumulator; `IntegerOfBoolean` accumulates on Int128 with the loud D1 escape.
>   Goldens `pb65_domain_guards` + `pb65_wide_values`.
> - ✅ **LANDED 2026-08-09 — integer-operand `**` is receiver-independent (RV-15.64.4-1).** The
>   receiver-bearing arm forced `FloatWorkingScale` (≥9) onto an integer-exact power; the ×10⁹ landing
>   overflowed Int128 INSIDE `PowNativeInt`, saturated through the double fallback, and MOD consumed the
>   sentinel — 320612800 into S9(9)/S9(18)/S9(28), 13657001 into S9(31), the receiver's PICTURE selecting
>   the value (the two-arm shape's FIFTH sighting: PB32 fixed the receiver-less arm and left this one).
>   Now: non-negative LITERAL exponent → `PowNativeInt(b, e)` exact at scale 0, both arms, no receiver
>   read; negative/runtime-item exponent → `PowNativeIntDec` on the SDIDI carrier (owns its scale at run
>   time — 2 ** −2 keeps 0.25, `A ** X` keeps the 30-digit exactness, one value for every context).
>   Sweep: `ModReal`/`RemReal` ride the exact carrier `%` instead of the catastrophically-cancelling
>   literal EAE. Golden `pb65_power_receiver` (four receiver widths + DISPLAY/IF + REM + runtime-exponent
>   + reciprocals + 10³⁰, every value from the python oracle). The past-Int128 sentinel-as-argument
>   residue (`MOD(A ** 3, B)` from `Int128.MaxValue`) is pre-existing and now ledgered as [[PB69]].
> - ✅ **LANDED 2026-08-09 — the bounded-codomain quantizer (RV-15.75.4-1 → CONFORMS; RV-15.10.4-1 and
>   RV-15.11.4-1 gain the native-path clamp beside their PB56 Dec-carrier fix).** `FromDouble`'s
>   away-from-zero rounding stays (it recovers SQRT(10)**2 → 10, NIST IF136A); a §15.x.4-bounded function
>   clamps the QUANTIZED value via `FromDoubleBounded` against Int128 scale-37 constants (exact division,
>   never a double that loses the bound past 15 digits). The family enumerated MECHANICALLY from the
>   rules: RANDOM [0,1) open · ATAN open ±π/2 · ASIN/ACOS closed-but-IRRATIONAL (asin(1)@9 rounded to
>   1.570796327 > π/2, outside even the closed bound) · FRACTION-PART's EAE-derived |v| < 1; SIN/COS's
>   closed rational ±1 deliberately absent. The catalog row carries the bound (`IntrinsicCodomain`), the
>   renderer tail dispatches once. Golden `pb65_codomain_clamp` (boundaries both sides, untouched
>   interior, the SQRT no-bound contrast, 20k-draw RANDOM population).
> - ✅ **LANDED 2026-08-18 (DEVLOG 1306) — the last five members, all measured before and after:**
>   **RV-15.59.4-1 D2** — a MIXED selection list (a float beside a fixed item) delivered the selected argument
>   through binary64: `MAX(F1 N1)` = 13 for N1 = 999999999999999999, `MEDIAN(F1 N1 N2)` = 0; MAX/MIN/ORD-MAX/
>   ORD-MIN/MEDIAN over a mixed list now evaluate on the SDIDI carrier under native too (the content, exactly);
>   an all-float list keeps its double, the arithmetic statistical family keeps the D16 float lane (golden
>   `pb65_selection_mixed_carriers`). **FMT-15.43.2 / FMT-15.58.2** — §8.3.2.1 rule 5 written ONCE
>   (`DataBinder.ScreenRepositoryIntrinsicName`, asked by every declaration funnel — COBOLNET1649) and the
>   FUNCTION-less reference is the function whenever the REPOSITORY names it (`HIGHEST-ALGEBRAIC(A1)` = +999,
>   never the shadowing table); an un-declared intrinsic name stays a legal user word (golden
>   `pb65_repository_r5_intrinsic_names`, negatives `pb65-repository-*`). **FMT-15.45.2** — `functionArgument`
>   admits a boolean expression behind the ARGUMENT-scoped `boolArgAhead()` (INTEGER-OF-BOOLEAN(BIT-A B-AND
>   BIT-B) = 8; a following B-AND outside the list is not the argument's) and B"" lexes (§8.3.3.4.4 GR4); the
>   bit-group shape waits on GROUP-USAGE ([[PB79]]) (golden `pb65_boolean_expression_argument`).
>   **AR-15.79.3-4** — `>>LEAP-SECOND ON` is a real group fact (`LeapSecondDirectiveProcessor` → the ONE
>   `DirectiveResults` record → `DataBinder.LeapSecond` → the renderer's `leapSecond: true`): a seconds subfield
>   may be 60, standard numeric time form is bounded at 86,401, 86,400 presents as 23:59:60 (goldens
>   `pb65_leap_second_on/off`, negatives for SR1 and the 2002 gate; construct `leap-second-directive-2002`;
>   `ConditionalCompilationProcessor`'s five leave-bools became ONE set, `Bind`'s four directive parameters ONE
>   record). **RV-15.75.4-3 / RV-15.38.4-2** — A.1 items 145 and 87 written in CONFORMANCE.md §7 from what was
>   MEASURED (`RandomSeedSubsetTests`: seeds 0..65,535 pairwise distinct; FORMATTED-CURRENT-DATE now carries
>   the clock's ticks × 100 — no double on the way — 7 significant fraction digits). **FMT-15.7.2** — the ABS
>   edition window is 2014 by kb/Work R28's adversarially verified WG4 list; the row closes.
> Each member: cite, fix at the one site, sweep its siblings, golden, re-verdict its rows.

### The rows this note owns (re-verdict on landing)

> RV-15.63.4-1, RV-15.71.4-2, RV-15.72.4-2, RV-15.63.4-2, RV-15.72.4-3, AR-15.23.3-4
> AR-15.23.3-6, AR-15.25.3-4, AR-15.25.3-6, RV-15.25.4-1, AR-15.75.3-2, AR-15.74.3-2
> AR-15.9.3-2, AR-15.17.3-1, AR-15.17.3-2, RV-15.79.4-1, AR-15.79.3-4, RV-15.45.4-1
> FMT-15.43.2, FMT-15.58.2, FMT-15.45.2, RV-15.10.4-1, RV-15.11.4-1, RV-15.75.4-3
> FMT-15.7.2, RV-15.38.4-2
> RV-15.17.4-1, RV-15.27.3-3, RV-15.73.3-3, RV-15.35.4-1, RV-15.64.4-1, RV-15.75.4-1
> RV-15.59.4-1, RV-15.13.4-1 (PB56 residue — probe-and-close or fix at their own single sites)

> ⚙ **2026-08-09 PROBE SWEEP (wf_fdd1492c — probe + adversarial refute, every verdict a pasted CLI run + a --check'd citation; full per-row evidence/mechanisms/blockers frozen in `docs/rearchitecture/backlog-probe-sweep-2026-08-09.json`):** the 6 probeable tail rows LIVE, 3 unprobeable.
