---
title: "PB706 — `ORGANIZATION IS RECORD SEQUENTIAL` does not parse (`COBOL0001: no viable alternative at input 'RECORD'`, exit 65 at every edition): `organizationType : LINE SEQUENTIAL | SEQUENTIAL | RELATIVE | INDEXED` has no RECORD SEQUENTIAL alternative, although §12.4.5.10.2 prints `{ {LINE|RECORD} SEQUENTIAL | RELATIVE | INDEXED }` and §12.4.5.10.3 GR3 defines the phrase — RENDERED 2026-09-06 (folio 327) and the shape is SETTLED: `RECORD` is NOT underlined, so the fix is `RECORD? SEQUENTIAL` and bare `SEQUENTIAL` stays legal at every edition; the transcription's §12.4.5.10.2 figure note, which claimed otherwise, was repaired in the same change set"
id: PB706
kind: defect
status: open
severity: MAJOR
area: grammar
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["12.4.5.10.2", "12.4.5.10.3", "12.4.5.1"]
inventory_rows: []
tags: [cobolsharp, work, defect]
---

# PB706 — OPEN — a phrase the standard defines and the grammar cannot write

Found 2026-09-05 by the `PB692` implementer (report §"New defects" N1) while probing the organization spellings its
screen must cover: `SELECT F ASSIGN TO "f.dat" ORGANIZATION IS RECORD SEQUENTIAL ACCESS MODE IS SEQUENTIAL.` →
`COBOL0001: no viable alternative at input 'RECORD'`, exit 65, all four editions.

## The rule

§12.4.5.10.3 GR3 (`cite.py --check 12.4.5.10.3 "The RECORD SEQUENTIAL phrase specifies that the file organization
is record sequential"` → OK 3)) defines the phrase; §12.4.5.10.2 prints it in the general format. A phrase the
standard defines and prints cannot be unwritable, whichever way the underline question resolves.

## The mechanism

`src/Cobol.Net.Frontend/Grammar/Core/CobolIO.g4#organizationType : LINE SEQUENTIAL | SEQUENTIAL | RELATIVE |
INDEXED` — the RECORD SEQUENTIAL alternative is absent. Every RECORD SEQUENTIAL program is refused, not only SR2's.

## The transcription question to settle first (rule 1: render the diagram)

The transcription disagrees with itself about whether `RECORD` is underlined: §12.4.5.10.2's figure note says
RECORD is underlined and the inner brace is a required choice — which would make the ubiquitous bare
`ORGANIZATION IS SEQUENTIAL` illegal — while the diagram markup in both §12.4.5.10.2 and §12.4.5.1 Format 3 leaves
RECORD un-underlined, Format 3's own figure note says explicitly that RECORD is not underlined, and Format 4 prints
a bare `[ [ORGANIZATION IS] SEQUENTIAL ]` with no LINE/RECORD brace. Three signals to one say RECORD is an optional
word. Render both pages (`clause_page.py 12.4.5.10.2`, `clause_page.py 12.4.5.1`) and measure the underline; if
the §12.4.5.10.2 figure note is wrong, repair the transcription on discovery.

## Fix

Either reading makes the phrase legal: `organizationType : (LINE | RECORD)? SEQUENTIAL | RELATIVE | INDEXED` if
RECORD is optional (the likely answer), or a `RECORD SEQUENTIAL` alternative if it is required. The binder's
`FileModel.Organization` already has one value for record sequential (`Sequential`) — map RECORD SEQUENTIAL onto it,
never a fourth value. Positive goldens at 85 (the 1985 text also prints the form — confirm) and 2023 with the phrase
written; the corpus sweep for the 824 omitted-organization entries is unaffected. Coordinate with PB695's
CobolIO.g4 family and PB704 (same file, different rules). `Generated/` is never committed.

## Registrar verification 2026-09-06 — the page is RENDERED and the open question is ANSWERED

Train 14's lander re-collected this defect from the same two implementer reports (PB692 §N1 and PB688 §N1);
verified against main at `0db46983` and folded here rather than filed again (CLAUDE.md rule 8).

**Re-measured, not inherited.** `src/Cobol.Net.Frontend/Grammar/Core/CobolIO.g4:154-159` is unchanged by train 14,
and `…/scratchpad/reg14/recseq.cob` (`SELECT F ASSIGN TO "f.dat" ORGANIZATION IS RECORD SEQUENTIAL ACCESS MODE IS
SEQUENTIAL.`) draws `COBOL0001: no viable alternative at input 'RECORD'` + `COBOL0312` at `--std 85`, `2002` **and**
`2023` — edition-invariant, as this note says.

**⛔ THE RENDER THIS NOTE'S TITLE ASKS FOR IS DONE, and it decides the shape.**
`python scripts/spec/clause_page.py 12.4.5.10.2` → PDF page 357 / printed folio 327;
`python scripts/render-spec-page.py 357` → `E:\Temp\spec_p357.png`, read as an image. Measured off the render:
**`ORGANIZATION` underlined · `IS` NOT · `LINE` underlined · `RECORD` NOT underlined · `SEQUENTIAL` underlined ·
`RELATIVE` underlined · `INDEXED` underlined.** The three-signals-to-one reading in this note was right.

So `RECORD` is an OPTIONAL word — `cite.py --check 5.2.3 "Optional words are reserved words or context-sensitive
words"` → **OK §5.2.3**, *"shown in uppercase and not underlined in general formats"*; `--check 8.3.2.4.3 "may be
specified at the user's option with no effect on the semantics of the format"` → **OK §8.3.2.4.3** — and the inner
brace is a required choice between the required word `LINE` and the optional word `RECORD`. Taking the `RECORD`
arm and omitting the optional word leaves bare `SEQUENTIAL`, which is corroborated from the other end by
`--check 12.4.5.10.3 "When the ORGANIZATION clause is not specified, sequential organization with the RECORD
SEQUENTIAL phrase is implied"` → **OK §12.4.5.10.3 6)**. **The grammar shape is therefore settled — one
alternative, and bare `SEQUENTIAL` must not move:**

```antlr
organizationType
    : LINE SEQUENTIAL
    | RECORD? SEQUENTIAL
    | RELATIVE
    | INDEXED
    ;
```

**Transcription defect found and REPAIRED in the same change set** (`feedback_fix_transcription_errors_immediately`):
`specs/ISO_COBOL.md` §12.4.5.10.2's **figure note** claimed *"`ORGANIZATION`, `LINE`, `RECORD`, `SEQUENTIAL`,
`RELATIVE`, and `INDEXED` are underlined"*. The diagram markup above it was always correct (bare `RECORD`, no
`<u>`); only the prose note was wrong, and it is the note the two implementers read. Rewritten 2026-09-06 to match
the render and the §12.4.5.1 Format 3 note's existing wording, plus the optional-word consequence. Note the shape:
`feedback_spec_diagrams_render_pdf` — the rule text was faithful and the prose ABOUT the diagram was not.

**Still open for the fix, and NOT answered by the render:** the edition band. `RECORD` as a word in this clause
rides the same 2023 Foreword bullet as LINE SEQUENTIAL, so written-out `RECORD SEQUENTIAL` may need the 2023 gate
[[PB688]] added, while bare `SEQUENTIAL` must stay ungated at 85 — a two-arm gate on one alternative
(`feedback_two_arm_dispatch`).

**Retired ids (recorded once, in [[PB726]]):** PB723 was minted for this finding by the train-14 registrar and is
BURNED — never reassign it. Its content is this section.
