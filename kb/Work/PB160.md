---
title: "PB160 — the Annex A.1 documentation register is 169 obligations behind and batch 8 named seven concrete rows: ACCEPT items 1/5 (conversion, transfer size — PB139 fixed the implementation to match the 80-character determination; the documentation rows remain), CANCEL items 18/19, CONTINUE item 39 (precision + 86,400 max live only in a code comment), DISPLAY items 56-halves/57/58; audit_annex_a1.py --check exists and CONFORMANCE.md §7 has no rows for any of them"
id: PB160
kind: analysis
status: open
severity: MINOR
area: docs
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
inventory_rows: ["GR-14.9.1.4-1", "GR-14.9.1.4-2"]
tags: [cobolsharp, work, analysis]
---

# PB160

Rows: GR-14.9.1.4-1/-2, GR-14.9.5.4-5/-10, GR-14.9.9.4-1 (doc leg), GR-14.9.11.4-2/-7, plus the §A clause's 222 inventory rows. The mechanical driver exists (scripts/spec/audit_annex_a1.py); the work is deriving each determination from what the code DOES (the one code disagreement — ACCEPT's record size — was fixed by PB139; the remaining work is documentation), writing the CONFORMANCE.md rows, and re-verdicting the A.1 inventory rows they discharge. Batch the seven named rows first; the remaining register burn-down is its own campaign.

*(Found by Phase-B batch 8 — §8.8.1 + §14.9.1–14.9.12, 2026-08-29; one adjudicator + one independent refuter per subject. Full forensic evidence: the named rows' `notes` in `tests/version-matrix/traceability-inventory.json`; the raw finding lines in the session scratchpad `pb-batch8/findings.txt`.)*

**2026-09-02 — the mechanism this note was waiting on exists, and it measures the claim exactly.** "Re-verdicting
the A.1 inventory rows they discharge" was impossible until a DOC row could carry a verdict at all: all 222 sat
`verdict: ""` / `state: GAP` while §7 already held 39 determinations. A DOC row now closes on its **computed**
§7 anchor (`docs/CONFORMANCE.md#DOC-A.1-<n>`, from `inventory-schema.json` → `kinds.DOC`) plus a greenfield
`src/Cobol.Net.*` site plus a spec-derived test, and the 37-row back-fill landed against it.

Of the **seven rows this note named**: items **18** and **56** closed (`2023/pb154_cancel_active`;
`2002/pb148_display_forms` + `2023/comp5_display_beyond_picture`); items **19**, **57** and **58** came back
**CONFORMS-but-untested** — the determination is written and correct, and each is one golden from done; items
**1**, **5** (ACCEPT conversion and transfer size) and **39** (CONTINUE) are **still unwritten in §7** and remain
this note's actual remaining work. `GR-14.9.11.4-1`'s bare `docs/CONFORMANCE.md` citation — which resolved on
`File.Exists` alone — was re-anchored to `#DOC-A.1-56`, and `GR-14.9.11.4-6`'s to `#DOC-A.1-58`.

⚠ **Status stays `open`**: three of the seven rows this note is about have no determination yet, so its stated
goal is not met. What is gone is the excuse — the register burn-down is now visible in the GAP count rather than
only in `audit_annex_a1.py`'s own output.

## Batch-b3 extension (lane 3, §13.18.52) — 2026-09-04

Two more concrete missing rows, found by the lane-3 SIGN-clause adjudication: the Annex A.1 determination table in docs/CONFORMANCE.md has NO `DOC-A.1-177` and no `DOC-A.1-178` row (the sequence runs … 171, 179, 180 …). They are §13.18.52.4 GR4's and GR5's implementor determinations — the sign representation when no SIGN clause is specified, and the positioning the SIGN clause chooses. The determinations are MADE in code and consistent (a signed DISPLAY item with no SIGN clause takes the TRAILING over-punch; `01 A PIC S9(3) VALUE -123.` renders `12L`), so this is a DOCUMENTATION gap, not a behavioural one — which is why the two inventory rows stayed CONFORMS in batch b3 and are recorded here instead.
