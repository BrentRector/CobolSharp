---
title: "PB656 — `tests/version-matrix/reserved-words.json`'s per-edition flags DISAGREE with the project's own removed-construct rows: `SEGMENT-LIMIT` is `r2002: true` while `segment-limit-removed-2002` says the segmentation facility left the standard at 2002, and `PADDING` has the same shape — a word cannot be reserved by an edition that no longer has the construct spelling it, so one of each pair is wrong; the flags' provenance is GnuCOBOL's per-standard DIAGNOSTIC lists, not the ISO §8.9 tables, and the repo holds no 2002/2014 text to settle it — PB300 sidestepped the conflict (it chose `removedIn` to agree with the flags) and said so in VCR row 7.20"
id: PB656
kind: analysis
status: open
severity: MINOR
area: frontend/words
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["8.9"]
inventory_rows: ["SR-8.3.2.1-2"]
tags: [cobolsharp, work, analysis]
---

# PB656 — OPEN — two tables about one fact

Found 2026-09-05 by the `PB300` implementer (report §"New defects" D2), which needed the removal edge of the
PADDING CHARACTER clause and found the two places that state edition facts disagreeing. §8.9 is authoritative for
the 2023 edition only (`OK §8.9`); for 1985, 2002 and 2014 the repository has no text, and
`tests/version-matrix/reserved-words.json`'s `r85`/`r2002`/`r2014` flags are generated (`scripts/gen-reserved-words.ps1`,
[[PB618]]'s subject) from GnuCOBOL's per-standard word lists — which are that compiler's diagnostic lists and can
keep a word reserved past the deletion of the construct that spelled it.

## The measurement to make

Join `tests/version-matrix/constructs.json` rows carrying `removedIn` against `reserved-words.json` on the
construct's leading word(s), and report every pair still flagged reserved AT `removedIn` (SEGMENT-LIMIT / 2002,
PADDING / 2014 are the two known). For each pair, decide from the only evidence available — the 2023 text's Annex
on removed elements where it lists them, the construct's own history in `docs/VERSION_CHANGE_REFERENCE.md`, and
(per [[survey_compilers_on_latitude]]) what the other compilers reserve at that standard — and record the decision
in the VCR row beside the construct, then correct the FLAG (a one-line JSON edit) so the two tables agree; the
generator's input, not its output, carries the correction ([[PB618]]).

## Why it matters

An over-reserved word at an edition is [[PB655]]'s class 3 by another route: `01 SEGMENT-LIMIT PIC X.` at
`--std 2002` is refused on a flag the standard of that year may not support. Consistency between the two tables is
also what lets [[PB655]]'s drift test be written against ONE source of edition truth.

## Extended by REGISTRAR 15b (batch 15b, pin @`071ff04ea`) — rows `SR-8.3.2.1-2`

COBOL is in the §8.12 directive-word list and absent from the 2023 §8.9 list; reserved-words.json flags it r2002/r2014=true (COBOLNET0901 at 2002/2014) while its own provenance says 'removed post-85' — the per-edition flag is unverified

**SR-8.3.2.1-2**

> Compiler-directive words, except those that are also reserved words, may be used as user-defined …

`python scripts/spec/cite.py --check 8.3.2.1 "Compiler-directive words, except those that are also reserved words, may be used as user-defined"` → `OK  §8.3.2.1 2)  (General)`

#### `SR-8.3.2.1-2` — PARTIAL (adjudicator + refuter record, verbatim)

Rule: `OK  §8.3.2.1 2)` "Compiler-directive words, except those that are also reserved words, may be used as user-defined words and system-names." The §8.12 list (`OK §8.12` "The following words are reserved in compiler directives") was split against tests/version-matrix/reserved-words.json: 35 words §8.9 never reserves (CALL-CONVENTION, CHECKING, COBOL-WORDS, COMPILE-TIME-ARITHMETIC-EXPRESSIONS, DE-EDITING, DEFINE, DEFINED, EQUATE, EXTERNAL-FILE-FILE-STATUS, FIXED, FLAG-02/-14, FUNCTION-ARGUMENT, IMP, I-O-DECLARATIVE, I-O-STATUS-04/-07, LEAP-SECOND, LISTING, MOVE-TO-SAME-NAME, NUMVAL, PARAMETER, POP, PROPAGATE, PUSH, READ-PREVIOUS, REF-MOD-ZERO-LENGTH, SUBSTITUTE, TURN, UNDEFINE, VALUE-EDITING, VALUE-FIG-CON-LENGTH, VALUE-ZERO, WRITE-END-OF-PAGE, ZERO-LENGTH) plus the directive-reserved exception-names EC-SIZE / EC-ALL / EC-SIZE-OVERFLOW. The main-program lexer gives none of them a token (directives are consumed by the preprocessor before the parser, CompilerDirectiveCatalog), so each reaches cobolWord as IDENTIFIER and the §8.9 funnel (VersionConformancePass.VisitCobolWord) passes it. PROBES (pinned cobol.exe, out/probe/cdw): cdw.cob declares all 38 as 01-level data-names and DISPLAYs each -> runs at 2023 printing 38 Y; cdw2.cob (the 35-char word dropped, which COBOLNET1567 correctly refuses below 2023 as a §8.3.2.1 length error, not this rule) runs identically at 85/2002/2014. cdw3.cob at 2014 and 2023 puts >>DEFINE DEFINE AS 5 and >>TURN EC-SIZE CHECKING ON in the SAME compilation as 01 TURN, 01 DEFINE, 88 CHECKING, 01 PUSH, section UNDEFINE and paragraph POP -> prints 34Q / D5 / CK (the >>IF DEFINE = 5 compilation-variable and the data-name DEFINE coexist). System-name half: cdw4.cob SOURCE-COMPUTER. DEFINE. OBJECT-COMPUTER. PROPAGATE. (computer-name is a system-name, §8.3.2.3) compiles and runs at 85 and 2023. The EXCEPT half: B-AND (a §8.12 word §8.9 reserves from 2002) as a data-name -> COBOLNET0901 at 2014 and 2023, accepted at 85; XOR (§8.9-reserved at 2023 only per the reserved-word table) -> accepted at 85/2014, COBOLNET0901 at 2023. No spec-derived test names this rule (grep of tests/ for the sentence and for 'compiler-directive word' as a user word finds none), so test-ref is empty. (Only one COBOLNET0901 is printed per compilation — the reservation gate reports the first offending use, the PROCEDURE reference — which is a reporting choice, not this rule.) || REFUTED: PARTIAL — The adjudicator's census of the section 8.12 words that section 8.9 does not reserve is wrong: it lists COBOL-WORDS, which appears in 8.9 only inside a NOTE, and it omits COBOL. COBOL is in the section 8.12 list and is absent from the 2023 section 8.9 list. At 2023, COBOL is accepted as a computer-name, a data-name and a section-name. At 2002 and 2014 the pinned compiler refuses it with COBOLNET0901 ('COBOL is a reserved word in COBOL-2002'). The claimed editions include 2002 and 2014, and the refusal rests on ReservedWords.Table.cs / reserved-words.json flags r2002=true and r2014=true. That row's own provenance string says '1985-reserved, removed post-85 (GnuCOBOL per-standard lists; absent from ISO 2023 section 8.9)', which contradicts those flags. Whether the refusal is correct at 2002 and 2014 therefore depends on edition data that the 2023 text cannot decide and that nobody has verified. That makes one edge unverified, so the verdict is PARTIAL. All the other arms held: 34 other non-reserved directive words and 4 more exception-names (EC-I-O, EC-DATA-INCOMPATIBLE, EC-PROGRAM-NOT-FOUND, EC-SIZE-ZERO-DIVIDE) were accepted at 2002/2014/2023 as program-name, computer-name, mnemonic-name, condition-name (switch status), alphabet-name, class-name, symbolic-character, file-name, record-name, data-name, index-name, level-88 condition-name, section-name and paragraph-name.
