---
title: "PB1262 — the CONSTANT RECORD subtree screen omits the OCCURS formats it must bar: an occurs-depending (Format 2, and the report-writer Format 3 STEP spelling the data division wrongly admits) or dynamic-capacity (Format 4) table compiles under a CONSTANT RECORD (§13.18.38.3 SR19, SR23, SR33)"
id: PB1262
kind: defect
status: open
severity: MINOR
area: binding/data-division
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1260", "PB1265"]
spec_refs: ["13.18.38.3"]
inventory_rows: ["SR-13.18.38.3-19", "SR-13.18.38.3-33", "SR-13.18.38.3-23"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14e** from adjudication batch d1-14e (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probe sources under the batch's `out13/probe/<subject>/`). Checked against main 071ff04ea: trains 58/59 did not touch this mechanism.

# PB1262 — OPEN

## The rules (every quotation run through `cite.py --check` by the registrar)

- `python scripts/spec/cite.py --check 13.18.38.3 "The DEPENDING phrase shall not be specified in any data item subordinate to a data item described with the CONSTANT RECORD clause"` → `OK  §13.18.38.3 23)  (Syntax rules)`
- `python scripts/spec/cite.py --check 13.18.38.3 "A format 2 OCCURS clause shall not be specified in any data item subordinate to a data item described with the CONSTANT RECORD clause"` → `OK  §13.18.38.3 19)  (Syntax rules)`

## Measured

### Finding occurs-clause-p1 — §13.18.38.3 SR19, SR33 (under-rejects; rows SR-13.18.38.3-19, SR-13.18.38.3-33)

**Mechanism.** The CONSTANT RECORD subtree screen omits the OCCURS formats it is required to bar: occurs-depending (Format 2) and dynamic-capacity (Format 4) tables compile under a CONSTANT RECORD

**Repro.** `01 N PIC 9 VALUE 2. 01 C CONSTANT RECORD. 05 T PIC X OCCURS 1 TO 3 DEPENDING ON N VALUE "A".` (out13/probe/occurs-p1/sr19_constrec_odo.cob); `01 C CONSTANT RECORD. 05 T PIC X OCCURS DYNAMIC FROM 2 VALUE "A".` (sr33_constrec_dyn.cob)

**Expected, from the standard.** Both rejected. SR19: 'A format 2 OCCURS clause shall not be specified in any data item subordinate to a data item described with the CONSTANT RECORD clause'. SR33: the same for the dynamic-capacity-table format, on the CONSTANT RECORD item itself too. The fixed Format 1 table stays legal (sr33_constrec_fixed_ok).

**Observed.** Both compile and run (print 'A'). The screen at DataBinder.cs ~873-892 (via IsConstantRecordItem) lists BLANK WHEN ZERO / SYNCHRONIZED / BASED / ANY LENGTH / TYPEDEF / format 2 PICTURE, never OCCURS.

**Code site.** src/Cobol.Net.Compiler/Binding/DataBinder.cs (CONSTANT RECORD subtree screen, ~873-892)

### Row SR-13.18.38.3-23 — PARTIAL (no separate finding; the adjudicator's / refuter's record)

Code location: `src/Cobol.Net.Compiler/Binding/DataBinder.cs#BuildForest (CONSTANT RECORD placement, §13.18.15.3 SR1)`

cite.py --check 13.18.38.3 "The DEPENDING phrase shall not be specified in any data item subordinate to a data item described with the CONSTANT RECORD clause" -> OK. Premise check: this is a FORMAT 3 (report-writer) rule, and a report group description entry has no CONSTANT RECORD clause, so the forbidden shape cannot be written. Probe probe/occurs-p1/sr23_rw_constrec: `01 DL TYPE DETAIL CONSTANT RECORD.` in the REPORT SECTION fails at the word CONSTANT (COBOLNET0901). CONSTANT RECORD is also confined to WORKING-/LOCAL-STORAGE (DataBinder.cs ~878). Vacuously satisfied; the data-division twin is SR19 (NOT-IMPLEMENTED). || REFUTED: PARTIAL — The 'vacuous' premise holds only for the REPORT SECTION. The compiler parses Format 3 (STEP) in the data division (the adjudicator's own fmt_step_ws superset finding), so the forbidden shape IS writable and is accepted: `01 R CONSTANT RECORD. 05 T PIC X OCCURS 1 TO 5 DEPENDING ON N STEP 1.` in WORKING-STORAGE compiles and runs (r23_step_ws rc=0 'OK'). There is no CONSTANT RECORD + DEPENDING check anywhere (the SR19 twin is NOT-IMPLEMENTED). Report-section half verified vacuous, data-division half unchecked => PARTIAL. Fixing the FMT superset parse (reject STEP outside a report group) would make the rule vacuous again; alternatively the missing SR19 check would cover both.

## What the fixer owes

Re-probe on your own build first (MANDATORY-PRACTICES I3). Fix the mechanism at the code sites above — one rule, one place (the `cluster:` notes share these files; take them as one fill unit). One positive golden at the introducing edition and one negative below it / per violated rule, each derived from the quoted rule. Sweep the sibling arms named in the findings (CLAUDE.md rule 4). On landing flip `status`, write `closes_rows` from `inventory_rows`, and re-verdict each row with `record_verdicts.py`.
