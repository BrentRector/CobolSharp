---
title: "PB132 — ✅ LANDED 2026-08-29 (ScreenCallOperand = the ONE chokepoint over the resolved mode; SR6/SR8 via BitLayout.StartBitWithin; ONE VLG predicate; SR22 both arms; found+fixed PB161). WAS: CALL's operand screens are the two-arm-dispatch shape five times over: SR22's class screen runs only on the EXPLICIT BY VALUE arm (a bare argument inheriting Value mode is unscreened; an index-NAME BoundIndexRef slips the screen's BoundNumRef guard); OoIsObjectData is INVOKE-only; SR1/SR10/SR12/SR13 have no screens though every predicate already exists"
id: PB132
kind: defect
status: landed
severity: MAJOR
area: binding
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB132

Rows: SR-14.9.4.3-1/-3/-6/-7/-8/-10/-12/-13/-18/-22, findings [call-statement-p1/p2].

Each screen's predicate is already in the tree, unreached from CALL: DataBinder.OoIsObjectData (SR3's factory/instance WS ban) has exactly two consumers, both INVOKE; CheckByValueClass early-returns unless the operand is BoundNumRef (an index-name arrives as BoundIndexRef — class index per §8.5.2.1 Table 2/PB124 — and is accepted BY VALUE) and runs only on the explicit arm (`BY VALUE N X` screens N, not X); ReferenceResolver.HasVariableLengthSubordinate (SR12) is called by STRING/UNSTRING for the same rule shape and never by CallBinder — NOTE: IntrinsicBinder.IsVariableLengthGroup (PB124 wave 4) is a SECOND copy of the predicate; the fix must reconcile the two, not mint a third; StrongTypeModel.IsStronglyTyped (SR10) is likewise unreached for BY REFERENCE; StatementBinder.InMethod (SR13) is read by BindGoback in the same file and not by BindCall; SR6's bit-byte-alignment needs an offset query BitLayout does not yet expose; SR7's screen-name arm silently null-resolves (the PB88 wrong-stage shape). Also SR18: the explicit BY VALUE arm has Place null so the ANY-LENGTH loop can never match it — the two arms each cover the half the other misses.

*(Found by Phase-B batch 8 — §8.8.1 + §14.9.1–14.9.12, 2026-08-29; one adjudicator + one independent refuter per subject. Full forensic evidence: the named rows' `notes` in `tests/version-matrix/traceability-inventory.json`; the raw finding lines in the session scratchpad `pb-batch8/findings.txt`.)*
