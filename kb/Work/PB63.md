---
title: "PB63 — LANDED 2026-08-18 — EXCEPTION-FILE/-LOCATION/-STATEMENT and MODULE-NAME: resolver, gating and width defects across the exception surface"
id: PB63
kind: defect
status: landed
severity: MAJOR
area: runtime/exceptions
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB63 — LANDED 2026-08-18 — EXCEPTION-FILE/-LOCATION/-STATEMENT and MODULE-NAME: resolver, gating and width defects across the exception surface

> ⚙ **flags:** wrong-answer, silent · **19 inventory rows** (triage 2026-08-09, probe+refute confirmed; the
> per-row mechanisms live in the triage record and each row's inventory notes)

> Family clusters inside one surface: `BindExceptionFileArg`'s resolver filters by bare name match with
> no `HasFd && !IsSort` screen (shared by both EXCEPTION-FILE forms); `FileRegistry.ExceptionFile` gates
> r2a on an `EverAccessed` flag written at only two call sites; `EcFunctions.File` recovers the display
> name by `LastIndexOf("::")` string surgery; `ProcedureTableBuilder.AddAnonymousParagraph` stores a
> DISPLAY placeholder where SS15.30/31 define the returned name; `EcWrap`/`BindRaise` take `Start.Line`
> from the ANTLR stream (an ordinal, not the listing line); the four FMT rows share ONE mechanism — the
> SS8.4.3.2.3 SR2 keyword-omitted reference resolves at some reference sites and not others; and
> MODULE-NAME's returned item carries no 63-character width anywhere down the chain (r1/r2), with an
> IsMain-less TOP-LEVEL arm (r10). Three rows are DOC-layer only (RV-15.65.4-3/5/6 — A.1 register
> entries), closable with CONFORMANCE.md rows alone.
> Wrong answers are real and silent but confined to exception-handling/introspection paths.

### ✅ LANDED 2026-08-18 (DEVLOG 1299) — 19 of 19 rows CONFORMS (GAP 4067 → 4048)

> `BindExceptionFileArg` screens `HasFd && !IsSortMerge` and cites the function's own clause; the SELECT-spelled
> name rides `FileModel.SelectName` → `FileConnector.SelectName` (registered by every `CobolFile.Register*`), and
> both EXCEPTION-FILE forms read it — the two `LastIndexOf("::")` strips are gone; `EverAccessed` is set by the ONE
> I-O-status assignment path (the `FileConnector.Status` setter); `EcBinder.EcLocation` builds §15.30.3 r2b's three
> shapes (an empty field for the paragraph-name-OMITTED paragraph — `ProcedureTableBuilder` now stores the empty
> name — the section alone, `para OF section`) and names a METHOD by its METHOD-ID (`OoMethodScope.MethodName`);
> `ModuleStack.Name`'s TOP-LEVEL arm tests `IsMain` (r10 at the host boundary → r3's documented space);
> `ModuleStackInvariantTests` pins r1/r2/r10; CONFORMANCE.md gains A.1 rows 133/134/136/137 and the r2b3
> line-identifier determination. Goldens `pb63_exception_file_select_names`, `pb63_exception_location_procedure_field`
> (also the FMT-15.30/31/32/33 forms), `pb63_module_name_lengths`; negatives `pb63-exception-file-{sd,no-fd,n-sd}`.
> **Registered:** PB82 — bind-time diagnostics carry no source position, and post-COPY line numbers are resultant-text
> ordinals (found through RV-15.31.3-L2.3; the determination is documented, PB82 will revise it to source lines).

### The rows this note owns (re-verdict on landing)

> AR-15.28.3-1, AR-15.29.3-1, FMT-15.30.2, FMT-15.31.2, FMT-15.32.2, FMT-15.33.2
> RV-15.28.4-1, RV-15.28.4-2, RV-15.29.4-1, RV-15.29.4-2, RV-15.30.3-2, RV-15.31.3-2
> RV-15.31.3-L2.3, RV-15.65.4-1, RV-15.65.4-2, RV-15.65.4-10, RV-15.65.4-3, RV-15.65.4-5
> RV-15.65.4-6

> ⚙ **2026-08-09 PROBE SWEEP (wf_fdd1492c — probe + adversarial refute, every verdict a pasted CLI run + a --check'd citation; full per-row evidence/mechanisms/blockers frozen in `docs/rearchitecture/backlog-probe-sweep-2026-08-09.json`):** 11 rows LIVE (the refuter overturned the prober's FMT-15.32.2/FMT-15.33.2 FIXED claims), 5 fixed-upheld (test-needed), 3 unprobeable (blockers recorded in the evidence file).
