---
title: "PB728 — `ConditionRenderer.RenderMembershipTest` reads `parent.Pic?.Category` (line 464) where D20's ONE operand-category reader is `OperandPic` (`StringCategoryOf`, line 295, whose own comment says \"an elementary item's picture, a bit / national group's as-if picture\"), so a level-88 whose conditional variable is a `GROUP-USAGE NATIONAL` group is weighed by the ALPHANUMERIC collating sequence over a national image — MEASURED as a runtime wrong answer: the group leg answers FALSE where the declared national sequence requires TRUE, and the identical elementary 88 is LOUDLY DECLINED"
id: PB728
kind: defect
status: landed
severity: MAJOR
area: codegen/conditions
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.18.29.4", "8.8.4.2.9", "8.8.4.2.8", "13.18.63.3", "14.7.8"]
inventory_rows: []
tags: [cobolsharp, work, defect, train-14]
---

# PB728 — LANDED — the level-88 group arm takes the wrong collating sequence, and it also slips the screen that would have declined it

Found by the [[PB678]] implementer (report §N2) by inspection, filed with the explicit caveat *"Repro: not yet
cut … measuring it is the note's first step (`feedback_reachability_is_measured_not_deduced` applies: this is a
hypothesis, not a diagnosis)"*. **That step is done. It is not a hypothesis — it is a runtime wrong answer, and
the measurement found a second harm the report did not have.**

## The facts

**What the standard says.** A national group is not a group for comparison purposes — it *is* an elementary
national item, and the category is national, not null:

- `cite.py --check 13.18.29.4 "a national group is treated as though it were an elementary data item of usage
  national and class and category national"` → **OK §13.18.29.4 2) b)** (and GR2a: *"The subject of the entry is a
  national group; its class and category are national"*).
- `--check 13.18.29.4 "a bit group is treated as though it were an elementary data item of usage bit and class and
  category boolean"` → **OK §13.18.29.4 1) b)**.
- `--check 8.8.4.2.9 "national"` → **OK §8.8.4.2.9 (Comparison of national operands)** — national operands compare
  under the national sequence; §8.8.4.2.8 is the boolean twin (compare by value, right-extended with boolean
  zeros).
- `--check 14.7.8 "collating sequence"` → **OK §14.7.8 2)** — *"When the range of values is defined by alphanumeric
  or national literals, the range of values depends on the collating sequence used for evaluation of the range."*
  So for a THROUGH range the sequence is not decoration; it decides membership.

**What the code does.** `src/Cobol.Net.Compiler/CodeGen/Emit/ConditionRenderer.cs:464`, inside
`RenderMembershipTest`:

```csharp
var cat = parent.Pic?.Category;
string collate = ctx.CollateArgFor(cat);
```

`Pic` is **null for every group**, so `cat` is null and `CollateArgFor(null)` returns `""`
(`EmitCore.cs:58-63` — `CollatingClass.National → NatCollateArg`, `Alphanumeric → CollateArg`, `_ → ""`)… except
that the operand text three lines earlier has already been rendered through the string path, which for a national
group emits `.AsNat()`. The two halves disagree about what the operand IS.

The correct reader is 169 lines up in the same file, `StringCategoryOf` at `:295`, whose own comment states the
contract: *"**THE ONE category reader (D20/PB79)**: an elementary item's picture, **a bit / national group's
as-if picture**; an alphanumeric group has none and takes the alphanumeric (image) branch."* It reads
`f.Place.Item.OperandPic?.Category`, and `DataItem.OperandPic` is `Pic ?? AsIfPic` (`Model/DataItem.cs:83`) —
exactly the §13.18.29.4 GR1b/GR2b as-if picture. **One rule, written in two places, and only one of them was
updated** (`feedback_one_rule_one_place`, `feedback_two_arm_dispatch`).

**MEASURED — a runtime wrong answer, not an inspection.** Compiler on main (`0db46983`), sources in
`…/scratchpad/reg14/`. The discriminator declares two DIFFERENT sequences so neither leg can pass for the wrong
reason: `ALPHABET REV-AN IS "CBA"` (⇒ C < B < A) and `ALPHABET REV-NAT FOR NATIONAL IS N"BAC"` (⇒ B < A < C),
both installed by `OBJECT-COMPUTER. XCOMP PROGRAM COLLATING SEQUENCE IS REV-AN REV-NAT.`

`nat88b.cob` — equality form, one 88 over a `GROUP-USAGE NATIONAL` group and one over a national elementary.
Emitted C# (`nat88b.g.cs:58` and `:63`):

```csharp
if ((CobolString.Compare(GN.AsNat(), "ABC", __COLLATE)     == 0))   // 88 over the national GROUP  ← alphanumeric weights
if ((CobolString.Compare(EN,          "ABC", __COLLATE_NAT) == 0))  // 88 over the national ELEMENTARY ← correct
```

The image is national (`.AsNat()`), the weights are alphanumeric. Positive identification, not inference.

`nat88d.cob` — the same 88 as a THROUGH range, `88 GN-IN-RANGE VALUE N"AAA" THRU N"CCC"`, with `GN-A` holding
`N"CCC"`, run:

```
GROUP=FALSE
```

emitted as `CobolString.Compare(GN.AsNat(), "AAA", __COLLATE) >= 0 && CobolString.Compare(GN.AsNat(), "CCC",
__COLLATE) <= 0`. Under the declared **national** sequence REV-NAT (B < A < C) the range N"AAA" THRU N"CCC" is a
valid ascending range and `N"CCC"` is its upper bound, so the conforming answer is **TRUE**. Under the
alphanumeric REV-AN (C < B < A) the same range is INVERTED and therefore empty, so the answer is FALSE. **The
compiler answers FALSE. Silently.**

## The second harm the report did not have: the group also slips the screens

The identical THROUGH over a national **elementary** does not compile at all —
`src/Cobol.Net.Compiler/Binding/DataBinder.cs:2556`:

```
nat88c.cob(16,11): error COBOLNET0899: condition-name 'EN-IN-RANGE': a THROUGH range over a national
conditional variable (ordered by the national collating sequence) is recognized but not yet implemented
(Phase 4a residue) - (ISO 13.18.63 SR31)
```

The group form draws **nothing** and proceeds to emit the wrong answer. Same for the boolean ban: `bit88.cob`
puts the identical `88 … VALUE B"000" THRU B"111"` on a `GROUP-USAGE BIT` group and on a `PIC 1(3) USAGE BIT`
elementary — only the elementary is reported (`COBOLNET0898`, §13.18.63.3 SR29: `cite.py --check 13.18.63.3 "If
the category of the subject of the entry is boolean, the THROUGH phrase shall not be specified"` → **OK
§13.18.63.3 29)**). Both screens read `parent.Pic` (`DataBinder.cs:2553` and `:2556`) and a group has none.

⛔ **That half is [[PB575]]'s, not this note's** — PB575 already owns "every Format-3 VALUE guard in
`BindCondition` reads `parent.Pic`, and a BIT or NATIONAL GROUP conditional variable has none". It is recorded
here because the two COMPOUND and must be fixed together: PB575 is why the source reaches codegen at all, PB728
is what codegen then answers. Fixing PB575 alone converts today's silent wrong answer into a loud decline (an
improvement, and it would MASK PB728 for the national THROUGH shape while leaving the equality shape — which
PB575's screens do not touch — silently wrong).

**Note the boundary carefully:** the emitted equality form above (`nat88b`) is reached with NO diagnostic from
either note's screens, at every edition. That is the shape with no other owner.

## Why it matters

A condition-name test is the most-written conditional in COBOL, and a `GROUP-USAGE NATIONAL` group with an 88 on
it is the natural way to write one over a multi-field national key. The answer is wrong, the wrongness is
invisible (no diagnostic, correct-looking generated code), and it only manifests when a non-native collating
sequence is declared — i.e. in exactly the internationalized programs GROUP-USAGE NATIONAL exists to serve. Under
the default native sequence the two collations coincide, so **every existing green test passes**
(`feedback_green_gates_arent_evidence`): the corpus has no case that declares different alphanumeric and national
alphabets and then writes an 88 over a national group.

## The fix shape

`RenderMembershipTest` must take its category from the ONE reader, not from `Pic`:

```csharp
var cat = parent.OperandPic?.Category;
```

⛔ **Do not stop at the one line** (CLAUDE.md rule 4/5). `RenderCondition88` at `:435` has the same shape —
`c.Parent.Item.IsGroup || c.Parent.Item.Pic?.Category is …` — where the `IsGroup` disjunct accidentally covers the
national/bit group for the *isString* decision but for the wrong reason (it treats them as alphanumeric groups),
and `:402` (`fld?.Place.Item.Pic?.Category is PicCategory.Numeric`) is a third. The right change is to make
`Pic?.Category` unavailable as a category source in this file: route every category read through
`StringCategoryOf`/`OperandPic`, and pair it with a drift test that fails if a new `Pic?.Category` appears in
`ConditionRenderer.cs` — a semgrep rule beside the existing `bound-node-carries-rendered-text` is the cheap form,
since `scripts/semgrep/baseline.json` is already the mechanism for "this pattern shall not grow".

Ships with: a positive golden at 2023 that declares REV-AN and REV-NAT with DIFFERENT orders and asserts the
group 88 answers under REV-NAT (the `nat88b`/`nat88d` shape — the deliberately-different-orders discipline
[[PB678]]'s `pb678_sort_national_key_collating` already uses), the boolean twin (a `GROUP-USAGE BIT` 88, boolean
right-zero extension per §8.8.4.2.8), and the ALPHANUMERIC-group control that must stay on `__COLLATE`
(`feedback_edition_gate_sweep`'s over-rejection twin: the fix must not move the alphanumeric group). Prove the
golden load-bearing by swapping the two alphabets and watching it fail.

## Siblings / sweep

- [[PB575]] — the BINDER half of the same `parent.Pic`-is-null family (SR10 size limit, the class funnel, the
  SR29 boolean ban and the national stage all fall through on a group). **Land together.**
- The comparison, MOVE and INSPECT paths that already read `OperandPic` are the correct arm and are the model —
  `StringCategoryOf` names D20/PB79 as the decision. Sweep the tree for the complement:
  `grep -rn "Pic?\.Category" src/Cobol.Net.Compiler/CodeGen/` and ask of each hit whether a bit/national GROUP can
  reach it. `ConditionRenderer.cs` has four (`:402`, `:435`, `:464`, plus the three `PicCategory.ObjectReference`/
  `Pointer`/`ProgramPointer` tests at `:120`/`:131`/`:141`, which are safe only because no group can carry those
  categories — state that, do not assume it).
- [[PB678]] (landed) — the SORT/MERGE comparator selected by key CLASS: the same rule (§13.18.29.4 GR1b/GR2b, a
  group operating as an elementary item of its own class), fixed for sort keys and still wrong for condition
  names. The pattern's third face.
## Landed - 2026-09-06, by the [[PB741]] fix-lane sweep (same file, same mechanism)

PB741 is the twin defect one arm away: the relation renderer asked a ONE-operand classifier a TWO-operand
question. Its root fix - the comparison class is decided ONCE, from BOTH operands, through
`CollatingSelection.ForComparison`, and `EmitContext.CollateArgFor` takes the pair - swept every category
read in `ConditionRenderer.cs`, and this note's site was one of them.

**What landed here.**

- `RenderMembershipTest` reads `parent.OperandPic?.Category` (the ONE reader, ISO 13.18.29.4 GR1b/GR2b) and
  routes it through `CollateArgFor(cat, cat)`. A level-88 VALUE literal is always of the conditional
  variable's own category (13.18.63.3 SR2/SR4/SR5/SR10 - each `cite.py --check`ed), so passing it twice is
  stated, not assumed. The `pad: '0'` decision reads the same category, so a BIT group now gets 8.8.4.2.8's
  boolean-zero extension instead of a space pad.
- `Visit(BoundRangeMembership)` (the EVALUATE THRU carrier) had the SAME hole in a second spelling - a local
  `IsNationalOperand` reading `Place.Item.Pic` - and now asks
  `CollateArgFor(StringCategoryOf(Left), StringCategoryOf(Lo))`, which also picks up a national ref-mod
  slice and a national function result.
- `RenderCondition88`'s `isString` reads `OperandPic` too. Byte-identical today (`IsGroup` covered those
  groups) but for the right reason, which is what this note asked for.
- The note's remaining `Pic?.Category` sites were examined and STATED rather than assumed: the object /
  pointer / program-pointer tests (`:135`/`:146`/`:156`) and `RenderClass`'s numeric test read `Pic` safely,
  because the only as-if pictures that exist are a bit group's (boolean) and a national group's (national),
  so no group can carry those four categories. Each now carries that sentence in the source.
- The preventive the note asked for was, at THIS landing, a unit text scanner
  (`CollatingComparisonClassDriftTests.NoNewCategoryRead_GoesThroughPic_WhereAGroupCanReachIt`) permitting the
  four group-proof categories plus one named exemption, `IsNationalOperand`'s field arm. **The train-18 landing
  below closed that exemption and routed the four group-proof reads through `OperandPic` too, so the rule became
  ABSOLUTE** and moved to where an absolute source-text rule belongs: the semgrep invariant
  `cobolnet-condition-category-from-raw-picture`, scoped to `ConditionRenderer.cs`, with a deliberate violation
  in the semgrep testdata. The scanner was DELETED rather than kept beside it (one mechanism per job); the
  drift-test file keeps its behavioural facts - the comparison-class matrix as data and the two end-to-end
  shapes.

**MEASURED, before and after, by temporarily reverting the two sites and rebuilding.** This note's own
`nat88b`/`nat88d` shapes, re-cut as `scratchpad/PB741/probe/pb728repro.cob`: the national-group 88 emitted
`CobolString.Compare(GN.AsNat(), "ABC", __COLLATE)` and now emits `__COLLATE_NAT`; the THROUGH form answered
`FALSE` and now answers `TRUE`; the BIT group moved from `__COLLATE` with no pad to `pad: '0'` with no
collate; the ALPHANUMERIC group control stayed on `__COLLATE` (the over-fix guard).

**Golden:** `tests/conformance/2002/pb728_level88_group_usage_collating.cob` (+ `.out`, + the 2002 manifest)
- `ALPHABET REV-AN IS "CBA"` against `ALPHABET REV-NAT FOR NATIONAL IS N"ABC"` (opposite orders, so no leg
can pass for the wrong reason), V01 the national-group range discriminator, V02 its relation twin (8.8.4.5.3
GR2 says they are the same comparison), V03/V04 the alphanumeric group and elementary that must NOT move,
V05 the boolean group's pad arm. Every expected value derived in the program's own header.

**One detail of this note did not hold:** `CollateArgFor(null)` returned `CollateArg` (the alphanumeric
weight table), not `""` - the emitted `__COLLATE` the note measured is the correct reading, and the harm is
the one the note described.

**Left open by THIS landing, and closed by the next one:** [[PB575]] - the BINDER screens
(`DataBinder.cs:2553`/`:2556`) still read `parent.Pic`, so the 13.18.63.3 SR29 boolean-THROUGH ban and the
COBOLNET0899 national-THROUGH residue did not fire for a GROUP conditional variable. This note explicitly
assigned that half to PB575; **it landed in train 18, in the section below.**

## Landed 2026-09-06 (train 18) — the eleven-arm sweep, and [[PB575]], onto the pair classifier

⛔ **Read this section on top of the PB741 one above.** PB741 landed the comparison-class MECHANISM
(`CollatingSelection.ForComparison(left, right)` and the paired `EmitContext.CollateArgFor(left, right)`,
whose one-argument overload is deleted) and with it the level-88 membership and EVALUATE THRU sites. This
section is the INDEPENDENT sweep of the same rule, reconciled onto that mechanism: every site it reclassified
asks the pair classifier, and the arms below are the ones PB741 did not reach.

**Reproduced first**, on this worktree's own build (base origin/main `0db46983`, merged forward to `0a24276f`):
`nat88d` printed `GROUP=FALSE`; `nat88b`'s group leg emitted `CobolString.Compare(GN.AsNat(), "ABC", __COLLATE)`
against the elementary twin's `__COLLATE_NAT`; `nat88c` declined only the ELEMENTARY leg (COBOLNET0899) and
`bit88` only the elementary leg (COBOLNET0898). Every fact in this note held.

**One correction to the note's mechanism sentence.** `CollateArgFor(null)` does NOT return `""` — the parenthetical
in "The facts" is wrong. A null category classifies ALPHANUMERIC ("A null picture is an ordinary (alphanumeric)
group — §8.8.4.2.1 'For comparison, an alphanumeric group item shall be treated as an elementary alphanumeric
data item'", stated on that classifier; PB741's citation sweep corrected the §8.8.4.2.3 SR2 spelling this
implementer inherited — SR2 is the identifier-CLASS syntax rule and says nothing about groups), so a null
category returns `, __COLLATE`. That is why the emitted evidence in this very note shows `__COLLATE` and not an
absent argument, and it is also what made the ALPHANUMERIC-group control safe to preserve.

**The rule, re-derived.** §8.8.4.5.3 2) — "The rules for comparing a conditional variable with a condition-name
value are the same as those specified for relation conditions" — hands the question to §8.8.4.2.1, whose group
sentence is the whole answer: "For comparison, an alphanumeric group item shall be treated as an elementary
alphanumeric data item. … A national group item or a bit group item shall be treated as an elementary national
data item or an elementary bit data item, respectively." So the SUBJECT'S CLASS selects §8.8.4.2.9 (the national
program collating sequence), §8.8.4.2.8 (a boolean VALUE comparison with right zero extension, no collating
sequence at all) or §8.8.4.2.7 (the alphanumeric sequence) — for a group exactly as for an elementary item.
§14.9.39.4 6) settles the WRITING side by naming the population outright: "when the conditional variable is an
alphanumeric group item, bit group item, or national group item …". All cite-checked.

**The fix is ONE classification, in the one place each phase decides it.** `DataItem.OperandPic` — the ONE reader
already named by `StringCategoryOf`, `CollatingSelection` and `SortCollation` — replaced every raw picture read
on the condition-name path: `RenderCondition88` now classifies the conditional variable ONCE and passes the
category down to `RenderMembershipTest` (which no longer derives one, and asks
`CollateArgFor(cat, cat)` / `CollatingSelection.ForComparison(cat, cat)` — PB741's pair rule, never a second
classifier), and `ConditionRenderer.cs` has **zero** raw picture reads left, enforced by a new semgrep rule
(`cobolnet-condition-category-from-raw-picture`) whose behavioural twin is
`ConditionNameSubjectClassDriftTests`.

**Five sibling arms the sweep found, all measured, all fixed by the same reader:**

1. **The EQUALITY form** — the shape this note says has no other owner. With `ALPHABET AN-EQ IS "A" ALSO "B"`
   (A and B at ONE position) and `ALPHABET NAT-D FOR NATIONAL IS N"A" N"B"` (distinct), `88 GN-IS-A VALUE N"AAA"`
   over `N"BBB"` answered **TRUE** where its elementary twin answered FALSE. Now both FALSE.
2. **The BOOLEAN form** — `cat` null meant no `pad: '0'`, so §8.8.4.2.8's right extension with boolean zeros was
   lost: `88 GB-ON VALUE B"10"` over `B"100"` answered **FALSE** where the elementary twin answered TRUE.
3. **The figurative WIDTH** — `StringMembershipValue`'s `Pic?.Length ?? ImageWidth` fell back to the group's
   STORAGE unit: `88 GB-ALL1 VALUE ALL B"1"` on a 3-bit group repeated to **one** position (its one byte), so
   §8.3.3.6.4 GR2 was applied at the wrong count. `OperandPic.Length` is the as-if PICTURE's 3.
4. **`IsNationalOperand`, the private two-case copy of the class rule** reading `Place.Item.Pic.Category`.
   It fed TWO callers. Its `EVALUATE … WHEN literal THRU literal` caller — `EVALUATE GN WHEN "AAA" THRU "AAA"`
   (a §8.8.4.2.6 mixed comparison, which converts the alphanumeric operand to national) answered **IN** where
   `EVALUATE EN …` answered OUT — was independently closed by PB741, which routes that surface through
   `CollateArgFor(StringCategoryOf(Left), StringCategoryOf(Lo))`. **Its OTHER caller is the one this landing
   closes**, and PB741 reported it as a known hole it was leaving behind: `RenderClass`'s
   `__CLASSIFY.For(isNational)` argument under a `CHARACTER CLASSIFICATION` locale, where a national GROUP took
   the ALPHANUMERIC classification phrase while its elementary twin took the national one. The helper is now
   `StringCategoryOf(op) is PicCategory.National`, which also picks up the ref-mod and function-result operands
   the copy could not see.
5. **`SET condition-name TO TRUE`** — the writing arm, `SetEmitter.EmitSet`. All three group kinds fell to the
   loud default: a national / bit group emitted `FromNat(NotImplemented.Value<string>(…))` (a runtime throw) and
   an ordinary group emitted a bare `GA = <string>` that **did not compile** (CS0029), because
   `PlaceRenderer.Write`'s member arm assigns the record struct. §14.9.39.4 GR6 names all three, so all three now
   store: bit/national through `FromBits`/`FromNat`, and the ordinary group through THE ONE group-image store
   (`PlaceRenderer.WriteGroupImage`, §14.9.25.4 GR4).

**The safe siblings, stated rather than assumed:** the object-reference / pointer / program-pointer tests at the
top of `RenderRelational` cannot see a group (no group carries those categories, and a bit / national group's
as-if picture is `1(m)` / `N(m)`), and `RenderClass`'s NUMERIC test likewise. They were routed through
`OperandPic` anyway — behaviour-identical — so the drift rule can forbid a raw picture read here outright.

**PB575 landed with it**, because it is the same rule in the binder: all four `BindCondition` guards (the SR29
boolean THROUGH ban, the national THROUGH stage, the range category funnel and the singleton funnel) now read
`OperandPic`. Rows `SR-13.18.63.3-24` and `SR-13.18.63.3-29` re-verdicted PARTIAL → CONFORMS.

**What this landing deliberately did NOT do — and what the train did instead.** This cluster's plan was to leave
the pre-existing COBOLNET0899 national-THROUGH stage standing and merely EXTEND it to the GROUP shape, so the
two shapes would at least agree, reporting the discharge as a fresh finding ([[PB761]]). ⛔ **That interim did
not survive contact with [[PB741]]**, which had landed hours earlier: PB741's golden
`tests/conformance/2002/pb728_level88_group_usage_collating` asserts a national GROUP THROUGH range COMPILING
AND ANSWERING CORRECTLY under a non-native national alphabet, so extending a "not yet implemented" stage over it
turned a landed golden RED. The train measured the stage instead of choosing a side — with the guard disabled,
the elementary and group shapes both answer correctly and both flip with the national alphabet — and
**discharged it**, in its own commit. See [[PB761]]'s landed section for the measurement, the citation finding
and the goldens; the under-rejection that discharge would otherwise have opened (alphanumeric literals on a
national subject) is closed in the same change set. And PB575's SR10 **size** claim is mis-scoped:
`ValueSubject.ForConditionName` (kb/Work PB598) derives, with citations, that SR4/SR5/SR10's size sentences name
"an elementary item" and a "group item" as the VALUE-clause bearer and so do not reach a Format-3 subject at all —
an oversize boolean literal on a bit group is legal source that is permanently false (§8.8.4.5.3 2)), not a wrong
acceptance. Only PB575's CLASS claim was a defect, and that one is closed.

**Ships with:** the positive golden `tests/conformance/2023/pb728_condition_name_group_subject_class.cob`
(fourteen assertions across all five arms, with the deliberately-different-alphabets discipline; proven
load-bearing by swapping the two `ALSO` phrases, which flips six of them), three negatives
(`pb728-cond-name-bit-group-through`, `pb575-cond-name-bit-group-alnum-value`,
`pb575-cond-name-national-group-alnum-value`), and `ConditionNameSubjectClassDriftTests` — six facts, every one
proven to FAIL against the reinstated defect before being trusted.

**Both goldens are kept, and they assert different arms.** PB741's
`tests/conformance/2002/pb728_level88_group_usage_collating` pins the national-group RANGE against its relation
twin (§8.8.4.5.3 GR2) and the two over-fix guards, under `REV-AN`/`REV-NAT`; this landing's
`tests/conformance/2023/pb728_condition_name_group_subject_class` pins the EQUALITY form, the boolean pad, the
figurative width and all three `SET … TO TRUE` stores, under `AN-EQ`/`NAT-D`. Neither is a subset of the other.
