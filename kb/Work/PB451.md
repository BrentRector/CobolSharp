---
title: "PB451 — SET Format 5's receiver discrimination is implemented on ONE arm of each of two dispatches: the class-name sender never asks interface-vs-class, so the rule that PERMITS (SR11) and the rule that RESTRICTS (SR13) collapse into one refusal of legal source; the SELF sender's class leg omits the placement test its interface twin has, and the emitter then hands the user a Roslyn CS0030 against generated code"
id: PB451
kind: defect
status: landed
severity: MAJOR
area: binder/oo
wrong_answer: false
crashes: true
silent: false
rejects_legal_source: true
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.39.2", "14.9.39.3", "14.9.39.4", "11.4.4", "9.3.11", "13.18.60"]
inventory_rows: []
tags: [cobolsharp, work, defect]
---

# PB451 — SET Format 5's receiver discrimination is half-written on two dispatches

Lane-3 adjudication batch b2, group g7-set-a. **Every repro below was re-run on HEAD `89044aa0`**; all still
reproduce.

## 1. THE MECHANISM

§14.9.39.3's Format-5 rules are organised on **two axes at once**: which *sender* is written
(identifier-4 / object-class-name-1 / SELF / NULL) and how the *receiver* is described (interface-name /
object-class-name / ACTIVE-CLASS / universal, and with or without FACTORY and ONLY). `OoBindSetObjectRef`
(`src/Cobol.Net.Compiler/Binding/Procedure/Verbs/OoBinder.cs:855-977`) dispatches on the sender, and then
inside two of those sender arms it must dispatch again on the receiver. **Both inner dispatches were written
with one arm finished.**

**Dispatch A — the SELF sender** (`OoBinder.cs:902-926`). The receiver loop splits on what the receiver's type
name resolves to:

    if (host.OoClasses?.Find(tcn) is { } tcls)                       // :905  CLASS receiver
        if (!cur.ConformsTo(tcls)) … SR12c2                          // :907-910   ← the ONLY test
    else if (host.OoClasses?.FindInterface(tcn) is { } tiface        // :920  INTERFACE receiver
             && !host.OoClasses.ImplementsClosure(cur, host.OoInFactory).Contains(tiface))
        … SR10d1 / SR10d2                                            // :922-925

The interface leg threads `host.OoInFactory` through and even names SR10d1 vs SR10d2 — it **knows** whether the
SET sits in a factory or an instance method. The class leg three lines above never asks. SR12 c) imposes four
conditions on a SELF sender into a class-typed receiver and the class leg implements exactly one of them, c)2.
`host.OoInFactory` appears in this whole arm exactly once, at :921.

That omission is not a soft miss. `OoEmitter.EmitSetObjectRef` (`CodeGen/Verbs/OoEmitter.cs:398-429`) renders
`({tp.Item.Pic!.ClrType})({src})` with `src == "this"` under a doc-comment (`:395-397`) that asserts "**The cast
renders total: conformance was bind-checked (0867)**". SR12 c)3. is precisely the check that makes that
invariant true. In a factory method `this` is the synthesized factory singleton `CLS__FACTORY`, unrelated to the
instance type `CLS`, so the emitted C# does not compile and the user gets a Roslyn error naming a type that
appears nowhere in their program.

⛔ **This is the SAME failure the SR10d fix already repaired for interface receivers, on the sibling arm.** The
comment at `OoBinder.cs:912-919` describes it verbatim ("the emitter rendered a raw `(I)(this)` cast — a runtime
InvalidCastException, or a Roslyn CS error on generated user source …, which the G4 no-CS-on-user-source rule
forbids"). Ask which arm you fixed.

**Dispatch B — the object-class-name sender** (`OoBinder.cs:951-966`). The arm tests only
`tp.Item.Pic!.ObjectClassName is not null` — "is the receiver typed at all" — and refuses **every** typed
receiver with one staged-loud message that names SR13. For a receiver described with an object-class-name that
outcome is right (SR13 requires the FACTORY phrase, which cannot be declared). For a receiver described with an
**interface-name** the governing rule is **SR11**, which *explicitly permits* the assignment when the sender
class's factory carries a matching IMPLEMENTS clause — and that conforming program is refused too, by a message
naming a rule whose own precondition is false for it. The discrimination needed is a call away and is used twice
in this very file (`FindInterface` at :920; `OoConformance.ObjectRefWideningMismatch`).

**The descriptor underneath both.** `PicInfo` (`Binding/Model/PicInfo.cs:345`) models §13.18.60's four-axis
object-reference operand — kind · name · FACTORY · ONLY — as ONE nullable string, `ObjectClassName`. "Typed AND
factory" is therefore not an expressible state, which is why SR13's own mandated receiver shape cannot be
written, why SR13 a)/b) are unreachable, and why SR14's ACTIVE-CLASS rules have no subject.

**And the closure is the wrong relation for SR10 d).** `ImplementsClosure`
(`src/Cobol.Net.Compiler/Oo/OoClassTable.cs:55-71`) walks the *transitive* implements relation — direct
IMPLEMENTS, plus every interface those inherit, plus every inherited class's implements. SR10 d) asks a
**literal** question about the clause text.

> ⛔ **THIS PARAGRAPH IS WRONG AND WAS WITHDRAWN 2026-09-09 — see the LANDED section at the foot of this note,
> item 4.** "Described with an IMPLEMENTS clause that references int-1" is the standard's prose for the
> §11.4.4 / §11.8.4 GR2 relation, not a literal claim about the clause's operand list; §9.3.8.2.3 rule 5 b)2.a.
> states the same obligation with that spelling where SET SR10 b)1. writes "shall implement int-1". Acting on
> this paragraph would have made the compiler REJECT LEGAL SOURCE. `ImplementsClosure` is correct; the three
> probes cited below as evidence against it (`r01`, `r02`, `r06`) are CONFORMING programs and now ship as
> POSITIVE legs of `conformance:2002/pb451_set_format5_self_sender`.

## 2. THE RULES, QUOTED, WITH THEIR `--check` LINES

    python scripts/spec/cite.py --check 14.9.39.3 "Identifier-3 shall be any item of class object that is permitted as a receiving item"
    OK  §14.9.39.3 8)  (Syntax rules)

    python scripts/spec/cite.py --check 14.9.39.3 "if the SET statement is contained in a method within the factory definition of the class, that factory definition shall be described with an IMPLEMENTS clause that references int-1"
    OK  §14.9.39.3 10)  (Syntax rules)          ← SR10 d)1.

    python scripts/spec/cite.py --check 14.9.39.3 "if the SET statement is contained in a method within the instance definition of the class, that instance definition shall be described with an IMPLEMENTS clause that references int-1"
    OK  §14.9.39.3 10)  (Syntax rules)          ← SR10 d)2.

    python scripts/spec/cite.py --check 14.9.39.3 "the factory object of object-class-name-1 shall be described with an IMPLEMENTS clause that references int-1"
    OK  §14.9.39.3 11)  (Syntax rules)

    python scripts/spec/cite.py --check 14.9.39.3 "the class containing the data item referenced by identifier-4 shall be the same class or a subclass of the class specified in the description of the data item referenced by identifier-3"
    OK  §14.9.39.3 12)  (Syntax rules)          ← SR12 c)2., the ONE condition implemented

    python scripts/spec/cite.py --check 14.9.39.3 "if the data item referenced by identifier-3 is described without a FACTORY phrase, the method containing the SET statement shall be defined in the instance definition of its containing class"
    OK  §14.9.39.3 12)  (Syntax rules)          ← SR12 c)3.

    python scripts/spec/cite.py --check 14.9.39.3 "if the data item referenced by identifier-3 is described with a FACTORY phrase, the method containing the SET statement shall be defined in the factory definition of its containing class"
    OK  §14.9.39.3 12)  (Syntax rules)          ← SR12 c)4.

    python scripts/spec/cite.py --check 14.9.39.3 "the data item shall be described with the FACTORY phrase"
    OK  §14.9.39.3 13)  (Syntax rules)

    python scripts/spec/cite.py --check 14.9.39.3 "If the data item referenced by identifier-3 is described with an ACTIVE-CLASS phrase, the data item referenced by identifier-4 shall be one of the following"
    OK  §14.9.39.3 14)  (Syntax rules)

    python scripts/spec/cite.py --check 14.9.39.4 "a reference to the factory object of the class identified by object-class-name-1 is placed into each data item referenced by identifier-3 in the order specified"
    OK  §14.9.39.4 10)  (General rules)

⚠ **A `--check` OK is evidence about the TEXT, not about which rule you meant, and this clause proves it.**
SR14 b)1. and b)2. are **word-for-word identical** to SR12 c)3. and c)4.; `--check` on that sentence returns
**12)**, never 14). So rows L8.1/L8.2 cannot be pinned by that fragment — they are pinned by SR14's own lead-in
(returned above as 14)) plus the observation that its sub-rules repeat SR12 c)'s text verbatim. That identity is
not a nuisance: **it is the argument for one shared placement predicate** (§4).

**The SR10 d) reading, stated plainly because it is interpretive.** SR10 spells inheritance out wherever it
means it — a) says "an interface-name that identifies int-1 **or an interface inheriting from int-1**", and
b)/c) say "shall **implement** int-1", which §9.3.11 / §11.4.4 GR1 make the transitive relation. d) says
*neither*: it says the definition "shall be **described with an IMPLEMENTS clause that references int-1**". On
that reading `ImplementsClosure` is strictly more permissive than the rule, and the leniency is not
dialect-gated.

## 3. EXPECTED vs OBSERVED — RE-RUN ON HEAD 89044aa0

Probes from `.../lane3/out-b2/probe/set1/`, `.../setp3/` and `.../rf-setp3/`, all `--std 2023 --run`:

    # SR11 — legal source REFUSED, with a citation to the wrong rule
    cobol p5_classname_into_iface.cob     # 01 RFI USAGE OBJECT REFERENCE IFAC ; CB's FACTORY. IMPLEMENTS IFAC.
      (25,12): error COBOLNET0867: SET 'RFI' TO CB: a factory-object sender (SR13) into a TYPED receiver
               needs the FACTORY OF usage phrase - not yet carried (universal receivers accept it)
      expected: compiles; the following INVOKE RFI "MK" prints MADE
    cobol p10_classname_into_typed.cob    # differs ONLY in `01 OB USAGE OBJECT REFERENCE CB`
      (25,12): error COBOLNET0867: … the IDENTICAL message
      → the two programs differ only in the receiver's declared kind and the compiler cannot tell them apart

    # SR12 c)3. — a Roslyn error against generated code instead of a bind diagnostic
    cobol j1_self_in_factory.cob
      error: backend compilation failed (generated C# at j1_self_in_factory.g.cs):
        (37,30): error CS0030: Cannot convert type 'CFAC__FACTORY' to 'CFAC'
    cobol p06_sr12c3.cob
        (87,29): error CS0030: Cannot convert type 'CC3__FACTORY' to 'CC3'
    cobol p11_sr12c3_hier.cob             # the same violation across a hierarchy
        (99,29): error CS0030: Cannot convert type 'CSUBB__FACTORY' to 'CBASEB'
      expected: rejected at bind, citing §14.9.39.3 SR12 c)3. against the SET statement's own source line —
                the exact twin of the SR10d message the interface leg already emits

    # the controls that make it an ARM asymmetry rather than a general failure
    cobol k1_self_in_instance.cob    → SELFOK                      (legal instance-method SELF runs)
    cobol p08_sr12c2_pos.cob         → SPONG                       (the class leg is live)
    cobol p07_sr12c2_neg.cob         → COBOLNET0867 … (ISO §14.9.39.3 SR12c2)   (its one condition fires)
    cobol p04_sr10d1_neg.cob         → COBOLNET0867 … the factory definition of class 'CFD1' does not
                                       IMPLEMENT interface 'IFD1' (ISO §14.9.39.3 SR10d1)
                                       — the SAME placement question, caught, on the interface arm

    # SR10 d) — the closure is more permissive than the rule text
    cobol r01_d2_inherited_iface.cob  → R01PONG    # OBJECT. IMPLEMENTS IR1B; IR1B INHERITS FROM IR1A;
                                                   # receiver typed IR1A. The clause references IR1B, not int-1.
    cobol r02_d2_inherited_class.cob  → R02PONG    # CDER2's instance definition carries NO IMPLEMENTS CLAUSE
                                                   # AT ALL; only its base CBAS2's does. Receiver typed IR2.
    cobol r06_d1_inherited_iface_factory.cob → R06FPONG            # the same on the FACTORY arm

    # the descriptor: SR13's own mandated receiver, and SR13 a)'s ONLY phrase, cannot be declared
    cobol p3_factory_usage.cob
      (15,8): error COBOLNET0899: USAGE OBJECT REFERENCE FACTORY OF (a factory-object reference,
              ISO §13.18.60) is recognized but not yet implemented (the universal-reference wave)
    cobol p7_only_phrase.cob
      (15,40): error COBOL0307: unexpected 'ONLY'.

There is **no reading** of "described with an IMPLEMENTS clause that references int-1" under which a definition
with no clause at all satisfies it, so `r02` is the sharpest of the three.

## 4. THE CODE SITES AND THE SHAPE OF THE REPAIR

| arm | file:line |
|---|---|
| SELF arm, CLASS leg (one condition) | `OoBinder.cs:905-911` |
| SELF arm, INTERFACE leg (has the placement test) | `OoBinder.cs:920-925` |
| class-name-sender arm, blanket typed-receiver refusal | `OoBinder.cs:951-966` (the refusal at `:957-964`) |
| the emitter that assumes the bind check | `CodeGen/Verbs/OoEmitter.cs:395-397`, `:423-428` |
| the over-permissive relation | `src/Cobol.Net.Compiler/Oo/OoClassTable.cs:55-71` `ImplementsClosure` |
| the scalar where the spec has a tuple | `src/Cobol.Net.Compiler/Binding/Model/PicInfo.cs:345` `ObjectClassName` |
| the receiver shapes the grammar lacks | `src/Cobol.Net.Frontend/Grammar/CobolOO.g4` `objectReferenceUsage` |

⛔ **Four repairs, each structural, and none of them a new arm** (CLAUDE.md rule 5):

1. **One placement predicate.** `RequireSelfPlacement(receiverDescriptor, host.OoInFactory)`, hoisted out of the
   interface leg so **both** receiver kinds run it. SR12 c)3./c)4. and SR14 b)1./b)2. are the same sentence
   twice; writing it once closes four rows and keeps SR14 automatic when the ACTIVE-CLASS descriptor lands.
2. **Branch dispatch B on `FindInterface` first.** An interface-typed receiver runs SR11's factory-side
   `ImplementsClosure(cls, factory: true)` test (already used by `ValidateImplements`); only a class-typed
   receiver reaches SR13's refusal. Note `OoConformance.cs` computes the *instance*-side closure
   unconditionally, so the same factory/instance distinction SR10 b)1.-vs-b)2. draws is missing in the same
   place — one fix, two rules.
3. **`ObjectClassName` becomes `ObjectRefDescriptor(Kind, Name, Factory, Only)`** and `objectReferenceUsage`
   grows the `FACTORY OF` and `ONLY` shapes. Until it does, SR13 a)/b), SR12 a)1./b)1./c)1., SR14 and GR10's
   typed form have no expressible input, and every screen above is guessing from a string.
4. ~~**SR10 d) screens direct membership**, not the closure: `c.FactoryImplements` / `c.Implements` for the
   *containing* definition, at `OoBinder.cs:921`, with the transitive closure kept for b)/c) where the standard
   asks for "implement".~~ ⛔ **WITHDRAWN 2026-09-09 — this repair would have rejected legal source.** See the
   LANDED section, item 4, for the four grounds from the standard.

## 5. THE SIBLING SWEEP THE FIXER MUST DO

* Every other `ImplementsClosure` caller — `ValidateImplements`, `ObjectRefWideningMismatch`, the INVOKE
  conformance path — must be re-read against whether its rule says "described with an IMPLEMENTS clause" or
  "shall implement". They are different relations and the code has one.
* `OoBinder.cs:937-947` (the identifier-4 sender arm) tests `tp.Item.Pic!.ObjectClassName is not null` with the
  same "typed at all" shape; SR12 a)/b)/c) and SR10 a)/b)/c) need the same interface-vs-class split.
* The universal-receiver lane is where every factory assignment currently lives (`p4_gr10_universal` runs and
  reaches the FACTORY method) — every one of those programs silently gives up static conformance, so the
  descriptor landing must sweep them.
* [[PB456]] is the *selection* half of Format 5: these screens only run when the re-route fires at all.

## 6. ROWS

* **`GR-14.9.39.4-10`** — NOT-IMPLEMENTED in the form SR13 requires. The universal lane runs and places the
  factory object into each receiver in order (semantics correct where writable); the verdict rests on the
  SR13-mandated `FACTORY OF` receiver being refused at declaration (`p3_factory_usage`).
* **`SR-14.9.39.3-11`** — DIVERGES. Rests on `p5_classname_into_iface`: conforming source refused, and the
  message names SR13, whose precondition is false for it.
* **`SR-14.9.39.3-12`** — DIVERGES. Rests on c)2. being the only condition implemented and c)3. producing
  CS0030 against generated code (`j1_self_in_factory`, `p06_sr12c3`, `p11_sr12c3_hier`).
* **`SR-14.9.39.3-13`** — PARTIAL. The leading requirement produces the right *outcome* for a class-typed
  receiver (`p10_classname_into_typed` refused) but for the wrong reason — a not-yet-implemented notice, not a
  rule — and both sub-rules a) and b) have no expressible input.
* **`SR-14.9.39.3-L4.1` / `-L4.2`** (SR10 d)1. / d)2.) — PARTIAL. Direct IMPLEMENTS is implemented, correctly
  keyed to the factory-vs-instance definition, and d)2. is pinned by a spec-derived positive/negative pair; the
  verdict rests on `r06` / `r01` / `r02` showing the transitive closure admitting programs the clause-text
  obligation excludes.
* **`SR-14.9.39.3-L7.3`** (SR12 c)3.) — DIVERGES. Fully reachable today and unchecked; measured CS0030.
* **`SR-14.9.39.3-L7.4`** (SR12 c)4.), **`-L8.1` / `-L8.2`** (SR14 b)1. / b)2.) — NOT-IMPLEMENTED. Their
  violating programs cannot be *written* today, because each needs a `FACTORY OF` or `ACTIVE-CLASS` receiver
  descriptor (repair 3). They are latent instances of the SAME missing predicate: if repair 3 lands without
  repair 1, the FACTORY-OF wave puts the identical CS0030 on the c)4. shape.

---

## LANDED — 2026-09-09 (`status: landed`; all ten rows CONFORMS, `inventory_rows` emptied)

**[[PB389]] closed the mechanism; this round derived, pinned and re-verdicted every row it left behind.**
The four structural repairs §4 asked for are all in the tree, and none of them is a new arm:

1. **One placement predicate.** `OoBinder.OoBindSetObjectRef`'s SELF arm is a four-arm switch over the
   receiver's `ObjectRefKind`, and `rd.Factory != host.OoInFactory` is written ONCE — serving SR12 c)3.,
   SR12 c)4., SR14 b)1. and SR14 b)2., which are the same sentence four times (`cite.py --check` on SR14
   b)1.'s text returns rule **12**, never 14; the fragment that discriminates SR14 b) is its own lead-in,
   *"the predefined object SELF"* — SR12 c) writes *"the predefined object REFERENCE SELF"*). The measured
   CS0030 against generated user source is gone: `pb451-self-sender-factory-method-instance-receiver` is now
   a bind rejection naming §14.9.39.3 SR12c3.
2. **Dispatch B branches on the RECEIVER.** The blanket `ObjectClassName is not null` refusal is gone. A
   class-NAME sender is written as the description its factory object actually has —
   `ObjectClass(name, factory: true, only: true)` — and handed to the ONE table, so **SR11 is the table's
   SR10 b)1., SR13's lead is SR12 a)3., SR13 a) is SR12 a)1. and SR13 b) is SR12 a)2.** No rule is written
   twice. `SET RFI TO CB` (the note's own `p5_classname_into_iface`, the program this defect refused)
   compiles and runs.
3. **`ObjectClassName` became `ObjectRefDescriptor(Kind, Name, Factory, Only)`** and `objectReferenceUsage`
   grew the `FACTORY OF` and `ONLY` shapes — PB389.
4. ⛔ **Repair 4 is WITHDRAWN, and this is the one place this note was wrong.** §4 asked for SR10 d) to
   screen DIRECT `IMPLEMENTS` membership rather than the transitive closure, on the reading that *"described
   with an IMPLEMENTS clause that references int-1"* is a literal claim about the clause's operand list. It
   is not; it is the standard's prose for the §11.4.4 / §11.8.4 GR2 relation, on four independent grounds:
   **(a)** §11.4.4 GR1 defines the clause THROUGH implementation — *"The IMPLEMENTS clause specifies the
   names of the interfaces that are implemented by the factory object of the containing class **according to
   9.3.11, Interface implementation**"*; **(b)** §9.3.8.2.3 rule 5 b)2.a. states the SAME obligation about
   the SAME objects with that phrasing (*"the factory object of the specified class shall be described with
   an IMPLEMENTS clause that references int-r"*) where SET SR10 b)1. writes *"shall implement int-1"* — two
   spellings, one rule; **(c)** Annex D.19.5 paraphrases §13.18.60.4 GR22 c), normatively *"shall implement
   interface-1"*, as *"an object described with an IMPLEMENTS clause that references the interface
   specified"*, so the standard glosses its own rule with the other spelling; **(d)** the reductio — under
   the literal reading, inside a subclass whose own definition carries no IMPLEMENTS clause `SET R TO SELF`
   is illegal while `SET T TO SELF` (T typed at the containing class) followed by `SET R TO T` is legal
   under SR10 b)2., identical semantics one statement apart. `OoClassTable.ImplementsClosure` is therefore
   CORRECT, and the note's `r01` / `r02` / `r06` probes are CONFORMING programs that a repair-4 fix would
   have started rejecting. All three shapes are now shipped as POSITIVE legs of
   `conformance:2002/pb451_set_format5_self_sender`, and d)1.'s refusing arm — which had no shipped witness
   at all — is `conformance:negative/pb451-self-sender-factory-not-implements`.

**One defect of this note's own was found and fixed while pinning it.** The class-name-sender arm printed
*"(ISO §14.9.39.3 SR13)"* for EVERY receiver kind, so a program refused under SR11 or SR14 was told it had
broken a rule whose own precondition is false for it — and the sender's identity, which is §14.9.39.4 GR10,
was attributed to SR13 as well. The governing rule is now READ OFF THE RECEIVER
(`OoConformance.ClassNameSenderRule`: SR11 · SR13 · SR14 · SR8) and pinned by
`ObjectRefDescriptorDriftTests.EveryObjectRefKind_HasItsOwnClassNameSenderRule`, which fails if a new
`ObjectRefKind` falls through to the discard.

**Shipped.** Two positive goldens (`pb451_set_format5_class_name_sender`, `pb451_set_format5_self_sender`)
and eleven negatives, one clause each: SR11, SR13's lead, SR13 a), SR13 b), SR14's closed list, SR12 b)2.,
SR12 c)3., SR12 c)4., SR14 b)1., SR14 b)2., SR10 d)1.

**§5's sibling sweep, discharged.** `ImplementsClosure`'s other callers were re-read against the same
question and are on the correct side of it (the relation is the transitive one everywhere the standard asks
either way). The identifier-4 sender arm's `ObjectClassName is not null` shape is gone with the scalar. The
universal-receiver lane is no longer where every factory assignment lives: `FACTORY OF` receivers are
declarable and statically checked. [[PB456]], the SELECTION half of Format 5, is untouched by this and
remains its own note.
