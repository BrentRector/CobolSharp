---
title: "PB888 — a COMPILER TEMP loses GROUP-USAGE: `DataBinder.Oo.cs#CreateCompilerTemp` and `#CloneTempNode` are the 3rd and 4th HAND-LISTED description copiers (Pic / OwnSign / OwnUsage / Justified / BlankWhenZero and nothing else), so a function result typed as a national group binds as an alphanumeric group — `FUNCTION LENGTH` over an `01 … GROUP-USAGE NATIONAL. 02 PIC N(4).` result answers 8 where the byte-identical working-storage control answers 4"
id: PB888
kind: defect
status: landed
severity: MAJOR
area: binding/oo
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: [PB889]
spec_refs: ["8.4.3.2.4", "11.7.4", "15.50.4"]
inventory_rows: []
closes_rows_reason: "GR-8.4.3.2.4-1 is one rule of four paragraphs; PB888 made its function-prototype-name-1 paragraph conform; its function-pointer-name-1 paragraph was kb/Work/PB847's arm, and PB847 LANDED on main (wave 46 group H, through the same UdfActivate result temporary) before this note did, so the train-48 lander did NOT record the PARTIAL verdict (a landed note cannot hold a defective row) and left the row GAP for a whole-rule re-adjudication."
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-19 by **REGISTRAR #6** — the lead-filing pass over the implementer, finisher and lander reports
that landed after registrar #5 (PB416, PB426, PB482, PB368, PB391, PB555, PB401, PB403, PB388, PB436, PB408,
PB447, PB419, PB427, PB443, PB396, PB411, PB420, PB425, PB877, PB391-finisher, PB405, PB522, PB502; landing
trains 33-38 and train-pb877). **Every probe below was REBUILT from the source report and RE-RUN on this
worktree's own `dotnet build CobolSharp.sln -c Debug` (0 errors) at main `80dd97e2`**, and **every citation was
re-derived with `python scripts/spec/cite.py --check`** — the command line is printed beside the quotation.
Sources under `<scratch>/reg6/probe/`.

# PB888 — LANDED — the fourth hand-written copy of a data description

Source: the `PB522` report, "New defects" item 1.

## The measurement

`<scratch>/reg6/probe/p42_udf_groupusage.cob` (PROGRAM-ID `R6P42`, FUNCTION-ID `R6NGFN`), `--std 2023 --run`:

```cobol
FUNCTION-ID. R6NGFN.
LINKAGE SECTION.
01 RESULT-NG GROUP-USAGE NATIONAL.
   02 RN PIC N(4).
PROCEDURE DIVISION RETURNING RESULT-NG.
...
01 CTL-NG GROUP-USAGE NATIONAL.          *> the control: same description, in working storage
   02 CN PIC N(4).
    DISPLAY "LEN-CTL=" FUNCTION LENGTH(CTL-NG)
    DISPLAY "LEN-FN=" FUNCTION LENGTH(FUNCTION R6NGFN)
```

```
LEN-CTL=4
LEN-FN=8
rc=0
```

Same description, two answers. The control is right (§15.50.4 rule 2 counts national POSITIONS); the
temporary is counting alphanumeric ones.

## The rule

```
$ python scripts/spec/cite.py --check 8.4.3.2.4 "the description, class, and category of the temporary data item"
  OK  §8.4.3.2.4 1)  (General rules)
      If function-prototype-name-1 is specified, the description, class, and category of the temporary data
      item is that specified by the description in the linkage section of the item specified in the RETURNING
      phrase of the ...
```

"the description ... is that specified by the description" — the whole entry, not a chosen subset of clauses.

## The code site

* `src/Cobol.Net.Compiler/Binding/DataBinder.Oo.cs#CreateCompilerTemp` (~L571)
* `src/Cobol.Net.Compiler/Binding/DataBinder.Oo.cs#CloneTempNode` (~L601)

Both copy `Pic`, `OwnSign`, `OwnUsage`, `Justified`, `BlankWhenZero` and nothing else. `GroupUsage` is not in
the list, so it does not survive; nor would any clause added tomorrow.

## The remedy — a classified copy, not a sixth field

`CopyEntryDescription` is the existing home for "copy a description entry". The fix is to fold both temps
into it. The report flagged §8.4.3.2.4 GR1 / §11.7.4 as having "their own exclusion story and a pre-gating
residue check" — which clauses a temporary must NOT inherit is a real question and it is the ONE place it
should be answered. **A hand list where a classified copy belongs is the defect; a longer hand list is not
the fix.**

## The sibling sweep

Grep for every site that constructs a `DataItem` by naming clauses one at a time: `CreateCompilerTemp`,
`CloneTempNode`, and whatever `SendingValueTemp` builds (PB886 measures the same shape for a MOVE
intermediate's carrier). A drift test asserting that no code outside `CopyEntryDescription` assigns more than
one description clause onto a synthesised item makes the next clause automatic.

## Rows

`GR-8.4.3.2.4-1` is a GAP row with no live claimer; claimed here.

## Landed (wave 47 group P)

Re-probed first on the worktree's own build: `LEN-CTL=4 / LEN-FN=8`, as filed. **The fix is the one the note
named, not a longer list.** `CreateCompilerTemp` and `CloneTempNode` now carry only identity and the
MemberOnly fields (level, name, OCCURS) and route every clause through `DataBinder.CopyEntryDescription`, whose
`bool copyAlignment` became a `DescriptionCopyScope` (`TypeSubject` · `Entry` · `CompilerTemp`) — the one axis
on which the rules make the copies differ. The temp's exclusions are answered in ONE place, as the note asked:
a new `DescriptionCopyKind.EntryOnly` class (VALUE in both spellings, ANY LENGTH) travels with every entry copy
and not onto a temporary — a temporary is stored by its pre-op before any read, a Format-2 VALUE's FROM
subscripts would address an OCCURS a temp's root never reproduces, and ANY LENGTH is §13.18.2.3 SR2's
linkage-section parameter shape.

`python scripts/spec/cite.py --check 8.4.3.2.4 "the description, class, and category of the temporary data item"`
→ `OK §8.4.3.2.4 1)`.

`python scripts/spec/cite.py --check 13.18.2.3 "elementary level 1 entry in the linkage section"`
→ `OK §13.18.2.3 2)`.

**Sibling measured and fixed by the same change**: the §14.9.25.4 GR1 intermediate (`SendingValueTemp`) is the
same constructor, and `MOVE NG(I) TO NA NB` over a `GROUP-USAGE NATIONAL` table element gave `" X   "` in both
receivers where the one-receiver MOVE gives `"XYZ   "`; it now gives `"XYZ   "`. The property / inline-invocation
temps are the same constructor too. SYNCHRONIZED / ALIGNED (a group temp's slack bytes) now travel with it.

**Drift tests** (`DescriptionCopyCompletenessDriftTests`): the audit runs per scope; a Theory asserts none of
`CloneItem` / `CreateCompilerTemp` / `CloneTempNode` spells a clause; and a repo-wide scan fails on any site
outside the ONE copy that hand-copies two or more clause fields from one source (`X = src.X`), with its failure
branch proven on the exact pre-PB888 initializer. Goldens:
`tests/conformance/2002/pb888_compiler_temp_takes_the_whole_description` (+ `.out`) and
`tests/conformance/negative/pb888-national-group-result-below-2002`.

**Row** (as the implementer left it; see the train-48 note below): `GR-8.4.3.2.4-1` recorded PARTIAL — the function-pointer-name-1 paragraph of the same GR has no arm
(kb/Work PB847), so the row passes to PB847 rather than closing here.


**Train 48 lander (2026-09-22):** PB847 had already LANDED on main (wave 46 group H) when this cluster was
merged, so moving the row to PB847's `inventory_rows` would leave a PARTIAL row claimed by a landed note
(DefectiveRowCoverageDriftTests). The `pb888.json` batch was NOT applied; `GR-8.4.3.2.4-1` stays GAP and
needs one whole-rule re-adjudication now that both paragraphs have an arm.
