---
title: "PB1292 — the SOURCE clause's identifier-1 arm is narrower than the expression arm: a subscripted or reference-modified identifier is refused COBOLNET0899 (ReportSourceSubscripted) so a VARYING counter cannot subscript a source item, and LookupQualified cannot see the current report's sum counters (§13.18.53.3 SR4) — `SOURCE SN` draws 0899 'does not resolve', and `SC OF CFG` (group-qualified sum counter, §13.15.3 SR7) draws COBOLNET2145"
id: PB1292
kind: defect
status: open
severity: MINOR
area: binding/report-writer
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1271", "PB1293", "PB1294", "PB1306"]
spec_refs: ["13.18.53.4", "13.18.64.4", "13.18.53.2", "13.18.53.3", "13.18.54.4"]
inventory_rows: ["GR-13.18.53.4-1", "FMT-13.18.53.2", "SR-13.18.53.3-4", "GR-13.18.64.4-4", "GR-13.18.54.4-5"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14e** from adjudication batch d1-14e (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probe sources under the batch's `out13/probe/<subject>/`). Checked against main 071ff04ea: trains 58/59 did not touch this mechanism.

# PB1292 — OPEN

## The rules (every quotation run through `cite.py --check` by the registrar)

- `python scripts/spec/cite.py --check 13.18.53.4 "If identifier-1 is specified without the ROUNDED phrase, it specifies the sending operand of an implicit MOVE statement in which the data item referenced by identifier-1"` → `OK  §13.18.53.4 1)  (General rules)`
- `python scripts/spec/cite.py --check 13.18.64.4 "Each value of data-name-1, established in this way, persists throughout the processing of the associated occurrence of the data item or report item"` → `OK  §13.18.64.4 4)  (General rules)`
- `python scripts/spec/cite.py --check 13.18.53.3 "If identifier-1 specifies a report section item, it shall be a report counter identifier or a sum counter defined in the current report"` → `OK  §13.18.53.3 4)  (Syntax rules)`
- ⚠ NOT validated verbatim (the adjudicator's paraphrase or a cross-clause quote — re-quote before it reaches code, a golden or the DEVLOG): "this allows data-name-1 to be used ... as a subscript to a source data item"

## Measured

### Finding source-clause — ISO §13.18.53.2; §13.18.53.4 GR1 (rejects-legal-source; rows GR-13.18.53.4-1, FMT-13.18.53.2)

**Mechanism.** The SOURCE clause's identifier-1 arm (BindSourceReference) refuses a subscripted or reference-modified identifier as COBOLNET0899 'not yet implemented', although the same identifiers are accepted inside an arithmetic-expression operand

**Repro.** probe out13/probe/source-clause/n1.cob: WORKING-STORAGE `01 WS-T PIC X(2) OCCURS 3 VALUE "TT".`; RD entry `05 COLUMN 12 PIC X(2) SOURCE WS-T(2).` (and n1b.cob: `SOURCE WS-X(2:2).` over `01 WS-X PIC X(4) VALUE "WXYZ"`), --std 2023.

**Expected, from the standard.** §13.18.53.2 admits identifier-1 (any identifier, §8.4.3 - subscripted and reference-modified included) and §13.18.53.4 GR1 moves the referenced item to the printable item: n1 prints `TT` at column 12, n1b prints `XY`.

**Observed.** error COBOLNET0899: RD 'R2': SOURCE WS-T(2) - a subscripted or reference-modified SOURCE operand (ISO §13.18.53) is not yet implemented (likewise WS-X(2:2)). BindSourceReference returns null on any subscriptPart/refModPart; FieldDataSource stores only base name + qualifiers (KeyReference). The SUM clause's identical gap was closed by PB482 (the addend became a bound expression); the SOURCE identifier arm should bind through the same one expression/place binder the FieldComputeSource arm already uses, with the implicit MOVE (not COMPUTE) semantics kept. No kb/Work note owns this staging.

**Code site.** src/Cobol.Net.Compiler/Binding/DataBinder.Reports.cs#BindSourceReference; #FieldDataSource

### Finding source-clause — ISO §13.18.53.3 SR4 (rejects-legal-source; rows SR-13.18.53.3-4, FMT-13.18.53.2)

**Mechanism.** Two-arm dispatch on SR4: the identifier-1 arm resolves through LookupQualified, which cannot see sum counters, while the arithmetic-expression arm admits the current report's sum counters via ReportSectionNameIn

**Repro.** probe out13/probe/source-clause/p4.cob: RD R2 CONTROL IS FINAL. DE entry `05 RUNTOT COLUMN 18 PIC 9(4) SUM WS-AMT.`; CF FINAL entry `05 COLUMN 13 PIC 9(4) SOURCE RUNTOT.` Compare p7.cob where the same entry is `SOURCE (RUNTOT + 0).`

**Expected, from the standard.** §13.18.53.3 SR4: 'If identifier-1 specifies a report section item, it shall be a report counter identifier or a sum counter defined in the current report' - RUNTOT is such a sum counter, so p4 compiles, and the CF FINAL line prints the sum counter's current content exactly as p7 does (0000 here, since RUNTOT resets at the end of each DE, §13.18.54.4 GR2).

**Observed.** p4: error COBOLNET0899: RD 'R2': SOURCE 'RUNTOT' does not resolve to a data item (ISO §13.18.53.3 SR4). p7 (expression arm) compiles and prints 0000. The identifier arm (ResolveReports, FieldDataSource case -> LookupQualified) and the expression arm (FieldComputeSource case -> ReportSectionNameIn(countersOf: current report)) implement SR4 twice with different populations; the identifier arm should consult the same sum-counter table (model.Sums by name, current report only) and bind a FieldSumSource-style reader.

**Code site.** src/Cobol.Net.Compiler/Binding/DataBinder.Reports.cs#ResolveReports (FieldDataSource case); #ReportSectionNameIn

### Finding varying-clause — ISO §13.18.64.4 GR4 NOTE; §13.18.53 (rejects-legal-source; rows GR-13.18.64.4-4)

**Mechanism.** F4 - a subscripted SOURCE operand is refused wholesale (ReportSourceSubscripted, COBOLNET0899), so the VARYING counter cannot serve as a subscript to a source data item, the use §13.18.64.4 GR4's NOTE names first after the bare source. This is a §13.18.53 SOURCE-clause-wide stage with no owning kb/Work note found.

**Repro.** 01 WS-S VALUE "ABCDEFGHI". 05 WS-C PIC X OCCURS 9.  ... 03 COLUMN 1 PIC X OCCURS 3 TIMES STEP 2 VARYING K FROM 1 BY 2 SOURCE WS-C(K). (probe vsub)

**Expected, from the standard.** GR4 NOTE 'this allows data-name-1 to be used ... as a subscript to a source data item'; K = 1, 3, 5 -> `A C E`.

**Observed.** error COBOLNET0899: RD 'R-V': SOURCE WS-C(K) - a subscripted or reference-modified SOURCE operand (ISO §13.18.53) is not yet implemented

**Code site.** src/Cobol.Net.Compiler/Binding/DataBinder.Reports.cs line ~2358 (ReportSourceSubscripted); DiagnosticCatalog.ReportSourceSubscripted line ~970

### Row GR-13.18.54.4-5 — PARTIAL (no separate finding; the adjudicator's / refuter's record)

Code location: `src/Cobol.Net.Compiler/Binding/ReferenceResolver.cs#SumCounterFor; src/Cobol.Net.Compiler/Binding/Model/Place.cs#ReportSumCounterPlace; src/Cobol.Net.Compiler/Binding/DataBinder.cs#SumCounters`

GR5 'the data-name is the name of the sum counter, not the name of the associated printable item, if any' (cite.py --check OK). The data-name resolves to ReportSumCounterPlace (the counter), read at the counter's scale; golden reads CF-U after TERMINATE as the counter value 00 and the altered counter prints 0511. Ambiguous shared names diagnosed (COBOLNET2145, conformance:negative/pb882-sum-counter-ambiguous). kb/Work PB840/PB882 landed. || REFUTED: PARTIAL — The procedure division works: an unqualified name, or one qualified by report-name, reaches the counter (MOVE, ADD, SUBTRACT, COMPUTE, read after TERMINATE; SN=-7, R5 -018 vs R6 103). Two legal references to the sum counter's data-name fail. (a) Qualifying the counter by the name of its containing group, as in MOVE SC OF CFG TO WS-X, is refused with COBOLNET2145 'may be qualified only by the report-name'. But §13.15.3 SR7 explicitly lists a report group data-name 'as a qualifier for a SUM counter', and §8.4.2.2.2 Format 1 allows data-qualifiers before the report qualifier. (b) SOURCE SN inside the report (in the same group or in a DE group) draws COBOLNET0899 'does not resolve to a data item', whether the counter is printable or not. The FieldDataSource arm uses only LookupQualified over ordinary storage, yet §13.18.53.3 SR4 admits 'a sum counter defined in the current report'. No kb/Work note owns either case (grep of kb/Work for 0899 plus sum counter SOURCE finds none).

## What the fixer owes

Re-probe on your own build first (MANDATORY-PRACTICES I3). Fix the mechanism at the code sites above — one rule, one place (the `cluster:` notes share these files; take them as one fill unit). One positive golden at the introducing edition and one negative below it / per violated rule, each derived from the quoted rule. Sweep the sibling arms named in the findings (CLAUDE.md rule 4). On landing flip `status`, write `closes_rows` from `inventory_rows`, and re-verdict each row with `record_verdicts.py`.
