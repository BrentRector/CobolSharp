---
title: "PB538 — sync-left-right-phrase-discarded"
id: PB538
kind: analysis
status: open
severity: MINOR
area: frontend/grammar
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.18.55.4"]
inventory_rows: ["GR-13.18.55.4-4", "GR-13.18.55.4-5"]
tags: [cobolsharp, work, analysis]
---

# PB538

## Mechanism — synchronized-clause
sync-left-right-phrase-discarded - the SYNCHRONIZED clause's LEFT/RIGHT selection is parsed and thrown away, and the determination that makes throwing it away conforming is never written down. `syncClause : (SYNCHRONIZED | SYNC) (LEFT | RIGHT)?` parses all three shapes, but the binder's whole treatment is `else if (clause.Context.syncClause() is not null) synchronized = true;` - `syncClause()` has exactly ONE call site in the compiler, the LEFT/RIGHT token is never read, and DataItem carries a single `bool Synchronized`, so the model cannot represent which of the three the program wrote. That is behaviourally correct ONLY under a one-byte natural boundary, and nothing states one: docs/CONFORMANCE.md DOC-A.1-195 says 'no physical alignment' and 'no implicit FILLER' - statements about PADDING - and is written against GR9/GR8/GR10 and 8.5.1.6.3, never against GR3/GR4/GR5, while the REQUIRED Annex A.1 item 197 determination ('positioning when neither RIGHT nor LEFT is specified') has no DOC-A.1-197 row in CONFORMANCE.md at all (47 DOC- rows; 195 present, 196 and 197 absent, though both are in docs/rearchitecture/spec-rule-catalog.json). Two rules therefore pass on an unstated premise, and the one bit a future alignment implementation would need has already been discarded at the clause site. The fix has two halves and both are cheap: record the selection (a `SyncAlignment { None, Left, Right }` on DataItem, set from the syncClause token, the way OwnSign records the SIGN selection) so the distinction survives; and add the DOC-A.1-197 row stating the natural boundary COBOL.NET recognises for SYNCHRONIZED - one byte - which is what makes GR2, GR3, GR4 and GR5 simultaneously true.

### Expected, from the standard
13.18.55.4 GR4 - 'SYNCHRONIZED LEFT specifies that the elementary item is to be positioned such that it will begin at the left byte of the natural boundary in which the elementary item is placed.' GR5 - 'SYNCHRONIZED RIGHT specifies that the elementary item is to be positioned such that it will terminate on the right byte of the natural boundary in which the elementary item is placed.' GR3 leaves the bare form's positioning to the implementor; 8.5.1.6.4 makes natural boundaries a machine property and says 'The way in which data is stored is determined by the runtime module, and need not respect these natural boundaries' and 'Each implementor who provides for special types of alignment shall specify the precise interpretations that are to be made'; Annex A.1 item 197 - 'SYNCHRONIZED clause (positioning when neither RIGHT nor LEFT is specified). This item is required. This item shall be documented in the implementor's user documentation.' So all three forms may collapse to one behaviour, but ONLY if the implementor's natural boundary is one byte, and THAT determination is the thing the standard requires to be written down.

```
$ python scripts/spec/cite.py --check 13.18.55.4 "SYNCHRONIZED RIGHT specifies that the elementary item is to be positioned such that it will terminate on the right byte of the natural bound..."
  OK  §13.18.55.4 5)  (General rules)
```

### Observed
One program, three shapes, one answer. Probe E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/sy_layout compiles `01 G1. 05 A1 PIC X. 05 B1 PIC S9(4) COMP SYNCHRONIZED. 05 C1 PIC X.` beside the SYNC LEFT, SYNC RIGHT and no-clause variants of the identical shape and prints SYNC=0004, LEFT=0004, RGHT=0004, PLAIN=0004, with the synchronized item itself ELEM=0002 and its unsynchronized twin ELEMP=0002. Reading confirms why: `grep -rn "syncClause()" src/Cobol.Net.Compiler/` returns exactly one line (DataBinder.cs:2308) and there is no SyncLeft/SyncRight/SyncKind/SyncDirection member anywhere in src/. The shapes parse at every edition (sy_elem_left_85/2002/2014/2023, sy_elem_right_85/2002/2014/2023, sy_group_left_2023) and the figure's complement is enforced (`SYNC LEFT RIGHT` -> COBOL0307, sy_fmt_both_lr). No wrong answer is reachable today, which is why the harm is silent rather than wrong-answer - but the conformance of GR4 and GR5 currently rests on a premise no document states, and the LEFT/RIGHT bit is unrecoverable once discarded.

### Repro
cd E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe ; "E:/CobolSharp/.claude/worktrees/lane3-pin-89044aa0/src/Cobol.Net.Cli/bin/Debug/net10.0/cobol.exe" sy_layout/sy_layout.cob --run  -> SYNC=0004 / LEFT=0004 / RGHT=0004 / PLAIN=0004 / ELEM=0002 / ELEMP=0002. Then: grep -rn 'syncClause()' E:/CobolSharp/.claude/worktrees/lane3-pin-89044aa0/src/Cobol.Net.Compiler/  -> one hit, DataBinder.cs:2308. Then: grep -n 'DOC-A.1-19' E:/CobolSharp/.claude/worktrees/lane3-pin-89044aa0/docs/CONFORMANCE.md  -> only DOC-A.1-195.

### Code site
src/Cobol.Net.Frontend/Grammar/Core/CobolData.g4:656-658 (`syncClause : (SYNCHRONIZED | SYNC) (LEFT | RIGHT)?`) with src/Cobol.Net.Compiler/Binding/DataBinder.cs:2308-2309 (the sole `syncClause()` call site; sets a bare bool) and src/Cobol.Net.Compiler/Binding/Model/DataItem.cs:249-253 (`public bool Synchronized`). Documentation half: docs/CONFORMANCE.md DOC-A.1-195 (line ~1001) and the absent DOC-A.1-196 / DOC-A.1-197 rows.

## Sibling sweep the fixer owes
Every bug here is a pattern (CLAUDE.md rule 4). Before closing this note, sweep the sibling arms named in the mechanism above and add the ones that fail to `inventory_rows`.

## Provenance
Lane-3 adjudication batch b3 (subjects: synchronized-clause), adjudicated against the compiler pinned at 89044aa0 and RE-PROBED on the registrar's worktree cut from 6e7caa91 (a 1942-program sweep of `out-b3/probe/` plus targeted re-probes); the finding reproduces on today's tree.

## Repro programs
`E:\Temp\claude\E--CobolSharp\a9d94900-79fe-4a38-89f0-921976861543\scratchpad\repros\PB538\` — sy_layout. The implementer's opening move is to rerun them against a freshly built `cobol.exe`.

