---
title: "PB1356 — the restrictions on text a COPY/REPLACE may produce are unchecked beyond GR10: a nested COPY WITH a REPLACING phrase inside library text (§7.2.3.4 GR12 permits only one without) is processed, a replacement that introduces a COPY statement / SOURCE FORMAT directive / comment / blank line (COPY GR13, REPLACE GR9) is accepted — and a REPLACE-inserted COPY makes the REST of the compilation group vanish with no diagnostic"
id: PB1356
kind: defect
status: open
severity: MINOR
area: frontend/text-manipulation
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1354", "PB1355", "PB1384", "PB1357"]
spec_refs: ["7.2.3.4", "7.2.4.4"]
inventory_rows: ["GR-7.2.3.4-12", "GR-7.2.3.4-13", "GR-7.2.4.4-9"]
closes_rows: []
closes_rows_reason: "open — nothing landed yet"
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 15a** from adjudication batch 15a (subjects in §7.2 / §7.3, adjudicator pin `E:\CobolSharp-pins\adj-d2` @ `071ff04ea`; nothing under `src/` landed between the pin and `8bd4c7c9f`, so every measurement below stands). Probe programs: `E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d2\out\probe\<subject>\`.

# PB1356 — OPEN

## The rule (every citation re-run by the registrar)

> If the REPLACING phrase is not specified, the library text may contain a COPY statement that does not include a REPLACING phrase. …

`python scripts/spec/cite.py --check 7.2.3.4 "If the REPLACING phrase is not specified, the library text may contain a COPY statement that does not include a REPLACING phrase."` → `OK  §7.2.3.4 12)  (General rules)`

> The replacing action of a COPY statement shall not introduce a COPY statement, a SOURCE FORMAT directive, a comment, or a blank line. …

`python scripts/spec/cite.py --check 7.2.3.4 "The replacing action of a COPY statement shall not introduce a COPY statement, a SOURCE FORMAT directive, a comment, or a blank line."` → `OK  §7.2.3.4 13)  (General rules)`

> shall not contain a COPY statement, a REPLACE statement …

`python scripts/spec/cite.py --check 7.2.4.4 "shall not contain a COPY statement, a REPLACE statement"` → `OK  §7.2.4.4 9)  (General rules)`

## Finding — copy-statement-p2 (under-rejects)

The restrictions on text a COPY may bring in are unchecked beyond GR10: a nested COPY WITH a REPLACING phrase inside library text (GR12 permits only one without) and a replacement that introduces a COPY statement, SOURCE FORMAT directive, comment or blank line (GR13) are accepted and processed

**Rows:** GR-7.2.3.4-12, GR-7.2.3.4-13

**Repro:**

```
p6: p6out.cpy = `COPY p6in REPLACING ==IN-A== BY ==IN-B==.`; main `COPY p6out.` DISPLAY IN-B. p7: p7book.cpy has a line `ZZTOKEN`; `COPY p7book REPLACING ==ZZTOKEN== BY ==COPY p6in.==.` DISPLAY IN-A. p7b: pseudo-text-2 is a `*>` comment. (E:/Temp/claude/E--CobolSharp/a700c052-f9e4-4942-84ff-a13c7505d6ec/scratchpad/adj-d2/out/probe/copy-p2/p6.cob, p7.cob, p7b.cob)
```

**Expected from the spec:** Each is diagnosed: GR12 'the library text may contain a COPY statement that does not include a REPLACING phrase'; GR13 'The replacing action of a COPY statement shall not introduce a COPY statement, a SOURCE FORMAT directive, a comment, or a blank line.'

**Observed:** p6 prints IN, p7 prints IN (the introduced COPY was expanded), p7b prints XA — all with no diagnostic. ResolveOneCopy checks only the outer-REPLACING-over-nested-COPY case (COBOLNET1640); ExpandCopiesOneLevel recurses into the replaced text unconditionally.

**Code site:** src/Cobol.Net.Frontend/Preprocessor/CopyProcessor.cs#ResolveOneCopy (line 440 GR10 check only); src/Cobol.Net.Frontend/Preprocessor/CopyProcessor.cs#ExpandCopiesOneLevel (line 492 recursion into replaced text)

## Finding — replace-statement-p2 (silent)

replace-resultant-text-unscreened: nothing checks the text a REPLACE produces for a COPY statement / REPLACE statement / SOURCE FORMAT directive, and an inserted COPY makes the rest of the compilation group vanish with no diagnostic

**Rows:** GR-7.2.4.4-9

**Repro:**

```
WORKING-STORAGE SECTION. REPLACE ==YY== BY ==COPY CPB==. YY. 01 W2 PIC X(3) VALUE 'W2 '. PROCEDURE DIVISION. DISPLAY W2 STOP RUN.
```

**Expected from the spec:** GR9: the produced text 'shall not contain a COPY statement, a REPLACE statement, a SOURCE FORMAT directive' - the source is nonconforming and must be diagnosed; at minimum the rest of the program must not disappear.

**Observed:** out/probe/replace-p2/gr9e.cob and gr9b.cob compile rc=0 with no diagnostic; generated __Activate is empty (DISPLAY W2 gone). Control ==FOO CPB== draws COBOL0001. ==REPLACE OFF== as pseudo-text-2 surfaces only as an unrelated COBOL0307 'unexpected OFF'.

**Code site:** src/Cobol.Net.Frontend/Preprocessor/CopyProcessor.cs#ApplyReplacements (insertion at 217) - no resultant-text screen; the silent swallow of the stray COPY word is downstream (lexer/parser recovery, not located)

## What the fixer owes (and the sibling sweep)

Screen the resultant text of each replacing action (COPY and REPLACE share `ApplyReplacements` — screen once there) and the nested-COPY header (PB1354's parser knows whether REPLACING is present). Find and remove the downstream silent swallow of the stray COPY word (lexer/parser recovery — not located by the adjudicator). Negative goldens per rule.

**Rows claimed:** GR-7.2.3.4-12, GR-7.2.3.4-13, GR-7.2.4.4-9
