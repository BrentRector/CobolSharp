---
title: "PB151 — ✅ LANDED 2026-08-29 (AllocateReal ceilings + UpByReal GR19; the GR5 notAvail leg + full-Int128 size; OPTIONS INITIALIZE first consumer; COBOLNET1695 at bind; cell model → PB164). WAS: ALLOCATE: a float CHARACTERS operand truncates where GR1 rounds UP (the Scale==0 split reads a Real-carried double as integral; EmitSetPointerUpDown repeats the split so a float SET UP BY bypasses the GR19 integrality raise); the not-available leg does not exist (checked-cast OverflowException instead of P=NULL + EC-STORAGE-NOT-AVAIL; a >2^64 size WRAPS to a small live allocation — the PB22 cast family's unswept sibling); a BASED record with any non-DISPLAY leaf compiles clean and crashes at run time while the EXTERNAL twin gets a bind-time diagnostic"
id: PB151
kind: defect
status: landed
severity: MAJOR
area: codegen
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB151

Rows: GR-14.9.3.4-1 (DIVERGES), -2/-3/-4/-5/-7/-9, findings [ALLOCATE statement].

(1) PtrEmitter.EmitAllocate splits on x.Scale == 0; a NATIVE float operand carries Scale 0 with Real:true so `(long)(double)` truncates 2.5 → 2 where GR1 rounds up (the fixed-point arm is right; STANDARD-DECIMAL is right — the refuter proved the boundary). Same split in EmitSetPointerUpDown. (2) CobolPtr.Allocate has no failure path; `checked((int)size)` throws OverflowException unhandled where GR5 requires NULL + nonfatal EC-STORAGE-NOT-AVAIL (the shape exists next door in Free's notAlloc + EmitFree's gated dispatch); the `(long)` narrowing before it wraps modulo 2^64 (a 20-digit size allocates five characters and returns a VALID pointer) — kb/Work PB22's own root cause, never swept to PtrEmitter. (3) ForceStringCanonical rejects any BASED class with a non-DISPLAY leaf; PtrBindBasedAndAddressables bare-continues with NO diagnostic and EmitAllocate louds at RUN time — while CallMakeExternal turns the IDENTICAL failure into a bind-time COBOLNET (two-arm). GR4a/b labels are swapped at three sites (→ PB159).

## ⤴ Leg (3)'s POSTURE SUPERSEDED 2026-08-30 by the PB164 Step D second-gate widening

PB151 made a BASED record with a non-DISPLAY leaf a bind-time **COBOLNET1695** — the honest loud, given the
cell model did not carry such leaves. [[PB164]]'s Step D widening (DESIGN-data-model §2.3 Step D item 4) gives
`ForceStringCanonical` THE ONE image predicate, so that shape is now ordinary cell-backed storage and the
diagnostic is GONE. It had to go: §13.18.5.3 SR1/SR2 restrict a BASED subject only to "not class object" and
"not a dynamic-length elementary item or a variable-length group", and §14.9.3.3 SR1 asks only for the BASED
clause — a COMP leaf is legal COBOL and 1695 was rejecting it. The negative fixture `pb151-based-comp-leaf` is
DELETED and replaced by the positive golden `2023/stepd_based_comp_leaf`, which pins the cell's bytes over the
pointer-deref bridge with FUNCTION ORD (§15.70.1) and proves the codec is an involution in both directions.
Inventory row GR-14.9.3.4-3 re-points at that golden. COBOLNET1695 itself still stands for the residue the cell
genuinely cannot carry (NATIONAL / USAGE BIT / pointer-class leaves). **Status unchanged — PB151's own fixes
(the AllocateReal ceilings, the GR5 leg, the OPTIONS INITIALIZE consumer) are untouched; only leg (3)'s
posture moved, exactly as leg (3) itself predicted ("cell model → PB164").**

*(Found by Phase-B batch 8 — §8.8.1 + §14.9.1–14.9.12, 2026-08-29; one adjudicator + one independent refuter per subject. Full forensic evidence: the named rows' `notes` in `tests/version-matrix/traceability-inventory.json`; the raw finding lines in the session scratchpad `pb-batch8/findings.txt`.)*
