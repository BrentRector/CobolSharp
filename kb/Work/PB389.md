---
title: "PB389 — the object-reference descriptor is a SCALAR (one nullable class name) where §13.18.60.2 prints a TUPLE (kind × FACTORY × ONLY × name): ACTIVE-CLASS and ONLY are rejected outright and 15+ SET/EXIT/GOBACK rules have no expressible subject"
id: PB389
kind: defect
status: landed
severity: MAJOR
area: frontend/grammar-oo
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: true
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.18.60.2", "13.18.60.3", "13.18.60.4", "14.2.1", "14.9.14.3", "14.9.18.3", "14.9.39.3", "14.9.39.4", "D.9.2.2"]
inventory_rows: ["GR-14.9.39.4-9", "SR-14.9.39.3-14", "SR-14.9.39.3-L2.1", "SR-14.9.39.3-L2.2", "SR-14.9.39.3-L3.1", "SR-14.9.39.3-L3.2", "SR-14.9.39.3-L5.1", "SR-14.9.39.3-L5.3", "SR-14.9.39.3-L6.1", "SR-14.9.39.3-L6.2", "SR-14.9.39.3-L6.3", "SR-14.9.39.3-L7.1"]
tags: [cobolsharp, work, defect]
---

# PB389 — a scalar where the standard prints a tuple, and fifteen rules with nowhere to live

## THE MECHANISM

`§13.18.60.2` prints the OBJECT REFERENCE operand as a choice of three shapes over four independent axes —
**KIND** ∈ {universal, interface-name-1, object-class-name-1, ACTIVE-CLASS} × **FACTORY** present/absent ×
**ONLY** present/absent × the name itself. The compiler models ONE axis. `PicInfo.ObjectClassName`
(`src/Cobol.Net.Compiler/Binding/Model/PicInfo.cs:345`) is a single nullable string: null means universal,
non-null means "some name", and the KIND is *re-derived downstream* by asking the class table twice
(`OoConformance.cs:378` `FindInterface(recvName)`, then `:405` `Find(sendName)`). The grammar matches that
scalar exactly — `src/Cobol.Net.Frontend/Grammar/Core/CobolOO.g4:184-188` is

```antlr
objectReferenceUsage
    : OBJECT REFERENCE FACTORY OF className   // binder-staged COBOLNET0899
    | OBJECT REFERENCE className
    | OBJECT REFERENCE
    ;
```

— no ACTIVE-CLASS alternative, no trailing ONLY. **This is the `model_the_rule_shape_not_one_case` shape**: a
scalar column where the rules name a tuple. It costs three distinct things: (1) ACTIVE-CLASS and ONLY are
**legal COBOL-2002+ that the compiler rejects**; (2) every SET Format-5 syntax rule that DISCRIMINATES on an
unmodelled axis is unlocated — nothing in `src/` cites SR10c, SR12a1, SR12a3, SR12b, SR12c1, SR12c4 or SR14;
(3) the FACTORY-presence rules are not merely absent, they are *protected only by a rejection* — the day the
FACTORY-OF wave lands, `OoConformance.cs:381`'s hard-wired `ImplementsClosure(sc, factory: false)` silently
answers b2's question for b1, and no `PicInfo` field exists to compare FACTORY presence at all.

The same scalar reaches the EXIT/GOBACK RAISING lane through a second door: `raisingClause` is
`RAISING cobolWord+`, so §14.2.1's `[ FACTORY OF ]` in the procedure-division header is a raw parse error and an
interface-name draws COBOLNET0858 — the identifier end and the header end are BOTH closed, so neither alone can
be opened. On top sits a latent message defect: SR5d/SR4d is tested as `ObjectClassName is not { } declared`
("no class name recorded"), which is true of a factory, interface or ACTIVE-CLASS reference too — so the moment
those land, legal operands get rejected as *"a UNIVERSAL object reference"*, naming a rule the program did not
break.

## THE RULES, QUOTED, EACH `--check`ED ON THIS TREE

* `--check 13.18.60.2 "ACTIVE-CLASS"` → **`OK §13.18.60.2 (General format)`**, the printed line being
  `│ OBJECT REFERENCE │ [ FACTORY OF ] ACTIVE-CLASS │ │` — FACTORY, ACTIVE-CLASS and ONLY are underlined
  required words of their alternatives.
* `--check 13.18.60.3 "The ACTIVE-CLASS phrase may be specified only in a factory definition, an instance
  definition, or the linkage or local-storage section of a method definition"` → **`OK §13.18.60.3 16)`**.
* `--check 14.9.39.3 "If the data item referenced by identifier-3 is described with an ACTIVE-CLASS phrase"` →
  **`OK §14.9.39.3 14)`** (SR14, Format 5).
* `--check 14.9.39.3 "if described with a FACTORY phrase, the factory object of the specified class shall
  implement int-1"` → **`OK §14.9.39.3 10)`** (SR10 b)1.).
* `--check 14.9.39.3 "Identifier-12 shall reference a data item of category function-pointer"` →
  **`OK §14.9.39.3 20)`** (SR20, Format 8).
* `--check 14.9.39.3 "If identifier-7 references a restricted program-pointer, identifier-8 shall be the
  predefined address NULL"` → **`OK §14.9.39.3 22)`** (SR22, Format 9).
* `--check 14.9.39.3 "Identifier-9 shall not be a data-pointer restricted to a type described with the STRONG
  phrase"` → **`OK §14.9.39.3 24)`** (SR24), whose statement is Format 10 —
  `--check 14.9.39.2 "Format 10"` → **`OK §14.9.39.2 (General formats)`**, `Format 10 (data-pointer-arithmetic)`.
* `--check 14.9.14.3 "If the data description entry of identifier-1 specifies an interface-name"` →
  **`OK §14.9.14.3 5) b)`** — EXIT's own SR5b, *not* GOBACK's SR4b (both exist; the antecedent
  "this EXIT statement" is what separates them — see PB388).
* `--check 14.9.39.4 "If identifier-4 is specified, a reference to the object identified by identifier-4 is
  placed into each data item referenced by identifier-3 in the order specified"` → **`OK §14.9.39.4 9)`**.

Editions: the whole OO surface is a COBOL-2002 introduction, so 2002/2014/2023 are in scope and 1985 is
correctly gated out. Annex A.4.10 declares only multiple inheritance and parametric polymorphism optional, so
**none of this is covered by an owner non-support determination** — it is required conformance.

## OBSERVED ON HEAD (`89044aa0`), re-run — not inherited from the 5fe593a0 pin

```
$ cobol.exe p01_activeclass_usage.cob --std 2023 --run     # 01 A USAGE OBJECT REFERENCE ACTIVE-CLASS.
error COBOLNET0813: data item 'A': USAGE OBJECT REFERENCE names the unknown class or interface 'ACTIVE-CLASS' …
error COBOLNET0901: 'ACTIVE-CLASS' is a reserved word in COBOL-2023 and cannot be used as a user-defined word
rc=70        <-- a REQUIRED WORD OF THE FORMAT reported as a botched user name, twice over

$ cobol.exe p02_only_usage.cob --std 2023 --run            # 01 C USAGE OBJECT REFERENCE CON1 ONLY.
error COBOL0307: unexpected 'ONLY'. A period may be missing at the end of the previous sentence.   rc=65

$ cobol.exe p03_factoryof.cob --std 2023 --run             # 01 F USAGE OBJECT REFERENCE FACTORY OF CFAC1.
error COBOLNET0899: USAGE OBJECT REFERENCE FACTORY OF … recognized but not yet implemented   rc=70

$ cobol.exe p05_sr10d1_pos.cob --std 2023 --run            # positive control: the OO surface is live
FPONG    rc=0
$ cobol.exe sr5_positive_typed_ref.cob --std 2023 --run    # positive control for EXIT … RAISING
HANDLED / AFTER-CALL   rc=0

$ cobol.exe sr5_interface_ref.cob --std 2023               # interface-typed ref + PD header RAISING IERR8
error COBOLNET0858: PROCEDURE DIVISION RAISING IERR8: not an exception-name or a class of the compilation group
error COBOLNET0849: EXIT PROGRAM RAISING 'W-E': its declared class 'IERR8' … (ISO 14.9.18.3 SR4a)   rc=70
$ cobol.exe sr5_factory_of.cob --std 2023                  # PROCEDURE DIVISION RAISING FACTORY OF CFAC5.
error COBOL0307: unexpected 'OF'.   rc=65                  <-- the header end, dead before any rule applies
$ cobol.exe f5_pp_restricted.cob --std 2023   → COBOLNET0899 (PROGRAM-POINTER TO prototype)
$ cobol.exe e1_fp_decl.cob      --std 2023   → COBOLNET0899 (FUNCTION-POINTER)
```

Three legal-source rejections, one loud stage, one parse error, and two positive controls proving the
surrounding OO machinery is live — so these are **descriptor** rejections, not an inert OO surface.

⚠ **TWO OF THE ADJUDICATION'S CLAIMS DO NOT SURVIVE RE-MEASUREMENT ON HEAD** (they were true at the pin):

* **`USAGE POINTER TO type-name-1` is no longer a bare `COBOL0307`.** The restricted data-pointer LANDED:
  `CobolData.g4:522/528` carries `dataPointerUsage : POINTER [TO type-name]`, and the probe's
  `01 P1 USAGE POINTER TO T-A.` now draws the correct named refusal
  `COBOLNET0881 … the TYPEDEF clause shall be specified for the subject of such an entry (ISO 13.18.60.3 SR18)`
  — and **the probe's source was never legal**: `--check 13.18.60.3 "TYPEDEF clause shall be specified"` →
  `OK §13.18.60.3 18)`. The finding validated its RULE and not its PREMISE.
* **SR24's subject is now declarable, so its harm inverted from reject-legal to UNDER-REJECT.** Measured here:
  `01 REC-T IS TYPEDEF STRONG. / 01 PTR-T IS TYPEDEF USAGE POINTER TO REC-T. / 01 P1 TYPE PTR-T.` compiles and
  runs; the restriction is genuinely modelled (`SET P1 TO ADDRESS OF OTH-ITEM` is refused by name with
  `COBOLNET0869 … restricted to type 'REC-T' … (ISO 14.9.39.3 SR19)`); and then **`SET P1 UP BY 4` COMPILES AND
  RUNS, printing `NO-SR24-SCREEN`, rc=0** — Format-10 arithmetic on a STRONG-restricted data-pointer, which SR24
  forbids outright. See the row below.

## THE CODE SITES, AND THE SHAPE OF THE REPAIR

* `src/Cobol.Net.Frontend/Grammar/Core/CobolOO.g4:184-188` — `objectReferenceUsage`, three alternatives where
  §13.18.60.2 prints five shapes.
* `src/Cobol.Net.Compiler/Binding/Model/PicInfo.cs:345, :351, :403` — the one nullable string.
* `src/Cobol.Net.Compiler/Binding/DataBinder.cs:2349-2352` — `FACTORY OF` → COBOLNET0899, and the item is left
  UNTYPED afterwards, so every later statement mentioning it emits a second, misleading diagnostic.
* `src/Cobol.Net.Compiler/Oo/OoConformance.cs:369-412` (`ObjectRefWideningMismatch`, with `:381`'s hard-wired
  `factory: false`) and `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/OoBinder.cs:855-977`
  (`OoBindSetObjectRef`) — the consumers that re-derive the kind because the descriptor does not carry it.
* `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/EcBinder.cs#EcBindRaising` — the SR5/SR4 band, including the
  `ObjectClassName is not { } declared` test that stands in for "universal".
* `src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4#raisingClause` — `RAISING cobolWord+`, no `[FACTORY OF]`,
  no interface-name.

**Repair (CLAUDE.md rule 5 — the descriptor, not fifteen checks):**

1. Give the model an explicit `ObjectRefDescriptor(Kind, Name, Factory, Only)` where `Kind` ∈ {Universal,
   Interface, ObjectClass, ActiveClass}. Every consumer then ASKS the descriptor instead of re-deriving the kind
   from the class table, and "universal" stops being spelled "no class name recorded" — which retires the latent
   SR5d/SR4d misdiagnosis in the same change.
2. Teach `objectReferenceUsage` the two missing shapes (`[FACTORY OF] ACTIVE-CLASS`, trailing `ONLY`) and
   `raisingClause` the §14.2.1 header shapes (`[FACTORY OF] object-class-name-1`, `interface-name-1`).
3. The fifteen stranded rules then become table lookups over the descriptor — and the relations they need
   already exist: `OoConformance.ConformsTo` (SR12a2), `cur.ConformsTo(tcls)` at `OoBinder.cs:907` (SR12c2),
   `ImplementsClosure(cls, factory:)` already called BOTH ways at `OoConformance.cs:85-86`, and an ACTIVE-CLASS
   operand's containing class is the `host.OoCurrentClass` the SELF arm already reads.
4. **Pair it with a drift test** asserting every §13.18.60.2 descriptor shape round-trips (declare → bind →
   descriptor → diagnostic), so the next axis cannot be dropped silently.
5. Converge the four refusal STYLES on one named stage while the work is in flight: today the same gap is
   presented as COBOLNET0813+0901 (ACTIVE-CLASS), COBOL0307 (ONLY), COBOLNET0899 (FACTORY OF, PROGRAM-POINTER
   TO, FUNCTION-POINTER). Only the 0899s tell the user the truth.

## THE SIBLING SWEEP THE FIXER MUST DO

* Every consumer of `PicInfo.ObjectClassName` (`grep -n ObjectClassName src/`) — each one is a place where a
  kind is being guessed.
* Every `ImplementsClosure(` call site: `:381` is the one hard-wired arm; `:85-86` shows the correct two-arm
  pattern (`feedback_two_arm_dispatch`).
* The refusal-then-cascade shape: a rejected description leaves the item untyped, so `FACTORY OF` and
  `ACTIVE-CLASS` each produce a SECOND wrong diagnostic downstream. Poison the item on refusal so later sites
  stay quiet — this is a general binder rule, not an OO one; sweep the other `RecognizedNotImplemented` sites.
* The USAGE tail family as a whole (`POINTER TO`, `PROGRAM-POINTER TO`, `FUNCTION-POINTER TO`,
  `OBJECT REFERENCE …`): `POINTER TO` has now landed, which is the template for the other two.

## Rows

Seventeen rows, one mechanism. In one clause each:

* **`SR-14.9.39.3-14`** (NOT-IMPLEMENTED) — SR14's subject is an ACTIVE-CLASS-described identifier-3; the phrase
  has no grammar alternative, measured above.
* **`SR-14.9.39.3-L3.1` / `L3.2`** (SR10 c)1./c)2.), **`L6.1` / `L6.2` / `L6.3`** (SR12 b)1./b)2./b)3.) — all
  need an ACTIVE-CLASS sender; unwritable.
* **`SR-14.9.39.3-L5.1`** (SR12 a)1.) and **`L7.1`** (SR12 c)1.) — both turn on the ONLY flag; `ONLY` is a raw
  parse error, and `ObjectRefWideningMismatch` applies the a)2. SUBCLASS relation unconditionally, which is the
  WRONG rule the moment ONLY lands.
* **`SR-14.9.39.3-L5.3`** (SR12 a)3.) — the FACTORY-presence equality; unviolatable today only because the
  FACTORY description is refused, with no field to compare when it is not.
* **`SR-14.9.39.3-L2.1`** (SR10 b)1.) — the factory closure; `OoConformance.cs:381` can only ever ask b)2.'s
  question. **`L2.2`** (SR10 b)2. + the head of c)) is PARTIAL: b)2. CONFORMS and is measured both ways
  (`sr10b2_class_implements` → PONG; `sr10b2_class_not_implements` → COBOLNET0867; the subclass sender walks the
  closure), while c) has no implementation.
* **`GR-14.9.39.4-9`** (PARTIAL) — the reference-placement semantics are right (a reference not a copy, every
  identifier-3, source order), but SR12 enumerates four alternatives for identifier-4 and the compiler cannot
  EXPRESS two of them, so GR9 can never execute for those operands. Its test-ref `conformance:2002/oo_universal`
  has ONE receiver per statement, so the "each … in the order specified" half is pinned by no shipped test —
  adding a multi-receiver row to that golden closes it.
* **`SR-14.9.14.3-5`** (PARTIAL, EXIT) and **`SR-14.9.18.3-4`** (PARTIAL, GOBACK) — one shared `EcBindRaising`:
  the head, a) and d) are enforced (and pinned for GOBACK by `OoSpineTests.GobackRaising_BindViolations_0849`);
  b) (interface-name) and c) (ACTIVE-CLASS) are unreachable from BOTH ends, measured above. For EXIT
  specifically, c) may be vacuous anyway — SR7 confines EXIT PROGRAM to a program procedure division while
  §13.18.60.3 SR16 confines an ACTIVE-CLASS description to a factory/instance definition or a method's
  linkage/local-storage section; say so when the fix lands rather than writing a screen that can never fire.
* **`SR-14.9.39.3-20`** (NOT-IMPLEMENTED) — FUNCTION-POINTER is refused at declaration (COBOLNET0899, measured
  on HEAD), and Format 8 has no SET arm at all: `BindSetTo`'s category re-routes test Pointer, ProgramPointer and
  ObjectReference and stop, so even a declarable function-pointer would fall into the Format-1 arithmetic store.
* **`SR-14.9.39.3-22`** (NOT-IMPLEMENTED) — `PROGRAM-POINTER TO prototype` is loud-staged (measured on HEAD), so
  identifier-7 can never reference a restricted program-pointer. Note for the lander: SR22's signature test and
  SR20's identical sentence for function-prototypes are ONE rule over two carriers — write it once.
* **`SR-14.9.39.3-24`** (NOT-IMPLEMENTED — **and its harm has INVERTED since the pin**) — the restricted
  data-pointer is now declarable and its restriction is enforced for SR19, so SR24's subject EXISTS; the SET
  Format-10 path simply has no STRONG screen, and `SET P1 UP BY 4` over a pointer restricted to a STRONG type
  **compiles and runs silently** (measured above, `sr24_strong_ptr_upby.cob`). This row is now an
  under-rejection in the Format-10 arm, not a declaration gap — it is the one row here that is NOT blocked on the
  descriptor work and can be fixed today, next to the existing `StrongTypeModel.PointerRestriction` calls in
  `PtrBinder.cs:52-78`.

---

## Residue ownership transferred — 2026-09-09, registrar (second pass)

The five rows this note carried as RESIDUE — the ones that kept it open after its own mechanism was fixed — now
have their own owners, and are removed from `inventory_rows` above:

| row | taken over by | why it is a separate mechanism |
|---|---|---|
| `SR-14.9.14.3-5` | [[PB815]] | `raisingClause : RAISING cobolWord+` carries one axis where §14.2.1 prints three — the HEADER end, which this note did not touch |
| `SR-14.9.18.3-4` | [[PB815]] | same grammar rule, GOBACK's side of it |
| `SR-14.9.39.3-24` | [[PB816]] | SET Format 10 has no STRONG screen; the subject is declarable today and was never blocked on the descriptor |
| `SR-14.9.39.3-20` | [[PB817]] | `USAGE FUNCTION-POINTER` is staged loud — a USAGE carrier, not a descriptor shape |
| `SR-14.9.39.3-22` | [[PB817]] | `USAGE PROGRAM-POINTER TO prototype`, the same rule over the second carrier |

The sixth residue — the six §13.18.60 rows whose recorded COBOLNET0899 evidence this note's landing falsifies —
is an **append on [[PB496]]**, which owns those rows; `DiagnosticCatalog.OoFactoryObjectReference` stays in place
until that re-adjudication, for the reason recorded there.

⛔ **`status` is deliberately still `open`, and this is a measured decision, not an oversight.** The brief that
produced this transfer asked for `status: landed` in the same change set. On `main` at `d3ba5795` this note's fix
is **not yet merged** (it lands with train 25), and all twelve remaining rows above are still defective:

```
GR-14.9.39.4-9      PARTIAL          GAP        SR-14.9.39.3-L5.1   NOT-IMPLEMENTED  GAP
SR-14.9.39.3-14     NOT-IMPLEMENTED  GAP        SR-14.9.39.3-L5.3   NOT-IMPLEMENTED  GAP
SR-14.9.39.3-L2.1   NOT-IMPLEMENTED  GAP        SR-14.9.39.3-L6.1   NOT-IMPLEMENTED  GAP
SR-14.9.39.3-L2.2   PARTIAL          GAP        SR-14.9.39.3-L6.2   NOT-IMPLEMENTED  GAP
SR-14.9.39.3-L3.1   NOT-IMPLEMENTED  GAP        SR-14.9.39.3-L6.3   NOT-IMPLEMENTED  GAP
SR-14.9.39.3-L3.2   NOT-IMPLEMENTED  GAP        SR-14.9.39.3-L7.1   NOT-IMPLEMENTED  GAP
```

Flipping `status` now would leave all twelve with no live note claiming them and turn
`DefectiveRowCoverageDriftTests` RED on `main`. CLAUDE.md rule 8 puts the flip in *"the commit that lands it"* —
i.e. train 25's landing commit, where the verdict batch that closes these rows also lands. **The transfer above
is the part that had to happen first**, and it is what lets that commit flip the status without orphaning a row.
---

## LANDED — 2026-09-09, landing train 25 (`status: landed`; the five residue rows moved to their own owners in the transfer above, so nothing this note still claims is defective)

**The descriptor is a tuple.** `PicInfo.ObjectRef` is an `ObjectRefDescriptor(Kind, Name, Factory, Only)`
(`src/Cobol.Net.Compiler/Binding/Model/ObjectRefDescriptor.cs`), `Kind ∈ {Universal, Interface, ObjectClass,
ActiveClass}`; `PicInfo.ObjectClassName` is DELETED, and every one of its fifteen consumers now ASKS the
descriptor. `ObjectRefDescriptor.ClrTypeName` is the SINGLE derivation of the emitted C# type — `StorageFormPass`
had a second copy and now reads the same property. For an ACTIVE-CLASS reference `Name` is the CONTAINING class
(§13.18.60.3 SR16 guarantees one), handed to the data binders by `OoDriver` as `DataBinder.OoOwnerClassName`, so
SR12 b)2. asks the descriptor instead of re-deriving the class from ambient binder state.

**The grammar prints what the standard prints.** `CobolOO.g4#objectReferenceUsage` is now the three stacked
alternatives of the §13.18.60.2 general format — rendered from the canonical PDF (printed folio 503) because the
diagram is load-bearing — plus the bare universal form. `ACTIVE-CLASS` became a real lexer token with a
`tests/version-matrix/cobol-words.json` row, so `gen-cobol-words.ps1` DERIVES its reservation gate from
`reserved-words.json`: at `--std 85` it is still an ordinary user word (`01 ACTIVE-CLASS PIC X.` compiles and
displays), at 2002+ the §8.9 funnel answers COBOLNET0901. The grammar keeps the SUPERSET parse for
`[FACTORY OF] className [ONLY]` because it cannot separate an interface-name from an object-class-name; the
binder makes the general-format rejection once the name resolves.

**One adjudication, one table.** `DataBinder.OoBindObjectRefDescriptor` is the ONE place a written phrase becomes
a descriptor (COBOLNET0813 unknown name · **COBOLNET1925** FACTORY/ONLY on the interface-name alternative ·
**COBOLNET1924** §13.18.60.3 SR16 placement, both arms). `OoConformance.ObjectRefWideningMismatch` became
`ObjectRefAssignmentMismatch` — the whole §14.9.39.3 SR10/SR12/SR14 sender-into-receiver table, keyed on the
RECEIVER's description. "Widening" named only SR12 a)2., which was the single rule the scalar could express.
§9.3.8.2.3 rule 5 shares the table with `activeClassSenderAdmitted: false`, because its printed 5 b)/5 c)
enumerate no ACTIVE-CLASS sender where SET SR10 c)/SR12 b) do; `DescriptionMismatch` now compares the FULL tuple
(§9.3.8.2.3 rule 2 c): "the presence or absence of the FACTORY and ONLY phrases is the same"), and
`ConformanceDescriptor`'s universal-crossing key carries `SignatureKey` so two different descriptions can never
share a signature.

**SR11 and SR13 fell out of the same table.** A class-NAME sender is the factory object of EXACTLY that class, so
`OoBindSetObjectRef` hands the table the description that object actually has —
`ObjectClass(name, factory: true, only: true)` — and SR13's "shall be described with the FACTORY phrase" is
SR12 a)3., SR13 a) is SR12 a)1., SR13 b) is SR12 a)2. and SR11 is SR10 b)1. Before this, every typed receiver was
refused outright and the rejection WAS the rule's only enforcement.

**The latent misdiagnosis is retired.** `EcBinder`'s SR4d/SR5d test was `ObjectClassName is not { } declared` —
"no class name recorded", which is equally true of a factory, an interface and an ACTIVE-CLASS reference. It now
asks `IsUniversal`, so the moment those shapes became declarable they were NOT refused as "a UNIVERSAL object
reference". Same for `OoBinder`'s §14.9.23.3 SR7 identifier-2 gate, `ReferenceResolver`'s §8.4.3.9.3 SR2
object-property gate (which also learned that a FACTORY-OF receiver selects the FACTORY accessors) and
`OoEmitter`'s SET … TO EXCEPTION-OBJECT narrow.

**INVOKE learned the second roster.** §9.3.6 gives a class two method interfaces and the receiver's FACTORY axis
picks one: `OoBindInstanceInvoke` calls `FindFactoryMethod` for a FACTORY-OF reference and qualifies the argument
profiles with `FactoryCsName`. §16.2.1.2 GR1's created object is described `ObjectClass(cls, only: true)` for a
named class and `ActiveClass(cur)` through SELF|SUPER, which is what lets `INVOKE C "NEW" RETURNING <C ONLY ref>`
compile and an ACTIVE-CLASS receiver take a SELF-NEW result.

**Evidence.** `tests/conformance/2002/pb389_object_reference_descriptor.cob` (+`.out`, expected values derived
from §16.2.1 GR1, §14.9.23.4 GR2, §14.9.39.4 GR9 and §9.3.6 — never from a run) covers SR12 a)1./a)2., SR12 b),
SR13 b), SR10 b)1./c)1./c)2., SR14 b) and GR9's multi-receiver half; eight negatives in
`tests/conformance/negative/` cover COBOLNET1924 (both SR16 arms), COBOLNET1925 and SR12 a)1./a)3./b)1./b)3./c)1.
`ObjectRefDescriptorDriftTests` is the drift pin: all EIGHT writable shapes, each proved to accept a sender of its
own description AND to refuse a sender its rule's closed list excludes. Three `constructs.json` rows carry the
per-edition gate for ACTIVE-CLASS, FACTORY OF and ONLY.

**Rows.** Twelve of the note's seventeen flipped to CONFORMS — measured on the train-25 MERGED tree, where
the batches read **GAP 2697 → 2685**, not on the implementer's own base. The other FIVE were never this
mechanism, and the registrar's transfer above moved them to their own owners BEFORE this landing, which is
what lets `status` flip here without leaving a defective row unclaimed:
* `SR-14.9.14.3-5` (EXIT RAISING SR5) and `SR-14.9.18.3-4` (GOBACK RAISING SR4) stay PARTIAL. The descriptor half
  landed — SR4d/SR5d no longer misname a typed operand — but the b) (interface-name) and c) (ACTIVE-CLASS) legs
  need the OTHER door the mechanism section named: `CobolParserCore.g4#raisingClause` is `RAISING cobolWord+`, so
  §14.2.1's `[FACTORY OF]` in the procedure-division header is a raw parse error and an interface-name draws
  COBOLNET0858. That is a `raisingClause`/§14.2.1 mechanism, and it is [[PB815]]'s now.
* `SR-14.9.39.3-20` (FUNCTION-POINTER), `-22` (PROGRAM-POINTER TO prototype) and `-24` (SET Format-10 over a
  STRONG-restricted data-pointer) stay NOT-IMPLEMENTED: they are the USAGE POINTER-TAIL family, not the
  object-reference descriptor, and `-24` is a live UNDER-REJECTION whose subject is already declarable.
  `-20` and `-22` are [[PB817]]'s; `-24` is [[PB816]]'s.