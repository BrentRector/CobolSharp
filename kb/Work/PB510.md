---
title: "PB510 — bwz-lexer-zero-synonyms"
id: PB510
kind: defect
status: open
severity: MINOR
area: frontend/lexer
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.18.8.2"]
inventory_rows: ["FMT-13.18.8.2"]
tags: [cobolsharp, work, defect]
---

# PB510

## Mechanism — blank-when-zero-clause
bwz-lexer-zero-synonyms - the lexer folds three distinct reserved words into one token. CobolLexer.g4:722 declares `ZERO : 'ZERO' | 'ZEROS' | 'ZEROES' ;`, so blankWhenZeroClause (`BLANK WHEN? ZERO`) accepts all three spellings and the distinction is unrecoverable downstream. ZERO/ZEROS/ZEROES are interchangeable only as the FIGURATIVE CONSTANT (the figurative-constant general format lists all three as required alternatives); in the BLANK WHEN ZERO clause ZERO is a required word of the clause's own general format, which admits no synonym.

### Expected, from the standard
ISO/IEC 1989:2023 13.18.8.2 general format, rendered from the canonical PDF page 409 (scripts/render-spec-page.py 409): `BLANK WHEN ZERO` with BLANK and ZERO UNDERLINED and WHEN NOT underlined. WS-1 (`BLANK ZERO`, the optional word omitted) is legal. WS-2 and WS-3 are not: the format's required word is ZERO, and the clause's own format is the only licence for the words it contains.

```
$ python scripts/spec/cite.py --check 13.18.8.2 "s required word is ZERO, and the clause"
  FAIL: §13.18.8.2 exists but does NOT contain 's required word is ZERO, and the clause'
  ⛔ REGISTRAR: no quotation in this finding validated verbatim against §13.18.8.2. The MECHANISM below is measured and stands, but the QUOTATION is the finding's paraphrase — quote the clause and re-run `--check` before it reaches a code comment, a golden or the DEVLOG (CLAUDE.md rule 1).
```

### Observed
Observed `1[     ]2[     ]3[     ]` - all three compile and all three blank, at every edition. This is the lowest-harm row in the subject and the fix is lexer-wide rather than BLANK WHEN ZERO-specific (the same token merge is relied on deliberately elsewhere - see the `BINARY ZERO // ZERO already covers ZEROES / ZEROS` comment at src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4:244), so it is filed as its own mechanism rather than clustered with the missing syntax-rule validator. It is recorded because the general format is the rule this row adjudicates and the acceptance is measured, not because a user is likely to be harmed by it.

### Repro
E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/bwz/a8.cob - `01 WS-1 PIC 9(5) BLANK ZERO.`, `01 WS-2 PIC 9(5) BLANK WHEN ZEROES.`, `01 WS-3 PIC 9(5) BLANK WHEN ZEROS.` Run E:/CobolSharp/.claude/worktrees/lane3-pin-89044aa0/src/Cobol.Net.Cli/bin/Debug/net10.0/cobol.exe a8.cob --run.

### Code site
src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4:722 with src/Cobol.Net.Frontend/Grammar/Core/CobolData.g4:660-663. Any fix must be structural (the three words are distinct reserved words the parser cannot currently tell apart), and must be swept against every other format that writes one of the three as a required word before the token is split.

## Sibling sweep the fixer owes
Every bug here is a pattern (CLAUDE.md rule 4). Before closing this note, sweep the sibling arms named in the mechanism above and add the ones that fail to `inventory_rows`.

## Provenance
Lane-3 adjudication batch b3 (subjects: blank-when-zero-clause), adjudicated against the compiler pinned at 89044aa0 and RE-PROBED on the registrar's worktree cut from 6e7caa91 (a 1942-program sweep of `out-b3/probe/` plus targeted re-probes); the finding reproduces on today's tree.

## Repro programs
`E:\Temp\claude\E--CobolSharp\a9d94900-79fe-4a38-89f0-921976861543\scratchpad\repros\PB510\` — bwz. The implementer's opening move is to rerun them against a freshly built `cobol.exe`.

