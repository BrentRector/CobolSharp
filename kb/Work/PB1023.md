---
title: "PB1023 — ✅ LANDED 2026-09-22 (wave 54). WAS: WRITE … ADVANCING identifier-2 applies NO operand-class screen: a `PIC X VALUE \"2\"` advances two lines and a `PIC 9V9` holding 1.5 advances one, with no diagnostic — §14.9.51.3 SR14 requires an integer data item"
id: PB1023
kind: defect
status: landed
severity: MINOR
area: binder/io
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB210", "PB211", "PB212"]
spec_refs: ["14.9.51.3"]
inventory_rows: []
closes_rows: ["SR-14.9.51.3-14", "SR-14.9.51.3-15"]
tags: [cobolsharp, work, defect]
---

Filed 2026-09-22 by **REGISTRAR #12** from `leads-w48-w52.md` (wave 52 group B, PB210/PB211/PB212 implementer, worktree wf_31320d12-51e-2). Re-probed on the registrar's own `dotnet build CobolSharp.sln -c Debug` (0 warnings, 0 errors) at main `37f19e028`; every citation below re-run through `python scripts/spec/cite.py --check` on this tree.

# PB1023 — LANDED

## Landed (2026-09-22, wave 54 group H)
Re-probed first on the implementer's own build: `wadvx.cob` (PIC X "2") and `wadvv.cob` (PIC 9V9 1.5) both compiled
clean. Reproduced — and the SIBLING ARM of the same position was open too: `ADVANCING 1.5 LINES`, `"2" LINES`,
`-1 LINES` and a constant-name standing for 1.5 all compiled (integer-1, §14.9.51.3 SR15 "Integer-1 shall be positive
or zero").

**Fix.** identifier-2 is the `OperandPositions.WriteAdvancingIdentifier` row (IntegerItem) of train 52's ONE
operand-class screen, and integer-1 is screened beside it in `SequentialIoBinder.ScreenAdvancingCount` through the ONE
integer classifier (`IntrinsicResultType.IsIntegerOperand`) plus SR15's sign test — both on the BOUND shape, so a
constant-name is integer-1 (§13.10.4 GR1), under ONE new code, COBOLNET2365 (both lanes). The drift test's template
harness gained a declared print file so a WRITE row has an end-to-end template (every shape × every row);
DESIGN-binder-bound-tree.md §3.6 names the row.

**Goldens.** `conformance:85/pb1023_write_advancing_count` (DISPLAY / BINARY / PACKED integer items and the zero
literal, values from §13.18.34.4 GR7's LINAGE-COUNTER increments); `conformance:negative/pb1023-write-advancing-count`
(all editions, COBOLNET2365, both arms).

## Original filing

## Repro — measured
`scratchpad/w52b/wadvx.cob` (PIC X identifier-2) and `wadvv.cob` (PIC 9V9) → both compile and run to `DONE`.

## The rule
`OK  §14.9.51.3 14)` — "Identifier-2 shall reference an integer data item."

## Code site
`SequentialIoBinder.BindAdvancing` binds identifier-2 as a bare FieldOperand. Same mechanism as PB210–PB212 (an operand
position with no row in `OperandPositions`): one row plus one `OperandClassScreen` call, with a NEW diagnostic code
(allocate centrally).
