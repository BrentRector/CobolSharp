---
title: "PB450 — SET's printed OUTER REPETITIONS were dropped from the grammar in Format 4 and Format 7 while the identically-shaped Format-3 twin eighteen lines away carries its own, so conforming COBOL dies as a bare COBOL0001; Format 7's receiver is additionally hard-coded to arity one and SR18 is decided by a scope-blind name map"
id: PB450
kind: defect
status: landed
severity: MAJOR
area: frontend/grammar
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: [PB388, PB956, PB953, PB954]
spec_refs: ["14.9.39.2", "14.9.39.3", "14.9.39.4", "8.4.3.11.4", "13.16.3"]
inventory_rows: []
closes_rows: ["GR-14.9.39.4-8", "FMT-14.9.39.2", "GR-14.9.39.4-12", "GR-14.9.39.4-13"]
tags: [cobolsharp, work, defect]
---

# PB450 — LANDED (both halves; the one held row handed to [[PB956]] on 2026-09-22) — SET's printed outer repetitions are missing from Formats 4 and 7

Lane-3 adjudication batch b2, group g7-set-a. **Every repro below was re-run on HEAD `89044aa0`**; all still
reproduce.

## 1. THE MECHANISM

Three of SET's printed formats put an **ellipsis outside a brace**. The grammar transcribed one of them and
not the other two, in the same file, eighteen lines apart.

    CobolParserCore.g4:1230   setSwitchStatement  : SET (dataReference+ TO (ON | OFF))+ ;     ← outer repeat PRESENT
    CobolParserCore.g4:1248   setBooleanStatement : SET dataReference+ TO (TRUE_ | FALSE_) ;  ← outer repeat ABSENT
    CobolParserCore.g4:1255   setAddressStatement : SET ADDRESS OF dataReference TO (dataReference | NULL_)
                                                  | SET dataReference TO ADDRESS OF dataReference ;

Formats 3 and 4 have the **same printed skeleton** — an outer brace around `{ name } … TO { keyword | keyword }`
with a trailing ellipsis on the outer brace. One transcription carries it, its twin does not.

Format 7 is worse than a missing repeat: it is **split into two fixed productions on the sender's spelling**
instead of on the format. The printed receiving operand is one brace, `{ ADDRESS OF data-name-1 | identifier-5 }`,
with the ellipsis **outside** it — so a Format-7 statement may carry any number of receivers, each independently
either spelling, mixed in any order, and identifier-6 may itself be a data-address-identifier. The two
productions cover two cells of that cross-product and hard-code arity one in both. The plural-`identifier-5`
case survives only **by accident**, through a different rule: `SET P1 P2 TO P0` is claimed by
`setToValueStatement` and re-routed semantically at `SetBinder.cs:345-346` into `BindSetPointer`, which does take
a target list. That accident is the tell — the arity is not a considered restriction.

Because these are **parse** failures, no syntax rule is ever asked about the operands: SR17 and SR18 are
unreachable on every shape the grammar cannot express.

## 2. THE RULES, QUOTED, WITH THEIR `--check` LINES

    python scripts/spec/cite.py --check 14.9.39.2 "The outer braces enclose the single repeated unit; the trailing"
    OK  §14.9.39.2   (General formats)
        > **Figure notes (SET statement Format 4 (condition-setting) syntax diagram).** … The outer braces
          enclose the single repeated unit; the trailing `…` repeats the whole braced portion …

    python scripts/spec/cite.py --check 14.9.39.2 "repeats the braced receiving operand"
    OK  §14.9.39.2   (General formats)
        > **Figure notes (SET statement Format 7 (data-pointer-assignment) syntax diagram).** … The braces are a
          plain required choice — exactly one of the two alternatives — and the following `…` repeats the braced
          receiving operand.

    python scripts/spec/cite.py --check 14.9.39.4 "If multiple condition-names are specified, the results are the same as if a separate SET statement had been written for each condition-name-1"
    OK  §14.9.39.4 8)  (General rules)

    python scripts/spec/cite.py --check 14.9.39.4 "the address identified by identifier-6 is stored in each data item referenced by identifier-5 in the order specified"
    OK  §14.9.39.4 12)  (General rules)

    python scripts/spec/cite.py --check 14.9.39.4 "the address identified by identifier-6 is assigned to each based item referenced by data-name-1 in the order specified"
    OK  §14.9.39.4 13)  (General rules)

    python scripts/spec/cite.py --check 14.9.39.3 "Identifier-5 shall reference a data item of category data-pointer. Identifier-6 shall be of category data-pointer"
    OK  §14.9.39.3 17)  (Syntax rules)

    python scripts/spec/cite.py --check 14.9.39.3 "Data-name-1 shall be a based data item"
    OK  §14.9.39.3 18)  (Syntax rules)

    python scripts/spec/cite.py --check 8.4.3.11.4 "Data-address-identifier creates a unique data item of class pointer and category data-pointer"
    OK  §8.4.3.11.4 1)  (General rules)

GR12 and GR13 corroborate the figure independently: both say "each … **in the order specified**", wording that
presupposes a list. §8.4.3.11.4 GR1 makes `ADDRESS OF x` category data-pointer, which is exactly what SR17's
second sentence demands of identifier-6 — so the data-address-identifier sender is legal, not an extension.

The adjudication's diagram claims were made from a **rendered** PDF page 760 / folio 730, not from the OCR
(CLAUDE.md rule 1); the figure notes above agree with that render.

## 3. EXPECTED vs OBSERVED — RE-RUN ON HEAD 89044aa0

Probes from `.../lane3/out-b2/probe/set1/`, `--std 2023 --run` (identical at `--std 2002`):

    cobol h2_cond_repeat.cob       # SET CF-A TO TRUE CG-A TO TRUE
      h2_cond_repeat.cob(17,34): error COBOL0001: unexpected 'TO'
      expected: WS-F = 1 and WS-G = 3 (GR8 — as if written as separate SET statements)

    cobol h8_switch_repeat.cob     # the Format-3 CONTROL: SET SW1 TO ON SW2 TO OFF
      S1ON / OK                                            ← the same printed shape, and it works

    cobol h2b_cond_single.cob      # the inner-repeat CONTROL: SET CF-A CG-A TO TRUE
      F=[1] G=[3]                                          ← the inner `…` is present

    cobol c1_two_addr_recv.cob     # SET ADDRESS OF B1 ADDRESS OF B2 TO P1
      (18,30): error COBOL0001: unexpected 'ADDRESS'   (+2 cascades)
      expected (GR13): B1 and B2 both rebased onto P1

    cobol c2_two_ptr_recv_addrsend.cob   # SET P1 P2 TO ADDRESS OF REC1
      (18,25): error COBOL0001: no viable alternative at input 'SETP1P2TOADDRESS'   (+4 cascades)
      expected (GR12): P1 and P2 both address REC1

    cobol c3_mixed_recv.cob        # SET ADDRESS OF B1 P1 TO P2
      (18,30): error COBOL0001: unexpected 'P1'

    cobol i3_addr_to_addr.cob      # SET ADDRESS OF B1 TO ADDRESS OF R1
      (11,33): error COBOL0001: no viable alternative at input 'ADDRESS'   (+2 cascades)

    cobol c4_one_addr_recv.cob     # CONTROL: SET P1 TO ADDRESS OF REC1 / SET ADDRESS OF B1 TO P1
      B1A=[AAAA]
    cobol c7_two_ptr_recv.cob      # CONTROL: SET P1 P2 TO P1
      OK

Every individual capability is present — multi-receiver pointer assignment, `ADDRESS OF` as receiver,
`ADDRESS OF` as sender. Only the grammar shape that lets them **combine** is missing.

### 3b. SR18's second half — the scope-blind name map (refuter, re-measured on HEAD)

↔ **This half is a DISTINCT mechanism and has its own note, [[PB467]]** (the resolver defect: a scoped question
answered from the flat unit-wide map, reaching `ALLOCATE`'s §14.9.3.3 SR1 through the same helper). PB450 keeps
the `SR-14.9.39.3-18` row because it owns the format-shape mechanism; PB467 claims no rows. **Fix them
together** — the summary below is what PB467 measures in full.

`PtrResolveBased` (`PtrBinder.cs:293-303`) reads `ctx.Data.ByName` **directly** instead of the scope-aware
`SymbolTable.TryResolve`, which consults `scope.Method.ByName` first. Measured on HEAD, probes from
`.../lane3/out-b2/probe/rf-set1/`:

    cobol s2_method_local_based.cob --std 2023 --run
      … error COBOLNET0869: 'MB': the operand shall be a BASED level-01/77 item (ISO §14.9.39 SR18 / …)
      — MB is declared `01 MB BASED` in the METHOD, i.e. exactly what SR18 requires. Wrong verdict.
    cobol s4_method_local_storage_based.cob --std 2023 --run
      … the same COBOLNET0869 on a method LOCAL-STORAGE `01 MB BASED`.
    cobol s3_method_shadow_nonbased.cob --std 2023 --run
      … NO SR18 diagnostic at all — a method-local NON-based `01 SHR` that legally shadows an object-level
        `01 SHR BASED` is accepted as based. The opposite wrong verdict, same map.

Both directions flip when the *same* declaration moves between the object's and the method's data division,
which is the proof that scope — not the declaration — is deciding. Every one of these programs also draws the
named residue `COBOLNET0899: BASED data or ADDRESS OF in a class definition's data divisions is recognized but
not yet implemented`, so SR18's whole domain inside class definitions is loud-unimplemented today.

## 4. THE CODE SITES AND THE SHAPE OF THE REPAIR

| arm | file:line |
|---|---|
| Format 4, outer repeat missing | `src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4:1248-1250` |
| Format 3, the twin that has it | `CobolParserCore.g4:1230-1232` |
| Format 3's group re-assembler | `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/SetAlterBinder.cs:167` `SwitchBindSet` |
| Format 4's single-group binder | `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/SetBinder.cs:451-465` `BindSetCondition` |
| Format 7, arity-one productions | `CobolParserCore.g4:1255-1258` |
| Format 7's single-receiver binder | `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/PtrBinder.cs:30-90` `BindSetAddress` (`sa.dataReference(0)` / `(1)`) |
| SR18's scope-blind lookup | `PtrBinder.cs:293-303` `PtrResolveBased` (`ctx.Data.ByName` … `list[0]`) |

⛔ **The repair is three productions and one lookup, not four more alternatives** (CLAUDE.md rule 5):

1. **One shape for Formats 3 and 4.** `SET (dataReference+ TO position)+` with a **single** group-splitting
   helper, replacing `SwitchBindSet`'s hand-written token-position re-assembly. A second hand-written
   re-assembler beside it is the thing to avoid; the FALSE arm ([[PB455]]) then gets its grouping for free.
2. **One production for Format 7.** `setAddressStatement : SET setAddressReceiver+ TO (dataReference | addressOf | NULL_)`
   with `setAddressReceiver : ADDRESS OF dataReference | dataReference`, bound to a **receiver list whose
   elements carry their own kind**. GR12 and GR13 then become one loop over one list, exactly as the standard
   writes them, and the next receiver spelling is automatic.
3. **SR18 resolves through the SymbolTable**, not `ctx.Data.ByName`. The class doc-comment at `PtrBinder.cs:22`
   already flags the bypass as a "documented SymbolTable bypass … a flagged behavior-sensitive follow-up"; the
   measurement in §3b is that follow-up coming due.

## 5. THE SIBLING SWEEP THE FIXER MUST DO

* The two other outer-ellipsis sites in this clause: Format 6 (now refused by name, COBOLNET1707 — see the
  ATTRIBUTE landing `60bf02d7`) and Format 11's LC_ choice-indicator group.
* `PtrResolveBased` has a second caller — ALLOCATE, `PtrBinder.cs:187` — so the scope fix lands on
  §14.9.3 SR1 in the same edit; sweep FREE and `SET ADDRESS OF … TO NULL` with it.
* The stale in-code format ordinals around these arms (`PtrBinder.cs:38`'s "GR12-13" label on the
  based-receiver arm, where GR12 governs identifier-5 and never reaches it; the ordinal in
  `CobolParserCore.g4:1247`'s `setBooleanStatement` comment; the pointer-SET diagnostics' ordinal at
  `SetBinder.cs:194/:204/:217` and `BoundTree.cs:744`) belong to **[[PB388]]**'s sweep — cross-reference, do
  not re-tabulate.
* [[PB449]] is the *selection* half of the same statement: fixing the grammar to admit a list does nothing if
  the format is still chosen from operand `[0]`.

## 6. ROWS

* **`FMT-14.9.39.2`** — DIVERGES. Rests on the measured parse failures of Format 4's outer repetition and
  Format 7's receiving list/mixed brace (§3), against Format 3's working twin.
* **`GR-14.9.39.4-8`** — PARTIAL. The per-condition-name semantics are correct for the shape that parses
  (`h2b_cond_single` → `F=[1] G=[3]`); the verdict rests on the compound form being a syntax error at every
  edition.
* **`SR-14.9.39.3-17`** — PARTIAL. Both sentences are enforced over the shapes the grammar admits (a non-pointer
  identifier-6 and a non-pointer identifier-5 both draw COBOLNET0869); the verdict rests on four legal Format-7
  statements never reaching the screen at all.
* **`SR-14.9.39.3-18`** — PARTIAL. The outer-program single-receiver lane is right in both directions and its
  rejection arm is pinned by `conformance:negative/set-address-non-based`; the verdict rests on (i) the
  measured method-local scope inversions in §3b, (ii) the COBOLNET0899 class-definition residue covering the
  whole domain, and (iii) SR18 never being asked about the second and later `ADDRESS OF data-name-1` operands.


## 2026-09-21 — HALF 1 LANDED (wave 42 group A): Format 4's outer repetition, and the structure §4 asked for

⛔ **THE NOTE STAYS OPEN.** §4's repair item 1 (Formats 3 and 4) is landed; item 2 (Format 7's receiving list)
is NOT, and item 3 (SR18 through the SymbolTable) landed earlier as [[PB467]]. One row moves.

**Reproduced first, on this worktree's own build**, exactly as §3 recorded it — `SET CF-A TO TRUE CG-A TO TRUE`
was `error COBOL0001: unexpected 'TO'` at every edition while its Format-3 twin compiled.

**The printed figure was RE-RENDERED, not inherited** (`specs-private` PDF p760 / folio 730 at 200dpi, CLAUDE.md
rule 1): Format 4 is `SET { { condition-name-1 } … TO { TRUE | FALSE } } …` with both ellipses, and Format 7 is
`SET { ADDRESS OF data-name-1 | identifier-5 } … TO identifier-6` — the `…` after the receiving brace, the
sender OUTSIDE the repetition. Both figure notes re-validated with `cite.py --check 14.9.39.2`.

**What landed**

* `setBooleanStatement : SET setConditionPhrase+` — the printed outer repetition.
* ⛔ **AND THE REPEATED UNIT IS A NAMED RULE IN BOTH FORMATS**, which is §4's "not a second hand-written
  re-assembler". An inline `( … )+` group flattens the phrases into one `dataReference()` list and one `TO()`
  list, which is why `SetAlterBinder.SwitchBindSet` **and** the legacy `DataStatementBinder.BindSetSwitch` each
  carried their own token-index re-assembly of a structure the parser already knew. `setSwitchPhrase` /
  `setConditionPhrase` are those units; all three binders now loop over phrase nodes and both re-assemblers are
  deleted. `PrintedFormatAlternativeDriftTests.EverySetStatementRule_NamesItsRepeatedUnitInsteadOfInliningIt`
  fails any future `set…Statement` that inlines its repeated unit, so Format 7's rule cannot regress into one.
* `BoundSetConditions` carries `(Place, Condition88, ToTrue)` triples — **the arm is the GROUP's**, which is GR8
  taken literally, and the emitter reads it per store.
* The COBOL-2002 gate on the FALSE arm is asked **per phrase** (`VersionConformancePass.VisitSetBooleanStatement`
  over `setConditionPhrase()`); a statement-level question would have found no `FALSE_` token once the phrase
  rule landed and let a 2002 construct through at `--std 85`. `negative/pb450-set-false-in-second-group-below-2002`
  pins exactly that position.

**Goldens:** `85/pb450_set_outer_repetition` (both twins in one program — Format 4's outer and inner `…`, and
Format 3's outer `…` on ON and OFF), `2002/pb450_set_condition_mixed_groups` (groups that disagree, and written
order), `negative/pb450-set-false-in-second-group-below-2002`.

**Row:** `GR-14.9.39.4-8` PARTIAL → CONFORMS (batch `batch-03-pb450-and-the-renamed-recipe-drift-test.json`).

**⛔ NEXT STEP FOR THE FINISHER — §4's item 2, verbatim, and nothing else is left of this note:**
`setAddressStatement : SET setAddressReceiver+ TO (ADDRESS OF? dataReference | dataReference | NULL_)` with
`setAddressReceiver : ADDRESS OF? dataReference | dataReference`, bound to a receiver LIST whose elements carry
their own kind, so §14.9.39.4 GR12 and GR13 become one loop over one list. Today `PtrBinder.BindSetAddress`
reads `sa.dataReference(0)` / `(1)` and `BoundSetAddressOfBased` holds ONE based item, so the mixed and
multi-receiver shapes of §3's probes (`c1_two_addr_recv`, `c2_two_ptr_recv_addrsend`, `c3_mixed_recv`,
`i3_addr_to_addr`) are still `COBOL0001`. `FMT-14.9.39.2`, `SR-14.9.39.3-17` and `SR-14.9.39.3-18` stay claimed
here and stay GAP until that lands.

## Rows adopted 2026-09-21 by **REGISTRAR #8** — SET Format 7's receiver arity

`GR-14.9.39.4-12` and `GR-14.9.39.4-13` are adopted from `kb/Work/PB388`, whose implementer report asks for
exactly this handoff (*"PB450 should adopt `GR-14.9.39.4-12`/`-13`"*) and whose own note records that it holds
them only because an adjudication batch gave them to it: *"Those two deserve their own notes; they are recorded
here because the b2 clustering gave this note their rows and a row must not go unowned."*

Both rows ARE this note's mechanism, read from the inventory:

* `GR-14.9.39.4-12` — FORMAT 7 — *"If identifier-5 is specified, the address identified by identifier-6 is
  stored in EACH data item referenced by identifier-5 IN THE ORDER SPECIFIED"*
* `GR-14.9.39.4-13` — FORMAT 7 — *"If data-name-1 is specified, the address identified by identifier-6 is
  assigned to EACH based item referenced by data-name-1 IN THE ORDER SPECIFIED"*

"EACH … IN THE ORDER SPECIFIED" is a general rule ABOUT the outer repetition this note is about: while Format 7's
printed `{ identifier-5 } …` is missing from the grammar, the rules governing what the statement does to the
second and subsequent operands cannot be satisfied, because those operands cannot be written. So the surface and
the two general rules are one fix, and the rows close with it.

Both are **DIVERGES/GAP**; neither had a non-terminal claimer other than `PB388`, which is `half` and is closing.
`PB388`'s claim is deliberately left in place — a row may be co-claimed and dropping one is the landing's job.

## 2026-09-21 — HALF 2 LANDED (wave 43 group D): Format 7 is ONE production with a receiving LIST

⛔ **THE MECHANISM IS FINISHED.** §4's repair item 2 — the last of the three — is landed; item 1 landed in
train 43 and item 3 landed earlier as [[PB467]]. **The note stays open for ONE ROW ONLY**, and that row's sole
remaining ground is a DIFFERENT mechanism (see "the handoff" below).

**Reproduced first, on this worktree's own build**, exactly as §3 recorded it — all four shapes still died in
the parser at every edition:

    SET ADDRESS OF B1 ADDRESS OF B2 TO P1   error COBOL0001: unexpected 'ADDRESS'
    SET P1 P2 TO ADDRESS OF REC1            error COBOL0001: cannot parse construct near 'REC1'
    SET ADDRESS OF B1 P1 TO P2              error COBOL0001: unexpected 'P1'
    SET ADDRESS OF B1 TO ADDRESS OF R1      error COBOL0001: unexpected 'ADDRESS'

**The printed figure was RE-RENDERED, not inherited** (`specs-private` PDF p760 / folio 730 at 300 dpi,
CLAUDE.md rule 1): Format 7 is `SET { ADDRESS OF data-name-1 | identifier-5 } … TO identifier-6` — the brace a
PLAIN required choice, the `…` OUTSIDE it, `SET`/`ADDRESS`/`TO` ruled and `OF` NOT. The figure note says the
same in words and was re-validated: `cite.py --check 14.9.39.2 "repeats the braced receiving operand"` → OK.

**What landed**

* `setAddressStatement : SET setAddressReceiver+ TO setAddressSender` — ONE production for the printed figure,
  replacing two fixed productions split on the SENDER's spelling that each hard-coded arity one. The repeated
  unit is a NAMED rule, which is half 1's discipline held for this format; `setAddressReceiver` carries its own
  kind (`(ADDRESS OF?)? dataReference`), so no binder re-derives the spelling from a token position.
* ⛔ **ONE BOUND NODE, BECAUSE IT IS ONE PRINTED FORMAT.** `BoundSetAddressOfBased` is DELETED and
  `BoundSetPointer` now holds `IReadOnlyList<BoundPointerReceiver>`, each element `(Pointer | Based)`. GR12 and
  GR13 are ONE loop in `SetEmitter.EmitSetPointer` over that list — which is how the standard writes them
  ("EACH … in the order specified") — and a MIXED statement becomes expressible at all. Both routes into
  Format 7 build the same list: `SetBinder.BindSetPointer` (the plain identifier-5 route through
  `setToValueStatement`/`setObjectReferenceStatement`) and `PtrBinder.BindSetAddress` (the ADDRESS-bearing one).
* SR17, SR18 and SR19 are asked **per receiving operand**, so they reach the second and later operands the old
  arity could not express. SR19's restriction screen is per receiver because the restriction is data-name-1's,
  not the statement's.
* ⛔ `setObjectReferenceStatement` MOVED AHEAD of `setAddressStatement` in `setStatement`, and the order is now
  the routing: SR19 lets identifier-6 be the predefined address NULL and SR9 lets identifier-4 be the predefined
  object reference NULL, so `SET X TO NULL` is token-identical in Formats 5 and 7 and only the RECEIVER's
  category tells them apart — a BIND question (`SetFormatSelection`), not the parser's.
  `PrintedFormatAlternativeDriftTests.SetObjectReferenceStatement_IsOfferedBeforeSetAddressStatement` holds it,
  and `EverySetStatementRule_NamesItsRepeatedUnitInsteadOfInliningIt` now pins this rule's shape too.
* The sibling arm §5 named — `DataBinder.Ptr.cs#PtrScanAddressOfTargets`, the storage-forcing scan — read
  `GetChild(1)` and `dataReference(1)` to tell the two old productions apart. It asks `setAddressSender`'s own
  ADDRESS phrase now, which is the only data-address-identifier in the grammar; an `ADDRESS OF` in a RECEIVING
  operand is data-name-1, a BASED item that is never storage-forced.
* The legacy `DataStatementBinder.BindSetAddress` (the differential oracle until P15, which shares this grammar)
  reads the new tree and binds only the two arity-one shapes it always did.

**The arity sweep, run and empty.** Every other `set…Statement` was read for a hard-coded arity against its
printed figure: Formats 8, 9, 15 and the `TO ENTRY` vendor spelling already write `dataReference+`, Format 16's
printed receiving operand IS one operand, Formats 3 and 4 landed in half 1, and Format 6 is refused by name
(COBOLNET1707). Format 7 was the last one.

**Goldens:** `2002/pb450_set_format7_receiving_list` (all four measured shapes plus a THREE-operand mixed list),
`negative/pb450-set-address-second-operand-below-2002` (the COBOL-2002 gate at the SECOND operand — the program
carries exactly one `setAddressStatement`, and it is the mixed one, so the case cannot stay green if the mixed
list regresses), `negative/pb450-set-format7-second-receiver-not-pointer` (SR17 at the second operand).

**Rows:** `FMT-14.9.39.2`, `GR-14.9.39.4-12` and `GR-14.9.39.4-13` DIVERGES → CONFORMS
(batch `batch-01-pb450-format7-receiving-list.json`). `SR-14.9.39.3-17` was already CONFORMS on [[PB456]]'s
evidence and is **not** claimed as closed here; the batch only ADDS the evidence §6 said it lacked (SR17 asked
about an operand the grammar could not express) and corrects its diagnostic text, which named the SENDER's
spelling and carried a U+2026 the renderer transliterates to ASCII ([[PB388]]).

⛔ **"ONLY ADDS" WAS NOT WHAT THE BATCH DID — repaired by `batch-04-evidence-union-repair.json`.**
`record_verdicts.py` REPLACES a row's `code-location` and `test-ref` rather than merging them, so batch 1's two
PB450 witnesses overwrote `SR-14.9.39.3-17`'s three PB449/PB456 witnesses and its one code-location overwrote
their three. Batch 4 restores the union, so SR17's two sentences are witnessed at arity one (PB456: a
non-pointer sender, a numeric receiver, the selected-format positive) AND over the printed receiving list
(PB450). The same slip hit `SR-14.9.13.3-3` through batch 3 — see [[PB594]] — where it also left a reference to
a case that does not exist; a sweep of all 3,826 `conformance:` references in the inventory found no third
instance, so the pattern is the TOOL's replace-not-merge semantics, not a rotted corpus. Registered as a lead.

**⛔ THE HANDOFF — the one row left, and it is NOT this note's mechanism.** `SR-14.9.39.3-18` stays PARTIAL.
Two of §6's three grounds are gone: the method-local scope inversions were fixed by [[PB467]], and SR18 now
reaches the second and later `ADDRESS OF data-name-1` operands (measured: `SET ADDRESS OF B1 ADDRESS OF
NOT-BASED TO P1` draws COBOLNET0869 against NOT-BASED). The third remains and belongs to the **OO cell/bridge
emission residue**, measured on this build — a method writing `SET ADDRESS OF OB TO P1` over an object-level
`01 OB BASED` draws `COBOLNET0899: BASED data or ADDRESS OF in a class definition's data divisions is
recognized but not yet implemented (… ISO §13.18.5 / §8.4.3.11)` plus a COBOLNET0869 about the operand's cell.
No `kb/Work` note owns that residue today; it is reported as a lead for the registrar, and this note holds the
row only so it does not go unowned.


## THE RESUMED LANDING'S OWN FINDINGS (wave 43 group E)

Nothing below changes the mechanism above; all three are defects the resumed landing found IN ITS OWN CHANGE
SET while re-probing and re-checking it, and repaired in the same change set.

**1. `record_verdicts.py` REPLACES a row's evidence, and two batches subtracted while their prose said they
added.** Batch 1 left `SR-14.9.39.3-17` with the two PB450 witnesses ALONE, dropping the three PB449/PB456
witnesses and three code-locations; batch 3 left `SR-14.9.13.3-3` with two of five test-refs and one of seven
code-locations, and misspelled one of them into `negative/pb398-range-alphabet-numeric`, a case that does not
exist. A CONFORMS row resting on a dangling witness is exactly what the evidence invariant forbids. All 3,830
`conformance:` references in the inventory were swept: that was the ONLY dangling one, so the pattern is the
tool's replace-not-merge semantics and not a rotted corpus. `batch-04-evidence-union-repair.json` restores both
unions.

⚠ **AND THE TOOLING WAS NOT AT FAULT FOR THE DANGLING REFERENCE — THE MISSING GATE WAS.**
`record_verdicts.py` says so in its own header: whether a `test-ref` names a test that is really on disk "is
checked by the C# battery gate `SpecTraceabilityInventoryDriftTests` — deliberately NOT here", and that test
has an `UnresolvedTestRefs` arm with its own fixture (`conformance:2023/no_such_case`). It would have caught
`pb398-range-alphabet-numeric` the moment the Unit leg ran. The predecessor's gate died inside the Conformance
leg and its `STATUS.md` reported the group finished anyway, so the leg that owns this check never ran. What IS
uncaught is the OTHER half: `record_verdicts.py` REPLACES `code-location`/`test-ref` rather than merging them,
and no gate can detect a batch that SUBTRACTS witnesses — a row left with fewer, still-resolving references
looks exactly like a row that never had more. That is the lead.

**2. Four quotations were the DIAGNOSTIC's words attributed to the standard.**
`cite.py --check 14.9.39.3 "shall be a USAGE OBJECT REFERENCE data item"` FAILS; SR8 reads *"Identifier-3 shall
be any item of class object that is permitted as a receiving item"* (`--check` OK). The same shape held for
`§8.8.1.1's "of category alphanumeric is not a numeric operand"`, which is COBOLNET0844's text. Repaired in
`PicInfo.MessageTagRecovery`, `SetBinder.BindMessageTagSet`, `DiagnosticCatalog.McsMessageTagSetUnsupported`
and `docs/CONFORMANCE.md` §4 item 1 — the diagnostics' words are attributed to the diagnostics, with the rule's
own text beside them.

**3. ⛔ THE RESTRICTED-DATA-POINTER CONVERSE IS §14.9.39.3 SR19'S THIRD SENTENCE, NOT SR20 — six code sites and
two goldens, one of them user-visible.** *"If identifier-6 references a restricted data-pointer, either
identifier-5 shall reference a data-pointer restricted to the same type or data-name-1 shall be a typed item of
the type to which identifier-6 is restricted"* (`cite.py --check 14.9.39.3` → OK, **19)**) is an UNNUMBERED
continuation paragraph of rule 19. **RENDERED, not inherited** (folio 736 / PDF p766 at 300 dpi): 20) is
FORMAT 8's *"Identifier-12 shall reference a data item of category function-pointer ..."*. The wrong number had
propagated into `PtrBinder` (including the COBOLNET0869 message a user reads), `DataBinder`, `PicInfo`,
`StrongTypeModel` (×2), `negative/pb153-set-unrestricted-to-address-of-strong` and
`2023/pb153_restricted_pointer` — CLAUDE.md rule 1's named failure mode exactly. All corrected; the sweep for
`SR19/SR20` in a restriction context is now empty. The message's parenthetical was ALSO wrong in one arm: it
named §8.4.3.11.4 GR2 (the data-address-identifier rule) even when the sender was a plain restricted pointer
ITEM, whose restriction comes from §13.18.60.4 GR23 (*"If type-name-1 is specified, this data item is a
restricted data-pointer"*, `--check` OK). It now follows the sender's actual spelling.

**And that arm is a CELL THIS LANDING OPENED.** A plain restricted pointer could not stand as identifier-6
beside an `ADDRESS OF` receiver while the grammar held two productions split on the sender's spelling —
measured, `SET ADDRESS OF B1 P-PLAIN TO P-RESTRICTED` was `error COBOL0001: unexpected 'P-PLAIN'`. The code
comment claimed leaving it out "would under-reject exactly the shape this landing opened" and nothing pinned
it, which is a green gate that never looked at what changed. `negative/pb450-set-format7-restricted-sender-
second-receiver` pins it at the SECOND receiving operand with the first one conforming, at 2002/2014/2023, and
`SR-14.9.39.3-19` — CONFORMS on ONE positive that exercised only sentence 1's TO NULL arm — is re-verdicted
with the witnesses for all three sentences (`batch-05-sr19-evidence.json`; still CONFORMS, no GAP change).

---

## 2026-09-22 — REGISTRAR #9: the held row and the two leads have owners, so this note is LANDED

`status` open → **landed**, `inventory_rows` → `[]` (the `closes_rows` this note's two halves earned are
unchanged). Nothing about the mechanism changed; what changed is that the three things this note was carrying
FOR SOMEBODY ELSE now have notes of their own, which is the only reason it was still open
(*"The note stays open for ONE ROW ONLY, and that row's sole remaining ground is a DIFFERENT mechanism"*, half
2 above).

- **`SR-14.9.39.3-18` → [[PB956]]** (the OO cell/bridge emission residue: BASED data or an `ADDRESS OF` target
  in a class definition's data divisions refuses the whole compilation, COBOLNET0899). Re-measured on this
  worktree at main `c982251df` — `scratchpad/reg9/probe/oob.cob` still draws it. The row moves to the note that
  owns the mechanism, exactly as this note's handoff paragraph asked. The row's verdict is untouched (PARTIAL),
  and a non-terminal note claims it throughout, so `DefectiveRowCoverageDriftTests` never sees a gap.
- **Finding 1 (`record_verdicts.py` REPLACES a row's evidence) → [[PB953]].** The mechanism was re-read in the
  script here, not inherited: `record_verdicts.py:219-226` assigns every one of `inventory_schema.py:52`'s six
  `ADJUDICATED` fields, so `derivation` and `notes` are erasable the same way the two evidence lists are.
- **Finding 2 (a recovery `PicInfo`'s category read as an analysis) → [[PB954]].** Re-measured:
  `01 W-BAD PIC 9V9V9.` + `ADD W-BAD TO W-N` still prints the correct COBOLNET1934 and then the false
  COBOLNET0844.
- Finding 3 was already owned by [[PB916]] and stays there.
