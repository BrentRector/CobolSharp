---
title: "PB1530 — the EC-EXTERNAL-FILE-MISMATCH ASSIGN consistency check compares the RESOLVED target, while §7 DOC-A.1-72 documents TEXTUAL sameness of the clause: ASSIGN DISK \"xa.dat\" vs ASSIGN \"xa.dat\", PRINTER vs DISK, and a device-name vs the same literal all pass unflagged"
id: PB1530
kind: defect
status: landed
severity: MINOR
area: runtime/io
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB685", "PB673"]
spec_refs: ["12.4.5.3", "A.1"]
inventory_rows: []
closes_rows: []
closes_rows_reason: "a documentation fix: §7 DOC-A.1-72 now states the rule the code implements (operand-text fingerprint), recorded CONFORMS — but the row stays CONFORMS-but-untested, so no inventory row closed; the ASSIGN-arm witness (a >>TURN-enabled EC-EXTERNAL-FILE-MISMATCH golden) is still owed by the golden lane"
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 22:46 UTC by **REGISTRAR-1** (cloud, branch `claude/adj-registrar-1`, kb/Work PB1522 step 2) from the lane-3 adjudication of the last 208 verdict-less inventory rows (wave 1 `claude/adj-cal-1` + `claude/adj-a1-w1-s1..s4`, wave 2 `claude/adj-w2-*`, all pinned at `0caa7d5`). Every citation below was re-run by the registrar through `python scripts/spec/cite.py --check`.

# PB1530 — LANDED

## The rule and the determination

> b) … The implementor shall specify the consistency rules …

`cite.py --check 12.4.5.3 "The implementor shall specify the consistency rules"` → OK §12.4.5.3 1) b). docs/CONFORMANCE.md §7
`DOC-A.1-72` specifies them as TEXTUAL SAMENESS OF THE CLAUSE for the ASSIGN target of an EXTERNAL file.

## Repro (refuter probes `/tmp/adj/refute-probe/c1`, `c2`, `c7`, adjudication batch `adj-a1-w1-s3`; not re-probed by the registrar)

Two programs, one EXTERNAL FD, `>>TURN EC-EXTERNAL-FILE-MISMATCH CHECKING ON` in both:

- `ASSIGN DISK "xa.dat"` vs `ASSIGN "xa.dat"`;
- `ASSIGN PRINTER "xa.dat"` vs `ASSIGN DISK "xa.dat"`;
- `ASSIGN XADAT` vs `ASSIGN "XADAT"`

→ NO condition in all three. Under the documented rule each is a mismatch.

## Code sites

`src/Cobol.Net.Runtime/Control/ExternalTable.cs` `SelectFingerprint` / `ExternalStore.Describe` fingerprint the RESOLVED ASSIGN target
(the field that feeds `FileConnector.Associate`), so a device-name prefix and the device-name-vs-literal distinction are invisible.

## Decide, then make ONE of them true

Either the §7 row changes to "the resolved target" (and says why that satisfies §12.4.5.3 GR1 b), or the fingerprint carries the
written clause. Also: no spec-derived test drives the ASSIGN arm with checking on (`pb673_external_assign_executor` has no
`>>TURN`) — the witness must.

Row: `DOC-A.1-72` (adjudicated **PARTIAL** on `claude/adj-a1-w1-s3`; not recorded — DOC rows wait on PB1535).

Harm flag: `under_rejects: true` — a mismatch the documented rule defines is not raised as EC-EXTERNAL-FILE-MISMATCH.

## Landed — PB1522 step 3 (doc-rows-1 integrator, 2026-09-24 23:55 UTC, branch `claude/adj-doc-rows-1`)

Decided by making the DOCUMENTATION true, not the code: §12.4.5.3 1) b) (`cite.py --check 12.4.5.3` OK) says only *"A consistent specification … The implementor shall specify the consistency rules"*, so comparing a normalized operand is within latitude. The corrected §7 row DOC-A.1-72 states what `OoEmitter.SelectFingerprint` builds: the ASSIGN operand text, device class dropped, a literal by its value, case ignored, with NO host-path mapping (re-probed `/tmp/adj/doc/doc-w1-s3/refute-probe/x4.cob`: `ASSIGN TO XADAT` vs `"xadat.txt"` → EC-EXTERNAL-FILE-MISMATCH although both open `xadat.txt`), and the USING data-name by spelling. `docs/COBOLNET_FILES_DESIGN.md` and the `pb673_external_assign_executor` golden comment no longer say "textual sameness". The row's two old Pinned-by tests were dropped (neither exercises the ASSIGN arm).
