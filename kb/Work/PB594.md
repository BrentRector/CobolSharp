---
title: "PB594 — `EVALUATE … WHEN literal-3 THRU literal-4 IN alphabet-name-1` is REJECTED: §14.9.13.2's range-expression general format carries a trailing `[IN alphabet-name-1]` and §14.9.13.3 SR3 provides for it, but `CobolExpressions.g4#valueRange` is `valueOperand (THRU|THROUGH) valueOperand` with no alphabet tail, so legal source dies at `no viable alternative at input 'IN'` at every edition"
id: PB594
kind: defect
status: landed
severity: MAJOR
area: frontend/grammar
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.13.2", "14.9.13.3", "8.8.4.2.7", "8.8.4.2.9"]
inventory_rows: []
closes_rows: []
closes_rows_reason: "this note DISCHARGED on re-measurement — it no longer reproduces. `SR-14.9.13.3-3` was already OK/CONFORMS, closed by the kb/Work PB398 landing 2573338ec; this landing re-verdicted it with the semantic evidence PB594 asked for and closed no row of its own."
tags: [cobolsharp, work, defect]
---

# PB594 — LANDED (DISCHARGED — fixed by PB398, 2573338ec) — the EVALUATE range expression lost its alphabet-name

Reported by the golden-lane round-3 `misc-p1` writer while deriving `SR-14.9.13.3-1`'s golden; the round-3
refuter independently confirmed it by reading both grammar rules ("`valueRange` has no `IN alphabet-name-1`
tail, and no `evaluateWhenItem` alternative can absorb it for a LITERAL operand"). **Re-derived and re-probed by
the lander before this note was filed.**

## The rule

`specs/ISO_COBOL.md` §14.9.13.3 SR3 — checked here:

```
python scripts/spec/cite.py --check 14.9.13.3 "Alphabet-name-1 may be specified only when the literals or
    identifiers specified in the THROUGH phrase are of class alphabetic, alphanumeric, or national"
  -> OK  §14.9.13.3 3)  (Syntax rules)
```

A syntax rule that constrains *when* alphabet-name-1 may be specified presupposes that it may be specified at
all; §14.9.13.2's range-expression general format is
`{identifier-3|literal-3|arithmetic-expression-3} {THROUGH|THRU} {identifier-4|literal-4|arithmetic-expression-4}
[ IN alphabet-name-1 ]`, and the phrase selects the collating sequence the THROUGH comparison uses.

## The repro (this tree, 2026-09-05)

```cobol
       SPECIAL-NAMES.
           ALPHABET ALPHA-1 IS STANDARD-1.
       …
           EVALUATE W-X
               WHEN "A" THRU "F" IN ALPHA-1
                   DISPLAY "IN-RANGE"
               WHEN OTHER
                   DISPLAY "OUT"
           END-EVALUATE.
```
```
probeA.cob(14,34): error COBOL0001: no viable alternative at input 'IN'
probeA.cob(14,34): error COBOL0307: unexpected 'IN'. A period may be missing at the end of the previous sentence.
probeA.cob(15,20): error COBOL0001: unexpected 'DISPLAY'
probeA.cob(16,16): error COBOL0307: unexpected 'WHEN'. A period may be missing at the end of the previous sentence.
probeA.cob(18,12): error COBOL0306: unexpected 'END-EVALUATE'. …
exit 65
```

The cascade is the tell: the parser does not merely lose the phrase, it loses the rest of the EVALUATE.

## The mechanism

`src/Cobol.Net.Frontend/Grammar/Core/CobolExpressions.g4:37`

```antlr
valueRange
    : valueOperand (THRU | THROUGH) valueOperand
    ;
```

There is no optional `IN alphabet-name-1` tail, and no alternative of `evaluateWhenItem` can absorb the phrase
when the operand is a literal. The sibling `valueClauseRange` (line 42) is the VALUE-clause range and is
correctly tail-less — §13.18.63 has no alphabet phrase — so this is a one-rule fix, not a shared one.

## What the fix owes

⚙ **Do not stop at the grammar.** Adding the tail makes the source parse; SR3's *constraint* then has to be
enforced (alphabet-name-1 only when the THROUGH operands are class alphabetic, alphanumeric or national, with
SR3's national sub-case), and the binder has to make the range comparison actually USE the named alphabet's
collating sequence rather than the program collating sequence — otherwise the phrase parses and is silently
ignored, which is the worse defect. §8.8.4.2.7's relation-condition rule is the semantics to reuse — "Comparison is made with respect to the collating sequence of characters specified for the current alphanumeric program collating sequence" (`cite.py --check 8.8.4.2.7` → OK), with the national twin at §8.8.4.2.9 (`--check` → OK); the §8.8.4.1.2 this line carried until landing train 3 does not exist — §8.8.4.1 is General and has no sub-clauses;
`PROGRAM COLLATING SEQUENCE` already has a resolved-alphabet path from `DataBinder.Switches.cs`.

Sweep the sibling shape: any other general format with a trailing `[IN alphabet-name-1]` (the relation condition
and SORT/MERGE `COLLATING SEQUENCE` are the near neighbours) should be checked for the same omission before this
closes.

## Inventory

`SR-14.9.13.3-3` is the row this blocks. Round 3 closed `SR-14.9.13.3-1` (the THRU/THROUGH equivalence) on
`conformance:2023/l1_evaluate_thru_through_equivalent`, which deliberately writes no alphabet phrase; SR3's row
stays open and gets its golden with this fix.

## 2026-09-21 — DISCHARGED (wave 43 group D): it no longer reproduces

**Re-probed on this worktree's own build before anything was touched**, at `--std` 85, 2002, 2014 and 2023.
§"The repro"'s program compiles and runs — `IN-RANGE` — where it was `COBOL0001: no viable alternative at input
'IN'` plus the four-diagnostic cascade that lost the rest of the EVALUATE.

**The landing** is [[PB398]] — `git log -S 'valueOperand (THRU | THROUGH) valueOperand (IN cobolWord)?'` →
`2573338ec` "PB398: EVALUATE's partial expression, and one IN alphabet-name resolver". `CobolExpressions.g4`
line 79 now reads `valueOperand (THRU | THROUGH) valueOperand (IN cobolWord)?`.

**⛔ AND THE HALF THIS NOTE SAID WAS THE REAL BAR IS MET TOO.** §"What the fix owes" warned that a grammar-only
fix would leave "the phrase parses and is silently ignored, which is the worse defect", so the discharge was
measured on the SEMANTICS, not on the parse:

* **SR3's constraint is enforced.** `EVALUATE W-N WHEN 10 THRU 20 IN ALPHA-1` over a numeric operand draws
  `COBOLNET1998: an EVALUATE WHEN THROUGH range: 'IN ALPHA-1' may be specified only when the operands of the
  THROUGH phrase are of class alphabetic, alphanumeric or national (ISO §14.9.13.3 SR3 / §13.18.63.3 SR31)`.
* **The named alphabet actually DECIDES the comparison.** Over
  `ALPHABET ALPHA-2 IS "C" "B" "A" "F" "E" "D"`, the subject `"B"` answers `ALPHA2-OUT` for
  `WHEN "A" THRU "F" IN ALPHA-2` and `NATIVE-IN` for the identical range with no phrase — at every edition. The
  resolver is `DataBinder.Switches.cs#TryResolveRangeAlphabet` and the node is
  `BoundRangeMembership(… Alphabet:)`, which §14.7.8 rule 2 is implemented from; the VALUE clause's identical
  phrase reaches the same code, which is the one resolver §14.7.8's opening sentence asks for.
* The sibling sweep §"What the fix owes" asked for is satisfied by the shape PB398 chose: the alphabet phrase
  resolves through ONE carrier registration shared by the EVALUATE and VALUE clauses.

**Rows:** none. `SR-14.9.13.3-3` was already OK/CONFORMS on [[PB398]]'s five witnesses
(`conformance:2002/pb398_range_in_alphabet`, `…/negative/pb398-range-alphabet-numeric-operands`,
`…/negative/pb398-range-alphabet-class-mismatch`, `…/negative/pb398-range-alphabet-undeclared`,
`unit:PartialExpressionSpineDriftTests.ValueRange_CarriesTheInAlphabetPhrase`); batch
`batch-03-pb563-and-pb594.json` only records the re-measurement above in its notes.

⛔ **AND BATCH 3 SUBTRACTED EVIDENCE WHILE MEANING TO ADD IT — repaired by
`batch-04-evidence-union-repair.json`.** `record_verdicts.py` REPLACES a row's `code-location` and `test-ref`
rather than merging them, so batch 3's two-item `test-ref` overwrote all five of PB398's and its one-item
`code-location` overwrote all seven — and its `negative/pb398-range-alphabet-numeric` was a misspelling of
`negative/pb398-range-alphabet-numeric-operands`, a reference to a case that does not exist. A row that is
CONFORMS on a dangling witness is exactly the shape the evidence invariant forbids. The whole inventory was
swept: that was the ONLY dangling `conformance:` reference in all 3,830 of them, so this is this group's own
defect and not an inherited pattern. Batch 4 restores the union. ⚠ `record_verdicts.py` leaves reference
RESOLUTION to `SpecTraceabilityInventoryDriftTests` by design and says so in its header, and that test has an
`UnresolvedTestRefs` arm — it would have caught this the moment the Unit leg ran, which the predecessor's
killed gate never did. The uncaught half is that the recorder REPLACES evidence instead of merging it, and no
gate can see a batch that SUBTRACTS witnesses; that is the lead ([[PB450]] §"the resumed landing's own
findings").
