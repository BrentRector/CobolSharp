---
title: "PB1537 — §12.3.7.3 SR11's screen `TryClauseTextLiteral` refuses EVERY figurative constant for ORDER TABLE literal-9 and LOCALE literal-4 (`ORDER TABLE T1 IS SPACE` → COBOLNET0898), where SR11 bans only a SYMBOLIC-CHARACTER figurative constant and §8.3.3.6.3 SR1 admits the rest"
id: PB1537
kind: defect
status: open
severity: MINOR
area: binding
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB226", "PB1544"]
spec_refs: ["12.3.7.3", "8.3.3.6.3"]
inventory_rows: ["DOC-A.1-42", "DOC-A.1-181"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 23:55 UTC by the **doc-rows-1 INTEGRATOR** (cloud, branch `claude/adj-doc-rows-1`, kb/Work PB1522 step 3) from the §7 determination writers and refuters (`adjudication/doc-rows-1/`). Every citation below was re-run through `python scripts/spec/cite.py --check`, and the repro was re-probed on the Debug build of this branch.

# PB1537 — OPEN

## The rule

> 11) Literal-1, literal-2, literal-3, literal-4, literal-5, literal-6, and literal-9 shall specify neither a symbolic-character figurative constant nor a zero-length literal.

`cite.py --check 12.3.7.3 "Literal-1, literal-2, literal-3, literal-4, literal-5, literal-6, and literal-9 shall specify neither a symbolic-character figurative constant nor a zero-length literal"` → OK §12.3.7.3 11).

> 1) A figurative constant may be used whenever 'literal' appears in a format or when a rule allows it, with the following restrictions: …

`cite.py --check 8.3.3.6.3 "A figurative constant may be used whenever 'literal' appears in a format or when a rule allows it"` → OK §8.3.3.6.3 1).

## Repro (`/tmp/adj/doc/doc-w1-s2/refute-probe/r42f.cob`, `/tmp/adj/doc/doc-w1-s6/refute-probe/p181/b.cob`; re-probed, `--std 2023`)

```cobol
       SPECIAL-NAMES.
           ORDER TABLE T1 IS SPACE.
```

Observed: `error COBOLNET0898: ORDER TABLE T1 IS SPACE: literal-9 shall specify neither a symbolic-character figurative constant nor a
zero-length literal (ISO §12.3.7.3 SR11)` — the message quotes the rule that does NOT forbid SPACE. `LOCALE LA IS SPACE` is refused
the same way. Expected: accepted (SPACE is a legal literal-9 / literal-4; it then names an ordering table / locale that is not
available, which is item 42's / item 181's run-time answer — COBOLNET1662 advice and EC-ORDER-NOT-SUPPORTED / EC-LOCALE-MISSING).

## Mechanism and code site

`DataBinder.TryClauseTextLiteral` (the one SR11 screen, `src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs`, wired by landed
kb/Work PB226 to all seven literals) classifies ANY figurative constant as the symbolic-character kind. Fix: refuse only a
symbolic-character figurative constant (and a zero-length literal); every other figurative constant is a literal of its class.
§7 rows DOC-A.1-42 and DOC-A.1-181 state the SR11 restriction and flag this over-rejection; they are recorded PARTIAL on it.

## Sibling sweep

All seven SR11 literals share the helper (ALPHABET literal-1/-2/-3, CLASS literal-5/-6): after the fix, `ALPHABET A IS SPACE` and
`CLASS C IS SPACE` must follow their own rules. Also PB1544 (a symbolic character refused as an intrinsic argument — another
place a figurative constant stands for a literal).
