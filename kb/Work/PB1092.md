---
title: "PB1092 — two Annex A.1 determinations the compiler MAKES are documented nowhere: item 43 (currency symbol equivalence of non-COBOL characters — `SwitchBindCurrency` folds with `char.ToUpperInvariant`, so é ≡ É) and item 186 (the ordinal scheme of a literal-phrase coded character set — `CodedCharacterSet.CharAt`'s position+1, which its own summary says is 'documented in CONFORMANCE.md'); CONFORMANCE.md §2 row 27 further misattributes a CODE-SET obligation to GR7 k4"
id: PB1092
kind: defect
status: open
severity: MINOR
area: docs/conformance
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
cluster: ["PB1089", "PB1095"]
spec_refs: ["12.3.7.3", "12.3.7.4"]
inventory_rows: ["SR-12.3.7.3-20", "GR-12.3.7.4-L2.4", "DOC-A.1-8", "DOC-A.1-43", "DOC-A.1-186"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 04:03 PDT by **REGISTRAR 14a** from adjudication batch d1-14a (pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probes under `scratchpad\adj-d1\out\probe\`). Not fixed by trains 58/59 (their src diff was checked).

# PB1092 — OPEN — two Annex A.1 determinations the compiler MAKES are documented nowhere: item 43 (currency symbol equivalence of non-COBOL characters — `SwitchBindCurrency` fold

## Rule text (every citation re-run by the registrar)
- `python scripts/spec/cite.py --check 12.3.7.4 "If a character code set is being specified, the implementor defines the ordinal number within the character code set being specified"` → `OK  §12.3.7.4 7) 2.  (General rules)`

### Adjudicator finding (special-names-paragraph-p1, harm silent)
- **Mechanism:** Annex A.1 item 43 (currency symbol - equivalence of non-COBOL characters, required + documented) has no DOC-A.1-43 row in docs/CONFORMANCE.md although the compiler makes a definite choice (char.ToUpperInvariant, so é and É are equivalent currency symbols)
- **Repro:** SPECIAL-NAMES. CURRENCY SIGN IS "EUR" PICTURE SYMBOL "é".  01 E PIC É9.99.  MOVE 1.5 TO E  DISPLAY E  (--std 2023) -> runs, E=[EUR1.50]
- **Expected (spec):** §12.3.7.3 SR20 first sentence: for a currency symbol outside the COBOL character repertoire 'any equivalence between that character and any other character in the computer's compile-time coded character set is defined by the implementor'; Annex A.1 item 43 makes it required and documented.
- **Observed:** The equivalence exists (Unicode invariant upper-casing) but is documented nowhere; the conforming half (COBOL letters fold per §8.1.3.2 GR3 b, national = alphanumeric) is pinned by conformance:2023/l1_picture_currency_equivalent. Note A.1 item 43 itself cites 'Syntax rule 21' - the standard's own mis-reference; the rule is SR20.
- **Code site:** src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs#SwitchBindCurrency (char.ToUpperInvariant key); docs/CONFORMANCE.md A.1 table
- **Clause (adjudicator):** §12.3.7.3 SR20; Annex A.1 item 43

## The DOC-A.1-186 measurement (row GR-12.3.7.4-L2.4, batch 14a)
Rule = GR7 k)4 (cite.py --check 12.3.7.4 "If a character code set is being specified, the implementor defines the ordinal number within the character code set being specified" -> OK): an implementor obligation to DEFINE the ordinals of the unspecified characters, and Annex A.1 item 186 requires it documented ("The implementor shall document the scheme used for assigning the ordinal number" - 12.3.7 GR7k4). DEFINED in code: CodedCharacterSet.CharAt makes a literal-phrase set's ordinal = collating position + 1, so the unspecified characters follow in native relative order exactly as k3 places them (65,536 ordinals). MEASURED (probe/snp3/k4.cob, same AL as g9also): SYMBOLIC CHARACTERS S73 S74 S56 IS 73 74 56 IN AL -> 'D', 'E', '0' (agreeing with ORD under that sequence); CLASS CD IS 73 THRU 75 IN AL -> "DEF" in class, "DEG" not. NOT DOCUMENTED: docs/CONFORMANCE.md has no DOC-A.1-186 row although the CodedCharacterSet summary says 'documented in CONFORMANCE.md', and CONFORMANCE.md §2 row 27 states the opposite ('a literal-phrase alphabet's remapped ordinals (GR7 k4 leaves that correspondence to the implementor and this one does not define it)') - that sentence is about the on-medium CODE-SET correspondence, a different obligation, but it misattributes it to k4. Definition half verified, documentation half absent -> PARTIAL. The documentation debt is owned by open note A11 (Annex A.1 register); the k4 misattribution in row 27 is noted for that note.

## What the fixer owes
Write DOC-A.1-43 and DOC-A.1-186 rows (the `DOC-A.1-<n>` key, audited by `audit_annex_a1.py --check`, A11) stating the determinations the code already makes; correct row 27's k4 attribution. A.1 item 43 itself cites 'Syntax rule 21' — the standard's own mis-reference; the rule is SR20. (A11 is the register; this note carries the two rows so the DefectiveRowCoverage gate sees an owner.)

Rows claimed: SR-12.3.7.3-20, GR-12.3.7.4-L2.4. Flip `status` and write `closes_rows` in the landing commit (CLAUDE.md rule 8).

## More A.1 items (lock mode, native ordinals)
**More undocumented A.1 determinations measured by batch 14a** (no rows claimed here; the DOC rows are A11's):
Annex A.1 items 153 (record-locking default mode, §12.4.5.9.4 GR1), 154 and 155 (run-unit / file-connector lock
maxima, GR7) — lock-mode-clause adjudicator: no DOC-A.1-153/154/155 rows; item 8 (GR6, native ordinals) — no
DOC-A.1-8 row. Write them with 43 and 186.

## Registrar-1 (kb/Work PB1522) — rows adjudicated on the `claude/adj-*` branches

Filed 2026-09-24 by REGISTRAR-1 (cloud, branch `claude/adj-registrar-1`). The adjudicators (tree `0caa7d5`) attributed these rows to this note; this note now claims them in `inventory_rows`.

- `DOC-A.1-8` — adjudicated **DIVERGES** on `claude/adj-a1-w1-s1`; NOT recorded — DOC rows wait on owner decision PB1535.
- `DOC-A.1-43` — adjudicated **DIVERGES** on `claude/adj-a1-w1-s2`; NOT recorded — DOC rows wait on owner decision PB1535.
- `DOC-A.1-186` — adjudicated **DIVERGES** on `claude/adj-w2-a1-s3`; NOT recorded — DOC rows wait on owner decision PB1535.

Adjudicator finding (`adj-a1-w1-s2`, harm silent): a1-register-rows-missing-for-determinations-the-code-makes

- Repro: grep '^| DOC-A.1-(32|40|42|43|44|46|51|52) |' docs/CONFORMANCE.md -> none; python scripts/spec/audit_annex_a1.py lists them undischarged
- Expected: Each item: 'This item is required/conditionally required ... shall be documented in the implementor's user documentation' (§A.1, cite.py OK per row)
- Observed: The code fixes each answer (FillChar values; FindCopybook search order; ORDER TABLE literal-9 set - documented but under §2 row 25, not the §7 key; ToUpperInvariant currency equivalence; empty extra-prohibition set; per-usage representations with 47/74/81/213/217 unregistered; DELETE FILE = one host unlink; every host path treated as concurrently accessible).
- Code site: docs/CONFORMANCE.md §7

Adjudicator finding (`adj-w2-a1-s3`, harm silent): A fixed implementor determination on a REQUIRED + documented A.1 item has no docs/CONFORMANCE.md §7 row (and no prose answer, or a contradicting one)

- Repro: 186: ALPHABET AL IS "Z" "Y" "X"; SYMBOLIC CHARACTERS S4 IS 4 IN AL -> U+0000. 203: PCS=EBCDIC, 88 VALUE "Z" THRU "a" excludes '_' (native includes it). 217: BYTE-LENGTH(PROGRAM-POINTER)=8, non-COBOL target -> NULL. 218: USE AFTER ERROR on status 35 continues; with >>TURN EC-I-O CHECKING ON the run unit terminates.
- Expected: §12.3.7.4 GR7 k4 / §14.7.8 r2 / §13.18.60.4 GR24 / §14.9.49.4 GR7c+GR12c: the implementor defines AND (A.1 186/203/217/218) documents the choice
- Observed: Each behaviour is deterministic in code but no §7 row states it; 186 is contradicted by §2 row 27; 218 is additionally bifurcated on checking state, which the rule does not key on
- Code site: CollatingModel.cs#CodedCharacterSet.CharAt; CollatingModel.cs#CollatingSelection.ForComparison; Runtime/Control/ProgramPointer.cs; EcEmitter.cs#EmitIoCheckEc vs DispatchEmitter.cs#EmitUseMachinery

Adjudicator finding (`adj-a1-w1-s1`, harm silent): Annex A.1 determinations that the code already fixes are written nowhere under their DOC-A.1-<n> key (or only incidentally, off-key)

- Repro: grep -n 'DOC-A.1-<n>' docs/CONFORMANCE.md returns nothing for each item. Probes: ord.cob (group BYTE-LENGTH 3 = no filler, so the group is coincident with its first item; ORD("A")=66 = code point + 1); hx2.cob (X"414" -> COBOLNET1635, so 2 digits per character); call17.cob (a failed CALL has no other effect: RETURNING and USING items unchanged); chr.cob (CHAR on a locale tie returns the lowest-coded member); rep.cob ('§' accepted in a REPLACE text-word); enc.cob (per-file BOM, UTF-8 default, U+FFFD substitution, UTF-16 copybook in a UTF-8 group).
- Expected: Each item: 'This item is required [or provided]. This item shall be documented in the implementor's user documentation.'
- Observed: The behavior is deterministic and the code fixes it, but no determination row exists. Item 9's only statement cites §8.3.3.2.3 SR5 instead of SR6. Item 16's substance sits in §3 prose and in DOC-A.1-19 and does not name the EC-PROGRAM-NOT-FOUND outcome. The DOC-A.1-22 row documents the CHAR-NATIONAL twin of item 21 but not CHAR.
- Code site: docs/CONFORMANCE.md §7

Each row's full evidence (rule text with its `cite.py --check` line, probes, determination) is the record's `notes` in `adjudication/registrar-1/batch-{doc,nondoc}.json` on the registrar branch.

## PB1522 step 3 (doc-rows-1 integrator, 2026-09-24 23:55 UTC, branch `claude/adj-doc-rows-1`)

Rows **43** (currency-symbol equivalence) and **186** (literal-phrase ordinal scheme) are written in `docs/CONFORMANCE.md` §7 and recorded CONFORMS (test-needed); DOC-A.1-8 likewise. §2 row 27 no longer attributes a CODE-SET obligation to GR7 k4 — it points at the item-186 row. What remains here: re-verdict SR-12.3.7.3-20 and GR-12.3.7.4-L2.4 (PARTIAL on the missing determinations, which now exist) through a batch, then land this note.
