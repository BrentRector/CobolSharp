---
title: "PB1234 — a floating-point CONTROL operand CRASHES at program activation: ReportWriterEmitter emits NotImplemented `CONTROL operand … is a floating-point item, which has no prior-control RESTORE channel` because CallEmitter.CallStringWrite has no float arm"
id: PB1234
kind: defect
status: open
severity: MAJOR
area: codegen
wrong_answer: false
crashes: true
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: []
spec_refs: ["13.18.16.4"]
inventory_rows: ["GR-13.18.16.4-4"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14d** from adjudication batch d1-14d (the adjudicators' pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probes under `scratchpad\adj-d1\out13\probe\`). The given repro was re-run once on registrar 14d's Debug build of `071ff04ea` (main after trains 58/59) and reproduces unless stated otherwise.

# PB1234 — OPEN

## Measured

**control-clause** — A floating-point CONTROL operand (FLOAT-LONG/FLOAT-SHORT/any Pic.IsFloat) has no prior-control restore channel: ReportWriterEmitter emits a runtime NotImplemented loud instead of the get/set delegate pair, because CallEmitter.CallStringWrite has no float arm.

- **Repro:** probe out13/probe/control-clause/flt.cob: `01 CN USAGE FLOAT-LONG VALUE 1.` with `RD R-1 CONTROLS ARE FINAL CX CN` - compiles, then dies at program activation with NotImplementedCobolFeatureException `CONTROL operand 'CN' is a floating-point item, which has no prior-control RESTORE channel` at --std 2002/2014/2023.
- **Expected (spec):** No syntax rule of §13.18.16.3 excludes a floating-point data-name-1, so the program is conforming; §13.18.16.4 GR3 defines a prior control 'having the same data description as the corresponding data item' (cite OK 3)) and GR4a stores the priors into the control items before CF printing and restores the new current values afterward (cite OK 4) a)) - the report must break on CN changes with the CF showing the prior float value.
- **Observed:** Runtime crash at activation before any statement runs; the limb is labelled an implementation limit in kb/Work PB177 (landed) but no open note owns it. Rejecting legal source at run time is debt only an owner decision may accept.
- **Code site:** src/Cobol.Net.Compiler/CodeGen/Verbs/ReportWriterEmitter.cs (CONTROL loop, `place.Item.Pic is { IsFloat: true }` limb ~line 390); src/Cobol.Net.Compiler/CodeGen/Verbs/CallEmitter.cs#CallStringWrite (no float arm)
- **Clause (adjudicator):** 13.18.16.4

## The rules — every citation re-run through `cite.py --check` by registrar 14d

- `GR-13.18.16.4-4`: `cite.py --check 13.18.16.4 "If a control break has been detected during the execution of a GENERATE statement, the"` → OK  §13.18.16.4 4)  (General rules)

## Rows claimed

- `GR-13.18.16.4-4` — batch 14d verdict PARTIAL

## What the fixer owes (sibling sweep)

Every other consumer of the prior-value get/set delegate pair (SUM, SOURCE restore) for float items; a compile clean + runtime NotImplemented is the worst shape — at minimum it must not reach run time.
