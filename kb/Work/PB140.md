---
title: "PB140 — ✅ LANDED 2026-08-29 (the Status-setter chokepoint; loud Require/MisroutedVerb; the 1692/1693 bind screens; CLOSE failure boundary at the base; openness split from the FPI; GetAttributes probe + GR17 medium refusal). WAS: leaks at five seams: PrevOpWasSuccessfulRead survives UNLOCK, a failed OPEN, CLOSE-WITH-LOCK and the shared-open 61 arm (DELETE/REWRITE after them answer 00 where §9.1.13.7 requires 43); FileRegistry.Status fail-opens to '00' for an unregistered connector while the verb branches on the runtime local (one statement, two channels, opposite answers); FileConnector.Close maps no exceptions (an OS-level close failure aborts the run unit instead of '30'); CloseWithLock poisons after an unsuccessful close; OptionalAbsent doubles as the open-mode bit forcing CloseCore to change the file position indicator GR6 says is unchanged"
id: PB140
kind: defect
status: landed
severity: MAJOR
area: runtime
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB140

Rows: GR-14.9.10.4-2 (DIVERGES), GR-14.9.10.4-10/-16/-17, GR-14.9.6.4-1/-4/-6/-8, SR-14.9.10.3-3, SR-14.9.6.3-1, findings [delete-statement-p1/p2]/[close-statement].

One chokepoint fix serves most of it: route every I-O status assignment through one connector entry that clears the read-gate pair (the refuter enumerated FOUR leak sites; the REWRITE sweep-claim on file_sharing_seq was REFUTED — RewriteShared returns 51 before the gate, so that golden stands). The fail-open Status('00') + the SD-file bindability (EmitFileRegistration skips IsSortMerge while FilesByName holds SD files; BindClose and BindDelete/BindDeleteFile never call UnsupportedOrg — the ONE missing call closes CLOSE-of-SD, DELETE-of-SD and SR1's organization restriction together) make an unregistered miss report SUCCESS to the FILE STATUS item. FileConnector.Close gets the Open twin's exception mapping ('30'); Sequential CloseCore's unguarded Dispose is the one arm of a three-arm pattern (Relative/Indexed catch). CloseWithLock locks unconditionally even after '42'. OptionalAbsent must split from the open-mode predicate (one bit, two jobs — why CLOSE UNIT on an absent optional file answers 07 and why CloseCore mutates the FPI). Also DeleteRecord's fall-throughs return '30' without SetStatus, and GR16/GR17's '37' legs (unstattable/write-protected media map to IOException → '30'; File.Exists swallows access errors → '05').

*(Found by Phase-B batch 8 — §8.8.1 + §14.9.1–14.9.12, 2026-08-29; one adjudicator + one independent refuter per subject. Full forensic evidence: the named rows' `notes` in `tests/version-matrix/traceability-inventory.json`; the raw finding lines in the session scratchpad `pb-batch8/findings.txt`.)*
