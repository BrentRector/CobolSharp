---
title: "PB1544 — a SYMBOLIC-CHARACTER figurative constant is refused as an intrinsic-function argument (`FUNCTION ORD(SA)` → COBOLNET1639 'SA is not defined') although §8.3.3.6.3 SR1 admits a figurative constant wherever 'literal' appears and §8.4.3.2.3 SR8 makes argument-1 an identifier or a literal; SPACE and QUOTE are accepted in the same position"
id: PB1544
kind: defect
status: open
severity: MINOR
area: binding
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1537"]
spec_refs: ["8.3.3.6.3", "8.4.3.2.3"]
inventory_rows: ["SR-8.3.3.6.3-1"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 23:55 UTC by the **doc-rows-1 INTEGRATOR** (cloud, branch `claude/adj-doc-rows-1`, kb/Work PB1522 step 3) from the §7 determination writers and refuters (`adjudication/doc-rows-1/`). Every citation below was re-run through `python scripts/spec/cite.py --check`, and the repro was re-probed on the Debug build of this branch.

# PB1544 — OPEN

## The rules

`cite.py --check 8.3.3.6.3 "A figurative constant may be used whenever 'literal' appears in a format or when a rule allows it"` → OK §8.3.3.6.3 1).
`cite.py --check 8.4.3.2.3 "Argument-1 shall be an identifier, a literal, a boolean expression, or an arithmetic expression"` → OK §8.4.3.2.3 8).

## Repro (scratch `fc1.cob`, and `/tmp/adj/doc/doc-w1-s6/probe/p182/sy6.cob`; `--std 2023`)

```cobol
       SPECIAL-NAMES.
           SYMBOLIC CHARACTERS SA IS 66.
       PROCEDURE DIVISION.
           DISPLAY FUNCTION ORD(SPACE)
           DISPLAY FUNCTION ORD(QUOTE)
           DISPLAY FUNCTION ORD(SA)
```

Observed: `error COBOLNET1639: 'SA' is not defined` at the third line (the first two compile). Expected: compiles; `ORD(SA)` is 66.

## Mechanism

The intrinsic-argument binder resolves a bare word as a data-name and never consults the SPECIAL-NAMES symbolic-character table,
which the other literal positions (MOVE, DISPLAY, VALUE) do. The fix is ONE literal-operand resolver shared by every literal
position (the figurative-constant table plus the symbolic characters), so the next literal position gets both kinds automatically.

## Sibling sweep

Every operand position the grammar spells `literal` but binds through a data-name path: user-defined function arguments (kb/Work
PB1418 owns that binder), CALL … BY CONTENT literal, INSPECT / STRING / UNSTRING literals, EVALUATE WHEN.
