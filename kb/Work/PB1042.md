---
title: "PB1042 — a dynamic-capacity table, or a dynamic-length item inside a table element, in an EXTERNAL record (or an ADDRESS-OF/BASED record) is refused COBOLNET0899 `cannot be cell-backed`, although §8.5.1.9.1 3) bars such tables only from the FILE section"
id: PB1042
kind: defect
status: open
severity: MINOR
area: binder/data
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1026", "PB1056"]
spec_refs: ["8.5.1.9.1", "13.18.22.4"]
inventory_rows: ["SR-8.5.1.9.1-3", "GR-8.4.3.11.4-1"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-23 by **REGISTRAR #13** from `leads-w53-w57.md` and the train-57 lander report (w57c lead `wf_3ea96171-c58-3`). Re-probed on registrar #13's own Debug build of `c54434a8d` (BelowNormal).

# PB1042 — OPEN

## Measured
```cobol
       IDENTIFICATION DIVISION.
       PROGRAM-ID. B16ED.
       DATA DIVISION.
       WORKING-STORAGE SECTION.
       01 XR EXTERNAL.
          05 T PIC X(2) OCCURS DYNAMIC CAPACITY IN XC.
          05 XC PIC 9(4) COMP.
       PROCEDURE DIVISION.
           DISPLAY "RAN"
           STOP RUN.
```

→ `error COBOLNET0899: EXTERNAL record 'XR' cannot be cell-backed — EXTERNAL record 'XR' has a dynamic-capacity table
('T', ISO §8.5.1.9) with no fixed extent — the shared byte cell cannot carry it — recognized but not yet implemented`.
(The probe's COBOLNET1523 is the probe's own fault — `XC` is also declared — and is not part of this defect.)

## The rule
`cite.py --check 8.5.1.9.1 "it may be defined in any place, other than the file section, in which a fixed-capacity table may be defined"` → `OK §8.5.1.9.1 3)`.

## Code site
`DataBinder.VariableLengthCellResidueOf` (the refusal); the root is the struct composer's A.1 item 57 exclusion, not
PB1026's (PB1026 carried out-of-line RECORDS of an EXTERNAL FD in the cell's dyn slots).

## What the fixer owes
Give the run-unit EXTERNAL cell a slot per dynamic table (or per occurrence of a dynamic-length item in a table
element), the PB1026 dyn-slot mechanism generalized from records to members. Same for the BASED/ADDRESS-OF record
arm. Golden: two programs sharing the EXTERNAL record, one growing the table, the other reading it.

## Extended by REGISTRAR 15b (batch 15b, pin @`071ff04ea`) — rows `GR-8.4.3.11.4-1`, `SR-8.5.1.9.1-3`

batch 15b: ADDRESS OF a group CONTAINING a dynamic-capacity table and an EXTERNAL record with an OCCURS DYNAMIC subordinate are both still refused (COBOLNET0869 / COBOLNET0899)

**GR-8.4.3.11.4-1**

> Data-address-identifier creates a unique data item of class pointer and category data-pointer that contains …

`python scripts/spec/cite.py --check 8.4.3.11.4 "Data-address-identifier creates a unique data item of class pointer and category data-pointer that contains"` → `OK  §8.4.3.11.4 1)  (General rules)`

**SR-8.5.1.9.1-3**

> it may be defined in any place, other than the file section, in which …

`python scripts/spec/cite.py --check 8.5.1.9.1 "it may be defined in any place, other than the file section, in which"` → `OK  §8.5.1.9.1 3)  (General)`

#### `GR-8.4.3.11.4-1` — PARTIAL (adjudicator + refuter record, verbatim)

GR1 "creates a unique data item of class pointer and category data-pointer that contains the address of identifier-1" (cite.py --check 8.4.3.11.4 -> OK 1)). Probe p1.cob: ADDRESS OF a WS record, of a subscripted occurrence W-T(2), of an elementary item at offset 0 (P = P2 -> SAME), of a LOCAL-STORAGE item, relation `IF ADDRESS OF W-REC = P`, and `SET ADDRESS OF B TO ADDRESS OF W-REC` then a MOVE into W-A visible through B (QBCD) - all correct; F1.cob FILE SECTION record correct; RS.cob national group, national leaf, ODO group and ODO element all accepted and run. PARTIAL because a legal operand is still refused by a named storage residue: `01 DT. 05 DX OCCURS DYNAMIC. 10 DY PIC X(2).` + `SET P TO ADDRESS OF DT` -> COBOLNET0869 'could not be placed on addressable cell storage (... dynamic-capacity table ... no fixed extent)'; SR6 forbids only an ELEMENT of / an item SUBORDINATE TO a dynamic-capacity table, not the group containing it. That rejection is owned by open kb/Work PB1042 (rejects_legal_source) - not re-reported here.

#### `SR-8.5.1.9.1-3` — DIVERGES (adjudicator + refuter record, verbatim)

Rule (--check OK 8.5.1.9.1 3)): 'it may be defined in any place, other than the file section, in which a fixed-capacity table may be defined and may be nested in any combination to the same number of levels as a fixed-capacity table' + non-contiguity/allocation implementor-defined + 'If neither is specified, the current capacity is initialized to zero' + TO = expected capacity exceedable with a nonfatal exception + maximum capacity = implementor/resource limit. VERIFIED: FILE SECTION rejected (conformance-test:OccursDynamicGuardTests.FileSectionDynamicTable_Rejected1526); WORKING-STORAGE and LOCAL-STORAGE accepted and run (probe p3); nesting dynamic-in-fixed, dynamic-in-dynamic, fixed-in-dynamic and 7 levels of dynamic all run (p3, p4); initial capacity: none -> 0, FROM 3 -> 3, Format 2 VALUE of 2 -> 2 (p3 C0=0 C1=3 C2=2 Z2=AB); TO exceedable (dyn_capacity_bounds). DIVERGES (rejects-legal-source): probe p4 original - 01 WX EXTERNAL with an OCCURS DYNAMIC subordinate -> COBOLNET0899 'cannot be cell-backed ... recognized but not yet implemented', though EXTERNAL WORKING-STORAGE is a place a fixed-capacity table may be defined. This half is OWNED by the open register note PB1042 (inventory_rows SR-8.5.1.9.1-3, EXTERNAL / ADDRESS-OF / BASED records) - not re-reported as a new finding. The maximum-capacity (resource) sentence's runtime defect is recorded under GR-8.5.1.9.6-2. Row stays open until PB1042 lands; no single spec-derived test closes all halves (test-needed).
