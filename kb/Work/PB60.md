---
title: "PB60 — OPEN (NEW 2026-08-09) — NUMVAL/NUMVAL-C/NUMVAL-F value paths parse by remove-then-scan, erasing the format rules they must enforce"
id: PB60
kind: defect
status: open
severity: MAJOR
area: runtime/intrinsics
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB60 — OPEN — NUMVAL/NUMVAL-C/NUMVAL-F value paths parse by remove-then-scan, erasing the format rules they must enforce

> ⚙ **flags:** wrong-answer, silent · **13 inventory rows** (triage 2026-08-09, probe+refute confirmed; the
> per-row mechanisms live in the triage record and each row's inventory notes)

> **The shape: `Replace`/`Trim` then scan.** `Numval` opens with `text.Trim()` plus a TrimEnd (SS15.67.3
> r2 permits embedded spaces ONLY before the first digit — the trim erases the distinction); `NumvalF`
> opens with `text.Replace(" ", "")` (SS15.69.3 r5 forbids spaces inside the significand — erased);
> `NumvalC` removes the currency string with an unanchored, uncounted `Replace` BEFORE format validation,
> so a currency string embedded mid-number, or occurring twice, values as if legal, and the ordering
> (currency-Replace before space handling) inverts the rules' composition. The currency environment is a
> SCALAR pair (`CurrencyString`/`CurrencyPicSymbol`) where SS12.3.7 admits a SET of CURRENCY SIGN
> clauses, and `DecimalPointIsComma` scoping sits at the wrong binder layer (a SCOPING defect outside
> SS15.67). `BindNumvalCFamily` still keyword-sweeps argument positions blind.
> **The fix**: a real format-rule parser per function (the SS15.67.3/68.3/69.3 grammars are small),
> scanning WITHOUT pre-normalization, the DPC/currency environment passed in — goldens per format branch.
> (The RENDERER routing rows — NumvalF's missing StandardDecimal branch, the working-scale quantization —
> are PB56's; the parser rows are this note's.)
>
> ✅ **THE ONE-SCAN UNIFICATION LANDED 2026-08-09 (6 of 13 rows → CONFORMS: AR-15.67.3-2, RV-15.67.4-2,
> AR-15.69.3-5, AR-15.68.3-4, RV-15.68.4-2, RV-15.68.4-3):** each family's TEST- validator and value
> function are now projections of ONE positional scan (`NvScan` for NUMVAL/NUMVAL-C, `NvfScan` for
> NUMVAL-F — validate positionally, accumulate in the same pass), retiring the remove-then-scan shape
> whole. Measured flips: TAB-led argument EC'd (r2's ignorable char is SPACE only) · "-12-" EC'd (the
> formats are alternatives, never a toggle) · "1 2" EC'd under NUMVAL-F (r5's except-clause) · the
> currency consumed at its ONE r4a position, so `NUMVAL-C("R123.45CR","R")` = **-123.45** where the old
> unanchored Replace ate the R of CR and valued a validator-certified string as 0. Sibling landed:
> `TestNumvalF` gains the mode-dependent digitCap its two siblings had (the emitter passes DigitCapFlag).
> Golden `pb60_numval_one_scan` (14 rows, both projections witnessed). The probe fleet's full per-row
> evidence: the 2026-08-09 wf_fdd1492c journal.
> ✅ **THE CHANNEL THREADING LANDED 2026-08-09 (RV-15.68.4-1 → CONFORMS — 7 of 13):** the MOVE sender
> renders under the RECEIVER's scale (`MoveEmitter.SenderContext` — Receiverless stays true, so the
> float family's binary64 sending is untouched), each relation side renders knowing the OTHER side's
> static scale (`ConditionRenderer.StaticScaleOf`), and the receiver-less DISPLAY floor is the
> DOCUMENTED §15.4.1 native determination (CONFORMANCE.md item 92's NUMVAL paragraph). One value in
> every channel — MOVE stored 0.123456000 where COMPUTE gave 0.123456789 before; the golden's CM/MV/EQ
> legs pin the agreement.
> ✅ **THE BINDER WALK + SCREENS LANDED 2026-08-09 (FMT-15.68.2, AR-15.68.3-1, AR-15.68.3-2 → CONFORMS
> — 10 of 13):** BindNumvalCFamily walks the format by position (the CONVERT-5a template; ANYCASE once,
> last), r1's CATEGORY screen rejects the edited categories the class row admits, and r2's content
> halves screen literal argument-2 at bind with the runtime `InvalidCurrency` twin in both NUMVAL-C
> (EC + default 0) and TEST-NUMVAL-C (EC + the r1c LENGTH+1 leg — 0 would falsely certify). Three
> negatives pb60-numvalc-*.
> ✅ **THE SDIDI DEC BRANCH LANDED 2026-08-17 (RV-15.67.4-1(a) → CONFORMS — 11 of 13; and RV-15.69.4-3,
> the NUMVAL-F twin PB56 reassigned here, → CONFORMS):** `CobolIntrinsics.NumvalDec / NumvalCDec / NumvalFDec`
> project the ONE scan to `CobolDec` exactly at the parsed scale (NUMVAL-F's E-exponent through the new
> `CobolDec.FromParsed` — the §8.8.1.5.2 r2 range check), dispatched by `IntrinsicRenderer.RenderDec`'s NUMVAL
> arm, routed unconditionally under the standard modes; the reject projections extracted (`NumvalReject` /
> `NumvalCReject` / `NumvalFReject` / `NumvalCInvalidCurrency`) so both modes say one thing about a malformed
> argument. Measured: receiver-less `NUMVAL("1.2345678")` renders 1.2345678 (was 1.234567), NUMVAL-C
> 1234.5678901234 (was 1234.567890), NUMVAL-F 0.00123456789012345 (was 0.001234567), and a 34-digit argument
> renders exactly where the native projection printed the Int128 saturation sentinel. Golden
> `pb60_numval_standard_decimal` (19 rows); unit: four NUMVAL tests in `CobolIntrinsicsDecTests` incl. the
> native-agreement theory. Design: `COBOLNET_NUMERIC_DESIGN.md` D3 + CONFORMANCE.md item 92 (native-only).
> Three siblings surfaced while probing and are REGISTERED, not folded in: [[PB74]] (the SDIDI final
> transfer's widening arm masks ON SIZE ERROR), [[PB75]] (an SDIDI range overflow outside an arithmetic
> statement is a raw CLR crash), [[PB76]] (the far-below-precision remainder marker reads as a half-tie).
> **STILL OPEN (3 rows):** AR-15.68.3-3 (the CURRENCY SIGN SET model + multi-character currency strings,
> §12.3.7.3 r23 — a DataBinder.Switches model change) · AR-15.67.3-5 (DECIMAL-POINT IS COMMA scoping — a
> contained program does not inherit the outer SPECIAL-NAMES, §12.3.4-level) · **RV-15.69.4-2 (NATIVE-mode
> NUMVAL-F, reassigned here by PB56 and now listed):** the receiver-less / float-receiver / MOVE-sender
> channels saturate the Int128 carrier at ws 9 — `DISPLAY FUNCTION NUMVAL-F("5E+30")` prints the sentinel
> 170141183460469231731687303715.884105727, `NUMVAL-F("5E+30") = NUMVAL-F("9E+30")` is TRUE, `MOVE … TO PIC 9(5)`
> stores the sentinel's low digits 03715 (the exact value's are 00000), a COMP-2 receiver gets 1.7E+29, and
> `NUMVAL-F("1.5E-12")` into a COMP-2 is 0 — the float family's Receiverless/Real arm (PB13) was never swept
> to this member. §15.69.4 r2 grants native an APPROXIMATION; the fix shape is the float family's convention
> (binary64 in the receiver-less/float-receiver contexts, the exact Int128 parse for a fixed arithmetic
> receiver), recorded on item 92 when it lands.

### The rows this note owns (re-verdict on landing)

> AR-15.67.3-2, AR-15.67.3-5, AR-15.68.3-1, AR-15.68.3-2, AR-15.68.3-3, AR-15.68.3-4
> FMT-15.68.2, RV-15.67.4-1, RV-15.67.4-2, RV-15.68.4-1, RV-15.68.4-2, RV-15.68.4-3
> AR-15.69.3-5, RV-15.69.4-2, RV-15.69.4-3 (the last two reassigned from [[PB56]] at its landing)

> ⚙ **2026-08-09 PROBE SWEEP + THE NIGHT'S THREE LANDINGS:** the sweep measured all 13 rows live (evidence in `docs/rearchitecture/backlog-probe-sweep-2026-08-09.json`); the landings above then closed 10. The remaining three rows' apply plan is FROZEN at `docs/rearchitecture/pb60-remaining-apply-plan-2026-08-09.md` (the SDIDI Dec branch — renderer arm + CobolDec.From projections of the one scan; the CURRENCY SIGN SET model; the DECIMAL-POINT IS COMMA containment scoping). Re-scout anchors at apply.
