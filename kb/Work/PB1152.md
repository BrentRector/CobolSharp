---
title: "PB1152 — INSPECT BACKWARD matches a multi-character literal ENDING at the current position (the runtime reverses the text AND the patterns), where §14.9.22.4 GR8 NOTE 2 matches starting at the leftmost character at the current position: Annex D.25 EXAMPLE 5 row 1 'ABABBCAB' prints COUNT-0=1 COUNT-1=1 COUNT-2=0, the compiler gives 1 0 0"
id: PB1152
kind: defect
status: open
severity: MAJOR
area: runtime/verbs
wrong_answer: true
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1124", "PB1123"]
spec_refs: ["14.9.22.4"]
inventory_rows: []
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14b** from a SIDE LEAD an adjudicator of batch **d1-14b** reported under `dossier_gaps` (pinned tree @`68bbdb00f`; not re-probed by the registrar — the repro path is the adjudicator's).

# PB1152 — OPEN

## The rule
> "Matching always takes place starting at the leftmost character at the current character position"
`cite.py --check 14.9.22.4 "Matching always takes place starting at the leftmost character at the current character position"` → `OK  §14.9.22.4 8)  (General rules)`

## Measured (inspect-statement-p2 adjudicator)
Probe `out/probe/inspect-p2/p4.cob` (pinned cobol.exe): Annex D.25 EXAMPLE 5 row 1 `ABABBCAB` → COUNT-0=1 COUNT-1=0 COUNT-2=0 where the example prints 1 1 0; row 3 `BCABCABD` prints COUNT-2=4, the compiler 0. ⚠ Row 2's printed final value `AZDBABC` contains a 'Z' no phrase of the example can produce, so EXAMPLE 5 has at least one erratum — adjudicate against the RULE text, not the table.

## Code site
`src/Cobol.Net.Runtime/Verbs/CobolInspect.cs` — the BACKWARD arm reverses text and patterns before the forward cycle.

## What the fixer owes
Scan right-to-left over CURRENT positions while comparing each literal left-to-right from the current position (no pattern reversal), for TALLYING, REPLACING and CONVERTING; golden from the rule text with EXAMPLE 5's consistent rows.
