---
title: "PB1526 — an integer intrinsic argument with a RUNTIME fraction is silently truncated even with EC-ARGUMENT-FUNCTION checking on: FACTORIAL(X / 2 + 1) with X = 5 answers FACTORIAL(3) = 6 (IntegerArgReal truncates; the static screen fails open on a nested quotient)"
id: PB1526
kind: defect
status: open
severity: MAJOR
area: runtime/intrinsics
wrong_answer: true
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB617"]
spec_refs: ["15.3", "5.5"]
inventory_rows: ["GR-5.5-3"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 22:46 UTC by **REGISTRAR-1** (cloud, branch `claude/adj-registrar-1`, kb/Work PB1522 step 2) from the lane-3 adjudication of the last 208 verdict-less inventory rows (wave 1 `claude/adj-cal-1` + `claude/adj-a1-w1-s1..s4`, wave 2 `claude/adj-w2-*`, all pinned at `0caa7d5`). Every citation below was re-run by the registrar through `python scripts/spec/cite.py --check`.

# PB1526 — OPEN

## The rules

> 6) Integer. An arithmetic expression that will always result in an integer value or an integer data item shall be specified.

`cite.py --check 15.3 "An arithmetic expression that will always result in an integer value"` → OK §15.3 6).

> If the evaluation of an argument results in an incorrect value for that argument … the EC-ARGUMENT-FUNCTION exception condition is set to exist.

`cite.py --check 15.3 "the EC-ARGUMENT-FUNCTION exception condition is set to exist"` → OK §15.3 14).

> a) If native arithmetic is in effect, the implementor shall define when the operand represents an integer.

`cite.py --check 5.5 "the implementor shall define when the operand represents an integer"` → OK §5.5 3) a).

## Repro (re-probed by the registrar, Debug build of `0caa7d5`, `--std 2023`)

```cobol
       01 X PIC 9 VALUE 5.
       PROCEDURE DIVISION.
      >>TURN EC-ARGUMENT-FUNCTION CHECKING ON
           DISPLAY FUNCTION FACTORIAL(X / 2 + 1).
           DISPLAY "ES=" FUNCTION EXCEPTION-STATUS.
           DISPLAY FUNCTION FACTORIAL(X * 0.5).
           DISPLAY FUNCTION ORD(FUNCTION CHAR(X / 2 + 64)).
```

Observed: `6`, `ES=` (blank), `2`, `66`. Expected: `X / 2 + 1` = 3.5 and `X * 0.5` = 2.5 are not always integral, so either the
bind-time screen refuses them (as it does `FACTORIAL(X / 2)`: COBOLNET1627) or, with checking enabled, the non-integer value
raises EC-ARGUMENT-FUNCTION (fatal) — never a silent FACTORIAL(3). `CHAR(66.5)` silently returns ordinal 66.

## Two holes, one path (code sites)

1. **Static** — `src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs` (`NotAlwaysIntegral` / `CollectAdditive`) checks a
   quotient only at the ROOT of the argument expression and treats a non-additive subtree as opaque, so a division or a
   fractional-literal product nested in an additive spine fails open. This is the data-item sibling of PB617 (literal-only spines).
2. **Runtime** — `src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.cs#IntegerArgReal` is `(long)Math.Truncate(v)` and the Int128
   intake narrows the scaled value, so the fraction is discarded and EC-ARGUMENT-FUNCTION is never set. The fail-open design of (1)
   is only sound once (2) exists.

The subscript and reference-modification general rules DO test integrality at run time (`EC-BOUND-SUBSCRIPT … 2.500000000 is not an
integer`), so the de-facto native-arithmetic definition ("the exact scaled value has a zero fraction", §5.5 3) a) is already made —
its documentation is DOC-A.1-124 (PB1534 / PB1535). Row GR-5.5-3 recorded **PARTIAL**.

## Sibling sweep

Every integer-argument intake in `CobolIntrinsics` (`IntegerArg`, `IntegerArgReal`, the WIDE twin) and every intrinsic whose
signature marks an argument integer; PB617 for the static literal half.
