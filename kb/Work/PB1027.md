---
title: "PB1027 — ADJUDICATION: the implicit AFTER ADVANCING 1 LINE of a WRITE without ADVANCING when NO line is open — a strict §14.9.51.4 GR25 reading puts a blank line after a BEFORE write and a blank first line on a LINAGE page; the code (SequentialConnector.ImplicitAdvanceIsBefore) and the NIST corpus do neither"
id: PB1027
kind: adjudication
status: open
severity: MINOR
area: runtime/io
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB964", "PB578"]
spec_refs: ["14.9.51.4", "13.18.34.4"]
inventory_rows: ["GR-14.9.51.4-25"]
closes_rows: []
tags: [cobolsharp, work, adjudication]
---

Filed 2026-09-22 by **REGISTRAR #12** from `leads-w48-w52.md` (wave 48 group Y, PB964 implementer, worktree wf_ffdefe6a-659-8). Adjudication, no probe: the observable is the physical line image of a line-sequential or LINAGE file after a BEFORE write followed by a plain WRITE.

# PB1027 — OPEN

## The rule
`OK  §14.9.51.4 25)` — "If the ADVANCING phrase is not used, automatic advancing shall be provided by the implementor to
act as if the user has specified AFTER ADVANCING 1 LINE."
After `WRITE … BEFORE ADVANCING 1`, the device is already positioned at the start of a fresh line; "AFTER 1" read
literally advances again, leaving an empty line. On a LINAGE file's first write of a page the same reading leaves line 1
blank.

## What the adjudicator owes
Decide under the ISO → GnuCOBOL precedence (`feedback_follow_gnucobol_on_split_latitude` covers only implementor
latitude — "shall be provided by the implementor to act as if" is a behaviour, not a choice, so the literal reading
wins unless the standard elsewhere says a positioned device satisfies it). Measure GnuCOBOL's output for both shapes;
record the ruling in docs/CONFORMANCE.md and golden it, and reconcile with the NIST goldens that pass today.
