---
title: "PB176 — the ODO group-sender path emits a call to AsImage() on a group whose struct never receives one: a group carrying BOTH an OCCURS DEPENDING table and a dynamic-length/dynamic-capacity member fails BACKEND compilation (CS1061 in generated C#) on legal source — a loud-failure violation where the Tier-C stage (or the GR8a current-count slice over a defined image) belongs"
id: PB176
kind: defect
status: landed
severity: MAJOR
area: codegen
wrong_answer: false
crashes: true
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.11.4", "13.18.38"]
tags: [cobolsharp, work, defect]
---

# PB176

Found 2026-08-30 by the PB164 VLG-display landing probes, and PROVEN PRE-EXISTING at `4006248f` by
stash/build/probe/pop — the VLG slice neither introduced nor changed it. Repro (legal source, compiles
under GnuCOBOL):

    01 G.
       05 N PIC 9(1) VALUE 2.
       05 GD PIC X DYNAMIC LENGTH.
       05 T PIC X(3) OCCURS 1 TO 5 DEPENDING ON N.
    ...
       DISPLAY "G=[" G "]"

→ `error: backend compilation failed ... CS1061: '_T_0' does not contain a definition for 'AsImage'`.

MECHANISM: a group operand with an ODO beneath takes the OdoGroupPlace sender (the §13.18.38 GR8
current-count slice), which composes over the group's `AsImage()` — but the struct-method emission gate
(`ElementImageCapable`, and `IsImageCapable` before it) refuses a group with a dynamic member, so the
call site references a method the struct never gets. The failure is at BACKEND compile on the
DECLARATION+use, not a runtime Tier-C loud — the loud-failure rule violated in the worst direction
(uncompilable output on legal input).

WHAT WOULD CLOSE IT: the OdoGroupPlace sender must consult the same image-capability predicate every
other sender consults and stage the Tier-C loud (TierCIsland.Reason — the derived offender text now
names the dynamic mechanism) when the group has no AsImage — or, the complete fix, compose the GR8a
slice over the PB164 current-extent machinery where `GroupImageCodec.CurrentExtentImageCapable` holds
(an ODO group WITHOUT other dynamics is image-capable and already works; the broken shape is exactly
ODO ∧ dynamic-member). Sweep: every OdoGroupPlace consumer (DISPLAY/MOVE/WRITE senders), and any other
sender path that calls `AsImage()` without consulting the emission predicate — the PB164 family's
two-arm-dispatch shape, one more time.

## ✅ LANDED 2026-08-30 — DEVLOG Entry 1399

`PlaceRenderer.GroupImage` — THE ONE image reader — now carries the capability guard its write twin
`WriteGroupImage` always had (the SEVENTH two-arm-dispatch instance), so every consumer (the GR8
SendingImage slice, ReceiveInto, CALL's boundary read, INSPECT) stages the runtime Tier-C loud with the
derived dynamic-mechanism message where the ODO∧dynamic group used to fail BACKEND compilation.
`TierCRejectionTests.DisplayOdoGroupWithDynamicMember_FailsLoudNotCs1061` pins the restored posture.

**THE SKEPTIC ROUND (two read-only adversarial agents; both earned their dispatch):**
- Skeptic 1 BROKE the first cut: the guard sat BEFORE the OdoGroupPlace unwrap, and
  `CallEmitter.CallStringRead` hands the WRAPPER in (alone among callers) — a Tier-B WINDOWED ODO group
  with a dynamic member (reachable via Format-2 CALL / RETURNING / INVOKE) would have turned its valid
  window Read into a loud throw. Fixed: unwrap first, guard after — the MemberPlace inner meets the
  guard with the EXACT predicate (fires ⟺ the struct has no AsImage), so the CS1061 fix holds and the
  window shapes keep arm 1.
- Skeptic 2 PROVED the guard's sufficiency (`IsImageCapable ⟹ ElementImageCapable` — every guarded site
  can only over-refuse, never CS1061; the CurrentImage gate arm-for-arm equivalent to its emission
  condition) and found FOUR remaining holes in the family: (D) the day-old DISPLAY CurrentImage arm
  lacked the Tier-B-window shape test — fixed in-change (`vp is not RedefViewPlace`); (A) OO method
  group formals/RETURNING emit image methods with NO gate, (B) `CallStringWrite`'s fall-through assigns
  string-into-struct (CS0029 — the WRITE twin of this very defect), (C) `AsStorageImage` spells
  `.AsImage()` itself without the window arm — A+B registered as [[PB177]] (with the missing
  Format-2/RETURNING/INVOKE variable-length screens and the dynamic-length-under-REDEFINES screen
  question), C as [[PB178]]. Also noted: `WriteGroupImage`'s DynTablePlace arm is now unreachable
  (over-loud, pre-existing posture; tidy with PB177).

Sibling sweep: every other direct `.AsImage()` emission site verified guarded; one stale SortEmitter
comment (outside the PB164 sweep's eleven sites) corrected. Wave gate GREEN (Conformance 1354 with the
new lock fact · Unit 5027 · characterization 33); probes: the ODO∧dynamic shape compiles and throws
Tier-C, the VLG golden unchanged. Battery #36 accrues (PB168 + PB164-VLG + this).
