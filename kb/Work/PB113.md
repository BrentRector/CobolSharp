---
title: "PB113 — ✅ LANDED with PB64 T6 (2026-08-28): the report-group PICTURE bind routes through the ONE format-2 analyzer (DataBinder.Reports.cs reads pictureLocalePhrase; the SIGN non-arm asserted — report groups carry no twin of §13.16.3 SR19)"
id: PB113
kind: defect
status: landed
severity: MAJOR
area: feature/locale
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.15.4", "13.18.40.2"]
closes_rows: []
closes_rows_reason: "no inventory row is attributable to this landing — derived by scripts/spec/backfill_closes_rows.py from the inventory's own commit history, re-derivable"
tags: [cobolsharp, work, defect]
---

# PB113 — report-writer format-2 PICTURE: a silent wrong answer today

**Measured 2026-08-28 (the PB64 T6 hardening sweep, read-only):** `DataBinder.Reports.cs:401` binds a report
group entry's PICTURE by reading `clause.pictureClause()?.PIC_STRING()` and never inspects
`pictureLocalePhrase()`. So `02 X PIC ZZ9.99 LOCALE SIZE IS 12.` inside a REPORT SECTION is accepted TODAY
with NO diagnostic — not even the (pre-T6) COBOLNET1518 the working-storage arm raises — and the picture is
analyzed as Format 1: the item's width comes from the mask instead of §13.18.40.4 GR17's integer-1, and no
locale editing exists. §13.15.4 GR2 imports the PICTURE clause's own rules into report group description
entries, so format 2 is LEGAL there and the acceptance is a silent wrong answer, independent of whether T6 has
landed.

**Sibling fact asserted at the same time (a deliberate NON-arm):** §13.15.3 carries NO SIGN-with-LOCALE
prohibition (the §13.16.3 SR19 / §13.17.3 SR9 pair does not exist for report groups), so the fix must NOT copy
the SR19 check to `DataBinder.Reports.cs:408` — a positive golden pins a report group entry carrying both
LOCALE and SIGN as ACCEPTED.

**Fix (rides the PB64 T6 landing):** route the report-group PICTURE bind through the SAME format-2 analyzer as
the data-division and screen arms — one analyzer, three callers ([[feedback_one_rule_one_place]]). Golden: a
report group format-2 item edited through the report writer, plus the LOCALE+SIGN positive.

Related: [[PB64]], [[PB114]].

## Registrar 14f — 2026-09-24 (measured on its own Debug build of main `071ff04ea`)

**Correction: this note's "deliberate NON-arm" does not hold.** It asserted that a report group item may
carry both a LOCALE picture and SIGN, because §13.15.3 has no twin of §13.16.3 SR19. The conclusion is wrong for
a different reason: the SIGN clause's OWN subject rule already excludes the pair.
```
$ cite.py --check 13.18.52.3 "a numeric report group description entry whose picture character-string contains the symbol 'S'"
  OK  §13.18.52.3 1)  (Syntax rules)
```
A format-2 (LOCALE) picture is not "numeric … containing the symbol 'S'". PB537 (wave 58, landed) enforces SR1
as COBOLNET2422 at both the data-division and report sites. The data-division twin
`tests/conformance/negative/pb64t6-picture-locale-sign` now draws both 1674 and 2422, and both rules are
violated. This is a reading of the spec, not a regression: this note's non-arm was
[[a_real_clause_can_answer_a_different_question]]. No inventory row carried the non-arm, since `SR-13.16.3-19`
is the data-division rule and stays CONFORMS, so there is nothing to re-verdict.
