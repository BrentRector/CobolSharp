---
title: "PB984 — docs/CONFORMANCE.md carries the Annex A.1 row `DOC-A.1-56` (DISPLAY conversion of data) TWICE, at lines 1306 and 1341, with different wording — and `audit_annex_a1.py --check` reports no findings, because it compares the SET of row keys with the inventory and a duplicated key is invisible to a set"
id: PB984
kind: defect
status: open
severity: MINOR
area: docs/conformance
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
cluster: [PB160]
spec_refs: ["14.9.11.4"]
inventory_rows: []
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-22 by **REGISTRAR #11** from wave 46 group M's lead A (PB871 implementer report; lander lead M-A).
**Re-checked on main `6871b6e10`**: `grep -n "DOC-A.1-56" docs/CONFORMANCE.md` → two table rows, line 1306
("**DISPLAY statement — conversion of data**, §14.9.11 GR1") and line 1341 ("**DISPLAY statement — data conversion**,
§14.9.11.4 GR1"). Both now state owner decision R13 correctly (PB871's lander fixed the contradicting sentence), but a
reader of the register, and every tool that maps a key to ONE row (the inventory row's computed code-location
`docs/CONFORMANCE.md#DOC-A.1-56`), sees one of two texts arbitrarily.

## What the fixer owes

1. Merge the two rows into one (keep the §14.9.11.4 GR1 citation — re-run `cite.py --check 14.9.11.4` on the text the
   merged row quotes — and both evidence links).
2. `scripts/spec/audit_annex_a1.py` (`ITEM_KEY_RX = ^DOC-A\\.1-(\\d+)$`, ≈:111) counts every §7 row key and fails on a
   key that appears more than once; make it fail once on the current file before the merge (memory
   `green_gates_arent_evidence`). Sweep the other DOC-A.3 / A.4 registers the audit family reads for the same set-vs-list
   blindness.

Process-only: no program's behaviour changes. PB160 (the A.1 documentation backlog) is in flight on another branch,
so this is a separate note rather than an edit to PB160.

## A second duplicated key — REGISTRAR #12 (2026-09-22, main `37f19e028`)

`grep -n "^| DOC-A.1-92 " docs/CONFORMANCE.md` → TWO rows, lines 1400 ("Characteristics and representation of a numeric
intrinsic function's returned value, and its text image", §15.4.1 + §14.9.11.4 GR1) and 1401 ("Evaluation of an
exact-family intrinsic whose ARGUMENT is floating-point", §15.4.1). Unlike item 56 these are two DIFFERENT topics under
one key, so the fix is not a merge: decide which text is Annex A.1 item 92 (read the item in the transcription) and give
the other its own row or fold it into the matching one. The audit's set-vs-list blindness (fixer item 2) is the same
for both keys; the train-49 lander reported both.
