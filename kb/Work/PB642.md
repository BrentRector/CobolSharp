---
title: "PB642 — the NIST guard scored a FALSE REGRESSION under full-fan-out contention: battery #48's one red, IF137A 'DIFF — REGRESSION!', preserved a print file that is BYTE-IDENTICAL to the golden after the guard's own normalization (17 of 17 tests passed; serial re-run MATCH 3/3; a 12-wide contention probe MATCH 12/12) — the compare arm's verdict was corrupted, not lost, which guard-fast's header says can no longer happen; the retry path re-observes only LOST (no-verdict) results, so a corrupted DIFF is never re-taken, and the forensics preserve the LAST candidate's normalized form (an empty stdout) rather than the one the verdict was scored on"
id: PB642
kind: defect
status: open
severity: MAJOR
area: scripts/guard
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: []
inventory_rows: []
tags: [cobolsharp, work, defect]
---

# PB642 — OPEN — a regression the instrument invented

Found 2026-09-05 attributing battery #48 (`<NEW scratch>/battery-48/`, run in the detached worktree at `6eaa98cf`,
train 5's head): `=== NIST: 352 MATCH, 1 REGRESSION(S) ===` naming IF137A (STANDARD-DEVIATION), the audit's one
finding being the same line, every other leg green and the GnuCOBOL differential at zero flips. This note is the
third face of [[PB473]] (the compare arm scoring a report it could not read as a wrong answer, battery #43) and
[[PB478]]: the evidence rules now separate "wrong answer" from "no answer", and this run produced a wrong answer
that was not one.

## The measurement (all on the battery worktree's own build at `6eaa98cf`)

- `/tmp/gf_forensics/IF137A/if137a.txt` (the program's print file, `cp -p`-preserved, mtime 03:55:17 = the
  run's end): 44 lines, "017 OF 017 TESTS WERE EXECUTED SUCCESSFULLY", and `tr -d '\r' | sed 's/ *$//; …'` of it
  is BYTE-EQUAL to the preserved `expected.norm` (1983 bytes). The compiler passed IF137A.
- `actual.norm` in the same forensics: 0 bytes, mtime 03:55:23 — six seconds after the print file's last write.
- `IF137A-stdout.txt` and `-stderr.txt`: both empty; the run's rc was 0 (the verdict took the "ran to
  completion, produced bytes, comparison said they differ" branch).
- Serial re-observation through the same group runner, three times: `IF137A: MATCH`. Twelve group runners for
  IF137A launched concurrently on the same worktree: `IF137A: MATCH` ×12, no stderr.

So at the moment the guard normalized `$WORK/if137a.txt` (between 03:55:19 and :23) the comparison returned a
difference for a file whose last write was at :17 and whose content equals the golden. The mechanism was not
reproduced; the shape is what the note records.

## Three holes, each its own fix

1. **The forensics do not preserve the evidence the verdict was scored on.** `guard_output_verdict` normalizes
   EVERY candidate into the one `$CMP/actual.norm`, so after the loop the file holds the LAST candidate tried
   (the empty stdout), and `guard_preserve` copies that. Candidate 1's normalized form — the thing that "differed"
   — is gone. Preserve per candidate (`actual.<basename>.norm`) and record which candidate each diff rc came from.
2. **A REGRESSION is scored on ONE observation under contention.** `guard-fast.sh` step 3b re-takes LOST
   observations ("contention can no longer corrupt a verdict — it can only LOSE one") — the premise this battery
   refutes. A non-match whose preserved report normalizes EQUAL to the golden is a contradiction the harness can
   detect itself, and any regression is rare enough to re-observe once at serial isolation before scoring:
   route `regression` verdicts through the same retry path as `no-verdict`, and score the second observation.
3. **The mechanism is unmeasured.** The candidates for a transient difference on a complete file: a truncated
   read while the writer's handle was not yet released (the .NET process exited, but the runtime's PRINT-FILE
   writer may be closed by a process-exit hook rather than the program's CLOSE — the compile log's
   `CBL0702: I/O operation on file 'PRINT-FILE' which has not been OPENed` warning says the file's lifecycle is
   unusual in this program), a Defender/indexer sharing conflict on a freshly written file, or MSYS `mktemp -d`
   returning a directory another group was still using. A stress probe (N-wide, hundreds of iterations, logging
   candidate-1's normalized form and the diff rc on every non-match) decides; hole 1 is its prerequisite.

## What the battery record says

Battery #48 is green on the compiler once attributed: the one NIST regression is this note's false DIFF, the audit
finding is the same line, and everything else — Conformance 5806/5806, Unit 5413/5413, characterization 33/33,
both citation audits, the 21 evidence witnesses, 1323 differential cases at zero flips — is green as printed.

## 2026-09-06 — registrar append: two more instances (RL203A, SQ223A), and the mechanism narrowed

Added by the registrar rather than opened as a new id (`PB781` was allocated for this and **left unallocated**):
this note already owns "the NIST guard scores a FALSE REGRESSION under full-fan-out contention on a report that
is byte-identical to its golden", including the fix — hole 2, *route `regression` verdicts through the same
retry path as `no-verdict`*. The PB750 finisher hit it twice in one run and proposed the same fix from scratch;
recording it here rather than in a fourth note is what keeps the instances countable.

### The two new instances

Measured 2026-09-06 by the PB750 finisher (worktree `agent-a38afaefd55fc0663`, base `1e7b5085`, Windows Git
Bash, `JOBS=32`; his report §"The gate" and §"The two reds in run 1"):

```
run 1:  === NIST (cobol): 362 MATCH, 2 REGRESSION(S) ===
        UNEXPECTED: SQ223A expected MATCH, got DIFF — REGRESSION!
        UNEXPECTED: RL203A expected MATCH, got DIFF — REGRESSION!
run 2, same tree, no edits between:
        === NIST (cobol): 364 MATCH, 0 REGRESSION(S) ===   audit CLEAN   ALL GREEN
```

Attribution, all four checks this note's own §"The measurement" prescribes:

- `cmp tests/nist/valid/RL203A.txt /tmp/gf_forensics/RL203A/rl203a.txt` → **byte-identical** (2054 bytes, 41 CRs).
  Same for `SQ223A` (2281 bytes, 41 CRs).
- Serial re-observation through the real `guard-run-group.sh`: `RL201A: MATCH / RL202A: MATCH / RL203A: MATCH`
  for RL203A's own chain group, and `SQ223A: MATCH`.
- The discriminator was run too, which is what makes the attribution evidence rather than assertion: `RL203A`
  **alone** (outside its `g203|RL201A RL202A RL203A` chain) does genuinely DIFF, with a 3511-byte report full of
  `FAIL* … INCORRECT RECORD COUNT`. So a real difference still looks like one, and the guard's grouping was
  correct.
- Forensic mtimes rule out a partial write: `RL203A`'s report was last written at `09:00:01.358`, and the
  comparison's `expected.norm` at `09:00:05.492` — four seconds later.

**Two in one run of 376, where battery #48 gave one in 376 and battery #43 one in 376.** The rate is not falling.

### ⛔ The mechanism, narrowed — and one premise of the incoming report does NOT hold

The report proposes the fix as *"`guard_normalize_into` … still produces its output through a two-stage PIPE
(`tr -d '\r' | sed`), and a truncated result exits 0 and is indistinguishable from a content difference — the
residual of PB473, which removed the process substitutions from the diff's inputs but left a pipe inside the
normalization."* **Re-read on main at `9fad13e5`, the second half of that is already done**
(`scripts/guard-verdict.sh:72-77`):

```sh
guard_normalize_into() {
    local -
    set +e -o pipefail                                  # <- the pipeline's status IS the function's status
    [ -f "$1" ] || return 2
    tr -d '\r' < "$1" | sed 's/ *$//; s/COMPUTED=  [0-9]*/COMPUTED=  XXXXXXXXX/' > "$2"
}
```

and its status is read at both call sites — `:117-125` (the golden → `COMPARE NO-VERDICT`) and `:132-137` (a
candidate → `trouble=1` at `:134`, which `:173-175` turns into a NO-VERDICT). PB473 item 1 landed and holds. What is missing is
narrower and worth stating exactly, because it is what a fixer must build:

1. **Nothing asserts the normalized OUTPUT's own content.** `pipefail` catches a stage that FAILS; a pipeline in
   which `tr` and `sed` both exit 0 while delivering short data produces a truncated `$2` with rc 0, and `diff`
   at `:140` then answers `1` — a *content difference* — which falls straight through to `REGRESSION`. The cheap
   structural assertion is on the function's own inputs and output: a normalized file whose line count is
   inconsistent with its source is a lost observation, `COMPARE NO-VERDICT`, not a difference.
2. **`content=1` at `:131` is measured on the RAW candidate, not on the normalized copy** — so the "wrong answer
   vs no answer" discriminator cannot see a normalization that produced nothing either.
3. **And a scored `regression` is still never re-observed** — this note's hole 2, unchanged. `guard-fast.sh`
   step 3b already re-observes NO-VERDICTs serially, so both 1 and 3 are one arm each on machinery that exists.

⚠ **Honest limit on the attribution.** Neither this note's IF137A nor the two above was reproduced *at the
function*: what is measured is that the report was byte-identical, that a serial re-observation matched, and that
a genuinely wrong report still scores REGRESSION. `guard_normalize_into` is the strongest CANDIDATE mechanism —
it is the one place a complete file becomes a shorter one — but this note's hole 3 ("the mechanism is
unmeasured") is still open, and item 1 above is a repair that makes the guard honest whether or not it is the
cause. Do not write the mechanism into a commit message as established.
