---
title: "PB1001 — LINE integer-1 ON NEXT PAGE (§13.18.35) is refused with COBOLNET0899 \"not yet implemented\" at every edition — legal report source rejected, and §13.18.35.4 GR4 a) 2) cannot be reached"
id: PB1001
kind: defect
status: landed
severity: MINOR
area: binder/report
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB951", "PB957"]
spec_refs: ["13.18.35.2", "13.18.35.4"]
inventory_rows: []
closes_rows: ["GR-13.18.35.4-4", "SR-13.18.35.3-7", "SR-13.18.35.3-8"]
tags: [cobolsharp, work, defect]
---

Filed 2026-09-22 by **REGISTRAR #12** from `leads-w48-w52.md` (wave 49 group B, PB951/PB957 implementer, worktree wf_ca96470f-e1c-2). Re-probed on the registrar's own `dotnet build CobolSharp.sln -c Debug` (0 warnings, 0 errors) at main `37f19e028`; every citation below re-run through `python scripts/spec/cite.py --check` on this tree.

# PB1001 — OPEN

## Repro — measured
`scratchpad/reg12/q05_linenp.cob`: `RD RP PAGE LIMIT 20 LINES.` `01 DL TYPE DETAIL LINE 5 ON NEXT PAGE.` →
`error COBOLNET0899: RD 'RP': LINE … NEXT PAGE (ISO §13.18.35) is not yet implemented` (descriptor
`report-line-next-page`), followed by a cascade `a COLUMN clause with no LINE clause in effect`.

## What the fixer owes
Implement the NEXT PAGE arm of the LINE clause (the group is presented on the next page at absolute line integer-1),
following `docs/rearchitecture/DESIGN-*` for Report Writer and the §13.18.35.4 rules (find GR4's exact text with
`cite.py --check 13.18.35.4 "<sentence>"` — it was not quoted by the lead). The cascade diagnostic must go too: a
refused LINE clause should not leave its COLUMNs orphaned (the PB909 two-jobs shape). Golden at 2002.

## Landed — 2026-09-22 (wave 54 E, with PB1002)

Re-probed first on the worktree's own build (train 50 `25faf9f60`, which carries PB957's NEXT GROUP engine):
`q05_linenp.cob` still drew `COBOLNET0899 … LINE … NEXT PAGE (ISO §13.18.35) is not yet implemented` plus the
`a COLUMN clause with no LINE clause in effect` cascade. After: it compiles and prints `HELLO` on line 5 of page 1 —
the chronologically first body group takes no page fit test (§13.18.35.4 GR4), so the phrase does not advance it.

- **Both operand forms are live** (§13.18.35.2 Format 1, rendered: the brace is `integer-1 [ON NEXT PAGE]` |
  `{PLUS|+} integer-2` | bare `ON NEXT PAGE`). The phrase is a FLAG, not a kind: `ReportLineModel.NextPage` /
  `ReportGroupLine.NextPage`, set by `RepeatedLine` on the group's FIRST report line only, on an Absolute line
  (integer-1) or a Relative one (the bare operand). Every phrase-less placement rule stays the one rule.
- **Engine** — `PresentBody`: a first PRESENT line with the flag takes no test and the fit is unsuccessful (GR4a);
  placement is then GR5a (integer-1) or GR5b3 (FIRST DETAIL). `PresentHeadingFooting`: a report footing whose first
  line carries it is on a page by itself (GR5a) — the bare `PageFeed`, no PH before it (§13.18.57.4 GR6b), no PF
  after it (GR6f 2); the bare form starts at the HEADING integer (GR7f). `PlaceAfterSavedNextGroup`: §13.18.37.4
  GR4a 2 is now reachable and is GR4a 1 after the one advance the save location forces.
- **Syntax rules** — `ScreenReportLineClauses` on COBOLNET2199 (the LINE family code), once per WRITTEN clause over
  the flat entry array: SR7 (first LINE clause only) and SR8 (body group or report footing only), with PB1002's
  SR3/SR5. The group's TYPE comes from `GroupKindOf`, extracted from `BindGroupType` so there is ONE TYPE reader.
- **Deleted** — the `ReportLineNextPage` descriptor (`report-line-next-page`); a tombstone comment stands in
  `DiagnosticCatalog`. The COLUMN cascade went with the refusal (no LINE clause is refused any more).
- **Determinations** (docs/CONFORMANCE.md, the LINE NEXT PAGE block): the bare operand's placement; SR5 refuses the
  bare operand in an unpaged report; GR4a 2 takes ONE advance. GnuCOBOL 3.2 parses the phrase but gives it no
  run-time effect, so it offered no precedent.
- **Goldens** — `2002/pb1001_line_next_page` (two reports: absolute and bare body groups, a PRESENT WHEN-decided first
  line, the NEXT GROUP save path, both report-footing forms); negatives `pb1001-next-page-not-first-line` (SR7),
  `pb1001-next-page-in-page-heading` (SR8).
- **Rows** — GR-13.18.35.4-4, SR-13.18.35.3-7 and SR-13.18.35.3-8 closed CONFORMS; GR-13.18.37.4-4 restated (GR4a 2
  reachable). Batch `scratchpad/w54e/PB1001/batch.json` (with PB1002's rows).
