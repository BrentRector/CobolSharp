---
title: "PB1166 — OoConformance.DescriptionMismatch's PICTURE/category identity is missing four axes of §14.8.3.3: no currency-STRING axis and no DECIMAL-POINT IS COMMA axis (a DPC class returning Z9,99 into a non-DPC Z9,99 receiver is accepted and reads 1234 for 12.34), alphabetic and alphanumeric PICTUREs of equal length compare equal, bit-group / national-group items have no arm (refused as alphanumeric groups although rules 2/3 make them match elementary boolean/national items), and LOCALE identity over-normalises ('en-US' vs 'EN_us.UTF-8' accepted)"
id: PB1166
kind: defect
status: open
severity: MAJOR
area: binder/call-oo
wrong_answer: true
crashes: false
silent: false
rejects_legal_source: true
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1164", "PB1165", "PB1167", "PB480", "PB1497", "PB1500"]
spec_refs: ["14.8.3.3", "14.8.3.1", "12.3.7.4"]
inventory_rows: ["GR-14.8.3.3-L2.1", "GR-14.8.3.3-L2.2", "GR-14.8.3.3-L2.3", "GR-14.8.3.3-2", "GR-14.8.3.3-L3.1", "GR-14.8.3.3-L3.2", "GR-14.8.3.3-L3.3", "GR-9.3.8.2.3-3", "GR-9.3.8.2.3-6", "GR-9.3.6-L3.3", "GR-9.3.6-L4.1", "GR-9.3.6-L4.2", "GR-9.3.5.3-3", "GR-9.3.5.3-4", "GR-9.3.5.3-5", "GR-9.3.5.3-6"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14c** from lane-3 adjudication batch **d1-14c** (adjudicated on the pinned tree `68bbdb00f`; trains 58/59 checked — nothing that landed since touches this mechanism unless the body says otherwise).

# PB1166 — OPEN

## The rule (every citation re-run by the registrar through `cite.py --check`)

> 1) Currency symbols match if and only if the corresponding currency strings are the same.

`python scripts/spec/cite.py --check 14.8.3.3 "…"` → `OK  §14.8.3.3 1)  (Elementary items)`

> 2) Period picture symbols match if and only if the DECIMAL-POINT IS COMMA clause is in effect for both the activating and the activated runtime elements or for neither of them.

`python scripts/spec/cite.py --check 14.8.3.3 "…"` → `OK  §14.8.3.3 2)  (Elementary items)`

> 2) A bit group item matches an elementary boolean data item of usage bit described with the same number of boolean positions.

`python scripts/spec/cite.py --check 14.8.3.3 "…"` → `OK  §14.8.3.3 2)  (Elementary items)`

> 3) A national group item matches an elementary data item of usage national described with the same number of national character positions.

`python scripts/spec/cite.py --check 14.8.3.3 "…"` → `OK  §14.8.3.3 3)  (Elementary items)`

## Finding — `returning-items` (under-rejects; rows `GR-14.8.3.3-L2.1`, `GR-14.8.3.3-L2.2`, `GR-14.8.3.3-L2.3`)

**Mechanism.** The returning-item PICTURE identity compare is a raw character-string compare that ignores each element's SPECIAL-NAMES: no currency-STRING axis and no DECIMAL-POINT IS COMMA axis

**Repro.** Main 'SPECIAL-NAMES. CURRENCY SIGN IS "USD" WITH PICTURE SYMBOL "$".' '01 R PIC $$$9.99.' 'INVOKE O "GETC" RETURNING R'; class RIC10 'SPECIAL-NAMES. CURRENCY SIGN IS "EUR" WITH PICTURE SYMBOL "$".' method GETC 'RETURNING LR' LR PIC $$$9.99 (p10.cob). And class RIC9 'DECIMAL-POINT IS COMMA' returning LR PIC Z9,99 into a main (no DPC) R PIC Z9,99 (p9.cob); likewise PIC Z9.99 both sides (p9p.cob).

**Expected from the spec.** Rule 1: currency symbols match only if the currency strings are the same (USD vs EUR -> no match). Rules 2/3: period and comma symbols match only if DECIMAL-POINT IS COMMA is in effect in both elements or neither (class only -> no match). All three pairs are non-conforming -> COBOLNET0828 at the INVOKE.

**Observed / reasoning.** All three compile and run: p10 prints [ EUR12.34] into a USD-described receiver, p9 prints [12,34] (the receiver reads 1234 where the sender meant 12.34), p9p prints [12.34]. The conforming halves hold (p10m: 'USD' string under symbols '$' and 'U' accepted). DescriptionMismatch's NumericEdited arm compares EditMask only; the currency string and DPC state of the activating and activated elements are never consulted. Reachable only across separately-defined units, since contained units inherit SPECIAL-NAMES (§12.3.7.4 GR1). The §14.8.2.3.2 a)/b) BY REFERENCE twins share the comparator.

**Code site.** src/Cobol.Net.Compiler/Oo/OoConformance.cs#DescriptionMismatch (NumericEdited/National/Boolean arm EditMask compare)

## Finding — `returning-items` (rejects-legal-source; rows `GR-14.8.3.3-L3.2`, `GR-14.8.3.3-L3.3`)

**Mechanism.** DescriptionMismatch has no bit-group / national-group arm: §14.8.3.1's NOTE (such groups are elementary) and §14.8.3.3's matching rules are absent, so a group side falls into the alphanumeric-group arms and is refused

**Repro.** Nested SB11 'LINKAGE SECTION. 01 LB PIC 1(8) USAGE BIT. PROCEDURE DIVISION RETURNING LB.'; caller '01 GB USAGE BIT. 05 GB1 PIC 1(4). 05 GB2 PIC 1(4).' 'CALL "SB11" AS NESTED RETURNING GB'; nested SN11 returning 'PIC N(5)' into '01 GN GROUP-USAGE NATIONAL. 05 GN1 PIC N(2). 05 GN2 PIC N(3).' (p11.cob); reverse direction p12.cob.

**Expected from the spec.** 8 boolean positions / 5 national positions on each side -> conforming; runs printing [1111][0000], [AB][CDE] (p11) and [ABCDE] (p12).

**Observed / reasoning.** COBOLNET1736 'a group argument requires a group or alphanumeric formal' (p11, both CALLs) and 'a group formal requires a group or alphanumeric argument' (p12). The same comparator serves §14.8.2.3.2 b)/c) (BY REFERENCE arguments) and the INVOKE lane, so the sibling rows are affected identically. The inverse under-reject (a national group paired with an alphanumeric group of equal width, which §14.8.3.2 forbids because a national group is not an alphanumeric group) is likely but not probed.

**Code site.** src/Cobol.Net.Compiler/Oo/OoConformance.cs#DescriptionMismatch (formal.IsGroup and arg.IsGroup arms)

## Finding — `returning-items` (under-rejects; rows `GR-14.8.3.3-2`)

**Mechanism.** The returning-item PICTURE identity treats alphabetic and alphanumeric PICTUREs of equal length as the same clause (one Alphanumeric category, null EditMask)

**Repro.** Nested SA13 'LINKAGE SECTION. 01 LA PIC A(5). PROCEDURE DIVISION RETURNING LA.'; caller '01 EX PIC X(5).' 'CALL "SA13" AS NESTED RETURNING EX' (p13.cob).

**Expected from the spec.** The sending operand is not an object reference, so 'the receiving operand shall have the same ... PICTURE ... clauses'; PIC A(5) and PIC X(5) are different PICTURE clauses and none of the listed exceptions applies -> COBOLNET1736.

**Observed / reasoning.** Compiles and prints [ABCDE]. DescriptionMismatch's Alphanumeric arm compares JUSTIFIED, EditMask (null for both) and Length only. ALIGN and DYNAMIC LENGTH, also on the list, are compared nowhere in DescriptionMismatch (not probed). The §14.8.2.3.2 BY REFERENCE twin shares the arm.

**Code site.** src/Cobol.Net.Compiler/Oo/OoConformance.cs#DescriptionMismatch (case PicCategory.Alphanumeric)

## Refuter evidence — row `GR-14.8.3.3-L3.1` (PARTIAL)

PARTIAL — Both halves hold on the INVOKE lane (adjudicator) and on the CALL/program-prototype lane (re-probed here: R1 de-DE vs en-US rejected COBOLNET1736, R2 SIZE 20 vs 22 rejected, R3 same literal accepted, R4 both unnamed accepted, R5 named vs unnamed rejected, R7/R8 digit-only 9(5)/Z(5) LOCALE pictures also screened). But the ONLY-IF half over-matches: the rule defines the external identification as 'the external-locale-name or literal value' and requires the SAME one, while OoConformance.DescriptionMismatch -> LocaleSymbol.SameLocaleAs -> LocaleIdentification.SameLocale compares Normalize()d tags case-insensitively (strips the '.codeset' suffix, maps '_' to '-', folds C/POSIX/INVARIANT/ROOT/und to root). Probe R6: prototype LOCALE DD IS "en-US" vs caller LOCALE US IS "EN_us.UTF-8" compiles clean although the two literal values differ. An under-rejection at the identity edge; the mainstream pairs conform. Also the diagnostic cites §14.8.2.3.2 rule 2 (the argument rule) rather than §14.8.3.3 for a returning pair - cosmetic. No spec-derived test pins the returning pair (test-ref empty).

**Code site.** src/Cobol.Net.Compiler/Oo/OoConformance.cs#DescriptionMismatch

## What the fixer owes

One comparator, all axes: give DescriptionMismatch the activating and activated elements' SPECIAL-NAMES state (currency strings, DPC) for the NumericEdited/National/Boolean arms, a PICTURE-clause identity that distinguishes A from X (and compares ALIGN and DYNAMIC LENGTH, also on the §14.8.3.3 list and compared nowhere), bit-group / national-group arms per the §14.8.3.1 NOTE, and a LOCALE identity that compares the external-locale-name or literal value as written. The comparator also serves §14.8.2.3.2 (BY REFERENCE arguments) and the INVOKE lane — sweep both. Probe the inverse under-reject the adjudicator flagged but did not run (a national group paired with an alphanumeric group of equal width).

## Extended 2026-09-24 by REGISTRAR 15d (batch 15d, pinned `071ff04ea`)

Rows added to `inventory_rows`: `GR-9.3.8.2.3-3`, `GR-9.3.8.2.3-6`, `GR-9.3.6-L3.3`, `GR-9.3.6-L4.1`, `GR-9.3.6-L4.2`, `GR-9.3.5.3-3`, `GR-9.3.5.3-4`, `GR-9.3.5.3-5`, `GR-9.3.5.3-6`.

The SAME comparator (OoConformance.DescriptionMismatch) serves §9.3.8.2.3 rules 3/6 (interface conformance, IMPLEMENTS and OVERRIDE), §9.3.6 match rule 3 e) (BY REFERENCE method matching) and §9.3.5.3 items 3/4/6 (the method resolution signature), and batch 15d measured every axis this note already names on those three lanes: alphabetic vs alphanumeric PICTURE, ALIGNED, DYNAMIC LENGTH, the currency STRING (not the symbol), DECIMAL-POINT IS COMMA per source element, and LOCALE identity by literal value (GR-9.3.5.3-5: `en_US.UTF-8` vs `en-US` accepted as a conforming override). One comparator fix closes all of them — the fixer's goldens must cover the override, IMPLEMENTS and INVOKE lanes, not only §14.8.3.3's RETURNING.

## The rule (every citation re-run through `cite.py --check`)

> Currency symbols match if and only if the corresponding currency strings are the same.

`OK  §9.3.8.2.3 3) a)  (Conformance between interfaces)`

### Finding — `conformance-for-object-orientation` (under-rejects; rows `GR-9.3.8.2.3-3`, `GR-9.3.8.2.3-6`)

**Mechanism.** OoConformance.DescriptionMismatch (the ONE identical-description comparator, pair mode) never compares three of the clauses rule 3/6 list: the alphabetic-vs-alphanumeric PICTURE distinction (PicInfo.IsAlphabetic is carried, EditMask is null for both), ALIGNED (DataItem.IsAligned) and DYNAMIC LENGTH (DataItem.IsDynamicLength) — so a non-conforming implementation or override compiles and runs

**Repro.** --std 2023, pinned cobol.exe: INTERFACE-ID. IR3A with METHOD-ID. M, LINKAGE `01 A PIC A(4).`, PROCEDURE DIVISION USING A; CLASS-ID. CR3A, OBJECT. IMPLEMENTS IR3A, METHOD-ID. M with `01 A PIC X(4).` USING A -> compiles, runs 'OK' (probe out/probe/oo/R3A). Same for `PIC 1(8) USAGE BIT ALIGNED` vs `PIC 1(8) USAGE BIT` (R3O), `PIC X DYNAMIC LENGTH` vs `PIC X` (R3N2), and RETURNING `PIC A(4)` vs `PIC X(4)` (R6A).

**Expected from the spec.** COBOLNET0841 on each: §9.3.8.2.3 rule 3 (and rule 6 for returning items) requires 'the same ALIGNED, ANY LENGTH, BLANK WHEN ZERO, DYNAMIC LENGTH, JUSTIFIED, PICTURE, SIGN, and USAGE clauses'; PIC A(4) and PIC X(4) are different PICTURE clauses, and ALIGNED / DYNAMIC LENGTH present on one side only are different clause sets, so the class does not conform to the interface (§9.3.11).

**Observed / reasoning.** DescriptionMismatch's Alphanumeric arm compares only JUSTIFIED, EditMask and Length; PictureAnalyzer sets EditMask only for edited pictures and records the A-picture as IsAlphabetic, which nothing in the comparator reads. No arm reads IsAligned or IsDynamicLength. The same comparator answers §14.8.2.3.2 BY REFERENCE INVOKE/CALL-AS-NESTED argument conformance and the universal-dispatch ConformanceDescriptor (whose locked invariant tracks DescriptionMismatch), so the sibling sweep must cover those askers too.

**Code site.** src/Cobol.Net.Compiler/Oo/OoConformance.cs#DescriptionMismatch (Alphanumeric / NumericEdited-National-Boolean arms); src/Cobol.Net.Compiler/Oo/OoConformance.cs#ConformanceDescriptor

### Finding — `conformance-for-object-orientation` (under-rejects; rows `GR-9.3.8.2.3-3`, `GR-9.3.8.2.3-6`)

**Mechanism.** The rule 3/6 exceptions a) and b) are not modeled: the numeric-edited identity is PictureAnalyzer.CanonicalCurrencyMask (currency SYMBOL canonicalized, currency STRING discarded) and the raw character-string for '.'/',' — so two currency symbols bound to DIFFERENT currency strings match, and a period/comma picture symbol matches across units where DECIMAL-POINT IS COMMA is in effect for only one

**Repro.** --std 2023: interface IR3CU3 with SPECIAL-NAMES `CURRENCY SIGN IS "EUR" WITH PICTURE SYMBOL "F".`, prototype M USING A, `01 A PIC F9.99.`; class CR3CU3 with `CURRENCY SIGN IS "GBP" WITH PICTURE SYMBOL "F".` implementing M with `01 A PIC F9.99.` -> compiles, runs 'OK' (R3CU3). Interface without DECIMAL-POINT IS COMMA, `01 A PIC Z9.99.`; class with `DECIMAL-POINT IS COMMA`, `01 A PIC Z9.99.` -> 'OK' (R3DP1). RETURNING twins R6E / R6B also 'OK'.

**Expected from the spec.** COBOLNET0841: §9.3.8.2.3 3) a) 'Currency symbols match if and only if the corresponding currency strings are the same' (EUR vs GBP -> no match); 3) b) 'Period picture symbols match if and only if the DECIMAL-POINT IS COMMA clause is in effect for both or for neither of these interfaces' (in effect for one -> no match). Controls that behave correctly today: F->EUR vs U->EUR accepted (R3CU1, correct), Z9,99 (DPC) vs Z9.99 (non-DPC) refused (R3DP3, correct), DPC in both accepted (R3DP2, correct).

**Observed / reasoning.** The Length compare catches only strings of different length (R3CU2 USD vs $ refused as 'length mismatch 7 vs 5'), which is an accident of width, not the rule. The comparator has no access to the owning unit's currency-string table or DPC setting; the fix belongs in the picture identity (carry the currency string per symbol and the DPC flag, or canonicalize to them) so every DescriptionMismatch asker inherits it — including §14.8.2.3.2, whose own identical-description rule has the same exceptions.

**Code site.** src/Cobol.Net.Compiler/Binding/PictureAnalyzer.cs#CanonicalCurrencyMask; src/Cobol.Net.Compiler/Oo/OoConformance.cs#DescriptionMismatch (EditMask compare)

### Finding — `objects-p1` (under-rejects; rows `GR-9.3.6-L3.3`)

**Mechanism.** The BY REFERENCE elementary identity check (DescriptionMismatch) compares a subset of the clause list in §9.3.6 3) e) and §14.8.2.3.2 rule 2: it ignores ALIGNED and DYNAMIC LENGTH, compares numeric-edited PICTUREs by EditMask text without the currency STRING, and ignores whether DECIMAL-POINT IS COMMA is in effect on each side. A class and a program in one compilation group each carry their own SPECIAL-NAMES, so all of these are observable at compile time

**Repro.** E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d2b\out\probe\objects-p1\cu.cob: the driver has `CURRENCY SIGN IS "L" WITH PICTURE SYMBOL "$"` and `01 E PIC $9.99`; the class method's formal is `01 P PIC $9.99` (default currency); `INVOKE R "M" USING E`. E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d2b\out\probe\objects-p1\dp2.cob: DECIMAL-POINT IS COMMA in the driver only, with `PIC 9,99` on both sides. E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d2b\out\probe\objects-p1\cl_AL.cob: argument `PIC 1(8) USAGE BIT ALIGNED` against formal `PIC 1(8) USAGE BIT`. E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d2b\out\probe\objects-p1\cl_DL.cob: argument `PIC X DYNAMIC LENGTH` against formal `PIC X`.

**Expected from the spec.** §9.3.6 3) e): the argument 'has the same ALIGNED, ANY LENGTH, BLANK WHEN ZERO, DYNAMIC LENGTH, JUSTIFIED, PICTURE, SIGN, and USAGE clauses', with 1. 'Currency symbols match if and only if the corresponding currency strings are the same' and 2. period/comma symbols matching only when DECIMAL-POINT IS COMMA is in effect for both or neither (§14.8.2.3.2 2) b)). No method matches, so for a typed receiver the §14.9.23.3 SR5 c) conformance failure is a compile-time diagnostic (COBOLNET0828), and at runtime it is §9.3.6 step 6.

**Observed / reasoning.** All four compile and run: cu -> P=L1.23, dp/dp2 -> P=1,23, cl_AL and cl_DL -> M-RAN. With a different-length currency string the pair is refused, but only because the LENGTHS differ. The universal ConformanceDescriptor has the same blind spots, since its N:/S: keys carry no currency, decimal-point, ALIGNED or DYNAMIC LENGTH axis. Sibling claim to correct: landed kb/Work PB427's note says the currency and DECIMAL-POINT exceptions 'are not OBSERVABLE at compile time' because a contained program cannot have its own CONFIGURATION SECTION. That is false for OO: a CLASS definition and a program in the same compilation group each have their own, as cu.cob and dp2.cob show. Sweep the §14.8.2.3.2 CALL (Format-2 / NESTED / prototype) lanes that share DescriptionMismatch.

**Code site.** src/Cobol.Net.Compiler/Oo/OoConformance.cs#DescriptionMismatch (NumericEdited/National/Boolean and Alphanumeric arms); src/Cobol.Net.Compiler/Oo/OoConformance.cs#ConformanceDescriptor

### Finding — `objects-p2` (under-rejects; rows `GR-9.3.6-L4.1`, `GR-9.3.6-L4.2`)

**Mechanism.** The BY REFERENCE identical-description check compares PICTURE text without the per-source-element context the rule names: the CURRENCY string and DECIMAL-POINT IS COMMA in effect on each side are never compared

**Repro.** (C2) Program SPECIAL-NAMES `CURRENCY SIGN IS "USD" WITH PICTURE SYMBOL "$"`, class SPECIAL-NAMES `CURRENCY SIGN IS "GBP" WITH PICTURE SYMBOL "$"`; argument `01 A PIC $999`, method formal `01 L PIC $999`; INVOKE O "M" USING A. (D1) Program with no DECIMAL-POINT IS COMMA, class with `DECIMAL-POINT IS COMMA`; argument and formal both `PIC 99.99`; INVOKE O "M" USING A (probe/objects-p2/gen3.py).

**Expected from the spec.** §9.3.6 match rule 3) e) 1./2. (and the identical §14.8.2.3.2 a)/b)): 'Currency symbols match if and only if the corresponding currency strings are the same'; 'Period picture symbols match if and only if the DECIMAL-POINT IS COMMA clause is in effect for both or for neither of these interfaces'. Neither pair matches, so the typed-receiver INVOKE must be rejected at compile time (the §14.8.2.3.2 conformance diagnostic, COBOLNET0828).

**Observed / reasoning.** Both compile clean and run (C2 'M:USD012', D1 'M:12.00'). OoConformance.DescriptionMismatch's numeric-edited arm compares Justified/BWZ/Usage/SignKind/EditMask text/LocaleEdit/Length - the LOCALE leg is fully source-context-aware (L1-L6 all right), but no currency-string or DPC fact rides the comparison. A pair whose text already differs (D2 99.99 vs 99,99; C1 USD vs $ by length) is refused for the wrong reason. The same function is the CALL lane's §14.8.2.3.2 check, so CALL BY REFERENCE across separately compiled/nested units with different CURRENCY/DPC is the sibling to sweep. No kb/Work note owns it.

**Code site.** src/Cobol.Net.Compiler/Oo/OoConformance.cs#DescriptionMismatch (numeric-edited arm, lines ~520-562)

### Finding — `polymorphism` (under-rejects; rows `GR-9.3.5.3-3`, `GR-9.3.5.3-4`)

**Mechanism.** signature-picture-context: OoConformance.DescriptionMismatch compares a PICTURE by its canonicalized EditMask text and never by the source-unit context that gives the symbols meaning -- neither PicInfo.CurrencyString nor the DECIMAL-POINT IS COMMA mode -- so across two classes (or a program and a class) with different SPECIAL-NAMES an override, an IMPLEMENTS pair and a BY REFERENCE INVOKE argument all pass as identical descriptions

**Repro.** out/probe/polymorphism/p2.cob: CLASS-ID PB2A with SPECIAL-NAMES DECIMAL-POINT IS COMMA, METHOD-ID M USING L1 PIC ZZ9.99; CLASS-ID PB2B INHERITS FROM PB2A (no SPECIAL-NAMES), METHOD-ID M OVERRIDE USING L1 PIC ZZ9.99 -> compiles and runs 'DONE'. p6.cob: base CURRENCY SIGN IS "E" WITH PICTURE SYMBOL "$" + PIC $$9.99, sub default currency + PIC $$9.99 -> compiles and runs. p10.cob (INVOKE sibling): program with CURRENCY "E" passes PIC $$9.99 BY REFERENCE to a default-currency method formal PIC $$9.99 -> runs 'M L1=E12.34'.

**Expected from the spec.** 9.3.8.2.3 3) a) 'Currency symbols match if and only if the corresponding currency strings are the same.' and 3) b) 'Period picture symbols match if and only if the DECIMAL-POINT IS COMMA clause is in effect for both or for neither of these interfaces'; 9.3.5.3 items 3) and 4) put both clauses in the method resolution signature, so 11.7.3 SR3 (no superclass method with the same signature) and SR9 (conformance) both fail -> a compile-time diagnostic on the overriding method (COBOLNET0829 family). 14.8.2.3.2 2) a)/b) give the same exceptions for the INVOKE BY REFERENCE argument -> diagnostic at the INVOKE. Controls that must stay accepted: DPC in both classes (p3), same currency string in both (p5).

**Observed / reasoning.** DescriptionMismatch's NumericEdited/Alphanumeric arms compare f.EditMask vs a.EditMask ordinally; PicInfo canonicalizes the currency symbol to '$' in EditMask and carries the string in CurrencyString, which no comparator reads; the comma mode is only passed to FloatMask.Parse at render time. PB427 already noted PicInfo is 'currency-canonicalized, decimal-point-resolved' but judged the 8.5.3.1 exceptions unobservable at compile time because contained programs cannot have a configuration section -- CLASS-IDs can, which is what makes this observable. One comparator serves three askers (override SR9, IMPLEMENTS 9.3.11, INVOKE 14.8.2.3.2) so the fix is one place: compare CurrencyString (when the picture has a currency symbol) and the DPC mode (when it has a period or comma symbol).

**Code site.** src/Cobol.Net.Compiler/Oo/OoConformance.cs#OoConformance.DescriptionMismatch (NumericEdited/National/Boolean arm and Alphanumeric arm)

### Finding — `polymorphism` (under-rejects; rows `GR-9.3.5.3-6`)

**Mechanism.** signature-clause-list: DescriptionMismatch does not compare the ALIGNED clause or the DYNAMIC LENGTH clause of an elementary formal/returning item, two of the eight clauses 9.3.5.3 6) and 9.3.8.2.3 3) require to be the same

**Repro.** out/probe/polymorphism/p11.cob: base METHOD-ID M USING L1 with 01 L1 PIC X DYNAMIC LENGTH; subclass M OVERRIDE with 01 L1 PIC X -> compiles, runs 'DONE'. p12.cob: base 01 L1 PIC 1(8) USAGE BIT ALIGNED; override 01 L1 PIC 1(8) USAGE BIT -> compiles, runs 'DONE'.

**Expected from the spec.** 9.3.8.2.3 3): the corresponding formal 'has the same ALIGNED, ANY LENGTH, BLANK WHEN ZERO, DYNAMIC LENGTH, JUSTIFIED, PICTURE, SIGN, and USAGE clauses' -> 11.7.3 SR9 violated (and SR3: signatures differ) -> compile-time diagnostic on the override. Same clause list in 14.8.2.3.2 2) for an INVOKE BY REFERENCE argument. Control: JUSTIFIED (p13) is already rejected COBOLNET0829.

**Observed / reasoning.** The elementary arms of DescriptionMismatch check ANY LENGTH, JUSTIFIED, BLANK WHEN ZERO, USAGE, SIGN and the PICTURE; ALIGNED and DYNAMIC LENGTH appear only inside StrongTypeMismatch's message text (the strongly-typed-group path), never as a comparison. Sweep the other askers of the same comparator (IMPLEMENTS, INVOKE BY REFERENCE) with the fix.

**Code site.** src/Cobol.Net.Compiler/Oo/OoConformance.cs#OoConformance.DescriptionMismatch

### Row `GR-9.3.5.3-5` — DIVERGES (editions 2002, 2014, 2023 (the OO module, and with it the method resolution signature, is a COBOL-2002 introduction -- Annex D.19.5.4.3 describes parametric polymorphism as part of the OO facility; none before 2002))

CONTEXT (applies to every 9.3.5.3 row): the seven items define the METHOD RESOLUTION SIGNATURE. The feature that DISTINGUISHES methods by it (overloading) is optional -- cite.py OK 9.3.5.3 7) 'Parametric polymorphism is an optional feature' -- and is NOT claimed (docs/CONFORMANCE.md 5 A.4.10 item 3; refused COBOLNET0822 in OoClassTable.Build, both instance and factory arms). But the signature is consumed by MANDATORY rules: cite.py OK 8.4.6.5 'The methods declared in an object class definition shall have unique method resolution signatures', OK 11.7.3 3) 'If the OVERRIDE phrase is specified, there shall be a method with the same method resolution signature', OK 11.7.3 9) (conformance of an inherited/implemented same-named method). inventory-schema.json derived-verdicts.oo-optional-items-only deliberately EXCLUDES these rows and asks for per-row adjudication, so no selector covers them. COBOL.NET's signature is the degenerate one (the NAME: roster key ExternalizedName) and the remaining components are enforced as SR9 conformance by OoConformance.ValidateOverrideSignatures -> MethodConformanceMismatches -> DescriptionMismatch (COBOLNET0829); for any program that does not overload, a component is conforming iff that comparator rejects every override the component distinguishes. Probes: out/probe/polymorphism/p*.cob, pinned cobol.exe, --std 2023 default. Rule: cite.py OK 9.3.5.3 5) 'The LOCALE clause'; consumer 9.3.8.2.3 3) 'Additionally, locale specifications in the PICTURE clauses match if and only if' same SIZE and same external identification. DescriptionMismatch compares format-2 picture text, Length (SIZE) and locale identity by EXTERNAL identification. Probe p7: base LOCALE LC1 IS "en-US", sub LOCALE LC1 IS "fr-FR", both PIC $9999 LOCALE IS LC1 SIZE IS 8 -> COBOLNET0829 'LOCALE mismatch' (correct: same locale-name, different external identification). Probe p8: LC1 IS "en-US" vs LC2 IS "en-US" -> accepted (correct: different names, same external identification). Both directions verified; no spec-derived test pins them. || REFUTED: DIVERGES — The consumer rule is cite.py OK 9.3.8.2.3 3) b) 'both specify the LOCALE phrase without a locale-name or both specify the LOCALE phrase with the same external identification, where the external identification is the external-locale-name or literal value associated with the locale-name'. Identity is therefore the LITERAL VALUE. The comparator instead uses LocaleSymbol.SameLocaleAs -> LocaleIdentification.SameLocale, which is OrdinalIgnoreCase over the L1-NORMALIZED tag. So different literal values that normalize to …
