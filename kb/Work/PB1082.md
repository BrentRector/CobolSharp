---
title: "PB1082 — `ResolveProgramCollating` validates PROGRAM COLLATING SEQUENCE alphabet-name-1 only against the NATIONAL alphabet registry, so an undeclared word, a data-name or a class-name is accepted silently and leaves the program on the native order at every edition (its alphabet-name-2 sibling slot refuses the undeclared case — the two-arm shape)"
id: PB1082
kind: defect
status: open
severity: MINOR
area: binder/special-names
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1074"]
spec_refs: ["12.3.6.3"]
inventory_rows: ["SR-12.3.6.3-1"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 04:03 PDT by **REGISTRAR 14a** from adjudication batch d1-14a (pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probes under `scratchpad\adj-d1\out\probe\`). Not fixed by trains 58/59 (their src diff was checked).

# PB1082 — OPEN — `ResolveProgramCollating` validates PROGRAM COLLATING SEQUENCE alphabet-name-1 only against the NATIONAL alphabet registry, so an undeclared word, a data-name o

## Rule text (every citation re-run by the registrar)
- `python scripts/spec/cite.py --check 12.3.6.3 "Alphabet-name-1 shall reference an alphabet that defines an alphanumeric collating sequence"` → `OK  §12.3.6.3 1)  (Syntax rules)`

### Adjudicator finding (object-computer-paragraph, harm under-rejects)
- **Mechanism:** ResolveProgramCollating validates alphabet-name-1 only against the NATIONAL alphabet registry: any name that is not a FOR NATIONAL alphabet but also not a declared alphanumeric alphabet (undeclared word, data-name, class-name, ...) is silently accepted and leaves the program on the native order ('the historical 85-surface leniency'), at every edition including strict --std 2023 and --std 85.
- **Repro:** OBJECT-COMPUTER. X PROGRAM COLLATING SEQUENCE IS NOSUCH.  (no SPECIAL-NAMES ALPHABET NOSUCH)  then IF "A" < "B" ... — compiles clean and runs under the native order; same for `IS WS-A` (a WORKING-STORAGE data-name) and `IS HEXD` (a SPECIAL-NAMES CLASS name). Probes sr1a/sr1b/sr1d.cob under E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d1\out\probe\object-computer-paragraph.
- **Expected (spec):** §12.3.6.3 SR1: "Alphabet-name-1 shall reference an alphabet that defines an alphanumeric collating sequence." A word that is not an alphanumeric alphabet-name violates a syntax rule and shall be diagnosed (COBOLNET0898, the diagnostic the national-alphabet arm and the SR2 undeclared arm already use).
- **Observed:** DataBinder.Switches.cs ResolveProgramCollating: `if (Alphabets.TryGetValue(alnumName, ...)) ... else if (NationalAlphabets.ContainsKey(alnumName)) Error(...); // else: an undeclared name-1 stays inert`. The SR2 slot in the same method rejects the undeclared case, so this is the two-arm-dispatch shape (one slot strict, its sibling lenient). The leniency's stated witness (NC219A) declares its PCS alphabet; every NIST program on disk writing PROGRAM COLLATING SEQUENCE IS declares the name, so rejecting breaks no corpus program.
- **Code site:** src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs#ResolveProgramCollating (the `else: an undeclared name-1 stays inert` arm)
- **Clause (adjudicator):** ISO/IEC 1989:2023 §12.3.6.3 SR1

## What the fixer owes
Refuse the `else` arm (COBOLNET0898, as the SR2 slot does). The stated NC219A witness declares its alphabet; every NIST program writing PROGRAM COLLATING SEQUENCE declares the name, so no corpus program breaks — confirm with the corpus leg. Same rule shape as PB1074's eager file-level alphabet validation: share the helper.

Rows claimed: SR-12.3.6.3-1. Flip `status` and write `closes_rows` in the landing commit (CLAUDE.md rule 8).
