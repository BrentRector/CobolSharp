---
title: "PB1425 — the identifier grammar's objectReference tier is narrower than §8.4.3.1: identifier Format 5 (object-view `identifier AS {class|interface|UNIVERSAL}`) has no grammar or binder surface, Format 6's qualified `object-class-name OF SUPER` has no alternative, a Format-4 inline invocation is not admitted in object-reference-only positions (SET Format 5, INVOKE identifier-1), and Format 7's `property-name OF identifier-3` binds only a bare-word qualifier — SR1's 'any identifier in any identifier slot' fails for all four"
id: PB1425
kind: defect
status: open
severity: MAJOR
area: frontend/grammar
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1197", "PB1136", "PB1430", "PB1448", "PB1449", "PB1450", "PB1275", "PB1137", "PB285"]
spec_refs: ["8.4.3.1.3", "8.4.3.1.2", "8.4.3.5"]
inventory_rows: ["SR-8.4.3.1.3-6", "FMT-8.4.3.1.2", "GR-8.4.3.1.4-1", "SR-8.4.3.1.3-7", "GR-8.4.3.4.4-1", "SR-8.4.3.1.3-8", "SR-8.4.3.1.3-1", "FMT-8.4.3.9.2", "FMT-8.4.3.5.2", "GR-8.4.3.5.4-1", "GR-8.4.3.5.4-2", "GR-8.4.3.5.4-3", "GR-8.4.3.5.4-4", "GR-8.4.3.5.4-5", "GR-8.4.3.5.4-6", "GR-8.4.3.5.4-7", "SR-8.4.3.5.3-1", "SR-8.4.3.5.3-2", "FMT-8.4.3.8.2", "SR-8.4.3.8.3-4", "SR-8.4.3.8.3-6", "GR-8.4.3.8.4-4", "SR-8.4.3.8.3-3", "SR-8.4.3.8.3-7", "SR-8.4.3.8.3-2"]
closes_rows: []
tags: [cobolsharp, work, defect]
---
Filed 2026-09-24 by **REGISTRAR 15b** from lane-3 adjudication batch **15b** (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d2` @`071ff04ea`). Not re-probed by the registrar (MANDATORY-PRACTICES R2: every lead carries its repro and code site); origin/main since the pin carries only registrar landings (no src change), so nothing landed has touched this mechanism.

# PB1425 — OPEN

## The rule (every citation re-run by the registrar through `cite.py --check`)

**SR-8.4.3.1.3-6**

> Object-view-1 is defined by 8.4.3.5, Object-view. …

`python scripts/spec/cite.py --check 8.4.3.1.3 "Object-view-1 is defined by 8.4.3.5, Object-view."` → `OK  §8.4.3.1.3 6)  (Syntax rules)`

**GR-8.4.3.1.4-1**

> The order in which the various components of an identifier are applied is as …

`python scripts/spec/cite.py --check 8.4.3.1.4 "The order in which the various components of an identifier are applied is as"` → `OK  §8.4.3.1.4 1)  (General rules)`

**GR-8.4.3.4.4-1**

> An inline method invocation references a temporary data item with the same class, category …

`python scripts/spec/cite.py --check 8.4.3.4.4 "An inline method invocation references a temporary data item with the same class, category"` → `OK  §8.4.3.4.4 1)  (General rules)`

## Finding — `identifier` (rejects-legal-source; rows `SR-8.4.3.1.3-6`, `FMT-8.4.3.1.2`, `GR-8.4.3.1.4-1`)

**Mechanism.** Identifier Format 5 (object-view, 8.4.3.5) has no grammar or binder surface; `identifier AS {class|interface|UNIVERSAL}` is refused with a misleading reserved-word diagnostic

**Repro.** A class KDER INHERITS FROM KBASE; in a program: 01 D USAGE OBJECT REFERENCE KDER. 01 U USAGE OBJECT REFERENCE. ... INVOKE KDER "NEW" RETURNING D. SET U TO D AS UNIVERSAL.  (or INVOKE D AS KBASE "SPEAK"). cobol --std 2023 (also 2002/2014).

**Expected from the spec.** 8.4.3.1.2 Format 5 + 8.4.3.5.2: `D AS UNIVERSAL` is an identifier treated as USAGE OBJECT REFERENCE with no class (8.4.3.5.4 GR7), so the SET compiles and U references D's object; `D AS KBASE` is treated as OBJECT REFERENCE KBASE with a runtime conformance check that sets EC-OO-CONFORMANCE on failure (GR2). The source is legal and must compile.

**Observed / reasoning.** error COBOLNET0901: 'AS' is a reserved word in COBOL-2023 and cannot be used as a user-defined word (ISO §8.9) (plus COBOL0301 for the INVOKE form). No objectView rule exists in src/Cobol.Net.Frontend/Grammar and EC-OO-CONFORMANCE has no raise site; object-view is not an A.4.10 optional item, so this is mandatory 2002+ OO core. The ordering component 8.4.3.1.4 GR1 c) therefore does not exist either. Previously recorded only in docs/rearchitecture/PHASE-13-plan-vs-spec-review.md R19; no kb/Work note owns it.

**Code site.** src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4#objectReference (no AS-view tier); src/Cobol.Net.Frontend/Grammar/Core/CobolOO.g4

## Finding — `identifier` (rejects-legal-source; rows `SR-8.4.3.1.3-7`, `FMT-8.4.3.1.2`)

**Mechanism.** Predefined-object Format 6's qualified SUPER arm `object-class-name-1 OF SUPER` has no grammar alternative; objectReference admits only bare SUPER

**Repro.** CLASS-ID. KDER INHERITS FROM KBASE. (REPOSITORY CLASS KBASE.) OBJECT. PROCEDURE DIVISION. METHOD-ID. TALK. PROCEDURE DIVISION. INVOKE KBASE OF SUPER "SPEAK". END METHOD TALK. -- with KBASE defining method SPEAK. cobol --std 2002 (2014, 2023 identical).

**Expected from the spec.** 8.4.3.8.2 prints `[ object-class-name-1 OF ] SUPER`; 8.4.3.8.3 SR6: with one INHERITS class 'object-class-name-1 may be specified', SR4 requires it to be that class. The INVOKE compiles and GR4 restricts method search to KBASE: the program displays KBASE's SPEAK output.

**Observed / reasoning.** error COBOLNET0901: 'SUPER' is a reserved word in COBOL-2002 and cannot be used as a user-defined word (ISO §8.9) - the parser reads `KBASE OF SUPER` as a data-name qualified by a user word. objectReference is `dataReference | NULL_ | SELF | SUPER` with no qualified arm. The mandatory-qualifier case (SR5) needs multiple class inheritance, which A.4.10 item 1 declines, but the single-parent optional form is core. No kb/Work note owns it (DESIGN-SPEC-RECONCILIATION.md row 43 mentions it as deferred prose only).

**Code site.** src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4#objectReference

## Finding — `inline-method-invocation` (rejects-legal-source; rows `GR-8.4.3.4.4-1`)

**Mechanism.** Object-reference-only operand positions (SET Format 5 identifier-4, INVOKE identifier-1) do not admit a Format-4 identifier, so an inline invocation whose GR1 b) temp is an object reference is refused; the functionCall-pairing drift test cannot see these positions because no function returns an object reference

**Repro.** class PRBC method ME: 01 LK-ME USAGE OBJECT REFERENCE PRBC. PROCEDURE DIVISION RETURNING LK-ME. SET LK-ME TO SELF. Program: (a) SET U1 TO A1 :: "ME". -> probe p14_setinline; (b) INVOKE A1 :: "ME" "GETNAME" RETURNING W. -> probe p16_invokerecv

**Expected from the spec.** §8.4.3.4.4 GR1: the inline invocation references a temporary data item with the description of the method's RETURNING item (an object reference described with PRBC); §8.4.3.1.3 SR5 makes it an identifier. §14.9.39.3 SR9 'Identifier-4 shall be an object reference' and §14.9.23.3 SR1 'Identifier-1 shall be an object reference' are satisfied: (a) compiles and U1 references A1's object; (b) compiles and W = 'ACCOUNT '.

**Observed / reasoning.** (a) COBOLNET0867 'identifier-4 shall be an object reference ... never a literal or an arithmetic expression'; (b) COBOL0001 unexpected '::' (INVOKE's objectReference rule is dataReference|NULL|SELF|SUPER). Sweep every object-reference operand position (SET Format 5/7 sending, INVOKE receiver, object-view, relation already works).

**Code site.** SET object-reference binder emitting COBOLNET0867 (SetBinder); src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4#objectReference; tests/Cobol.Net.Tests.Unit/InlineMethodInvocationOperandDriftTests.cs

## Measured (rows this note claims beyond the findings above)

### `SR-8.4.3.1.3-8` — PARTIAL (adjudicator + refuter record, verbatim)

SR8 (cite.py --check 8.4.3.1.3 8) OK): Format 7 is the 8.4.3.9 object property `property-name-1 OF identifier-1`. It parses as a qualified dataReference and ReferenceResolver's object-property binder re-reads it when the qualifier is an object reference (REPOSITORY PROPERTY gate SR1, universal-reference refusal SR2); OoBinder desugars GET/SET per GR1-GR3. Probe o2.cob: `MOVE NM OF D TO W` -> ABCDEF; `NM OF D (2:3)` -> BCD. pb987 golden cites 8.4.3.9. Probes under out/probe/ident with the pinned cobol.exe. || REFUTED: PARTIAL — Format 7 is `property-name-1 OF identifier-3`, and SR1 lets identifier-3 be any identifier. OoTryBindPropertyReference binds only a single bare-word qualifier (a class-name or a typed object reference). A qualified, subscripted object reference is refused as 'not defined'. The bare form works (the adjudicator's probe and my control).

### `SR-8.4.3.1.3-1` — DIVERGES (adjudicator + refuter record, verbatim)

SR1 (cite.py --check 8.4.3.1.3 1) OK): any identifier slot of an identifier format admits any identifier format. Probes p1.cob: function-identifier inside a function argument that is itself ref-modified `FUNCTION UPPER-CASE (FUNCTION LOWER-CASE (A OF G (N))(1:3))` -> G2G; function-identifier in a ref-mod leftmost position `W (FUNCTION LENGTH (W) - 1 : 2)` -> ef; function-identifier as a subscript `A OF G (FUNCTION INTEGER (N))(1:1)` -> G; ref-mod identifier as a function argument -> C. Inline invocation chained on its own temporary is pinned by the spec-derived golden pb428 item 6, which cites this rule. Recursion through Format 5 is impossible only because Format 5 does not exist - that defect belongs to SR-8.4.3.1.3-6, not to this rule. Probes under out/probe/ident with the pinned cobol.exe. || REFUTED: DIVERGES — SR1 makes every identifier slot inside another identifier format accept ANY identifier format. The adjudicator probed only function, subscript and ref-mod nesting. Two other nestings reject legal source: an object property whose identifier-3 is a qualified, subscripted object reference (Format 7 over Format 2), and a data-address-identifier whose identifier-1 is reference-modified (Format 9 over Format 3). Format 5 is also absent, which is SR6's defect.

## What the fixer owes

Make objectReference = the identifier formats that YIELD an object reference (data reference, inline invocation, object view, predefined object incl. `class OF SUPER`, object property), one rule used by every object-reference position (INVOKE, SET, RAISE — PB1197, PB1136), with the object-view binder per §8.4.3.5; OoTryBindPropertyReference resolves a general identifier-3.

## Folded 2026-09-24 by REGISTRAR 15c from its draft PB1425 (lane-3 batch d2-15c, pinned tree `071ff04ea`)

**Why here.** the identifier grammar's objectReference tier is narrower than §8.4.3.1 — 15c adds its own measurements of the same four gaps: Format 7's bare-word property receiver (PB1425's draft), Format 5 object-view (PB1425's), Format 6 `x OF SUPER` with its SR4/GR4 (PB1425's), and SELF/SUPER refused in relation conditions, INVOKE arguments and as a property object, with §8.4.3.8.3 SR2 then needing a rule-specific receiving-operand diagnostic (PB1425's).

**The draft's title.** the object-property binder accepts only a bare single-word receiver: ReferenceResolver.OoTryBindPropertyReference returns null unless qualifiers.Count == 1 and that word is a class-name or an unqualified, unsubscripted typed object reference, so every other identifier-1 form §8.4.3.9.2 admits (qualified, subscripted, SELF/SUPER, a chained property, an inline invocation, an object-view) falls to 'not defined' or a diagnostic naming a compiler temp

### The rule (every citation re-run by the registrar through `cite.py --check`)

> may be any of the formats for an identifier

`python scripts/spec/cite.py --check 8.4.3.1.3 "…"` → `OK  §8.4.3.1.3 1)  (Syntax rules)`

### Finding — `object-property` (rejects-legal-source; rows `FMT-8.4.3.9.2`)

**Mechanism.** The object-property binder accepts only a bare single-word receiver: ReferenceResolver.OoTryBindPropertyReference returns null unless qualifiers.Count == 1 and that word is a class-name or an unqualified, unsubscripted typed object-reference data-name, so every other identifier-1 form §8.4.3.1 admits (qualified, subscripted, SELF/SUPER, a chained property, an inline invocation, an object-view) falls to the generic 'not defined' diagnosis

**Repro.** pinned cobol.exe --std 2002: E:/Temp/claude/E--CobolSharp/a700c052-f9e4-4942-84ff-a13c7505d6ec/scratchpad/adj-d2/out/probe/objprop/p7.cob (method SHOW in class CPA7 with REPOSITORY PROPERTY BAL: `DISPLAY BAL OF SELF.`) -> COBOLNET0901 'SELF is a reserved word'; E:/Temp/claude/E--CobolSharp/a700c052-f9e4-4942-84ff-a13c7505d6ec/scratchpad/adj-d2/out/probe/objprop/p6.cob (01 TT TYPEDEF STRONG. 05 A USAGE OBJECT REFERENCE CPA6 OCCURS 2. 01 T TYPE TT. ... `MOVE 7 TO BAL OF A OF T(2)`) -> COBOLNET1639; p1.cob `BAL OF A(2)` -> COBOLNET2096 naming the compiler temp '__PROP-TEMP-200002'; p8.cob `DISPLAY BAL OF INNER OF B` (INNER an object-reference PROPERTY) -> COBOLNET1639

**Expected from the spec.** All compile and run: p7 displays 00100; p6 displays 00007. §8.4.3.9.2 identifier-1 is any identifier (§8.4.3.1.3 SR1 recursive definition), §8.4.3.1.4 GR1 d) applies OF to 'the identifier on the right' after qualification/subscripting (a), and §8.4.3.8.3 SR3 names the object-property identifier as a legal home of SUPER (SELF likewise, SR7: not universal).

**Observed / reasoning.** Line 96 `if (qualifiers.Count != 1 || ...) return null;` then `data.Symbols.TryResolve(recv, ...)` on the single word; subscripts on the reference are left for the ordinary resolver, which applies them to the returned temp. SELF/SUPER never reach this path (not a qualifier word).

**Code site.** src/Cobol.Net.Compiler/Binding/ReferenceResolver.cs#OoTryBindPropertyReference (:93-:163)

### What the fixer owes

Resolve identifier-1 through the ordinary identifier resolver (qualification, subscripts, then OF property) instead of a one-word special case, so every identifier format is automatic; SELF/SUPER arrive with PB1425 (identifier Format 6). COBOLNET2096 must never name `__PROP-TEMP-…`. Goldens p1/p6/p7/p8 from the repro.

## Folded 2026-09-24 by REGISTRAR 15c from its draft PB1425 (lane-3 batch d2-15c, pinned tree `071ff04ea`)

**Why here.** the identifier grammar's objectReference tier is narrower than §8.4.3.1 — 15c adds its own measurements of the same four gaps: Format 7's bare-word property receiver (PB1425's draft), Format 5 object-view (PB1425's), Format 6 `x OF SUPER` with its SR4/GR4 (PB1425's), and SELF/SUPER refused in relation conditions, INVOKE arguments and as a property object, with §8.4.3.8.3 SR2 then needing a rule-specific receiving-operand diagnostic (PB1425's).

**The draft's title.** the object-view (identifier Format 5, `identifier-1 AS {class [FACTORY OF / ONLY] | interface | UNIVERSAL}`) has no grammar production, no UNIVERSAL token, no binder re-description and no EC-OO-CONFORMANCE raise site, so it is a parse error (COBOLNET0901 on AS) in every position

### The rule (every citation re-run by the registrar through `cite.py --check`)

> This reference of identifier-1 is treated at compile-time as though it had the description specified by the AS phrase

`python scripts/spec/cite.py --check 8.4.3.5.4 "…"` → `OK  §8.4.3.5.4 1)  (General rules)`

> the EC-OO-CONFORMANCE exception condition is set to exist

`python scripts/spec/cite.py --check 8.4.3.5.4 "…"` → `OK  §8.4.3.5.4 2)  (General rules)`

### Finding — `object-view` (rejects-legal-source; rows `FMT-8.4.3.5.2`, `GR-8.4.3.5.4-1`, `GR-8.4.3.5.4-2`, `GR-8.4.3.5.4-3`, `GR-8.4.3.5.4-4`, `GR-8.4.3.5.4-5`, `GR-8.4.3.5.4-6`, `GR-8.4.3.5.4-7`, `SR-8.4.3.5.3-1`, `SR-8.4.3.5.3-2`)

**Mechanism.** The object-view (identifier Format 5, §8.4.3.1.2 / §8.4.3.5) has no grammar production, no UNIVERSAL token, no binder re-description and no EC-OO-CONFORMANCE raise site, so `identifier-1 AS {class [FACTORY OF/ONLY] | interface | UNIVERSAL}` is a hard compile error in every position although OO is claimed mandatory surface.

**Repro.** probe/object-view/OV1.cob (--std 2023): 01 U USAGE OBJECT REFERENCE. 01 B USAGE OBJECT REFERENCE OVB. ... INVOKE OVD "NEW" RETURNING D. SET U TO D. SET B TO U AS OVB. INVOKE B "SPEAK". -> COBOLNET0901 'AS' is a reserved word ... (also OV2 'INVOKE U AS OVD "SPEAK"', OV3 'SET D TO B AS UNIVERSAL').

**Expected from the spec.** §8.4.3.5.4 GR1/GR2: U AS OVB is treated at compile time as USAGE OBJECT REFERENCE OVB; the object is an OVD (subclass), so no EC-OO-CONFORMANCE; program compiles and prints DERIVED (OV1), DERIVED (OV2); an object not of the class/subclass sets EC-OO-CONFORMANCE (fatal, §14 exception table). §8.4.3.5.3 SR1 (SUPER/NULL, non-object identifier) and SR2 (object-view as receiving operand, e.g. OV4 'SET U AS OVB TO D') must be refused by rule-specific diagnostics.

**Observed / reasoning.** All three probes refused at parse with COBOLNET0901 on 'AS'; grep of src for object-view/UNIVERSAL finds no production; ExceptionCatalog.cs:166 declares EC-OO-CONFORMANCE but nothing raises it. SR1/SR2 are only incidentally 'enforced' because the whole construct is unparsable.

**Code site.** src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4#dataReference (line 673, no AS arm); src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4 (no UNIVERSAL token); src/Cobol.Net.Runtime/Exceptions/ExceptionCatalog.cs:166 (EC-OO-CONFORMANCE never raised)

### What the fixer owes

Implement FROM docs/rearchitecture's OO deep-dive: one identifier alternative (so INVOKE receivers, SET senders, arguments, RAISE — PB1197 — all get it), a UNIVERSAL token, a binder re-description of identifier-1, a run-time conformance check raising EC-OO-CONFORMANCE (declared at ExceptionCatalog.cs:166, raised nowhere), and rule-specific diagnostics for SR1 (SUPER/NULL/non-object identifier-1) and SR2 (object-view as a receiving operand). Edition gate at 2002 (constructs.json row + COBOLNET0900 below). Goldens OV1–OV3 plus SR1/SR2 negatives.

## Folded 2026-09-24 by REGISTRAR 15c from its draft PB1425 (lane-3 batch d2-15c, pinned tree `071ff04ea`)

**Why here.** the identifier grammar's objectReference tier is narrower than §8.4.3.1 — 15c adds its own measurements of the same four gaps: Format 7's bare-word property receiver (PB1425's draft), Format 5 object-view (PB1425's), Format 6 `x OF SUPER` with its SR4/GR4 (PB1425's), and SELF/SUPER refused in relation conditions, INVOKE arguments and as a property object, with §8.4.3.8.3 SR2 then needing a rule-specific receiving-operand diagnostic (PB1425's).

**The draft's title.** `object-class-name-1 OF SUPER` has no grammar surface — objectReference admits a bare SUPER only — so the qualified SUPER form, its §8.4.3.8.3 SR4 INHERITS check and its GR4 restricted method search do not exist: `INVOKE PSOFA OF SUPER 'SPEAK'` is COBOLNET0901

### The rule (every citation re-run by the registrar through `cite.py --check`)

> If the INHERITS clause of the containing class definition specifies only one object-class-name, object-class-name-1 may be specified

`python scripts/spec/cite.py --check 8.4.3.8.3 "…"` → `OK  §8.4.3.8.3 6)  (Syntax rules)`

### Finding — `self-and-super` (rejects-legal-source; rows `FMT-8.4.3.8.2`, `SR-8.4.3.8.3-4`, `SR-8.4.3.8.3-6`, `GR-8.4.3.8.4-4`)

**Mechanism.** F1 - `object-class-name-1 OF SUPER` has no grammar surface: objectReference admits a bare SUPER only, so the qualified SUPER form, its SR4 INHERITS check and its GR4 restricted method search do not exist

**Repro.** E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d2\out\probe\self-and-super\PSOF.cob: CLASS-ID. PSOFB INHERITS FROM PSOFA; method SPEAK OVERRIDE does `INVOKE PSOFA OF SUPER \"SPEAK\"` then DISPLAY \"DOG\"; the program does INVOKE PSOFB \"NEW\" RETURNING D / INVOKE D \"SPEAK\". `cobol PSOF.cob --std 2023 --run` (also 2002/2014).

**Expected from the spec.** Legal with a single base (§8.4.3.8.3 SR6 'If the INHERITS clause of the containing class definition specifies only one object-class-name, object-class-name-1 may be specified'); GR4 restricts the search to PSOFA's methods, so the output is ANIMAL then DOG. PSBAD.cob (`INVOKE PSBADB OF SUPER`, naming the containing class rather than an INHERITS class) shall be rejected with an SR4 diagnostic.

**Observed / reasoning.** Both PSOF.cob and PSBAD.cob fail identically with COBOLNET0901 "'SUPER' is a reserved word in COBOL-2023 and cannot be used as a user-defined word (ISO §8.9)" - the parser takes `PSOFA OF` as a qualified dataReference and hits the reserved token. grep of every .g4 finds SUPER only in objectReference, the lexer and CobolWords; no OF-SUPER production or binder arm exists in src. The negative fixture tests/conformance/negative/oo-multi-base-super.cob and landed kb/Work PB285 both name this gap in prose, but no open kb/Work note owns the rows. A.4.10 item 1 (multi-base CLASS-ID) is declined, but SR6's single-base case is inside the claimed OO core.

**Code site.** src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4#objectReference; src/Cobol.Net.Compiler/Binding/Procedure/Verbs/OoBinder.cs#OoBindByReceiver (searchRoot selection: isSuper -> cur.Base only)

### What the fixer owes

Add the `object-class-name-1 OF SUPER` alternative with the SR4 check (object-class-name-1 names a class in the containing class's INHERITS clause) and GR4's restricted search in OoBinder.OoBindByReceiver. Multi-base CLASS-ID stays declined (A.4.10 item 1); single-base is claimed. Goldens PSOF (ANIMAL then DOG) and PSBAD (SR4 negative); tests/conformance/negative/oo-multi-base-super.cob names the gap in prose.

## Folded 2026-09-24 by REGISTRAR 15c from its draft PB1425 (lane-3 batch d2-15c, pinned tree `071ff04ea`)

**Why here.** the identifier grammar's objectReference tier is narrower than §8.4.3.1 — 15c adds its own measurements of the same four gaps: Format 7's bare-word property receiver (PB1425's draft), Format 5 object-view (PB1425's), Format 6 `x OF SUPER` with its SR4/GR4 (PB1425's), and SELF/SUPER refused in relation conditions, INVOKE arguments and as a property object, with §8.4.3.8.3 SR2 then needing a rule-specific receiving-operand diagnostic (PB1425's).

**The draft's title.** SELF and SUPER (identifier Format 6) are admitted only as an INVOKE / inline receiver, a SET Format 5 sender and a RAISE operand — `objectReference` is reached from four rules and never from the identifier rule — so the legal object-class positions refuse them (IF O = SELF, BY CONTENT/VALUE SELF, BAL OF SELF / OF SUPER), and §8.4.3.8.3 SR2 (never a receiving operand) is 'enforced' only by that same accident with a reserved-word message

### The rule (every citation re-run by the registrar through `cite.py --check`)

> An operand of class object may be compared with another operand of class object

`python scripts/spec/cite.py --check 8.8.4.2.15 "…"` → `OK  §8.8.4.2.15   (Comparisons of operands of class object)`

> This identifier format shall not be specified as a receiving operand

`python scripts/spec/cite.py --check 8.4.3.8.3 "…"` → `OK  §8.4.3.8.3 2)  (Syntax rules)`

### Finding — `self-and-super` (rejects-legal-source; rows `FMT-8.4.3.8.2`, `SR-8.4.3.8.3-3`, `SR-8.4.3.8.3-7`)

**Mechanism.** F2 - SELF/SUPER (identifier Format 6, predefined-object) are admitted only as an INVOKE/inline receiver, a SET Format 5 sender and a RAISE operand; they are not identifiers anywhere else, so the legal object-class positions reject them: relation conditions, INVOKE USING BY CONTENT/VALUE arguments, and the object of an object-property identifier

**Repro.** E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d2\out\probe\self-and-super\PSCMP.cob (inside a method: `SET O TO SELF` / `IF O = SELF DISPLAY \"EQ\" END-IF`), PSCMP2.cob (`IF SELF = O`), PSUSEC.cob (`INVOKE SELF \"TAKE\" USING BY CONTENT SELF`, TAKE has LINKAGE `01 P USAGE OBJECT REFERENCE`), PSUSEV.cob (BY VALUE SELF), PSSELFU.cob (`USING SELF`), PPROP.cob / PPROP2.cob (`DISPLAY BAL OF SELF` / `DISPLAY BAL OF SUPER`, BAL a PROPERTY data item of the base, REPOSITORY PROPERTY BAL). `cobol <f> --std 2023 --run`.

**Expected from the spec.** §8.4.3.1.2 lists `{EXCEPTION-OBJECT | NULL | SELF | [object-class-name-1 OF] SUPER}` as identifier Format 6; §8.4.3.8.3 SR7 describes SELF/SUPER as class object, category object reference; §8.8.4.2.15 'An operand of class object may be compared with another operand of class object' so IF O = SELF is legal and true after SET O TO SELF (prints EQ); SELF as a BY CONTENT/VALUE argument to a universal object-reference parameter is legal (prints TOOK then DOG); §8.4.3.8.3 SR3 names 'the object in an object-property identifier' as a SUPER position and §8.4.3.9.3 SR2 excludes only universal and NULL, so `BAL OF SELF` / `BAL OF SUPER` compile and display 00100. SR1 (method-only) and SR2 (never receiving) must then be enforced at each new position.

**Observed / reasoning.** IF O = SELF -> COBOL0001 'cannot parse construct near DISPLAY'; IF SELF = O -> COBOL0001 near '='; USING SELF -> COBOLNET0901 'SELF is a reserved word'; BY CONTENT SELF / BY VALUE SELF -> COBOL0001; BAL OF SELF / BAL OF SUPER -> COBOLNET0901. Only `objectReference` names the SELF/SUPER tokens, and it is referenced solely by invokeTarget, inlineMethodInvocation, setObjectReferenceStatement and raiseStatement - the dataReference/identifier rule never reaches it. (IF O = NULL does parse and run - PSNULLC - so the relation-condition gap is SELF/SUPER-specific.)

**Code site.** src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4#objectReference (four referencing rules only); src/Cobol.Net.Frontend/Grammar/Core/CobolOO.g4#invokeArgument; the relation-condition operand and object-property reference paths (ReferenceResolver.OoTryBindPropertyReference)

### Refuter evidence — row `SR-8.4.3.8.3-2` (PARTIAL)

PARTIAL — No SR2 mechanism exists. Nine receiving-position probes each fail with COBOLNET0901 "'SELF' is a reserved word ... cannot be used as a user-defined word (ISO §8.9)": SET SELF TO NULL, RETURNING SELF, INITIALIZE SELF, SET SELF O / O SELF TO NULL, MOVE .. TO SELF, SET ADDRESS OF SELF, SET SUPER TO NULL and ACCEPT SELF. The source is rejected only because SELF/SUPER are not identifiers at all, which is the rejects-legal-source defect F2. The stated reason is also false: the source does not use SELF as a user-defined word, it uses a predefined-object identifier as a receiving operand. Nothing tests it. When F2 is fixed by admitting SELF/SUPER as identifier Format 6, every one of these positions would start compiling without a diagnostic. A verdict that depends on a separate defect staying unfixed, with no rule-owned diagnostic, is not CONFORMS.

**Code site.** src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4#objectReference; src/Cobol.Net.Compiler/Binding/Procedure/Verbs/SetFormatSelection.cs

### What the fixer owes

Make the predefined-object references (EXCEPTION-OBJECT, NULL, SELF, [x OF] SUPER) ONE identifier alternative so every sending position is automatic, and add a rule-specific SR2 diagnostic for every receiving position (SET SELF TO NULL, RETURNING SELF, INITIALIZE SELF, MOVE … TO SELF, ACCEPT SELF, SET ADDRESS OF SELF) — when the identifier arm lands those positions would otherwise start compiling silently. PB1137 owns BY CONTENT SELF on the INVOKE argument lane; PB1425 the property receiver. Goldens PSCMP (EQ), PSUSEC/PSUSEV, PPROP/PPROP2 + the nine SR2 negatives.

**Repro added 2026-09-24 (golden-lane-2 misc-p23 writer, pin f0d582ccd):** `IF O = SELF` inside a factory method
(`SET O TO SELF` first) is COBOL0001 — §8.8.4.2.15 permits comparing two object operands and its NOTE names the
predefined object references. Source: scratchpad 9db39f61…\gl\run\misc-p23\probe\cmp.cob.
