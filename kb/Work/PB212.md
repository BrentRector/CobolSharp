---
title: "PB212 — the SET RECEIVER has no class screen: §14.9.39.3 SR1 requires \"a data item of class index or an integer data item\", SetBinder.SetTargetOf applies none, and `SET WS-X TO 5` with WS-X PIC X(4) compiles clean — the receiving-side twin of a screen the SENDING side has had for a month"
id: PB212
kind: defect
status: open
severity: MINOR
area: binding
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: [PB922, PB388]
spec_refs: ["14.9.39.3"]
inventory_rows: ["SR-14.9.39.3-1", "SR-14.9.39.3-3", "SR-14.9.39.3-4", "SR-14.9.39.3-2"]
tags: [cobolsharp, work, defect]
---

# PB212

Found by the PB169–PB172 burn-down cluster's funnel-entry sweep (2026-08-31). ISO §14.9.39.3 syntax rule 1
(`cite.py --check 14.9.39.3 "Identifier-1 shall reference a data item of class index or an integer data item"`
→ OK) constrains the SET *receiver*; `SetBinder.SetTargetOf` applies no class rule at all.

⛔ IT IS THE TWO-ARM SHAPE AGAIN, and that is the reason to register it rather than leave it in prose: the
arithmetic-statement twin `ExpressionBinder.ScreenResultant` exists and does exactly this job for ADD /
SUBTRACT / MULTIPLY / DIVIDE / COMPUTE resultants (kb/Work PB128), citing each verb's own resultant SR. SET's
receiver was never given one. One rule, two arms, one written — the repository's most reproducible defect
shape, recorded in feedback_two_arm_dispatch.

⚠ Note the SENDING side of the same statement was fixed by cluster B (PB172 widened the §8.8.1.1 intrinsic
screen to the index-name window contexts, of which SET is one), which makes the receiving gap the more
visible of the pair: `SET IX TO FUNCTION LOWER-CASE(X)` is now rejected while `SET WS-X TO 5` with an
alphanumeric receiver still compiles.

WHAT WOULD CLOSE IT: extend `ScreenResultant` (or a sibling that shares its classifier) to the SET receiver
with §14.9.39.3 SR1's own citation, negative fixtures per excluded class, a golden for the two legal shapes,
and a row in the funnel entry-point table.

## 2026-09-04 — lane-3 adjudication batch b2 widened this note to THREE rows; the SET Format-1 receiver/sender pair is one gate, not one screen

**Rows claimed: `SR-14.9.39.3-1`, `SR-14.9.39.3-3`, `SR-14.9.39.3-4`.** All three are answered — or rather not
answered — at the SAME site, so they are this note's. Everything below was **re-measured on this worktree's
HEAD `cobol.exe` (base 89044aa0)** at `--std 2023`; the b2 adjudication also measured `--std 85` identical.
Probes in `probe/set1/` and `probe/set2/`.

### SR1 (this note's own rule) — confirmed, and it has a SILENT arm the note does not mention

`--check 14.9.39.3 "Identifier-1 shall reference a data item of class index or an integer data item"` →
`OK §14.9.39.3 1)`.

This note's own case still reproduces: `01 WS-X PIC X(4).` + `SET WS-X TO 5` (`set1/a1_alnum_recv.cob`) compiles
with zero diagnostics, then throws `NotImplementedCobolFeatureException: arithmetic into a non-fixed-point
target 'WS-X'`. A GROUP receiver (`a3_group_recv`) and a `USAGE POINTER` receiver via `SET P1 TO 4`
(`k4_int_to_ptr`) do the same.

⛔ **NEW: two shapes do not crash — they RUN and STORE.**

- `01 WS-D PIC 9(4)V99.` + `SET WS-D TO 5` (`a2_nonint_recv`) prints `D=[000500]`, rc 0, no diagnostic. Class
  numeric but NOT an integer data item, so SR1 excludes it.
- `01 WS-E PIC ZZZ9.` + `SET WS-E TO 5` (`a6_edited_recv`) prints `E=[   5]`. Category numeric-edited — neither
  class index nor an integer data item.

So ONE missing screen produces TWO harms: a run-time throw for the non-fixed-point receivers, and a **silent
acceptance-and-store** for the non-integer numeric and numeric-edited ones. The silent arm is the worse of the
two and is the one this note's title does not name. Note what makes these land in the arithmetic lane at all:
`SetBinder.cs`'s category re-routes fire only when the SENDER is a bare data reference, so a LITERAL sender
takes every non-numeric receiver straight past them.

### SR3 and SR4 are NOT closed by this note's stated fix

`--check 14.9.39.3 "If identifier-1 references a data item of class index, arithmetic-expression-1 shall not be specified"`
→ `OK §14.9.39.3 3)`.
`--check 14.9.39.3 "If identifier-1 references a numeric data item, index-name-2 shall be specified"` →
`OK §14.9.39.3 4)`.
`--check 14.9.39.4 "the occurrence number of the table element referenced by index-name-2"` → `OK §14.9.39.4 2)`
(rule 2 c) — the confirming half: the only defined semantic for a numeric Format-1 receiver is to receive an
occurrence number from an index-name.

**These are RELATIONS between the receiver's class and which sender alternative is legal**, and this note's
stated fix — extend `ScreenResultant` (or a sibling sharing its classifier) to the SET receiver — looks at ONE
operand and has no sender to consult. Landing PB212 exactly as written would leave both rows open.

Measured, all compiling clean and running with no diagnostic:

| probe | source | rule violated | OBSERVED |
|---|---|---|---|
| `set2/sr3_idxdata_arith` | `01 IXD USAGE INDEX.` + `SET IXD TO 3.` | SR3 | prints `OK-SR3-ARITH` |
| `set2/sr3_idxdata_expr` | `SET IXD TO N + 1.` | SR3 | prints `OK-SR3-EXPR` |
| `set2/sr4_num_literal` | `01 N PIC 9(4).` + `SET N TO 3.` | SR4 | `N=0003` |
| `set2/sr4_num_expr` | `SET N TO M + 1.` | SR4 | `N=0008` |
| `set2/sr4_num_ident2` | `SET N TO IXD.` (identifier-2, not index-name-2) | SR4 | `N=0002` |

Legal controls still work: `sr3_idxdata_idxname` (`SET IXD TO IDX`) → `OK-SR3-LEGAL`; `sr4_num_idxname_legal`
(`SET IDX TO 4. SET N TO IDX.`) → `N=0004`. So this is pure under-rejection, not a mis-selected format.

### The shape the fix should take, and one test that must move

⛔ Not three independent `if`s. §14.9.39.4 GR2 defines semantics for exactly two receiver/sender pairings and no
others, because no third pairing is legal — so the right structure is a small **receiver-class × sender-
alternative table consulted once in `BindSetTo`**, which gives SR1 its home as the receiver-class column and
makes SR3/SR4 the cells rather than extra clauses (CLAUDE.md rule 5: the shape that makes the next case
automatic).

`tests/Cobol.Net.Tests.Unit/FlagDirectiveTests.cs:615`
(`Compile_RangeExceptionForIndex_NotFlagged_OnPlainNumericReceiver`) uses `SET N TO 3.` with `01 N PIC 9(4).` as
its fixture and asserts only that no FLAG-02 warning appears — a green test standing on source SR4 forbids
(`feedback_green_test_can_hold_a_gap_open`). The fix must re-point it at a legal SET or at another verb.

## Appended 2026-09-13 by REGISTRAR #4 — a lead that belongs to this note's mechanism

Re-measured on this worktree's own `dotnet build CobolSharp.sln -c Debug` at main `b5ca78fb`, and again at `e13ef810` (train 32's head) with the same result; every citation below was re-derived with `cite.py --check`.

### §14.9.39.3 SR3 / SR4 re-measured (from the PB459 finisher report, item (a))

`scratchpad/reg4/probe/p12_set_numeric_to_nonint.cob` (PROGRAM-ID `R4P12`), `--std 2023 --run`:

```cobol
       01 N PIC 9(4).
           SET N TO 2.5
           DISPLAY "N=" N
```
```
N=0000            (compiles; no diagnostic; the statement stores nothing recognisable)
```

```
$ python scripts/spec/cite.py --check 14.9.39.3 "If identifier-1 references a numeric data item, index-name-2 shall be specified"
  OK  §14.9.39.3 4)  (Syntax rules)
$ python scripts/spec/cite.py --check 14.9.39.3 "If identifier-1 references a data item of class index, arithmetic-expression-1 shall not be specified"
  OK  §14.9.39.3 3)  (Syntax rules)
```

Both rules are unenforced: `SetBinder.BindSetTo` builds a `BoundSetTo` for any receiver/sender combination, and
the statement then takes the Format-1 landing. The two SRs are a screen over (receiver kind, sender kind) and
would be ONE predicate at `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/SetBinder.cs#BindSetTo`, around the
`SetTargetOf` loop. Rows `SR-14.9.39.3-3` / `-4` are already this note's.

## ⬆ Appended 2026-09-20 by REGISTRAR #7 — SR4 belongs here too, and it is MEASURED

The `PB449-finisher` report asks that §14.9.39.3 **SR4** be added to this note rather than filed separately,
because it is the SENDER half of the Format-1 screen whose RECEIVER half (SR1) this note owns. Agreed, and
measured: `<scratch>/reg7/probe/q_set_num_lit.cob`, `--std 2023`, `--run`:

```cobol
       01 N4 PIC 9(4).
       PROCEDURE DIVISION.
           SET N4 TO 5
```
```
N4=0005
```

No diagnostic at any edition.

```
$ python scripts/spec/cite.py --check 14.9.39.3 "index-name-2 shall be specified"
OK  §14.9.39.3 4)  (Syntax rules)
    4) If identifier-1 references a numeric data item, index-name-2 shall be specified.
```

SR4 makes a LITERAL sender illegal over a numeric receiver — the sender shall be an index-name. `SetBinder`'s
`BindSetTo` Format-1 arm has **no sender screen at all**, which is the same absence this note records for the
receiver: one format, two operand screens, neither written. Row `SR-14.9.39.3-4` is already NOT-IMPLEMENTED /
GAP and already names this note, so nothing moves — this paragraph records that the row's mechanism is now
measured rather than inferred, and that SR1 and SR4 are ONE fix.

## Row adopted 2026-09-21 by **REGISTRAR #8** — SR2 is the sender half of the screen this note owns

`SR-14.9.39.3-2` is adopted from `kb/Work/PB388`, whose implementer report asks for the handoff and whose own
note says the row is not its mechanism. It belongs here on the note's own terms: this note owns
`SR-14.9.39.3-1`, `-3` and `-4`, all of them the Format-1 screen in `SetBinder`/`BindSetTo`, and registrar #7's
2026-09-20 append to this note already measured the sender side — *"`BindSetTo`'s Format-1 arm has no sender
screen at all, so SR1 and SR4 are ONE fix."* SR2 IS that sender screen:

```
$ python scripts/spec/cite.py --check 14.9.39.3 "Identifier-2 shall reference a data item of class index"
  OK  §14.9.39.3 2)  (Syntax rules)   [FORMAT 1]
```

### ⭐ DETERMINATION the implementer makes — recorded here, NOT escalated

`PB388` calls this row *"an ADJUDICATION before it is a repair"*, and the question is real: **what does SR2 do to
`SET IX TO <integer data item>`?** SR1's receiving brace admits *"a data item of class index or an integer data
item"*, and if SR2's sending rule is read as strictly as it is written, an integer sender is not admitted at all
and a great deal of ordinary COBOL stops compiling.

It is recorded as a DETERMINATION rather than an owner question on `kb/Work/PB921`'s precedent, because **both
readings make today's behaviour wrong** and the note is therefore a defect either way:

* SR2 read strictly → the rule is written down NOWHERE and legal-looking source is admitted unscreened.
* SR2 read as governing only the class-index ALTERNATIVE of Format 1's sending brace → the rule is STILL written
  down nowhere; its complement is enforced only incidentally by the §8.8.1.1 arithmetic-operand screen, so every
  rejection cites §8.8.1.1, and a future narrowing of that screen (`kb/Work/PB172` narrowed it once already)
  would silently delete SR2's only enforcement.

The implementer derives the reading from the RENDERED §14.9.39.2 Format-1 figure (`scripts/render-spec-page.py`
— the braces are load-bearing and the OCR'd diagrams are systematically lossy) and implements it, citing which
reading and why. `feedback_owner_decision_protocol` applies if the figure does not settle it: survey the model
implementations and follow them.
