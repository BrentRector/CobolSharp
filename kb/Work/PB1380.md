---
title: "PB1380 — a reference-modified level-66 RENAMES … THRU alias DROPS its reference modifier: `ReferenceResolver`'s RENAMES arm returns a `RenamesPlace` BEFORE the `RefModPlace` wrap, so `MOVE RN(2:3) TO X` moves all six characters and `RN(2:N)` with N = 0 overwrites the whole span instead of raising EC-BOUND-REF-MOD (directive omitted) — a wrong answer on legal source, reached by the REF-MOD-ZERO-LENGTH refuter"
id: PB1380
kind: defect
status: open
severity: MAJOR
area: compiler/binding
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB945"]
spec_refs: ["7.3.23.3", "8.4.3.3.2", "8.4.3.3.4", "13.18.45.4"]
inventory_rows: ["GR-7.3.23.3-1"]
closes_rows: []
closes_rows_reason: "open — nothing landed yet"
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 15a** from adjudication batch 15a (subjects in §7.2 / §7.3, adjudicator pin `E:\CobolSharp-pins\adj-d2` @ `071ff04ea`; nothing under `src/` landed between the pin and `8bd4c7c9f`, so every measurement below stands). Probe programs: `E:\Temp\claude\E--CobolSharp\a700c052-f9e4-4942-84ff-a13c7505d6ec\scratchpad\adj-d2\out\probe\<subject>\`.

# PB1380 — OPEN

PB945 (open) is the Place-model 'is this a ref-mod place' type-list analysis — the same Place shape; the fixer should read it first. Not registered anywhere before (refuter searched PB430/PB1030).

## The rule (every citation re-run by the registrar)

> When this directive is omitted or is specified as off, then when reference-modification results in a zero-length data item …

`python scripts/spec/cite.py --check 7.3.23.3 "When this directive is omitted or is specified as off, then when reference-modification results in a zero-length data item"` → `OK  §7.3.23.3 1)  (General rule)`

> alphanumeric group item …

`python scripts/spec/cite.py --check 13.18.45.4 "alphanumeric group item"` → `OK  §13.18.45.4 2)  (General rules)`

## Row GR-7.3.23.3-1 — PARTIAL (adjudicator + refuter record)

Rule: 'When this directive is omitted or is specified as off, then when reference-modification results in a zero-length data item, the exception condition EC-BOUND-REF-MOD is raised' (cite.py --check 7.3.23.3 -> OK §7.3.23.3 1)); companion §8.4.3.3.4 5)c) 'However, when the REF-MOD-ZERO-LENGTH directive is in effect, a zero-length result is allowed' (--check OK). Code: RefModZeroLengthState.IsOnAt folds source-ordered ON/OFF toggles (strictly preceding the site line, POP-aware) with default on=false (omitted == OFF); a site not ON keeps the zero-length length check. Probes with the pinned cobol.exe under >>TURN EC-BOUND-REF-MOD CHECKING ON + USE AFTER EXCEPTION declarative (out/probe/rmzl/p1..p3.cob): directive OMITTED -> EC-BOUND-REF-MOD caught for MOVE S(1:N) sender, MOVE into T(2:N) receiver (T unchanged), DISPLAY S(N+1:N) (computed zero), FUNCTION UPPER-CASE(S)(1:N), IF S(1:N) = SPACES / = 'A', IF S(1:N) IS ALPHABETIC, EVALUATE S(1:N), FUNCTION LENGTH(S(1:N)), INSPECT S(2:N), and literal S(1:0); explicit OFF -> raised; ON -> not raised; OFF after ON -> raised again. Both halves (omitted, explicit OFF) verified. Covering test pb1004 L1 pins the OMITTED half (R=RAISED under PERFORM WHEN EC-BOUND-REF-MOD); conformance:2023/ref_mod_zero_length and pb425 pin the ON contrast only; no existing golden asserts the explicit-OFF raise (the OFF half is probe-verified, untested). || REFUTED: PARTIAL — The directive fold (RefModZeroLengthState.IsOnAt, default OFF, POP-aware, over final preprocessed text) and the runtime CobolString.RefMod/StrSpliceInto zero-length check are correct for every ordinary operand shape, but a legal operand shape escapes the rule: a reference-modified level-66 RENAMES THRU alias (an alphanumeric group item per §13.18.45.4 GR2, admissible identifier-1 per §8.4.3.3.2 SR1) never raises EC-BOUND-REF-MOD on a zero-length result with the directive omitted, because ReferenceResolver's RENAMES arm returns a RenamesPlace BEFORE the RefModPlace wrap, discarding the reference modifier entirely (RN(2:3) moves all six characters; RN(2:N) with N=0 overwrites the whole span). Also: the explicit-OFF half is only probe-verified (no golden), and the RENAMES silent ref-mod drop appears unregistered in kb/Work (searched PB430/PB1030 etc.) and needs a defect note.

**Code location:** src/Cobol.Net.Compiler/Binding/RefModZeroLengthState.cs#RefModZeroLengthState.IsOnAt; src/Cobol.Net.Frontend/Preprocessor/RefModZeroLengthDirectiveProcessor.cs#RefModZeroLengthDirectiveProcessor; src/Cobol.Net.Compiler/Binding/Bound/MoveClassifier.cs

## What the fixer owes (and the sibling sweep)

Wrap the RENAMES place in the ref-mod place like every other identifier (the resolver's arm order is the defect); sweep every early-return arm in `ReferenceResolver` for the same bypass (subscripted RENAMES, qualified RENAMES). Goldens: RN(2:3) moves three characters; RN(2:N) N=0 raises EC-BOUND-REF-MOD with the directive omitted, not with it ON.

**Rows claimed:** GR-7.3.23.3-1
