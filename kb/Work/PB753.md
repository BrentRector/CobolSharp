---
title: "PB753 — a shared INPUT connector delivers a record image a sibling I-O connector has already REWRITTEN: `SequentialConnector._reader` is a buffering `StreamReader` whose read-ahead is a SNAPSHOT, and nothing invalidates it when another §9.1.15 participant writes through the same physical file. MEASURED — `FR` reads record 1, `FW` REWRITEs record 3 to 'NEW' and reports '00', `FR` then reads `R-3=OLD` while the file on disk holds `DISK-3=NEW`. §14.9.35.4 GR4 says the successful REWRITE RELEASED that record to the operating environment; the connector's own `SeekToRecord` already writes the invalidation rule down for its OWN seek and never for a sibling's write — one rule, one arm"
id: PB753
kind: defect
status: landed
severity: MAJOR
area: runtime/io
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.35.4", "9.1.15", "14.9.30.1", "14.9.30.4", "14.9.51.4"]
inventory_rows: ["GR-14.9.35.4-4"]
tags: [cobolsharp, work, defect, files, file-sharing]
---

# PB753 — READ — the read-ahead is a snapshot of a file another connector is changing

Found 2026-09-06 by the `PB739` implementer (report, "New defects (a)"), **MEASURED on its own fixed tree**, and
filed rather than folded in because it is a different mechanism from PB739: PB739 was the WRITE side (a release
positioned and numbered from state captured at OPEN); this is the READ side (a record image served from a buffer
filled before the release happened).

## ⛔ Reproduction status — read this before designing the fix

- **The measurement is real and is NOT re-run here.** It was taken on the PB739 implementer's **fixed** tree —
  i.e. with PB739's own change set applied, which is in flight in landing trains 16–17 together with
  [[PB713]] — using `…\a9d94900-79fe-4a38-89f0-921976861543\scratchpad\PB739\probe\pb739rs.cob` (`PB739RS`).
  That tree is not main.
- **This registrar did not compile or run anything.** Main's `cobol.exe` was deliberately not used
  (`feedback_reachability_is_measured_not_deduced` — a reachability claim is a probe to run, not a conclusion).
- ⛔ **Reproduce on main after trains 16–17 land**, before the fix is designed. The probe is the whole
  specification of the repro and needs no rewriting.
- ⚠ **What READING main says about reachability, stated as reading and not as measurement.** Every element of the
  mechanism below is at a main line number, and neither of the two things that would block the probe on main is
  on this path: both `SELECT`s in `pb739rs.cob` carry `SHARING WITH ALL OTHER` **and** `LOCK MODE IS MANUAL`, so
  `FileRegistry.RegisterSharing` (`src/Cobol.Net.Runtime/IO/FileRegistry.cs:711`) sets `SharedStreams` on both —
  which takes the INPUT and I-O opens down the `FileShare.ReadWrite` arms and therefore clear of [[PB740]]'s
  unshared-posture refusal — and `SequentialConnector.SeedSharedWriteBase` returns immediately unless the mode is
  EXTEND (`SequentialConnector.cs:109`), so [[PB713]]'s `IOException` is not on an I-O open either. **That is an
  argument, not a run.** Treat it as the hypothesis the reproduction tests, not as a claim that main is already
  broken this way.

## What was measured

`pb739rs.cob` — one `ASSIGN TO "pb739rs.dat"`, `ORGANIZATION IS SEQUENTIAL`, three `SELECT`s: `FS` seeds
**300 four-byte records** (1200 bytes — deliberately over the 1024-character `StreamReader` buffer, which is what
makes the read-ahead observable at all), then `FR` opens INPUT and reads record 1, then `FW` opens I-O, reads
forward to record 3, `REWRITE`s it to `NEW`, and closes; `FR` then reads records 2 and 3.

```
R-1=OLD   W-IO=00   W-RW=00
R-2=OLD   R-3=OLD           ← the record FW replaced, served from FR's buffer
DISK-3=NEW                  ← what is actually in the physical file
```

Both statuses are `'00'`. **No status anywhere says the reader is looking at superseded data**, which is why
`silent: true` accompanies `wrong_answer: true`: a concurrent read/update pass computes from data the same run
unit has already replaced, and reports success at every step.

**The APPEND case is NOT affected, and this was measured too** (`pb739rv.cob`, `pb739rw.cob`): a record appended
by a sibling IS delivered to an already-open reader, both when the reader has read nothing and when it has
already consumed the whole file — because a reader can only have buffered what existed. Recorded here so a
later reader does not widen the note to a shape that was checked and is correct.

## What the standard prints — every clause re-derived with `cite.py --check`, none inherited

- `--check 14.9.35.4 "The successful execution of the REWRITE statement releases a logical record to the
  operating environment"` → **OK §14.9.35.4 4)**. The REWRITE that reported `'00'` did not merely intend to
  write; GR4 says the record **is** with the operating environment. The measurement confirms the standard is
  satisfied on disk — `DISK-3=NEW`.
- `--check 9.1.15 "The sharing with all other mode allows concurrent access to a physical file through other file
  connectors specifying input, I-O, or extend mode"` → **OK §9.1.15 3)**. The concurrency the probe sets up is
  **legal**, explicitly. This is the clause that makes the situation the compiler's problem rather than the
  program's.
- `--check 14.9.30.1 "For sequential access, the READ statement makes available the next logical record from a
  file"` → **OK §14.9.30.1**. What a READ makes available is *the next logical record from the file* — not the
  next record from a buffer of what the file said earlier. Together with GR4 that is the whole derivation: a
  READ executed after a release must deliver the released record.

⚠ **The derivation deliberately does not lean on a rule about buffering, because the standard has none to lean
on.** §9.1.15's model is file connectors over a physical file; the read-ahead is entirely ours. That is what
makes this a defect and not an implementor-defined latitude question — no reading of §9.1.15 3) admits an
input connector that shows a record the physical file no longer contains.

## The mechanism, at current line numbers on main (`0a24276f`)

1. `src/Cobol.Net.Runtime/IO/SequentialConnector.cs:21` — `private StreamReader? _reader;`. A `StreamReader`
   buffers; the class header says so itself at `:28-29`: *"the StreamReader BUFFERS, so BaseStream.Position is
   the buffer-fill boundary, never the read position"*.
2. `:297-300` — the INPUT open. The §9.1.15 participant arm, `:298`, is
   `new StreamReader(new FileStream(HostPath, FileMode.Open, FileAccess.Read, FileShare.ReadWrite),
   Encoding.Latin1)`: the **OS handle** was made shareable, and the **buffer above it** was not made coherent.
   That is the whole defect in one line — sharing was solved at the handle and not at the read.
3. `:337-338` — the I-O open, `SharedStreams ? FileShare.ReadWrite : FileShare.Read`, so the sibling's handle can
   coexist.
4. `:745-753` — the record-sequential REWRITE writes through **`_reader.BaseStream`**, seeking to
   `_lastReadBlockStart`, writing the Latin-1 bytes and calling `stream.Flush()` at `:751`. The bytes reach the
   file. Nothing notifies any other connector.
5. `:690-700` (`FillChars`, the fixed-width/varying path — `int n = _reader!.Read(buf, total, count - total);` at
   `:695`) and `:710-723` (the line-sequential path — `_reader.Read()` / `_reader.Peek()`) serve from the buffer
   filled before step 4. There is **no** invalidation on either path.

## ⛔ The rule is already written down — for one arm only

`SeekToRecord` (`:846-849`) is the ONLY `DiscardBufferedData` call in the file, and its doc comment at `:844-845`
states the rule this defect breaks, in the standard's own terms:

> `StreamReader.DiscardBufferedData` is required: the reader buffers ahead of the base stream, so seeking the
> stream alone would keep serving stale characters.

So the connector knows that a change to the underlying stream invalidates its buffer — **when the change is its
own** (a `START`). The identical change made by a *sibling* connector over the same physical file was never
given the same treatment. `feedback_two_arm_dispatch` (a dispatch with two arms, only one ever fixed) and
`feedback_one_rule_one_place` (the extraction IS the fix): the invalidation rule must stop being a private
courtesy inside `SeekToRecord` and become a property of the shared physical file.

## The fix shape

⛔ **Not a `DiscardBufferedData()` sprinkled at the top of `Read`.** That would be correct-by-accident and
per-call-site, and CLAUDE.md rule 5 asks for the shape that makes the next case automatic. Two admissible
structures, both of which put the rule where the sharing already lives:

1. **A mutation generation on the physical file's shared state.**
   `src/Cobol.Net.Runtime/IO/Sharing/PhysicalFileTable.State` is already the per-host-path record of who is open
   and what is locked, keyed by resolved host path — the same key `KeyedStoreTable` uses. Give it a monotonically
   increasing *release counter* bumped by every WRITE/REWRITE/DELETE that reaches the physical file, have each
   reader record the value its buffer was filled at, and re-fill when they differ. This is the read-side twin of
   PB739's write-side rule stated in PB739's own DEVLOG paragraph — **the position and the identity of a released
   record are read from the shared medium at the release, never from per-connector state captured at OPEN** — and
   it generalises to `RelativeConnector`/`IndexedConnector` unchanged should they ever stop being store-backed.
2. **An unbuffered reader for a §9.1.15 participant.** Simplest and demonstrably sufficient (the participant
   already takes a different `FileStream` arm at `:298`, so there is a natural place for it), but it pays the
   cost on every read of every sharing-active file whether or not anyone else is writing, and it leaves the rule
   unstated — a third organization or a fourth framing can re-introduce the divergence.

⭐ Shape 1 is preferred, shape 2 is the fallback, and **whichever is chosen the drift test is the deliverable
that keeps it true**: no read path under `src/Cobol.Net.Runtime/IO` may serve characters buffered before the
shared state's release counter last moved. Pair it with the probe as a golden — the matrix, not the case
(`feedback_probe_the_shape_the_subject_hides`): reader-then-writer and writer-then-reader, buffer-crossing and
sub-buffer record counts, all three sequential framings (record / varying / line), and the APPEND legs above as
the controls that must stay green.

## Sweep

- **All three sequential framings share `_reader`.** The probe is record-sequential; `_lineSequential`'s
  `ReadPhysicalLine` (`:710-723`) and the varying framing both read through the same buffered `_reader`, so the
  same staleness is expected there and is **unmeasured**. PB739's write side found precisely this — *"it was
  wider than the report that filed it: all three sequential framings lost a record"* — so assume the same width
  here until measured (`feedback_scan_all_similar`).
- **The keyed organizations are already correct, and for the reason that names the fix.** `RelativeConnector` and
  `IndexedConnector` do not read the host file per operation: they attach to the ONE `KeyedStoreTable` store for
  the path ([[PB143]]), so every mutation is instantly visible to every attached connector. That is shape 1
  above, already built, for the other two organizations — which is the strongest argument that the sequential
  connector's shared medium wants the same treatment rather than a buffer tweak.
- **Not this note:** [[PB740]] (an unshared connector's OS posture refusing opens Table 19 permits) and
  [[PB713]] (`SeedSharedWriteBase`'s `FileShare.Read` probe colliding with a held write handle) are both about
  the *handle*, before any record is read. This is about the *buffer*, after. Checked and excluded deliberately
  so the three are not conflated by a later reader — though all three want the same restructuring of where a
  connector's stream posture is decided, so **landing order matters: PB713 → PB739 → PB740 → PB753**.

## Registrar verification 2026-09-06 — the blast radius grew, and every line number above is stale

Verified against main (`9fad13e5`) and against [[PB740]]'s branch (`397595e5`, landing train 18). Status
UNCHANGED — this is an update to the forensics, not a re-adjudication. Nothing was compiled or run here either.

### 1. PB740 widens the population: a program with NO sharing clause anywhere can reach this

The note above is written throughout against connectors that WROTE a `SHARING` or `LOCK MODE` clause — it quotes
`SharedStreams ? FileShare.ReadWrite : FileShare.Read` and reasons from `RegisterSharing` setting `SharedStreams`
on both `SELECT`s. PB740 removes that precondition: with the OS posture derived from the sharing mode instead of
from the presence of a clause, **two CLAUSE-LESS connectors can hold one physical file open at the same time for
the first time.** PB740 measured it — clause-less `OPEN INPUT F-A` then `OPEN EXTEND F-B` went from
`B-OPEN=30 / B-WRITE=48` with the appended record never written, to `00 / 00` and the record present.

⚠ **But read which pair that measurement is, because it is not this note's pair.** PB740's clause-less leg is
INPUT + EXTEND, and the section "What was measured" above records that the APPEND case is deliberately NOT
affected: a reader can only have buffered what already existed. This note's pair is INPUT + **I-O** with a
REWRITE. What makes it reachable clause-lessly is a separate and checkable fact about the ARBITER, and it holds
on main today:

- `FileRegistry.ImplementorDefaultSharing` is `null` — UNDETERMINED (`FileRegistry.cs:699`), so a clause-less
  connector carries no sharing mode into arbitration.
- `FileRegistry.Conflicts` (`:845-858`) enumerates every candidate sharing mode for an unknown side and returns
  `false` — *no conflict* — as soon as ONE candidate pair is a Table 19 *Normal open*. `ALL OTHER × input`
  existing against `ALL OTHER × I-O` incoming is such a cell, so **a clause-less INPUT + clause-less I-O pair is
  already permitted by the arbiter on main**; the only thing refusing it is the non-participant's
  `FileShare.Read` handle, which is precisely what PB740 removes.

So the reachability argument becomes: after train 18, the probe is **PB753's own `pb739rs.cob` with the `SHARING`
and `LOCK MODE` clauses deleted from all three `SELECT`s**. That is one edit to a file that already exists, and it
is the measurement this note still owes (`feedback_reachability_is_measured_not_deduced` — the above is reading,
not a run).

### 2. The symbols and line numbers, re-derived — main first, then the branch

PB739 and PB713 have landed since this note was filed, so of the mechanism's ten anchors above, ONE still
resolves.

| the note says | on main `9fad13e5` |
|---|---|
| `_reader` field at `:21` | ✔ unchanged, `SequentialConnector.cs:21` |
| INPUT open at `:297-300`, the `new FileStream(…, FileShare.ReadWrite)` arm at `:298` | `:358-360` — and the raw `FileStream` is gone: `_reader = new StreamReader(HostFile.OpenConnectorStream(HostPath, FileMode.Open, FileAccess.Read, SharedStreams, FileOptions.SequentialScan), Encoding.Latin1)` |
| I-O open at `:337-338`, quoting `SharedStreams ? FileShare.ReadWrite : FileShare.Read` | `:420-421`; **the ternary is no longer in this file at all** — it moved to `HostFile.OpenConnectorStream` (`FileSupport.cs:250-252`) under [[PB713]], which is the single site the note's own fix shape wanted |
| `FillChars` at `:690-700`, the read at `:695` | `:775-790`, the read at `:780` |
| line-sequential `ReadPhysicalLine` at `:710-723` | `:792-…`, reads at `:795` / `:804` / `:808` |
| record-sequential REWRITE at `:745-753`, `Flush()` at `:751` | `:830-838`, `Flush()` at `:836` — and note it `Seek`s the BaseStream back to `resume` afterwards, restoring the stream position while leaving the reader's BUFFER untouched, which is the defect stated in three lines of code |
| — (not mentioned) | ⛔ **a SECOND write-through-`_reader.BaseStream` arm exists**: the line-sequential REWRITE at `:840-859`, `Flush()` at `:859`. The note's sweep predicted the read side was three-framings-wide; the WRITE side is two arms wide, and both must invalidate |
| `SeekToRecord` at `:846-849`, doc comment `:844-845` | `:929-934`; `DiscardBufferedData()` at `:934` is still the file's ONLY call |
| `FileRegistry.cs:711` `RegisterSharing` | `:706`; `FileConnector.SharedStreams` is `FileConnector.cs:168` (`=> SharedPhysical is not null`) |
| `SequentialConnector.SeedSharedWriteBase` returns unless EXTEND at `:109` | ⛔ **the symbol no longer exists anywhere in `src/`.** PB739 deleted the captured write base outright — `:396-398` now opens through `HostFile.OpenAppendStream` / `SharedAppendStream`, which positions at the physical end per write. The note's second reachability argument is therefore MOOT rather than wrong: there is no early return left to clear |

**On PB740's branch (`397595e5`)** the same mechanism is intact and the anchors move again: `SharedStreams` is
deleted in favour of `HostShare`; both stream opens funnel through the private `OpenReader` (`:203`) /
`OpenWriter` (`:218`), called from INPUT at `:460` and I-O at `:523`; and `HostFile.OpenConnectorStream` takes a
`FileShare` (`FileSupport.cs:251`). ⭐ **One branch-only fact the fix must account for:** the new virtual
`FileConnector.Reposture`, overridden at `SequentialConnector.cs:160-190`, REBUILDS `_reader` through `OpenReader`
at `:172`/`:176` whenever the admitted set widens — which discards the stale buffer, **accidentally, and only at
that moment.** It is not an invalidation rule and must not be mistaken for one when this note is worked: it fires
on an OPEN by another connector, never on a sibling's REWRITE, so it will make some orderings of the probe pass
for the wrong reason (`feedback_probe_the_shape_the_subject_hides`). The matrix in the fix shape above already
asks for reader-then-writer AND writer-then-reader; keep both, and add a leg where no OPEN happens between the
REWRITE and the READ.

## ✅ Landed 2026-09-06 — the release rule, stated at BOTH layers that buffer

**Reproduced first, on this worktree's own `cobol.exe`** (main `9fad13e5` + PB740's finisher branch merged in as
the base, which is what the note's reachability argument said it needed): `pb739rs.cob` printed
`R-1=OLD / W-IO=00 / W-RW=00 / R-2=OLD / R-3=OLD / DISK-3=NEW`, character for character what the note recorded.
The sweep it predicted held too: RECORD VARYING and LINE SEQUENTIAL were measured and are **both** broken the
same way (`p53v.cob`, `p53l.cob`), and the keyed organizations were measured and are **correct** (`p53k.cob` —
`REL-R-3=NEW`, `IDX-R-3=NEW`), for the reason the note gave.

**The rule, re-derived.** §14.9.35.4 GR4 and §14.9.51.4 GR12 are the SAME sentence for the two verbs that release
(*"The successful execution of the REWRITE statement releases a logical record to the operating environment"* /
*"…of a WRITE statement…"*), §9.1.15 3) makes the concurrency legal, and §14.9.30.4 GR21 c)/d) say what the
sibling's READ then owes: the record selected is *"the first existing record **in the physical file** whose
relative key number is greater than the file position indicator if NEXT is specified or implied"*, and that
record *"is made available in the record area associated with file-name-1"*. Every clause `cite.py --check`ed,
including the two the note carried.

**Shape 1, as the note preferred — and the note's estimate was one mechanism short.** The generation is on the
physical file's shared state, exactly as designed:

- `PhysicalFileTable.State.ReleaseGeneration` counts the releases that have REACHED the physical file.
  `SequentialConnector.NoteRelease` is the ONE place it moves, called from `ReleaseRecord`'s flush arm (WRITE)
  and from the new `OverwriteInPlace` (REWRITE) — which is itself the extraction the fix needed, because the
  record-sequential and line-sequential REWRITE arms had two copies of the same seek/write/flush/restore and only
  one of them could have been given the announcement. `NoteRelease` advances the RELEASING connector's own
  watermark in the same breath: a sequential REWRITE targets a record at or before its own file position
  indicator, so a connector's own release cannot invalidate its own read-ahead and a READ/REWRITE loop pays
  nothing.
- `NextFrame` — the ONE physical framing walk, already documented as such — begins with `EnsureReaderCoherent()`,
  which re-anchors the reader at the LOGICAL offset (`_readOffset` / `_lineByteOffset`; `BaseStream.Position` is
  the buffer-fill boundary) and discards the read-ahead when the generation has moved.

⛔ **And that was NOT enough, which is the part no reading could have predicted.** With the generation in place
the probe still printed `R-3=OLD`. There are **two** buffers under a sequential read, not one:
`StreamReader.DiscardBufferedData` reaches only the reader's own characters, and the `FileStream` beneath it kept
its own 4096 bytes — `FileStream.Seek` reuses its read buffer whenever the target offset lands inside it, so the
re-anchor read the superseded bytes straight back out of it. So `HostFile.OpenConnectorStream` now takes the same
posture decision `OpenConnectorWriteStream` takes: a handle whose §9.1.15 file lock admits another writer is
UNBUFFERED (`bufferSize: 1`), the exact mirror of PB739's `SharedAppendStream`, and for the mirror reason — one
managed buffer between the connector and the medium is the most that leaves GR21 c) true. Both halves were then
proved RED by injection, 25 of 47 each — 19 as first built, and 25 once the probes were repaired; see
"Registrar verification answered" below.

**The reach PB740 widened is covered.** A clause-less pair has `SharedPhysical == null` (it sets no record locks —
§12.4.5.9.4 GR1 b) 2.), so the generation could not be reached through it. `FileConnector.Physical` now holds the
physical file's state for EVERY connector — Table 19 arbitrates every open over it — and `SharedPhysical` is the
§9.1.16 record-locking VIEW of it, derived rather than stored, both written by one `AssociatePhysical` call at the
one site PB713's ordering requires. PB739's `SharingParticipationIsAssignedInExactlyOnePlace` was re-pointed at
that call and still asserts the same invariant.

**Goldens** (both registered enabled): `tests/conformance/2023/pb753_shared_read_coherence.cob` (`PB753RDC`, the
full matrix incl. LINE SEQUENTIAL) and `tests/conformance/2002/pb753_shared_read_coherence.cob` (`PB753R02`, the
floor's subset). Expected output was written from GR4/GR12/GR21 c)/d) BEFORE the run and matched line for line.
`SharedReadCoherenceDriftTests` carries the matrix the note asked for — every framing × three sharing spellings,
a buffer-crossing AND a sub-buffer record count, reader-first AND writer-first, plus the controls: the sibling
APPEND (still delivered), a lone I-O connector rewriting its own records, and both keyed organizations.

**Row:** `GR-14.9.35.4-4` (REWRITE releases) was a GAP row with no code-location and no test; it is now CONFORMS.
GAP 2777 → 2776 of 4348. `GR-14.9.30.4-21` stays DIVERGES — its two open divergences are [[PB343]] and [[PB336]],
neither of them this.

### Registrar verification answered (2026-09-06)

The registrar's pass over this note raised three things; each is re-derived here on the branch's own tree,
not inherited.

**1) What actually makes a clause-less INPUT + I-O pair reachable.** The Landed paragraph above said only
that such a pair has `SharedPhysical == null`. That is why the generation had to move onto
`FileConnector.Physical`; it is not why the pair exists. The pair exists because
`FileRegistry.ImplementorDefaultSharing` is `null` (`FileRegistry.cs:705`) — §9.1.15's implementor default is
UNDETERMINED for this compiler ([[PB322]]) — so `FileRegistry.Conflicts` (`:868`) enumerates every standard
sharing mode for BOTH sides and returns "no conflict" the moment one candidate cell of Table 19 is a
`NormalOpen`; ALL OTHER/INPUT against ALL OTHER/I-O is such a cell. So the ARBITER always permitted the pair,
and what refused it was the host handle's `FileShare.Read` — which is exactly what [[PB740]] fixed. PB753
therefore inherits its reach from PB740 and not from any change of its own.

**2) Three statements in the filing sections above no longer describe the tree.** They are left in place as
the record of what was filed, and corrected here (`feedback_docs_current_state_only` applies to the doc set,
not to a note's forensic history):

- `SequentialConnector.SeedSharedWriteBase` — the reachability argument's "returns immediately unless the
  mode is EXTEND" — **no longer exists anywhere in `src/`**. [[PB713]] and [[PB739]] replaced the write-base
  probe with the release-ordinal mint on the physical file's state.
- `SharedStreams` — the `SharedStreams ? FileShare.ReadWrite : FileShare.Read` ternary the argument leaned on
  — is gone as a field; the posture decision is `FileLockPosture.For` and it is SPENT in `FileSupport.cs`
  (`HostFile.OpenConnectorStream` / `OpenConnectorWriteStream`), which is also where PB753's `bufferSize`
  decision now lives. The only surviving mention is a doc comment in `FileConnector.cs` recording what the
  boolean used to answer.
- **The mechanism section listed ONE write-through-`_reader` arm and there are TWO** — the record-sequential
  REWRITE it named and the LINE SEQUENTIAL REWRITE (§14.9.35.4 GR17) it did not. That is the two-arm shape
  this repo keeps producing (`feedback_two_arm_dispatch`), and it is why the fix extracted `OverwriteInPlace`
  before announcing anything: an announcement added at the arm the note named would have left the other arm
  silent. Both arms are measured — `SharedReadCoherenceDriftTests` runs every case over LINE SEQUENTIAL as
  well as the two record framings.

**3) `Reposture` discards the stale buffer by accident, and it WAS holding probes green for the wrong
reason.** `FileRegistry.SyncHostPostures` calls `FileConnector.Reposture` on every other connector whenever
the §9.1.15 union over the physical file changes, and `SequentialConnector.Reposture` implements that by
REBUILDING the handle at the logical offset — which throws the read-ahead away as a side effect. For
`SHARING WITH ALL OTHER` the union is already `FileShare.ReadWrite` and nothing moves; for the two
undetermined-default spellings it widens at the sibling's OPEN and narrows again at its CLOSE. Measured, by
injecting `EnsureReaderCoherent` out of `NextFrame`:

- `SharedReadCoherenceDriftTests` went red on **19 of 47**, and the green ones included all six
  `ASiblingsRewriteIsVisibleToAnAlreadyReadingConnector` cells at `LockModeOnly` / `NoClause`;
- the 2023 golden printed `F-R3=OLD / V-R3=OLD / L-R3=OLD` and still `N-R3=NEW` — **the clause-less leg, the
  one this note's whole reach argument rests on, proved nothing.**

Repaired by flipping the axis the probes held fixed (`feedback_probe_the_shape_the_subject_hides`): the
reader now re-fills its read-ahead AFTER the sibling's OPEN and reads on while the sibling is STILL OPEN, so
no reposture stands between the fill and the read in any spelling. The sibling rewrites ordinal 4, so the
golden's clause-less leg expects `N-R3=OLD` (never rewritten) and `N-R4=NEW` (§14.9.35.4 GR4 released it,
§14.9.30.4 GR21 c)/d) select it from the physical file). `TheRuleHoldsWhetherOrNotTheFileFitsInTheReadAhead`
keeps the CLOSE-FIRST ordering measured and its doc now says why it is pinned to `AllOtherClause`.

Re-measured after the repair: **25 of 47** red with `EnsureReaderCoherent` injected out (was 19) and **25 of
47** red with `HostFile.OpenConnectorStream`'s `bufferSize: 1` injected back to 4096 — both halves still
measured, and all nine reader-first cells now measure the rule. 47/47 green with neither injection, and both
goldens match their expected output line for line.
