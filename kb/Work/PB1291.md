---
title: "PB1291 — §13.4.6.3 SR4 (an SD's record description entry only after FROM or INTO in an input-output statement) is screened only in the WRITE/REWRITE record-name slot: `WRITE IN-REC AFTER ADVANCING SRT-REC2 LINES` with SRT-REC2 an SD record compiles clean"
id: PB1291
kind: defect
status: open
severity: MINOR
area: binding/files
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1290"]
spec_refs: ["13.4.6.3"]
inventory_rows: ["SR-13.4.6.3-4"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14e** from adjudication batch d1-14e (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probe sources under the batch's `out13/probe/<subject>/`). Checked against main 071ff04ea: trains 58/59 did not touch this mechanism.

# PB1291 — OPEN

## The rules (every quotation run through `cite.py --check` by the registrar)

- `python scripts/spec/cite.py --check 13.4.6.3 "A record description entry associated with file-name-1 shall not be specified in an input-output statement other than following the word FROM or the word INTO"` → `OK  §13.4.6.3 4)  (Syntax rules)`

## Measured

### Row SR-13.4.6.3-4 — DIVERGES (no separate finding; the adjudicator's / refuter's record)

Code location: `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/SequentialIoBinder.cs#UnsupportedOrg (WRITE/REWRITE record-name resolving to the SD); src/Cobol.Net.Compiler/Binding/Validation/StatementValidation.cs#ScreenSortMergeFile`

Rule: "A record description entry associated with file-name-1 shall not be specified in an input-output statement other than following the word FROM or the word INTO." (--check 13.4.6.3 -> OK 4)). Both halves probed: prohibited arm -- `WRITE SRT-REC.` (v4, also at --std 85) and `REWRITE SRT-REC.` (v5) reject COBOLNET1692; permitted arm -- `READ IN1 INTO SRT-REC` (v10) and `WRITE IN-REC FROM SRT-REC` (v11) compile clean. No spec-derived test pins the SR4 record-name arm (no negative names an SD record in WRITE/REWRITE; the positive FROM/INTO arm is unpinned) -> test-needed. || REFUTED: DIVERGES — The rule forbids a record description entry of the SD in ANY input-output-statement position other than after FROM/INTO; the compiler only screens the WRITE/REWRITE record-name slot (the check resolves the record back to the file and fires the file-name diagnostic). An SD level-01 record used as the ADVANCING identifier -- `WRITE IN-REC AFTER ADVANCING SRT-REC2 LINES` with `SD SRT. ... 01 SRT-REC2 PIC 9(2).` -- compiles clean at --std 2023 (harm: under-rejects; the program reads a sort record area outside any sort). WRITE/REWRITE record-name arm and the FROM/INTO permitted arm do behave (probes c/d/e reject; g/h/k compile). No test pins the rule. cite.py --check 13.4.6.3 -> OK 4).

## What the fixer owes

Re-probe on your own build first (MANDATORY-PRACTICES I3). Fix the mechanism at the code sites above — one rule, one place (the `cluster:` notes share these files; take them as one fill unit). One positive golden at the introducing edition and one negative below it / per violated rule, each derived from the quoted rule. Sweep the sibling arms named in the findings (CLAUDE.md rule 4). On landing flip `status`, write `closes_rows` from `inventory_rows`, and re-verdict each row with `record_verdicts.py`.
