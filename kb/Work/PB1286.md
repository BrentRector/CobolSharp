---
title: "PB1286 — the RD's CODE clause (§13.18.12, listed in §13.14.2's general format) is refused as not-implemented (COBOLNET0899 ReportCodeClause) at every edition with no owner — legal source rejected"
id: PB1286
kind: defect
status: retired
severity: MINOR
area: binding/report-writer
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1129"]
spec_refs: ["13.14.2"]
inventory_rows: []
closes_rows: []
closes_rows_reason: "retired unlanded: a duplicate of PB1129 (the same mechanism, filed by an earlier registrar); its rows [\"FMT-13.14.2\"] and its evidence were folded into PB1129"
tags: [cobolsharp, work, defect]
---

# PB1286 — RETIRED — folded into PB1129

Registrar 14b's PB1129 already owns the RD CODE clause's COBOLNET0899 staging (DataBinder.BindReportDescriptionClauses, already extended once by 14d from PB1218); PB1286 measured the same stage from the §13.14.2 RD general-format side. The rows and the evidence below now live in PB1129; this note is kept only so references to PB1286 resolve.

## As filed (OPEN before the fold)

Filed 2026-09-24 by **REGISTRAR 14e** from adjudication batch d1-14e (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probe sources under the batch's `out13/probe/<subject>/`). Checked against main 071ff04ea: trains 58/59 did not touch this mechanism.



## The rules (every quotation run through `cite.py --check` by the registrar)

- `python scripts/spec/cite.py --check 13.14.2 "CODE"` → `OK  §13.14.2   (General format)`
- `python scripts/spec/cite.py --check 13.18.12.4 "When the CODE clause is specified, literal-1 or identifier-1 is automatically placed in the first characters of each logical record written to the report file"` → `OK  §13.18.12.4 1)  (General rules)`

## Measured

### Finding report-description-entry — 13.14.2 / 13.18.12 (rejects-legal-source; rows FMT-13.14.2)

**Mechanism.** The CODE clause of the RD is refused as not-implemented (COBOLNET0899 ReportCodeClause) at every edition, with no owning kb/Work note

**Repro.** FD RPT REPORT IS R-1. ... RD R-1 CODE 'AB' PAGE 20.  -> error COBOLNET0899: RD 'R-1': the CODE clause (ISO §13.18.12) is not yet implemented

**Expected, from the standard.** code-clause is a printed alternative of the §13.14.2 general format (semantics §13.18.12); a conforming RD with CODE shall compile and the report records carry the code per §13.18.12.

**Observed.** BindReportDescriptionClauses emits ReportCodeClause (RecognizedNotImplemented) for any CODE clause; CONFORMANCE.md A.4.11 lists it as staged loud; PB1056 records that such descriptors have no owner. Needs its own note (13.18.12 rows).

**Code site.** src/Cobol.Net.Compiler/Binding/DataBinder.Reports.cs#BindReportDescriptionClauses; src/Cobol.Net.Editions/Diagnostics/DiagnosticCatalog.cs#ReportCodeClause

## What the fixer owes

Re-probe on your own build first (MANDATORY-PRACTICES I3). Fix the mechanism at the code sites above — one rule, one place (the `cluster:` notes share these files; take them as one fill unit). One positive golden at the introducing edition and one negative below it / per violated rule, each derived from the quoted rule. Sweep the sibling arms named in the findings (CLAUDE.md rule 4). On landing flip `status`, write `closes_rows` from `inventory_rows`, and re-verdict each row with `record_verdicts.py`.
