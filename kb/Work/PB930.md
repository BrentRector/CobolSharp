---
title: "PB930 — plan §0's SESSION BOOTSTRAP still tells a new session `git checkout phase-14`, a branch no work has happened on since the 2026-09-06 owner decision routed every push through `scripts/push-main.sh` to `main`: §0's oldest paragraphs describe a tree from before the 2026-08-04 consolidation and want ONE header sweep, not four spot repairs"
id: PB930
kind: defect
status: open
severity: MINOR
area: docs
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
cluster: []
spec_refs: []
inventory_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-20 by **REGISTRAR #7** — the lead-filing pass over the wave-39 implementer reports, the wave-40 PB246 report, the wave-41 finisher and implementer reports (PB449-finisher, PB511-finisher, PB566-finisher, PB245, PB560, PB412), the train-40 and train-42 lander reports, and DEVLOG 1617-1621. **Every probe below was rebuilt from the source report and RE-RUN on this worktree's own `dotnet build CobolSharp.sln -c Debug` (0 warnings, 0 errors) at main `c701786d5`**, and **every citation was re-derived with `python scripts/spec/cite.py --check`** — the command line is printed beside the quotation. Probe sources under `<scratch>/reg7/probe/`.

# PB930 — OPEN — the first instruction a new session reads

Source: the `PB245` report, "New defects" item 2.

## The measurement

`docs/COBOLNET_REARCHITECTURE_PLAN.md` §0, SESSION BOOTSTRAP step ③, reads `git checkout phase-14`.
Every landing since 2026-09-06 has been on `main` — owner decision Q23 routes every push through
`scripts/push-main.sh`, which pushes `HEAD` to `ci/<short-sha>`, waits for the required `ci-gate` check and
fast-forwards `main` on green. A session that follows step ③ literally starts on a branch nobody has
landed to in two weeks.

## Why it is a sweep and not a line edit

The PB245 cluster already repaired THREE stale-register phrases in the same neighbourhood (land-a-fix
step 7, §0's single-write rule, §0's NEXT item 1). This is the fourth found by the same reading, and all
four have one cause: §0's oldest paragraphs were written before the 2026-08-04 register consolidation and
before the push protocol, and they have been edited around rather than re-read.

The fix is to read §0's procedural paragraphs top to bottom against the CURRENT tree — CLAUDE.md rule 6,
"every doc except DEVLOG describes the CURRENT compiler" — and to make each statement a statement about
today. `docs/DOC_INDEX.md` is the companion check.

⛔ And §0 must not regrow a worklist while it is being edited (CLAUDE.md rule 8). The sweep touches the
bootstrap, the gates and the live-state narrative; it adds no list.

## Rows

None — documentation.
