---
title: "PB1525 — there is no §4.2.10 nonstandard-extension register and no user-invocable warning mechanism: COMP-1/-2/-3/-5 and every other claimed extension compile silently at every --std, and docs/CONFORMANCE.md identifies no claimed extensions"
id: PB1525
kind: defect
status: open
severity: MAJOR
area: binding
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB298", "PB1139", "PB1508", "PB755", "PB756"]
spec_refs: ["4.2.10"]
inventory_rows: ["GR-4.2.10-1", "GR-4.2.10-3"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 22:46 UTC by **REGISTRAR-1** (cloud, branch `claude/adj-registrar-1`, kb/Work PB1522 step 2) from the lane-3 adjudication of the last 208 verdict-less inventory rows (wave 1 `claude/adj-cal-1` + `claude/adj-a1-w1-s1..s4`, wave 2 `claude/adj-w2-*`, all pinned at `0caa7d5`). Every citation below was re-run by the registrar through `python scripts/spec/cite.py --check`.

# PB1525 — OPEN

## The rule (§4.2.10)

> Documentation associated with an implementation shall identify nonstandard extensions for which support is claimed and shall specify any reserved words added for nonstandard extensions.

> An implementation shall provide a warning mechanism that optionally may be invoked by the user at compile time to indicate use of a nonstandard extension in a compilation group. This warning mechanism shall flag only extensions that are syntactically distinguishable.

Both `cite.py --check 4.2.10 …` → OK §4.2.10 3).

Severity MAJOR although no program computes a wrong answer: this is a §4 CONFORMANCE obligation of the implementation
itself, and without it the §4.2.1 claim cannot be made for any compilation group that uses a vendor usage.

## Repro (re-probed by the registrar, Debug build of `0caa7d5`)

`/tmp/adj/adj-w2-misc-s3/probe/ext1.cob`: `01 A PIC S9(5) COMP-3 VALUE 12. 01 B PIC S9(5) COMP-5 VALUE 3. 01 C COMP-2 VALUE 1.5.`
`ADD B TO A. DISPLAY A ' ' B ' ' C.` → prints `00015 00003 1.5` with ZERO diagnostics at `--std 85` and at `--std 2023`.
`cobol --help` lists no extension-flagging option (only `--permissive`, which is about REMOVED ISO constructs, and `--sign-encoding`).

## What exists and what does not

- MET: the reserved-word half — docs/CONFORMANCE.md D-RW1 lists the extension-reserved words, equal to `cobol-words.json`
  `extensionReserved`, pinned by `unit:CobolWordsDriftTests.LexerWordTokens_Cost_The_User_No_Word_Iso_Leaves_Free`.
- MET (GR-4.2.10-3 a): the selectable nonstandard behaviours have standard defaults and a CLI selector.
- NOT MET: no section of docs/CONFORMANCE.md identifies the nonstandard extensions for which support is claimed (COMP-1/-2/-3/-5,
  COMPUTATIONAL-n, END-INVOKE, the OPTIONS order-freedom, …); no warning mechanism exists. Vendor usages bind silently in
  `src/Cobol.Net.Compiler/Binding/PictureAnalyzer.cs#ParseUsage` (COMP-3 → `Usage.Packed`).

## Why this is ONE mechanism (rule 5)

Each per-construct note that cites §4.2.10 for its own extension — PB1139 (COLLATING-less SORT/MERGE), PB1455
(subscript-between-qualifiers), PB1508 (order-free OPTIONS), PB755/PB756 (optional underlined words), PB758 (END-INVOKE),
PB396 — is a ROW of the missing register. Fix shape: a data-driven extension register (the `cobol-words.json`
`extensionReserved` pattern) read by ONE binder seam (the `EditionContext.Declined` pattern) that emits a suppressible
Warning under a user option (e.g. `--flag-extensions`), and a CONFORMANCE.md section generated from, and drift-tested
against, that register — so the next extension is a row, never a bespoke decision.

Rows: GR-4.2.10-1 and GR-4.2.10-3 recorded **DIVERGES**. GR-4.2.10-2 (the class-2 members) is PARTIAL and owned by PB298
(its one silent member, the OPTIONS FLOAT-DECIMAL clause); it moves here when the register lands.

Harm flag: `under_rejects: true` — the missing warning is the §4.2.10 flagging obligation not firing on a syntactically distinguishable extension.
