---
title: "PB1288 — BindReportEntry's clause screens are asked in the wrong place: a TYPE clause is honoured at any level and overwrites the enclosing group's Kind (§13.15.3 SR5), COLUMN subordination is read as 'a LINE is in effect' (SR9), the PICTURE requirement is asked only for printable entries so an elementary SOURCE/SUM entry with no COLUMN is never checked (SR12), and PB507's BLANK WHEN ZERO / JUSTIFIED subject screen (CheckClauseSubjects) runs over the data forest only, never over report entries (§13.15.4 GR2)"
id: PB1288
kind: defect
status: open
severity: MINOR
area: binding/report-writer
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1287", "PB1289", "PB507"]
spec_refs: ["13.15.3", "13.15.4"]
inventory_rows: ["SR-13.15.3-5", "SR-13.15.3-9", "SR-13.15.3-12", "GR-13.15.4-2"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14e** from adjudication batch d1-14e (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probe sources under the batch's `out13/probe/<subject>/`). Checked against main 071ff04ea: trains 58/59 did not touch this mechanism.

# PB1288 — OPEN

## The rules (every quotation run through `cite.py --check` by the registrar)

- `python scripts/spec/cite.py --check 13.15.3 "The TYPE clause may be specified only in a level 1 entry and shall be specified in every level 1 entry"` → `OK  §13.15.3 5)  (Syntax rules)`
- `python scripts/spec/cite.py --check 13.15.3 "Every elementary entry with a COLUMN clause but no LINE clause shall be subordinate to an entry with a LINE clause"` → `OK  §13.15.3 9)  (Syntax rules)`
- `python scripts/spec/cite.py --check 13.15.3 "A PICTURE clause shall be specified in every elementary entry that has a SOURCE or SUM clause"` → `OK  §13.15.3 12)  (Syntax rules)`

## Measured

### Finding report-group-description-entry — §13.15.3 SR5 (under-rejects; rows SR-13.15.3-5)

**Mechanism.** report-type-clause-placement-unscreened: nothing checks where a TYPE clause is written. BindGroupType runs for a TYPE clause at any level and overwrites the enclosing group's Kind, and ReportGroupModel.Kind defaults to Detail, so a level 1 entry without TYPE silently becomes a detail group

**Repro.** (a) 01 D1. 03 LINE 1. 05 COLUMN 1 PIC X SOURCE WA. + GENERATE D1 (probe rgde/sr5notype). (b) 01 D1 TYPE DE. 03 LINE 1 TYPE RH. 05 COLUMN 1 PIC X SOURCE WA. + GENERATE D1 (probe rgde/sr5typesub)

**Expected, from the standard.** Both programs are refused at compile time under §13.15.3 SR5: 'The TYPE clause may be specified only in a level 1 entry and shall be specified in every level 1 entry.' (a) breaks the second half and (b) the first.

**Observed.** (a) compiles and prints `A` (rc 0) at --std 85, 2002 and 2023. (b) is not refused at the TYPE clause. The subordinate TYPE RH retypes D1 as a report heading, and the program then fails only at `GENERATE D1` with COBOLNET0899 'the named report group is not a DETAIL group (ISO §14.9.16.3 SR1)'. Without that GENERATE the group would silently print as a report heading.

**Code site.** src/Cobol.Net.Compiler/Binding/DataBinder.Reports.cs#BindReportEntry (clause loop `BindGroupType(t, group, model)` with no level test); #ReportGroupModel.Kind default

### Finding report-group-description-entry — §13.15.3 SR9 (under-rejects; rows SR-13.15.3-9)

**Mechanism.** report-column-subordination-read-as-line-in-effect: §13.15.3 SR9 is a SUBORDINATION rule, but the binder asks whether a report line is 'in effect' (`st.Line`, the last line opened in the group, reset only at level 1). A COLUMN entry that follows a LINE entry's subtree without being subordinate to it passes, and is silently placed on that earlier line

**Repro.** 01 D1 TYPE DE. 03 LINE 1. 05 COLUMN 1 PIC X SOURCE WA. 03 COLUMN 5 PIC X SOURCE WB. + GENERATE D1 (probe rgde/sr9sib; sr9deep is the cousin variant under 03 G1 / 03 G2)

**Expected, from the standard.** Refused at compile time. §13.15.3 SR9: 'Every elementary entry with a COLUMN clause but no LINE clause shall be subordinate to an entry with a LINE clause.' The 03 COLUMN 5 entry is subordinate only to the 01 entry, which has no LINE clause.

**Observed.** Compiles (rc 0 at --std 85, 2002, 2023) and prints `A   B` on report line 1. The fully line-less case (sr9none) is refused, but on COBOLNET0899 (the 'recognized but not implemented' code) citing §13.18.14, not §13.15.3 SR9. The fix belongs in the flat per-RD screen family (ScreenReportLineNesting / ScreenReportEntryClausePresence), which already walks subordination on the level-number hierarchy.

**Code site.** src/Cobol.Net.Compiler/Binding/DataBinder.Reports.cs#BindReportEntry (`if (line is null)` -> DiagnosticCatalog.ReportColumnWithoutLine); #ReportGroupBuild.Line

### Finding report-group-description-entry — §13.15.3 SR12 (under-rejects; rows SR-13.15.3-12)

**Mechanism.** report-picture-required-only-on-printable-branch: the SR12 PICTURE requirement is asked only inside BindReportEntry's `columns.Count > 0` printable-item branch, so an elementary SOURCE or SUM entry without a COLUMN clause, which is legal, is never checked for its required PICTURE

**Repro.** (a) 01 D1 TYPE DE. 03 LINE 1. 05 COLUMN 1 PIC X SOURCE WA. 05 SOURCE WB. (probe rgde/srcnopic). (b) RD R1 CONTROL FINAL ... 01 TYPE CF FINAL. 03 LINE 3. 05 S1 SUM WN. 05 COLUMN 1 PIC X VALUE "T". (probe rgde/sumnopic)

**Expected, from the standard.** Both are refused. §13.15.3 SR12: 'A PICTURE clause shall be specified in every elementary entry that has a SOURCE or SUM clause.' The rule has no COLUMN condition, and §13.18.54.4 GR1 sizes the sum counter from that PICTURE.

**Observed.** Both compile and run (rc 0). (b) creates a sum counter whose description the standard requires the programmer to supply. With a COLUMN clause (`05 COLUMN 1 SOURCE WA.`) the rule IS enforced (COBOLNET0899 citing SR12), but on the shared 'not implemented' code.

**Code site.** src/Cobol.Net.Compiler/Binding/DataBinder.Reports.cs#BindReportEntry (DiagnosticCatalog.ReportItemMissingPicture inside `if (columns.Count > 0)`); #BindSumClause

### Row GR-13.15.4-2 — PARTIAL (no separate finding; the adjudicator's / refuter's record)

Code location: `src/Cobol.Net.Compiler/Binding/DataBinder.Reports.cs#BindReportEntry; src/Cobol.Net.Compiler/Binding/DataBinder.Reports.cs#ScreenReportUsage; src/Cobol.Net.Compiler/Binding/PictureAnalyzer.cs#Analyze`

GR2 brings every syntax and general rule of the USAGE, PICTURE, BLANK WHEN ZERO and JUSTIFIED clauses into the report group entry. Verified: PICTURE goes through the one PictureAnalyzer, format 2 LOCALE included (PB113 landed). The USAGE clause is screened per §13.18.60.3 SR7 and GR1/GR2 inheritance (PB541 landed; conformance:negative/pb541-report-usage-comp). JUSTIFIED runtime alignment works (probe gr2: `PIC X(5) JUSTIFIED RIGHT SOURCE WA` printed `    A`). BLANK WHEN ZERO runtime blanking works (`PIC 99 BLANK WHEN ZERO VALUE 0` printed blanks). NOT enforced: §13.18.8.3 SR1 ('may be specified only for an elementary item described ... as category numeric-edited or as numeric without the picture symbol S'), where probe bwzx `05 COLUMN 1 PIC X(3) BLANK WHEN ZERO SOURCE WA.` compiled and ran (rc 0). Also NOT enforced: §13.18.32.3 SR3 ('The JUSTIFIED clause may be specified only for a data item whose category is alphabetic, alphanumeric, boolean, or national'), where probe justnum `05 COLUMN 1 PIC 99 JUSTIFIED SOURCE WN.` compiled and printed `01`. Both gaps are shared with the data-division arm (probe wsbwz: `01 X PIC X(3) BLANK WHEN ZERO.` and `01 Y PIC 99 JUSTIFIED.` accepted). The open kb/Work PB507 owns the BLANK WHEN ZERO half ('the BLANK WHEN ZERO clause has NO validator anywhere in the typed-native compiler') and names the JUSTIFIED sibling. The JUSTIFIED SR3 category rule looked unowned in a keyword search: the registrar should confirm whether PB507 or PB512 claims it. cite.py --check 13.15.4 'the USAGE, PICTURE, BLANK WHEN ZERO and JUSTIFIED clauses are the same clauses' -> OK; --check 13.18.8.3 and 13.18.32.3 SR3 -> OK. PARTIAL: the imported rules are enforced for USAGE and PICTURE and missing for two of the BWZ/JUSTIFIED syntax rules.

## What the fixer owes

Re-probe on your own build first (MANDATORY-PRACTICES I3). Fix the mechanism at the code sites above — one rule, one place (the `cluster:` notes share these files; take them as one fill unit). One positive golden at the introducing edition and one negative below it / per violated rule, each derived from the quoted rule. Sweep the sibling arms named in the findings (CLAUDE.md rule 4). On landing flip `status`, write `closes_rows` from `inventory_rows`, and re-verdict each row with `record_verdicts.py`.
