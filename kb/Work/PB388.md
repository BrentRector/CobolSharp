---
title: "PB388 — INHERITED clause citations across the shared-verb diagnostics, the SET format ordinals and the SEARCH GR numbering: wrong §/rule numbers PRINTED TO USERS and propagated into tests and two shipped goldens"
id: PB388
kind: defect
status: half
severity: MAJOR
area: docs/citations
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: [PB900, PB838, PB913, PB932, PB450, PB212, PB929]
spec_refs: ["14.9.14.3", "14.9.14.4", "14.9.17.3", "14.9.18.3", "14.9.20.1", "14.9.29.3", "14.9.37.4", "14.9.39.2", "14.9.39.4"]
inventory_rows: ["GR-14.9.37.4-1", "GR-14.9.39.4-12", "GR-14.9.39.4-13", "SR-14.9.39.3-10", "SR-14.9.39.3-2"]
tags: [cobolsharp, work, defect]
---

# PB388 — the citation is inherited, never re-derived, and two of them are in the text the user reads

<!-- audit-doc-citations: names-misfilings — this note IS the misfiling register; every row below PAIRS a
     clause with a quotation that is NOT in it, which is the point of the note. -->

## HALF LANDED — 2026-09-13 (implementer worktree agent-aca196966c1ba6e41, base main d80cb676)

⛔ **THE CITATION MECHANISM THIS NOTE IS ABOUT IS LANDED AND CLOSED.** The note stays `half` for ONE
reason: four of its five inventory rows rest on OTHER mechanisms (the note said so from its first
paragraph), and a row must not go unowned — `DefectiveRowCoverageDriftTests` fails exactly on a landed
note still holding a defective row, which is the event it exists to catch. **Re-home them and this note
closes:** `GR-14.9.37.4-1` → PB405 (the dispatcher-`break` idiom — **done: train 36 re-measured it
CONFORMS, so it is no longer a defective row of this note**) · `SR-14.9.39.3-10` → PB389 (the
`objectReferenceUsage` spelling) · `GR-14.9.39.4-12`/`-13` and `SR-14.9.39.3-2` → the two notes the
registrar is asked to file in PB388's implementer report (the SET Format-7 receiver ARITY, and SET
Format-1 SR2). Nothing in the citation half remains open.

**Every ordinal below was RE-DERIVED from the standard before it was written, and the note's own claims did not
survive that** — which is the mechanism this note is about, operating on the note (CLAUDE.md rule 1). Two of its
findings were WRONG and one had already been repaired:

* ⛔ **The SEARCH "uniform shift GR1→GR5, GR3→GR8, GR4→GR6" is NOT what the tree had.** §14.9.37.4 is
  partitioned by the standard's OWN banners — `ALL FORMATS` = GR1–2, `FORMAT 1` = GR3–4, `FORMAT 2` = GR5–9 —
  and the serial-SEARCH citations of **GR4** (`SearchBinder.cs`'s initial-index guard, `ControlFlowEmitter`'s,
  `OdoModel`'s, `EcBinder.cs:441`'s GR4/GR6/GR9 range-EC set) are **CORRECT**: GR4 is the serial scan and it is
  where EC-RANGE-SEARCH-INDEX/-NO-MATCH are set. So are BOTH goldens the note calls wrong —
  `2023/ec_range_search.cob` (GR4/GR6/GR9) and `2002/ca36_search_range_dispatch.cob` (GR1b2). The REAL SEARCH
  defects were five different sites: `GR5b` for the per-pass WHEN re-evaluation (that is GR1's sentence, "Any
  subscripting specified in a WHEN phrase is evaluated each time the conditions in that WHEN phrase are
  evaluated", with GR4's repeat), `GR5` for first-true-wins (GR4), `GR5–8` for the whole serial scan (GR1–GR4),
  and `GR8a/b/c` for the VARYING arms — GR8 is Format 2's one-sentence rule and **has no sub-items at all**;
  the serial VARYING rules are GR3 a) / b) 1. / b) 2. / c) 1. / c) 2.
* ⛔ **`OoBinder`'s "constant SR12 tail" no longer exists.** `OoConformance.ObjectRefAssignmentMismatch` is now a
  TABLE that returns the arm's own citation (`SR12 a)1.`, `SR12 a)2.`, `SR12 a)3.`, `SR12 b)3.`, `SR12c1`…), and
  the string `9.3.8.2/SET SR10` is nowhere in the tree. Fixed between the note's pin and HEAD (the PB389
  descriptor work); re-measured, not assumed.
* ⚠ **The "bare `.` receiver" of COBOLNET0869 was never a name-substitution bug.** The message template opened
  `SET … TO 'x'` with U+2026, and the diagnostic renderer transliterates to ASCII — so the user read `SET . TO
  'WS-N'`. The repair is the same either way (the receivers are in hand and are now named), but the diagnosis
  in this note was wrong about the mechanism.

**What landed.**

1. **The citation is a property of the SITE, not of the message string** — `src/Cobol.Net.Compiler/Binding/EcRaiseSite.cs`.
   RAISE (§14.9.29.3), GOBACK RAISING (§14.9.18.3) and EXIT PROGRAM / FUNCTION / METHOD RAISING (§14.9.14.3)
   are bound by one path, and each rule it enforces has a different ordinal per statement: level-3 name EXIT
   SR3 / GOBACK SR2 / RAISE SR1 · identifier-1 an object reference EXIT SR5 / GOBACK SR4 / RAISE SR2 (`a)`
   declared-class, `d)` universal) · the LAST phrase EXIT SR6 / GOBACK SR5. The record carries verb + clause +
   ordinals; `EcNameResolution.TryResolve`'s `requireLevel3: bool` became `level3: EcRaiseSite?`; the five call
   sites (CallBinder, ControlFlowBinder, UdfBinder, OoBinder ×2) pass the site. Five messages (COBOLNET0710,
   0717, 0849 ×3) and OoBinder's LAST-phrase message now compose their own statement's citation. **Measured:**
   `EXIT PROGRAM RAISING EXCEPTION EC-BOUND` printed `(ISO 14.9.29.3 SR1)` and now prints `(ISO 14.9.14.3 SR3)`;
   the GOBACK arm still prints `(ISO 14.9.18.3 SR2)`.
2. **The SET format ordinals** (§14.9.39.2: 4 = condition-setting, 7 = data-pointer-assignment, 10 =
   data-pointer-arithmetic) — 24 sites repaired across `BoundTree`, `SetBinder`, `SetEmitter`,
   `InitializeEmitter`, `PtrBinder`, `DiagnosticCatalog`, `CobolParserCore.g4`, two tests, three goldens and
   `kb/Spec/Lookup/IR Mapping.md`, including both user-visible COBOLNET0869 messages. `PtrBinder`/`BoundTree`'s
   "GR12–13" on the ADDRESS-OF-receiver arm is GR13 alone (GR12 is the identifier-5 pointer-receiver arm).
3. **The SEARCH GR numbering** — 12 sites, per the derivation above.
4. **Siblings swept out of the same shape:** `§14.9.28 Format 4` ×4 (PERFORM prints THREE formats; VARYING is a
   PHRASE, §14.9.28.4 GR12/GR13) · `§14.9.14.4 GR6` for EXIT PERFORM CYCLE (GR5 b); GR6 is EXIT PARAGRAPH) ·
   `§14.9.12.3 SR6` (that clause has four) · `§14.9.47.4 GR6` for VALIDATE (§14.9.47 is UNLOCK; VALIDATE is
   §14.9.50, whose GR6 IS the five-stage rule) · `§14.9.1 / §14.9.42` for the GIVING forms (ACCEPT and STOP;
   they are ADD §14.9.2 and SUBTRACT §14.9.44) · SUBTRACT's `§14.9.44.3 (GIVING resultant)` → SR4, the ADD
   twin's own wording.
5. **THE STRUCTURE (CLAUDE.md rule 5): an ORDINAL family in `scripts/spec/audit_code_citations.py`.**
   `cite.py` and `audit_doc_citations.py` both need a QUOTE, and a Format number is not one — so this whole
   class was unchecked. Five new checks, each exact: **FORMAT** (no such general format), **FORMAT-RULE** (the
   standard's own `FORMAT n` banners partition every rule block, so `Format 4 … GR12` where rule 12 is printed
   under FORMAT 7 is decidable), **FORMAT-NAME** (the line spells one format's three-part name and cites
   another's ordinal), **RULE** (the clause has no such rule number), **SUBITEM** (that rule has no sub-items).
   It is LINE-based, unlike the three comment-block checks — which is how a citation inside a DIAGNOSTIC
   MESSAGE STRING comes under a gate for the first time. `--self-test` fires each on its real defect and is
   silent on the repaired twin. Rule identity comes from `spec-rule-catalog.json` (a second rule parser read
   sub-items as rules); text, banners and every veto come from the standard.
6. **Drift tests.** `EcRaiseSiteDriftTests` re-derives all five sites' ordinals from the catalog by matching the
   rule TEXT, asserting each match is UNIQUE — a renumbering fails a test instead of misdirecting a reader.
   The negative pair `l1-exit-raising-level2-name` / **`pb388-goback-raising-level2-name`** (new) pins BOTH arms
   through the corpus runner, each `.err` now carrying its own statement's citation;
   `l1-exit-raising-ecuser-not-in-pd.err` likewise. `PointerDataTests.SetPointer_IndexNameReceiver_…` pins that
   the index-name receiver is refused as a CATEGORY error and that COBOLNET1639 is NOT emitted.

**⛔ RULE and SUBITEM found 195 sites in 124 files and DO NOT GATE YET.** They print their count on every gate
run and `--check-all` gates on them; the sweep that closes them needs its own note (one derivation per site —
e.g. `§14.9.6.4 GR11/GR12` where CLOSE has ten GRs, `§8.5.2.3 GR6` where it has four). Flagged to the registrar
in the implementer report; nothing here was suppressed to make a gate green.

**Rows — none re-verdicted, and each says why.**

* `GR-14.9.37.4-1` — **CLOSED BY LANDING TRAIN 36's RE-MEASURE (PARTIAL → CONFORMS).** PB405 (train 35)
  fixed the dispatcher-`break` idiom this row's verdict rests on; that work was NOT on this tree, so the row was
  left for the lander, and the lander measured it on the merged tree rather than inheriting either report's
  word: `PERFORM 3 TIMES … SEARCH E … WHEN E(IX1) = 3 GO TO HIT-PARA … END-PERFORM` now prints `LOOP` then
  `HIT-PARA` where it printed `LOOP` then `NOT-REACHED-2`, and the isolating control (a bare `GO TO` out of an
  inline `PERFORM 3 TIMES`, no SEARCH anywhere) prints `C1-OUT`. The witness is PB405's
  `DispatcherTransferIdiomDriftTests`, whose fixture carries a `GO TO` inside a SEARCH `AT END` phrase and one
  inside an EVALUATE arm, both inside an inline PERFORM, with a companion fact asserting those transfers really
  are inside lowered containers.
* `GR-14.9.39.4-12` / `-13` — re-measured on this tree at `--std 2002`: `SET P1 P2 TO ADDRESS OF BUF.` →
  `COBOL0001: missing token before 'BUF'`. Still DIVERGES. The mechanism is `setAddressStatement`'s hard-coded
  single receiver, not a citation, and it wants its own note (this note said so from the start).
* `SR-14.9.39.3-10` — the diagnostic half this note owned is GONE (the SR12 tail above). The row stays PARTIAL
  on PB389's half: `objectReferenceUsage` cannot spell a FACTORY-phrased class sender or ACTIVE-CLASS.
* `SR-14.9.39.3-2` — the FALSEHOOD is fixed: `SET IX TO P1` over `05 E PIC X OCCURS 3 INDEXED BY IX` no longer
  opens with `COBOLNET1639: 'IX' is not defined`; the pointer route asks `IndexFieldOf` FIRST and reports the
  category error it actually caught (§14.9.39.3 SR17 receiver, SR1 for the Format-1 alternative). The ROW stays
  PARTIAL: SR2 ("Identifier-2 shall reference a data item of class index") is still written down nowhere, and
  deciding what it does to `SET IX TO integer-item` is an adjudication, not a citation repair.



⚠ **FLAG PROVENANCE — read this before the flags.** This note's OWN mechanism (citation drift) changes no
program's behaviour: it is `silent`. `wrong_answer` and `rejects_legal_source` are set by two of the five
INVENTORY ROWS this note owns, whose divergences rest on *other* mechanisms (the dispatcher-`break` idiom and the
SET Format-7 receiver arity) — both re-measured on HEAD in **Rows** below. **Those two deserve their own notes**;
they are recorded here because the b2 clustering gave this note their rows and a row must not go unowned.

## THE MECHANISM

Four distinct sites, one shape: a clause number written for one verb, one format numbering or one edition, then
carried forward by copy without ever being re-derived — CLAUDE.md rule 1's named failure mode, operating in
*shipped diagnostic text* rather than in a design doc.

1. **Shared-verb diagnostics.** `EcBinder`/`EcNameResolution` serve RAISE, GOBACK RAISING and EXIT … RAISING
   from one code path, and each message hard-codes ONE verb's clause. `EcNameResolution.cs:52` prints
   *"(ISO §14.9.29.3 SR1)"* — RAISE's rule — for an EXIT PROGRAM statement; `EcBinder.cs:205` prints
   *"(ISO §14.9.18.3 SR2 …)"* — GOBACK's — likewise; `EcBinder.cs:178/:185/:195` print
   *"§14.9.18.3 SR4 / SR4d / SR4a"* where an EXIT statement's rules are §14.9.14.3 SR5 / SR5d / SR5a. The verb
   STRING is already threaded through this path (`"EXIT PROGRAM RAISING"` appears in every message); the clause
   is not.
2. **The SET format ordinals.** §14.9.39.2 numbers the formats 4 = condition-setting, 5 =
   object-reference-assignment, 7 = data-pointer-assignment. Eighteen sites in `src/` and `tests/` say otherwise
   — the data-pointer slice is labelled "Format 4" and the condition-name slice "Format 5" — and **two of them
   are the message text of COBOLNET0869**, so a programmer is sent to the condition-setting page for a pointer
   rule. `PtrBinder.cs` and `BoundTree.cs:762` get Format 7 right twenty lines away, so the file disagrees with
   itself.
3. **The SEARCH GR numbering.** Every Format-1 citation in the SEARCH path names, under the 2023 clause label
   `§14.9.37.4`, a rule number belonging to FORMAT 2, and the shift is uniform: **GR1→GR5, GR3→GR8, GR4→GR6**.
   What hid it is that the numbering is internally consistent and its most-cited member coincides (GR9 is the
   SEARCH ALL rule in both schemes), so the wrong scheme's citations all "look" right. It has already propagated
   out of `src/` into three test files and **two shipped goldens**.
4. **A clause that answers a different question.** `ControlFlowEmitter.EmitGoToDepending` documents itself as
   *"(ISO §14.9.20 Format 2)"* and *"the selector 'shall be an integer' (§14.9.20.3 SR2)"*. §14.9.20 is
   INITIALIZE. Because the clause-keyed source dossier greps on the citation, **the only file that implements
   GO TO DEPENDING's GR2 was absent from the 14.9.17 file list** — a wrong citation corrupts every mechanical
   selector built on citations, which is the cost that makes this more than cosmetics.

## THE RULES, QUOTED, EACH `--check`ED — AND THE TRAP THEY DEMONSTRATE

⛔ **The same sentence lives in three different clauses.** Running `--check` on the quote proves nothing about
which rule you meant; the ANTECEDENT ("this EXIT statement" / "this GOBACK statement") is what disambiguates:

* `--check 14.9.14.3 "If exception-name-1 is a level-3 exception-name for EC-USER, exception-name-1 shall be
  specified in the RAISING phrase of the procedure division header"` → **`OK §14.9.14.3 3)`** … *"of the source
  element in which this **EXIT** statement is contained."*
* the identical fragment `--check 14.9.18.3 …` → **`OK §14.9.18.3 2)`** … *"in which this **GOBACK** statement is
  contained."*
* `--check 14.9.29.3 "Exception-name-1 shall be a level-3 exception-name"` → **`OK §14.9.29.3 1)`** (RAISE), and
  `--check 14.9.14.3` on the same words → **`OK §14.9.14.3 3)`** (EXIT). Three OKs, three different rules.
* `--check 14.9.14.3 "Identifier-1 shall be an object reference, subject to the following constraints"` →
  **`OK §14.9.14.3 5)`**; `--check 14.9.14.3 "Identifier-1 shall not be a universal object reference"` →
  **`OK §14.9.14.3 5) d)`**. So COBOLNET0849's three messages want SR5 / SR5a / SR5d, not GOBACK's SR4 family.
* `--check 14.9.17.3 "Identifier-1 shall reference a numeric elementary data item that is an integer"` →
  **`OK §14.9.17.3 1)`** (GO TO's real rule), while `--check 14.9.20.1 "INITIALIZE statement provides the ability
  to set selected data items"` → **`OK §14.9.20.1 (General)`** — the clause the emitter names is INITIALIZE.
* `--check 14.9.14.4 "CYCLE phrase causes control to be passed to an implicit CONTINUE statement immediately
  preceding the END-PERFORM"` → **`OK §14.9.14.4 5) b)`**; `--check 14.9.14.4 "EXIT PARAGRAPH statement causes
  control to be passed to an implicit CONTINUE statement"` → **`OK §14.9.14.4 6)`**. `StatementEmitter.cs:140`
  cites **GR6** for EXIT PERFORM CYCLE — GR6 is EXIT PARAGRAPH; CYCLE is GR5b.
* `--check 14.9.39.2 "Format 7 (data-pointer-assignment)"`, `"Format 4 (condition-setting)"`,
  `"Format 5 (object-reference-assignment)"` → all three **`OK §14.9.39.2 (General formats)`**. A format ORDINAL
  is not a quotation, so `--check` can never catch this class on its own — see the repair.
* SEARCH, both halves of the collision: `--check 14.9.37.4 "If index-name-1 is specified in the INDEXED BY phrase
  in the OCCURS clause associated with identifier-1, the index referenced by index-name-1 is the search index"` →
  **`OK §14.9.37.4 3)`** (Format 1 — what `SearchBinder` implements), while `--check 14.9.37.4 "Any other indexes
  associated with identifier-1 remain unchanged by the search operation"` → **`OK §14.9.37.4 8)`** (Format 2 —
  what `SearchBinder` CITES). `--check 14.9.37.4 "control is transferred according to the rules for that
  statement"` → **`OK §14.9.37.4 1) b) 2.`**

## OBSERVED ON HEAD (`89044aa0`) — the compiler's own words

```
$ cobol.exe sr3_level2_name.cob --std 2023            # EXIT PROGRAM RAISING EXCEPTION EC-BOUND
error COBOLNET0710: EXIT PROGRAM RAISING: exception-name 'EC-BOUND' is a level-2 name; only a LEVEL-3
    exception-name may be raised (ISO 14.9.29.3 SR1)        <-- RAISE's rule, on an EXIT statement (want 14.9.14.3 SR3)
$ cobol.exe sr3_ecuser_not_in_pd.cob --std 2023
error COBOLNET0717: … (ISO 14.9.18.3 SR2 - otherwise EC-RAISING-NOT-SPECIFIED, Table 13)   <-- GOBACK's rule
$ cobol.exe sr5_nonobject_identifier.cob --std 2023
error COBOLNET0849: EXIT PROGRAM RAISING 'WS-N': identifier-1 shall be a USAGE OBJECT REFERENCE data item
    (ISO 14.9.18.3 SR4)                                      <-- GOBACK's SR4 (want 14.9.14.3 SR5)
$ cobol.exe c5_nonptr_send.cob --std 2023
error COBOLNET0869: SET . TO 'WS-N': a data-pointer sender shall be NULL or another USAGE POINTER item
    (ISO 14.9.39 Format 4; ADDRESS OF senders are a later increment)   <-- Format 7; and the parenthetical is
                                                                        stale (ADDRESS OF senders DO work today)
```

Note in that last line the receiver renders as a bare `.` — the message's name substitution is also broken on
this arm; worth fixing in the same pass.

**NOT OPEN — fixed between the pin and HEAD:** the adjudication's grammar-banner sweep (15 wrong `// VERB
(§14.9.N)` banners) and `CobolIO.g4`'s `openFileSpec` tape-phrase comment were repaired by `9c08e6fe`
("Citation sweep: ~200 phantom sites…"); re-measured here by matching every banner against the spec's own
`14.9.N` heading title, **29 of 31 now agree**, the two remaining being `ALTER` and `ENTRY`, which cite a clause
for statements that HAVE no 14.9 clause in 2023 and should cite none.

## THE CODE SITES, AND THE SHAPE OF THE REPAIR

Shared-verb diagnostics — `src/Cobol.Net.Compiler/Binding/EcNameResolution.cs:26, :52`;
`src/Cobol.Net.Compiler/Binding/Procedure/Verbs/EcBinder.cs:90, :166, :178, :185, :195, :205`;
`src/Cobol.Net.Compiler/CodeGen/StatementEmitter.cs:130, :140`.
SET ordinals — `SetBinder.cs:54, :192, :203, :213, :226, :245, :337, :449`; `BoundTree.cs:744`;
`SetEmitter.cs:32, :41, :178, :205`; `CobolParserCore.g4:1247, :1273`; `OoBinder.cs:859`;
`tests/Cobol.Net.Tests.Conformance/PointerDataTests.cs:8, :44`. (`SetBinder.cs:213` and `:226` are the two inside
COBOLNET0869's user-visible text.)
SEARCH GR scheme — `SearchBinder.cs:18-19, :45, :46, :48, :58`; `ControlFlowEmitter.cs:374, :396, :427`;
`BoundTree.cs:837`; `tests/Cobol.Net.Tests.Conformance/SearchDifferentialTests.cs:8`, `SpecPinnedNistTests.cs:19`,
`UdfInvocationTests.cs:171`; goldens `tests/conformance/2023/ec_range_search.cob:1-6` and
`tests/conformance/2002/ca36_search_range_dispatch.cob:5`.
GO TO — `ControlFlowEmitter.cs:32, :38`. Precision-only: `ArithmeticBinder.cs:80` passes
`"§14.9.44.3 (GIVING resultant)"` where the ADD twin at `:47` passes `"§14.9.2.3 SR4"`.
Still wrong downstream: `tests/Cobol.Net.Tests.Conformance/ArithmeticGivingDifferentialTests.cs:8`
(`§14.9.1 / §14.9.42` = ACCEPT/STOP; want `§14.9.2 / §14.9.44`).

**The repair is not 40 edits (CLAUDE.md rule 5).** Three structural pieces:

1. **A citation belongs to the SITE, not to the message string.** The shared EXIT/GOBACK/RAISE path already
   threads the verb; thread `(verb, clause)` as one value so a message physically cannot name another verb's
   clause. Same for the OO SET tail (`OoBinder.cs:942` appends a CONSTANT "SR12" sentence to every rejection on
   that path, so an SR10 violation prints two rule numbers, one of them wrong — see the SR-14.9.39.3-10 row).
2. **A citation LINT that resolves the ordinal, not just the quote.** `scripts/spec/cite.py --check` validates
   TEXT; a wrong FORMAT number or a wrong GR number inside a right clause is invisible to it. Extend the existing
   `scripts/spec/audit_doc_citations.py` over `.cs`, `.g4` and `.cob` comments to resolve `§<clause> Format <n>`
   against the general-format list the spec's own `<clause>.2` carries, and `§<clause> GR<n>/SR<n>` against the
   rule list — failing on a mismatch. **Make it fail once against the sites above before fixing them.**
3. **The banner check that already paid off.** The `// VERB (§14.9.N)` ↔ spec-heading match, run above by hand,
   found the two survivors; it is four lines of Python and belongs in the same lint.

## THE SIBLING SWEEP THE FIXER MUST DO

* Every message string in the tree that contains `ISO §` or `ISO 14.` and is produced by a path serving MORE THAN
  ONE verb — the EXIT/GOBACK/RAISE family is the one measured, but the arithmetic, MOVE and SET binders share
  screens the same way (`ArithmeticBinder`'s `Receivers(…, clause)` parameter is the right pattern: it is already
  a parameter, it is simply passed a coarser value on the SUBTRACT arm).
* Every `Format <n>` in `src/` and `tests/`, not just §14.9.39's: the ordinal drift is a 2002→2023 renumbering
  artefact and any clause whose formats were renumbered carries the same risk.
* Both SEARCH goldens and the three test files, since a corrected src comment beside an uncorrected golden banner
  re-seeds the error on the next read.
* `docs/` — rule 6 makes every doc describe the current compiler; a design doc that inherited "Format 4" for the
  pointer slice will re-seed all eighteen sites.

## Rows

* **`GR-14.9.37.4-1`** (was PARTIAL; **CONFORMS since landing train 36** — see “Rows” above) — the verdict
  rested on the **dispatcher-`break` idiom**, not on the citations:
  GR1b2's "control is transferred according to the rules for that statement" is emitted as
  `__pc = __searchR; break;` (`ControlFlowEmitter.cs:464-465`), and inside an inline PERFORM the `break` leaves
  the C# LOOP instead of returning to the dispatcher. **Re-measured on HEAD:** `r13_gr1b2_resume.cob` (the SEARCH
  inside `PERFORM 2 TIMES`, a `USE AFTER EXCEPTION CONDITION EC-RANGE-SEARCH-NO-MATCH` declarative doing
  `RESUME AT R-PARA`) prints `DECL-RAN` then `AFTER-PERFORM`; the byte-identical control `r14_gr1b2_control.cob`
  without the enclosing PERFORM prints `DECL-RAN` then `R-PARA`. **This is a WRONG ANSWER on legal source and it
  reaches every sibling `__pc = N; break;` site — `BoundGoTo` (StatementEmitter.cs:111), `BoundExitSection`,
  `BoundNextSentence`'s no-label fallback and the EcEmitter resume sites — while `EXIT PERFORM` already got the
  `goto __pexit` fix (StatementEmitter.cs:142). It wants its own note.**
* **`GR-14.9.39.4-12`** and **`GR-14.9.39.4-13`** (DIVERGES) — one grammar defect, not a citation one:
  `CobolParserCore.g4:1261-1265` hard-codes ONE receiver in each `setAddressStatement` alternative where Format 7
  prints `SET { ADDRESS OF data-name-1 | identifier-5 } … TO identifier-6` with a repeat ellipsis over the whole
  brace, so **legal source is rejected**. Re-measured on HEAD at `--std 2002`: `SET P1 P2 TO ADDRESS OF BUF.` →
  `COBOL0001: no viable alternative at input 'SETP1P2TOADDRESS'`; `SET ADDRESS OF C1 P1 TO P0.` →
  `COBOL0001: unexpected 'P1'`; `SET ADDRESS OF C1 ADDRESS OF C2 TO P.` → `COBOL0001: unexpected 'ADDRESS'`;
  control `SET P1 P2 TO P0.` (the spelling that escapes into `setToValueStatement`) runs and prints `C1=W`, so
  the EACH/ORDER semantics are right wherever the statement parses. This note's contribution to the two rows is
  the stale `PtrBinder.cs:38` comment ("GR12–13" on an arm only GR13 governs).
* **`SR-14.9.39.3-10`** (PARTIAL) — four of the rule's five alternatives are implemented; b1 (a FACTORY-phrased
  class sender) and c (ACTIVE-CLASS) are unreachable because `objectReferenceUsage` cannot spell them — that is
  **PB389's mechanism**, and the row is cross-referenced there. This note owns the row for the diagnostic half:
  `OoBinder.cs:942` appends the constant `(ISO 14.9.39.3 SR12 …)` tail to EVERY rejection on the path, so an SR10
  violation prints as `… (ISO 9.3.8.2/SET SR10) (ISO 14.9.39.3 SR12 …)`. **Re-measured on HEAD** with
  `p2b_class_noimpl.cob --std 2023` — the doubled, half-wrong citation is verbatim in the output above.
* **`SR-14.9.39.3-2`** (PARTIAL) — the rule ("Identifier-2 shall reference a data item of class index") is written
  down NOWHERE; its complement is enforced incidentally by the §8.8.1.1 arithmetic-operand screen, so every
  rejection cites 8.8.1.1 and a future narrowing of that screen (PB172 already narrowed it once) would silently
  delete SR2's only enforcement. The path that reaches SR2's question directly **states a falsehood about the
  program**: `SET IX TO P1` over `05 E PIC X OCCURS 3 INDEXED BY IX` prints, FIRST,
  `COBOLNET1639: 'IX' is not defined — no declaration in this source element gives the name 'IX'` (re-measured on
  HEAD), because `SetBinder.cs:337`'s pointer re-route resolves an index-name as a data item. The source is
  correctly refused; the first thing the user is told is untrue.

## `GR-14.9.37.4-1`'s wrong answer belongs to the `break`-in-a-lowered-loop family — 2026-09-04

This note owns the row because batch b2 recorded its verdict here, but the DEFECT under it is not a citation
defect: it is the dispatcher idiom `__pc = N; break;` emitted inside a C# loop, which is `PB405`'s mechanism.
Read `PB405` before touching it — the family is five emit sites (`PB405`, `PB413`, `PB414`, `PB403`'s
`BoundExitPerform` fallback and this SEARCH `RESUME AT`) and one repair. This note's `wrong_answer` flag exists
because of that row; the citation defects it otherwise records are `silent` and `rejects_legal_source`.

---

## ⛔ REGISTRAR #6 (2026-09-19) — what the post-landing reports add to this note

Three leads from the `PB388` implementer's own report and from trains 36-38 belong HERE rather than in a new
note, because this note already owns the mechanism (an inherited clause or rule ordinal) and two of the three
rows.

**1. The non-gating ordinal sweep is 203 sites, not 195.** The PB388 report said "195 rule-ordinal citations
… the RULE/SUBITEM sections list 124 files". Train 36 measured **203** on the merged tree; trains 37 and 38
each re-measured **203**, unchanged. The number moves with every landing that adds a comment, which is the
argument for finishing it rather than for re-counting it. **The gate for it already exists —
`python scripts/spec/audit_code_citations.py --check-all` — and flipping `--check-all` to `--check` is the
definition of done for this note's remaining half.** Each site needs its OWN derivation, not a bulk edit:
§14.9.6.4 has TEN general rules and CLOSE is cited at GR11/GR12; §14.9.12.3 has four SRs; §8.5.2.3 has four
GRs and is cited at GR6.

**2. `SR-14.9.39.3-2` (this note's row) is an ADJUDICATION before it is a repair.**
```
$ python scripts/spec/cite.py --check 14.9.39.3 "Identifier-2 shall reference a data item of class index"
  OK  §14.9.39.3 2)  (Syntax rules)
      2) Identifier-2 shall reference a data item of class index.
```
The rule is written down NOWHERE in the compiler. Its complement is enforced incidentally by the §8.8.1.1
arithmetic-operand screen, so a future narrowing of that screen deletes SR2's only enforcement. ⚠ **Whether
SR2 refuses `SET index-name TO integer-item` is an adjudication, not a repair** — do not "fix" it without
one, because the answer decides whether a very common statement stops compiling.

**3. The FORMAT-NAME check this note added was itself repaired structurally, and its TWIN is still open.**
Train 36 found the new check firing on `tests/conformance/negative/pb489-linage-operand-is-linage-counter.cob:6`
— a CORRECT line — because it tested whether the WHOLE LINE spelled any format name of the cited clause, so a
second citation's name was attributed to the first. Fixed by `_names_format` → `_name_hits` (positions) plus
`_owning_cite`. **The RULE-ORDINAL arm did not get `_owning_cite`**, and train 37 measured a false finding
from exactly that (203 → 204 on PB408's comment, worked around in the comment rather than in the audit).
That is filed as `kb/Work/PB900`, which clusters with this note and with PB838.

**Row note:** `GR-14.9.39.4-12` / `-13` (this note's) describe `setAddressStatement`'s hard-coded single
receiver against §14.9.39.2 Format 7's repeat. That lead is ALSO the subject of `kb/Work/PB450` (open),
whose title states it directly — "Format 7's receiver is additionally hard-coded to arity one". One
implementer takes the pair; nothing is re-filed.

## ⬆ Appended 2026-09-20 by REGISTRAR #7 — the SET diagnostics' elided receivers, MEASURED, and what is actually wrong

The `PB449` report asks that the `SET … TO SELF/SUPER`, `TO ENTRY` and `TO ADDRESS OF FUNCTION` messages be
added here, on the grounds that "the renderer eats" their `U+2026` so "the user reads `SET . TO 'X'`, a
statement nobody wrote".

**Half of that is not what happens on this tree.** `<scratch>/reg7/probe/r_set_super.cob`, `--std 2023`:

```
r_set_super.cob(7,12): error COBOLNET0867: SET … TO SUPER: SUPER shall not be the sending operand of an
object-reference SET (ISO §14.9.39.3 SR9)
```

The ellipsis renders as an ellipsis. Whether it degrades to `.` is a CONSOLE ENCODING question and belongs
to **PB899** (the CLI writing diagnostics in the OEM code page, byte-verified there), not here.

**What IS wrong, and what this note should carry:** the message elides the receiving operands *while the
binder holds them*. `SET U TO SUPER` should read `SET U TO SUPER`, naming U, exactly as the Format-7
instance this note already fixed does. The sites are `SetBinder.cs:637`, `:686`, `:694`, `:742`, `:754`,
`:763` and `OoBinder.cs:834`, `:865` — eight messages built with a literal `…` where the receiver list is
in scope. That is the same repair, one format family over, and it belongs to this note's sweep.

⚠ Two adjacent facts for whoever takes it. **(1)** COBOLNET0867 has NO `DiagnosticCatalog` descriptor at
all and thirteen raw call sites in `OoBinder.cs` — that is **PB914**, and it is the same file and the same
sitting. **(2)** The ordinal-citation sweep this note owns stood at **208** on the train-42 tree (PB246's
report and the train-40 lander both print it); it was 203 at train 38. The number moves with every landing,
which is the argument for finishing the sweep rather than re-counting it.


## ⬆ THE REGISTRAR-#7 APPENDIX IS LANDED — 2026-09-20/21 (wave 42 group G, `f09cda1f6`)

The appendix above reproduced exactly as filed and is repaired. **Structurally, not as eight edits**
(CLAUDE.md rule 5): the naming of a SET receiver list had TEN hand-written copies of
`string.Join(' ', refs.Select(r => $"'{r.GetText()}'"))` beside FIVE siblings that elided the same list, so
it now has ONE home — `SetFormatSelection.Written` — and the SELF/SUPER sender word travels with the flag
(`sorSelf || sorSuper` had collapsed two different statements into one bool). Every receiving brace in
§14.9.39.2 is printed `{ … } …`, so every SET diagnostic about a receiver is about a LIST, and the binder
holds that list where it reports.

**⛔ AND THE SWEEP FOUND AN INHERITED CITATION IN SHIPPED DIAGNOSTIC TEXT** — this note's own mechanism, one
format family over. The SELF-outside-a-method arm cited §14.9.39.3 SR12 c). SR12 governs a receiver described
with an OBJECT-CLASS-NAME and its c)3./c)4. are about the factory-vs-instance PLACEMENT of a method that
exists; the probe's receiver is a UNIVERSAL object reference, which SR12 never reaches. The rule that SELF
needs a method AT ALL is the identifier's own —
`--check 8.4.3.8.3 "This identifier format may be specified only in a method definition"` → **OK
§8.4.3.8.3 1)** — and all three sites that enforce that one sentence now cite it (OoBinder's SET arm, its
INVOKE SELF/SUPER arm and EcBinder's RAISE SELF arm; the latter two cited the bare construct clause §8.4.3.8,
which is PB838's "a citation that omits the subclassifying subclause is invisible" shape). SUPER's
§14.9.39.3 SR9 was re-derived and is CORRECT.

Witnesses: `tests/conformance/negative/pb388-set-pointer-self-sender` (COBOLNET0869, the whole message in the
`.err`) · `pb388-set-object-ref-self-outside-method` (COBOLNET0867 with the re-derived §8.4.3.8.3 SR1) ·
`SetDiagnosticNamesReceiversDriftTests` (Unit), which caught a TENTH live copy of the idiom on its first run.

**WHY THIS NOTE IS STILL `half`, stated precisely.** Nothing in the citation half remains open and the
appendix is closed. Two of the five rows still have no OPEN owner to move to, which is the only reason:

* `GR-14.9.37.4-1` — CONFORMS since landing train 36; no longer a defective row.
* `GR-14.9.39.4-12` / `-13` — **`kb/Work/PB450` (open) should adopt them**: its title already states
  "Format 7's receiver is additionally hard-coded to arity one", which IS these rows' mechanism. Flagged to
  the registrar rather than moved here, because moving a row is a claim about PB450's scope and PB450's body
  has to say so in the same change set.
* `SR-14.9.39.3-10` — its intended home `kb/Work/PB389` is **`landed`**, so it cannot take an open defective
  row without turning `DefectiveRowCoverageDriftTests` red. It needs a new owner (a registrar id).
* `SR-14.9.39.3-2` — "Identifier-2 shall reference a data item of class index" is written down nowhere, and
  deciding what it does to `SET IX TO integer-item` is an ADJUDICATION, not a citation repair. No owner.

## Registrar handoff 2026-09-21 by **REGISTRAR #8** — the two orphan rows have owners, and the third was already stale

This note's own implementer report closes with *"PB388 cannot close until two rows get an owner — registrar
action, not a defect"*. Done here, and a third row moved with them. **Nothing is removed from this note's
`inventory_rows`**: a row may be co-claimed, the `DefectiveRowCoverage` gate needs at least one non-terminal
claimer, and dropping a claim is the landing's business, not a registrar's.

* **`GR-14.9.39.4-12` and `GR-14.9.39.4-13`** (both DIVERGES/GAP) → **`kb/Work/PB450`** (open), which is
  *"SET's printed OUTER REPETITIONS were dropped from the grammar in Format 4 and Format 7"*. Both rules are
  Format 7's "EACH data item referenced by identifier-5 / each based item referenced by data-name-1 IN THE ORDER
  SPECIFIED" — the receiver ARITY this note names, and PB450's mechanism exactly. Added to its rows.
* **`SR-14.9.39.3-2`** (PARTIAL/GAP) → **`kb/Work/PB212`** (open), the Format-1 SET screen. SR2 is the SENDER
  half of the rule whose receiver half is SR1, and registrar #7 already recorded in PB212 that
  *"`BindSetTo`'s Format-1 arm has no sender screen at all, so SR1 and SR4 are ONE fix."* SR2 is that screen.
  The adjudication this note flags — what SR2 does to `SET IX TO integer-item` — is recorded INSIDE PB212 as a
  DETERMINATION rather than escalated, on `kb/Work/PB921`'s precedent, because the rule has to be written down
  either way.
* ⭐ **`SR-14.9.39.3-10`** needs no new note at all: this note says the row *"stays PARTIAL on PB389's half:
  `objectReferenceUsage` cannot spell a FACTORY-phrased class sender or ACTIVE-CLASS."* **That is no longer true
  on this tree.** `src/Cobol.Net.Frontend/Grammar/Core/CobolOO.g4:224` reads

  ```
  objectReferenceUsage
      : OBJECT REFERENCE (FACTORY OF)? ACTIVE_CLASS          // GR22 e)
      | OBJECT REFERENCE (FACTORY OF)? className ONLY?       // GR22 c)/d)
      | OBJECT REFERENCE                                     // GR22 b)
      ;
  ```

  — `kb/Work/PB496` landed the missing alternatives. The row is now defective for want of a VERDICT, not for
  want of a surface, which is `kb/Work/PB929`'s population; it is claimed there for the aimed golden round.

## Backlog tally re-measured 2026-09-21 by **REGISTRAR #8**

`python scripts/spec/audit_code_citations.py` on this tree (main `c774def44`):

```
⚠ 328 MEASURED, NOT YET GATING (155 DIAG-UNQUALIFIED · 105 RULE · 68 SUBITEM)
```

Unchanged from the train-43 lander's own re-measurement, so the number in plan §9 is current. The **155
`DIAG-UNQUALIFIED`** sites are this note's implementer report's "New defects" item 1, filed there as *"the same
root cause as the 102 RULE + 68 SUBITEM backlog — ONE ordinal-citation sweep, one note"* — i.e. THIS note's
sweep, not a new one. Two fresh instances were produced incidentally by this pass's probes and are worth having
as witnesses, because they show the shape is still being EMITTED and not merely still present:

```
error COBOLNET0900: numeric-literal VALUE for a numeric-edited item requires COBOL-2023 … (ISO §13.18.63 SR6 …)
error COBOLNET0898: condition-name 'NR': the VALUE of a national data item … (ISO §13.18.63 SR5)
```

Both cite `§13.18.63` where the syntax rules live in `§13.18.63.3`.
