---
title: "PB1187 — TERMINATE restores the control data items (§14.9.46.4 GR3 d)) BEFORE producing the last page footing and the report footing (GR3 c)), so the RF — and the final PF — are composed from the CURRENT control values instead of the prior ones (`RF 3 4` where the rule gives `RF 1 1`)"
id: PB1187
kind: defect
status: open
severity: MAJOR
area: runtime/report-writer
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1186", "PB1188"]
spec_refs: ["14.9.46.4", "13.18.57.4"]
inventory_rows: ["GR-14.9.46.4-3", "GR-13.18.16.4-5"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14c** from lane-3 adjudication batch **d1-14c** (adjudicated on the pinned tree `68bbdb00f`; trains 58/59 checked — nothing that landed since touches this mechanism unless the body says otherwise).

# PB1187 — OPEN

## The rule (every citation re-run by the registrar through `cite.py --check`)

> c) The report footing is printed, if defined. (GR3 a) prior values in, b) control footings, c) report footing, d) control items restored)

`python scripts/spec/cite.py --check 14.9.46.4 "…"` → `OK  §14.9.46.4 3) c)  (General rules)`

## Finding — `terminate-statement` (wrong-answer; rows `GR-14.9.46.4-3`)

**Mechanism.** TerminateCore restores the control data items (GR3 d) before producing the last page footing and the report footing (GR3 c), so the report footing is composed from the CURRENT control values instead of the prior ones

**Repro.** RD R-1 CONTROL IS FINAL WS-A WS-B; DE/CF WS-B/CF WS-A/CF FINAL groups; 01 RF-1 TYPE RF LINE PLUS 1 with COLUMN 4 PIC 9 SOURCE WS-A and COLUMN 6 PIC 9 SOURCE WS-B. WS-A=1 WS-B=1; OPEN OUTPUT; INITIATE R-1; GENERATE DET-1; MOVE 3 TO WS-A; MOVE 4 TO WS-B; TERMINATE R-1; CLOSE. (out/probe/terminate-statement/p1.cob)

**Expected from the spec.** §14.9.46.4 GR3 orders the actions a) control items <- prior values, b) control footings, c) report footing, d) control items restored. The RF is produced between a) and d), so it shows the prior values: last line `RF 1 1`. The last page's page footing (printed before the RF per §13.18.57.4 GR6f) likewise precedes d).

**Observed / reasoning.** Report file: `DE 1 1 / CFB 1 1 / CFA 1 1 / CFF 1 / RF 3 4` at --std 2023, 2002 and 85. In ReportWriter.TerminateCore the GR3d loop `for (...) _controls[i].Set(current[i]);` sits inside the `if (_controls.Count > 0 && ...)` block, before `PresentPageFooting()` and `PresentHeadingFooting(rf)`. Fix: move the save/restore to bracket the PF and RF presentations too (d executes last), and pin it with a golden whose RF and PF SOURCE a control item changed after the last GENERATE.

**Code site.** src/Cobol.Net.Runtime/IO/ReportWriter.cs#TerminateCore (the GR3d restore loop placed before the PF/RF presentation)

## What the fixer owes

Move ReportWriter.TerminateCore's GR3 d) restore loop (`_controls[i].Set(current[i])`) to AFTER PresentPageFooting() and PresentHeadingFooting(rf). Golden p1.cob whose RF and PF SOURCE a control item changed after the last GENERATE, at 85 and 2023.

## Extended 2026-09-24 by REGISTRAR 14d (folded from PB1235 — the same mechanism)

Registrar 14c's PB1187 already owns TERMINATE's GR3 step order (restore d) runs before the PF/RF of c)); PB1235 measured the same inversion and adds the §13.18.16.4 GR5 row it also violates.

PB1235 as filed: PB1235 — TERMINATE restores the control items to their current values (§14.9.46.4 GR3 d)) BEFORE printing the final page footing and the report footing, where the rule orders a) prior values in, b) the control footings, c) the report footing, d) restore — so the RF (and the last PF) SOURCE the CURRENT control values instead of the prior ones

Filed 2026-09-24 by **REGISTRAR 14d** from adjudication batch d1-14d (the adjudicators' pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probes under `scratchpad\adj-d1\out13\probe\`). The given repro was re-run once on registrar 14d's Debug build of `071ff04ea` (main after trains 58/59) and reproduces unless stated otherwise.

# PB1235 — OPEN

## Measured

**control-clause** — TerminateCore restores the control items to their current values (GR3d) BEFORE printing the final page footing and the report footing, where §14.9.46.4 orders a) priors in, b) CFs, c) report footing, d) restore - so the RF (and the last PF, printed before the RF) see the NEW values instead of the prior ones.

- **Repro:** probe out13/probe/control-clause/term.cob: CONTROLS ARE FINAL CX CN; last GENERATE with CX=B CN=3; then MOVE 9 TO CN; TERMINATE R-1. RF `02 COLUMN 7 PIC 999 SOURCE CN`, PF likewise.
- **Expected (spec):** RF prints `RF  B 003` and the final PF `PF  B 003`: §14.9.46.4 GR3 c) "The report footing is printed, if defined." precedes d) "The contents of any control data items are restored to the values they had at the start of execution of the TERMINATE statement." (both cite OK), and §13.18.16.4 GR5 treats TERMINATE 'as though a highest-level control break had been detected' (cite OK 5)).
- **Observed:** Output `PF  B 009` / `RF  B 009` at --std 85/2002/2014/2023 (CFs correctly show B 003). ReportWriter.cs TerminateCore: `for (...) _controls[i].Set(current[i]); // GR3d` runs before `PresentPageFooting()` and `PresentHeadingFooting(rf) // GR3c`. Outside this chunk's four rows (GR-13.18.16.4-5 was closed by PB205) - observed while probing GR4; unfiled in kb/Work.
- **Code site:** src/Cobol.Net.Runtime/IO/ReportWriter.cs#TerminateCore (~lines 578-593)
- **Clause (adjudicator):** 14.9.46.4

## The rules — every citation re-run through `cite.py --check` by registrar 14d

- `GR-14.9.46.4-3`: `cite.py --check 14.9.46.4 "If at least one GENERATE statement has been executed for a report during the interval"` → OK  §14.9.46.4 3)  (General rules)
- `GR-13.18.16.4-5`: `cite.py --check 13.18.16.4 "When a TERMINATE statement is executed, if any control footing is defined for the report"` → OK  §13.18.16.4 5)  (General rules)

## Rows claimed

- `GR-14.9.46.4-3` — batch 14d verdict (not in this batch; inventory verdict stands until the fixer re-derives it)
- `GR-13.18.16.4-5` — batch 14d verdict (not in this batch; inventory verdict stands until the fixer re-derives it)

## What the fixer owes (sibling sweep)

ReportWriter.cs#TerminateCore ~578-593. GR-13.18.16.4-5 is inventory-CONFORMS (pb205/rw_suppress pin the GENERATE arm, not TERMINATE); the fixer re-derives it.
