---
title: "PB546 — ONE national leaf has TWO widths and the consumers split: DataItem.ElementaryByteWidth answers `2 * ElementaryImageWidth` (the documented D-N1 UTF-16 model, what FUNCTION BYTE-LENGTH / FUNCTION LENGTH / the declared group size report) while DataItem.IsCharacterImage/ImageWidth deliberately cont"
id: PB546
kind: defect
status: open
severity: MAJOR
area: codegen/data-division
wrong_answer: true
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: true
blocked_by: []
cluster: [PB943, PB944]
spec_refs: ["13.18.60.4"]
inventory_rows: ["GR-13.18.60.4-8", "SR-14.8.2.2-1", "GR-8.4.3.3.4-3"]
tags: [cobolsharp, work, defect]
---

# PB546

## Mechanism — usage-clause-p3
usage-p3/F2 — ONE national leaf has TWO widths and the consumers split: DataItem.ElementaryByteWidth answers `2 * ElementaryImageWidth` (the documented D-N1 UTF-16 model, what FUNCTION BYTE-LENGTH / FUNCTION LENGTH / the declared group size report) while DataItem.IsCharacterImage/ImageWidth deliberately contributes the leaf's CHARACTER positions ("never byte-doubled for national"), which is what the group-image codec and every channel built on it lay out. Every leaf DECLARED after a national leaf therefore sits at half the offset the model published, and a group MOVE, a reference-modified group and a CALL BY REFERENCE window all read the wrong bytes with no diagnostic. The FILE-record channel refuses the same shape LOUD (COBOLNET0899), so one of the four byte channels is honest and three are silently wrong — and the drift test that exists to catch exactly this, ImageWidthIsStorageWidthTests, carves national out by name and calls it "the ONE sanctioned divergence", so it can never go red over it. This is PB188's landed shape (a wrong-width window "silently shifting every following member of the group image") repeated for the national leaf, and no kb/Work note owns it: PB327 owns the FILE channel only, PB231 the BASED/ALLOCATE refusal, both rejects-legal-source rather than wrong-answer.

### Expected, from the standard
13.18.60.4 GR8 (cite.py --check OK): "National characters shall be represented in the storage of the computer as characters of a uniform size equal to or a multiple of the size of characters in the computer's alphanumeric character set. Each implementor shall specify the size and representation of characters stored for usage NATIONAL." ONE uniform size, everywhere. COBOL.NET's specified size is 2 bytes per national position (D-N1; docs/CONFORMANCE.md DOC-A.1-33 names UTF-16BE as the storage form and DOC-A.1-209 relies on "the national determination is 2 bytes per position" to satisfy GR7's own <= test). So for `01 G. 05 N PIC N(2). 05 A PIC X(2).` the group is 6 bytes with N at bytes 1-4 (00 41 00 42) and A at bytes 5-6: BYTE-LENGTH-G=6, A-AT-5-6=[YZ], and a callee with `01 W PIC X(6)` reads 0,65,0,66,89,90 through FUNCTION ORD minus one.

```
$ python scripts/spec/cite.py --check 13.18.60.4 "National characters shall be represented in the storage of the computer as characters of a uniform size equal to or a multiple of the size o..."
  OK  §13.18.60.4 8)  (General rules)
```

### Observed
Measured with the pinned compiler at --std 2023. q73: BYTE-LENGTH-G=6, LENGTH-G=6, BYTE-LENGTH-N=4 — the model publishes the 2-byte size — but A-AT-3-4=[YZ] and A-AT-5-6=[  ]: the alphanumeric leaf declared at bytes 5-6 is actually at 3-4 and the declared tail is blank. q52 (CALL BY REFERENCE into `01 W PIC X(6)`) returns ORD 66 67 90 91 33 33 = A,B,Y,Z,space,space — the same halved layout across a program boundary. q25/q27 (group MOVE into PIC X(8)) return the same. q60 writes the same record shape to a SEQUENTIAL file and is refused: COBOLNET0899 "national data in a file record (data item 'N' of record 'R') is recognized but not yet implemented - the record codec is single-byte and the national character is two bytes (Phase 4a residue; ISO 8.1.2 / 13.18.60.4 GR8)". The code states the split in its own comments: DataItem.cs ElementaryByteWidth `Usage.National => 2 * ElementaryImageWidth` versus IsCharacterImage's "national and boolean leaves ... contribute their CHARACTER positions to a group image (ImageWidth = Length - never byte-doubled for national)", while IsImageCapable's doc comment asserts "ImageWidth ... IS ByteWidth for every item that reaches an image - the ONE-WIDTH invariant". tests/Cobol.Net.Tests.Unit/ImageWidthIsStorageWidthTests.cs#NationalLeaf_IsExactlyTwoBytesPerPosition asserts ImageWidth 4 / ByteWidth 8 and labels it sanctioned, which is why no gate is red today.

### Repro
Write to E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/usage-p3/q73_gr8_group_national_only.cob:

       IDENTIFICATION DIVISION.
       PROGRAM-ID. UP3GR8J.
       DATA DIVISION.
       WORKING-STORAGE SECTION.
       01 G.
          05 N PIC N(2) USAGE NATIONAL.
          05 A PIC X(2).
       PROCEDURE DIVISION.
       MAIN.
           MOVE N"AB" TO N
           MOVE "YZ" TO A
           DISPLAY "BYTE-LENGTH-G=" FUNCTION BYTE-LENGTH(G)
           DISPLAY "LENGTH-G=" FUNCTION LENGTH(G)
           DISPLAY "A-AT-3-4=[" G(3:2) "]"
           DISPLAY "A-AT-5-6=[" G(5:2) "]"
           STOP RUN.

Run: E:/CobolSharp/.claude/worktrees/lane3-pin-89044aa0/src/Cobol.Net.Cli/bin/Debug/net10.0/cobol.exe q73_gr8_group_national_only.cob --run --std 2023 -o q73.dll
The CALL variant is q52_gr8_call.cob (callee linkage `01 W PIC X(6)`), the group-MOVE variant q25/q27, and the loud FILE variant q60_gr8_file.cob.

### Code site
src/Cobol.Net.Compiler/Binding/Model/DataItem.cs#DataItem.ElementaryByteWidth (Usage.National arm) vs src/Cobol.Net.Compiler/Binding/Model/DataItem.cs#DataItem.IsCharacterImage / DataItem.ImageWidth; consumers in src/Cobol.Net.Compiler/CodeGen/DataDivision/GroupImageCodec.cs and RecordStructEmitter; the sanctioning drift test tests/Cobol.Net.Tests.Unit/ImageWidthIsStorageWidthTests.cs#NationalLeaf_IsExactlyTwoBytesPerPosition

## Sibling sweep the fixer owes
Every bug here is a pattern (CLAUDE.md rule 4). Before closing this note, sweep the sibling arms named in the mechanism above and add the ones that fail to `inventory_rows`.

## Provenance
Lane-3 adjudication batch b3 (subjects: usage-clause-p3), adjudicated against the compiler pinned at 89044aa0 and RE-PROBED on the registrar's worktree cut from 6e7caa91 (a 1942-program sweep of `out-b3/probe/` plus targeted re-probes); the finding reproduces on today's tree.

## Repro programs
`E:\Temp\claude\E--CobolSharp\a9d94900-79fe-4a38-89f0-921976861543\scratchpad\repros\PB546\` — usage-p3. The implementer's opening move is to rerun them against a freshly built `cobol.exe`.

## Re-measured 2026-09-05 by the PB231 NATIONAL-third implementer — and now an OWNER DECISION

PB231's NATIONAL third (train 7) gave every BYTE-ADDRESSED shape of a record — BASED, EXTERNAL, REDEFINES — the
storage extent §13.18.44.4 GR1 and §14.9.3.4 GR3 count (two bytes per national position under D-N1), moving nine
`ImageWidth` byte-channel sites to `ElementaryByteWidth`, and DELIBERATELY left the character-position channel
(`RenamesPlace` widths, `AsIfPic`'s GR2b `m`, `DisplayTextWidth`, `IsCharacterImage`, `GroupImageCodec.AsImageOf`)
on `ImageWidth`, because moving it is THIS note. The two storage shapes of one record now disagree in the open:

```cobol
       01 W.
          05 W-N PIC N(2) VALUE N"AB".
          05 W-X PIC X(2) VALUE "HI".
           DISPLAY W                           *> ABHI   (4 positions)
           DISPLAY FUNCTION LENGTH(W)          *> 6
           DISPLAY FUNCTION BYTE-LENGTH(W)     *> 6
```

while the byte-identical `01 R BASED. 05 R-N PIC N(2). 05 R-X PIC X(2).` allocates 6 bytes and reads its members
positionally at the right offsets. The FD/SD record area, marshalled by the same character-position codec, cannot
carry a national leaf at all — `DataBinder.GateNationalRecords` refuses it loudly, the correct refusal PB231 did
NOT fold into the byte-window gate (recorded as D-N5 in `COBOLNET_DATA_MODEL_DESIGN.md`: folding it would have
traded a loud correct refusal for a silent halving on disk).

**The derivation.** §15.50.4 r3 makes an alphanumeric group's LENGTH "the length of argument-1 in alphanumeric
character positions" (`OK §15.50.4 3)`); §13.18.60.4 GR8 makes a national character's representation "a uniform
size equal to or a multiple of" an alphanumeric one (`OK §13.18.60.4 8)`) — two positions under D-N1; §8.5.2.1
makes a group's character positions the storage of its subordinates. A group holding `N(2)` + `X(2)` is six
alphanumeric character positions and `DISPLAY W` sends those six. `docs/CONFORMANCE.md` DOC-A.1-57 documents the
current behaviour as "a NATIONAL member displays ONE character per position while LENGTH/BYTE-LENGTH count its TWO
bytes each — the one sanctioned width divergence (D-N1/D-N3)". The derivation says the divergence is a wrong
answer, not a determination.

**Why `blocked: true` (on the owner, not on a note).** The fix — byte-double `ImageWidth` for a national leaf, the
V59/PB164 move for national — re-decides group DISPLAY, group MOVE, group reference modification, §13.18.29.4 GR2b's
as-if `PICTURE N(m)` and the on-disk record layout at once, and overturns DOC-A.1-57 together with the A.1 items
that lean on the same width (measure them from the row's `Pinned by` cells and the D-N3 cross-references).
Blast radius measured by the implementer: six positive corpus files put a national leaf under a group
(`2002/intrinsics_byte_length`, `2014/pb206_value_size_categories`, `2023/pb164_vlg_display`,
`2023/pb207_bit_group_value`, `2023/pb61_length_byte_length_rule_branches`, `2023/pb79_group_usage_national`) plus
six negatives; every expected output must be RE-DERIVED from §15.50.4 r3 / §13.18.60.4 GR8 before it is changed.

**⛔ Bare question for the owner:** should a group containing a NATIONAL member present its national characters as
two alphanumeric positions each in every character-position channel (DISPLAY, group MOVE, reference modification,
the FD/SD record area), as §15.50.4 r3 with §13.18.60.4 GR8 derives — overturning DOC-A.1-57's documented "one
sanctioned width divergence" — or does the divergence stand as an owner-signed determination?

## 2026-09-05 — the determination was TAKEN by PB327 (landing train 11)

PB327 (the record image becomes the record BYTES) could not land without choosing: an alphanumeric group containing a national leaf now MOVEs, compares and DISPLAYs as its BYTES — the reading §13.18.29.4 GR2 NOTE predicts, whose named remedy (GROUP-USAGE NATIONAL) is now implemented as the `AsNat()` face. D-N5 is rewritten to DISCHARGED and DOC-A.1-57 no longer carries the "one sanctioned width divergence". The bare question above therefore changes shape: does the owner want it REVERSED to the character reading for DISPLAY? The reversal is one arm in `PlaceRenderer.GroupImage` plus two goldens. Stays `blocked: true` as the holder of that question.

## Two more consumers — REGISTRAR #12 (2026-09-22, main `37f19e028`)

Wave 49 group A (PB943/PB944 implementer) found the split on the GROUP-RECEIVER side, re-measured here:
- `scratchpad/w49a/p546.cob`: `01 NG. 05 NE PIC N(2) USAGE NATIONAL. 05 AX PIC X(2).` then `MOVE "0A0B0C" TO NG` →
  **`AX=[  ]`**; §14.9.25.4 GR4's unconverted alphanumeric move fills NG's bytes left to right, so AX receives `0C`.
  `MoveEmitter.EmitGroupMove` sizes the receiver by `target.Item.ImageWidth` (characters) while the group codec counts
  bytes.
- `scratchpad/reg12/q09_dlnat.cob`: `MOVE G TO DN` (G a 3-character group, DN `PIC N DYNAMIC LENGTH`) gives
  `FUNCTION LENGTH(DN)` = **6**, while the same move into `FN PIC N(3)` stores `ABC`: the IsDynamicLength arm of
  `EmitGroupToElementaryMove` calls DynStore on the group's image without the decode the fixed national/bit receivers now
  get (the one receiver shape PB943/PB944's branch does not cover).
`OK  §14.9.25.4 4)` — "Any move that is not an elementary move, and does not reference a variable-length group, is treated
exactly as if it were an alphanumeric to alphanumeric elementary move, except that there is no conversion of data from
one form of internal representation to another." Both are this note's mechanism (one national leaf, two widths):
the fix that makes the width single must reach both, and each gets a golden.

## Extended 2026-09-24 by REGISTRAR 14b (adjudication batch d1-14b, pinned @68bbdb00f)
The two-widths split reaches the parameter conformance screen: §14.8.2.2 SR1's 'same number or a smaller number of bytes' is computed on `DataItem.ImageWidth` (one unit per national position) in `OoConformance.DescriptionMismatch` (`formal.ImageWidth > arg.ImageWidth`, `f.Length > arg.ImageWidth`), so an alphanumeric group holding national leaves is under-measured and legal pairs are refused. Row co-claimed with PB1112 (the category half).
`cite.py --check 14.8.2.2 "the formal parameter shall be described with the same number or a smaller number of bytes as the corresponding argument"` → `OK  §14.8.2.2 1)  (Group items)`

## Extended 2026-09-24 by REGISTRAR 15c (lane-3 batch d2-15c, pinned tree `071ff04ea`)

Registrar 15c: one more consumer split — a reference-modified BOOLEAN item of USAGE NATIONAL (`PIC 1(4) USAGE NATIONAL`, B1(1:2)) is sized one byte per position (BYTE-LENGTH 2, no CobolBits.NatBytes wrap) while the whole item and the numeric-national slice use the national width (BYTE-LENGTH(B1) = 8).

## The rule (every citation re-run by the registrar through `cite.py --check`)

> as if it were redefined as a data item of class and category national of the same size

`python scripts/spec/cite.py --check 8.4.3.3.4 "…"` → `OK  §8.4.3.3.4 3)  (General rules)`

### Refuter evidence — row `GR-8.4.3.3.4-3` (PARTIAL)

PARTIAL — GR3 applies to EVERY usage-NATIONAL category other than national, and 13.18.60.3 SR12 makes BOOLEAN one of them (boolean, national, national-edited, numeric, numeric-edited). The adjudicator probed numeric/numeric-edited/national-edited/national-group only. For a PIC 1(4) USAGE NATIONAL item the ref-mod slice is NOT operated upon as a national item: FUNCTION BYTE-LENGTH(B1(1:2)) returns 2 while BYTE-LENGTH(B1) returns 8, i.e. the 2-position slice is sized as 1 byte per position (emitted CobolIntrinsics.ByteLength(CobolString.RefMod(B1,1,2)) with no CobolBits.NatBytes wrap, whereas the numeric-national slice gets NatBytes and correctly returns 4). As-if-national-of-same-size (GR3) + GR6 usage preserved => 2 national positions = 4 bytes (15.14.4 GR1). Numeric/numeric-edited/national-edited/group arms re-verified OK (TRAILING SEPARATE -123 (3:2) -> '3-', GR4). Test-ref pb646_national_form_numeric pins only the numeric arm.

**Code site.** src/Cobol.Net.Compiler/Binding/Model/Place.cs#RefModPlace.CategoryOf; src/Cobol.Net.Compiler/Binding/Model/Place.cs#RefModPlace.PositionCount
