---
title: "PB1296 — the report engine has no sum-counter SIZE ERROR: SumEntry.Value is an unbounded long accumulated by `s.Value += t.Addend()` with no test against the counter's §13.18.54.4 GR1 digit count, so EC-REPORT-SUM-SIZE is never raised and an overflowed counter prints truncated digits"
id: PB1296
kind: defect
status: open
severity: MAJOR
area: runtime/report-writer
wrong_answer: true
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1295", "PB1297"]
spec_refs: ["13.18.54.4"]
inventory_rows: ["GR-13.18.54.4-4", "GR-13.18.54.4-2"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14e** from adjudication batch d1-14e (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probe sources under the batch's `out13/probe/<subject>/`). Checked against main 071ff04ea: trains 58/59 did not touch this mechanism.

# PB1296 — OPEN

## The rules (every quotation run through `cite.py --check` by the registrar)

- `python scripts/spec/cite.py --check 13.18.54.4 "Each addition is tested for size error; if a size error occurs, the EC-REPORT-SUM-SIZE exception condition is set to exist"` → `OK  §13.18.54.4 3)  (General rules)`
- `python scripts/spec/cite.py --check 13.18.54.4 "the printable item is filled with spaces"` → `OK  §13.18.54.4 4)  (General rules)`

## Measured

### Finding sum-clause — ISO/IEC 1989:2023 13.18.54.4 GR1, GR2, GR3, GR4 (wrong-answer; rows GR-13.18.54.4-4, GR-13.18.54.4-2)

**Mechanism.** The report engine has no sum-counter SIZE ERROR INDICATOR: SumEntry.Value is an unbounded long accumulated by `s.Value += t.Addend()` with no size-error test against the counter's GR1 digit count, so EC-REPORT-SUM-SIZE is never raised and an overflowed counter prints its MOVE-truncated low digits instead of spaces.

**Repro.** out13/probe/sum-clause/p1.cob: WS-K PIC 99 VALUE 60; CONTROL FOOTING FINAL `COLUMN 1 PIC 99 SUM WS-K`; INITIATE, GENERATE DET twice, TERMINATE.

**Expected, from the standard.** The counter has 2 integral digits (GR1); the second addition gives 120, a size error (GR3: 'Each addition is tested for size error; if a size error occurs, the EC-REPORT-SUM-SIZE exception condition is set to exist'), so at print GR4 applies: 'the printable item is filled with spaces' -> the footing shows two spaces in columns 1-2 (and the indicator is unset again at the end of the group, GR2).

**Observed.** Observed '20' in columns 1-2 (the 120 total truncated by the MOVE). ExceptionCatalog lists EC-REPORT-SUM-SIZE but no raise site exists (grep over src); ReportWriter.Initiate's comment calls it a 'seam'. PicInfo.SumCounterItem also clamps a 0-digit PICTURE (PIC X(4)/N(4)) to 1 digit, which prints '4' for a total of 24.

**Code site.** src/Cobol.Net.Runtime/IO/ReportWriter.cs#Generate (SUM accumulation loop); src/Cobol.Net.Runtime/IO/ReportWriter.cs#SumEntry; src/Cobol.Net.Runtime/IO/ReportWriter.cs#EndOfGroupSumReset

## What the fixer owes

Re-probe on your own build first (MANDATORY-PRACTICES I3). Fix the mechanism at the code sites above — one rule, one place (the `cluster:` notes share these files; take them as one fill unit). One positive golden at the introducing edition and one negative below it / per violated rule, each derived from the quoted rule. Sweep the sibling arms named in the findings (CLAUDE.md rule 4). On landing flip `status`, write `closes_rows` from `inventory_rows`, and re-verdict each row with `record_verdicts.py`.
