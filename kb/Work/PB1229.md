---
title: "PB1229 — the shared compile-time evaluator accepts the figurative ZERO/ZEROS/ZEROES as an arithmetic-expression operand (EvalArith maps ZERO_ARITH to 0), so `01 K CONSTANT AS ZERO + 1.` compiles although §13.10.3 SR6/SR7 exclude figurative constants from arithmetic-expression-1; the figurative screen exists only on the directive entry point"
id: PB1229
kind: defect
status: open
severity: MINOR
area: frontend
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1230"]
spec_refs: ["13.10.3", "7.3.6.2"]
inventory_rows: ["SR-13.10.3-6", "SR-13.10.3-7"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14d** from adjudication batch d1-14d (the adjudicators' pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probes under `scratchpad\adj-d1\out13\probe\`). The given repro was re-run once on registrar 14d's Debug build of `071ff04ea` (main after trains 58/59) and reproduces unless stated otherwise.

# PB1229 — OPEN

## Measured

**constant-entry** — F-ZERO-OPERAND — the shared compile-time evaluator accepts the figurative ZERO as an arithmetic-expression-1 operand (EvalArith maps ZERO_ARITH to 0); the figurative screen exists only on the directive entry point

- **Repro:** 01 K CONSTANT AS ZERO + 1. 01 K2 CONSTANT AS ZEROES * 3. PROCEDURE DIVISION. DISPLAY K ' ' K2.
- **Expected (spec):** Rejected: §13.10.3 SR6 'Neither literal-1 nor any of the literals in arithmetic-expression-1 shall be a figurative constant'; §7.3.6.2 SR1b 'All operands shall be fixed-point numeric literals' (via SR7).
- **Observed:** Compiles clean, prints '1 0'. CompileTimeExpressionEvaluator.EvalArith: `if (pe.ZERO_ARITH() is not null) return 0m;` — the §7.3.3 SR10 ContainsToken(ZERO_ARITH) scan guards only the directive operand path. Check the DEFINE/EVALUATE-directive arithmetic twin for the same hole (sibling sweep).
- **Code site:** src/Cobol.Net.Frontend/Expressions/CompileTimeExpressionEvaluator.cs#EvalArith
- **Clause (adjudicator):** §13.10.3 SR6/SR7; §7.3.6.2 SR1b

## The rules — every citation re-run through `cite.py --check` by registrar 14d

- `SR-13.10.3-6`: `cite.py --check 13.10.3 "Neither literal-1 nor any of the literals in arithmetic-expression-1 shall be a"` → OK  §13.10.3 6)  (Syntax rules)
- `SR-13.10.3-7`: `cite.py --check 13.10.3 "Arithmetic-expression-1 shall be formed in accordance with 7.3.6, Compile-time arithmetic"` → OK  §13.10.3 7)  (Syntax rules)

## Rows claimed

- `SR-13.10.3-6` — batch 14d verdict DIVERGES
- `SR-13.10.3-7` — batch 14d verdict DIVERGES

## What the fixer owes (sibling sweep)

Put the screen in the evaluator's shared entry so the directive and the constant entry cannot drift (one rule, one place).
