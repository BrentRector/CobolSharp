---
title: "PB1225 — the constant-entry grammar makes the un-underlined optional word AS REQUIRED: `01 K CONSTANT 8.` and `01 K2 CONSTANT \"AB\".` are parse errors although §13.10.2 underlines neither IS nor AS nor OF"
id: PB1225
kind: defect
status: open
severity: MINOR
area: frontend/grammar
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1226", "PB1227", "PB1228"]
spec_refs: ["13.10.2"]
inventory_rows: ["FMT-13.10.2"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14d** from adjudication batch d1-14d (the adjudicators' pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probes under `scratchpad\adj-d1\out13\probe\`). The given repro was re-run once on registrar 14d's Debug build of `071ff04ea` (main after trains 58/59) and reproduces unless stated otherwise.

# PB1225 — OPEN

## Measured

**constant-entry** — F-AS-OPTIONAL — the constant-entry grammar makes the un-underlined optional word AS REQUIRED

- **Repro:** WORKING-STORAGE SECTION. 01 K CONSTANT 8. / 01 K2 CONSTANT "AB". then DISPLAY K K2 (--std 2023)
- **Expected (spec):** Compiles; DISPLAY prints 8 and AB. §13.10.2: AS is not underlined in the printed format (PDF p385 measured: AS at x330.1 carries no rule; 1/01/CONSTANT/BYTE-LENGTH/LENGTH/FROM do), so it is an optional word.
- **Observed:** COBOL0001 'cannot parse construct near 8' / near '"AB"'. constantEntryBody is `CONSTANT (IS? GLOBAL)? (AS constantValue | FROM cobolWord)`.
- **Code site:** src/Cobol.Net.Frontend/Grammar/Core/CobolData.g4#constantEntryBody
- **Clause (adjudicator):** §13.10.2 (figure note: '`IS`, `AS`, and `OF` are not underlined' — cite.py OK)

## The rules — every citation re-run through `cite.py --check` by registrar 14d

- `FMT-13.10.2`: `cite.py --check 13.10.2 "CONSTANT"` → OK  §13.10.2   (General format) (general format; the fixer renders the PDF page — rule 1)

## Rows claimed

- `FMT-13.10.2` — batch 14d verdict DIVERGES

## What the fixer owes (sibling sweep)

Render the §13.10.2 page (rule 1: underlining decides); recheck every optional word in constantEntryBody.
