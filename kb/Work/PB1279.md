---
title: "PB1279 — a Tier-B REDEFINES class whose canonical lies WITHIN an OCCURS has no backing access path (ReferenceResolver.PlaceForItem → BuildBackingPath null → DeferredShape.NestedClassBacking), so the shape §13.18.44.3 SR5 sentence 2 expressly permits is deferred and aborts at run time"
id: PB1279
kind: defect
status: open
severity: MINOR
area: binding/redefines
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1280", "PB1282", "PB986"]
spec_refs: ["13.18.44.3", "13.18.44.4"]
inventory_rows: ["SR-13.18.44.3-5", "GR-13.18.44.4-1", "GR-13.18.44.4-2"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14e** from adjudication batch d1-14e (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probe sources under the batch's `out13/probe/<subject>/`). Checked against main 071ff04ea: trains 58/59 did not touch this mechanism.

# PB1279 — OPEN

## The rules (every quotation run through `cite.py --check` by the registrar)

- `python scripts/spec/cite.py --check 13.18.44.3 "However, data-name-2 may be subordinate to an item whose data description entry contains an OCCURS clause"` → `OK  §13.18.44.3 5)  (Syntax rules)`

## Measured

### Finding redefines-clause — §13.18.44.3 SR5 (sentence 2); §13.18.44.4 GR1, GR2 (rejects-legal-source; rows SR-13.18.44.3-5, GR-13.18.44.4-1, GR-13.18.44.4-2)

**Mechanism.** nested-class-backing: a Tier-B REDEFINES class whose canonical lies WITHIN an OCCURS has no backing access path (ReferenceResolver.PlaceForItem -> BuildBackingPath null -> DeferredShape.NestedClassBacking), so the shape §13.18.44.3 SR5 sentence 2 expressly permits is refused - COBOLNET0899 at compile time for a receiver, COBOLNET1756 + run-time abort for a sender. The deferral has no owning kb/Work note (PB1056's census gap).

**Repro.** WORKING-STORAGE: `01 T. 05 E OCCURS 3. 10 A PIC X(4). 10 B REDEFINES A. 15 B1 PIC X(2). 15 B2 PIC 99.` PROCEDURE: `MOVE "ab12" TO A(2). DISPLAY B1(2) " " B2(2).` (probe tree scratchpad/adj-d1/out13/probe/redefines-clause/ (pinned adj-d1 cobol.exe, --std 2023 --run unless stated) sr5_occ_grp.cob; also sr5_sub_occurs_ok.cob with `10 A PIC X(2). 10 B REDEFINES A PIC 99.` MOVE "12" TO A(2), ADD 1 TO B(2), DISPLAY A(2)).

**Expected, from the standard.** Compiles and prints `ab 12` (resp. `13`): SR5 'However, data-name-2 may be subordinate to an item whose data description entry contains an OCCURS clause'; GR1 associates B's storage with A's first bit in each occurrence; GR2 lets either name reference it.

**Observed.** rc 70: COBOLNET0899 'receiving operand A(2) names a declared item in a reference shape COBOL.NET does not yet implement as a receiver' and COBOLNET1756 'a REDEFINES view whose backing lies within an OCCURS - not implemented; reaching this statement aborts the run unit'. The same-PICTURE (Tier-A) shape works (sr5_occ_tierA -> 12), so only the string-canonical class arm is unbuilt.

**Code site.** src/Cobol.Net.Compiler/Binding/ReferenceResolver.cs#PlaceForItem (the `BuildBackingPath(sc) is not { } backing` arm); src/Cobol.Net.Compiler/Binding/RefResolution.cs#DeferredShapes (NestedClassBacking)

## What the fixer owes

Re-probe on your own build first (MANDATORY-PRACTICES I3). Fix the mechanism at the code sites above — one rule, one place (the `cluster:` notes share these files; take them as one fill unit). One positive golden at the introducing edition and one negative below it / per violated rule, each derived from the quoted rule. Sweep the sibling arms named in the findings (CLAUDE.md rule 4). On landing flip `status`, write `closes_rows` from `inventory_rows`, and re-verdict each row with `record_verdicts.py`.
