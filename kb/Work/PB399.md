---
title: "PB399 — EVALUATE has no syntax-rule screen at the binder: a subject/object count mismatch is a run-time crash one way and a SILENT partial match the other, and no range operand is screened for class, excluded class or variable-length group"
id: PB399
kind: defect
status: landed
severity: MAJOR
area: binder/evaluate
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.13.3", "14.9.13.2", "8.8.4.2.16"]
inventory_rows: ["SR-14.9.13.3-2", "SR-14.9.13.3-4", "SR-14.9.13.3-7", "SR-14.9.13.3-9"]
tags: [cobolsharp, work, defect]
---

# PB399 — four EVALUATE syntax rules with no screen, and an index guard standing in for one of them

## 1. The mechanism

Four of EVALUATE's syntax rules are **syntax** rules — a violation shall be a compile-time diagnostic — and
none of the four has a screen. Two distinct absences produce them.

**(a) SR2, the operand-count rule, is stood in for by a bounds guard inside the binding loop.**
`EvaluateBinder.Bind` walks the WHEN groups with `for (int i = 0; i < groups.Length; i++)`
(`src/Cobol.Net.Compiler/Binding/Procedure/Verbs/EvaluateBinder.cs:45`) and, at `:48`, returns
`new BoundUnsupported("EVALUATE: more WHEN objects than subjects (ISO §14.9.13 SR)")` when `i` runs past the
subject array. That is a defensive index guard wearing a rule's clothes, and the two directions fail
differently *because* of it:

- **MORE objects than subjects** — the guard fires, but into the BOUND TREE, so the "diagnostic" arrives as an
  unhandled `NotImplementedCobolFeatureException` at RUN TIME, and only if that WHEN is actually reached. It is
  coverage-shaped in exactly the way PB47 described for the Table-15 screen before that screen moved to compile
  time.
- **FEWER objects than subjects** — there is no check anywhere. The loop terminates at `groups.Length`, the
  surplus SUBJECTS are never paired with anything, and the WHEN phrase matches on the strict subset of pairs
  that happened to be written. This half is the more insidious: illegal source silently produces a program that
  takes a branch on a subset of its own selection subjects, with no diagnostic at any stage and no run-time
  signal. That is the one-arm-of-two shape (PB45, PB47) again.

**(b) The range-expression operands are handed straight to `BindValueOperand` with no admissibility screen of
any kind**, so SR4, SR9 and SR7 a) all fail on one absence. `EvaluateBinder.cs:218-219` binds
`range.valueOperand(0)` and `range.valueOperand(1)` and goes directly to `left >= lo && left <= hi`; nothing
between the parse and the emit asks what class the operands are, whether the two classes match, or whether
either is a variable-length group. The Table-15 screen cannot catch these **and is not the right place to
try**: `ObjectOperandKind` (`EvaluateBinder.cs:118`) classifies a `valueRange` as one
`EvaluateObjectOperand.RangeExpression` row and never looks inside it, which is correct — Table 15 is about the
subject/object PAIRING, not about a range's internals.

The pointer case is the sharpest. SR4 excludes class pointer outright, and independently 8.8.4.2.16 defines
only equality for pointer operands, so the `>=` / `<=` pair the lowering emits is not a comparison the standard
defines at all: the compiler invents an ordering on raw addresses, which is meaningless and platform-dependent.

## 2. The rules, quoted, each `--check`ed on this tree

    python scripts/spec/cite.py --check 14.9.13.3 "The number of selection objects within each set of selection objects shall be equal to the number of selection subjects"
      -> OK  §14.9.13.3 2)  (Syntax rules)
    python scripts/spec/cite.py --check 14.9.13.3 "The two operands in a range-expression shall be of the same class and shall not be of class boolean, message-tag, object, or pointer"
      -> OK  §14.9.13.3 4)  (Syntax rules)
    python scripts/spec/cite.py --check 14.9.13.3 "Neither identifier-3 nor identifier-4 shall reference a variable-length group"
      -> OK  §14.9.13.3 9)  (Syntax rules)
    python scripts/spec/cite.py --check 14.9.13.3 "shall be valid operands for comparison to the corresponding operand in the set of selection subjects in accordance with 8.8.4.2"
      -> OK  §14.9.13.3 7) a)  (Syntax rules)
    python scripts/spec/cite.py --check 8.8.4.2.16 "The operands are equal if they reference the same address"
      -> OK  §8.8.4.2.16   (Comparison of pointer operands)
    python scripts/spec/cite.py --check 14.9.13.4 "If the selection object is a range-expression, the pair is considered to be a conditional expression"
      -> OK  §14.9.13.4 4) a) 3.  (General rules)

⚠ The last line is a **transcription artifact, not a wrong citation**: in `specs/ISO_COBOL.md` the sub-items
4., 5. and 6. of GR4 a) are written unindented and unescaped (lines 23874 / 23876 / 23888) where 1., 2. and 3.
are indented and escaped, so `cite.py`'s locator attributes all three to the last properly-nested item. The
inventory already knows this — its row ids for those three are `GR-14.9.13.4-L2.4` / `-L2.5` / `-L2.6`. The
range lowering is a) 5.; it is quoted here only to show what the emitted `>=` / `<=` pair implements.

The correspondence stem SR7 a) is also load-bearing here and is quoted above; it makes 8.8.4.2 the
admissibility test for every selection-object operand, which is the rule the pointer range violates.

## 3. Expected vs observed — RE-MEASURED ON HEAD (89044aa0), all five programs

    $ cobol.exe p13-more-objects.cob --run     # one subject `EVALUATE WS-A`, one WHEN with `WHEN 1 ALSO 2`
      Unhandled exception. CobolNet.Runtime.NotImplementedCobolFeatureException: COBOL.NET: a COBOL feature
      that is not yet implemented was reached at run time: EVALUATE: more WHEN objects than subjects
      (ISO §14.9.13 SR)                                                                    [exit 0xE0434352]

    $ cobol.exe p02-sr2-fewer.cob --run        # `EVALUATE WS-A ALSO WS-B` (1, 9), one WHEN with `WHEN 1`
      MATCHED-ONE-OBJECT                                                                          [exit 0]

    $ cobol.exe p10-sr4-class.cob --run        # PIC X(3) subject, `WHEN "A" THRU 5` — alphanumeric x numeric
      OTHER                                                                                       [exit 0]

    $ cobol.exe p22-ptr-range.cob --run        # three USAGE POINTER items, `EVALUATE WS-P WHEN WS-Q THRU WS-R`
      PTR-OTHER                                                                                   [exit 0]

    $ cobol.exe p12-vlg.cob --run              # two groups each holding OCCURS 1 TO 5 DEPENDING ON, as the range ends
      VLG-RANGE                                                                                   [exit 0]

Expected in every case: **rejected at compile time** with a syntax diagnostic, and no executable produced.
p13/p02 violate SR2 (the positional correspondence SR7's stem requires is undefined when the counts differ);
p10 and p22 violate SR4 (different classes; class pointer excluded), p22 additionally SR7 a) via 8.8.4.2.16;
p12 violates SR9. Observed: p13 compiles clean and dies only when the WHEN is reached; the other four compile
clean, emit no diagnostic at any stage, and run to completion. **The branch taken is not the point** — the
point is that four syntax rules produced no diagnostic. Beyond the missing diagnostic: p22's emitted code
orders raw addresses, and p12's range bounds are read at whatever length the ODO counter holds at the moment
the WHEN is considered, so the same source can compare different numbers of characters on different runs.

## 4. The code site and the shape of the repair

- `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/EvaluateBinder.cs:45-48` — the `if (i >= subjects.Length)`
  guard, and the absence of any `groups.Length < subjects.Length` test anywhere in `Bind`.
- `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/EvaluateBinder.cs:215-231` — the `item.valueRange()` arm.
- `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/EvaluateBinder.cs:86-95` — `ScreenPairing`, the existing
  compile-time screen and the natural neighbour for the count check.
- `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/ConditionBinder.cs#CheckedRelational` — emits `>=` / `<=`
  with no 8.8.4.2 admissibility question for the range case.

**SR2:** one count comparison beside `ScreenPairing`, reported once through `ctx.Edition.Error` (a new
`DiagnosticCatalog` entry), covering BOTH directions and naming the two counts. The guard at `:48` then
becomes an assertion that can no longer fire — delete it or turn it into a `Debug.Assert`; leaving a live
`BoundUnsupported` behind a compile-time check is how the run-time arm survives a "fix".

**SR4 / SR9 / SR7 a):** ONE screen over the range PAIR — class equality, the four excluded classes, the
variable-length-group test, and the 8.8.4.2 admissibility question — called once from the `valueRange` arm.
Not three `if` statements, and structured so the **VALUE clause's THROUGH phrase can call the same screen**,
because 14.7.8 says the two share this specification. Where PB398 threads an alphabet through `valueRange`,
this screen is the natural place to ask whether that alphabet's kind matches the operand class (SR3), so the
two fixes should land against the same helper rather than against two.

## 5. The sibling sweep the fixer must do

- **`valueClauseRange` / the VALUE clause THROUGH phrase** — SR4's and SR9's VALUE-clause analogues live in
  13.18.61; if the screen is written only for EVALUATE, the same illegal range is still accepted in a VALUE.
- **Every other operand-count rule of the ALSO family** — the SEARCH ALL WHEN list and the `PERFORM VARYING …
  AFTER` chain both pair positionally; ask whether their guards are index guards too.
- **Every `BoundUnsupported` that names an ISO SR** — grep `BoundUnsupported(".*ISO` in the binders. A
  `BoundUnsupported` naming a *syntax* rule is by construction a rule reported at the wrong stage; each hit is
  either this same defect or wants the same treatment.
- **`CheckedRelational` callers** — if the 8.8.4.2 admissibility question is added inside `CheckedRelational`
  rather than in the range screen, it fires for plain relation conditions too, which is probably right but is a
  behaviour change across the whole condition spine; measure it before choosing where the question goes.
- Edition sweep: p13 and p02 were **re-measured on HEAD at `--std 85` and behave identically** (the same
  run-time exception and the same silent `MATCHED-ONE-OBJECT`), so the new diagnostic must be raised at every
  edition, not gated.

## 6. Rows

- **`SR-14.9.13.3-2`** (DIVERGES) — a syntax rule reported at run time in one direction and not at all in the
  other; both directions measured on HEAD (p13, p02).
- **`SR-14.9.13.3-4`** (NOT-IMPLEMENTED) — no class screen exists; measured twice, mixed-class (p10) and the
  explicitly excluded pointer class (p22).
- **`SR-14.9.13.3-9`** (NOT-IMPLEMENTED) — nothing on the range path asks whether an operand is a
  variable-length group; measured p12.
- **`SR-14.9.13.3-7`** (PARTIAL) — b) and c) *are* enforced through the Table-15 screen (COBOLNET1634, fixture
  `conformance:negative/pb47-evaluate-table15-invalid-combination`); a) is only partly enforced, as p10 and p22
  show; d) is unreachable because partial-expression-1 does not parse (PB398).

> ⚠ **ROW IDS RE-KEYED 2026-09-06 (kb/Work PB698).** The transcription repair returned the outdented sub-items of these rule blocks to their printed level, so the rows this note names moved: `GR-14.9.13.4-L2.4` -> `GR-14.9.13.4-4`. `inventory_rows` above is live; the ids in the prose are as they stood when it was written, and the adjudications they carry were migrated by the `pb698-rekey` and `pb698-fold` batches.

## 7. Landed — 2026-09-19, on a worktree of main 80dd97e2

All four rules are screened at compile time, at every edition, and the four rows are `CONFORMS`
(batch `pb399-evaluate-selection-set-screens`; GAP 2423 → 2419).

### What was wrong with §3 of this note, and it matters

**p12's premise was backwards.** §8.5.1.12.1 defines a variable-length group as "a group item whose data
description has at least one dynamic-length elementary item or dynamic-capacity table as a subordinate item".
An `OCCURS 1 TO 5 DEPENDING ON` group is **not** one — its size is its maximum — so p12 as written was LEGAL
SOURCE, and a screen built from that repro would have rejected it while leaving the shape SR9 names untouched.
Re-measured with `PIC X DYNAMIC LENGTH` members (p12b): the defect is real and worse than the note said — that
program compiled clean and aborted at RUN TIME on "whole-group image of 'WS-G1' with a dynamic-length /
dynamic-capacity member", not merely silently. Both directions are now pinned, the ODO one as legal.

**§4's repair sketch for SR7 a) was aimed one level too low.** The note asks whether the §8.8.4.2 admissibility
question should go "inside `CheckedRelational` rather than in the range screen". Measured first, as it asks: the
question is ALREADY asked — `ConditionBinder.BindComparison` carries the §8.8.4.2.2 Format 3 band (COBOLNET0868
/ COBOLNET0869) and reports `IF WS-P >= WS-Q` and `IF WS-P = WS-X` correctly. It was written in ONE CALLER of
the `BoundRelational` checkpoint, so the identical pair under EVALUATE reported nothing: `EVALUATE WS-P WHEN
WS-Q THRU WS-R` ordered raw addresses, and `EVALUATE WS-P WHEN WS-X` reached the BACKEND and failed as a raw C#
`CS1503: cannot convert from 'string' to 'ManagedPointer?'`. SEARCH WHEN, PERFORM UNTIL and the abbreviated
path had the same hole. This is the two-arm dispatch, and the fix is the MOVE, never a copy.

### The fix

- **SR2** — `EvaluateBinder.ScreenObjectCount`, beside `ScreenPairing`: one count comparison per WHEN phrase
  against the statement's subject count, BOTH directions, COBOLNET2106, ungated. The
  `if (i >= subjects.Length) return new BoundUnsupported(…)` guard is DELETED; the pairing loop clamps to
  `min(objects, subjects)` as pure recovery after the screen has failed the compile.
- **SR4 + SR9** — `EvaluateBinder.ScreenRangeOperands`, ONE screen over the range PAIR, called once from the
  `valueRange` arm BEFORE the collating class is asked, COBOLNET2107. A rejected range returns a
  `BoundConditionError`, so the comparison the operands are inadmissible for is never lowered. CLASS is
  §8.5.2.1 Table 2's, read through the ONE existing Table-2 lattice — `IntrinsicArgumentRules.ClassOf`
  projected by `TableTwoClass`. ⛔ A second class reader was written and then **deleted**: that lattice already
  existed, already modelled a figurative's candidate SET, the alphabetic split, the index row and the
  three-pointer-categories-one-class row, and a private copy would have been the `one_rule_one_place` defect.
- **SR7 a)** — the Format 3 band MOVED to `StatementValidation.CheckFormat3Relation`, reached from
  `CheckRelationalOperands`, which is what `CheckedRelational` already called. The two separately-written class
  halves became ONE screen, which also asks §8.8.4.2.3 SR5's **same-CATEGORY** question — neither old half did,
  so `IF a-data-pointer = a-program-pointer` was accepted — and reads class from the lattice rather than from
  `Pic?.Category == PicCategory.Pointer`, which is one of the THREE categories Table 2 folds into class
  pointer, so a program-pointer or function-pointer operand left the whole band silent. NULL takes the other
  operand's class (§8.4.3.10.3 SR1, §8.4.3.9) and is exempt from the category test.
- Citation repairs made in passing: "pointers are not ordered (§8.8.4.2.16)" is **§8.8.4.2.2 Format 3** —
  §8.8.4.2.16 says only "The operands are equal if they reference the same address". COBOLNET0868 gained the
  catalog descriptor it never had (the fourth bare-string code of kb/Work PB175's sweep, missed because its
  only site was in a condition binder rather than a verb binder).

### Rows

- **`SR-14.9.13.3-2`** DIVERGES → **CONFORMS**
- **`SR-14.9.13.3-4`** NOT-IMPLEMENTED → **CONFORMS**
- **`SR-14.9.13.3-9`** NOT-IMPLEMENTED → **CONFORMS**
- **`SR-14.9.13.3-7`** PARTIAL → **CONFORMS** — a) is a delegation to §8.8.4.2 and is now satisfied by
  construction, because the EVALUATE pair reaches the same checkpoint the written relation does.

Diagnostics allocated: **COBOLNET2106**, **COBOLNET2107** (2108 unused, returned).
Goldens: `conformance:85/pb399_evaluate_selection_set_shapes`,
`conformance:2002/pb399_format3_relation_legal_shapes`, nine `conformance:negative/pb399-*` cases (one per rule
arm and one per relation SURFACE), and `Format3RelationSurfaceDriftTests` — the drift test for the
one-checkpoint shape, five surfaces over one operand pair with a legal-pair complement leg so it cannot pass by
rejecting everything. Negative-controlled: 8 of its 21 rows go RED with the `CheckFormat3Relation` call
commented out.
