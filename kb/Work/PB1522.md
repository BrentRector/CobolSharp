---
title: "PB1522 — cloud adjudication of the last 208 never-adjudicated inventory rows: BOTH waves adjudicated (208/208) and the registrar ran on `claude/adj-registrar-1` — 22 rows recorded (11 non-DOC + 11 R42 withdrawals, GAP 1664 → 1652), 126 DOC rows held on owner decision PB1535, 60 NEEDS-OWNER-DECISION rows with their owner notes; step 3 (§7 rows, golden lane) remains"
id: PB1522
kind: analysis
status: open
severity: MAJOR
area: process/adjudication
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
cluster: ["PB1521", "R41"]
spec_refs: ["A.1"]
inventory_rows: []
closes_rows: []
closes_rows_reason: "a campaign note; the rows close through the registrar's record_verdicts.py batches, not through this note"
tags: [cobolsharp, work, analysis]
---

Opened 2026-09-24 (owner: spend the $250 cloud-session credit, expires 2026-11-04, on adjudication first — R41).
This note is the SELF-CONTAINED HANDOFF for a cloud orchestrator session: a cloud VM has no access to the owner's
local Claude memory, so everything needed is here and in `.claude/skills/workstream/templates/cloud-adjudicate-brief.md`.

# PB1522 — OPEN

## State (2026-09-24 23:00 UTC — after REGISTRAR-1)

**Both waves are adjudicated: 208 of 208 rows** (wave 1: `claude/adj-cal-1`, `claude/adj-a1-w1-s1..s4`, 90 rows; wave 2:
`claude/adj-w2-14-s1`, `adj-w2-14-12-s2`, `adj-w2-misc-s3`, `adj-w2-a1-s1..s3`, 118 rows; all pinned at `0caa7d5`).
Final verdicts: DIVERGES 79 · NEEDS-OWNER-DECISION 71 · PARTIAL 31 · CONFORMS 15 · NOT-IMPLEMENTED 12 (`adj-w2-14-s1`
never ran a refuter — all 22 of its rows are NEEDS-OWNER-DECISION, so none was owed).

**REGISTRAR-1** (branch `claude/adj-registrar-1`, dir `adjudication/registrar-1/`; `normalize.py` rebuilds every batch from
the eleven branches in one command, fixing shape only — no verdict changed, no evidence dropped):

| Batch | Rows | Status |
|---|---|---|
| `batch-nondoc.json` | 11 (PARTIAL 5 · DIVERGES 3 · NOT-IMPLEMENTED 2 · CONFORMS 1) | **recorded**, GAP 1664 → 1663 |
| `batch-r42.json` | 11 DOC rows → DOCUMENTED-NON-SUPPORT (owner decision R42) | **recorded**, GAP 1663 → 1652; R42 implemented as Annex A.1 arms of `screen-handling-only` / `commit-and-rollback-only` so the audit derives them |
| `batch-doc.json` | 126 DOC rows (DIVERGES 76 · PARTIAL 26 · CONFORMS 14 · NOT-IMPLEMENTED 10) | **NOT recorded — blocked on owner decision [[PB1535]]** (measured: recording them turns the inventory gate red, 122 anchors resolve nowhere — `doc-trial-gate.txt`) |
| excluded | 60 NEEDS-OWNER-DECISION | claimed by their owner notes: PB1151, PB1099 (+ DOC-A.1-161/-162 under Q3), PB1198 (+ DOC-A.1-212 under Q1), PB1255, PB1517, PB1518, PB1519, PB579 (+ DOC-A.1-47), PB468 Q9 (GR-4.4-1/-2), [[PB1536]] (DOC-A.1-149/-152/-200, FMT-5.2) |

Findings filed one note per mechanism: new PB1523 (GLOBAL bridge CS0102 crash), PB1524 (EC-OO-RESOURCE), PB1525 (§4.2.10
extension register), PB1526 (integer-argument fraction truncated), PB1527 (object reference in a STRONG group), PB1528
(unpositioned screen ON EXCEPTION diagnostic), PB1529 (CONTINUE AFTER wrap), PB1530 (EXTERNAL ASSIGN mismatch), PB1531
(EC-IMP family), PB1532 (finalizer close), PB1533 (DEFINE PARAMETER), PB1534 (41 A.1 determinations with no item note),
decisions PB1535 and PB1536; 44 owning notes extended (`adjudication/registrar-1/owners_map.py`).
The DEVLOG paragraph and the plan §0 refresh are drafted in `adjudication/registrar-1/LANDING.md` for the owner's LOCAL
orchestrator, which lands the branch through `scripts/push-main.sh`.

### Before the registrar (2026-09-24 14:50 PDT, kept for the record)


Inventory: 4348 rows, 208 with no verdict (144 Annex A.1 `DOC-A.1-*`, 64 elsewhere: §14 30, §11 7, §12 6, §4 5,
§9 4, §13 4, §5 3, §16 3, §8 2). **90 of the 144 A.1 rows are ADJUDICATED BUT NOT RECORDED** — their verdicts sit on
branches, not in `tests/version-matrix/traceability-inventory.json`:

| Branch | Rows | Final verdicts |
|---|---|---|
| `claude/adj-cal-1` (dir `adjudication/cloud-cal-1/`) | 10 | 3 CONFORMS · 6 DIVERGES · 1 NOT-IMPL |
| `claude/adj-a1-w1-s1` … `-s4` (dirs `adjudication/adj-a1-w1-s{1..4}/`) | 80 | 4 CONFORMS · 41 DIVERGES · 18 PARTIAL · 7 NOT-IMPL · 10 NEEDS-OWNER-DECISION |

Each dir: `final.jsonl` (record_verdicts shape, refuter overturns applied), `findings.json`, `refute.jsonl`,
`README.md`. Wave-1 findings: 63 rows harm=silent (the dominant mechanism — the code fixes an implementor
determination that `docs/CONFORMANCE.md` never files under its own `DOC-A.1-N` key; owners A11, PB1092, PB1369,
PB1397), plus 6 wrong-answer, 2 crashes (indexed OPEN OUTPUT on a full device → unhandled IOException; dynamic-
capacity table growth → OutOfMemoryException), 7 under-rejects. 10 NEEDS-OWNER-DECISION rows are A.1 items hanging
off declined modules (screen handling A.4.2, commit A.4.3) with no disposition path (A11).

## Billing — measured, do not re-learn

Cloud sessions launched by **RemoteTrigger routines bill to the PLAN**, not the credit (six routine runs, ~$30, left
the credit at $250). Sessions started **from the claude.ai/code web page, the desktop/mobile app, or `claude --cloud`
in an interactive terminal** draw the credit ($250 → $249 on a one-line test). Subagents inside a credit-billed
session bill to that session. Cost measured on wave 1: **~$0.34 per adjudicated row** (s1: $6.73 for 20 rows, 14.3M
cache-read, 99 % cache hits).

## Remaining work, in order

**Steps 1 and 2 are DONE (above).** What remains is step 2's blocked half (record `batch-doc.json` once PB1535 is
answered — `normalize.py` rebuilds it) and step 3.

1. **Wave 2 — adjudicate the other 118 rows.** Compute them, never hand-copy: the inventory rows whose `verdict` is
   empty, MINUS every `rule-id` in `adjudication/*/final.jsonl` on the `origin/claude/adj-*` branches
   (`git fetch origin 'refs/heads/claude/adj-*:refs/remotes/origin/claude/adj-*'`). Build inputs with
   `python scripts/spec/phase_b_batch.py A.1 4 5 8 9 11 12 13 14 16 --max-rules 10 --out /tmp/adj/in` (it already
   skips recorded rows; drop the files whose rules are on a branch). Split into batches of ~20 rows, grouping a
   clause's subjects together. Run each batch with the brief `.claude/skills/workstream/templates/cloud-adjudicate-brief.md`
   — in ONE cloud orchestrator session, as **parallel subagents (≤ 6 at a time; the VM is 4 cores / 15 GB and each
   batch builds nothing after the first build, so share one build: build once in the orchestrator before spawning)**,
   each with its own `{BATCH}` tag `adj-w2-<clause>-s<k>` and its own branch.
2. **Registrar — record all verdicts** (both waves) on ONE branch `claude/adj-registrar-1`: merge every
   `final.jsonl` into batch files and run `python scripts/spec/record_verdicts.py --dry-run` then for real (it is
   all-or-nothing and validates shape; fix records it rejects — e.g. a NOT-IMPLEMENTED needs no code-location, a
   CONFORMS needs one — and CHECK THE GAP DELTA it prints: a dropped record is a missing row). File the findings
   into `kb/Work/` per `.claude/skills/workstream/templates/registrar-brief.md` — one note per MECHANISM, fold into
   the owning note when one exists (A11 / PB1092 / PB1369 / PB1397 own most A.1 rows; take the next free PB id from
   `ls kb/Work`), `inventory_rows` claiming the rows. Commit on the branch and push. **A cloud session does not
   land on `main`**: `main` requires the `ci-gate` check and lands only through `bash scripts/push-main.sh`, run by
   the owner's LOCAL orchestrator, which also writes the DEVLOG entry, refreshes plan §0 and the ledger artifact.
3. **Then** (next PB notes, not this one): the cheap closing work the A.1 findings point at — writing the missing
   `| DOC-A.1-N |` determination rows in `docs/CONFORMANCE.md` against the code's actual behavior — and the golden
   lane for CONFORMS-untested rows (366 on 2026-09-24).

## Constraints a cloud session must keep

Commit only on `claude/*` branches; never push `main`. `specs-private` needs BrentRector/CobolSharp-private attached
(web page: the "+" beside the repo chip). The setup script and user-level SessionStart hook are PB1521 (verified by
cloud smoke #3). A cloud session cannot reach the owner's local memory, Chrome, or local scratchpad — report
everything in its final message and on its branches.
