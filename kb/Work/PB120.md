---
title: "PB120 — ✅ LANDED 2026-08-28 (compilation-scoped stamp + deterministic PE). WAS: WHEN-COMPILED's stamp is a PROCESS-static Lazy over an unset CompileClock — in a long-lived compiler process every subsequent compilation unit inherits the FIRST unit's timestamp (§15.99.3 r2 requires the compilation time of the unit containing the reference), and the generated PE carries a nondeterministic COFF stamp no one ties to the returned value (r3's object-code half unverified)"
id: PB120
kind: defect
status: landed
severity: MINOR
area: intrinsics
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["15.99.3"]
tags: [cobolsharp, work, defect]
---

# PB120

Inventory: RV-15.99.3-2 (DIVERGES), RV-15.99.3-3 (NOT-IMPLEMENTED). Fix direction: capture the stamp per
COMPILATION (a CompilerDriver-scoped value threaded to the renderer), not per process; decide r3's
object-code half explicitly (set Deterministic and document, or emit the stamp into the PE deliberately).
RV-15.99.3-1 (the Format21 layout) joined the standing CURRENT-DATE owner question — not this note.

*(Found by the Phase-B §15 close-out batch, 2026-08-28 — one adjudicator + one independent refuter per subject;
the full forensic evidence lives in the named inventory rows' `notes` fields,
`tests/version-matrix/traceability-inventory.json`.)*
