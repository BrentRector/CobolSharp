---
title: "PB508 — the GR2 promotion predicate hard-codes usage display. PictureAnalyzer.cs:345 reads `if (anyEdit || (blankWhenZero && digits > 0 && !signed && usage is Usage.Display))`, but SR2 admits the subject as usage display OR usage national, and GR2's own condition is only that the picture character-string be category numeric"
id: PB508
kind: analysis
status: open
severity: MINOR
area: binding/data-division
wrong_answer: false
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.18.8.4"]
inventory_rows: ["GR-13.18.8.4-2"]
tags: [cobolsharp, work, analysis]
---

# PB508

## Mechanism — blank-when-zero-clause
bwz-gr2-national-not-promoted - the GR2 promotion predicate hard-codes usage display. PictureAnalyzer.cs:345 reads `if (anyEdit || (blankWhenZero && digits > 0 && !signed && usage is Usage.Display))`, but SR2 admits the subject as usage display OR usage national, and GR2's own condition is only that the picture character-string be category numeric. A national-usage numeric subject therefore falls through to the plain-numeric return at PictureAnalyzer.cs:356 and is never defined numeric-edited - which would silently cost it GR1's blanking, the numeric-edited VALUE rules, the arithmetic-operand refusals of 8.8.1.1 / 14.9.2.3 SR2 and the de-editing MOVE. This is a scalar-condition-where-the-rule-names-a-SET shape: the predicate encodes one of the two usages SR2 lists.

### Expected, from the standard
ISO/IEC 1989:2023 13.18.8.4 GR2 - "If the subject of the entry is described by its picture character-string as category numeric, the BLANK WHEN ZERO clause defines the item as numeric-edited." - read with 13.18.8.3 SR2, which admits usage display OR usage national (both validated with scripts/spec/cite.py --check). WS-NAT is category numeric by its picture character-string and legal by SR2, so the clause defines it numeric-edited and GR1 blanks it on a zero store.

```
$ python scripts/spec/cite.py --check 13.18.8.4 "If the subject of the entry is described by its picture character-string as category numeric, the BLANK WHEN ZERO clause defines the item as..."
  OK  §13.18.8.4 2)  (General rules)
```

### Observed
LATENT, not yet live: the probe presently stops one stage earlier with `error COBOLNET0899: national-form numeric data (a numeric or numeric-edited PICTURE 9(5) with USAGE NATIONAL - national digits) is recognized but not yet implemented (Phase 4a residue)` from src/Cobol.Net.Compiler/Binding/PictureAnalyzer.cs:252, so the promotion predicate is unreachable and has never been contradicted by a test. That residue is owner-owned recognized-but-not-implemented non-support, outside this adjudication; the finding is that the predicate underneath it already encodes the wrong rule and will fail SILENTLY (a wrong category, no diagnostic) the day national numeric lands. Reading the code alone confirms it: after the COBOLNET0899 error the analyzer sets `usage = Usage.Display` at :256, so even the errored path never exercises the national arm.

### Repro
E:/Temp/claude/E--CobolSharp/c609c011-7fda-4dbc-a369-a563a47cedd1/scratchpad/lane3/out-b3/probe/bwz/a5.cob - `01 WS-NAT PIC 9(5) USAGE NATIONAL BLANK WHEN ZERO.` with `MOVE 0 TO WS-NAT` and a DISPLAY. Run E:/CobolSharp/.claude/worktrees/lane3-pin-89044aa0/src/Cobol.Net.Cli/bin/Debug/net10.0/cobol.exe a5.cob --run (any --std at 2002 or later).

### Code site
src/Cobol.Net.Compiler/Binding/PictureAnalyzer.cs:342-352 (the promotion condition) - the predicate should test the SET SR2 names (display or national), not the single Usage.Display; pair it with a drift test so the next usage the rules admit does not need a second edit.

## Sibling sweep the fixer owes
Every bug here is a pattern (CLAUDE.md rule 4). Before closing this note, sweep the sibling arms named in the mechanism above and add the ones that fail to `inventory_rows`.

## Provenance
Lane-3 adjudication batch b3 (subjects: blank-when-zero-clause), adjudicated against the compiler pinned at 89044aa0 and RE-PROBED on the registrar's worktree cut from 6e7caa91 (a 1942-program sweep of `out-b3/probe/` plus targeted re-probes); the finding reproduces on today's tree.

## Repro programs
`E:\Temp\claude\E--CobolSharp\a9d94900-79fe-4a38-89f0-921976861543\scratchpad\repros\PB508\` — bwz. The implementer's opening move is to rerun them against a freshly built `cobol.exe`.

