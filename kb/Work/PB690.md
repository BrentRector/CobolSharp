---
title: "PB690 — a sequential record character above U+00FF is silently corrupted to `?` on the medium: `SequentialConnector.OpenCore` builds every reader and writer with `Encoding.Latin1`, whose encoder replaces any char above U+00FF with 0x3F, so a record holding U+20AC writes `?` and reads back `?` with no status and no diagnostic — record and line sequential alike, which is why it is not part of the line-sequential character-set determination"
id: PB690
kind: defect
status: open
severity: MAJOR
area: runtime/io
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: true
blocked_by: []
spec_refs: ["8.1.2", "13.18.13"]
inventory_rows: ["DOC-A.1-31", "DOC-A.1-159"]
tags: [cobolsharp, work, defect]
---

# PB690 — OPEN — the file's coded character set was never chosen

Found 2026-09-05 by the `PB329` implementer (report §"New defects" N4), which deliberately left it out of the
line-sequential determination: the loss is a property of the FILE's coded character set and hits RECORD sequential
identically, where GR23 does not reach, so answering it inside item 115 would have fixed one arm of a two-arm
problem.

## The facts

The alphanumeric repertoire is UTF-16 (project decision; `charset_utf16`), but
`src/Cobol.Net.Runtime/IO/SequentialConnector.cs#OpenCore` constructs its four readers/writers with
`Encoding.Latin1`, and `Rewrite` uses `Encoding.Latin1.GetBytes`; `RecordFraming` frames the same bytes. Latin-1's
encoder maps every code point above U+00FF to `?` (0x3F). A record area holding `€` (U+20AC) is written as `?` and
read back as `?`: no status, no diagnostic, wrong data on the medium and in the program.

## The rule to derive from

§8.1.2 (`cite.py --check 8.1.2 "coded character set"` → OK) — the coded character sets an implementation supports
and the mapping between the alphanumeric repertoire and a file's coded character set are implementor-defined
(Annex A.1 items 31 and 34); the CODE-SET clause §13.18.13 (`--check 13.18.13 "CODE-SET"` → OK) is the program's
way of naming a file's coded character set through an alphabet-name. So the determination to make first: what coded
character set a sequential file WITHOUT a CODE-SET clause uses (UTF-8 is the only choice that loses nothing and
interoperates; Latin-1 is what the code does today), and what happens to a character the chosen set cannot
represent (a status in the '7x' record-content family, or the substitution character — never silence). The
determination goes in CONFORMANCE.md as DOC-A.1-31/-34 rows; the CODE-SET clause's own state (declined? staged?) is
measured while there.

## Fix

One encoding decision per connector, derived from CODE-SET or the determination's default, applied to reader,
writer, `Rewrite` and `RecordFraming` alike (one place — the connector's open, where PB673 now passes the
statement's operands); an unrepresentable character reported, not substituted; goldens on record and line
sequential with a supplementary-plane character round-tripping, and a negative-content case. The keyed
organizations' stores (`RecordFraming`) are the sibling to sweep. Fixed-width record files change their byte
layout under UTF-8 (a record's byte length is no longer its character length) — measure what PB327's byte-width
layout assumes before choosing.

## BLOCKED ON THE OWNER (2026-09-05) — the default coded character set is a product decision

The fix requires choosing what coded character set a sequential file WITHOUT a CODE-SET clause uses, and that
changes the bytes of every data file the compiler writes and the layout assumptions of every fixed-width file it
reads. Today the code is Latin-1 (lossy above U+00FF); the surveyed model implementation (GnuCOBOL) is
byte-transparent — it neither encodes nor decodes; a UTF-8 default loses nothing and interoperates but makes a
record's byte length differ from its character length, which PB327's byte-width layout must then account for.
**⛔ Bare question for the owner:** for a sequential file with no CODE-SET clause, what is the file's coded character
set — UTF-8 (lossless, variable-width bytes per character), Latin-1 with a '7x' status for an unrepresentable
character (fixed one byte per character, refuses rather than corrupts), or the program's own alphanumeric
repertoire written as UTF-16 (fixed two bytes, no interchange)? `blocked_by` is empty because the blocker is the owner.

## Registrar-1 (kb/Work PB1522) — rows adjudicated on the `claude/adj-*` branches

Filed 2026-09-24 by REGISTRAR-1 (cloud, branch `claude/adj-registrar-1`). The adjudicators (tree `0caa7d5`) attributed these rows to this note; this note now claims them in `inventory_rows`.

- `DOC-A.1-31` — adjudicated **DIVERGES** on `claude/adj-a1-w1-s2`; NOT recorded — DOC rows wait on owner decision PB1535.

Adjudicator finding (`adj-a1-w1-s2`, harm silent): charset-determination-unwritten-and-split

- Repro: 01 A PIC X(3) VALUE '€é'. DISPLAY FUNCTION ORD(A(1:1)) -> 8365 (U+20AC held in memory); FUNCTION BYTE-LENGTH(A) -> 3; FUNCTION CONVERT(A ANY ANUM HEX) -> 3FE920 (€ -> 0x3F); a sequential WRITE of the same record puts '?' on the medium (PB690)
- Expected: §8.1.2 'The implementor shall specify the set of characters in and the encoding of each of the computer's alphanumeric character set and the computer's national character set. When these are implemented as one character set, the implementor shall specify the characters that map into the computer's alphanumeric coded character set ...' (OK §8.1.2 3)); §8.3.3.2.3 SR2 / §8.3.3.2.4 GR3 c) for UTF-8/mixed in class alphanumeric (OK)
- Observed: No DOC-A.1-31/-36/-38 rows. Rows 33/188 say both repertoires are UTF-16 (total identity), row 209 says one byte per DISPLAY position; the code holds any UTF-16 unit in an alphanumeric item but serializes it as Latin-1 ('?' + EC-DATA-CONVERSION only when checked) at the storage image and silently on files. Which characters are alphanumeric, the DISPLAY encoding, and whether '€' in an alphanumeric literal is 'mixed national' (always-on, not the optional capability SR2 requires) are all unanswered.
- Code site: docs/CONFORMANCE.md §7 (missing rows); src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs#ByteSub; src/Cobol.Net.Compiler/CodeGen/Emit/OperandText.cs#AsStorageImage; src/Cobol.Net.Runtime/IO/SequentialConnector.cs#OpenCore

Each row's full evidence (rule text with its `cite.py --check` line, probes, determination) is the record's `notes` in `adjudication/registrar-1/batch-{doc,nondoc}.json` on the registrar branch.

## PB1522 step 3 (doc-rows-1 integrator, 2026-09-24 23:55 UTC, branch `claude/adj-doc-rows-1`)

A.1 item **159** (report-writer printable item correspondence) was OMITTED from §7: report print lines go through the sequential connector's Latin-1 writer, so a character above U+00FF in a printable item (e.g. a national item) becomes '?' on the medium — this note's mechanism. This note now claims DOC-A.1-159. Sibling filed the same day: kb/Work PB1542 (CODE-SET writes a non-IRV character unscreened / crashes on EBCDIC).

## doc-rows-2 landing (2026-09-25)

**doc-rows-2 wrote the §7 rows on owner decision R47** (DOC-A.1-31, -159; also -110, -106, -115, -146). The status
the refusal sets: **'71'** for a LINE SEQUENTIAL file (the standard's own value — §9.1.13.10 1), §14.9.51.4 GR23,
§14.9.35.4 GR17 d) — reached by narrowing item 115's character set to U+0020–U+00FF in an alphanumeric record
area) and **'91'** for a record sequential file and a report file (§9.1.13.11 1): 7x is confined to line
sequential, a 3x permanent error would be sticky for a refusable record, every other value asserts a different
condition; '91' is FATAL (a '9' first digit) and maps to EC-I-O-IMP — DOC-A.1-110 now defines '90' and '91').
The implementer also owes: `FileStatusCode` gains '91'; the `FileStatus.cs` doc comment 'THE ONE
IMPLEMENTOR-DEFINED I-O STATUS' changes; `LineSequentialCharacterSet` gains the U+00FF ceiling for an
alphanumeric record area (a NATIONAL record area keeps no ceiling — its characters are UTF-16BE); and a report
file's GENERATE/TERMINATE is not in §9.1.13.1's list of statements that set an I-O status, so the '91' lands in
the report file connector's implicit write and the landing golden pins what the rest of that GENERATE does.
