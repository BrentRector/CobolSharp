---
title: "PB37 — LANDED (closed 2026-08-31, SUPERSEDED by PB64 T6) — is NUMVAL-C's LOCALE phrase an §A.4.9 optional element? Answered EDITORIAL in 2026-08-03, and then overtaken: T6 IMPLEMENTED the keyword on both functions, so the question's practical consequence (seven documented-non-support rows) evaporated and the determination now licenses a CLAIM rather than an omission"
id: PB37
kind: defect
status: landed
severity: OWNER
area: locale
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: [15.68, 15.68.3, 15.94, A.4.1, A.4.9]
tags: [cobolsharp, work, defect]
---

# PB37 — LANDED — NUMVAL-C's LOCALE phrase is an §A.4.9 optional element (and is now implemented anyway)

> ⚙ **flags:** process · answered by the owner 2026-08-03 · **closed 2026-08-31, superseded by [[PB64]] T6.**

**The bare question:** Annex A §A.4.9 lists thirteen optional locale elements and NUMVAL-C is not one of them —
item 12 is *TEST-NUMVAL-C function, LOCALE key*. Is NUMVAL-C's LOCALE phrase therefore MANDATORY, or is its
omission from A.4.9 an editorial gap?

⚖ **THE OWNER'S ANSWER WAS A RULE, NOT A VERDICT (2026-08-03):** *"We must obey the spec and should follow the
other compilers if practical."* Applied to the evidence below, that rule reads the omission as **EDITORIAL**:
NUMVAL-C's LOCALE phrase is an §A.4.9 optional element.

**THE PREMISE, VERIFIED AGAINST THE SPEC** (not inherited — §A.4.9 read directly): thirteen items exactly, item
12 is *TEST-NUMVAL-C function, LOCALE keyword and locale-name-1 (15.94)*, and NUMVAL-C (§15.68) is absent. The
annex does enumerate function-level LOCALE keywords individually — items 6 and 13 are LOWER-CASE and UPPER-CASE —
so the granularity is per-function and NUMVAL-C looks dropped rather than deliberately excluded.

⭐ **AND THE SPEC ITSELF MAKES THE MANDATORY READING INCOHERENT.** The two general formats are character-identical
(`LOCALE [ locale-name-1 ]` + `[ ANYCASE ]`), and **§15.94 r1 states "The argument rules for the TEST-NUMVAL-C
function are the same as those specified in 15.68, NUMVAL-C function, Argument rules."** So §A.4.9 would be making
OPTIONAL a keyword whose rules are defined by REFERENCE to the one it leaves mandatory — an implementor could omit
item 12 and still owe every rule item 12 delegates to.

⭐ **THE GROUND THIS SHOULD HAVE RESTED ON, ADDED 2026-08-31.** §A.4.1 answers it without any editorial-gap
inference at all: *"This list identifies features, statements, formats (or parts of formats), clauses, or phrases
that are optional. **Any associated syntax rules, general rules, other rules, exception conditions, and I-O status
values are also optional, even if not explicitly listed.**"* (`cite.py --check A.4.1 …` → OK.) §15.94 r1 makes
§15.68's argument rules the *associated rules* of item 12, so they are optional by the annex's own terms. The
editorial reading is kept because it is independently sound and explains why the list looks incomplete — but the
conclusion no longer depends on it. Recorded in `docs/CONFORMANCE.md` §4.

**THE SURVEY, MEASURED** (`survey_compilers_on_latitude`; observational reading only — GnuCOBOL is GPL): GnuCOBOL
3.2's `cobc/reserved.c` gives **NUMVAL-C and TEST-NUMVAL-C IDENTICAL table rows** — same `NUMVALC_FUNC` category,
same `CB_FEATURE_ACTIVE`, same min/max **2 / 2** arguments, i.e. argument-1 plus the currency string and **no
LOCALE phrase on either**. Their testsuite exercises LOCALE-COMPARE / -DATE / -TIME / -TIME-FROM-SECONDS and never
NUMVAL-C LOCALE. **No implementation distinguishes the twins**, which is the practicality the owner's rule turns on.

## ⛔ SUPERSESSION — read this before acting on anything above

**This note's former NEXT ACTION was, by 2026-08-31, ACTIVELY WRONG, and following it would have caused a
regression.** It read:

> *"flip the seven §15.68.3 r5b rows to DOCUMENTED-NON-SUPPORT in the next batch's `record_verdicts.py` pass and
> add the determination to `docs/CONFORMANCE.md`'s locale non-support list."*

**[[PB64]] increment T6 (landed 2026-08-28) IMPLEMENTED the LOCALE keyword of both NUMVAL-C and TEST-NUMVAL-C.**
Rows `AR-15.68.3-L2.1` … `L2.7` are **CONFORMS and OK**, closed on `conformance:2002/pb64t6_numvalc_locale` plus
`NumvalCLocaleScan*` unit tests (measured against the live inventory, not remembered). Executing the old NEXT
ACTION would re-verdict seven CONFORMS-and-tested rows to documented non-support and un-do T6 — the register
would then describe a compiler that does not exist, and the A.4.9 "claimed whole" statement in `CONFORMANCE.md`
§5 would be contradicted by the inventory.

**The determination did not become wrong; its ROLE changed.** It licensed *documented non-support* while the
keyword was unimplemented; it now licenses the *claim* — an element must be optional-and-claimed for T6's
implementation to complete the A.4.9 module rather than merely add an extension. That is why the note closes as
**landed** rather than being deleted.

**Also corrected 2026-08-31, in `docs/COBOLNET_REARCHITECTURE_PLAN.md` §0**, which asserted two things that had
become false: that "eight rows … are DOCUMENTED-NON-SUPPORT and now CLOSED", and that "`COBOLNET1518` rejects the
phrase at bind time citing §A.4.9". **COBOLNET1518 was DELETED with the PB64 claim** (its code is never
reallocated), and the rows are CONFORMS.
