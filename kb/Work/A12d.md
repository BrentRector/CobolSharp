---
title: "A12d — THE MEASURED DISTRIBUTION"
id: A12d
kind: analysis
status: landed
severity: MAJOR
area: analysis
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, analysis]
---

# A12d — THE MEASURED DISTRIBUTION

- THE MEASURED DISTRIBUTION — FOUR `guard-fast` NIST runs on ONE unchanged tree gave FOUR different answers, hitting four DIFFERENT programs in three different failure modes
- ⛔ **MEASURED 2026-08-02 (PB10), same commit, nothing rebuilt between runs:** ① **353 MATCH / 0 REG → ALL GREEN** · ② **352 MATCH / 0 REG → ALL GREEN** (`SQ135A` ABSENT — the A12c false green) · ③ **351 MATCH / 2 REG** (`RL109A`+`RL110A` DIFF) · ④ **351 MATCH / 1 REG** (`NC224A` COMPILE FAILED). A fifth run earlier the same day gave `IC222A` COMPILE FAILED. **FIVE runs, five outcomes, five different programs, and the verdict line ranged from ALL GREEN to two regressions without one byte of the tree changing.** This is no longer an anecdote: it is the distribution A12 asked for, and it says the leg's verdict currently carries almost no information about the tree. ⚠ EVERY affected program was cleared MECHANICALLY, not statistically: `SQ135A`, `RL109A`, `RL110A`, `NC224A` and `IC222A` each contain **zero real INSPECT statements and zero function-identifiers**, each compiles CLEAN when re-run serially, and each is `corpus.tsv` GREEN inside the greenfield Conformance leg (4171/4171) that exercises the compiler PB10 actually changed. **THE MECHANISM FOR ③ IS NAMED AND IS IN OUR OWN DATA:** `tests/nist/corpus.tsv` records `RL109A ← RL108A` and `RL110A ← RL108A RL109A` — a producer/updater/verifier chain over one RELATIVE data file (guard.sh's own comment says so: "RL109A REWRITEs every 5th record … RL110A verifies") — and the leg runs at `-P$(nproc)`. A chain whose members can interleave, or whose data file survives a prior run, DIFFs without any compiler change. ⚠ Attribution for this instance is MECHANICAL, not statistical: RL109A/RL110A contain **zero INSPECT statements** (their "INSPECT" hits are the identifier `INSPECT-COUNTER` and CCVS boilerplate) and **zero function-identifiers** ("FUNCTION" appears once, in a comment), so the PB10 change cannot reach them; and all three programs are `corpus.tsv` GREEN and passed inside the greenfield Conformance leg (4171/4171), which exercises the compiler the change actually touches.
- Isolate each declared chain to its own working directory (the dependency data is ALREADY in corpus.tsv — the runner should consume it rather than rely on grouping heuristics), and re-measure the distribution over N runs afterwards to prove the variance is gone rather than assume it.
- With A12c, and before the P15 legacy-deletion decision rests on this leg
- **STRUCTURAL HALF DONE 2026-08-03; THE DISTRIBUTION IS NOT YET COLLECTED — do not read this row as closed.** ✅ Done: grouping is now the CONNECTED COMPONENTS of `corpus.tsv`'s declared `chain-preds` (332 groups over 376 programs, **longest serial group 40 → 9**), replacing a hand-written "these six suites run serially" list. Justified by existing evidence rather than guessed — `NistDifferentialTests` already isolates all 349 programs by declared predecessors and is green — and per-component dirs are strictly SAFER than ordering, because guard.sh's prose anti-dependencies ("no other TF022 writer between them") exist only where ONE directory is shared. It also corrected the hand list, which over-grouped `SQ204A`. A chain edge whose predecessor sorts AFTER its consumer would run backwards, so that is now ASSERTED in the grouper rather than left to luck. ⛔ **NOT done — and the reason is worth recording:** I collected exactly ONE clean post-change run (**353 MATCH · 0 REGRESSION · AUDIT CLEAN 376/376 · 0 harness events**) and then destroyed the second by killing the loop on a log line that arrives before the run ends. One run says nothing about variance, and the PRE-change runs in the same session showed the variance is real (one had `IF128A` DIFF — named by the audit, MATCHing on every later run, and attributable MECHANICALLY: `guard-fast` drives the LEGACY `cobolsharp.dll` and nothing this session touched is in its closure). ⭐ **The replacement is PASSIVE collection, which is better than a one-off measurement anyway:** every ordinary run now emits the AUDIT line and appends any retry/non-observation to `COBOLNET_HARNESS_LOG`. Close this row when several ordinary comprehensive gates have accrued with the audit clean and the log empty. `scripts/measure-battery-determinism.sh` remains for a deliberate N-run measurement. ⚠ And the grouping did NOT speed the leg up (NIST phase 564 s → 598 s): it is throughput-bound on `dotnet` cold-start, not tail-bound. See A13.
