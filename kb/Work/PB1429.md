---
title: "PB1429 — the §8.4.3.4.3 SR2 screen (neither NULL nor a universal object reference as identifier-1) runs only on the FIRST segment's written receiver: a universal temp as the identifier-1 of a CHAINED segment falls into the universal dynamic path and ends in an unreported refusal (internal error)"
id: PB1429
kind: defect
status: open
severity: MAJOR
area: binder/oo
wrong_answer: false
crashes: true
silent: false
rejects_legal_source: false
under_rejects: true
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1428", "PB1430"]
spec_refs: ["8.4.3.4.3"]
inventory_rows: ["SR-8.4.3.4.3-2"]
closes_rows: []
tags: [cobolsharp, work, defect]
---
Filed 2026-09-24 by **REGISTRAR 15b** from lane-3 adjudication batch **15b** (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d2` @`071ff04ea`). Not re-probed by the registrar (MANDATORY-PRACTICES R2: every lead carries its repro and code site); origin/main since the pin carries only registrar landings (no src change), so nothing landed has touched this mechanism.

# PB1429 — OPEN

## The rule (every citation re-run by the registrar through `cite.py --check`)

**SR-8.4.3.4.3-2**

> Identifier-1 shall be of class object; neither the predefined object reference NULL nor a …

`python scripts/spec/cite.py --check 8.4.3.4.3 "Identifier-1 shall be of class object; neither the predefined object reference NULL nor a"` → `OK  §8.4.3.4.3 2)  (Syntax rules)`

## Finding — `inline-method-invocation` (crashes; rows `SR-8.4.3.4.3-2`)

**Mechanism.** §8.4.3.4.3 SR2 screen runs only on the first segment's written receiver; a universal temp as the identifier-1 of a chained segment falls into the universal dynamic path and ends in an unreported refusal (internal error)

**Repro.** class PRBC method UNIV: LINKAGE 01 LK-U USAGE OBJECT REFERENCE. PROCEDURE DIVISION RETURNING LK-U. SET LK-U TO SELF. Program: MOVE A1 :: "UNIV" :: "GETNAME" TO W. (A1 USAGE OBJECT REFERENCE PRBC) -> probe p3_chainuniv.cob

**Expected from the spec.** §8.4.3.1.3 defines identifier recursively, so `A1 :: "UNIV"` is identifier-1 of the second invocation; §8.4.3.4.3 SR2: 'neither the predefined object reference NULL nor a universal object reference shall be specified' -> COBOLNET2138 naming SR2.

**Observed / reasoning.** error COBOLNET2362: inline method invocation of "GETNAME" was refused without a diagnostic naming the rule it breaks; this is a COBOL.NET internal error. The segment loop calls OoBindInstanceInvoke(site, prev, ...) which dispatches a universal receiver to OoBindUniversalInvoke; that never sets site.ImplicitReturningPlace and reports nothing. Fix: apply the SR2 universal screen to EVERY segment's receiver (the chained Place's description), one check for both arms.

**Code site.** src/Cobol.Net.Compiler/Binding/Procedure/Verbs/OoBinder.InlineInvocation.cs#OoBinder.OoBindInlineInvocation (segment loop, chained branch)

## What the fixer owes

Apply the SR2 screen to every segment's receiver (the temp of the previous segment) with a named diagnostic; negative golden for a chained universal temp.
