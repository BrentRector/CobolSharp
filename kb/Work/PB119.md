---
title: "PB119 — YEAR-TO-YYYY's float arm passes an OMITTED argument-3 to the core as an explicit 0 — YearToYyyyReal(yy, off, baseYear = 0) calls the three-argument core unconditionally, so a float-typed argument-1/2 with argument-3 omitted hits the r4 'greater than 1600' screen and returns the §15.3 default 0 (or aborts under checking) instead of windowing against the execution year; the exact arm is correct — the TWO-ARM DISPATCH defect shape, sixth occurrence"
id: PB119
kind: defect
status: open
severity: MAJOR
area: intrinsics
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["15.100.3", "15.100.4"]
tags: [cobolsharp, work, defect]
---

# PB119

Inventory: AR-15.100.3-5 (DIVERGES). The exact lane's two-argument overload IS the omitted form (PB57
landed that); `CobolIntrinsics.RealArgs.YearToYyyyReal` defaults baseYear to 0 instead of the execution
year. Fix: the Real twin defaults exactly as the core's omitted form; sweep RealArgs.cs for every other
defaulted-argument twin whose default differs from its exact sibling ([[feedback_two_arm_dispatch]]).

*(Found by the Phase-B §15 close-out batch, 2026-08-28 — one adjudicator + one independent refuter per subject;
the full forensic evidence lives in the named inventory rows' `notes` fields,
`tests/version-matrix/traceability-inventory.json`.)*
