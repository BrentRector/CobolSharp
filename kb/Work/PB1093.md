---
title: "PB1093 — the collating model answers 'the highest alphanumeric character' over the LATIN-1 block although the native alphanumeric set is the 65,536 UTF-16 code units: `CollatingTable.Extremes` starts GR8's walk at (char)0xFF and `AlphabetFigurative` maps a SPECIAL-NAMES HIGH-VALUE to U+00FF, so `ALPHABET AL IS HIGH-VALUE \"B\" \"A\"` puts X\"FF\" first and U+0100 compares GREATER than HIGH-VALUE; the file collating FINGERPRINT likewise hashes only U+0000..U+00FF, so two sequences differing above Latin-1 never conflict at OPEN"
id: PB1093
kind: defect
status: open
severity: MAJOR
area: binder/collating
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1074", "PB690"]
spec_refs: ["12.3.7.4", "12.4.5.7.4", "8.3.3.6.4"]
inventory_rows: ["GR-12.3.7.4-8", "GR-12.3.7.4-10", "GR-12.4.5.7.4-1", "GR-8.3.3.6.4-6"]
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 04:03 PDT by **REGISTRAR 14a** from adjudication batch d1-14a (pinned tree `E:\CobolSharp-pins\adj-d1` @68bbdb00f; probes under `scratchpad\adj-d1\out\probe\`). Not fixed by trains 58/59 (their src diff was checked).

# PB1093 — OPEN — the collating model answers 'the highest alphanumeric character' over the LATIN-1 block although the native alphanumeric set is the 65,536 UTF-16 code units: `C

## Rule text (every citation re-run by the registrar)
- `python scripts/spec/cite.py --check 12.3.7.4 "the figurative constants HIGH-VALUE and LOW-VALUE are associated with those characters having the highest and lowest positions"` → `OK  §12.3.7.4 10)  (General rules)`
- `python scripts/spec/cite.py --check 12.4.5.7.4 "Each collating sequence is a fixed file attribute"` → `OK  §12.4.5.7.4 1)  (General rules)`

### Adjudicator finding (special-names-paragraph-p3, harm wrong-answer)
- **Mechanism:** The alphanumeric HIGH-VALUE is pinned to the Latin-1 block (U+00FF) while the native alphanumeric character set - and every other part of the collating model - is the 65,536 UTF-16 code units: CollatingTable.Extremes starts GR8's walk at (char)0xFF for the alphanumeric arm (0xFFFF for national), and AlphabetFigurative maps a HIGH-VALUE written in SPECIAL-NAMES to U+00FF (U+FFFF for national). One rule ('the highest character of the sequence') answered over two different repertoires.
- **Repro:** probe/snp3/g10a.cob, --std 2023: OBJECT-COMPUTER. X PROGRAM COLLATING SEQUENCE IS AL. SPECIAL-NAMES. ALPHABET AL IS HIGH-VALUE "B" "A". 01 X PIC X. 01 Y PIC X. MOVE X"FF" TO X. IF X < "A" ... (prints FF-LT-A; ORD(X"FF") = 1). MOVE HIGH-VALUE TO Y. IF Y = X"FF" ... (HV-NOT-FF: Y is U+00FE). MOVE FUNCTION CHAR(257) TO X. IF X > Y ... (prints U0100-GT-HV).
- **Expected (spec):** Under the implementor's own GR6 definition (65,536 native characters in code-unit order; measured ORD(CHAR(65536)) = 65536): GR10 makes the SPECIAL-NAMES HIGH-VALUE the highest native alphanumeric character, U+FFFF, so ALPHABET AL puts U+FFFF at position 1, U+00FF stays in the GR7 k3 tail above 'A' (X"FF" < "A" FALSE; ORD(X"FF") is its k3 tail position, not 1). GR8 makes the runtime HIGH-VALUE the character at the highest position of AL, the last native code unit left unspecified: U+FFFF (U+FFFE if U+FFFF were specified). No character compares greater than HIGH-VALUE, so 'U0100-GT-HV' must not print.
- **Observed:** Measured with the pinned cobol.exe: FF-LT-A, HV-NOT-FF, LV-IS-FF, U0100-GT-HV, ORDFF=1. The national arm is correct on both rules (Extremes starts at 0xFFFF; alphabet_national golden). The same pin governs the EBCDIC code-name (its 256 specified positions make Extremes fall through to GR8's tie rule inside the Latin-1 block although U+0100..U+FFFF sit above them by the implementor's adopted k3 tail) and the native (no-PCS) HIGH-VALUE (ORD(HIGH-VALUE) = 256 of 65,536 - §8.3.3.6.4 GR6, outside this part; sweep it). docs/COBOLNET_DESIGN.md §14.9 / §18 #14 and PHASE4_RECONCILIATION call it a 'flagged divergence' kept for byte stability; no register note and no owner decision own it. The fix is one repertoire answer: either the native alphanumeric set really is 65,536 wide (then HIGH-VALUE is U+FFFF in both Extremes and AlphabetFigurative, and the single-octet HIGH-VALUE image at the Latin-1 file boundary becomes a codec question), or it is 256 wide (then the SR14 b1 ordinal bound, the k3 tail, CHAR/ORD and CodedCharacterSet.OrdinalCount are all wrong) - a design decision the fix must make and document, not a per-site patch.
- **Code site:** src/Cobol.Net.Compiler/Binding/CollatingModel.cs#CollatingTable.Extremes (char high = national ? (char)0xFFFF : (char)0xFF); src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs#AlphabetFigurative ("HIGH-VALUE" => national ? "\uFFFF" : "\u00FF")
- **Clause (adjudicator):** ISO/IEC 1989:2023 §12.3.7.4 GR8 and GR10 (cite.py --check 12.3.7.4 "the figurative constants HIGH-VALUE and LOW-VALUE are associated with those characters having the highest and lowest positions" -> OK; --check 12.3.7.4 "If more than one character has the highest position in the program collating sequence, the last character specified is associated with the figurative constant HIGH-VALUE" -> OK); GR6 for the repertoire

## What the fixer owes
One repertoire answer for the alphanumeric arm (memory/charset_utf16: the repertoire IS UTF-16): HIGH-VALUE = U+FFFF in `Extremes` and `AlphabetFigurative`, the fingerprint over the whole sequence (or a canonical digest of its specified part plus the k3 tail rule). Sweep: the native no-PCS HIGH-VALUE (ORD(HIGH-VALUE) = 256 of 65,536 — §8.3.3.6.4), the EBCDIC code-name path, and the design-doc 'flagged divergence' paragraphs (COBOLNET_DESIGN §14.9 / §18 #14, PHASE4_RECONCILIATION) — no register note or owner decision ever owned that divergence. If byte stability of a single-octet HIGH-VALUE image at the file boundary is wanted, that is a codec question (PB690), not a collating one.

Rows claimed: GR-12.3.7.4-8, GR-12.3.7.4-10, GR-12.4.5.7.4-1. Flip `status` and write `closes_rows` in the landing commit (CLAUDE.md rule 8).

## Extended by REGISTRAR 15b (batch 15b, pin @`071ff04ea`) — rows `GR-8.3.3.6.4-6`

HIGH-VALUE outside SPECIAL-NAMES is pinned to U+00FF in the native alphanumeric sequence (AlphabetDef.HighValue), the national context with no national PCS (FigurativeConstants.FillChar 'H'), and the tail of a literal-phrase alphabet — §8.3.3.6.4 GR6 is the same one-rule-over-two-repertoires defect

**GR-8.3.3.6.4-6**

> At compile time and when referenced in the SPECIAL-NAMES paragraph, the high-value format represents …

`python scripts/spec/cite.py --check 8.3.3.6.4 "At compile time and when referenced in the SPECIAL-NAMES paragraph, the high-value format represents"` → `OK  §8.3.3.6.4 6)  (General rules)`

### Finding — `figurative-constant-values` (wrong-answer; rows `GR-8.3.3.6.4-6`)

**Mechanism.** HIGH-VALUE is pinned to U+00FF where the runtime collating sequence has 65,536 positions: the native alphanumeric sequence (AlphabetDef.HighValue '?? 0xFF'), the national context with no national PCS (FigurativeConstants.FillChar/Fill 'H' -> U+00FF when cat is National), the tail of an alphanumeric literal-phrase alphabet (CollatingTable computes HighValue 'over the Latin-1 block' against §12.3.7.4 GR7 k3), and the SPECIAL-NAMES native alphanumeric extreme (DataBinder.Switches.AlphabetFigurative) — self-described in the design docs as 'the flagged §8.3.3.6 GR6/GR7 divergence ... kept byte-stable', with no owner decision behind it; the same compiler already answers U+FFFF for the national SPECIAL-NAMES arm, a LOCALE sequence and NationalAlphabetDef, so it gives two answers to one question

**Repro.** IDENTIFICATION DIVISION. PROGRAM-ID. HV1. DATA DIVISION. WORKING-STORAGE SECTION. 01 XA PIC X. 01 NA PIC N. PROCEDURE DIVISION. MOVE FUNCTION CHAR(20000) TO XA. IF XA > HIGH-VALUE DISPLAY 'X-GT' ELSE DISPLAY 'X-OK' END-IF. IF NX"4E00" > HIGH-VALUE DISPLAY 'N-GT' ELSE DISPLAY 'N-OK' END-IF. MOVE HIGH-VALUE TO XA. DISPLAY FUNCTION ORD(XA). STOP RUN.  (probes p1/p2/p3/p7.cob under out/probe/figurative-constant-values)

**Expected from the spec.** X-OK, N-OK, 65536: §8.3.3.6.4 GR6 makes HIGH-VALUE 'the character ... that has the highest ordinal position in the runtime collating sequence', national contexts using the national program collating sequence; the native alphanumeric and national sequences here have 65,536 positions (D-N1/D-N3, DOC-A.1-188), so HIGH-VALUE is U+FFFF (ordinal 65536). Under 'ALPHABET AL IS "B" "A" "C"' as PCS it is likewise U+FFFF (§12.3.7.4 GR7 k3: unspecified characters follow in native order). In SPECIAL-NAMES, 'CLASS HVC IS HIGH-VALUE' is the class {U+FFFF} (§12.3.7.4 GR10 native extreme).

**Observed / reasoning.** X-GT, N-GT, 256. p3.cob under the literal-phrase PCS: ORD(HIGH-VALUE)=256 and CHAR(20000) > HIGH-VALUE true. p7.cob: CHAR(65536) IS HVC false, CHAR(256) IS HVC true. Conforming arms for contrast: explicit national PCS (p5: HIGH-VALUE = NX"FFFF") and LOCALE PCS (p4: CHAR(20000) > HIGH-VALUE false). Every 'IF key = HIGH-VALUES' end-of-file sentinel and 'MOVE HIGH-VALUES' sort-high idiom silently loses to any stored character above U+00FF.

**Code site.** src/Cobol.Net.Compiler/CodeGen/Roslyn/FigurativeConstants.cs FillChar/Fill ('H' => ... 'ÿ' / "'\\u00ff'"); src/Cobol.Net.Compiler/Binding/CollatingModel.cs:178 AlphabetDef.HighValue; CollatingModel.cs CollatingTable HighValue (Latin-1 block, doc at :40-44); src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs:1646 AlphabetFigurative
