---
title: "PB244 — A legal DISPLAY group operand the STRUCT-SCOPED image composer cannot build is refused at RUN TIME: a pointer/object-class leaf and an access-path-dependent extent both become a Tier-C loud instead of a rendering — and a GREEN test pins the refusal"
id: PB244
kind: defect
status: open
severity: MINOR
area: codegen
wrong_answer: false
crashes: true
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.11.4"]
inventory_rows: ["GR-14.9.11.4-7"]
closes_rows: ["GR-14.9.11.4-4", "GR-14.9.11.4-6"]
tags: [cobolsharp, work, defect]
---

# PB244

**Rows:** `GR-14.9.11.4-6`, `GR-14.9.11.4-7`

A legal DISPLAY group operand the STRUCT-SCOPED image composer cannot build is refused at RUN TIME: a pointer/object-class leaf and an access-path-dependent extent both become a Tier-C loud instead of a rendering — and a GREEN test pins the refusal.

ROWS: GR-14.9.11.4-6 (PARTIAL — GR6, the sending size) and GR-14.9.11.4-7 (PARTIAL — GR7, the variable-length-group display format). Both were narrowed to a single residue each by the PB164/R40 wave, and the two residues are the same mechanism seen from two rules.

MECHANISM. A group identifier-1 reaches the DISPLAY emitter as an IMAGE: `GroupImageCodec.EmitCurrentImageMethod` composes the members' images inside the record STRUCT, and `AcceptDisplayEmitter.EmitDisplay` writes that one string. The composer is therefore bounded twice — by what a leaf can contribute as characters, and by what a struct-scoped method can see. Anything outside those bounds is not rendered and not diagnosed at bind: it is staged as a Tier-C runtime loud on source no syntax rule forbids.

SHAPE (a) — CLASS POINTER / OBJECT LEAF (GR-14.9.11.4-6's whole residue, and GR-14.9.11.4-4's residue member (1)). A group with a POINTER / PROGRAM-POINTER / FUNCTION-POINTER / OBJECT REFERENCE leaf has no character image in the byte-cell model, so it throws with no sending size at all. §14.9.11.3 SR1 excludes only the ELEMENTARY pointer/object IDENTIFIER — the containing GROUP is a legal identifier-1. That is the same SR1 reading PB148 relied on in the OTHER direction to build the elementary class gate, so the compiler now holds both halves of one rule and honours neither for the group. MEASURED: tests/Cobol.Net.Tests.Conformance/TierCRejectionTests.cs:66-89 — `AssertPointerGroupLoud` compiles `01 WS-GP. 05 WS-GP-A PIC X(3). 05 WS-GP-P USAGE POINTER.` then `DISPLAY WS-GP.` and asserts `Assert.False(ok, "a pointer-leafed group has no whole-group image — loud (§1.4)")` plus `Assert.Contains("Tier-C", detail)`. The program COMPILES and throws at run time — a GREEN test pinning the refusal of legal source, the feedback_green_test_can_hold_a_gap_open shape exactly.

SHAPE (b) — AN EXTENT THAT LIVES ON THE ACCESS PATH (GR-14.9.11.4-7's whole residue). A runtime-length item INSIDE a table element, and any member carrying or containing OCCURS DEPENDING, are rejected loudly because the current count/extent lives on the OPERAND's access path and is unreachable from a struct-scoped composer (the same boundary the named stage `IntrinsicBinder.VariableLengthGroupSum` documents). docs/CONFORMANCE.md §7 row 57 records these as 'Excluded shapes, rejected loudly rather than shown at a wrong extent' — an honest disclosure of the current state, but A.1 item 57 asks the implementor to define the FORMAT of a variable-length group's display, not to license refusing legal operands.

WHY ONE NOTE. The fix is one fix: ask the question of the REFERENCED operand and give the composer the operand's access path (the discriminator lesson PB189 states for its own arm), and decide what a pointer/object leaf CONTRIBUTES to a group image — the latter is an A.1 item 56/57 determination and is the one owner-visible question inside this note. Fixing either shape alone leaves the other, and leaves PB189's third arm.

SPEC REFS (carried from the rows): §14.9.11.4 GR4 (the transfer), GR6 (the sending size), GR7 (the VLG format, A.1 item 57); §14.9.11.3 SR1 (the ELEMENTARY-only class exclusion); §8.5.1.12.1 (a variable-length group is one with a dynamic SUBORDINATE — the clause PB189 uses to prove an ELEMENT is a fixed-length group).

SEVERITY NOTE: filed MINOR to match its open sibling PB189, which carries identical flags at MINOR; on what it DOES to a user program — a conforming DISPLAY aborts at run time — it would rank higher, and all three members should be re-ranked together.

## Mechanism

The DISPLAY sender obtains a GROUP operand's character image from the struct-scoped composer `src/Cobol.Net.Compiler/CodeGen/DataDivision/GroupImageCodec.cs#EmitCurrentImageMethod`, reached through `AcceptDisplayEmitter.EmitDisplay` / `OperandText.FieldAsString`; every shape that composer cannot represent is turned into a Tier-C runtime loud rather than into an image. GR4's 'the data item being transferred' and GR6's 'sending size' fail together because they are the same decision point.

## Lineage

PB164 + R40 (LANDED) narrowed both rows to exactly this residue — R40's review fleet caught the first CONFORMS flip over-claiming 'every operand kind', and the numeric leaf kinds (COMP/COMP-5/BINARY-*/PACKED/COMP-1/COMP-2/INDEX) plus the composable variable-length group are now pinned byte-level. PB176 (LANDED) added the GroupImage guard for the ODO-plus-dynamic sender. PB189 (OPEN) is the THIRD member of this same family (a subscripted dynamic-capacity-table ELEMENT is loud although its struct carries CurrentImage/AsImage) and already owns GR-14.9.11.4-4 — but PB189 covers only that row's residue member (2); member (1) is shape (a) of THIS note, so closing PB189 alone will not flip GR-14.9.11.4-4.

---

> Registered by the **defective-row registration wave** (2026-08-31). Every PARTIAL / NOT-IMPLEMENTED /
> DIVERGES row of the P14 traceability inventory now names a live work note in its `inventory_rows`
> frontmatter, and `DefectiveRowCoverageDriftTests` fails the battery if one stops doing so. The
> `inventory_rows` field is the SSOT for which rows this note owns; when the note lands, those rows must
> be re-verdicted in the same change set or the gate goes red.

## Shape (a) LANDED — wave 53 (w53c); shape (b) OPEN (the note stays open for it)

**Re-probed first** on the w53c build (after merging w52f): `w53c/p244a.cob` still aborted "whole-group image of
'WS-GP' with a pointer/object-class leaf", `w53c/p244b.cob` still aborted "... with a dynamic-length /
dynamic-capacity member". A third probe, `w53c/p244f.cob` (the SAME strong group declared BASED and ALLOCATEd),
displayed `[abc        ]` — the cell-backed twin already rendered the pointer's 8 reserved positions (D-SLOT) while
the record-struct twin aborted. That fixed the determination: one storage, one rendering.

**Determination (A.1 item 56, §14.9.11.4 GR1 — implementor-defined conversion):** a strongly-typed group with a class
pointer/object leaf transfers its STORAGE image, each such leaf as its `ByteWidth` (8) reserved placeholder
positions, spaces — never the managed reference. GnuCOBOL (the precedence tiebreaker) displays a group's raw
storage; this implementation's pointer has no address bytes, so its storage positions are the placeholders.

**Mechanism:** `DataItem.TransferImageCapable` / `ElementTransferImageCapable` (+ `Place.TransferImageCapable`,
forwarded by decorators, element-shaped on `DynTablePlace`) is `IsImageCapable` widened by exactly the slot-carried
leaf kind. The record struct emits a ONE-WAY `AsImage()` for it (`GroupImageCodec.EmitImageMethods` withholds
`FromImage` and the bit/national faces), the slot leaf contributing `GroupImageCodec.SlotPlaceholder` — the one
recipe the cell seed `ImageInitOfOne` also spells. Only the TRANSFER readers ask it, through a `transfer:` flag on
`PlaceRenderer.GroupImage`/`SendingGroupImage`/`SendingImage` and `OperandText.AsStorageImage`: the DISPLAY
emitter's group arm and `OperandText.NonElementaryMoveSender` (MOVE's sending group, §14.9.25.4 GR4; §14.9.25.3
SR2 constrains only a strongly-typed RECEIVER). ⛔ NOT folded into `IsImageCapable`: the placeholder image is not
injective and has no inverse, so strong-group EQUALITY (image-based for the two-way codec, §8.8.4.2.12) and every
read-back keep refusing it — widening the one predicate would have turned the comparison's runtime loud into a
silent wrong answer (`IF A = B` true for groups whose pointers differ). Measured before/after on `w53c/p244g.cob`.

**Rows:** `GR-14.9.11.4-4` and `GR-14.9.11.4-6` → CONFORMS (batch `w53c/PB244a-verdicts.json`, GAP 2175 → 2173 on
this branch). Goldens `conformance:2002/pb244_pointer_group_transfer` (+ negative
`pb244-pointer-group-transfer-85`, COBOLNET0900). `TierCRejectionTests.DisplayPointerGroup_FailsLoud` /
`MovePointerGroup_FailsLoud` — the GREEN tests that pinned the refusal — are now
`DisplayPointerGroup_RendersPlaceholderPositions` / `MovePointerGroup_SendsPlaceholderPositions`.

**NEXT — shape (b), `GR-14.9.11.4-7`:** the current-extent composer (`GroupImageCodec.EmitCurrentImageMethod`,
gated by `DataItem.CurrentExtentImageCapable`) must receive the operand's ACCESS PATH for an OCCURS DEPENDING
count (and admit a runtime-length item inside a fixed-OCCURS element). ⚠ It is a SHARED composer: DOC-A.1-57 names
the §14.8.2.2/§14.8.3.2 boundary crossing (PB204), the §14.9.25.4 GR9 MOVE, INITIALIZE and the CORRESPONDING
verbs (PB393) as its consumers, and `IntrinsicBinder.VariableLengthGroupSum` states the same boundary for
FUNCTION LENGTH — the fix widens all of them together, and `VarParts` / `AsVarImage` / `FromVarImage` must take
the same shapes. Repro: `w53c/p244b.cob`.

## Re-probed — wave 52 (w52f), NOT fixed in that wave

Both shapes still reproduce on the w52f build (after PB189 and PB202 landed there): shape (a) `w52f/t/p244a.cob` —
`DISPLAY WS-GP` over a STRONG typedef with a POINTER leaf aborts "whole-group image of 'WS-GP' with a
pointer/object-class leaf"; shape (b) `w52f/t/p244b.cob` — a variable-length group holding a DYNAMIC LENGTH leaf and a
member containing an OCCURS DEPENDING table aborts "whole-group image of 'G' with a dynamic-length / dynamic-capacity
member". **Ownership:** PB189's landing narrowed `GR-14.9.11.4-4` to exactly this note's shape (a), so the row moved here.
The w52f split judged this a SECOND mechanism from PB189/PB202's (those asked an existing image question of the right
operand; this one needs the composer to take the operand's ACCESS PATH for an ODO count, and an A.1 item 56/57
determination of what a pointer/object leaf contributes to a group image).

## Extended 2026-09-23 by REGISTRAR #13 (`leads-w53-w57.md`, train-57 lander report)
Re-probed on `c54434a8d` (w53c repros): `IF WS-GP = WS-GQ` over a STRONG group with a USAGE POINTER leaf
(`p244g.cob`) prints `abc` / `PTR-COPIED` and then aborts with `NotImplementedCobolFeatureException … whole-group
image of 'WS-GP' with a pointer/object-class leaf … Tier-C`; `STRING WS-GP DELIMITED BY SIZE … INTO WS-DST`
(`p244i.cob`) prints `11` (BYTE-LENGTH) and aborts the same way. Both are this note's scope.
- The equality leg: §8.8.4.2.12 compares strongly-typed groups element by element, so an image comparison is the
  wrong mechanism even once an image exists (the placeholder image is not injective) — the fixer owes the
  per-element comparison.
- The STRING leg: `cite.py --check 14.9.43.3` SR1 requires every identifier except identifier-4 to be 'described
  implicitly or explicitly as usage display or national'. Whether a group with a pointer leaf is 'implicitly usage
  display' is the question to adjudicate FIRST: if it is not, this leg is a compile-time under-reject, not a Tier-C
  build (w53c lead 2).
