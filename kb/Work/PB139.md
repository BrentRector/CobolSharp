---
title: "PB139 — ✅ LANDED 2026-08-29 (screens via one Table-16 ask + 0818/1691/1637; JUSTIFIED via the one StrStoreAligned dispatch; device record-boundary split + zero-length consume + boolean conversion defined). WAS: ACCEPT re-implements the MOVE rules instead of routing through MoveTable16 and the copy disagrees with the table (NATIONAL receivers loud, ALPHABETIC accepted post-PB124, JUSTIFIED ignored); pointer/object/boolean receivers are unscreened (CS0029 or raw-write); the device model splits long records on only one arm of its two-arm dispatch and a zero-length receiver fails to consume its record"
id: PB139
kind: defect
status: landed
severity: MAJOR
area: binding
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB139

Rows: SR-14.9.1.3-1/-3/-6, GR-14.9.1.4-1/-2/-4/-6, findings [ACCEPT statement]/[accept-statement].

(1) EmitAcceptTemporal's store is a hand copy of the MOVE rules that disagrees with Table 16 both ways: a NATIONAL receiver (Table-16 Yes, SR3-permitted) hits the default LoudStmt while an ALPHABETIC receiver (Table-16 No) is accepted — the binder's excuse comment ('await the PicCategory split') is STALE since PB124 landed CobolClass.Alphabetic; and the alphanumeric arm ignores item.Justified (RuntimeApi.StrStoreJustified exists; MoveEmitter honours it at three sites) — `ACCEPT WS-J FROM DAY` with JUSTIFIED RIGHT stores left-aligned. Route the temporal store through Table 16 (one rule, one place). (2) Receiver screens: only Usage.Index is checked; a pointer/object receiver emits raw C# (CS0029 — the PB111 shape); a boolean PIC 1 receiver takes raw device characters into the '0'/'1' substrate; an index-NAME draws COBOLNET1639 'not defined' instead of R16's COBOLNET1637 (ACCEPT was not swept); strongly-typed and variable-length groups unscreened (predicates exist). (3) Device: records LONGER than RecordSize contribute their full length on the wide-receiver arm (`line.Length < RecordSize ? PadRight : line`) — GR4a/b require the boundary split; the narrow-receiver arm is pinned correct (one arm fixed). RecordSize=80's A.1 item-5 determination is CONTRADICTED by that behavior, and items 1/5 are undocumented (→ PB160). Zero-length receiver never consumes its record (AcceptSource.Device early-returns before touching Console.In). COMP-1/COMP-2 receivers stay a documented-loud deferral pinned by CheckOnlyCompileTests.

*(Found by Phase-B batch 8 — §8.8.1 + §14.9.1–14.9.12, 2026-08-29; one adjudicator + one independent refuter per subject. Full forensic evidence: the named rows' `notes` in `tests/version-matrix/traceability-inventory.json`; the raw finding lines in the session scratchpad `pb-batch8/findings.txt`.)*
