---
title: "PB263 — every CALL ... BY CONTENT with a floating-point literal argument fails to compile with a RAW ROSLYN CS1503: CallEmitter renders the literal through EmitText.UnscaledLit, which returns a binary64 C# literal, and then hands it to ManagedPointer<long|Int128>.Cell — conforming source rejected at the wrong stage, with no COBOL diagnostic at all"
id: PB263
kind: defect
status: landed
severity: MAJOR
area: codegen/call
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.4", "14.2.3", "8.3.3.3.3"]
tags: [cobolsharp, work, defect, call, literal]
---

# PB263 — the CALL argument lane has its own literal rendering, and it cannot carry a float literal

Found by the SIBLING SWEEP of owner decision D-B (the arithmetic wave, 2026-08-31), which asked where else a
floating-point literal is rendered. **Pre-existing at HEAD `6cd228b6` — D-B neither caused nor fixed it.**

## Measured, 2026-08-31, at HEAD and again after D-B (identical both times)

```cobol
CALL "P5T" AS NESTED USING BY CONTENT 1.5E+3 BY REFERENCE R2
```

```
error: backend compilation failed (generated C# at pdb5.g.cs):
  (43,144): error CS1503: Argument 1: cannot convert from 'double' to 'long'
```

and with a wider literal, the Int128 carrier arm:

```cobol
CALL "P4T" AS NESTED USING BY CONTENT 1.2345678901234567890123E+3 BY REFERENCE R2
```

```
  (47,146): error CS1503: Argument 1: cannot convert from 'double' to 'System.Int128'
```

**BOTH carrier arms fail, so the defect is universal**: any floating-point literal, of any width, passed
BY CONTENT to a CALL. There is no workaround short of introducing a data item.

## The site and the root cause

`src/Cobol.Net.Compiler/CodeGen/Verbs/CallEmitter.cs`, the `case BoundNumericLiteral n:` arm of the argument
builder:

```csharp
var lit = UnscaledLit(n.Text);
int digits = n.Text.Count(char.IsAsciiDigit);
string cellT = lit.U ? "UInt128" : digits > 18 ? "Int128" : "long";
return $"new CobolArg({...}, ManagedPointer<{cellT}>.Cell({lit.Expr}), {digits}, {lit.Scale})";
```

`EmitText.UnscaledLit` returns `new NumX(t, 0, Real: true)` — the literal's own text as a C# `double` literal —
for any E-form literal, so `lit.Expr` is `1.5E+3` and the cell type is chosen from a DIGIT COUNT that also
miscounts (the exponent's digits are counted as significand digits: `"1.5E+3"` yields 3).

⛔ **The root cause is not the cast — it is that the CALL argument lane carries a SECOND literal rendering.**
`NumericRenderer.LiteralNum` is THE literal → `NumX` mapping (since D-B, one rendering for every position and
every arithmetic mode); this arm reaches past it to the raw `UnscaledLit` helper and then re-derives a carrier
and a scale of its own. That is [[feedback_one_rule_one_place]] in its usual shape, and it is why D-B's own
statement — "the literal is its exact §8.3.3.3.3 rule-5 value everywhere" — has an exception here.

## Fix shape (NOT implemented in the arithmetic wave — a different subsystem, §14.9.4 argument conformance)

Give `CallEmitter` ONE numeric-argument funnel used by BOTH the `BoundNumericLiteral` arm and the
`BoundComputedOperand` arm (which has its own, differently-broken, defect — [[PB264]]): render the operand
through `NumericRenderer`, land it once, and choose the carrier from the resulting `NumX` (`U` → `UInt128`,
`Dec`/wide → `Int128`, else `long`) rather than from a digit count taken off the source text. Two arms, one
rule — and the drift test that belongs with it is the one asking whether any emitter still calls
`EmitText.UnscaledLit` on operand text instead of going through the renderer (`OoEmitter.cs:838`, the INVOKE
argument path, is the third such site and has not been measured yet).

Goldens owed: a positive 2023 case passing a float literal BY CONTENT and BY VALUE and displaying what the
callee received, plus the wide fixed-point literal of [[PB264]] in the same program so the two arms are pinned
side by side.

## LANDED 2026-09-02 — together with [[PB264]] (one root cause, one funnel)

**The root cause was narrower and more structural than "the CALL lane has its own literal rendering".**
`EmitText.UnscaledLit`'s contract is "render a numeric literal as a scaled integer", and it silently returned
something else — the literal's own source text as a binary64 `Real` NumX — for the floating-point form alone. The
two consumers that render OPERAND text through it (`CallEmitter`, and `OoEmitter`'s INVOKE arm) believed the
contract. So the fix is at the helper, not at the call sites: `TryUnscaledParts` now decomposes BOTH notations to
the exact `(unscaled digits, scale)` pair — ISO §8.3.3.3.2 rule 4 for the fixed-point form, §8.3.3.3.3 rule 5
("the algebraic product of the value of its significand and the quantity derived by raising ten to the power of
the exponent") for the E-form — taken from `NumericLiteral.TryParseExact`'s (significand, power of ten) and
NEVER expanded to text, so a large exponent moves the SCALE NEGATIVE instead of building a multi-thousand-character
string. `CobolNum.Rescale` already carries a negative scale (it is what a PICTURE-P item uses).

⛔ **This did not touch the arithmetic lane.** `NumericRenderer.LiteralNum` takes a canonical float literal onto
the SDIDI (`Dec`) lane BEFORE reaching `UnscaledLit`, so D-B's §8.8.1.3 intermediate-lane determination is
unchanged; only the two operand-text consumers moved. That is why the blast radius is exactly the two defects.

**The carrier is now decided once.** `EmitCore.IntLiteralCore` returns the rendered text, the unsigned-wide flag
AND the C# carrier type together. The cell type used to be re-derived at the call site from
`Text.Count(char.IsAsciiDigit)`, which counts a float literal's EXPONENT digits as significand digits — `1.5E+3`
"has 3 digits", so the site asked for a `long` cell and handed it a `double`. Two derivations of one fact.

**Measured, at HEAD, before and after** (formal `PIC S9(5)V9(20)`):

| written | before | after |
|---|---|---|
| `BY CONTENT 1.5E+3` | CS1503 `double`→`long` | `1500` |
| `BY CONTENT 1.2345678901234567890123E+3` | CS1503 `double`→`Int128` | `1234.5678901234567890123` |
| `INVOKE … BY CONTENT 1.5E+3` | CS1503 `double`→`Int128` | `1500` |

Goldens: `tests/conformance/2023/pb263_call_literal_arg_exact_value` (the four-cell table — two notations x
BY CONTENT / BY VALUE, all four the same value — plus the narrow and signed float arms) and
`pb263_invoke_literal_arg_exact_value` (the third arm). Both registered in the 2023 manifest.

Inventory: `GR-8.3.3.3.3-5` was already CONFORMS, flipped by D-B on 2026-08-31 on the claim that the literal's
value is exact "in EVERY position" — a claim whose five goldens were all VALUE-clause or arithmetic positions, so
none of them could have contradicted it while these two positions did not even compile. Its evidence set now
carries the argument-lane goldens. **A green row held this open, exactly as [[feedback_green_gates_arent_evidence]]
predicts.**
