---
title: "PB1390 — the abbreviated-combined-relation tail is spelled non-uniformly across the OR / XOR / AND tiers of the condition grammar: OR admits only `abbreviatedAndChain` (abbreviated AND abbreviated…, no bare/NOT-bare continuation, no XOR), AND admits an abbreviated relation only after a leading one, and XOR's right operand cannot LEAD with one, so `IF A = B OR < C AND D`, `A = B OR < D AND C = 1`, `A = B XOR < C` and `A = B OR < C XOR = D` are COBOL0001 at every edition although §8.8.4.12.2 repeats ONE connective group in any position"
id: PB1390
kind: defect
status: open
severity: MAJOR
area: frontend/grammar
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1391", "PB1392", "PB1162"]
spec_refs: ["8.8.4.12.2", "8.8.4.12.1", "8.8.4.12.4"]
inventory_rows: ["FMT-8.8.4.12.2", "SR-8.8.4.12.1-1", "SR-8.8.4.12.1-2", "GR-8.8.4.12.4-1"]
closes_rows: []
tags: [cobolsharp, work, defect]
---
Filed 2026-09-24 by **REGISTRAR 15b** from lane-3 adjudication batch **15b** (adjudicators + refuters on the pinned tree `E:\CobolSharp-pins\adj-d2` @`071ff04ea`). Not re-probed by the registrar (MANDATORY-PRACTICES R2: every lead carries its repro and code site); origin/main since the pin carries only registrar landings (no src change), so nothing landed has touched this mechanism.

# PB1390 — OPEN

## The rule (every citation re-run by the registrar through `cite.py --check`)

**FMT-8.8.4.12.2**

> AND, OR, EXCLUSIVE-OR, XOR, and NOT are underlined in the printed standard (required words). …

`python scripts/spec/cite.py --check 8.8.4.12.2 "AND, OR, EXCLUSIVE-OR, XOR, and NOT are underlined in the printed standard (required words)."` → `OK  §8.8.4.12.2   (General format)`

**SR-8.8.4.12.1-1**

> The omission of the subject of the relation condition, or …

`python scripts/spec/cite.py --check 8.8.4.12.1 "The omission of the subject of the relation condition, or"` → `OK  §8.8.4.12.1 1)  (General)`

**GR-8.8.4.12.4-1**

> The effect of using abbreviations is as if the last preceding stated subject were …

`python scripts/spec/cite.py --check 8.8.4.12.4 "The effect of using abbreviations is as if the last preceding stated subject were"` → `OK  §8.8.4.12.4 1)  (General rule)`

## Finding — `abbreviated-combined-relation-conditions` (rejects-legal-source; rows `FMT-8.8.4.12.2`, `SR-8.8.4.12.1-1`, `SR-8.8.4.12.1-2`)

**Mechanism.** The abbreviated tail is spelled non-uniformly across the three logical tiers of the condition grammar: AND admits `abbreviatedRelation | unaryLogicalExpression`, OR admits `logicalXorExpression | abbreviatedAndChain` where abbreviatedAndChain = abbreviatedRelation (AND abbreviatedRelation)* (no bare / NOT-bare continuation, no XOR), and XOR's right operand is logicalAndExpression, which cannot LEAD with an abbreviatedRelation. The printed format repeats ONE group `{AND|OR|EXCLUSIVE-OR|XOR} {NOT|simple-rel-op|extended-rel-op} object-1` with any connective in any position, so several legal mixes are unparseable.

**Repro.** 01 A PIC 9 VALUE 1. 01 B PIC 9 VALUE 2. 01 C PIC 9 VALUE 3. 01 D PIC 9 VALUE 1.  IF A = B OR < C AND D DISPLAY "T" ELSE DISPLAY "F" END-IF (also --std 85); IF A = B OR < C AND NOT D ...; IF A = B XOR < C ...; IF A = B XOR NOT < C ...; IF A = B OR < C XOR = D ...

**Expected from the spec.** All compile. §8.8.4.12.4 GR1 expansions: (A=B) OR ((A<C) AND (A<D)) = F; (A=B) OR ((A<C) AND NOT (A<D)) = T; (A=B) XOR (A<C) = T; (A=B) XOR (A NOT< C) = F; (A=B) OR ((A<C) XOR (A=D)) = F (XOR binds tighter than OR, §8.8.4.9 precedence).

**Observed / reasoning.** Pinned cobol.exe: COBOL0001 unexpected 'D' / cannot parse near 'D' / unexpected '<' for the first four; COBOLNET0901 "'XOR' is a reserved word in COBOL-2023 and cannot be used as a user-defined word" for the OR<C XOR shapes. The tier shape partialExpression / partialXorExpression / partialAndExpression already demonstrates the fix pattern (a leading-element variant per tier); the abbreviated tail needs the same: every tier's non-first operand may begin with an abbreviatedRelation and continue through the ordinary AND/XOR tiers. EVALUATE partial-expressions reuse abbreviatedAndChain as their OR tail and inherit the same gap (sweep).

**Code site.** src/Cobol.Net.Frontend/Grammar/Core/CobolExpressions.g4#logicalOrExpression / #logicalXorExpression / #logicalAndExpression / #abbreviatedAndChain; mirrored in #partialExpression

## Finding — `complex-combined-conditions` (rejects-legal-source; rows `FMT-8.8.4.12.2`)

**Mechanism.** OUT-OF-SLICE LEAD (row FMT-8.8.4.12.2, not adjudicated here): logicalXorExpression's right operand is logicalAndExpression, which has no abbreviatedRelation / abbreviatedAndChain alternative at its left edge (only the OR tier carries abbreviatedAndChain), so an abbreviated combined relation after XOR / EXCLUSIVE-OR cannot parse although §8.8.4.12.2's format lists EXCLUSIVE-OR and XOR as its connectives.

**Repro.** IF A = 1 XOR = 2 DISPLAY "T" ELSE DISPLAY "F" END-IF / IF A = 1 XOR 2 ... / EVALUATE A WHEN > 5 XOR < 3 ... (probe/ccc/g5.cob, g3.cob, --std 2023).

**Expected from the spec.** With A=1: `A = 1 XOR = 2` means `A = 1 XOR A = 2` -> T; `A = 1 XOR 2` means `A = 1 XOR A = 2` -> T (§8.8.4.12.4 implied insertion).

**Observed / reasoning.** error COBOL0001: unexpected '=' (g5) / unexpected '<' (g3). Registrar should verify against the FMT-8.8.4.12.2 slice before filing.

**Code site.** src/Cobol.Net.Frontend/Grammar/Core/CobolExpressions.g4#logicalXorExpression; src/Cobol.Net.Frontend/Grammar/Core/CobolExpressions.g4#partialXorExpression

## Measured (rows this note claims beyond the findings above)

### `GR-8.8.4.12.4-1` — PARTIAL (adjudicator + refuter record, verbatim)

Rule: insert the last preceding stated subject and the last stated relational operator; "The insertion of an omitted subject and/or relational operator terminates once a complete simple condition is encountered within a complex condition." cite.py --check 8.8.4.12.4 (both sentences) -> OK. Code: AbbrevCarry threaded left-to-right through BindFlatSequence; a fully stated relation sets Subject+Op, an abbreviated relation updates Op only, class/sign/omitted/condition-name/boolean and parenthesized conditions Reset() the carry. MEASURED: probe ab1.cob runs ALL SIX NOTE examples (a > b AND NOT < c OR d; a NOT EQUAL b OR c; NOT a = b OR c; NOT (a GREATER b OR < c); NOT (a NOT > b AND c AND NOT d); NOT (a NOT > b XOR c AND NOT d)) plus three extra chains against their spec expansions over all 81 tuples of A..D in 1..3 -> N=0729 BAD=0000. Termination: `A = B OR C IS NUMERIC OR D` and `A < B AND (C = D) OR D` are rejected (D has no carried subject) with COBOLNET2318. Shapes the parser cannot reach (XOR-led / OR-then-AND-bare tails) are FMT/SR defects, not a failure of the insertion semantics. Conformance-test covers insertion, NOT-in-operator carry, the first NOTE example and the paren fresh scope; the termination-by-class/88 half is not in a spec-derived test. || REFUTED: PARTIAL — The insertion semantics are correct on every shape the compiler ACCEPTS: a randomized differential of 270 accepted chains, each with 81 value tuples, gave 0 mismatches against a Python evaluator of the spec expansion (NOT > AND > XOR > OR). The same differential shows the GR is not delivered for a large class of legal abbreviated conditions. Every chain where AND follows an OR-led abbreviated relation is rejected with COBOL0001, and so is every chain where an abbreviated relation follows XOR. That holds whether the AND's right side is a bare object (the adjudicator's OR-then-AND-bare case) or a fully STATED relation. The adjudicator did not report the stated case: `A = B OR < D AND C = 1` is rejected at (11,32) 'unexpected C'. That source is exactly this GR's termination half (a complete simple condition ends the insertion and starts a new subject), and it can never be exercised. About 18 of 290 random AND/OR chains (6%) and 31 of 80 chains with XOR were rejected. The GR has two halves (insert, then terminate at a complete simple condition). The insert half is verified on the reachable shapes. The terminate half is unreachable for OR-abbrev-AND-stated and has no spec-derived test (the adjudicator concedes the class/88 termination is untested). Calling the unreachable shapes only 'FMT/SR defects' is not enough for a CONFORMS on the semantic rule: the rule's effect never occurs for legal source. By the two-halves rule this is PARTIAL.

## What the fixer owes

Rewrite the abbreviated tail as ONE production reachable at every tier's non-first operand (the partialExpression / partialXorExpression / partialAndExpression leading-element pattern already in CobolExpressions.g4 is the shape), so any of AND / OR / XOR / EXCLUSIVE-OR may precede an abbreviated relation and any may follow it; the binder's AbbrevCarry is already correct on every reachable shape (0 mismatches over 270 random chains x 81 tuples), so the fix is grammar + the carry's termination on a stated relation. Sweep EVALUATE's partialExpression, which reuses abbreviatedAndChain as its OR tail. Goldens: the six §8.8.4.12.4 NOTE expansions plus the OR-then-AND-stated termination case.
