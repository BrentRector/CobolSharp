---
title: "PB1153 — `DISPLAY LINE-COUNTER` / `DISPLAY PAGE-COUNTER` (qualified or not, alone or after a literal) compiles clean and ABORTS at run time with NotImplementedCobolFeatureException 'computed expression in a string context' — the report special registers are the unswept sibling of R16's index-name fix"
id: PB1153
kind: defect
status: open
severity: MAJOR
area: codegen/operands
wrong_answer: false
crashes: true
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1049", "PB1130"]
spec_refs: ["8.4.3.15"]
inventory_rows: []
closes_rows: []
tags: [cobolsharp, work, defect]
---

Filed 2026-09-24 by **REGISTRAR 14b** from a SIDE LEAD an adjudicator of batch **d1-14b** reported under `dossier_gaps` (pinned tree @`68bbdb00f`; not re-probed by the registrar — the repro path is the adjudicator's).

# PB1153 — OPEN

## The rule
> "The PAGE-COUNTER and LINE-COUNTER identifiers are generated automatically and exist independently for each report"
`cite.py --check 8.4.3.15.1 "The PAGE-COUNTER and LINE-COUNTER identifiers are generated automatically and exist independently for each report"` → `OK  §8.4.3.15.1   (General)`

## Measured
initiate-statement adjudicator probe `out/probe/initiate/p5.cob`; invoke-statement-p2 adjudicator probe `pe11` (`DISPLAY "PC=" PAGE-COUNTER` after INITIATE). Both abort at run time.

## Code site
`src/Cobol.Net.Compiler/CodeGen/Emit/OperandText.cs:536` (`EmitText.LoudValue("string", "computed expression in a string context")`) — the operand-text renderer has no arm for a report special register; R16 (landed) added the INDEX-NAME arm only.

## What the fixer owes
Render a report counter as its numeric DISPLAY text in every string context (DISPLAY, STRING, MOVE to alphanumeric), and sweep every other special register that reaches the LoudValue fallback.
