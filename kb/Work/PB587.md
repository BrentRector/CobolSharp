---
title: "PB587 — §13.16.3 SR9's IMPLIED PICTURE is unimplemented and the entry emits UNCOMPILABLE C# on legal source: `01 W VALUE \"AB\".` (no PICTURE, an alphanumeric literal VALUE) shall imply `PICTURE X(2)`, but `DataBinder.BindEntry` binds NO item for an entry with no PICTURE, no USAGE and no members, and the emitter then references `W` — `error CS0103: The name 'W' does not exist` — a backend crash where the standard requires a two-character alphanumeric item"
id: PB587
kind: defect
status: landed
severity: MAJOR
area: binding/data-division
wrong_answer: false
crashes: true
silent: false
rejects_legal_source: true
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["13.16.3"]
inventory_rows: []
closes_rows: []
closes_rows_reason: "the note claimed no inventory row (inventory_rows was empty); the rows §13.16.3 SR9 owns were closed by PB504, the landing that fixed this mechanism"
tags: [cobolsharp, work, defect]
---

# PB587 — LANDED (by PB504) — a legal entry that produces no item

Found 2026-09-05 by the `PB485` implementer, which needed exactly this shape as the over-rejection guard for
its entry-format screen (a format-1 entry whose only clause is a VALUE clause looks like a condition-name
entry to a screen that reads the body before the level) and could only register it PENDING. Re-derived on
this tree for this note.

## Repro

```cobol
       01  W  VALUE "AB".
       ...
           DISPLAY W          *> expected: AB
```

`cobol … --std 2023` → the backend fails: `error CS0103: The name 'W' does not exist in the current context`.
The registered witness is `tests/conformance/2023/pb485_value_only_format1.cob` (PROGRAM-ID `PB485P4`), in the
2023 manifest's PENDING list with `.out` = `AB` and the derivation in its header; enable it with the fix.

## The rule

§13.16.3 SR9 (quoted by the PB485 implementer and `cite.py --check`ed there): "The PICTURE clause may be omitted
for an elementary item when an alphanumeric, boolean, or national literal that is not a zero-length literal is
specified in the data-item format of the VALUE clause. A PICTURE clause is implied as follows: a) if the literal
is alphanumeric, 'PICTURE X(length)' …" — with b) and c) for boolean (`PICTURE 1(length)`) and national
(`PICTURE N(length)`). Re-run `python scripts/spec/cite.py --check 13.16.3 "The PICTURE clause may be omitted
for an elementary item when an alphanumeric, boolean, or national literal that is not a zero-length literal is
specified in the data-item format of the VALUE clause"` as the first step of the fix.

## The mechanism

`src/Cobol.Net.Compiler/Binding/DataBinder.cs` `BindEntry`: an entry with no PICTURE clause, no USAGE clause and
no subordinate entries produces no `DataItem` (it is neither an elementary item with a picture nor a group), yet
the entry's name is still known to the procedure division, so references bind to a name the emitter never
declared. The fix is at the one place the entry's PICTURE is derived: when SR9's precondition holds (elementary,
no PICTURE, data-item-format VALUE with a non-zero-length alphanumeric / boolean / national literal), synthesize
the implied `PicInfo` from the literal's category and length before the rest of `BindEntry` runs, so every
downstream consumer sees an ordinary elementary item. A zero-length literal or a numeric literal without a
PICTURE stays what it is today (SR9 does not apply; check what diagnostic that case gets and that it is a named
rejection, not a backend crash — the same shape one clause over).

## Goldens

Positive per literal category (alphanumeric, boolean `B"…"`, national `N"…"`) with `FUNCTION LENGTH` read-outs
deriving the implied picture's length from the literal; the existing PENDING witness moved to `enabled`; a
negative for the zero-length literal and for a numeric literal with no PICTURE, each with the rule that rejects
it cited.

## Landed — by `f518da9e4` (PB504 + PB831 + PB828), confirmed 2026-09-20

⛔ **This note did NOT reproduce when the wave-42 group-E implementer re-probed it.** The repro was rerun first,
against a `dotnet build CobolSharp.sln -c Debug` of the implementer's own worktree (cut from main `545577479`):

```
01  W  VALUE "AB".     DISPLAY "W=[" W "] L=" FUNCTION LENGTH(W)
  →  W=[AB] L=2        (exit 0)
```

`git log -S` on `src/Cobol.Net.Compiler/Binding/DataBinder.ImpliedPicture.cs` names the landing:
`f518da9e4` "PB504+PB831+PB828: the implied PICTURE is synthesized, once" — the `SynthesizeImpliedPictures` bind
pass, between `ExpandTypes` and `UsageInheritancePass`, which is exactly the placement this note's "The
mechanism" section argued for (the entry becomes indistinguishable from one whose source wrote the PICTURE).
`kb/Work/PB504` is `landed` and closed `SR-13.16.3-9`, `SR-13.16.3-8` and `SR-13.15.3-14`.

**The WHOLE of §13.16.3 SR9 was re-probed, not just the repro line** (CLAUDE.md rule 3 — a note is not closed on
one arm):

| probe | expected from SR9 | measured |
| --- | --- | --- |
| `01 W VALUE "AB".` | implied `PICTURE X(2)` | `W=[AB] L=2` |
| `01 BV VALUE B"101".` | implied `PICTURE 1(3)` | `BV=3` |
| `01 NV VALUE N"AB".` | implied `PICTURE N(2)` | `NV=2` |
| `01 Z VALUE "".` | SR9 does not apply (zero-length) → SR8 rejects | COBOLNET0881 naming §13.16.3 SR8 and SR9's grant |
| `01 M VALUE 5.` | SR9 does not apply (numeric literal) → SR8 rejects | COBOLNET0881, same named rejection |

Both negatives are NAMED COBOL rejections, not backend crashes — the second half of this note's "The mechanism"
paragraph. The witness the note registered, `tests/conformance/2023/pb485_value_only_format1.cob`, is enabled.
Closed as already-fixed; no code change was made under this id.
