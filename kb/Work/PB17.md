---
title: "PB17 — LANDED 2026-08-04 — a function-identifier as a SUBSCRIPT or a REF-MOD position compiles clean and throws"
id: PB17
kind: defect
status: landed
severity: MAJOR
area: references
wrong_answer: false
crashes: true
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: [8.4.2.3.2, 8.4.2.3.4, 8.4.3.3.3, 8.4.3.1.2, 8.8.1.1, 15.4]
tags: [cobolsharp, work, defect]
---

# PB17 — LANDED 2026-08-04 — a function-identifier as a SUBSCRIPT or a REF-MOD position compiles clean and throws

> ✅ **LANDED.** Implemented from D18 in full, per the owner ruling — no stage-loud stopgap. All five shapes
> MEASURED green: `SUB=33` · `RM=[CD]` · `NESTED=33` · `UDFSUB=33` · `PEREVAL=3`. Goldens
> `tests/conformance/2023/pb17_function_subscript.cob` (positive, all five) and the two negative fixtures
> `pb17-function-subscript-varying-by` / `-evaluate-subject` (COBOLNET1509), all manifest-registered.
>
> ⚠ **TWO THINGS THE EXECUTION ORDER BELOW GOT WRONG, both corrected in D18 in the landing change set:**
> 1. **Step 3's `Scale: 0` temp was a latent WRONG-ANSWER bug.** A scale-0 §15.4 temporary truncates on the way
>    in, so `W-E(FUNCTION SQRT(2))` would have silently indexed occurrence 1 instead of setting
>    EC-BOUND-SUBSCRIPT per GR1b — legal source turned into a wrong answer by the temp's own description, inside
>    the very fix written to stop legal source from throwing. The temp is `PIC S9(21)V9(9)` (30 digits, the
>    `Int128` wide tier). This is what [[PB41]] was found by asking about, and why the two landed together.
> 2. **Step 4's "prefer generalizing the UDF pending list" understated the requirement — ONE list is a
>    CORRECTNESS constraint, not a tidiness preference.** The two pre-op kinds INTERLEAVE:
>    `FUNCTION F(W-E(FUNCTION INTEGER(3)))` registers the subscript temp while F's arguments bind and F's
>    activation after, so only a single list in registration order puts the store before the activation that
>    consumes it. The list moved to `DataBinder.PendingPreOps` (the one binder both clients can reach) and
>    `BoundUdfEvaluated.Activations` widened from `BoundCallProgram` to `BoundStatement`.
>
> ⚠ **THE INHERITED GAP, stated plainly:** the three windows `UdfStagePerEvaluationResidue` already staged loud
> for user functions (PERFORM VARYING BY, a VARYING AFTER-level FROM, an EVALUATE selection subject) now stage
> loud for function subscripts too — COBOLNET1509, both fixtures above. That is the precedent D18 required be
> followed "including its loud residue", and it rejects legal source in those three positions. It is a
> PRE-EXISTING GAP surface newly reachable by a second spelling, not a new decision — but it is debt, and the
> honest reading is that PB17 is complete for every window the machinery reaches and inherits the residue for
> three it does not. See [[feedback_no_deferral_default]].

⚙ **RE-MEASURED 2026-08-04 AT `b21b81d4`. Both shapes reproduce EXACTLY as filed** — which is worth saying,
because the last three entries measured did not:

```
MOVE W-E(FUNCTION INTEGER(3)) TO W-R      -> compiles clean; at RUN TIME:
    NotImplementedCobolFeatureException: reference 'W-E(FUNCTION INTEGER(3))'
MOVE W-A(FUNCTION INTEGER(3):2) TO W-R    -> same, 'W-A(FUNCTION INTEGER(3):2)'
```

The PB7/DA7 **wrong-stage** family: legal source that compiles clean and throws.

## ⛔ ONE OF THE THREE INHERITED CITATIONS WAS WRONG

`cite.py --check` rejected **§8.4.2.3.3**: the clause exists (Subscripts / Syntax rules) and says nothing about
arithmetic expressions — its eight rules are about qualification, OCCURS, subscript COUNT and ALL. The third
wrong inherited citation in one session (after §13.18.27 for GROUP-USAGE and §15.15.3's own chain).

**The corrected chain, every link `--check`ed:**

| # | clause | what it gives |
|---|---|---|
| 1 | **§8.4.3.1.2** Format 1 | a function-identifier IS an identifier |
| 2 | **§15.4** | "The evaluation of a function produces a returned value in a temporary elementary data item" |
| 3 | **§15.2** items 5–6 | integer and numeric functions are "of the class and category numeric" |
| 4 | **§8.8.1.1** | "An arithmetic expression may be an identifier referencing a numeric data item" ⇒ 1+2+3 make a numeric function-identifier a legal arithmetic expression |
| 5 | **§8.4.2.3.2** + **§8.4.2.3.4 GR1b** | a subscript may be `arithmetic-expression-1`; GR1b evaluates it |
| 6 | **§8.4.3.3.3 SR4** | "Leftmost-position and length shall be arithmetic expressions" |

## ⚖ AND THE PREMISE THAT COULD HAVE KILLED IT — CHECKED, IT HOLDS

§8.4.3.2.3 **SR11** bars a numeric function "where an integer operand is required" and **SR12** bars an integer
function "where an unsigned integer is required". If a subscript were such a position, the finding would
evaporate and the compiler's refusal would be right. **It is not.** §8.4.2.3.2's format says
`arithmetic-expression-1`, and **§8.4.2.3.4 GR1b sets EC-BOUND-SUBSCRIPT when the expression does not evaluate
to an integer** — a runtime exception condition that would be unnecessary if the position required an integer
syntactically. SR14 confirms it from the other side: it has to impose "only numeric literals or arithmetic
expressions whose result is a positive integer" as a SPECIAL restriction for a BY REFERENCE bit item, which
would be redundant if subscripts were already so constrained. **Both shapes are legal source.**

## ⛔ THE ENTRY'S STATED FIX IS WRONG, AND SO WAS MY FIRST REPLACEMENT FOR IT

The entry says "the durable fix is the segment renderer". Reading `ReferenceResolver.RenderSegment` confirms the
ROOT CAUSE — its `default:` arm literally lists `FUNCTION` among the token types it cannot render — but the fix
does not belong there. `RenderSegment` is a **hand-rolled expression compiler over flat SUBSCRIPT-mode tokens
that emits C# text at BIND time**; adding a FUNCTION arm means hand-writing intrinsic rendering into a
StringBuilder, i.e. a third expression compiler beside `ExpressionBinder` and `IntrinsicRenderer`.

⚠ **And the obvious re-architecture is BLOCKED BY AN OWNER RULING, not by taste.** `RefModPlace.Start`/`Length`
are documented as "the D10 TRANSITIONAL carrier … they become `BoundExpr` when PHASE 15 removes the SUBSCRIPT
lexer mode", deliberately the same shape as `RefModSpec` "so PHASE 15 migrates both in one move rather than
leaving a second, differently-shaped ref-mod behind". **D10 is relocated to PHASE 15 §"CUT 2.5"**, blocked
because the frozen legacy compiler still shares `SUB_*`/`SubscriptEntryContext` until Cut 2 deletes it. Pulling
the `BoundExpr` migration forward would create precisely the second ref-mod shape the design forbids.

## ✅ THE DESIGN — CANONICAL IN `COBOLNET_DATA_MODEL_DESIGN.md` **D18**

⛔ **IMPLEMENT FROM D18, NOT FROM THIS NOTE** (CLAUDE.md rule 2). `COBOLNET_DATA_MODEL_DESIGN.md` owns
`Place`/`ReferenceResolver` and now carries the decision in full, including the two rejected alternatives and
their reasons. What follows is the summary; D18 is the authority, and the "Hard problems" entry on the SUBSCRIPT
token stream now cross-references it.

### The standard already says "temporary data item" — use the ONE temp mechanism

**§15.4: "The evaluation of a function produces a returned value in a temporary elementary data item."** So
materialize it:

1. Bind the function-identifier through the ONE intrinsic pipeline → a `BoundIntrinsicCall`.
2. Synthesize a temp through **`DataBinder.CreateCompilerTemp`** — already documented as "THE ONE synthesized-
   compiler-temp constructor (property-reference temps, user-function result temps)".
3. Register a pending op storing the call into the temp before the carrying statement, drained into a
   `BoundSequence` by `StatementBinder` — **the exact pattern `ReferenceResolver.OoTryBindPropertyReference`
   already uses** for §8.4.3.9 object-property references.
4. The subscript segment then renders as an ordinary data-name — which `ResolveSubscriptName` already handles.

**Why this is the durable shape and not a workaround:** no new carrier, no second ref-mod, no grammar change, no
D10 conflict (when D10 lands, the temp path is deleted with the rest of the string carrier), and it reuses a
mechanism `ReferenceResolver` is ALREADY a client of. It also makes the NEXT case automatic — the same route
serves any segment form `RenderSegment` cannot render, not just `FUNCTION`.

⚠ **The sweep this opens:** `RenderSegment`'s `default:` arm also rejects `SUB_STRINGLIT`, `SUB_DECIMALLIT` and
`SUB_ALL`. Each needs its own adjudication (ALL is legal only in the §8.4.2.3.3 r6 positions; a decimal literal
subscript is legal source that GR1b then faults at run time via EC-BOUND-SUBSCRIPT). Do NOT route them blindly —
that is the PB1 lesson about enforcing an unaudited table.

## ⚖ OWNER DECISION 2026-08-04 — IMPLEMENT IN FULL; NO STAGE-LOUD STOPGAP

Asked whether to (a) implement in full next session, (b) convert the run-time crash into a bind-time diagnostic
now, or (c) both. **Ruling: (a) — the full implementation, executed fresh with a whole session's context.**
A stage-loud stopgap was explicitly NOT taken: it would reject legal source (a GAP, debt) and would mean two
changes to the same code. **So the run-time crash stands until the real fix lands** — that is a deliberate,
recorded choice, not an oversight.

## ▶ EXECUTION ORDER FOR THE NEXT SESSION

1. **Grammar** (pre-authorized, rule 7): add `subscriptExpressionFragment : arithmeticExpression EOF ;` to
   `Core/CobolExpressions.g4`, beside `functionArgListFragment` — an isolated entry rule referenced by nothing in
   `compilationUnit`, so ZERO blast radius on the main parse (the precedent's own words). Frontend class
   `SubscriptExpressionFragment` mirroring `DirectiveExpressionFragment` (DEFAULT lexer mode — a subscript is not
   a function-argument region, so do NOT prime `PrimeFunctionArgs`).
2. **Text capture** in `ReferenceResolver`: the idiom is `token.InputStream.GetText(new Interval(start, stop))`,
   exactly as `IntrinsicBinder.ReparseArgs` does.
3. **Temp**: `data.CreateCompilerTemp(model, …)` clones `model.Pic`, so pass a synthetic model
   `new DataItem { Level = 1, Pic = new PicInfo(PicCategory.Numeric, Usage.Display, …, Scale: 0, …) }`. §15.4
   leaves the temp's description to the implementor.
4. **Pending pre-op**: register on `DataBinder` (ReferenceResolver cannot reach StatementBinder — the dependency
   is `StatementBinder(DataBinder, ReferenceResolver)`, one-way). Drain at the `BindStatement` chokepoint, which
   ALREADY marks-on-entry and drains-its-own-suffix for two clients (`Udf.PendingCount`,
   `data.OoPendingPropertyOps`). ⭐ **Prefer generalizing the UDF pending list over adding a third** — a
   function-identifier is never a receiving operand (§8.4.3.2.3 SR1), so both are unconditional PRE-ops and the
   hoist discipline is identical.
5. **Bind** each drained fragment through `IntrinsicBinder.BindIntrinsicCore` — the documented ONE entry shared by
   the FUNCTION-keyword form, the keyword-omitted re-parse and every nested recursion (so a USER-defined function
   in a subscript falls out of the same change).
6. ⛔ **THE CORRECTNESS TRAP — do not hoist out of a repeatedly-evaluated condition.** §8.8.4.13 r2 evaluates a
   function "if and when the conditions containing them are evaluated". `UdfBinder` already solves this
   (`UdfAttachPerEvaluation` / `BoundUdfEvaluated`) and STAGES LOUD the windows it does not reach
   (`UdfStagePerEvaluationResidue`, COBOLNET1509). **Follow that precedent exactly, including its loud residue** —
   a subscript inside a PERFORM UNTIL / SEARCH WHEN / EVALUATE object is the same hazard.
7. **Goldens**: both shapes positive (subscript and ref-mod position), a nested case, a user-defined function in a
   subscript, and the per-evaluation residue as a negative fixture.
