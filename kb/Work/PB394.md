---
title: "PB394 — the sending operand / selection subject is re-rendered ONCE PER RECEIVER (and once per relational use), so a subscripted, reference-modified or function source and a value-varying EVALUATE subject give a WRONG ANSWER at every edition"
id: PB394
kind: defect
status: open
severity: MAJOR
area: codegen/operands
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.13.4", "14.9.25.4"]
inventory_rows: ["GR-14.9.13.4-3", "GR-14.9.13.4-4", "GR-14.9.25.4-1"]
tags: [cobolsharp, work, defect]
---

# PB394 — one rule ("evaluate the sender once"), two verbs, no hoist

## THE MECHANISM

Both halves are the same missing structure: **the sending value is never materialized**, so the emitter re-runs
the source EXPRESSION at every place it needs the value.

* **MOVE.** `MoveEmitter.Emit` is `for (int i = 0; i < m.Targets.Count; i++) { … }` and **every arm inside the
  loop re-renders `m.Source`** — `MoveKind.Convert` calls `ConvertSource(m.Source, …)`, and `RefModSlice`,
  `Group` and `GroupToElementary` each call `OperandText.AsString(m.Source, …)`. `MoveBinder.Bind` builds
  `BoundMove(source, resolved)` with one source node, N targets and no hoist. So with two receivers the
  subscript, reference modifier or function-identifier of the SENDER is evaluated a second time **after the
  first store has landed**. The RECEIVER side is already correct — item identification happens immediately
  before each store — which is exactly what makes this a **one-sided hoist**: the receiver arm was written to
  the rule and the sender arm was not.
* **EVALUATE.** `EvaluateBinder.BindWhenItem` calls `BindValueOperand(subjOp)` at `:210` — inside the **per-PAIR**
  method, i.e. once per subject/object pair, not once per statement — and `EvaluateEmitter` renders each pair's
  composed match into a chained `if/else if`, so the subject expression is emitted **once per RELATIONAL USE**,
  which is TWICE for a THRU arm (`left >= lo && left <= hi`, `:215-231`).

**The compiler already knows this is wrong.** `BindWhenItem` calls `UdfStagePerEvaluationResidue` at `:211`
quoting GR3 verbatim ("evaluated once per statement … this lowering re-binds subjects per WHEN"). But that stage
is narrowed to `UdfBinder.Pending` — **USER-defined function subjects only** — so every intrinsic-function
subject and every other value-varying subject passes silently. That is the two-arm shape again: the arm that
DIAGNOSES the residue was written; the arm that would make the residue unnecessary was not
(`feedback_two_arm_dispatch`).

## THE RULES, QUOTED, EACH `--check`ED ON THIS TREE

* `--check 14.9.25.4 "the reference modifier, subscript, or function-identifier is evaluated only once,
  immediately before data is moved to the first of the receiving operands"` → **`OK §14.9.25.4 1)`**, and the
  rule writes the required shape out as an equivalence — `MOVE a (b) TO b, c (b)` ≡ `MOVE a(b) TO temp / MOVE
  temp TO b / MOVE temp TO c(b)`, with `--check 14.9.25.4 "where 'temp' is an intermediate result item provided
  by the implementor"` → **`OK §14.9.25.4 1)`**.
* `--check 14.9.25.4 "Item identification for identifier-2 is performed immediately before the data is moved to
  the respective data item"` → **`OK §14.9.25.4 1)`** — the half that IS implemented, and the reason the fix
  must hoist the SENDER only.
* `--check 14.9.13.4 "each selection subject is evaluated and assigned a value, a range of values, or a truth
  value"` → **`OK §14.9.13.4 3)`** — the stem reads *"At the beginning of the execution of the EVALUATE
  statement…"*. After that point the subject IS a value; GR4a pairs each selection object against that assigned
  value, never against a fresh evaluation of the source expression.
* `--check 14.9.13.4 "execution continues with the first imperative-statement-1 following the selected WHEN
  phrase"` → **`OK §14.9.13.4 5) a)`**.

## OBSERVED ON HEAD (`89044aa0`) — every repro re-run, all still wrong

**MOVE, `--std 2023` (identical at 85/2002/2014):**

```
$ cobol.exe gr1_temp2.cob --run       # A(1..5)=1,2,5,4,9; B=3; MOVE A(B) TO B, C(B)
B=5
C=00009        <-- C(5) took A(5)=9; the temp equivalence requires C(5)=5
$ cobol.exe gr1_function_once.cob --run   # MOVE FUNCTION RANDOM TO R1, R2
R1=0000000087125568 / R2=0000000066093862 / DIFFERENT (GR1 VIOLATED)
$ cobol.exe gr1_recv_ident.cob --run  # control: MOVE 3 TO K, T(K) with K=1
K=3 T=00300    <-- the RECEIVER subscript correctly takes the NEW K
```

**EVALUATE, `--std 2023`** — the seeded sequence after `FUNCTION RANDOM(1)` is measured independently by
`p18-random-seq.cob`: `R1=.110743 R2=.467010 R3=.771604 R4=.657518`.

```
$ cobol.exe p19-random-match.cob --run
  # EVALUATE FUNCTION RANDOM / WHEN 0 THRU 0.4 → LOW / WHEN 0.4 THRU 1 → HIGH / WHEN OTHER
HIGH           <-- expected LOW: the single subject value is R1 = .110743
```

Tracing that against the measured sequence: arm 1 renders the subject **twice**, so `subj >= 0` sees
R1 = .110743 (true) and `subj <= 0.4` sees R2 = .467010 (FALSE) — **the two halves of ONE range test saw two
DIFFERENT subject values** — so arm 1 fails; arm 2 renders it twice more, `subj >= 0.4` sees R3 = .771604 and
`subj <= 1` sees R4 = .657518, both true, so HIGH prints. Four evaluations for one statement, and an answer
**no single subject value can produce**.

The COUNT is confirmed independently, without arithmetic on the trace:

```
$ cobol.exe p14-random-once.cob --run   # subject is FUNCTION RANDOM, three value WHENs
OTHER / NEXT=0657518     <-- next random is R4 ⇒ the statement consumed THREE
$ cobol.exe p15-random-base.cob --run   # identical WHENs, subject is a DATA ITEM holding one value
OTHER / NEXT=0467010     <-- next random is R2 ⇒ the statement consumed ONE
```

## THE CODE SITES, AND THE SHAPE OF THE REPAIR

* `src/Cobol.Net.Compiler/CodeGen/Verbs/MoveEmitter.cs#Emit` — the per-target `for` loop; every arm re-renders
  `m.Source`. `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/MoveBinder.cs#Bind` — `BoundMove(source,
  resolved)` with no hoist.
* `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/EvaluateBinder.cs:210` (`BindValueOperand(subjOp)` inside
  `BindWhenItem`), `:211-213` (the narrowed stage that documents the defect), `:215-231` (the range arm that
  renders `left` twice); `src/Cobol.Net.Compiler/CodeGen/Verbs/EvaluateEmitter.cs#Emit`;
  `src/Cobol.Net.Compiler/Binding/Procedure/Verbs/UdfBinder.cs#UdfStagePerEvaluationResidue`.

**Repair (CLAUDE.md rule 5 — a materialized value, not a widened stage):**

1. **MOVE**: hoist the sending VALUE into one C# local before the target loop and store that local into every
   receiver. It must capture the **value, not the place** — the group and ref-mod arms pass the raw source text
   to different renderers, so the one hoist has to cover all four `MoveKind`s. Hoisting unconditionally (not
   only when `Targets.Count > 1`) keeps the shape uniform and is what the rule's own `temp` equivalence
   describes.
2. **EVALUATE**: bind the subject ONCE, above the WHEN loop, into a temp every arm and every half of every range
   reads. That is also **the only shape in which GR3's "assigned a value, a range of values, or a truth value"
   is representable at all**. Binding per pair is precisely what forces the residue stage to exist — so the fix
   DELETES `UdfStagePerEvaluationResidue`'s reason to be, rather than widening it. ⚠ **Widening the stage would
   turn a wrong answer into a rejection of legal source**; do not do that as an interim step.
3. Add a drift test that a value-varying sender/subject is evaluated exactly once per statement — the
   `FUNCTION RANDOM` count technique above (measure the NEXT value) is a stable, seedable way to assert it
   without depending on any particular random value.

## THE SIBLING SWEEP THE FIXER MUST DO

* **Every statement with ONE sender and MANY receivers** shares the shape: `SET`, `ADD … TO`, `INITIALIZE …
  REPLACING`, `STRING … INTO`, `UNSTRING … INTO`, `SET … TO ADDRESS OF`. Ask at each whether the sender is
  rendered inside the receiver loop.
* **Every `if/else if` chain built from one subject**: EVALUATE's is the measured one; the abbreviated-relation
  lowering and SEARCH's WHEN chain are the same question (SEARCH's per-pass re-evaluation is REQUIRED by
  §14.9.37.4 GR1, so it is correct there — the point is to distinguish the two deliberately, not by accident).
* **A GREEN LEGACY TEST HIDES THE MOVE HALF**: `tests/CobolSharp.Tests.Integration/DataTests.cs:1088`
  `MoveSourceSubscriptEvaluatedOnce` belongs to the LEGACY `CobolSharp.Compiler` integration suite and never
  compiles `src/Cobol.Net.*`. It cannot close this row, and its green status is what made the greenfield hole
  invisible (`feedback_green_gates_arent_evidence`). The replacement must be a greenfield/conformance case.

## Rows

* **`GR-14.9.25.4-1`** (DIVERGES) — four of the rule's five halves CONFORM and were measured (receivers stored in
  order; receiver item identification per store; a zero-length identifier-2 left unchanged; a zero-length
  identifier-1 treated as a zero-length literal). The fifth — "evaluated only once, immediately before … the
  first of the receiving operands" — is a live silent wrong answer at every edition, with three independent
  repros (subscripted source, function-identifier source, and, as corroboration only, a reference-modified
  source, since §14.6.10 makes an overlapping ref-mod result undefined). One GR1 sentence remains UNVERIFIED and
  is recorded as such, not claimed: "The length of the data item referenced by identifier-1 is evaluated only
  once" with an ODO sender whose DEPENDING item is itself a receiver — same missing hoist, no discriminating
  legal case constructed yet.
* **`GR-14.9.13.4-3`** (DIVERGES) — sub-rules a/b/c/e/f are right (a numeric data item classifies as
  identifier-1; a class-test or level-88 subject is condition-1); d (a boolean-expression subject) is staged and
  CRASHES against a TRUE/FALSE object. The divergence is the stem: one evaluation per statement, measured as
  three (and four with THRU arms).
* **`GR-14.9.13.4-L2.5`** (PARTIAL) — the range FORM is right and survived 23 measured axes (inclusive at both
  bounds, inverted → empty, NOT inside/outside, alphanumeric/national/identifier/arithmetic bounds, §8.8.4.2.7
  space padding, a numeric-edited subject compared alphanumerically, and `WHEN NOT "Z" THRU "A"` under
  `>>TURN EC-RANGE-INVALID CHECKING ON`). Two reservations keep it PARTIAL: this note's re-render (a range arm
  evaluates the subject TWICE — charged to GR3, where the fix belongs), and a **SIGNED literal bound with a
  non-numeric subject** (`01 WS-A PIC X(2) VALUE "-3".` / `EVALUATE WS-A WHEN -5 THRU -1`), conforming source
  which produces no truth value at all but aborts with *"computed expression in a string context"* — the same
  `BindValueOperand`/SolePrimary sign misclassification as GR1's, inherited by the range bounds. Its cited
  test-ref `conformance:2023/ec_range_invalid` exercises only the inverted-alphanumeric empty branch with
  checking ON, so it pins neither the inclusive form nor the NOT form and does not close the row.

> ⚠ **ROW IDS RE-KEYED 2026-09-06 (kb/Work PB698).** The transcription repair returned the outdented sub-items of these rule blocks to their printed level, so the rows this note names moved: `GR-14.9.13.4-L2.5` -> `GR-14.9.13.4-4`. `inventory_rows` above is live; the ids in the prose are as they stood when it was written, and the adjudications they carry were migrated by the `pb698-rekey` and `pb698-fold` batches.
