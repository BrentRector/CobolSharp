---
title: "PB1177 — ANALYSIS — §14.9.40.4 GR17 ('a USE procedure invoked while a format 1 SORT statement is active does not complete normally → the SORT is terminated') is verified only for a USE invoked by an IMPLICIT USING/GIVING transfer (EmitTransferUse's notNormalLabel); a USE invoked by an EXPLICIT statement inside the input or output procedure has no end-label wiring, and whether GR17 or §14.9.33.4 GR2 a) governs that arm is unsettled"
id: PB1177
kind: analysis
status: open
severity: MINOR
area: codegen/sort
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
cluster: ["PB1139", "PB1036"]
spec_refs: ["14.9.40.4", "14.9.33.4"]
inventory_rows: ["GR-14.9.40.4-17"]
closes_rows: []
tags: [cobolsharp, work, analysis]
---

Filed 2026-09-24 by **REGISTRAR 14c** from lane-3 adjudication batch **d1-14c** (adjudicated on the pinned tree `68bbdb00f`; trains 58/59 checked — nothing that landed since touches this mechanism unless the body says otherwise).

# PB1177 — OPEN

## The rule (every citation re-run by the registrar through `cite.py --check`)

> 17) If a USE procedure invoked while a format 1 SORT statement is active does not complete normally, the SORT statement is terminated.

`python scripts/spec/cite.py --check 14.9.40.4 "…"` → `OK  §14.9.40.4 17)  (General rules)`

## Adjudicator evidence — row `GR-14.9.40.4-17` (PARTIAL)

Rule: 'If a USE procedure invoked while a format 1 SORT statement is active does not complete normally, the SORT statement is terminated.' (cite.py --check 14.9.40.4 OK, GR17). Verified arm: a USE invoked by an IMPLICIT transfer (USING/GIVING OPEN/READ/WRITE/CLOSE) -- EmitTransferUse passes notNormalLabel: tx.EndLabel, so a not-normal completion jumps to the SORT's end label (SortClose still runs); pb367b pins DECL-RAN / AFTER-SORT / SORTED=A. Unverified arm: a USE invoked by an EXPLICIT statement inside the input or output procedure (the SORT is equally 'active'); only the implicit-transfer sites carry the end label, and whether GR17 or 14.9.33.4 GR2 a) 1. (RESUME AT NEXT STATEMENT -> after the explicit statement 'unless general rules associated with the applicable statement specify otherwise') governs that arm is not settled here -- hence PARTIAL, not DIVERGES.

**Code site.** src/Cobol.Net.Compiler/CodeGen/Verbs/SortEmitter.cs#EmitTransferUse; src/Cobol.Net.Compiler/CodeGen/Verbs/SortEmitter.cs#EmitSort

## What the fixer owes

Probe the explicit-statement arm (a READ inside the INPUT PROCEDURE whose USE ends with a GO TO out / an EXIT PROGRAM), derive which rule governs, and either wire the SORT's end label into those USE invocations (SortEmitter.EmitSort) or record the reading with a golden. MERGE's twin (§14.9.24.4) shares the question.

**Widened by PB1036 (w59c, 2026-09-24):** a RELEASE/RETURN inside a SORT input/output procedure now raises
EC-SORT-MERGE-ACTIVE / -RELEASE and runs a declarative; if that declarative RESUMEs, the procedure continues, where
§14.9.40.4 GR17 (`OK §14.9.40.4 17)`) terminates the SORT. Repro `<scratchpad 9db39f61…>\w59c\p\srel.cob` prints two DECL
lines then `R 03 BBB` (expected: the SORT ends at the first DECL). Site: `SequentialIoEmitter.EmitUseHook` wires the
notNormalLabel only for implicit transfers.
