---
title: "PB426 — `CobolNum.FromAlphanumeric` accumulates EVERY digit of the sending operand into an Int128 with no cap and no guard, so §14.9.25.4 GR6 d)3's 31-character rule is not implemented: on legal source a 40-character alphanumeric sender wraps the accumulator and MOVE A40 TO PIC 9(9) silently stores 838277934 where the rule gives 234567890, identically at all four editions"
id: PB426
kind: defect
status: landed
severity: MAJOR
area: runtime/numeric
wrong_answer: true
crashes: false
silent: true
rejects_legal_source: false
under_rejects: false
process_only: false
blocked: false
blocked_by: []
spec_refs: ["14.9.25.4", "14.6.8.2"]
inventory_rows: ["GR-14.9.25.4-6"]
tags: [cobolsharp, work, defect]
---

# PB426 — the 31-position cap that keeps the carrier unreachable was dropped

## The mechanism

`src/Cobol.Net.Runtime/Values/Numeric/CobolNum.cs:651-658`:

```csharp
public static Int128 FromAlphanumeric(string image)
{
    if (string.IsNullOrEmpty(image)) return 0;
    Int128 mag = 0;
    foreach (char c in image)
        if (c is >= '0' and <= '9') mag = mag * 10 + (c - '0');
    return mag;
}
```

Every character position of the sending operand is consumed. There is no 31-position cap and no guard on the
accumulator. The standard caps the SENDING operand at 31 digits — the rightmost 31 character positions for a data
item, the rightmost 31 characters for a literal — precisely so that no implementation carrier can be exceeded;
dropping the cap makes the carrier reachable. A sending operand of 39 or more digit characters wraps the SIGNED
`Int128` to a negative value, and the unsigned receiver then stores that value's magnitude.

Below the wrap the missing cap is INVISIBLE, because both readings truncate high-order digits to the receiver's
digit count — which is why the hole outlived a large test corpus. The call-site comment at
`src/Cobol.Net.Compiler/CodeGen/Emit/NumericRenderer.cs:401` cites GR6 and GR6d3 by name, so the rule was known
and only its SIZE clause was dropped.

## The rule, quoted, with its `--check` lines

    python scripts/spec/cite.py --check 14.9.25.4 "in which case the rightmost 31 character positions are used"
    OK  §14.9.25.4 6) 1.  (General rules)
        a. If the sending operand is a data item, the number of digits is the number of character positions in
           the sending data item unless the number of character positions is greater than 31, in which case the
           rightmost 31 character positions are used.

    python scripts/spec/cite.py --check 14.9.25.4 "only the rightmost 31 characters in the literal are used"
    OK  §14.9.25.4 6) 1.  (General rules)
        c. If the sending operand is an alphanumeric or national literal, the number of digits is the same as
           the number of characters in the literal. If the number of characters exceeds 31, the size of the
           sending operand is 31 digits and only the rightmost 31 characters in the literal are used.

    python scripts/spec/cite.py --check 14.6.8.2 "aligned by decimal point"
    OK  §14.6.8.2 4)  (Fixed-point numeric and fixed-point numeric-edited receiving data items)
        4) If the receiving operand is a fixed-point numeric item, the data is aligned by decimal point and is
           transferred to the receiving digits with zero fill or truncation on either end as required.

⚠ `cite.py` prints the approximate rule path `6) 1.` for both sub-rules; the PRINTED rule number is 6) d) 3) a.
and 6) d) 3) c., which is what the inventory row `GR-14.9.25.4-L2.3` names. The tool's help warns the path is
approximate below two levels of nesting — the returned CLAUSE (§14.9.25.4) and the returned TEXT are what the
check establishes, and both are the ones cited here. ⚠ The b2 finding cited the alignment step as "§14.6.8"
bare; the sentence is in §14.6.8.2 GR4 and is cited that way above.

## Expected vs observed — RE-RUN ON HEAD (89044aa0)

    01 A40 PIC X(40) VALUE "1234567890123456789012345678901234567890".
    01 A35 PIC X(35) VALUE "12345678901234567890123456789012345".
    01 R9 PIC 9(9). 01 R9B PIC 9(9). 01 RED PIC 9(9).
    MOVE A40 TO R9 / MOVE A35 TO R9B / MOVE A40 TO RED

    cobol.exe l23_realistic.cob --std 2023 --run     rc=0
      A40->9(9) =838277934        EXPECTED 234567890
      A35->9(9) =789012345        correct — below the carrier threshold
      A40->ED   =838277934        EXPECTED 234567890

    01 R31 PIC 9(31).   MOVE "1234567890123456789012345678901234567890" TO R31     (sub-rule c, a LITERAL)
    cobol.exe l23_literal31.cob   --std 2023 --run   rc=0   R31=7560297064841152750825838277934
    cobol.exe l23_unsigned_int.cob --std 2023 --run  rc=0   R31=7560297064841152750825838277934
                                                            R18=000000000000000012   RS=012C   (both correct)
      EXPECTED R31=0123456789012345678901234567890

DERIVATION: the rightmost 31 characters of A40 are "0123456789012345678901234567890", i.e. the unsigned integer
123456789012345678901234567890; §14.6.8.2 GR4 then aligns by decimal point into `PIC 9(9)`'s nine digit
positions, truncating high-order, giving 234567890. Into `PIC 9(31)` the sending value is stored as it stands.

THE MECHANISM IS IDENTIFIED EXACTLY, not inferred: the full 40-digit parse taken mod 2**128 and read as a SIGNED
Int128 is -126561577560297064841152750825838277934, whose low-order 31 digits are
7560297064841152750825838277934 and whose low-order 9 are 838277934 — both observed values, to the digit. The
35-character control is correct, which places the threshold at the Int128 carrier rather than at the receiver.

EDITIONS: the b2 adjudication measured the same wrong value at 85, 2002, 2014 and 2023 (probe dir `edsweep`);
re-run on HEAD at 2023 above. Sub-rule (b), the figurative-constant digit count, CONFORMS and was re-measured on
HEAD: `MOVE ALL "7" TO PIC 9(3)V9(2)` stores 77777, spanning the fraction positions
(`l23_figurative_digits.cob --std 85 --run` -> `RA=77777`; at --std 2023 the same statement is correctly refused
COBOLNET0902 under §14.9.25.3 SR5, since SR5's surviving exception names an INTEGER numeric item and this
receiver is not one). So only the two SIZE clauses are missing.

## The code site and the shape of the repair

- `src/Cobol.Net.Runtime/Values/Numeric/CobolNum.cs:651-658` `#FromAlphanumeric` — the unguarded accumulator.
- Reached on the MOVE channel from `src/Cobol.Net.Compiler/CodeGen/Emit/NumericRenderer.cs:401` `#FieldNumCore`
  (the `{ Category: PicCategory.Alphanumeric or PicCategory.National }` arm, whose comment cites GR6/GR6d3) and
  from `src/Cobol.Net.Compiler/CodeGen/Verbs/MoveEmitter.cs:339` (the alphanumeric-sender-into-numeric-edited
  arm).

**FIX IT IN `FromAlphanumeric` ITSELF** — take the rightmost 31 CHARACTER POSITIONS of the image before
accumulating. That is the structural repair rather than the local one: it closes every channel at once and makes
the carrier unreachable BY CONSTRUCTION, which is exactly what the standard's cap is for. Guarding at the two
MOVE call sites would leave the other five channels below wrapping, and a guard that throws would convert a
silent wrong answer into a crash on legal source.
⚠ The cap is on CHARACTER POSITIONS, not on digit characters: §14.6.13.2 lets a non-digit position contribute no
digit (the behaviour `FromAlphanumeric` already implements), so the rightmost-31 window must be taken over the
image and only then scanned for digits. Truncating to 31 DIGITS would give a different, wrong answer for an image
with embedded non-digits. Golden both shapes.

## The sibling sweep the fixer must do

`FromAlphanumeric` is the shared unsigned-integer decode for every alphanumeric-in-a-numeric-context read, and
each inherits the same unguarded accumulator. Only the MOVE channel is adjudicated by this note's row; the fix
must cover them all and each needs a golden at 40 characters:

- the reference-modified numeric read — `NumericRenderer.cs:330`
- the group-image numeric read — `NumericRenderer.cs:365`
- INSPECT TALLYING — `InspectEmitter.cs:121, :125, :133`
- STRING / UNSTRING — `StringEmitter.cs:211, :226`

Then sweep the SHAPE, not just this function: every other place a COBOL size limit stated in the standard was
implemented as "accumulate and hope" rather than as a cap. Grep for `Int128`/`long` accumulators fed by a loop
over a user-controlled image.

## Rows

- `GR-14.9.25.4-L2.3` (DIVERGES) — sub-rule (b) conforms and is measured; sub-rules (a) and (c) diverge, and the
  divergence is a silent wrong answer on ordinary source at every edition. No test references the rule.

*(b2 adjudication, subject move-statement-p2, re-measured on HEAD 89044aa0 2026-09-04.)*

> ⚠ **ROW IDS RE-KEYED 2026-09-06 (kb/Work PB698).** The transcription repair returned the outdented sub-items of these rule blocks to their printed level, so the rows this note names moved: `GR-14.9.25.4-L2.3` -> `GR-14.9.25.4-6`. `inventory_rows` above is live; the ids in the prose are as they stood when it was written, and the adjudications they carry were migrated by the `pb698-rekey` and `pb698-fold` batches.

## Landed — 2026-09-13

**FIXED, and FOLDED WITH kb/Work PB844** (§14.9.25.4 GR6 d) 1's missing EC-DATA-INCOMPATIBLE): measured on this
worktree first, both notes reproduced EXACTLY, and both are the same mechanism — the treatment of an alphanumeric
or national SENDING OPERAND under GR6 d), implemented in the same decode and reached from the same five emit
sites. The repair is numeric design **D25**: one method per rule.

| rule | question | method |
|---|---|---|
| GR6 d) 3 a) / c) | how BIG is the operand — the rightmost 31 character positions | `CobolNum.FromAlphanumeric` |
| GR6 d) 1 closing sentence | is its content VALID — EC-DATA-INCOMPATIBLE | `CobolNum.FromAlphanumericSending` |
| §14.6.13.2 | what the digits decode to, no size rule | `CobolNum.DigitMagnitude` |

`FromAlphanumeric` now windows the image to its rightmost 31 CHARACTER POSITIONS before the digit scan, so the
`Int128` carrier is unreachable by construction (31 digits < 10³¹ < `Int128.MaxValue`) — which is what the
standard's cap is for. Re-run of this note's own repro on the fixed tree:

    A40->9(9)  =234567890     (was 838277934)
    A35->9(9)  =789012345     (unchanged — below the old wrap)
    A40->ED    =234567890     (was 838277934)
    LIT->9(31) =0123456789012345678901234567890   (was 7560297064841152750825838277934)

The window is over CHARACTER POSITIONS, not digit characters, exactly as this note required both shapes to be
goldened: `MOVE <PIC X(34) "12X456789012345678901234567890ABCD"> TO PIC 9(31)` stores
`0000456789012345678901234567890`, where a 31-DIGIT cap would never fire and answer
`0012456789012345678901234567890`.

**The sibling sweep this note asked for, done at the ONE decode** — the reference-modified read, the group-image
read, the current-record read, the numeric-edited arm and UNSTRING INTO (§14.9.48.4 GR11 c) transfers "according
to the rules for the MOVE statement") all render through one new helper, `NumericRenderer.AlnumNum`, instead of
five copies of the same interpolation.

**⚠ ONE CORRECTION TO THIS NOTE'S PLAN.** "Fix it in `FromAlphanumeric` itself" was right for the alphanumeric
operand and WRONG for its other callers: `CobolTable.Occ`, `CobolString.RefModPosition`, INSPECT's re-store of a
replaced numeric image and GR6 d) 3 b)'s figurative replication all decode an image whose size its own data
description already fixes, and GR6 d) 3 asks nothing of them. They take `DigitMagnitude`. ⚠ MEASURED rather than
asserted: pointing those sites at the capped entry changed NO answer on any shape probed (a group-aliased
`PIC S9(31) SIGN TRAILING SEPARATE` leaf does image as 32 characters, but the dropped position is a leading zero
in every reachable case), so the split is rule hygiene and future-proofing, not a second bug fix.

**Row `GR-14.9.25.4-6` re-verdicted DIVERGES → CONFORMS** (batch `pb426-pb844-move-gr6-alphanumeric-sender`, GAP
2501 → 2500). Both divergences the row recorded are closed and re-measured, and its two "unverified edges" are
settled — GR6 b) 1 is expressly undefined by the standard, and GR6 c)'s endianness-only sentence now has a probe
(FLOAT-BINARY-64 HIGH-ORDER-LEFT ↔ HIGH-ORDER-RIGHT round-trips 1.0E+300 unchanged).

**Goldens and tests:** `85/pb426_alnum_sender_31_character_cap`, `2002/pb426_alnum_sender_31_digit_receiver`,
`2002/pb426_alnum_sender_data_incompatible`, `negative/pb844-ec-data-incompatible-turn-below-2002`,
`MoveAlphanumericSenderTests` (18), `CobolNumAlphanumericSenderTests` (11), `SendingRefDriftTests` (4).
No diagnostic codes were needed — the fix raises an existing exception condition.
