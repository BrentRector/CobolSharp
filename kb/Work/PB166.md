---
title: "PB166 — legacy-suite intermittent: CobolSharp.Tests.Integration DataTests.Renames_Through_Synonym failed ONCE in battery #32's parallel Integration leg (no assert message captured), then passed a clean serial re-run and a clean full-suite re-run; unreproduced, cause unknown"
id: PB166
kind: defect
status: open
severity: MINOR
area: testing
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
spec_refs: []
tags: [cobolsharp, work, defect]
---

# PB166

Battery #32 (tree `e79e8e20`, 2026-08-29) ran the legacy guard's parallel Integration leg and
`CobolSharp.Tests.Integration.DataTests.Renames_Through_Synonym` failed once (618 ms, `int_rc=1`); the
xUnit failure DETAIL did not survive into `guard.log`, so the mechanism is unknown. Disposition per the
no-unnamed-flake rule: a clean serial re-run of THAT test (green) and a clean full-suite re-run
(503 passed / 1 pre-existing skip) — both on the same tree, same build. Battery #31's leg was green
(503/504), so the occurrence is new to the #32 run but not reproducible.

The test compiles PROGRAM-ID RENTEST2 through the LEGACY engine (`CompileAndRun`) — the suite retires at
the P15 legacy deletion. Candidate mechanisms if it recurs: a temp-path or assembly-name collision under
test parallelism (the `unique_programid_per_test` failure family), or an environment race in the legacy
CompileAndRun harness. ACTION ON RECURRENCE: capture the assert message (the guard should log the failing
test's output, not just its name — that capture gap is itself worth fixing), then diagnose; do not
re-disposition as flake a second time without a mechanism.
