---
title: "PB1536 — OWNER DECISION — two questions no existing note owns, raised by the PB1522 adjudication waves: (Q1) how is a CONDITIONALLY-required Annex A.1 item whose condition is ABSENT in COBOL.NET disposed of; (Q2) is FMT-5.2 (the notation clause §5.2, which prints no general format) retired as a catalog artifact"
id: PB1536
kind: decision
status: landed
severity: MINOR
area: process/traceability
wrong_answer: false
crashes: false
silent: false
rejects_legal_source: false
under_rejects: false
process_only: true
blocked: false
blocked_by: []
cluster: ["PB1535", "PB1534", "A11", "PB280", "PB284"]
spec_refs: ["A.1", "5.2.1"]
inventory_rows: []
closes_rows: ["DOC-A.1-13", "DOC-A.1-15", "DOC-A.1-30", "DOC-A.1-34", "DOC-A.1-37", "DOC-A.1-88", "DOC-A.1-96", "DOC-A.1-98", "DOC-A.1-142", "DOC-A.1-143", "DOC-A.1-149", "DOC-A.1-152", "DOC-A.1-200"]
closes_rows_reason: "the thirteen rows are Q1's (train 62); Q2's FMT-5.2 is not in closes_rows because it was RETIRED, not closed - the regenerated catalog no longer yields it, so build_inventory.py removed the row (GAP 1248 -> 1247) and there is no inventory row left for a back-link to name"
tags: [cobolsharp, work, decision]
---

Filed 2026-09-24 22:46 UTC by **REGISTRAR-1** (cloud, branch `claude/adj-registrar-1`, kb/Work PB1522 step 2) from the lane-3 adjudication of the last 208 verdict-less inventory rows (wave 1 `claude/adj-cal-1` + `claude/adj-a1-w1-s1..s4`, wave 2 `claude/adj-w2-*`, all pinned at `0caa7d5`). Every citation below was re-run by the registrar through `python scripts/spec/cite.py --check`.

# PB1536 — OWNER

The screen-selector question the adjudicators also raised (A.1 items hanging off the declined A.4.2 / A.4.3 modules — 3, 4, 11, 27,
28, 41, 45, 83, 91, 172, 199) is ANSWERED by owner decision R42 (2026-09-24) and recorded as DOCUMENTED-NON-SUPPORT on branch
`claude/adj-registrar-1`; it is not asked here. The MCS item 212 (MESSAGE-TAG) joins PB1198's Question 1 (the declined A.3 4)
facility), GR-4.4-1/-2 join PB468 Question 9, DOC-A.1-161/-162 join PB1099 Question 3, DOC-A.1-47 stays with PB579.

## Q1 — A conditionally-required A.1 item whose condition is absent

**When an Annex A.1 item is "conditionally required" and its condition does not exist in COBOL.NET, is the item withdrawn — and is it
recorded as (i) DOCUMENTED-NON-SUPPORT with no §7 row (R42's ground), (ii) a §7 row stating that the condition is absent plus a derived
selector like `a1-optional-not-provided`, or (iii) CONFORMS on a §7 vacuity row (the DOC-A.1-185 convention)?**

A.1's preamble: "Conditionally required: If the associated feature or language element is implemented then this element is also
required." (`cite.py --check A.1 "Conditionally required: If the associated feature or language element is implemented then this element is also required."` → OK §A.1).
The `a1-optional-not-provided` selector (PB280 Q1) covers requirement = OPTIONAL items with a "Not provided." §7 row only; nothing
reaches a conditionally-required item, and the audit's CANNOT-ARISE set is 84/85/86/173 only.

| Item | Subject | Condition, and why it is absent (adjudicator's probe at `0caa7d5`) | Adjudicated |
|---|---|---|---|
| 149 | RECORD DELIMITER consistency rules with external connectors | the optional feature-name-1; §7 DOC-A.1-150 records feature-name-1 as "Not provided." (written feature-name accepted-inert, COBOLNET1778) | NEEDS-OWNER-DECISION |
| 152 | circumstances other than a locked record returning status 51 | none exist: 51 is produced only by `PhysicalFileTable.LockRecord` and `FileRegistry.ConflictOnLockedRecord` | NEEDS-OWNER-DECISION |
| 200 | stage of LISTING / PAGE / COPY SUPPRESS | conditioned on producing listings; COBOL.NET produces none (§3 directives bullet) | NEEDS-OWNER-DECISION |
| 30 | >>IMP directive rules | no IMP directive is defined (probe `>>IMP SOMETHING` → anonymous `COBOL0001: unexpected '>'`) | NOT-IMPLEMENTED |
| 34 | literal correspondence compile-time ↔ runtime set | the two sets do not differ (both .NET UTF-16) | NOT-IMPLEMENTED |
| 37 | mechanism selecting a runtime encoding | only one encoding is supported | NOT-IMPLEMENTED |
| 142 | program-address ↔ non-COBOL program | no non-COBOL program can be declared (§7 DOC-A.1-19) | NOT-IMPLEMENTED |
| 143 | program-name formation for a non-COBOL program | the same | NOT-IMPLEMENTED |
| 88 | executing a non-COBOL function via a function-prototype-name | the same | PARTIAL |
| 96 | hex alphanumeric literal mapping when characters are not multiples of 4 bits | every character occupies 16 bits and a non-multiple-of-4 literal is refused (probe `X"8"`) | PARTIAL |
| 98 | hex national literal mapping, the same | the same | PARTIAL |

The eight adjudicated rows (30 … 98) are ALSO claimed by PB1535 (their batch is not recorded); this question decides what they
become. Recommendation: (ii) — it mirrors PB280 Q1's settled shape for optional items, keeps §7 the single place a user learns the
implementation's posture, and needs only one selector arm (`requirement: conditional` + a "Condition absent." cell).

## Q2 — FMT-5.2

**Retire FMT-5.2 as an extraction artifact (and fix the FMT scan to require a numbered construct clause), or re-scope it to §5.2.1's
meta-rule?**

The row's catalog text is only the anchor `<a id="section-5-2-1"></a>` (`has_diagram: false`); §5.2 "General formats" is the
description-techniques clause and prints no format. `scripts/spec/extract_rule_catalog.py` (l.558–588) emits an FMT row for ANY
heading titled "General format(s)", so §5.2's own heading manufactured it — the only FMT row at section depth ≤ 1. The one normative
sentence under it, "shall be written in the compilation group in the sequence given in the general format"
(`cite.py --check 5.2.1 "shall be written in the compilation group in the sequence given in the general format"` → OK §5.2.1), is
realized by every other FMT row (open defects PB1508, PB755, PB756, PB1455, PB1139 already sit there). Nearest note PB284 (catalog
denominator — rows LOST; this is a row MANUFACTURED). Recommendation: retire, with a drift assertion that every FMT row has a diagram
or a Format label.

## PB1522 step 3 (doc-rows-1 integrator, 2026-09-24 23:55 UTC, branch `claude/adj-doc-rows-1`)

Q1's population, re-derived by the §7 writers and refuters: DOC-A.1-**13** and **15** (calling / locating a non-COBOL program — conditionally required, condition absent) join it; DOC-A.1-**143** leaves it — A.1 marks item 143 OPTIONAL, so it is not a conditionally-required item at all: its §7 row opens 'Not provided.' and it is recorded DOCUMENTED-NON-SUPPORT by the `a1-optional-not-provided` selector (PB280 Q1).

## Decided 2026-09-24

Q1 — by owner decision R43 (rules whose trigger can only arise through an unsupported capability close as DOCUMENTED-NON-SUPPORT, re-adjudicated if the capability is added). Q2 — retire FMT-5.2: it was generated from a section HEADING, not a normative rule, so it is removed from the catalog/inventory (orchestrator, following the catalog's own definition of a row; no owner question needed).

## Q1 landed 2026-09-24 (wave 59 H, kb/Work R43 item 3)

The recommended shape (ii), exactly: `docs/CONFORMANCE.md` §7 carries a **`Condition absent.`** row for each of the twelve items (13, 15, 30, 34, 37, 88, 96, 98, 142, 149, 152, 200), each stating WHICH condition is absent, WHY, and the code that makes it absent, plus a §7 preamble paragraph naming the class and its contingency (re-adjudicate if the capability is ever added). The verdict is applied by a NEW derived selector, `inventory-schema.json` → `derived-verdicts.a1-condition-absent` (`requirement: conditionally required` AND `determination-prefix: Condition absent`) — a separate selector rather than an arm of `a1-optional-not-provided`, because the two rest on different owner decisions and a row's notes must cite the one that licenses it. The engine's `--self-test` gained the conditional axis cases; `DerivedVerdictDriftTests` gained `NoConditionAbsentA1Row_Diverges` and `TheA1ConditionAbsentSelector_IsStillSharp`.

Witnesses (each observes the ABSENCE, not merely the row): `conformance:2002/w59h_non_cobol_names_not_found` (13, 15, 88, 142 — and the optional 143, whose GAP it also closes: a real non-COBOL module deployed beside every program, `Cobol.Net.Runtime.dll`, is never called, never given an address, and a prototype naming it is EC-FUNCTION-NOT-FOUND); `conformance:negative/w59h-imp-directive-undefined` (30); `conformance:2002/l1c01_native_ordinal_positions_wide` (34, 37); the l1c12 hex goldens + `negative/r03-hex-literal-digit-grouping` (96, 98); the RECORD DELIMITER witnesses (149); the new `unit:LockedRecordStatusProducersDriftTests` — the '51' producer set, seen to fail on a planted third producer — plus `pb669_lock_visibility_plain_connector` (152); `conformance:2002/w59h_listing_stage_inert` + `pb725_directive_facility` (200). Batch `scratchpad/w59h/batch-6-condition-absent.json`: 13 rows, GAP 1454 → 1441. `audit_annex_a1.py --check`: no findings, REMAINING 57 → 46.

STILL OPEN: **Q2** (retire FMT-5.2 — a catalog-extractor change plus the drift assertion 'every FMT row has a diagram or a Format label'); FMT-5.2 stays in `inventory_rows`. The remaining conditionally-required items without a determination (36, 38, 46, 116, 167) are NOT this class yet — their owners (PB547, PB1253, PB1149) dispute whether the condition is absent.

## Q2 landed 2026-09-25 (wave 60 B)

**The mechanism.** `scripts/spec/extract_rule_catalog.py`'s FMT scan emitted a row for ANY numbered heading titled "General format(s)"; §5.2's own heading ("### 5.2 General formats", body = only the anchor of §5.2.1) manufactured FMT-5.2. **The fix is structural, not a list:** a candidate yields a row only when its body carries a diagram or the numbered-and-named "Format N" labels (`cite.py --check 5.2.1 "When more than one arrangement exists for a specific language construct, the general format is separated into multiple formats that are numbered and named."` → OK §5.2.1). A candidate with neither is either a PARENT clause (its next numbered heading is its own sub-clause — §5.2, skipped and printed) or a LEAF whose diagram was lost in transcription (still a row, and a loud `--check` failure). The design rule is written into `docs/rearchitecture/DESIGN-spec-conformance-review.md` §3 Phase A.

**Two siblings the sweep found, both fixed here.** (1) `has_diagram` keyed on ```` ``` ```` / `$$` only, so it was `false` on ALL 322 FMT rows once the transcription moved diagrams to `<pre>` — now `FMT_DIAGRAM` (`<pre`, a fence, also inside a `>` blockquote as §7.3.23.2 still is, or `$$`): 321/321 true. (2) The new bar caught a second undrawn row: §15.78.2 REVERSE was transcribed as bare text (no `<pre>`, no `<u>`); printed folio 908 (PDF page 938) re-rendered — FUNCTION and REVERSE underlined — and restored. FMT-15.78.2's rule-digest changed with the markup, so its CONFORMS verdict was RESTATED through batch `scratchpad/w60b/batch-1-fmt-15.78.2.json` (notes' transcription-defect paragraph replaced by the repair record).

**Drift assertion.** `CatalogCoverageDriftTests.EveryFmtRow_CarriesADiagramOrAFormatLabel` (over the written catalog, independent of the extractor) — seen RED on the pre-fix catalog, naming exactly `FMT-5.2` and `FMT-15.78.2`; the fabricated-input test reports the old FMT-5.2 text and passes a drawn and a labelled row.

**Retirement.** Catalog regenerated (4348 → 4347 rules, FMT 322 → 321), then `build_inventory.py` — the inventory's supported path for a row the catalog no longer yields ("REMOVED: FMT-5.2") — GAP 1248 → 1247. ⚠ A lander merging this re-runs `extract_rule_catalog.py` and `build_inventory.py` on the merged tree before re-applying the batch.
