# PB59 mechanism map — FROZEN EVIDENCE (7-agent Opus fleet, 2026-08-09)
> Line-anchored code sites, current behavior, fix inventories and risks for the seven PB59 fix families,
> mapped read-only while a battery ran. Families 1-4 and 6 LANDED 2026-08-09 (see kb/Work/PB59.md and
> DEVLOG 1268-1273); families 5 and 7 remain OPEN and this map is their plan-of-record. Line numbers are
> as-of the map's tree — RE-SCOUT before editing (persist_anchor_rescout). NOT a worklist: kb/Work/PB59.md
> owns the rows.



# ===== ? =====

## sites
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:34 — CobolIntrinsics.Char(long n, ushort[] weights)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:40 — CobolIntrinsics.Char — the pre-PB3 native-ordinal fallback (RV-15.15.4-1)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:42 — CobolIntrinsics.Char — the lowest-code scan that ignores source order (RV-15.15.4-2)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:122 — CobolIntrinsics.Ord(string s, ushort[] weights) — the landed PB3 arithmetic Char must mirror
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:18 — CobolIntrinsics.Char(long n) — the identity-PCS overload (unchanged; 65536 native positions)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:71 — CobolIntrinsics.CharNational(long n, NationalCollation national) — the shape Char must acquire
- src/Cobol.Net.Runtime/Values/Text/NationalCollation.cs:32 — NationalCollation.PositionCount — the domain bound CHAR's §15.15.3 r2 screen needs an alphanumeric twin of
- src/Cobol.Net.Runtime/Values/Text/NationalCollation.cs:37 — NationalCollation.Weight(char) — the one correct GR7 1.3 reader
- src/Cobol.Net.Runtime/Values/Text/NationalCollation.cs:49 — NationalCollation.CharAt(long) — the representative-by-position inverse
- src/Cobol.Net.Runtime/Values/Text/CobolString.cs:192 — CobolString.Weight(char, ushort[]) — the THIRD implementation of GR7 1.3, still pre-PB3
- src/Cobol.Net.Runtime/Values/Text/CobolString.cs:174 — CobolString.Compare(string?, string?, ushort[]) — every collated comparison flows through Weight
- src/Cobol.Net.Compiler/Binding/CollatingModel.cs:21 — CollatingTable record — no representative-by-position member
- src/Cobol.Net.Compiler/Binding/CollatingModel.cs:45 — NationalCollatingTable record — the twin that DOES carry RepByPos and NextFree
- src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs:377 — AlphabetBind — the 256-entry table + specOrder, the source-order information computed and discarded
- src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs:404 — AlphabetBind — the ALSO arm; next++ is UNCONDITIONAL (the national twin guards it)
- src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs:417 — AlphabetBind — the unspecified-tail fill (stops at 256; next ends as the alphanumeric NextFree)
- src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs:421 — AlphabetBind — GR8/GR9 extremes derived over 0..255 only (the national twin walks to U+FFFF)
- src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs:430 — AlphabetBind — THE ONLY CollatingTable construction site
- src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs:506 — AlphabetBindNationalLiteralPhrase — the repByPos construction to copy
- src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs:540 — AlphabetBindNationalLiteralPhrase — the guarded ALSO advance the alphanumeric arm lacks
- src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs:574 — AlphabetBindNationalLiteralPhrase — NationalCollatingTable construction (rep + nextFree carried)
- src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs:44 — DataBinder.Alphabets — the alphabet dictionary (null = identity, the H5 fast path)
- src/Cobol.Net.Compiler/CodeGen/ProgramEmitter.cs:160 — ProgramEmitter — the __COLLATE / __COLLATE_NAT emission (the alphanumeric side emits ONLY Positions)
- src/Cobol.Net.Compiler/CodeGen/ProgramEmitter.cs:166 — ProgramEmitter — the national emission that already ships rep + nextFree
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:610 — IntrinsicRenderer.Collate(BoundIntrinsicCall) — the trailing weights argument
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:651 — IntrinsicRenderer.RenderStringValue — the CHAR call site
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:245 — IntrinsicRenderer numeric channel — the ORD call site
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:630 — IntrinsicBinder — the H5 collate flag that decides whether Char/Ord get a table at all
- src/Cobol.Net.Compiler/CodeGen/Verbs/SortEmitter.cs:263 — SortEmitter.WeightsExpr / TableWeightsArg — CollatingTable.Positions consumers (inline table emission)
- src/Cobol.Net.Compiler/CodeGen/Verbs/SortEmitter.cs:270 — SortEmitter.TableWeightsArg — the second inline-table emission
- src/Cobol.Net.Compiler/Binding/DataBinder.cs:837 — DataBinder.ResolveKeyCollating — indexed-file key weights read Positions directly
- src/Cobol.Net.Compiler/CodeGen/Roslyn/FigurativeConstants.cs:45 — FigurativeConstants.FillChar — CollatingTable.HighValue/LowValue consumer
- src/Cobol.Net.Compiler/Binding/ConcatFolder.cs:144 — ConcatFolder — the second HighValue/LowValue consumer (bind-time fold)
- src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:153 — IntrinsicCatalog CHAR row (RuntimeMethod "Char", arg kind "i")
- src/CobolSharp.Runtime/Intrinsics/IntrinsicFunctions.cs:199 — LEGACY IntrinsicFunctions.Char(decimal, byte[]?) — differential oracle only, do NOT edit
- tests/conformance/2002/ca26_char_unicode_pcs.cob:14 — CA26 golden — the alphabet that CANNOT distinguish the old fallback from the mirror
- tests/conformance/2002/ca26_char_unicode_pcs.out:1 — CA26 expected output (ORD=00257 — correct under BOTH arithmetics for this alphabet)
- tests/conformance/2023/pb3_ord_collating_tail.cob:17 — PB3 golden — pins ORD's arithmetic under an ALSO collapse; CHAR must round-trip against it
- tests/conformance/2002/ca3_figurative_display_pcs.g.cs:12 — Committed .g.cs golden pinning the exact __COLLATE emission text
- tests/conformance/2023/manifest.json:104 — 2023 manifest — the registration list a new CHAR golden joins

## currentBehavior
CHAR and ORD read the SAME alphanumeric weights table (`__COLLATE`, `ushort[256]`, `Positions[c]` = c's 0-based position), but they implement DIFFERENT rules for it, and the alphanumeric table is structurally poorer than its national twin.

(1) ORD is CORRECT (PB3, DEVLOG 1121). `Ord(string, ushort[])` at CobolIntrinsics.Text.cs:122 does `c < weights.Length ? weights[c]+1 : highest+1+(c-weights.Length)+1`, i.e. it continues the sequence above the tabulated block per §12.3.7.4 GR7 1.3 instead of falling back to the native ordinal. `highest` is recomputed by scanning the array (lines 131-132), so no extra emitted data was needed.

(2) CHAR still carries the fallback PB3 deleted. `Char(long, ushort[])` at line 34: `if (wanted >= weights.Length && wanted <= 0xFFFF) return ((char)wanted).ToString();` — the exact inverse of the branch PB3 removed, and its comment still claims it is "the inverse of ORD's native-ordinal branch", which no longer exists. Under `ALPHABET AL IS "C" ALSO "A" ALSO "B"` (three chars collapsed to position 0, the other 253 Latin-1 units at 1..253, so nextFree = 254): the correct CHAR(255) is position 254 = U+0100 and CHAR(257) is position 256 = U+0102; measured, CHAR(255) returns the §15.3 one-space default (ORD of it = 34) and CHAR(257) returns U+0100 — two positions low (RV-15.15.4-1). CHAR and ORD are therefore not inverses under any ALSO-collapsing alphabet.

(3) CHAR's DOMAIN bound is also wrong (AR-15.15.3-2). §15.15.3 r2 bounds argument-1 by the number of positions in the sequence; that count is nextFree + (0x10000 − 256) = 65534 for the alphabet above. CHAR treats 0..255 as always occupied and 256..65535 as always legal, so it refuses the legal ordinal 255 and accepts the illegal 65536 (aliasing U+FFFF, true ordinal 65534).

(4) CHAR picks the WRONG representative for a shared (ALSO) position (RV-15.15.4-2). §15.15.4 r2 requires "the first character defined for that character position", and §12.3.7.4 GR7 1.6 makes literal-1 that character. CHAR instead scans `weights[]` ascending and returns the LOWEST-CODED member: under the alphabet above, CHAR(1) = 'A' where the spec requires 'C'. The information exists at bind time — `AlphabetBind` builds `specOrder` (DataBinder.Switches.cs:379) precisely so the GR8/GR9 tie rules can consult source order, then throws it away at line 430; the record `CollatingTable(ushort[] Positions, char HighValue, char LowValue)` has nowhere to put it. Its national twin `NationalCollatingTable(Codes, Position

## fixInventory
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:40 — DELETE the `wanted >= weights.Length && wanted <= 0xFFFF` fallback and replace it with Ord's mirror: compute `highest` by scanning `weights` (identical to lines 131-132), let `nextFree = highest + 1`; for `wanted >= nextFree` return code unit `weights.Length + (wanted - nextFree)`. Verify against Ord: Ord's ordinal for that code unit is `highest+1+(c-weights.Length)+1`, so position = nextFree + (c - weights.Length) — the two are exact inverses by construction.
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:34 — ADD the §15.15.3 r2 domain screen CHAR currently lacks: the sequence has `nextFree + (0x10000 - weights.Length)` positions; `wanted` outside 0..count-1 raises EC-ARGUMENT-FUNCTION and returns the §15.3 one-space default. This is the alphanumeric twin of NationalCollation.PositionCount (NationalCollation.cs:32). It fixes AR-15.15.3-2's BOTH halves at once (ordinal 255 must be accepted, 65536 must be refused).
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:42 — DELETE the ascending `weights[]` scan; a specified position must resolve through a representative-by-position array (the alphanumeric twin of `NationalCollation._repByPos`), not by lowest code. §15.15.4 r2 + §12.3.7.4 GR7 1.6.
- src/Cobol.Net.Compiler/Binding/CollatingModel.cs:21 — the alphanumeric `CollatingTable` record gains `ushort[] RepByPos` (indexed by position 0..NextFree-1, the FIRST character defined there in SOURCE order) and `int NextFree`, matching NationalCollatingTable's shape at line 45. Update the XML doc's `<param>` set in the same edit (rule 6).
- src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs:379-419 — AlphabetBind builds the representative list it currently discards. Copy the national builder's mechanics verbatim: a `List<char> repByPos` beside `specOrder`, `if (repByPos.Count == next) repByPos.Add(c);` inside `Assign` (line 515's twin), and the unspecified-tail loop at 418-419 appends each filled code as its own representative so RepByPos covers every position 0..next-1.
- src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs:411 — GUARD the ALSO arm's `next++` the way the national arm guards it at line 547 (`int before = repByPos.Count; … if (repByPos.Count > before) next++;`). Unguarded, an all-duplicate ALSO group advances past an unoccupied position and RepByPos acquires a hole; §12.3.7.4 GR7 1.3 admits no empty position.
- src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs:430 — the one construction site passes the two new members: `new CollatingTable(pos, high, low)` becomes the six-argument form. This is the only compile-break the record change creates.
- src/Cobol.Net.Compiler/CodeGen/ProgramEmitter.cs:161 — the emission must carry RepByPos to the runtime. Two shapes: (a) emit a second field `__COLLATE_REP` beside `__COLLATE`, keeping `ushort[]` everywhere; or (b) introduce an `AlphanumericCollation` runtime class mirroring NationalCollation (Weight/CharAt/PositionCount) and emit `__COLLATE` as that type. (b) is the rule-5 shape — it makes the next case automatic and gives the ONE reader that CobolString.Weight, Char and Ord all call — but it changes the type of every `ushort[]` consumer below, so decide before writing.
- src/Cobol.Net.Runtime/Values/Text/CobolString.cs:192 — SWEEP the third arm. `Weight(char, ushort[])`'s `: c` fallback is the same pre-PB3 rule; give it the identical `nextFree + (c - weights.Length)` continuation (or route it through the new AlphanumericCollation). Without this, CHAR/ORD and every relation condition, THRU test, SORT/MERGE key, indexed key and MAX/MIN string form disagree about the same character. Fix its stale comment ('matching ORD's native-ordinal branch') in the same edit.
- src/Cobol.Net.Compiler/Binding/DataBinder.Switches.cs:421-428 — the GR8/GR9 extremes scan only 0..255 while the national twin (565-572) walks the whole domain. Under the CA26 Unicode alphanumeric repertoire the highest position belongs to the largest UNSPECIFIED code unit, so HIGH-VALUE should be U+FFFF unless specified. Decide explicitly: fix it here, or record why the Latin-1 pin is correct — a silent inconsistency between the two builders is what this family exists to remove.
- Consumers to re-check after the record/emission change (all read Positions or the extremes, none currently read a representative): SortEmitter.cs:263 WeightsExpr, SortEmitter.cs:270 TableWeightsArg, DataBinder.cs:837 ResolveKeyCollating, FigurativeConstants.cs:45 FillChar, ConcatFolder.cs:144-147, NumericRenderer.cs:29, EmitCore.cs:34.
- Goldens: a NEW conformance case under an ALSO-collapsing alphabet is REQUIRED — the existing ca26_char_unicode_pcs.cob cannot fail either way (see risks). It must pin, at minimum, CHAR(1)='C' under `ALPHABET AL IS "C" ALSO "A" ALSO "B"` (r2 representative), ORD(CHAR(255))=255 and ORD(CHAR(257))=257 (the inverse round-trip), CHAR(65534) legal / CHAR(65535) and CHAR(65536) refused (the r2 domain bound), and an `IF` comparing two above-table characters so the comparison arm is proven to agree with ORD. Register the base name in tests/conformance/2023/manifest.json (or 2002/ — CHAR is a 85-era function; pick the edition folder the alphabet syntax needs).
- Regenerate tests/conformance/2002/ca3_figurative_display_pcs.g.cs (and the NIST .g.cs outputs IX210A/IX214A/IX215A) if and only if the __COLLATE emission text changes — they pin it verbatim.
- Docs in the same change set (rule 6): docs/COBOLNET_DESIGN.md's collating-sequence section and docs/COBOLNET_DATA_MODEL_DESIGN.md carry the CollatingTable shape; kb/Work/PB59.md's mechanism-family 2 bullet and the rows AR-15.15.3-2 / RV-15.15.4-1 / RV-15.15.4-2 re-verdict on landing; kb/Work/PB3.md line 53's claim that the fix 'did NOT need the data-structure change this entry predicted' is now half-false — CHAR's representative half DOES need it, and saying so is the honest correction.
- Validate every § mechanically before it enters a comment or golden header: `python scripts/spec/cite.py --check 15.15.3 "..."`, `--check 15.15.4 "..."`, `--check 12.3.7.4 "..."`. The GR7 1.3 text is quoted verbatim in CobolIntrinsics.Text.cs:104-107 and in kb/Work/PB3.md:37-40 — re-derive the clause NUMBER rather than inheriting it.

## risks
- tests/conformance/2002/ca26_char_unicode_pcs.cob PASSES UNDER BOTH ARITHMETICS and is therefore not evidence. Its alphabet is a 26-character permutation with NO ALSO collapse, so all 256 Latin-1 positions stay occupied: highest = 255, nextFree = 256 = weights.Length, and `wanted >= weights.Length ⇒ code = wanted` coincides exactly with `code = weights.Length + (wanted - nextFree)`. Its expected output (ORD=00257 / P-GT-R) is unchanged by the fix. A green run of this golden must not be reported as confirmation — the ALSO axis is the one it holds fixed.
- tests/conformance/2023/pb3_ord_collating_tail.cob pins ORD under `ALPHABET AL IS "A" ALSO "B"` (expected 1,1,67,255,256,257). Any change to the shared arithmetic — including introducing an AlphanumericCollation type — must leave this byte-identical; it is the regression net for the half already landed.
- tests/conformance/2002/ca3_figurative_display_pcs.g.cs:12 is a committed generated-source golden holding the literal `private static readonly ushort[] __COLLATE = { 0, 1, … 255 };`. Changing the emitted field's type or adding a sibling field breaks it, plus tests/nist/output/IX210A.g.cs, IX214A.g.cs, IX215A.g.cs which also contain __COLLATE. Byte-identity of generated source is an explicit project constraint (see FigurativeConstants.cs:63's BYTE-IDENTITY NOTE).
- CollatingTable is a POSITIONAL record with exactly one construction site (DataBinder.Switches.cs:430), so adding members compiles cleanly everywhere else — which means the compiler will NOT point at the consumers that should have been reconsidered: SortEmitter.cs:263/270 (statement-level COLLATING SEQUENCE alphabets get their own inline table and would silently keep the old representative-less shape), DataBinder.cs:837 (indexed-key weights), FigurativeConstants.cs:45 and ConcatFolder.cs:144-147 (the extremes).
- THE THREE-ARM TRAP. The same §12.3.7.4 GR7 1.3 rule is written in three places: Ord (fixed), Char (this fix), and CobolString.Weight:192 (untouched, still `: c`). Fixing only Char leaves ORD and relation conditions/SORT/indexed keys/MAX-MIN-string disagreeing about characters above the table. Ask which arms were fixed before calling this landed.
- AlphabetBind's ALSO arm increments `next` unconditionally (line 411) where the national arm guards it (line 547). Building RepByPos on top of the unguarded arm produces an array whose length disagrees with `next` for an all-duplicate ALSO group (e.g. `ALPHABET AL IS "AB", "A" ALSO "B"`), so CHAR at that position would index past the array or return a stale character. Fix the guard in the same change set as the array.
- The H5 hazard: `IntrinsicBinder.cs:630` passes weights ONLY when `ctx.Data.Collating is not null`, and an identity alphabet normalizes to `Alphabets.TryAdd(name, null)` at DataBinder.Switches.cs:373. The emitted `__COLLATE` field does not exist otherwise, so any new emitted companion (`__COLLATE_REP`) must be gated by the SAME `data.Collating is { }` test at ProgramEmitter.cs:160 or the generated C# will not compile.
- src/CobolSharp.Runtime/Intrinsics/IntrinsicFunctions.cs:199/217 is the LEGACY engine's Char/Ord (byte[] collating, the same lowest-code scan and the same `code >= collating.Length ? code + 1` fallback). It is the opt-in differential oracle (COBOLSHARP_LEGACY_DIFFERENTIAL=1), never authority — do not 'fix' it, but expect the differential to diverge on any new ALSO-collapse case and attribute that divergence deliberately rather than treating it as a red.
- The GnuCOBOL differential (tests/external/gnucobol/…run_functions.at exercises FUNCTION CHAR) may disagree on the above-table domain: GnuCOBOL's alphanumeric repertoire is 8-bit, so it has no U+0100 case at all. A divergence there is an out-of-domain difference, not evidence against the fix — but say so per-case rather than waving at the report.
- kb/Work/PB3.md:53 asserts 'It did NOT need the data-structure change this entry predicted. The dense 256-entry array is fine.' That sentence is true for ORD's arithmetic and FALSE for CHAR's representative — a reader trusting it will conclude no record change is needed and re-derive the lowest-code scan. Correct it when PB59-3 lands.
- Scope note against rule 3: the prompt names two edits (Char mirrors Ord; the table gains the representative array), but AR-15.15.3-2's domain bound, the ALSO `next++` guard, and CobolString.Weight are the same defect's siblings. Landing only the two named edits leaves CHAR accepting ordinal 65536 and leaves comparisons on the old rule — a partially-fixed CHAR that now looks correct in the round-trip test is a worse state than the current one, because the remaining hole loses its symptom.


# ===== ? =====

## sites
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:820 — IntrinsicRenderer.StrArgVisitor.Visit(BoundNumericLiteral)
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:813 — IntrinsicRenderer.StrArgVisitor.Loud
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:805 — IntrinsicRenderer.Str (the ONE entry into the intrinsic-argument string channel; one cached, function-blind visitor instance)
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:811 — IntrinsicRenderer.StrArgVisitor (class decl — LAST type in the file, file ends at line 837)
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:815 — IntrinsicRenderer.StrArgVisitor.Visit(BoundFieldOperand) — the delegation shape to mirror
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:833 — IntrinsicRenderer.StrArgVisitor.Visit(BoundFigurative)/Visit(BoundAllLiteral) — the PB25 delegating arms
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:593 — IntrinsicRenderer.StrArgList — shared by FIVE arms with three different §15.x.3 argument rules
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:663 — IntrinsicRenderer.RenderStringValue — "Concat" arm (§15.18, MUST admit numeric)
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:675 — IntrinsicRenderer.RenderStringValue — "BaseConvert" arm (§15.12, argument-1 only MUST admit numeric)
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:661 — IntrinsicRenderer.RenderStringValue — "MaxString"/"MinString" arm (StrArgList sibling that MUST NOT change)
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:673 — IntrinsicRenderer.RenderStringValue — "DisplayOf"/"NationalOf" arm (StrArgList sibling that MUST NOT change: §15.26.3 r1 = class national, §15.66.3 r1 = alphabetic/alphanumeric)
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:206 — IntrinsicRenderer.RenderNum — "OrdMaxString"/"OrdMinString" case (the FOURTH StrArgList caller, in the NUMERIC-result method; MUST NOT change)
- src/Cobol.Net.Compiler/CodeGen/Emit/OperandText.cs:207 — OperandText.AsStringVisitor.Visit(BoundNumericLiteral) — the DISPLAY-channel answer the admitting arm should delegate to
- src/Cobol.Net.Compiler/CodeGen/Emit/OperandText.cs:41 — OperandText.AsString (public entry; intercepts intrinsic + figurative before the visitor)
- src/Cobol.Net.Compiler/CodeGen/Emit/OperandText.cs:31 — OperandText — the cached flagged-instance precedent (four instances = deSign × floatCheck, zero per-call allocation)
- src/Cobol.Net.Compiler/CodeGen/Emit/OperandText.cs:241 — OperandText.IsStringVisitor.Visit(BoundNumericLiteral) — the classifier, unaffected by the fix
- tests/Cobol.Net.Tests.Unit/OperandStringChannelDriftTests.cs:88 — OperandStringChannelDriftTests.StrArgVisitor_KeepsTheDeliberatelyDifferentLeaves_Loud — THE PIN that holds the gap open
- tests/Cobol.Net.Tests.Unit/OperandStringChannelDriftTests.cs:97 — OperandStringChannelDriftTests.StrArgVisitor_KeepsTheDeliberatelyDifferentLeaves_Loud — the assertion body
- tests/Cobol.Net.Tests.Unit/OperandStringChannelDriftTests.cs:126 — OperandStringChannelDriftTests.BothStringChannels_CoverTheSameLeafSet_AndTheTableAccountsForEveryOne — the delegated/divergent buckets
- tests/Cobol.Net.Tests.Unit/OperandStringChannelDriftTests.cs:49 — OperandStringChannelDriftTests.StrArgVisitorBody — OPEN-ENDED slice to end-of-file (a second visitor class added after StrArgVisitor silently joins this "body")
- tests/Cobol.Net.Tests.Unit/OperandStringChannelDriftTests.cs:110 — OperandStringChannelDriftTests.Leaves — the regex that would union two visitors' arms if a second class lands after StrArgVisitor
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:546 — IntrinsicArgumentRules.DeliberatelyUnscreened — BASECONVERT and CONCAT are recorded as intentionally unscreened at bind time
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:1100 — IntrinsicBinder.CheckArgumentClasses — the guard that makes BASECONVERT/CONCAT arguments reach codegen unscreened
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:168 — IntrinsicBinder.OperandOf — a folded numeric function (FUNCTION LENGTH) becomes a BoundNumericLiteral, so it hits the same Loud arm
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:1137 — IntrinsicBinder.IsStringOperand — proves a BoundNumericLiteral can never select MaxString/MinString/OrdMaxString/OrdMinString, so those StrArgList callers are not reachable with a numeric literal
- src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:291 — IntrinsicCatalog — the BASECONVERT and CONCAT rows (ArgKinds "sii" / "s"; the hint column is unaudited and read by nothing)
- src/Cobol.Net.Compiler/Binding/IntrinsicResultType.cs:183 — IntrinsicResultType.Resolve — FollowsConcatArguments arm (a numeric-literal argument-1 already resolves to Alphanumeric, the §15.18.1 "Numeric usage Display" row)
- src/Cobol.Net.Compiler/Binding/Bound/BoundTree.cs:267 — BoundNumericLiteral — raw source text is what the admitting arm would render
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:161 — CobolIntrinsics.Concat — already string-typed, needs no change
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:168 — CobolIntrinsics.BaseConvert — already string-typed, needs no change
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:404 — IntrinsicFunctionDifferentialTests.Concat_ConcatenatesArgumentImages_2023 — the RV-15.18.4-1 test-ref; string literals only, no numeric case
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:415 — IntrinsicFunctionDifferentialTests.BaseConvert_ReExpressesInTargetBase_2023 — the RV-15.12.4-1 test-ref; every InlineData passes argument-1 as a STRING literal, which is why the numeric-literal hole was never caught
- tests/conformance/2023/manifest.json:94 — tests/conformance/2023/manifest.json — the sorted golden registry (PB25's entry is the precedent row)
- kb/Work/PB59.md:43 — PB59 — the rows this note owns, which include the four this fix re-verdicts

## currentBehavior
WRONG STAGE on legal source. A numeric literal in ANY intrinsic string-argument position compiles with zero diagnostics and aborts the run unit.

Path, end to end:
1. BIND. BASECONVERT and CONCAT are absent from `IntrinsicArgumentRules.Verified`, so `IntrinsicBinder.CheckArgumentClasses` (IntrinsicBinder.cs:1100) returns at its `TryGetValue` guard — no argument screen at all. Both are listed in `IntrinsicArgumentRules.DeliberatelyUnscreened` (IntrinsicArgumentRules.cs:547-548). The argument binds as `BoundNumericLiteral(Text)` (BoundTree.cs:267), holding the raw source text. A folded nested numeric function also arrives as this leaf via `IntrinsicBinder.OperandOf` (IntrinsicBinder.cs:168, "a folded LENGTH"). `IntrinsicResultType.Resolve` already answers correctly for a numeric argument-1 (Alphanumeric for both `FollowsArgument1` and `FollowsConcatArguments`, IntrinsicResultType.cs:160-161/183-184), so the result category is NOT part of the defect.
2. EMIT. `IntrinsicRenderer.RenderStringValue` routes CONCAT through `StrArgList(ic)` (:664) and BASECONVERT's argument-1 through `Str(ic.Args[0])` (:677). Both funnel into the ONE function-blind visitor `Str` (:805) → `StrArgVisitor.Visit(BoundNumericLiteral) => Loud(n)` (:820), which emits `NotImplemented.Value<string>("intrinsic string argument 'BoundNumericLiteral'")` (:813 + EmitCore.cs:81).
3. RUN. `NotImplementedCobolFeatureException`, exit 127. Measured in the triage record: `MOVE FUNCTION BASECONVERT(255, 10, 16) TO PIC X(24)` and `MOVE FUNCTION CONCAT(A 12)` both abort (rows AR-15.12.3-1 leg (a), RV-15.12.4-1, AR-15.18.3-1 leg (b), RV-15.18.4-1; AR-15.12.3-1 and RV-15.12.4-1 are refuter-CONFIRMED).

The rules being violated (both mechanically `--check`ed this session):
- §15.12.3 r1 — "Argument-1 is the input data item to be converted and shall be a usage display or national data item or literal, and, if the base specified in argument-2 is less than 11, shall also be an unsigned integer data item or literal." An unsigned integer LITERAL is expressly admitted.
- §15.18.3 r1 — "Argument-1 and argument-2 shall be data items or literals of class alphabetic, alphanumeric, boolean, numeric or national." Class NUMERIC is in the list; r3 narrows it to "usage display or national and … an unsigned integer".

The other side of the channel already has the answer: `OperandText.AsStringVisitor.Visit(BoundNumericLiteral) => EmitText.CsLiteral(n.Text)` (OperandText.cs:207). This is the identical PB25 shape — one rule, 

## fixInventory
- IntrinsicRenderer.cs:811-836 — add a SECOND cached StrArgVisitor configuration rather than a second class. Give the visitor a flag (e.g. `StrArgVisitor(IntrinsicRenderer owner, bool admitNumeric)`) whose only behavioural difference is `Visit(BoundNumericLiteral n) => admitNumeric ? OperandText.AsString(n, owner.Num) : Loud(n);` at :820 — delegating to the SAME OperandText.AsString the BoundFieldOperand (:815), BoundFigurative (:833) and BoundAllLiteral (:834) arms already delegate to, so the numeric-literal image stays written down once (OperandText.cs:207). ⛔ Do NOT introduce a second `private sealed class` — the drift test's StrArgVisitorBody() slices `src[at..]` to END OF FILE (OperandStringChannelDriftTests.cs:49-56) and StrArgVisitor is the last type in IntrinsicRenderer.cs (file ends 837), so a second class after it would be silently absorbed into the guarded 'body'.
- IntrinsicRenderer.cs:805-806 — replace the single lazily-cached `_strArg` field with the two flagged instances, mirroring OperandText.cs:31-39's four-cached-instance pattern (allocation-free per call), and expose the admitting entry as a distinct named method (e.g. `StrNum(BoundOperand op)`) so the call sites read as the rule they implement. Leave `Str(op)` as the non-admitting default — every other caller keeps today's semantics unchanged.
- IntrinsicRenderer.cs:593 — `StrArgList` must take the admission decision as a parameter (e.g. `StrArgList(BoundIntrinsicCall ic, bool admitNumeric = false)`), NOT change globally. Only CONCAT passes true.
- IntrinsicRenderer.cs:663-664 — the "Concat" arm calls the numeric-admitting list form (§15.18.3 r1: 'data items or literals of class alphabetic, alphanumeric, boolean, numeric or national'), with the citation in the arm comment.
- IntrinsicRenderer.cs:675-677 — the "BaseConvert" arm admits a numeric literal for `ic.Args[0]` ONLY (§15.12.3 r1). Arguments 2 and 3 already ride `ArgInt` and must stay there — §15.12.3 r1's second sentence makes them 'positive nonzero numeric integer literals or data items'.
- IntrinsicRenderer.cs:206-207, :661-662, :673-674 — VERIFY UNCHANGED after the StrArgList signature change: OrdMaxString/OrdMinString, MaxString/MinString and DisplayOf/NationalOf must keep the default non-admitting list. §15.26.3 r1 and §15.66.3 r1 exclude class numeric outright; a defaulted parameter is what keeps them so.
- OperandStringChannelDriftTests.cs:79-102 — rewrite StrArgVisitor_KeepsTheDeliberatelyDifferentLeaves_Loud. `BoundNumericLiteral` is no longer 'divergent by LEAF'; it is 'divergent by FUNCTION'. The new pin must assert BOTH halves: (a) the DEFAULT (non-admitting) configuration still renders Loud for BoundNumericLiteral — so NUMVAL/NUMVAL-F/DISPLAY-OF/NATIONAL-OF are not silently widened; and (b) the admitting configuration is reached from EXACTLY the BASECONVERT and CONCAT arms and no others (a source scan of RenderStringValue for the admitting entry's call sites, with the expected set spelled out). `BoundBoolOperand` stays a plain Loud leaf and its arm keeps the current assertion.
- OperandStringChannelDriftTests.cs:126-128 — the delegated/divergent buckets become three: delegated · divergent-always · admitted-per-function (BoundNumericLiteral). The 'unclassified' completeness check at :129-134 must still account for every leaf, so the third bucket has to be added to the Except() chain, not folded into an existing one.
- OperandStringChannelDriftTests.cs:49-56 + :110-111 — bound the body slice. If the flagged visitor stays one class this is unchanged; if the structure moves at all, StrArgVisitorBody() must end at the class's closing brace instead of end-of-file, or Leaves() will union arms from two types and the SetEquals check at :119 will pass for the wrong reason.
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:415-422 — add InlineData rows passing argument-1 as a NUMERIC LITERAL: `"255, 10, 16"` → `FF` (the refuter-confirmed AR-15.12.3-1 leg (a) / RV-15.12.4-1 probe) and at least one base-2..10 case. Every existing row quotes argument-1, which is why this hole survived the differential.
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:403-413 — extend Concat_ConcatenatesArgumentImages_2023 with `FUNCTION CONCAT(A, 12)` (AR-15.18.3-1 leg (b) / RV-15.18.4-1) and with a folded nested numeric function (`FUNCTION CONCAT(A, FUNCTION LENGTH(B))`) — that path reaches the same leaf through IntrinsicBinder.OperandOf:168 and would otherwise stay a run-time abort.
- tests/conformance/2023/ — new golden pair `pb59_numeric_literal_intrinsic_argument.cob` / `.out` in the PB25 shape (pb25_figurative_intrinsic_argument.cob is the model: the rule quoted in the source comments, one DISPLAY per leg), covering the admitted cases AND at least one NEGATIVE control proving the default arm still refuses (e.g. FUNCTION UPPER-CASE / NATIONAL-OF over a numeric literal stays refused). Register it in tests/conformance/2023/manifest.json (sorted list; PB25's row sits at line 94).
- IntrinsicArgumentRules.cs:546-549 — update the DeliberatelyUnscreened prose for BASECONVERT and CONCAT. The dictionary value is the note explaining why bind-time is silent; once the RENDERER admits numerics it must say so and name what is still unscreened (§15.12.3 r1's usage/unsigned-integer/base-range half; §15.18.3 r1's index/object/pointer exclusion and r2/r3's cross-argument usage + unsigned-integer conditions), or the note reads as 'nothing to do here'.
- kb/Work/PB59.md — flip the rows this change closes in the SAME commit (AR-15.12.3-1 leg (a), RV-15.12.4-1, AR-15.18.3-1 leg (b), RV-15.18.4-1 partial), and re-verdict the matching tests/version-matrix/traceability-inventory.json rows, whose `code-location` fields already name `IntrinsicRenderer.cs#StrArgVisitor` and `#StrArgList`. DEVLOG entry at the TOP, descending, per CLAUDE.md rule 6.

## risks
- ⛔ THE GREEN TEST IS THE GATE. tests/Cobol.Net.Tests.Unit/OperandStringChannelDriftTests.cs:84-102 (`StrArgVisitor_KeepsTheDeliberatelyDifferentLeaves_Loud`) asserts `Visit(BoundNumericLiteral)`'s RHS contains the literal token "Loud", and :127 lists the leaf in the `divergent` array. It will go RED the moment the arm changes. Its failure message actively argues against the fix ('the channel would start ACCEPTING an operand §15.3 excludes'). Updating the pin IS part of the fix, not a test repair — feedback_green_test_can_hold_a_gap_open. The refuter explicitly verified this pin exists and endorsed the per-function shape.
- The drift test reads SOURCE TEXT, not types: StrArgVisitorBody() (:49-56) returns `src[at..]` — from `private sealed class StrArgVisitor` to END OF FILE — and Leaves() (:110-111) regex-scans it. StrArgVisitor is the last type in IntrinsicRenderer.cs (class ends 836, file ends 837). A second visitor class placed after it makes `BothStringChannels_CoverTheSameLeafSet` pass on a UNION of two visitors' arms, and the per-leaf Regex.Match at :70/:95 silently binds to whichever arm appears first. A flag on one class avoids this entirely; a second class requires bounding the slice.
- StrArgList has FOUR callers with THREE different argument rules — :207 (OrdMaxString/OrdMinString, in RenderNum), :662 (MaxString/MinString), :664 (Concat), :674 (DisplayOf/NationalOf). This is the codebase's signature two-arm defect at 4× fan-out. Changing the shared helper's behaviour instead of parameterising it would silently admit a numeric literal into DISPLAY-OF (§15.26.3 r1 'class national') and NATIONAL-OF (§15.66.3 r1 'class alphabetic or class alphanumeric') — over-acceptance with no diagnostic, the exact inverse defect the PB49 test was written to prevent.
- `Str` has ~24 call sites in IntrinsicRenderer.cs (:218, 224, 232, 235, 243, 246, 248, 275, 282, 285, 295, 298, 301, 650, 677, 679, 680, 683, 688, 692, 719, 729, 764, 765, 767). Several are functions whose rules EXCLUDE numeric literals and whose correct fix is the opposite of this one — NUMVAL :218 (§15.67.3 r1) and NUMVAL-F :295 (§15.69.3 r1) have open inventory rows AR-15.67.3-1 / AR-15.69.3-1 demanding a COMPILE-TIME rejection. If `Str`'s default flips to admitting, those two defects change from 'wrong stage' to 'silently wrong answer', which is strictly worse.
- No bind-time screen backs the renderer: IntrinsicBinder.CheckArgumentClasses returns at its `Verified.TryGetValue` guard (:1100) for both functions. Admitting numerics in the renderer without §15.18.3 r3 / §15.12.3 r1's unsigned-integer condition means `FUNCTION CONCAT(A -12.5)` renders the literal text `-12.5` instead of being diagnosed, and `FUNCTION BASECONVERT(-5, 10, 16)` reaches the runtime and raises EC-ARGUMENT-FUNCTION where §4.2.2 makes it compile-time diagnosable. That is a NEW silent over-acceptance created by this fix unless the screen lands with it or the residue is recorded explicitly (the DeliberatelyUnscreened note is where it belongs).
- Two conformance tests are named as the inventory's `test-ref` for the very rules being fixed and currently pass while the defect is live: IntrinsicFunctionDifferentialTests.BaseConvert_ReExpressesInTargetBase_2023 (:415-422, all four InlineData rows quote argument-1) and Concat_ConcatenatesArgumentImages_2023 (:403-413, string literals only). They are evidence about what they RETURNED, never about what they never tried — feedback_measure_the_selectors_complement. If they stay green unchanged, the fix has no verification.
- BoundNumericLiteral carries RAW SOURCE TEXT (BoundTree.cs:267; built from `CheckLiteral(lit.GetText())` at ExpressionBinder.cs:142). `EmitText.CsLiteral(n.Text)` therefore reproduces any sign, decimal point or exponent verbatim. That is correct for §15.18.4 r1's 'all of the characters in argument-1' over a LEGAL (unsigned integer) argument, but it means the renderer cannot be the place that enforces r3 — do not add a normalisation there.
- IntrinsicBinder.OperandOf:168 turns a folded numeric intrinsic into a BoundNumericLiteral, so `FUNCTION CONCAT(A FUNCTION LENGTH(B))` shares this exact site. It is a second reachable shape that must be in the golden, or the fix will look complete while one arm of the leaf's producers stays unexercised.
- Edition gating: both functions are 2023-only (IntrinsicCatalog.cs:291-292) and IntrinsicFunctionDifferentialTests.Concat_And_BaseConvert_GatedBelow2023_1502 (:424-435) pins COBOLNET1502 at 2014. Any new golden must be filed under tests/conformance/2023/ and must not perturb that gate.


# ===== ? =====

## sites
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:276 — CobolIntrinsics.Repertoire — THE defect site (the 'toNational || c <= 0xFF' ternary)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:280 — CobolIntrinsics.Sub / CobolIntrinsics.ByteSub — the ONLY EC-DATA-CONVERSION raise site in the whole tree
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:256 — CobolIntrinsics.DisplayOf — Repertoire caller 1 of 3 (NAT→ANUM, sub live)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:263 — CobolIntrinsics.NationalOf — Repertoire caller 2 of 3 (ANUM→NAT; its computed sub is ALREADY dead)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:214 — CobolIntrinsics.Convert — Repertoire caller 3 of 3 (§15.19.4 r1/r3 arm, sub always null)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:228 — CobolIntrinsics.Convert — 8-BIT ASSUMPTION #1: the ANUM/ANY→byte reduction, one byte per code unit + ByteSub above 0xFF (the SECOND ARM of the same Latin-1 premise)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:244 — CobolIntrinsics.Convert — 8-BIT ASSUMPTION #2: the inverse, one alphanumeric char per byte (HEX→ANUM r1, HEX→BYTE r5)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:199 — CONVERT section header comment — the prose that DECLARES the 8-bit Latin-1 usage representation
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:250 — DisplayOf doc-comment — asserts the Latin-1 correspondence and the r2/r3 substitution as live
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:259 — NationalOf doc-comment — already states argument-2 is 'never consumed … semantically inert'
- src/Cobol.Net.Runtime/Exceptions/ExceptionState.cs:272 — ExceptionEngine.DataConversionError — the sink; its ONLY caller in src/Cobol.Net.* is Sub() above
- src/Cobol.Net.Runtime/Exceptions/ExceptionState.cs:831 — ExceptionState.DataConversionError — the static forwarder Sub() actually calls
- src/Cobol.Net.Runtime/Exceptions/EcFunctions.cs:63 — EcFunctions.FileN — NationalOf caller (runtime, NOT emitted code): EXCEPTION-FILE-N §15.29.4 r1c
- src/Cobol.Net.Runtime/Exceptions/EcFunctions.cs:72 — EcFunctions.FileN(string) — NationalOf caller: EXCEPTION-FILE-N §15.29.4 r2 (the 2023 connector form)
- src/Cobol.Net.Runtime/Exceptions/EcFunctions.cs:79 — EcFunctions.LocationN — NationalOf caller: EXCEPTION-LOCATION-N §15.31.3 r2b
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:673 — IntrinsicRenderer.RenderString — the DisplayOf/NationalOf arm (renders the FULL arg list, so argument-2 is evaluated and passed)
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:682 — IntrinsicRenderer.RenderString — the Convert arm (arg-1 + the three bind-resolved format ints)
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:593 — IntrinsicRenderer.StrArgList — how argument-2 reaches the runtime positionally
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:643 — IntrinsicBinder.BindIntrinsicCore — the ONE call site of CheckRepertoireArgs
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:933 — IntrinsicBinder.CheckRepertoireArgs — COBOLNET1546, all four arms
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:941 — CheckRepertoireArgs — the argument-1 CLASS arm (fail-open: OperandCategory returns null for a group, so the arm is skipped)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:952 — CheckRepertoireArgs — the argument-2 CLASS + WIDTH arms (the arms that must SURVIVE the fix even though the value is vacuous)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:892 — IntrinsicBinder.BindConvert — 8-BIT ASSUMPTION #3: the ANY→storage-encoding resolution says an alphanumeric operand's bits are Latin-1, 1 byte/char
- src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:201 — IntrinsicCatalog — the DISPLAY-OF / NATIONAL-OF rows (arity 1..2, RuntimeMethod names the runtime entry points)
- src/Cobol.Net.Compiler/Binding/Procedure/ExpressionBinder.cs:162 — ExpressionBinder.NationalLiteralOperand doc — asserts 'the D-N4 Latin-1 identity + substitution for wider characters' is live
- docs/CONFORMANCE.md:342 — Annex A.1 item 188 — the LANDED declaration this fix conforms the code to
- docs/CONFORMANCE.md:334 — Annex A.1 item 209 — the CONTRADICTING declaration: 8 bits per character position (why the byte/HEX arm cannot be swept in blind)
- docs/COBOLNET_DATA_MODEL_DESIGN.md:186 — D-N4 repertoire — the design SSOT sentence that must be re-stated
- docs/PHASE4_RECONCILIATION.md:713 — D-N4 Repertoire — the SECOND copy of the same sentence (one rule written down twice)
- tests/conformance/2002/national_intrinsics.cob:36 — golden national_intrinsics — the SUB1/SUB2 legs that PIN the substitution (must be re-derived, not deleted)
- tests/conformance/2002/national_intrinsics.out:5 — golden .out — the two lines that flip
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:604 — Convert_UntranslatableSetsDataConversion_WhenChecked_2023 — the ONLY test asserting the EC actually fires; it goes vacuous
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:613 — the same test's subject — N"AŠ" (U+0160) becomes translatable under the identity, so EXCEPTION-STATUS goes blank
- tests/Cobol.Net.Tests.Conformance/NationalBooleanLiteralTests.cs:310 — NationalLiteral_NonLatin1Char_Accepted_DisplayOfSubstitutes — asserts '?' and '#' for U+03A9; both flip to Ω
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:558 — Convert_Formats_2023 — the 8-bit HEX pins that DO NOT move if the byte arm stays 1 byte/char, and DO move if it widens
- tests/conformance/2023/intrinsics_convert.cob:2 — golden intrinsics_convert header — states the 8-bit Latin-1 premise; its AH=41 / RT=A lines pin it
- tests/Cobol.Net.Tests.Unit/ZeroLengthLiteralArgumentDriftTests.cs:32 — ZeroLengthLiteralArgumentDriftTests.Adjudicated — a drift test that PINS the NATIONAL-OF zero-length rule's HOME as CheckRepertoireArgs by name
- src/CobolSharp.Runtime/Intrinsics/IntrinsicFunctions.cs:650 — LEGACY oracle DisplayOf/NationalOf — already the identity (so the opt-in legacy differential AGREES with the fix, not with today's code)
- kb/Work/PB59.md:27 — PB59 mechanism family 1 — the note that owns this fix and the 28 inventory rows it re-verdicts

## currentBehavior
ONE translator, three callers, one boundary. `Repertoire(arg, toNational, sub)` (CobolIntrinsics.Text.cs:272) walks argument-1 char by char and appends `toNational || c <= 0xFF ? c : sub ?? Sub()`. Because `toNational` is evaluated FIRST and short-circuits, the ANUM→NAT direction is already the total UTF-16 identity and its `sub` (computed at line 264) is provably dead. Only the NAT→ANUM direction has a live boundary: a code unit above U+00FF is replaced by the caller's argument-2 character, or — when argument-2 is absent — by '?' from `Sub()` (line 280), which is the ONLY caller of `ExceptionState.DataConversionError` in `src/Cobol.Net.*` and therefore the only path by which EC-DATA-CONVERSION can ever be set. `ByteSub()` (line 285) is a second, distinct caller of `Sub()`, used by the ANUM/ANY→byte reduction at line 231.

Three callers reach `Repertoire`: `DisplayOf` (256, sub live), `NationalOf` (263, sub dead), and `Convert`'s §15.19.4 r1/r3 arm (214, `sub: null` — CONVERT takes no substitution character, so it always gets '?'+EC). `NationalOf` additionally has three NON-emitted runtime callers inside `EcFunctions` — `FileN()` (63), `FileN(string)` (72), `LocationN()` (79) — the EXCEPTION-FILE-N / EXCEPTION-LOCATION-N national twins; those are unaffected by value (identity direction) but they are the reason `NationalOf` cannot be deleted outright.

Compiler side: `IntrinsicRenderer` renders DISPLAY-OF/NATIONAL-OF through `StrArgList` (673–674), so argument-2 is genuinely evaluated and passed, not dropped at bind; CONVERT renders arg-1 plus the three bind-resolved format ints (682–684). `IntrinsicBinder.CheckRepertoireArgs` (933–966), called from exactly one site (643), enforces §15.26.3 r1/r2 and §15.66.3 r1/r2/r3 as COBOLNET1546 — argument-1 class, argument-2 class, argument-2 one-character width, and NATIONAL-OF's zero-length-literal prohibition. Those are ARGUMENT rules and stay valid whatever the returned value becomes; note both the class arms fail open (`OperandCategory` returns null for a group) and the width arm fails open (`KnownWidth` returns null for a group / ANY LENGTH) — separate PB59 rows, not this fix.

The contradiction this fix closes: docs/CONFORMANCE.md item 188 (landed, normative Annex A.1) declares the native alphanumeric repertoire to be UTF-16, one code unit per character position — and the compiler behaves that way everywhere else (a `PIC X` item stores U+4E16; `ORD` returns 19991; `CHAR`/`ORD` span 0xFFFF). Only line 276 says o

## fixInventory
- CobolIntrinsics.Text.cs:272–278 — REPLACE the translator body with the total identity. The whole `Repertoire` helper collapses: under item 188 both directions return `arg` unchanged. Prefer deleting the helper and its `toNational`/`sub` parameters over leaving a one-line pass-through, so no future reader re-introduces a boundary; if a named seam is wanted for the doc-comment's §-citations, keep `private static string Repertoire(string arg) => arg;` and let both directions call it (one mechanism, per feedback_one_mechanism_per_job).
- CobolIntrinsics.Text.cs:256–257 — DisplayOf keeps its `string? sub = null` parameter (the renderer passes argument-2 positionally and the catalog arity is 1..2) but stops consuming it: `public static string DisplayOf(string arg, string? sub = null) => arg;`. Do NOT drop the parameter — IntrinsicRenderer:673 renders the full arg list, so a one-parameter method would fail to compile the emitted C#.
- CobolIntrinsics.Text.cs:263–264 — NationalOf identically: `=> arg`, parameter retained, doc-comment's 'never consumed … semantically inert' promoted from a Latin-1-subset argument to the item-188 identity argument.
- CobolIntrinsics.Text.cs:214–215 — Convert's repertoire arm becomes `return arg;` for `!dstHex && dst != BYTE && (src == ANUM || src == NAT)`. Keep the arm (it is the guard that keeps NAT/ANUM character sources OUT of the byte pathway); only its body changes.
- CobolIntrinsics.Text.cs:280–284 — Sub() must NOT keep its current citation. After the change its only caller is ByteSub (byte serialization), and §15.19.4 r1/r3 / §15.26.4 r3 no longer describe anything reachable. Either (a) delete Sub and inline the raise into ByteSub with a citation that actually covers the byte path, or (b) OWNER DECISION: widen the ANUM byte reduction to 2 bytes per code unit (UTF-16BE, mirroring the NAT arm at 221–224), which makes the whole subsystem total and deletes Sub, ByteSub and the last EC-DATA-CONVERSION raise site outright. (b) is the only variant in which 'no EC raise reachable' is literally true; it changes CONVERT(x ANUM ANUM HEX) from 41 to 0041 and contradicts item 209, so it is a bare owner question, not an implementation detail. Ask it before writing code.
- CobolIntrinsics.Text.cs:228–232 + 244–247 + 892–896 in IntrinsicBinder — whichever way that decision goes, the 8-bit-ness must be stated in exactly ONE place and cited, not repeated in three comments plus a doc row. Extract the bytes-per-alphanumeric-character-position constant (and its inverse) so the ANUM→byte, byte→ANUM and ANY-resolution arms all read it; that is the structure that makes the next case automatic (CLAUDE.md rule 5).
- CobolIntrinsics.Text.cs:195–202, 250–255, 259–264, 266–271 — rewrite all four doc blocks. Every one currently asserts the Latin-1 correspondence as the implementor determination; they become the item-188 identity, with the byte serialization named as the separate §8.1.2 NOTE 2 determination and cross-referenced to item 209. Run `python scripts/spec/cite.py --check` on every § that survives the rewrite — do NOT carry a citation across an edit.
- docs/CONFORMANCE.md — ADD the missing Annex A.1 item 33 row ('Computer's coded character set (correspondence between alphanumeric and national characters)', required + documented, cross-referencing §15.19.4, §15.26.4 r1/r3 and §15.66.4 r1/r3). Item 33 is the obligation this whole defect exists under: the inventory notes record that DISPLAY-OF's correspondence is stated only in source comments and design docs. Adding it moves audit_annex_a1.py from 19 to 20 of 199; update the counts in the paragraph at docs/CONFORMANCE.md:300–307 by RE-RUNNING the script, never by hand.
- docs/CONFORMANCE.md:342 (item 188) — extend the determination sentence to state the CONSEQUENCE explicitly: because the two repertoires are the same UTF-16 set, the §15.26.4/§15.66.4/§15.19.4 correspondence is total, the substitution characters are never returned and EC-DATA-CONVERSION is never set from the repertoire path. A determination a reader has to infer is the failure item 33 is charging.
- docs/COBOLNET_DATA_MODEL_DESIGN.md:186–188 AND docs/PHASE4_RECONCILIATION.md:713–716 — the SAME D-N4 sentence is written down twice and both copies say NAT→ANUM keeps the Latin-1 subset identity with '?'+EC substitution. Rewrite BOTH in the same change set (feedback_one_rule_one_place: fix the duplication or the next edit updates one).
- src/Cobol.Net.Compiler/Binding/Procedure/ExpressionBinder.cs:162–164 — the N"…" literal doc-comment repeats 'the D-N4 Latin-1 identity + substitution for wider characters'; update.
- src/Cobol.Net.Runtime/Exceptions/EcFunctions.cs:12–13 and 60–61 — class and method doc-comments justify the -N twins by 'Latin-1 ⊂ national'. The justification becomes stronger (plain identity), but the words must change.
- tests/conformance/2002/national_intrinsics.cob:1–10 + 36–44 and its .out:5–6 — the header asserts the premise being retired and the SUB1/SUB2 legs pin '?' and '#'. RE-DERIVE, do not delete: keep the U+4E16 argument and change the expectations to SUB1=A世B / SUB2=A世B, which is now the stronger case — it proves the correspondence is total AND that argument-2 is inert. Same for the golden's header prose.
- tests/Cobol.Net.Tests.Conformance/NationalBooleanLiteralTests.cs:297–311 — flip the assertion to "AΩB\nAΩB" and rewrite the summary; keep the \u escape and the ENCODING-SENSITIVE warning, since the test's real job (one char > U+00FF survives source decoding) is unchanged and now has a sharper oracle.
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:603–627 — Convert_UntranslatableSetsDataConversion_WhenChecked_2023 becomes VACUOUS on the repertoire path (U+0160 is translatable). ⛔ Do not let it silently pass by asserting a blank status: either retarget it at whatever EC raise survives the byte-arm decision, or delete it and replace it with a positive test that EC-DATA-CONVERSION is NOT set for a wide character through NAT→ANUM (a green test pinning the old loud behaviour would hold the gap open — feedback_green_test_can_hold_a_gap_open).
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:566–571 (Convert_AnumNatAnum_RoundTrip_2023) — the comment's rationale ('Latin-1 ⊂ national') changes; ADD a leg with a wide character, because the round trip is now total and that is the whole point of the fix. This is the test that would otherwise pass for the wrong reason.
- NEW spec-derived golden (the half no existing test covers, per the inventory's own test-needed note): ONE program that pins the declared repertoire boundary from BOTH sides — FUNCTION ORD over a PIC X item holding a wide character AND FUNCTION DISPLAY-OF of the same character through a PIC N item, asserting they agree. Today ORD gives 19991 and DISPLAY-OF gives '?' (ordinal 64); after the fix both name the same character. Register it in tests/conformance/2002/manifest.json in the same commit (feedback_goldens_ship_with_the_feature).
- IntrinsicBinder.CheckRepertoireArgs:933–966 — NO functional change from this item; the §15.26.3/§15.66.3 ARGUMENT rules are independent of the returned value and argument-2 stays syntactically required to be one character of the right class even though it is never returned. Only the doc-comment gains a sentence saying so. ⚠ If the fix tempts you to relax or delete the argument-2 arms because the value is vacuous, that is rejecting legal-source screening for no reason — the standard still constrains the argument.
- kb/Work/PB59.md — flip mechanism family 1 to landed in the SAME commit and re-verdict the rows it owns on this axis: RV-15.26.4-1, RV-15.26.4-3, RV-15.66.4-1, RV-15.66.4-2, RV-15.66.4-3, RV-15.19.4-1, RV-15.19.4-3. Families 2 and 3 stay open, so the note does not close.
- tests/version-matrix/traceability-inventory.json — the notes on rows 44368, 46708 and 46721 explicitly say the fix shape is 'reconcile Repertoire's 0xFF boundary with the declared repertoire … AND add the Annex A.1 item 33 row … One owner-ratifiable change set, not two.' Update those rows' verdicts and test-refs; row 46708 currently records test-ref WITHDRAWN because national_intrinsics pins the contradiction — re-instating it depends on the golden rewrite above.

## risks
- tests/conformance/2002/national_intrinsics.out:5–6 (SUB1=A?B / SUB2=A#B) — the shipped golden directly pins both substitution behaviours. It is registered at tests/conformance/2002/manifest.json:57 and also listed in tests/CobolSharp.Tests.Integration/ConformanceTests.cs:157, so it runs from two harnesses.
- tests/Cobol.Net.Tests.Conformance/NationalBooleanLiteralTests.cs:310 — Assert.Equal("A?B\nA#B", stdout) for U+03A9. Both halves flip.
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:604–627 — the ONLY test in the tree that asserts EC-DATA-CONVERSION actually fires. After the fix its subject (U+0160) is translatable and EXCEPTION-STATUS goes blank, so the test fails; if 'fixed' by asserting blank it becomes a green test pinning nothing.
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:558 and 561 + tests/conformance/2023/intrinsics_convert.out:1 (AH=41) — these pin the 8-bit BYTE serialization, not the repertoire. They survive untouched if the byte arm stays 1 byte/char, and ALL of them break if the owner chooses the UTF-16BE widening. This is the fork that decides whether 'no EC raise reachable' is literally achievable.
- docs/CONFORMANCE.md:334 item 209 declares 8 bits per character position and justifies it by 'DISPLAY's one byte per character position'. A UTF-16 alphanumeric character position needs 16. Item 188 and item 209 cannot both stay as written once the byte arm is touched; leaving them is a documented self-contradiction of exactly the kind item 33 exists to prevent.
- src/Cobol.Net.Runtime/Exceptions/EcFunctions.cs:63, 72, 79 — three NON-emitted callers of NationalOf. They are value-identical before and after (identity direction), but they mean NationalOf's signature and behaviour are load-bearing outside the intrinsic renderer; pinned by tests/conformance/2002/exception_file_n.cob (whose header at line 4 also cites the D-N4 Latin-1 premise) and ExceptionConditionConformanceTests.IoAtEnd_ExceptionFileN_NationalTwin.
- tests/Cobol.Net.Tests.Unit/ZeroLengthLiteralArgumentDriftTests.cs:32–37 — an Adjudicated entry naming IntrinsicBinder.CheckRepertoireArgs as NATIONAL-OF's zero-length home, by string. Any relocation or rename of CheckRepertoireArgs fails this drift test; the reason text must move with the code.
- src/CobolSharp.Runtime/Intrinsics/IntrinsicFunctions.cs:650–664 — the legacy oracle's DisplayOf/NationalOf are already `return value` (identity). The opt-in legacy differential (COBOLSHARP_LEGACY_DIFFERENTIAL=1) therefore currently DISAGREES with Cobol.Net on a wide character and will AGREE after the fix; do not read a differential flip here as a regression. Note the legacy has no argument-2 overload at all (IntrinsicFunctions.cs:882–883 dispatch drops it), so it cannot arbitrate the argument-2 question.
- tests/version-matrix/traceability-inventory.json rows 44355 / 44368 / 44394 / 46708 / 46721 carry long verbatim probe transcripts asserting the CURRENT '?' behaviour as measured evidence. They are evidence records, not goldens, but a kb-sync or inventory audit that re-checks them against the compiler will report drift unless the verdicts and notes are updated in the same change set.
- CheckRepertoireArgs' class arms fail open for GROUP operands (OperandCategory returns null) and the width arm fails open for groups and ANY LENGTH (KnownWidth returns null) — backlog-triage-2026-08-09.json rows P26301/P26305/P26306/P26309 measured this. It is tempting to 'fix while in there'; it is a separate PB59 sub-item (task #10, KnownWidth totalization) and mixing it in makes the value fix unverifiable.
- docs/rearchitecture/PHASE-11-scout-notes.md:737 and docs/ISO2023_CONFORMANCE_PLAN.md:291 both describe CheckRepertoireArgs/COBOLNET1546 by line number and behaviour — stale-doc sweep targets at phase close, not blockers.
- A test battery is running now: none of the above was executed, all of it is read off the files at the line numbers cited. The two directions' current values were derived from the source, not measured this session — confirm with a wide-character probe before committing.


# ===== ? =====

## sites
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:918 — IntrinsicBinder.KnownWidth
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:962 — IntrinsicBinder.CheckRepertoireArgs — the ONLY KnownWidth call site (whole-repo grep: definition at 918, use at 962, nowhere else)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:916 — KnownWidth doc comment — states the partiality as if it were only about runtime lengths
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:933 — IntrinsicBinder.CheckRepertoireArgs (declaration)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:937 — CheckRepertoireArgs — the §15.66.3 r3 zero-length-literal guard the X'' lexer gap makes unreachable
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:941 — CheckRepertoireArgs — argument-1 class arm, `is { }` skips on a null category (group / ALL / figurative)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:953 — CheckRepertoireArgs — argument-2 class arm, same `is { }` skip
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:914 — IntrinsicBinder.OperandCategory (local alias — NOT a second implementation)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:904 — IntrinsicBinder.IsNationalOperand — alias caller #1 (only user is line 896, BindConvert's §15.19.3 SR7 ANY→raw-storage resolution)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:896 — IntrinsicBinder.BindConvert — the sole IsNationalOperand caller
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:625 — IntrinsicBinder.BindKnown — alias caller #2, the CHAR/ORD/MAX/MIN collating-sequence flag
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:643 — IntrinsicBinder.BindKnown — the one dispatch into CheckRepertoireArgs
- src/Cobol.Net.Compiler/Binding/IntrinsicResultType.cs:278 — IntrinsicResultType.OperandCategory — THE definition (public; only external reader is the IntrinsicBinder alias at line 914)
- src/Cobol.Net.Compiler/Binding/IntrinsicResultType.cs:241 — IntrinsicResultType.CategoryOf — in-file OperandCategory caller #1 (feeds the §15.x.1 FollowsArgument1 / FollowsConcatArguments rows at lines 161, 184)
- src/Cobol.Net.Compiler/Binding/IntrinsicResultType.cs:248 — IntrinsicResultType.FirstCategory — in-file OperandCategory caller #2 (MAX/MIN/ORD-MAX/ORD-MIN result type)
- src/Cobol.Net.Compiler/Binding/IntrinsicResultType.cs:225 — IntrinsicResultType.UniformArgumentType — the fall-through a total category would change for a group argument
- src/Cobol.Net.Compiler/Binding/IntrinsicResultType.cs:261 — IntrinsicResultType.OperandCategory doc — the standing argument for the null-on-group arm (must be answered, not ignored, by a totalization)
- src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4:815 — HEXLIT lexer token — the quotation arm takes zero-length HEXDIGITS, the apostrophe arm hard-codes `+`
- src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4:810 — HEXLIT's own comment — records the zero-length fix for the quotation delimiter only
- src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4:737 — fragment HEXDIGITS — the zero-or-more digit body the apostrophe arm does not use
- src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4:738 — fragment NAT_BODY / BOOL_BODY — the Group-B one-body pattern HEXLIT alone does not follow (both delimiters, one fragment)
- src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4:724 — fragment STR_BODY — the both-delimiters fragment that catches the mis-split `''` after `X` is orphaned as IDENTIFIER
- src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4:953 — SUBSCRIPT mode — SUB_STRINGLIT/SUB_NATLIT/SUB_BOOLLIT exist, there is NO SUB_HEXLIT twin (whole-file grep for HEXLIT: 730, 737, 739, 740, 742, 743, 815 only)
- src/Cobol.Net.Frontend/Common/CobolLiteral.cs:167 — CobolLiteral.DecodeHex — already correct for X'' (len 3 passes the guard, digits=="" → ""): the fix is lexer-only
- src/Cobol.Net.Frontend/Common/CobolLiteral.cs:45 — CobolLiteral.SplitLiteral — the delimiter-agnostic prefix split already admits X''
- src/Cobol.Net.Compiler/Validation/VersionConformancePass.cs:1603 — VersionConformancePass nonNumericLiteral override — the §8.3.3.2.3 r6 grouping screen a new X'' token flows through (n=0 → 0%2==0 → null, no false reject)
- src/Cobol.Net.Frontend/Grammar/Core/CobolExpressions.g4:486 — nonNumericLiteral — HEXLIT parser reference #1 of 3 (no parser change needed; the token type is unchanged)
- src/Cobol.Net.Frontend/Grammar/Core/CobolExpressions.g4:507 — concatOperand — HEXLIT parser reference #2 of 3
- src/Cobol.Net.Frontend/Grammar/Core/CobolExpressions.g4:545 — figurativeConstant — HEXLIT parser reference #3 of 3 (ALL X'' becomes reachable too)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/ConditionBinder.cs:151 — ConditionBinder.RefModLen — the EXISTING static ref-mod width extractor the total dispatch should reuse rather than re-derive
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/StringUnstringBinder.cs:286 — StringUnstringBinder.StrUnstrReceiveSize — a THIRD hand-rolled static width dispatch (group→ImageWidth, refmod→−1)
- src/Cobol.Net.Compiler/Binding/Model/DataItem.cs:357 — DataItem.ImageWidth — already the correct group answer (leaf sum, redefines excluded, bit walk)
- src/Cobol.Net.Compiler/Binding/Bound/BoundTree.cs:283 — BoundAllLiteral — carries Literal and Category; §8.3.3.6.4 GR3c gives it the literal's length, and KnownWidth has NO arm for it
- src/Cobol.Net.Compiler/Binding/Bound/BoundTree.cs:278 — BoundFigurative — §8.3.3.6.4 GR3b makes a bare figurative ONE character; KnownWidth has no arm (BindLengthFold at line 1258 already answers "1")
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:1258 — BindLengthFold — the EXISTING total-ish width fold whose figurative/ALL arms KnownWidth should mirror (feedback_one_rule_one_place)
- src/Cobol.Net.Compiler/Binding/Model/Place.cs:271 — RefModPlace.CategoryOf — the ONE §8.4.3.3.4 GR6 category rule; IntrinsicResultType.OperandCategory does NOT route through it
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:282 — IntrinsicArgumentRules.ClassOfPlace — the SIBLING classifier that IS total (refmod via GR6, group via §8.5.2.1); OperandCategory is its partial twin
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:455 — IntrinsicArgumentRules.Verified — the declared-PARTIAL siblings of the same missing width conjunct (AR-15.57.3-1)
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:83 — ArgRule — the per-position schema record; it carries a class Kind and a NoZeroLengthClause but NO width predicate
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:1115 — CheckArgumentClasses — the shared per-position enforcement loop where a width predicate would land, and its explicit "test the LITERAL, not the LENGTH" warning
- tests/conformance/2023/r03_hexadecimal_national_and_boolean_literals.cob:82 — the golden that pins zero-length hex — quotation delimiter ONLY, no apostrophe case
- tests/conformance/2023/r03_hexadecimal_national_and_boolean_literals.out:13 — r03 golden output — the two zero-length rows to extend

## currentBehavior
TWO independent partial dispatches, both silent.

(A) WIDTH. `IntrinsicBinder.KnownWidth` (IntrinsicBinder.cs:918) has exactly three arms plus `_ => null`, and exactly one caller: `CheckRepertoireArgs`'s §15.26.3 r2 / §15.66.3 r2 length screen at line 962, written `if (KnownWidth(args[1]) is { } w && w != 1)`. Because the guard is `is { }`, every null SKIPS the screen with no diagnostic. Null is returned for: a GROUP (`{ IsGroup: false, … }` fails the pattern), a REFERENCE-MODIFIED place (explicit `=> null` arm), an ANY LENGTH item, `BoundAllLiteral` (no arm), `BoundFigurative` (no arm), `BoundComputedOperand` (no arm), `BoundOperandError` (no arm). Measured in the 2026-08-09 triage and independently re-run by the refuter: `FUNCTION DISPLAY-OF(N-W, G6)` with a six-position group argument-2 exits 0 with no diagnostic and prints `G6=[A#B   ]` (only the group's first character is used); `FUNCTION DISPLAY-OF(N-W, ALL "AB")`, `FUNCTION NATIONAL-OF(A1, N-G)` (group of PIC N(2)), `FUNCTION NATIONAL-OF(A1, N-2(1:2))` (ref-mod, two national positions) and `FUNCTION NATIONAL-OF(A1, ALL "QQ")` are all accepted. The arm itself is alive — the plain two-character literal `"AB"` / `N-2 PIC N(2)` IS rejected with COBOLNET1546 — so this is precisely a partial-function defect, not a missing rule. The width knowledge already exists elsewhere: `DataItem.ImageWidth` (DataItem.cs:357) is the group answer, `ConditionBinder.RefModLen` (ConditionBinder.cs:151) is the static ref-mod answer, and `BindLengthFold` (IntrinsicBinder.cs:1258) already answers 1 for a figurative and `Literal.Length` for an ALL literal. Three hand-rolled width walks exist today (KnownWidth, RefModLen, StringUnstringBinder.StrUnstrReceiveSize at :286) and none is the total one.

(B) CATEGORY. `IntrinsicResultType.OperandCategory` (IntrinsicResultType.cs:278) is the matching partial: `BoundFieldOperand f => f.Place.Item.IsGroup ? null : f.Place.Item.Pic?.Category` returns null for a group, and `_ => null` catches BoundAllLiteral, BoundFigurative, BoundBoolOperand and BoundOperandError. Its two class call sites in `CheckRepertoireArgs` (lines 941 and 953) are both `is { }` guards, so the r1/r2 CLASS halves are skipped on exactly the shapes whose LENGTH half is also skipped. Two further deviations: a ref-modified operand reaches the `f.Place.Item.Pic?.Category` arm through `PlaceDecorator`'s forwarding, so it answers the INNER item's raw category and bypasses `RefModPlace.CategoryOf`'s §8.4.3.3.4 GR6 lettered r

## fixInventory
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:918 — replace the partial `KnownWidth` with a TOTAL static width dispatch. Add arms: `BoundFieldOperand { Place: RefModPlace }` → the static literal length via the existing `int.TryParse(rm.Length, …)` shape (null only when Start/Length are non-literal, i.e. genuinely runtime, and null for the omitted-length `(i:)` form); `BoundFieldOperand { Place.Item.IsGroup: true }` → `item.ImageWidth` (§8.5.2.1 / §15.50.4 r7a — but keep null when the group has a DYNAMIC LENGTH leaf or a DYNAMIC-CAPACITY table, mirroring BindLengthFold's HasDynamicLengthLeaf / HasDynamicCapacityTable guards at :1316/:1322); `BoundAllLiteral a` → `a.Literal.Length` (§8.3.3.6.4 GR3c); `BoundFigurative` → 1 (§8.3.3.6.4 GR3b). Keep null ONLY for genuinely runtime widths: IsAnyLength, IsDynamicLength, BoundComputedOperand, BoundOperandError.
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:916 — rewrite the doc comment so it enumerates what still answers null and WHY (runtime-only widths), instead of the current sentence that implies groups and ALL literals are runtime facts.
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:1258 + ConditionBinder.cs:151 + StringUnstringBinder.cs:286 — do NOT add a fourth width walk. Extract the static-width answer ONCE (a `StaticWidth(BoundOperand)` / `StaticWidth(Place)` pair, naturally on DataItem/Place or a small binder-side helper) and have KnownWidth, BindLengthFold's literal/figurative/ALL/group arms, ConditionBinder.RefModLen and StrUnstrReceiveSize read it. Rule 5: this is the restructuring that makes the NEXT length screen automatic. If the extraction is deferred, the deferral is an owner decision and must be written down.
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:962 — the call site stays `is { } w && w != 1`, but the diagnostic text must name the SHAPE as well as the number (a group and an ALL literal now reach it), and both §15.26.3 r2 and §15.66.3 r2 must be re-validated with `python scripts/spec/cite.py --check`.
- src/Cobol.Net.Compiler/Binding/IntrinsicResultType.cs:278 — totalize `OperandCategory` in the same change set, because row 34's class arm is skipped on the same shapes: add a group arm returning `PicCategory.Alphanumeric` per §8.5.2.1 (an alphanumeric group item has class and category alphanumeric — the only group form this compiler can express, exactly as the :261 comment establishes), a `BoundAllLiteral all => all.Category` arm, and route the ref-mod case through `RefModPlace.CategoryOf` (Place.cs:271) so §8.4.3.3.4 GR6's lettered rewrites apply here as they already do in IntrinsicArgumentRules.ClassOfPlace. Rewrite the :261 remark to say what it now DOES answer for a group and keep the national-group half of the argument intact.
- src/Cobol.Net.Compiler/Binding/IntrinsicResultType.cs:225 — audit `UniformArgumentType` under the new group arm: a group argument to MAX/MIN/ORD-MAX/ORD-MIN now resolves Alphanumeric instead of falling through to the integer/numeric row. That is the §15.59.1 answer and it agrees with `IntrinsicBinder.IsStringOperand` (:1143, which already calls a group a string operand) — but it is a behavior change and needs its own conformance case, or the group arm must be scoped to the argument-rule path with a written reason.
- src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4:815 — add the apostrophe production beside the quotation one, as a shared fragment rather than a second inline alternative, matching NAT_BODY/BOOL_BODY (:738–743): `fragment HEX_BODY : 'X' '"' HEXDIGITS '"' | 'X' '\'' HEXDIGITS '\'' ;` then `HEXLIT : HEX_BODY ;`. Cite §8.3.3.2.2 Format 2 (both delimiters in the general format) and §8.3.3.2.3 NOTE 2 (zero length legal) — render the PDF page for the Format 2 diagram before relying on the delimiter set, per the diagram-lossiness rule.
- src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4:810 — update HEXLIT's comment: it currently narrates the X"" fix only; it must record that both delimiters take HEXDIGITS and that the grouping rule stays at bind (CobolLiteral.HexGroupViolation).
- src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4:953–960 — decide the SUB_HEXLIT question explicitly (the two-arm sweep): either add `SUB_HEXLIT : HEX_BODY ;` beside SUB_NATLIT/SUB_BOOLLIT so the shared fragment serves both modes as the Group-B discipline requires, or record a measured probe showing the subscript/keyword-omitted capture path never sees a hex literal. Do not leave it unaddressed.
- tests/conformance/2023/r03_hexadecimal_national_and_boolean_literals.cob:82 — extend the existing golden with the apostrophe cases: `MOVE FUNCTION LENGTH(X'') TO L`, `MOVE X'' TO A5` (the statement the parse error rejected outright), and an `X'41'` control; extend .out accordingly. r03 is already in tests/conformance/2023/manifest.json:138, so no manifest row is needed.
- tests/conformance/negative/ — add the width fixtures the triage probes name, each with its .err (COBOLNET1546) and a manifest.json row: DISPLAY-OF argument-2 as a six-position alphanumeric group (P26305), as a group of PIC N(2) (P26306), and as ALL "AB" (P26309); NATIONAL-OF argument-2 as a group of PIC N(2) (P66304), as N-2(1:2) (P66305), and as ALL "QQ" (P66308). Add a POSITIVE case too — a one-position group and a one-character ALL literal as argument-2 must still compile — so the totalization is proved not to over-reject.
- tests/conformance/negative/ — add `FUNCTION NATIONAL-OF(X'')` drawing COBOLNET1546 §15.66.3 rule 3, which is the whole point of the lexer twin (the guard at IntrinsicBinder.cs:937 already exists and is simply unreachable today).
- tests/Cobol.Net.Tests.Unit/ — add a drift test asserting KnownWidth is TOTAL over the BoundOperand hierarchy (every concrete subtype in BoundTree.cs:254–298 either yields a width or is listed in an adjudication map with its runtime-only reason), the shape ZeroLengthLiteralArgumentDriftTests already uses. Make it fail once before trusting it (feedback_green_gates_arent_evidence).
- kb/Work/ — flip the status of the notes carrying AR-15.26.3-2, AR-15.66.3-2 and AR-15.66.3-3 in the same commit; AR-15.57.3-1 (LOWER-CASE/UPPER-CASE minimum-width conjunct, IntrinsicArgumentRules.cs:455) stays OPEN unless the same change set adds a width predicate to ArgRule — say which, explicitly.
- docs/ + DEVLOG.md — the deep-dive that owns the intrinsic argument screen records the new total width dispatch and the OperandCategory totalization; DEVLOG gets a top entry with a real timestamp.

## risks
- THE MOST LIKELY BREAKAGE IS OVER-REJECTION, and it is the exact failure PB1 caused (12 legal corpus programs turned away). A group argument-2 whose ImageWidth happens to be 1 must still be accepted, and a ref-mod whose static length is 1 must still be accepted. Neither has a positive fixture today — every existing golden uses a one-character LITERAL argument-2 (tests/conformance/2002/national_intrinsics.cob:43 `FUNCTION DISPLAY-OF(N-WIDE, "#")`, tests/conformance/2023/national_of_argument_forms.cob:18 `FUNCTION NATIONAL-OF("XY" N"#")`), so the corpus cannot catch an over-reject on the newly-total shapes. Add the positive cases with the negatives.
- IntrinsicResultType.FirstCategory (IntrinsicResultType.cs:248) feeds UniformArgumentType (:225), which types MAX/MIN/ORD-MAX/ORD-MIN. Giving a group a non-null category changes result-type resolution for `FUNCTION MAX(group …)` from the integer/numeric fall-through to Alphanumeric. IntrinsicBinder.IsStringOperand (:1143) already routes a group to the STRING comparison body, so today the two halves of that one decision DISAGREE for a group argument — this is the same split PB48 fixed for the figurative ZERO. Totalizing the category likely FIXES it, but it is a silent behavior change with no fixture; measure it.
- IntrinsicResultType.CategoryOf (:241) drives the FollowsArgument1 row (:161) and the FollowsConcatArguments row (:184). A group argument-1 to any function on those rows currently resolves Alphanumeric via the `is National ? … : Alphanumeric` ternary and will continue to — no change there — but CONCAT (§15.18) and the TRIM/REVERSE family should be spot-checked, since PB59-7 (CONCAT's ALPHABETIC result-category channel) touches the same resolver and the two tasks can collide.
- IntrinsicResultType.cs:261 carries an explicit, argued defence of the null-on-group arm, ending 'null — read by every caller as "no fixed class, skip" — is the honest answer rather than a guess', and it records that the arm 'survived a deliberate attempt to "fix" it'. Changing it without rewriting that remark leaves a doc contradicting the code. The remark's actual claim is about NATIONAL groups (GROUP-USAGE unmodelled); the alphanumeric-group case §8.5.2.1 answers is not covered by it — say so in the rewrite rather than deleting it.
- IntrinsicBinder.cs:625 `nationalArg` and :904 `IsNationalOperand` both read OperandCategory and feed the CHAR/ORD/MAX/MIN __COLLATE / __COLLATE_NAT flags (hazard H5) and BindConvert's §15.19.3 SR7 ANY→raw-storage resolution (:896). A group that starts answering Alphanumeric does not become National, so these should be inert — but the ref-mod change DOES move category (GR6c turns a ref-modified national-usage numeric into National), which flips `nationalArg` and therefore which collating table is passed. There is a SECOND, independent IsNationalOperand in the codegen layer (CodeGen/Emit/ConditionRenderer.cs:70, `BoundFieldOperand { Place.Item.Pic.Category: PicCategory.National }`) that does NOT know about GR6 — two copies of one rule, and only one would be fixed. This is the two_arm_dispatch shape; check which arm you fixed.
- The r03 golden (tests/conformance/2023/r03_hexadecimal_national_and_boolean_literals.out:13) pins `13=0001` — FUNCTION LENGTH(X"") answers 1, because BindLengthFold's literal arm is `Math.Max(1, s.Value.Length)` (IntrinsicBinder.cs:1175). The apostrophe twin will inherit that same 1. Whether §15.50.4 gives a zero-length literal a LENGTH of 0 or 1 is a separate question the golden currently freezes; do not silently change it while adding X'', and do not add an X'' row expecting 0 without deriving it from §15.50.4 first.
- tests/conformance/negative/national-of-wrong-category.err and display-of-wrong-category.err each contain only the bare code `COBOLNET1546`, so they will NOT catch a change in which of r1/r2/r3 fires or in the message text. A regression that swaps the rule-1 diagnostic for the rule-2 one passes both fixtures green. If the new negatives are written the same way they are equally blind — pin the rule ordinal in the .err text or accept that the fixture proves only that something was rejected.
- tests/Cobol.Net.Tests.Unit/ZeroLengthLiteralArgumentDriftTests.cs:32 adjudicates NATIONAL-OF OUT of the schema-driven zero-length rule on the grounds that it is 'enforced in IntrinsicBinder.CheckRepertoireArgs' and 'Verified enforced by SpecZeroLengthArguments below'. That adjudication is currently TRUE only for the quotation spelling — the apostrophe spelling never reaches the guard. The test is green today while the rule it certifies has a hole, which is feedback_green_test_can_hold_a_gap_open. Either the lexer fix closes it (then say so in the adjudication text) or the adjudication is overstated and must be narrowed.
- VersionConformancePass.cs:1594–1600 argues at length that the hex GROUPING rule is deliberately NOT a lexer rule, because a token that fails to match splits into IDENTIFIER + STRINGLIT and lands back in the silent-degradation hole. The apostrophe `+` is that exact hole, still open for the zero-length case — so the lexer fix is consistent with that doctrine, not a violation of it. Do not let the comment be read as a reason to leave the `+` alone.
- X"…" is ungated at every edition today (VersionConformancePass.cs:1592 gates NATLIT/BOOLLIT only, and constructs.json has no hex row), while NX at 85 IS gated (negative fixture r03-hex-national-literal-at-85). The apostrophe twin must inherit X"…"'s posture EXACTLY — adding an edition gate here would break every program below that edition that already compiles X'41' (feedback_edition_gate_sweep). If §8.3.3.2 does introduce the hexadecimal-alphanumeric literal at a specific edition, that is a separate finding about the EXISTING token, not something to smuggle in with the twin.
- src/Cobol.Net.Frontend/Generated/ is a build output and is never committed — the grammar change is not real until the solution is rebuilt. Build CobolSharp.sln (not a single project) before any --no-build run, or you will test a stale lexer and read a false green. A test battery is running as this map is written, so the rebuild must wait for it.
- The subscript-mode SUB_HEXLIT absence is REPORTED AS UNMEASURED. I did not run a probe (read-only task) and could not find one in the triage. `FUNCTION NATIONAL-OF(X"")` works because a '(' after the FUNCTION token stays in DEFAULT mode (CobolLexer.g4:105–119), and the keyword-omitted D2 form re-lexes its captured text through FunctionArgFragment with PrimeFunctionArgs (also DEFAULT mode) — so the gap may be genuinely unreachable. 'Unreachable' here is a probe to run, not a conclusion (feedback_reachability_is_measured_not_deduced).


# ===== ? =====

## sites
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:475 — BindIntrinsic dispatch → BindConvert (the ONLY caller)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:845 — BindConvert (declaration)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:851 — BindConvert — the POSITION-BLIND keyword harvest (FMT-15.19.2)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:854 — BindConvert — arity guard (counts operands/keywords, not positions)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:861 — BindConvert — source/destination format decode (the src/dst/hex codes)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:867 — BindConvert — invalid format-pair diagnostic (COBOLNET1514)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:873 — BindConvert — the §15.3 screen call (a NO-OP for CONVERT today)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:876 — BindConvert — the THREE existing syntax-rule screens (SR3 / SR8 / SR9)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:888 — BindConvert — SR1, literals only (no zero-length ITEM screen)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:892 — BindConvert — the ANY "concession" (bind-time remap, the substitute for a raw-storage channel)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:898 — BindConvert — result category (§15.19.1) + the three Convert* init-properties
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:904 — IsNationalOperand (one caller: line 896)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:968 — IsConvertFormatWord (one caller: line 851)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:1737 — KeywordWordOf — the bare-word view (callers: 685, 745, 806, 851, 982, 1035, 1053, 1295)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:1093 — CheckArgumentClasses — the PB12 per-position schema CONVERT never joined
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:1100 — CheckArgumentClasses — the TryGetValue guard that returns for CONVERT
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:1105 — CheckArgumentClasses — the per-position loop (schema.At(i))
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:933 — CheckRepertoireArgs — the EXISTING class screen of the identical shape (DISPLAY-OF/NATIONAL-OF, COBOLNET1546)
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:543 — DeliberatelyUnscreened — the CONVERT row that records the omission
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:549 — DeliberatelyUnscreened["CONVERT"]
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:126 — ArgSchema — the per-POSITION shape (no per-KEYWORD axis)
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:131 — ArgSchema.At
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:83 — ArgRule — the row shape a keyword-dependent rule must extend
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:308 — ClassOfPlace — the ONE existing USAGE-keyed arm (where r7's exclusion list would hang)
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:313 — ClassOfCategory — Table 2 (pointer/program-pointer collapse to ONE class, so r7 cannot be a class test)
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:324 — ClassOfCategory — the pointer collapse
- src/Cobol.Net.Compiler/Binding/Model/PicInfo.cs:56 — Usage enum — the axis r7 names (Index 73, ObjectReference 76, Pointer 96, ProgramPointer 99, FunctionPointer 103; NO MESSAGE-TAG member)
- src/Cobol.Net.Compiler/Binding/Bound/BoundTree.cs:234 — BoundIntrinsicCall.ConvertSource / ConvertDest / ConvertDestHex — how the renderer receives the formats
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:682 — RenderStringValue — the "Convert" arm (the ONLY reader of the three Convert* props)
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:805 — Str — the display-image channel ANY currently rides
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:815 — StrArgVisitor.Visit(BoundFieldOperand) — delegates to OperandText.AsString (DISPLAY image, not storage)
- src/Cobol.Net.Compiler/CodeGen/Emit/OperandText.cs:150 — FieldAsString — the GROUP storage-image arm (the one raw-bytes channel that exists today)
- src/Cobol.Net.Compiler/CodeGen/Emit/OperandText.cs:161 — FieldAsString — the ELEMENTARY numeric arm (FormatDisplay = digits, NOT storage bytes)
- src/Cobol.Net.Compiler/CodeGen/Emit/OperandText.cs:171 — FieldAsString — the digits produced for a BINARY/PACKED leaf in a string context
- src/Cobol.Net.Compiler/CodeGen/Emit/OperandText.cs:105 — NonTextBytes — the deliberate BYTES-ARE-NOT-TEXT decode (V59); the exact inverse of what ANY needs
- src/Cobol.Net.Compiler/CodeGen/DataDivision/GroupImageCodec.cs:80 — GroupImageCodec.EmitImageMethods — where AsImage()/FromImage() are generated
- src/Cobol.Net.Compiler/CodeGen/DataDivision/GroupImageCodec.cs:102 — GroupImageCodec.AsImageOf — the per-LEAF storage-bytes recipe an ANY channel must mirror for elementary items
- src/Cobol.Net.Compiler/CodeGen/Roslyn/RuntimeApi.cs:98 — RuntimeApi.NumFormatImage — the storage-bytes encoder for a numeric leaf
- src/Cobol.Net.Compiler/CodeGen/Roslyn/RuntimeApi.cs:757 — RuntimeApi.BitsPack — the sub-byte storage encoder
- src/Cobol.Net.Runtime/Values/Text/CobolBits.cs:39 — CobolBits.Pack — the existing trailing-zero-bit padder (high-order-first, ceil(count/8))
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:204 — CobolIntrinsics.Convert — the runtime entry (signature carries NO bit count)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:214 — Convert — the character (non-HEX) short-circuit into the ONE Repertoire translator
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:228 — Convert — the ANUM/ANY byte reduction (Latin-1 over the DISPLAY image, with a lossy substitution rule 2 does not authorize)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:236 — Convert — the ToHex call with NO §15.19.4 r2/r4 zero-bit padding step before it
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:272 — Repertoire — the shared translator (3 callers: Convert 215, DisplayOf 257, NationalOf 264)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:287 — HexDigitsToBytes — the SR4 screen with the Trim() hole (one caller: Convert 226)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:309 — ToHex — byte-array only, no bit-count parameter (one caller: Convert 236)
- src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:293 — IntrinsicCatalog CONVERT row — MinArgs 2 where the format's minimum is 3; ArgKinds "s" inert
- src/Cobol.Net.Compiler/Binding/IntrinsicResultType.cs:186 — IntrinsicResultType.Resolve — the FollowsDestinationFormat arm (defers to BindConvert)
- src/Cobol.Net.Frontend/Grammar/Core/CobolExpressions.g4:362 — functionArgument / fnArgPhraseWord — only ANY/ALPHANUMERIC/NATIONAL are tokens; NAT/ANUM/HEX/BYTE arrive as dataReference
- specs/ISO_COBOL.md:9501 — §8.10 context-sensitive words — ANUM (9501), BYTE (9513), HEX (9538), NAT (9557) are reserved ONLY in CONVERT
- specs/ISO_COBOL.md:30540 — §15.19.3 r7 — the usage exclusion list + the raw-storage licence
- specs/ISO_COBOL.md:30559 — §15.19.4 r2 — the zero-bit padding clause (ANUM HEX)
- specs/ISO_COBOL.md:30569 — §15.19.4 r4 — the zero-bit padding clause (NAT HEX, 16-bit multiple)
- tests/Cobol.Net.Tests.Unit/IntrinsicArgumentClassDriftTests.cs:187 — EveryBespokeBinder_CallsTheArgumentClassScreenItself — pins the literal call text inside BindConvert's textual body
- tests/Cobol.Net.Tests.Unit/IntrinsicArgumentClassDriftTests.cs:200 — EveryBespokeBinder_CallsTheArgumentClassScreenItself — the body boundary + exact-text assertion
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:557 — Convert_Formats_2023 — five value rows pinned
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:596 — Convert_AnyOverNational_ResolvesToNatStorage_2023 — pins the runtimeSrc remap's observable result
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:573 — Convert_SyntaxRuleViolations_1514 — SR3/SR9/SR8 negatives
- tests/conformance/2023/intrinsics_convert.cob:15 — the registered corpus golden (manifest.json line 64) — five pinned rows
- src/CobolSharp.Runtime/Intrinsics/IntrinsicFunctions.cs:885 — LEGACY "CONVERT" arm — a DIFFERENT contract (encoding names); do not confuse with the typed-native path

## currentBehavior
END-TO-END TODAY.

(1) DISPATCH. `IntrinsicBinder` line 475 routes CONVERT to `BindConvert` (IntrinsicBinder.cs:845) — the ONLY caller — before the generic path's arity check (line 501) and generic §15.3 screen (line 515). So the catalog row's `MinArgs 2 / MaxArgs 4` (IntrinsicCatalog.cs:293) is never consulted; the format's real minimum is 3 (argument-1 + source-format + destination-format) and only BindConvert's own guard applies.

(2) THE KEYWORD HARVEST IS POSITION-FREE. Line 851 loops every argument and, whenever `KeywordWordOf(a)` (line 1737) returns one of the seven words `IsConvertFormatWord` (line 969) recognizes, appends it to `kws` and skips the operand bind; everything else becomes an operand. `KeywordWordOf` returns either an `fnArgPhraseWord` token OR a bare unqualified/unsubscripted `dataReference` word uppercased. The grammar (CobolExpressions.g4:369-379) tokenizes only ANY / ALPHANUMERIC / NATIONAL — those three are §8.9 RESERVED (reserved-words.json: ANY r85+, ALPHANUMERIC r85+, NATIONAL r2002+). NAT, ANUM, HEX and BYTE are NOT reserved anywhere in the repo; they are §8.10 CONTEXT-SENSITIVE words, reserved only "CONVERT intrinsic function" (specs/ISO_COBOL.md lines 9501/9513/9538/9557), and §8.10's lead-in says a context-sensitive word used where the general format does not permit it "is treated as a user-defined word". Two measured consequences (inventory FMT-15.19.2, DIVERGES): `01 NAT PIC XX VALUE "41".` + `FUNCTION CONVERT(NAT HEX ANUM)` is REJECTED with COBOLNET1504 "0 operand + 3 format keyword(s) given"; and `CONVERT(ANUM WS-A ANUM HEX)` and `CONVERT(ANUM ANUM HEX WS-A)` are both ACCEPTED and print 41.

(3) FORMAT DECODE + STORAGE ON THE NODE. Lines 861-865 map `kws[0]` to `src` (0=ANY, 1=ANUM, 2=HEX, 3=NAT) and `kws[1]` to `dst` (1=ANUM, 3=NAT, 4=BYTE) with an optional trailing HEX → `hex`. Line 867 rejects any leftover. Lines 898-900 compute `category = dst == 3 ? National : Alphanumeric` (the §15.19.1 table) and build `new BoundIntrinsicCall(sig, operands, category) { ConvertSource = runtimeSrc, ConvertDest = dst, ConvertDestHex = hex }` — three init-properties declared at BoundTree.cs:236/240/243. `IntrinsicResultType.Resolve`'s `FollowsDestinationFormat` arm (IntrinsicResultType.cs:190) deliberately defers to BindConvert.

(4) EXISTING SCREENS — exactly four, all in BindConvert: SR3 (line 877), SR8 (line 881), SR9 (line 885), and SR1 restricted to a zero-length LITERAL (line 889). `CheckArgumentClasses(sig, operands)` is calle

## fixInventory
- POSITION-AWARE FORMAT PARSE — IntrinsicBinder.cs:849-859. Replace the `foreach` harvest with a positional walk over the §15.19.2 format `( argument-1 source-format destination-format )`, the same shape BindFindString already uses (IntrinsicBinder.cs:742-771, an explicit slot machine). Slot 0 = argument-1 and MUST bind through `BindArgOperand(argCtxs[0])` unconditionally, so a data item spelled NAT/ANUM/HEX/BYTE (context-sensitive per §8.10, specs/ISO_COBOL.md:9501/9513/9538/9557) is no longer swallowed; slots 1+ are format words. ANY/ALPHANUMERIC/NATIONAL remain unreachable as data-names (§8.9 reserved), so only the four context-sensitive words change behavior.
- REJECT MISPLACED FORMAT WORDS — IntrinsicBinder.cs:854-859 + 867-872. The positional walk must reject `CONVERT(ANUM WS-A ANUM HEX)` and `CONVERT(ANUM ANUM HEX WS-A)`, which today both compile and print 41. Fold the arity check into the walk: exactly 3 or 4 arguments (argument-1 + source-format + destination-format [+ HEX]).
- ARITY ROW — IntrinsicCatalog.cs:293. `MinArgs` is 2 where the format's minimum is 3. It is inert only because BindConvert returns above the generic arity check (IntrinsicBinder.cs:501); correct the row AND keep BindConvert's own guard, or the two disagree.
- §15.19.3 r5/r6 CLASS SCREENS — a new keyword-keyed screen invoked from BindConvert after the format decode (insert at IntrinsicBinder.cs:875, where the inert `CheckArgumentClasses(sig, operands)` already sits). r5: `src == ANUM` ⇒ argument-1 shall be class alphanumeric; r6: `src == NAT` ⇒ class national. Derive the operand class through the ONE existing classifier — `IntrinsicArgumentRules.ClassOf` (IntrinsicArgumentRules.cs:210) / `OperandCategory` (IntrinsicBinder.cs:914) — and REUSE the shape `CheckRepertoireArgs` (IntrinsicBinder.cs:933-966) already implements for DISPLAY-OF/NATIONAL-OF rather than hand-writing a third copy (feedback_one_rule_one_place; inventory AR-15.19.3-6 says this explicitly). Note r5's own NOTE — 'This is distinct from simply requiring the string to be of class alphanumeric' — so the class test is the MINIMUM, not the whole rule; record the residue.
- MODEL THE KEYWORD AXIS OR DOCUMENT WHY NOT — IntrinsicArgumentRules.cs:126/131/83. `ArgSchema` keys on ORDINAL only, which is the literal reason `DeliberatelyUnscreened["CONVERT"]` (line 549) exists. Either (a) extend `ArgSchema`/`ArgRule` with a per-call kind SELECTOR so a keyword-dependent rule is a table row like every other, and delete the DeliberatelyUnscreened entry; or (b) leave CONVERT out of `Verified` and rewrite line 549 to say the rule is enforced IN FULL by BindConvert's own screen — the disposition `HIGHEST-ALGEBRAIC`/`LOWEST-ALGEBRAIC` already use at lines 560-561. Do not leave a stale 'unscreened' note beside a landed screen.
- §15.19.3 r7 USAGE EXCLUSION — a USAGE-level predicate, which does not exist yet. Classes cannot express it: `ClassOfCategory` (IntrinsicArgumentRules.cs:324) collapses Pointer and ProgramPointer into one `CobolClass.Pointer`, and MESSAGE-TAG has no `Usage` member (PicInfo.cs:56). Add the predicate beside the one existing usage-keyed arm (`ClassOfPlace`, IntrinsicArgumentRules.cs:308) over `Place.Item.Pic.Usage` ∈ {Index, ObjectReference, Pointer, ProgramPointer, FunctionPointer} (PicInfo.cs:73/76/96/99/103), invoked from BindConvert when `src == ANY`. Build it as the SHARED usage screen — `DeliberatelyUnscreened["BASECONVERT"]` (line 547) is waiting on the same mechanism for §15.12.3 r1's display-or-national usage rule (CLAUDE.md rule 5: the shape that makes the next case automatic). MESSAGE-TAG stays unmodeled; the Usage-inventory drift test forces the decision when the usage lands.
- §15.19.3 r8/r9 COMBINATION CHECKS — IntrinsicBinder.cs:881 and 885 are already correct and exhaustively verified (inventory AR-15.19.3-8 / AR-15.19.3-9, both CONFORMS). The work is to keep them reading `src` (captured BEFORE any ANY remap) after the positional rewrite, and to add negative goldens for the source/destination combinations the position rewrite newly rejects.
- ANY RAW-STORAGE CHANNEL, COMPILER SIDE — IntrinsicRenderer.cs:682-684. `Str(ic.Args[0])` (line 805) must NOT be the ANY path. Add a storage-image renderer that mirrors `GroupImageCodec.AsImageOf` (GroupImageCodec.cs:102-116) for an ELEMENTARY place — numeric leaf → `RuntimeApi.NumFormatImage` (RuntimeApi.cs:102), USAGE BIT → `RuntimeApi.BitsPack` (RuntimeApi.cs:758), character/national leaf → its carrier — while a GROUP keeps `Read(p).AsImage()` (already the correct answer, OperandText.cs:150-153). Gate on `DataItem.IsImageCapable` (DataItem.cs:339) and stage loud for the Tier-C island rather than returning a plausible wrong image.
- ANY BIT-COUNT COMPANION — the same renderer must emit the argument's BIT count alongside the image, because r2/r4 padding is not computable from a char count. `DataItem.ByteWidth` (DataItem.cs:441) × `BitLayout.BitsPerCharacter` (BitLayout.cs:30, pinned 8) for a byte-aligned item; `Pic.Length` boolean positions for a USAGE BIT leaf (which is exactly the count `CobolBits.Pack` already takes).
- DELETE THE ANY REMAP — IntrinsicBinder.cs:892-896. Once the renderer delivers real storage bytes, `int runtimeSrc = src == 0 && IsNationalOperand(operands[0]) ? 3 : src;` and its helper `IsNationalOperand` (line 904) become a second mechanism for the same job (feedback_one_mechanism_per_job) and must go; `ConvertSource` (BoundTree.cs:236) then carries the WRITTEN source, and its doc comment's '0 = ANY' meaning is restored.
- RUNTIME SIGNATURE — CobolIntrinsics.Text.cs:207. `Convert(string arg, int src, int dst, bool dstHex)` cannot express a sub-byte argument. Add the bit count (and, for ANY, admit the raw byte image rather than a character string). The compiler-side call text is emitted at exactly ONE site (IntrinsicRenderer.cs:683), so the change has a single emit caller.
- §15.19.4 r2/r4 TRAILING ZERO-BIT PADDING — CobolIntrinsics.Text.cs:236, immediately before `ToHex(bytes)`. r2 pads to a multiple of 8 bits (one alphanumeric character), r4 to a multiple of 16 (one national character, D-N1). Reuse the padding arithmetic `CobolBits.Pack` (CobolBits.cs:39-49) already implements high-order-first rather than writing a second bit packer. ⚠ Derive the goldens from the RULE, not from §15.19.4 NOTE 3 c: the inventory shows `E0` is not obtainable from `B"101"` under either rule (r2 gives A0, r4 gives A000) — the NOTE is defective.
- HEX SOURCE TRIM HOLE — CobolIntrinsics.Text.cs:287-289. `s.Trim()` discards spaces that SR4 makes invalid digits: `PIC X(4) VALUE "  41"` + `CONVERT(WS-LEAD HEX BYTE)` returns "A" with no exception, and an all-spaces argument returns empty. The trim is needed for the fixed-width IMAGE, so the fix is a trailing-image trim plus a digits-only screen — not deleting the trim. (Shared with PB59-2, which owns the `BaseConvert` twin at CobolIntrinsics.Text.cs:176.)
- THE LOSSY ANUM→HEX SUBSTITUTION — CobolIntrinsics.Text.cs:228-232. Rule 2 carries no substitution clause, yet `bytes[i] = arg[i] <= 0xFF ? (byte)arg[i] : ByteSub()` silently replaces any code unit above 0xFF with 0x3F. This is the same unmade character-set decision PB59-1 owns for `Repertoire` (line 272-278) — sequence behind it, do not decide it twice.
- GOLDENS + MANIFEST, ONE COMMIT — extend `tests/conformance/2023/intrinsics_convert.cob` (already registered at `tests/conformance/2023/manifest.json:64`) and add spec-derived theories beside `Convert_Formats_2023` (IntrinsicFunctionDifferentialTests.cs:557): a data item named NAT as argument-1 (the FMT-15.19.2 repro), a misplaced-format-word negative, an r5 numeric-source negative, an r6 alphanumeric-under-NAT negative, an r7 pointer/index negative, `CONVERT(WS-A ANUM NAT HEX)` = 4100 (r4, no BIT usage needed), and a USAGE BIT ANY source (r2). Register any new negative in `tests/conformance/negative/manifest.json`.
- INVENTORY RE-VERDICT — flip AR-15.19.3-1/-4/-5/-6/-7, FMT-15.19.2 and RV-15.19.4-1/-2/-3/-4/-5 in `tests/version-matrix/traceability-inventory.json`, and update `kb/Work/PB59.md` status in the same change set (CLAUDE.md rule 8).

## risks
- tests/Cobol.Net.Tests.Unit/IntrinsicArgumentClassDriftTests.cs:187-205 — `EveryBespokeBinder_CallsTheArgumentClassScreenItself` reads IntrinsicBinder's SOURCE TEXT: it finds `private BoundExpr BindConvert(`, ends the body at the next `    private BoundExpr Bind`, and asserts the literal substring `CheckArgumentClasses(sig, operands)`. Extracting BindConvert's new screens into helper methods placed BETWEEN BindConvert and the next `private BoundExpr Bind…` silently truncates the body window; renaming the operand list breaks the exact-text match. Adjust the guard deliberately, never by weakening it.
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:596-601 — `Convert_AnyOverNational_ResolvesToNatStorage_2023` pins `CONVERT(N ANY ANUM HEX)` = "0041" and its comment names the SR7 reduction. The value survives a real raw-storage channel (a PIC N item's storage IS UTF-16BE), but the MECHANISM it documents — `runtimeSrc` at IntrinsicBinder.cs:896 — is being deleted. A green run here after the change proves nothing unless the test's comment and derivation are rewritten; this is the `green_gates_arent_evidence` shape.
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:557-564 — `Convert_Formats_2023` pins five values, including `CONVERT(A ANUM ANUM HEX)` = "41" and `CONVERT(N NAT ANUM HEX)` = "0041". The r2 padding step must be a NO-OP for byte-aligned sources or all five move. The `"414220"` row also pins that the fixed-width IMAGE (trailing 0x20) is what CONVERT sees — an ANY/storage channel must not accidentally change what the ANUM path receives.
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:573-584 — `Convert_SyntaxRuleViolations_1514` writes `CONVERT(A ANUM ANUM)` etc. with `A` a `PIC X` item. The positional rewrite must keep these reaching the SR3/SR8/SR9 arms rather than failing earlier with a position error, or the three negatives stop testing the rules they name.
- tests/conformance/2023/intrinsics_convert.cob + .out, registered at tests/conformance/2023/manifest.json:64 — pins AH/HA/HB/NH/RT. All five are conforming source, so the r5/r6 class screens must admit them; a screen that rejects `PIC XX VALUE "41"` as a HEX source, or `PIC N` as a NAT source, turns a green corpus red (the PB1 failure mode: 12 legal programs rejected by asserting an unaudited kind).
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:603-620 — `Convert_UntranslatableSetsDataConversion_WhenChecked_2023` pins the '?' + EC-DATA-CONVERSION behavior of `Repertoire` (CobolIntrinsics.Text.cs:272-278) for NAT→ANUM of U+0160. PB59-1 changes exactly that translator to a UTF-16 identity. Family 5 and PB59-1 collide at this one test and at CobolIntrinsics.Text.cs:228-232 — sequence them, and do not let Family 5 quietly re-decide the character-set question.
- tests/Cobol.Net.Tests.Unit/ZeroLengthLiteralArgumentDriftTests.cs:111 and :134 iterate `IntrinsicArgumentRules.Verified`. Adding a CONVERT row makes CONVERT participate in that drift test, and §15.19.3 r1 is a zero-LENGTH rule that reaches ITEMS (ANY LENGTH, DYNAMIC LENGTH, zero-occurrence ODO groups per §8.5.4) — whereas `NoZeroLengthClause` (IntrinsicArgumentRules.cs:62-82) is deliberately literal-only. Do not widen `NoZeroLengthClause` to items: that would over-reject in MAX/MIN/NATIONAL-OF/ORD-MAX/ORD-MIN/STANDARD-COMPARE, where a zero-length ITEM is legal.
- tests/Cobol.Net.Tests.Unit/IntrinsicArgumentClassDriftTests.cs:87-134 — `EveryVerifiedRule_CitesItsClause` and `EveryVerifiedRule_NamesARealFunction_WithAHandledCode` parse the `Verified` table with REGEXES over `Uniform('k', "§…")` / `Schema("§…", ['k',…])`. Introducing a third row constructor for a keyword-keyed rule makes both regexes silently match nothing for that row — the exact `green_gates_arent_evidence` trap the tests' own comments warn about ('fix the regex, do not lower the floor').
- IntrinsicCatalog.cs:293 is read by `IntrinsicResultTypeDriftTests` and `IntrinsicRealArgDriftTests`; changing MinArgs from 2 to 3 touches a row those drift tests enumerate.
- src/CobolSharp.Runtime/Intrinsics/IntrinsicFunctions.cs:885 holds a LEGACY `"CONVERT"` arm with an encoding-name contract, pinned by tests/CobolSharp.Tests.Unit/Runtime/IntrinsicFunctionTests.cs:1178 (`Call("CONVERT", { "Hello", "ASCII", "UTF-8" })`). It is not the typed-native path and must not be 'fixed' toward §15.19; the legacy differential is opt-in (COBOLSHARP_LEGACY_DIFFERENTIAL=1).
- The §15.19.2 destination-format FIGURE (specs/ISO_COBOL.md:30510-30521) uses braces throughout, which literally makes HEX mandatory in the non-BYTE branch — contradicting the §15.19.1 type table's 'ALPHANUMERIC alone' / 'NATIONAL alone' rows, §15.19.4 r1/r3, and NOTE 3 b. The transcription is FAITHFUL and the printed figure is the defective artefact (inventory FMT-15.19.2, 700-dpi verified). The compiler treats HEX as optional (IntrinsicBinder.cs:865) and is RIGHT to; do not 'fix' the parse toward the figure.
- Making the parse position-aware converts a bare NAT/ANUM/HEX/BYTE in argument-1 position from a silently-harvested keyword into an operand — which, if the name is undeclared, becomes a NEW name-resolution diagnostic on source that previously produced COBOLNET1504. Sweep the corpus for CONVERT references before landing so the change of diagnostic is intended rather than discovered.


# ===== ? =====

## sites
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:176 — CobolIntrinsics.BaseConvert — the value trim (defect arm 1 of 2)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:188 — CobolIntrinsics.BaseConvert — the fabricated zero (AR-15.12.3-2 defect 1)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:175 — CobolIntrinsics.BaseConvert — the accumulator type (AR-15.12.3-3: unbounded, so no maximum can be enforced)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:170 — CobolIntrinsics.BaseConvert — EC raise site 1 of 2 (base range, §15.12.3 r1); the return-"" convention the zero-digit raise must match
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:178 — CobolIntrinsics.BaseConvert — the digit loop + EC raise site 2 of 2 (§15.12.3 r2); line 180 is the undecided lowercase a-f admission
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:189 — CobolIntrinsics.BaseConvert — the unbounded result builder (the §15.4 returned-value half of AR-15.12.3-3)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:166 — CobolIntrinsics.BaseConvert — the doc comment that states the WRONG trim policy and must change with the code
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:289 — CobolIntrinsics.HexDigitsToBytes — the value trim (defect arm 2 of 2; AR-15.19.3-4 hole)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:294 — CobolIntrinsics.HexDigitsToBytes — the SR4 odd-count raise, and line 296 where a ZERO-digit input falls through silently
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:300 — CobolIntrinsics.HexDigitsToBytes — the non-hex-digit raise a leading space WILL reach once the trim is narrowed
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:306 — CobolIntrinsics.HexVal — the digit classifier (returns -1 for a space; already correct)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:226 — CobolIntrinsics.Convert — THE ONLY caller of HexDigitsToBytes (grepped; no other call site in src or tests)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:207 — CobolIntrinsics.Convert — entry; NO runtime zero-length (SR1) screen exists anywhere in the body (lines 207-248)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:228 — CobolIntrinsics.Convert — the ANUM/ANY byte arm, which must NOT gain a trim (a golden pins the trailing image space as content)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:328 — CobolIntrinsics.Substitute — the RUNTIME zero-length precedent the CONVERT SR1 screen should copy
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:89 — CobolIntrinsics.Ord — second runtime zero-length precedent
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Exact.cs:302 — CobolIntrinsics.Numval — ONE FUNCTION AWAY, the exact shape BaseConvert is missing: trim THEN a zero-digit raise
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:436 — CobolIntrinsics.BooleanOfInteger — the ONLY existing §15.4 implementor-maximum enforcement in the runtime; the shape BaseConvert's cap must mirror
- src/Cobol.Net.Compiler/Binding/ConcatFolder.cs:154 — ConcatFolder — the BIND-TIME 8,191 maximum precedent (§8.8.3.2 SR2-SR4)
- src/Cobol.Net.Compiler/Binding/Procedure/ExpressionBinder.cs:171 — ExpressionBinder.NationalLiteralOperand — the same 8,191 ceiling on the literal side (twin at line 184, BooleanLiteralOperand)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:889 — IntrinsicBinder.BindConvert — the COBOLNET1514 zero-length screen: literal-only, ISO 8.5.4 shape 8 of 9
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:918 — IntrinsicBinder.KnownWidth — the bind-time width channel; returns null for exactly the ref-mod / ANY LENGTH shapes 8.5.4 names, so it cannot answer the runtime shapes
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:475 — IntrinsicBinder.BindIntrinsic — THE ONLY caller of BindConvert
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:675 — IntrinsicRenderer.RenderStringValue — THE ONLY emit caller of CobolIntrinsics.BaseConvert
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:682 — IntrinsicRenderer.RenderStringValue — THE ONLY emit caller of CobolIntrinsics.Convert
- src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:291 — IntrinsicCatalog — the row binding the name BASECONVERT to RuntimeMethod "BaseConvert" (CONVERT's row is line 293)
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:547 — IntrinsicArgumentRules.DeliberatelyUnscreened — the recorded decision that BASECONVERT and CONVERT get NO generic bind-time screen
- docs/CONFORMANCE.md:323 — §7 Annex A.1 register — the table header; the maximum-length determination row (A.1 item 93, §15.4) does not exist
- docs/COBOLNET_INTRINSICS_DESIGN.md:266 — Edge cases — the NUMVAL value-path bullet; BASECONVERT/CONVERT have NO value-path entry in this doc at all
- src/CobolSharp.Runtime/Intrinsics/IntrinsicFunctions.cs:698 — LEGACY IntrinsicFunctions.Baseconvert — the second arm: same Trim() defect, long (not BigInteger), bases 2-36, dispatched at line 886
- src/CobolSharp.Runtime/Intrinsics/IntrinsicFunctions.cs:886 — LEGACY IntrinsicFunctions.Call — the only dispatch to the legacy Baseconvert (CONVERT's legacy twin is line 885, ConvertEncoding)
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:419 — BaseConvert_ReExpressesInTargetBase_2023 — PINS acc==0 returning "0" for a genuine one-digit "0"
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:561 — Convert_Formats_2023 — PINS the ANUM source's trailing image space as CONTENT (0x20 hexed); forbids a trim on the ANUM arm
- tests/conformance/2023/intrinsics_convert.cob:19 — corpus golden INTRCONV — the HEX source is PIC XX VALUE "41", EXACTLY filled, so no golden exercises a padded HEX source today
- tests/CobolSharp.Tests.Integration/ConformanceTests.cs:248 — GreenfieldOnly registry — where a new BASECONVERT/CONVERT corpus pair must be registered (intrinsics_convert is line 264)
- kb/Work/PB59.md:16 — PB59 frontmatter — spec_refs is EMPTY and must be populated by the landing change set

## currentBehavior
SPEC (all six citations mechanically validated with `python scripts/spec/cite.py --check`): §15.12.3 r2 "The characters used in argument-1 shall be digits in the range appropriate to the base indicated by argument-2"; §15.12.3 r3 "The maximum length of the input argument shall be such that neither it nor the returned value would exceed that defined by the implementor for a data item."; §15.19.3 r1 "Argument-1 shall not be of zero length."; §15.19.3 r4 "When the source-format is HEX, argument-1 shall be a valid string of hexadecimal digits of display or national usage representing complete bytes."; §15.4 "If the length of the returned value exceeds the maximum length specified by the implementor for a returned value, an EC-ARGUMENT-FUNCTION exception condition is set to exist."; §8.5.4 "A zero-length item is a data item or a literal whose minimum length is zero and whose length at runtime is zero." — and §15.3 rule 14 requires EC-ARGUMENT-FUNCTION when an argument evaluates to an incorrect value.

(1) BaseConvert (CobolIntrinsics.Text.cs:168-193). `value.Trim()` at line 176 strips leading AND trailing whitespace before the digit loop. Consequences, both measured in the 2026-08-09 triage: (a) LEADING spaces are silently discarded — BASECONVERT("  FF  ", 16, 10) returns 255 though a space is not a digit under r2 (an EMBEDDED space IS already correctly rejected, because Trim only touches the ends); (b) an all-spaces argument-1 yields ZERO digits, the loop body never runs, `acc` stays 0, and line 188's `if (acc == 0) return "0";` fabricates the answer "0" with no EC even under `>>TURN EC-ARGUMENT-FUNCTION CHECKING ON`. The guard is keyed on the accumulator VALUE, not on how many digits were consumed, so it cannot tell "no digits" from "the digit 0". Lowercase 'a'-'f' is accepted at line 180 under an adjudication the triage explicitly marked as NOT settled (§8.1.3.2 GR3a case-sensitivity vs r2's "basic-letters A to F"). Accumulator is `System.Numerics.BigInteger` (line 175) into an unbounded `StringBuilder` (lines 190-192): nothing legal is refused — PIC X(2000) ALL "F" base 16→2 returns a correct 8000-character result — but no implementor maximum exists, so §15.12.3 r3 / §15.4 have no enforcement point and the real failure mode at the .NET string limit is OutOfMemory, not an exception condition.

(2) HexDigitsToBytes (CobolIntrinsics.Text.cs:287-305), reached from exactly one site — Convert's `case HEX:` at line 226. `s.Trim()` at line 289 strips leading spaces

## fixInventory
- CobolIntrinsics.Text.cs:176 — replace `value.Trim()` with a trailing-SPACE-only trim (`value.TrimEnd(' ')`, not bare `TrimEnd()`, which also eats tab/NBSP). Leading and embedded spaces then reach the digit screen and raise at line 183. Rationale to record in the comment: the fixed-width IMAGE's trailing pad is not content; a leading space in a left-justified alphanumeric item IS content (§15.12.3 r2).
- CobolIntrinsics.Text.cs:175-188 — count the digits consumed and key the guard on the COUNT, not on `acc`. Zero digits => `Exceptions.ExceptionState.ArgumentError(...)` citing §15.12.3 r2 + §15.3, returning "" to match the two sibling raise sites at 172-173 and 183-184. One-or-more digits summing to zero must still return "0" — this is what keeps the `("\"0\", 10, 2", "0")` InlineData at IntrinsicFunctionDifferentialTests.cs:419 green.
- CobolIntrinsics.Text.cs:163-167 — rewrite the BaseConvert summary: "Leading/trailing spaces of the fixed-width argument image are ignored" becomes false the moment line 176 changes. State trailing-image-only, and state the zero-digit raise.
- CobolIntrinsics.Text.cs:180 — SETTLE the lowercase 'a'-'f' question deliberately in this change set (the triage flagged the prior adjudication as inherited-not-derived). Survey GnuCOBOL/IBM per the implementor-latitude doctrine, then either keep the lowercase arm with an explicit §15.12.3-r2-as-specific-rule comment or drop it; do not land a digit-loop edit that leaves it unstated.
- CobolIntrinsics.Text.cs:289 — `s.Trim()` => `s.TrimEnd(' ')`. Leading and embedded spaces then fall through HexVal (306-307) to the existing raise at 300-301; no new screen is needed for them.
- CobolIntrinsics.Text.cs:296 — add a zero-digit guard before `var b = new byte[t.Length / 2];`: an all-space HEX source has no digits, which violates §15.19.3 r4 (NOT SR1 — the ITEM is not zero length, so citing SR1 here would be a mis-citation). Raise EC-ARGUMENT-FUNCTION with the r4 message shape already used at 295/301.
- CobolIntrinsics.Text.cs:207 — add the runtime §15.19.3 SR1 screen at the top of `Convert` (`arg.Length == 0` => ArgumentError + ""), copying Substitute:328's shape. This is the ONLY place that can see ISO 8.5.4's eight runtime zero-length shapes; the bind-time literal screen structurally cannot.
- IntrinsicBinder.cs:889-890 — KEEP the literal screen (it is the compile-time catch for 8.5.4 shape 8) and extend its comment to name the runtime twin, so the two-arm relationship is written down. Optionally widen it through KnownWidth (918-924) for statically-zero non-literal shapes; KnownWidth returns null for ref-mod and ANY LENGTH, so it can never subsume the runtime check.
- CobolIntrinsics.Text.cs:228-232 — DO NOT add any trim to the ANUM/ANY byte arm. Only the HEX arm trims. IntrinsicFunctionDifferentialTests.cs:561 pins "414220" precisely because the ANUM image's trailing space is content.
- docs/CONFORMANCE.md §7 (table header line 323) — add the Annex A.1 item 93 row ("Function returned values — returned value length exceeds implementor-defined limits", §15.4, required + documented) stating the number, and re-run `python scripts/spec/audit_annex_a1.py --check` so the item-number cross-check re-derives it. §15.12.3 r3's data-item half has no separate A.1 number — say so in the row rather than inventing one.
- CobolIntrinsics.Text.cs:175 and 189-192 — once the maximum is DOCUMENTED, enforce it around the accumulator intake and the result builder, mirroring BooleanOfInteger:436 (`ExceptionState.ArgumentError(... §15.4)`). Order matters: CONFORMANCE.md:318-320 records that an undocumented determination and a wrongly-documented one are both non-conformance and the second is worse.
- docs/COBOLNET_INTRINSICS_DESIGN.md — add a value-path bullet beside the NUMVAL one at line 266 stating the trailing-image-only trim policy, the zero-digit raise, and the r4-vs-SR1 split for CONVERT HEX. BASECONVERT and CONVERT have no value-path entry in this doc at all (CLAUDE.md rules 2 and 6: same change set).
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs — add rows for: all-spaces BASECONVERT, leading-space BASECONVERT, leading-space HEX source, all-space HEX source, and a padded-but-valid HEX source (the positive case no golden covers today), each with checking ON (fatal EC) and OFF (the §15.3 default). Also add a corpus .cob/.out pair and register it near ConformanceTests.cs:248/264 if the frozen legacy cannot run it.
- IntrinsicArgumentRules.cs:547/549 — if any part of this lands as a BIND-time screen, amend the DeliberatelyUnscreened reasons in the same commit; leaving both the recorded 'unscreened' decision and a new screen is two mechanisms for one rule.
- kb/Work/PB59.md — flip the family-2 rows' status in the landing commit and populate the empty `spec_refs: []` with §15.12.3 r2/r3, §15.19.3 r1/r4, §15.4, §8.5.4, §15.3.
- tests/version-matrix/traceability-inventory.json — re-verdict AR-15.12.3-2, AR-15.12.3-3, AR-15.19.3-1 and AR-15.19.3-4 (all currently `state: GAP` with an EMPTY `test-ref`) and fill the test-ref with the new goldens.

## risks
- IntrinsicFunctionDifferentialTests.cs:419 `[InlineData("\"0\", 10, 2", "0")]` pins BASECONVERT("0",10,2)="0". A zero-digit guard written as `if (acc == 0) raise` turns this green test red and is the wrong fix: the discriminator is DIGITS CONSUMED, not the accumulator value.
- IntrinsicFunctionDifferentialTests.cs:561 pins CONVERT(A ANUM ANUM HEX)="414220" for `01 A PIC XXX VALUE "AB"` — the trailing image space hexed as 0x20. This is the guard against 'sweeping' the trim fix onto the ANUM arm at Text.cs:228-232.
- No golden or test exercises a PADDED HEX source: tests/conformance/2023/intrinsics_convert.cob:9 declares `01 WS-HX PIC XX VALUE "41"` — exactly filled — so TrimEnd is a no-op there and the corpus would stay green whether the trailing trim survived or not. The new behaviour is unpinned until a padded case is added (feedback_green_gates_arent_evidence).
- OperandStringChannelDriftTests.StrArgVisitor_KeepsTheDeliberatelyDifferentLeaves_Loud (tests/Cobol.Net.Tests.Unit/OperandStringChannelDriftTests.cs:85-102) PINS `Visit(BoundNumericLiteral) => Loud(n)` at IntrinsicRenderer.cs:820 as DIVERGENT BY DESIGN. So AR-15.12.3-1(a) / RV-15.12.4-1 — BASECONVERT(255, 10, 16) aborting with NotImplementedCobolFeatureException — CANNOT be fixed inside this family without deliberately amending that guard. It belongs to the per-function numeric-literal admission work, not here; touching it silently would defeat the drift test's purpose.
- The frozen legacy is a SECOND arm with the same defect: IntrinsicFunctions.Baseconvert (src/CobolSharp.Runtime/Intrinsics/IntrinsicFunctions.cs:698-711) also calls value.Trim(), uses long instead of BigInteger, and claims bases 2-36. Under COBOLSHARP_LEGACY_DIFFERENTIAL=1 it will now DISAGREE with the fixed greenfield on spaces and zero-digit input. That divergence is correct and must be attributed, not 'reconciled' — the legacy is a regression net, never authority (CLAUDE.md rule 1).
- tests/conformance/2023/intrinsics_string_2023.g.cs is an UNTRACKED build output (git ls-files lists only .cob/.out/.cpy under tests/conformance/2023), so a BaseConvert signature change breaks no code golden — but the .out byte-compare WILL catch any behaviour change, and intrinsics_string_2023.out currently pins DEC=255 / HEX=FF / BIN=A from literal arguments.
- Enforcing a maximum length is behaviour-visible on programs that work today: the triage measured PIC X(2000) ALL "F" base 16->2 returning an 8000-character result with no diagnostic. Any cap below 8000 converts a working program into an EC. Document the number in CONFORMANCE.md BEFORE enforcing it.
- `.Trim()` strips all Unicode whitespace; the COBOL fixed-width pad is specifically SPACE (U+0020). Using bare `TrimEnd()` at either site would keep silently swallowing tabs and NBSP — the same class of defect, one character narrower. Both sites need `TrimEnd(' ')`.
- After TrimEnd, an all-space HEX source becomes zero-LENGTH inside HexDigitsToBytes while argument-1 itself is NOT zero length. Raising that under the SR1 message would be a mis-citation of the kind feedback_a_real_clause_can_answer_a_different_question names; the governing rule is §15.19.3 r4.
- IntrinsicArgumentRules.DeliberatelyUnscreened (lines 546-549) records BASECONVERT and CONVERT as intentionally carrying NO generic bind-time screen. Any bind-time addition without amending those rows leaves two mechanisms for one rule, the exact drift the file's own remarks warn about.
- AR-15.12.3-2's lowercase 'a'-'f' question is an OPEN sub-decision inside this family, not settled background. The triage explicitly corrected the prior adjudication and demanded a deliberate re-derivation; a fix that edits the digit loop at Text.cs:178-180 and leaves line 180 untouched silently ratifies an unadjudicated behaviour.
- Callers enumerated (grepped, not assumed): HexDigitsToBytes has EXACTLY ONE caller, CobolIntrinsics.Text.cs:226. CobolIntrinsics.BaseConvert has exactly one emit site, IntrinsicRenderer.cs:675-677 (keyed by IntrinsicCatalog.cs:291). CobolIntrinsics.Convert has exactly one emit site, IntrinsicRenderer.cs:682-684 (IntrinsicCatalog.cs:293). BindConvert has exactly one caller, IntrinsicBinder.cs:475. Repertoire has THREE callers — Text.cs:215 (Convert), :257 (DisplayOf), :264 (NationalOf) — which belong to Family 1, but note that Convert.cs line 214-215 short-circuits to Repertoire BEFORE the byte path, so a character-to-character CONVERT never reaches HexDigitsToBytes at all.


# ===== ? =====

## sites
- docs/CONFORMANCE.md:342 — §7 Annex A.1 register — item 188 row (the UTF-16 repertoire determination)
- docs/CONFORMANCE.md:323 — §7 register table header (the 3-column row shape an item-33 row must join)
- docs/CONFORMANCE.md:340 — §7 neighbor row #1 (shape example — item 22, CHAR-NATIONAL, same repertoire family)
- docs/CONFORMANCE.md:341 — §7 neighbor row #2 (shape example — item 180, SMALLEST-ALGEBRAIC)
- docs/CONFORMANCE.md:347 — §7 register — LAST table row (the append point; rows are in landing order, not item order; file ends at line 348)
- docs/CONFORMANCE.md:300 — §7 preamble — the MEASURED coverage sentence + the discharged-item list (must gain 33)
- specs/ISO_COBOL.md:34283 — Annex A.1 item 33 — exact text (the obligation the row discharges)
- specs/ISO_COBOL.md:30870 — §15.26.4 r1 — DISPLAY-OF returned value rule 1 (one of the two rules A.1-33 names)
- specs/ISO_COBOL.md:30874 — §15.26.4 r3 — DISPLAY-OF returned value rule 3 (the substitution-character + EC-DATA-CONVERSION half)
- specs/ISO_COBOL.md:32519 — §15.66.4 r1 — NATIONAL-OF returned value rule 1
- specs/ISO_COBOL.md:32523 — §15.66.4 r3 — NATIONAL-OF returned value rule 3
- specs/ISO_COBOL.md:30453 — §15.18.4 r3 — the ALPHABETIC half of CONCAT's result type (cite.py --check 15.18.4 → OK, verified this session)
- specs/ISO_COBOL.md:30423 — §15.18.1 result-type table — the row the model cannot express
- specs/ISO_COBOL.md:29500 — §15.2 Types of functions — SIX types, and ALPHABETIC is not one of them
- docs/COBOLNET_INTRINSICS_DESIGN.md:33 — SPINE 1 paragraph — the ONLY place the deep-dive names DISPLAY-OF/NATIONAL-OF/CHAR (CONVERT is absent from it)
- docs/COBOLNET_INTRINSICS_DESIGN.md:234 — Hard-problems result-rule table — the ONLY CONVERT row in the deep-dive
- docs/COBOLNET_INTRINSICS_DESIGN.md:233 — Hard-problems result-rule table — the CONCAT row (no mention of the ALPHABETIC output row)
- docs/COBOLNET_INTRINSICS_DESIGN.md:207 — Hard problems — the closest thing to a repertoire section (it is about HIGH/LOW-VALUE and collating, not about the alphanumeric↔national correspondence)
- docs/COBOLNET_DESIGN.md:52 — §0.5 deep-dive table — the intrinsics subsystem's canonical doc is NOT under docs/rearchitecture/
- docs/COBOLNET_DATA_MODEL_DESIGN.md:186 — D-N4 repertoire — the design statement that contradicts a UTF-16 identity correspondence
- docs/PHASE4_RECONCILIATION.md:713 — D-N4 Repertoire (the second copy of the same decision)
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:272 — CobolIntrinsics.Repertoire — the ONE site implementing the A.1-33 correspondence
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:266 — Repertoire doc comment — the prose the item-33 row must agree with
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:197 — CONVERT char-set model block comment — the second prose statement of the same correspondence
- src/Cobol.Net.Compiler/Binding/Bound/BoundTree.cs:182 — BoundIntrinsicCall — ResultCategory is a positional PicCategory parameter (the channel that cannot say ALPHABETIC)
- src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:10 — IntrinsicType — the §15.2 six-type enum; no Alphabetic member
- src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:71 — IntrinsicSig.ResultCategory — the DECLARED-type category (read only when no call is in hand)
- src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:85 — IntrinsicSig.CategoryOf — THE §15.2-type → §8.4.2-category mapping, in one place
- src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:292 — The CONCAT catalog row
- src/Cobol.Net.Compiler/Binding/IntrinsicResultType.cs:183 — IntrinsicResultType.Resolve — the FollowsConcatArguments arm (implements r2 only; r3's alphabetic half is absent)
- src/Cobol.Net.Compiler/Binding/IntrinsicResultType.cs:278 — IntrinsicResultType.OperandCategory — the classifier every result rule reads (returns PicCategory, so PIC A is invisible)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:587 — BindIntrinsicCore — THE ONE result-category resolution (where an alphabetic rider would be set)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:649 — BindIntrinsicCore — the generic BoundIntrinsicCall construction (the node CONCAT takes)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:933 — CheckRepertoireArgs — the DISPLAY-OF/NATIONAL-OF bind screen (the item-33 pair's compile-time gate)
- src/Cobol.Net.Compiler/Binding/Model/PicInfo.cs:14 — PicCategory.Alphanumeric — the deliberate fold of PIC A into PIC X
- src/Cobol.Net.Compiler/Binding/Model/PicInfo.cs:205 — PicInfo.IsAlphabetic — the existing alphabetic-ness carrier at the ITEM end
- src/Cobol.Net.Compiler/Binding/PictureAnalyzer.cs:286 — PictureAnalyzer — the ONE site that sets IsAlphabetic
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/MoveBinder.cs:177 — MoveBinder.MoveCategoryLegality — senderIsAlphabetic (BoundFieldOperand ONLY; no intrinsic arm)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/MoveBinder.cs:167 — MoveBinder.MoveCategoryLegality — the sender-category arm that DOES read the intrinsic result (the other half of the same dispatch)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/MoveBinder.cs:214 — MoveBinder — the Table16 call site (both Table16Operands built here)
- src/Cobol.Net.Compiler/Binding/MoveTable16.cs:16 — Table16Operand — the alphabetic axis already exists at the TABLE end
- src/Cobol.Net.Compiler/Binding/MoveTable16.cs:62 — MoveTable16.Refusal — the BOOLEAN column arm that fires for an alphabetic sender (the observable defect)
- src/Cobol.Net.Compiler/Binding/MoveTable16.cs:20 — Table16Operand.Of(DataItem) — the SECOND constructor of a Table-16 position, DataItem-only (an intrinsic result cannot reach it)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/OoBinder.cs:744 — OoBinder.OoContentMismatch — the SECOND MoveTable16.Refusal caller (§14.8.2.3.3 rule 2d, INVOKE BY CONTENT)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/OoBinder.cs:599 — OoBinder — a THIRD hand-written Table-16 boolean arm reading IsAlphabetic directly
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/InitializeBinder.cs:239 — InitializeBinder.InitializeItemCategory — the other IsAlphabetic consumer (receiver-side only)
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:15 — CobolClass — the remark that must be overturned if an alphabetic result class is introduced
- src/Cobol.Net.Compiler/Binding/IntrinsicArgumentRules.cs:273 — IntrinsicArgumentRules.ClassOf1 — ResultCategory consumer (a nested CONCAT's class)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/StringUnstringBinder.cs:260 — StringUnstringBinder.UnstringSenderCategory — ResultCategory consumer
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:135 — IntrinsicBinder.ResultRefMod — ResultCategory consumer (§8.4.3.3.3 SR2 admissibility)
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:1146 — IntrinsicBinder.IsStringOperand — ResultCategory consumer
- src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:1244 — IntrinsicBinder.BindLengthFold — ResultCategory consumer
- src/Cobol.Net.Compiler/CodeGen/Emit/OperandText.cs:42 — OperandText.AsString — ResultCategory consumer (the string render channel)
- src/Cobol.Net.Compiler/CodeGen/Emit/OperandText.cs:239 — OperandText — ResultCategory consumer (is-this-text visitor)
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:56 — IntrinsicRenderer.RenderNum — ResultCategory consumer (loud guard)
- src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:817 — IntrinsicRenderer.StrArgVisitor — ResultCategory consumer (nested string argument)
- src/Cobol.Net.Compiler/Binding/Model/Place.cs:254 — RefModPlace.CategoryOf doc — the claim that ref-mod PRESERVES alphabetic-ness (§8.4.3.3.4 GR2 says it does not)
- specs/ISO_COBOL.md:6171 — §8.4.3.3.4 GR2 — a ref-modified PIC A item is class/category ALPHANUMERIC (contradicts Place.cs:254 and MoveBinder:177/217)
- scripts/spec/audit_annex_a1.py:70 — section7_rows — the parser a new item-33 row must satisfy (2+ pipe cells; cell 0 is the item key)
- scripts/spec/audit_annex_a1.py:50 — corresponds() — the identity test: a shared DOTTED CLAUSE, or ≥2 non-stopword overlaps with the A.1 subject
- docs/DOC_INDEX.md:113 — audit_annex_a1.py index row — ALREADY STALE (says 10/199, 189 remain; CONFORMANCE.md says 19/199, 180 remain)
- tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:404 — Concat_ConcatenatesArgumentImages_2023 — the only landed CONCAT behavior test (PIC X only, so the alphabetic flip does not touch it)
- tests/Cobol.Net.Tests.Unit/IntrinsicResultTypeDriftTests.cs:57 — SpecArgumentDependentFunctions — re-derives the population from §15.x.1 "Function type" tables
- kb/Work/PB59.md:45 — PB59 — the 28 rows this note owns, RV-15.18.4-3 among them

## currentBehavior
DOCS. docs/CONFORMANCE.md §7 (\"Annex A.1 — implementor-defined language element register\", heading at line 291, table header at 323, rows 325–347, file ends 348) carries 23 rows in LANDING order, not item order, so a new row is APPENDED after line 347. Each row is exactly three pipe cells: `| <A.1 item number> | **<bolded element paraphrase>**, <§ citations>, required + documented | <the determination prose, naming the implementing symbols and the pinning golden/test> |`. Item 188 (line 342) is the UTF-16 repertoire determination and is the nearest neighbour in substance: it declares the native alphanumeric AND national repertoires to be UTF-16, one code unit per character position (the D-N1 substrate), the UTF-16 phrase correspondence to be the BMP identity, and UCS-4/UTF-8 to be coded character sets only. There is NO item-33 row — the alphanumeric↔national CORRESPONDENCE (which item 188 does not answer; it answers the ALPHABET-clause phrase correspondence, a different question) is undocumented, while the compiler implements one. The §7 preamble at lines 293–321 hand-writes the coverage arithmetic (\"discharges 19 of the 199\", the item list at 301–303, \"180 obligations remain\"); docs/DOC_INDEX.md:113 states a DIFFERENT, older count (10 of 199, 189 remain) — the two already disagree.

SPEC. Annex A.1 item 33 (specs/ISO_COBOL.md:34283) is REQUIRED and REQUIRES DOCUMENTATION, and cross-references four places: §14.9.25 MOVE GR6, §8.8.4.2.11 locale-based comparison, §15.26 DISPLAY-OF returned value rules 1 and 3, §15.66 NATIONAL-OF returned value rules 1 and 3. The determination it demands is implemented in exactly one runtime method, `CobolIntrinsics.Repertoire` (src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:272), shared by DISPLAY-OF, NATIONAL-OF and CONVERT: ANUM→NAT is the identity, NAT→ANUM is the identity only for code units ≤ 0xFF and otherwise substitutes ('?' + EC-DATA-CONVERSION, or the caller's argument-2 with no EC). That 8-bit Latin-1 cut is the correspondence item 33 is asking about, and it CONTRADICTS item 188's declared UTF-16 repertoire at exactly one expression.

DESIGN DOCS. There is NO docs/rearchitecture/DESIGN-*.md for the intrinsics subsystem. docs/COBOLNET_DESIGN.md §0.5 (line 52) names `docs/COBOLNET_INTRINSICS_DESIGN.md` as the §12 deep-dive, and that doc is the SSOT. It says almost nothing about this family: line 33 lists NATIONAL-OF/DISPLAY-OF/CHAR/CHAR-NATIONAL among the functions returning `string`; the result-rule

## fixInventory
- docs/CONFORMANCE.md — APPEND the item-33 row after line 347 (the table's last row; the file ends at 348). Three cells, matching rows 340/341/342: `| 33 | **Computer's coded character set (correspondence between alphanumeric and national characters)**, §15.26.4 r1/r3 + §15.66.4 r1/r3 + §14.9.25 GR6 + §8.8.4.2.11, required + documented | <determination> |`. The Element cell MUST carry at least one of the dotted clauses A.1-33 itself cites (14.9.25 / 8.8.4.2.11 / 15.26 / 15.66) — audit_annex_a1.py:60 keys identity on clause intersection first, and a row citing only §15.19 (CONVERT) would fail the fallback word test against the A.1-33 subject.
- docs/CONFORMANCE.md — the determination text must state, in one place: which alphanumeric code points have national correspondents and which national code points have alphanumeric ones (today: NAT→ANUM iff ≤ U+00FF), the implementor-defined substitution character ('?'), that argument-2 suppresses the EC while its absence sets EC-DATA-CONVERSION, and that DISPLAY-OF, NATIONAL-OF and CONVERT's character pathway all read the SAME correspondence (`CobolIntrinsics.Repertoire`). It must also reconcile with item 188 (line 342) explicitly — 188 declares a UTF-16 repertoire, 33 declares the correspondence WITHIN it, and the 0xFF cut is the only place the two can appear to disagree.
- docs/CONFORMANCE.md:300–307 — the §7 preamble: add **33** to the numbered item list at lines 301–303, bump 'discharges 19 of the 199' to 20, and take the remaining count from a fresh `python scripts/spec/audit_annex_a1.py` run rather than decrementing by hand (the preamble says the script owns the count).
- docs/DOC_INDEX.md:113 — already stale ('10 of 199 obligations discharged; 189 remain'); update to the audit's current output. Also extend line 83's summary of what CONFORMANCE.md now carries with the repertoire-correspondence family.
- docs/COBOLNET_INTRINSICS_DESIGN.md — add the MISSING subsystem coverage: a Decisions entry (D9) or a Hard-problems section for the repertoire family covering CONVERT §15.19 (the ANY/ANUM/HEX/NAT/BYTE source–destination matrix, the byte serialization, the bit padding), DISPLAY-OF §15.26, NATIONAL-OF §15.66 and CHAR §15.15 / CHAR-NATIONAL §15.16. Today the doc mentions them only at line 33 (a name in a list), 207 (a HIGH/LOW-VALUE paragraph), 233/234 (two result-rule rows) and 253 (phrase-keyword argument classification). This is the canonical intrinsics doc per docs/COBOLNET_DESIGN.md §0.5 line 52 — extend it, do NOT create a docs/rearchitecture/DESIGN-intrinsics.md.
- docs/COBOLNET_INTRINSICS_DESIGN.md:233 — the CONCAT result-rule row and the D2 narrative (lines 47–59) must gain §15.18.4 r3's ALPHABETIC output row and say how the model carries it, since IntrinsicResultType's own comment (lines 176–182) already documents which §15.18.1 rows are unreachable and this one is not among them.
- docs/COBOLNET_DATA_MODEL_DESIGN.md:186–188 (D-N4) and docs/PHASE4_RECONCILIATION.md:713–716 (D-N4 Repertoire) — both assert 'NAT→ANUM keeps/remains the Latin-1 subset identity'. If the fix widens the correspondence, BOTH copies change in the same change set; if it does not, both must be cross-referenced from the new item-33 row so the determination has one home.
- src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Text.cs:272–278 — `Repertoire`'s `toNational || c <= 0xFF ? c : sub ?? Sub()`: the single expression the item-33 row documents. Its doc comment (266–271), the DisplayOf/NationalOf comments (250–264) and the CONVERT char-set block comment (197–202) are three more prose statements of the same rule and must all agree with the new row.
- ALPHABETIC CHANNEL — the carrier: add an alphabetic rider to `BoundIntrinsicCall` (BoundTree.cs:182), NOT a `PicCategory.Alphabetic` member (the RV row says 'a result-category channel change, not a PicCategory rewrite', and PicInfo.cs:14 + IntrinsicArgumentRules.cs:15 both document the fold as deliberate). Shape it like the existing `Collate`/`CollateNat`/`RefMod` riders so no downstream `ResultCategory` pattern match stops matching.
- ALPHABETIC CHANNEL — the resolver: `IntrinsicResultType.Resolve`'s `FollowsConcatArguments` arm (IntrinsicResultType.cs:183–184) must implement r3, which needs BOTH the class of every argument AND argument-1's USAGE ('if argument-1 is usage display'). `OperandCategory` (line 278) returns only `PicCategory`, so it cannot see PIC A — the rule needs a companion reader over `PicInfo.IsAlphabetic` / `PicInfo.Usage` for every argument, and must fail soft (a literal, a figurative, a group ⇒ not provably alphabetic ⇒ alphanumeric).
- ALPHABETIC CHANNEL — the setter: IntrinsicBinder.cs:587/649 is the generic path CONCAT takes. Audit the twelve bespoke `new BoundIntrinsicCall(...)` sites (IntrinsicBinder.cs:670, 712, 784, 834, 899, 1002, 1070, 1186, 1194, 1204, 1245, 1360) — D2's own note (design doc line 53) records that TRIM/SUBSTITUTE were left wrong once for exactly this reason.
- ALPHABETIC CHANNEL — the consumer: MoveBinder.cs:177 `senderIsAlphabetic` gains a `BoundComputedOperand { Expr: BoundIntrinsicCall ic }` arm reading the new rider, mirroring line 167. Nothing in MoveTable16 changes — `Table16Operand.IsAlphabetic` (MoveTable16.cs:16) and the boolean-column arm (62–67) already implement the rule.
- ALPHABETIC CHANNEL — the second Table-16 caller: OoBinder.cs:744 builds both operands with `Table16Operand.Of(DataItem)` (MoveTable16.cs:20), which is DataItem-only. Verify whether an INVOKE BY CONTENT argument can be a function-identifier; if it can, that arm needs the same rider or it is the two-arm defect again. OoBinder.cs:599–600 is a THIRD hand-written boolean/alphabetic arm to check.
- REF-MOD ARM (sibling, same channel): MoveBinder.cs:177 and 217 read `.Place.Item.Pic.IsAlphabetic`, and `RefModPlace.Item` returns the INNER item (Place.cs:37), while the category arms (162/143) correctly route a RefModPlace through `RefModPlace.CategoryOf`. §8.4.3.3.4 GR2 (spec line 6171) redefines a ref-modified usage-DISPLAY non-alphanumeric item as class and category ALPHANUMERIC, so `MOVE A-ITEM(1:2) TO BOOL-ITEM` is refused today and should be admitted. Place.cs:254–256's claim that 'GR6's base case preserves both' needs re-deriving against GR2 in the same change set.
- TESTS/GOLDENS: a golden pinning BOTH directions of §15.18.4 r3 — `MOVE FUNCTION CONCAT(picA-1 picA-2) TO boolean-item` rejected COBOLNET0819, and `MOVE FUNCTION CONCAT(picX-1 picX-2) TO boolean-item` still admitted — plus a unit assertion that the rider is set/cleared at bind time. `IntrinsicResultTypeDriftTests` (tests/Cobol.Net.Tests.Unit/IntrinsicResultTypeDriftTests.cs:57) re-derives the population from the spec and has an `EveryResultRule_IsHandledByTheResolver` guard: any new rule member must be handled there.
- REGISTER + INVENTORY: flip RV-15.18.4-3's `verdict`/`state`/`test-ref` in tests/version-matrix/traceability-inventory.json in the landing commit, and update kb/Work/PB59.md (status, and the 28-row list at lines 43–47) — PB59 owns RV-15.18.4-3 alongside RV-15.26.4-1/-3, RV-15.66.4-1/-2/-3 and the CONVERT rows, so the item-33 documentation and the alphabetic channel land against the same note.

## risks
- MAKING CONCAT ALPHABETIC IS A NEW REJECTION, not a new acceptance: MoveTable16.cs:62–67 refuses an alphabetic sender into a boolean receiver, so any program that today moves `FUNCTION CONCAT(picA picA)` into a boolean item stops compiling. `Concat_ConcatenatesArgumentImages_2023` (tests/Cobol.Net.Tests.Conformance/IntrinsicFunctionDifferentialTests.cs:404) uses PIC X and PIC X literals and is unaffected, but the full Conformance suite, the NIST corpus and the GnuCOBOL differential must be swept for PIC A arguments to CONCAT.
- audit_annex_a1.py IS A GATE (`--check`) and its `corresponds()` (line 50) accepts a row only if the Element cell shares a dotted clause with A.1-33's text, or overlaps its subject by ≥2 non-stopwords. A row headed with §15.19 (CONVERT) alone — the function whose code actually implements the correspondence — satisfies NEITHER, because A.1-33 never cites 15.19. Cite 15.26 and 15.66.
- THE COVERAGE COUNTS ARE HAND-MAINTAINED AND ALREADY DIVERGENT: docs/CONFORMANCE.md:300–307 says 19 of 199 discharged / 180 remain; docs/DOC_INDEX.md:113 says 10 of 199 / 189 remain. Both are prose, neither is generated. Update from a fresh script run, not by editing one of them.
- ITEM 188 (line 342) IS AN ADJACENT DETERMINATION THAT LOOKS LIKE THIS ONE. It answers §12.3.7.4 GR7 — the ALPHABET clause's UCS-4/UTF-8/UTF-16 phrase correspondence — not the alphanumeric↔national character correspondence. Filing the new determination as an edit to 188, or citing §12.3.7.4 in the item-33 row, repeats the exact failure the §7 preamble records at lines 317–321 (the §15.3.3.2 determination filed under item 87 instead of 202).
- `IntrinsicArgumentRules.cs:15–17` and `PicInfo.cs:14` are WRITTEN-DOWN DECISIONS against an Alphabetic member ('inventing a class the model cannot produce would make the §15.3 type-1 arm dead code that reads as coverage'). A fix that adds one without overturning both remarks leaves the codebase asserting two opposite things; a fix that adds a rider must say why the rider is not the same mistake.
- §15.2 (spec 29500) HAS NO ALPHABETIC FUNCTION TYPE, yet §15.18.1's table (spec 30423) names one. `IntrinsicType` is deliberately the §15.2 six. Adding an `IntrinsicType.Alphabetic` changes the meaning of `IntrinsicSig.CategoryOf` (IntrinsicCatalog.cs:85) — whose `_ => PicCategory.Numeric` catch-all would silently make an alphabetic function NUMERIC — and of `IntrinsicResultTypeDriftTests`' population derivation.
- ELEVEN OTHER SITES PATTERN-MATCH `ResultCategory` (IntrinsicBinder.cs:135/1146/1244, MoveBinder.cs:167, StringUnstringBinder.cs:260, IntrinsicArgumentRules.cs:273, IntrinsicResultType.cs:283, OperandText.cs:42/51/239, IntrinsicRenderer.cs:56/817). BoundTree.cs:196–203 records that a WRAPPER node around BoundIntrinsicCall silently stopped matching at every one of them and dropped a ref-mod — the same hazard applies to any restructuring of the result-category channel; a rider is the shape that survived.
- `Repertoire` IS SHARED BY THREE FUNCTIONS (CobolIntrinsics.Text.cs:215, 257, 264). Changing the correspondence to document item 33 changes CONVERT §15.19.4 r1/r3 at the same time — and PB59 owns nine separate CONVERT inventory rows (RV-15.19.4-1..-5, AR-15.19.3-*) whose verdicts are keyed to the current behaviour.
- §15.18.4 r3's condition is on USAGE as well as class ('if argument-1 is usage display'). IntrinsicResultType.cs:176–182 documents that the two `usage National` rows of the §15.18.1 table are unreachable because `PIC 9/1 USAGE NATIONAL` stages loud at the DATA DIVISION (COBOLNET0899). Whatever reads usage for r3 must not become a second dead lookup that reads as coverage.
- DOCS-CURRENT-STATE RULE: docs/CONFORMANCE.md, docs/COBOLNET_INTRINSICS_DESIGN.md, docs/COBOLNET_DATA_MODEL_DESIGN.md and docs/DOC_INDEX.md all describe the CURRENT compiler and must change in the SAME commit; docs/PHASE4_RECONCILIATION.md:713 holds a second copy of D-N4, so leaving it behind recreates the two-answers-to-one-question shape the project has already paid for.
