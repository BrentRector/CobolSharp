# PHASE-13 comprehensive plan-vs-spec review — findings ledger

> **STATUS: LIVE LEDGER (2026-07-19) — the disposition lines are the to-do state; update them as items land.**
> Review of the rearchitecture plan + implementation at HEAD `38c4a669` (branch `phase-13-m4-2023`) against
> `specs/ISO_COBOL.md` (ISO/IEC 1989:2023). Methodology: a 16-finder multi-agent Workflow across three axes —
> **A** spec-coverage omissions (9 finders by spec area), **B** plan-vs-implementation claim verification
> (3 finders), **C** citation drift in landed P13 code (4 finders) — each finding then adversarially verified
> by two independent lenses (spec re-read + code re-read) with a tiebreak judge. 184 agents launched,
> 85 completed (~7.7M tokens); a session-limit outage killed 99 agents mid-run, so coverage is PARTIAL —
> see section 5.

## 1. Executive summary

- **28 findings CONFIRMED** by both adversarial lenses: 1 critical, 14 major, 13 minor
  (26 unique after dedup — the COBOLNET1573 collision and the CONFORMANCE.md '04' false claim were each
  independently found by two finders).
- **37 findings UNVERIFIED** — plausible finder reports whose verification agents died on the session limit.
  Each needs a verify pass (folded into Wave I) before being treated as established.
- **3 finders never ran**: A3 (ISO §9 I-O concepts + §12 Environment Division sweep), A4 (ISO §13 Data
  Division clause sweep), C4 (independent Wave C grammar-construct re-derivation). These spec areas are
  UNREVIEWED by this pass.
- **The one critical finding**: diagnostic code **COBOLNET1573 is shipped with two meanings** — the
  `>>REF-MOD-ZERO-LENGTH` malformed-operand error (frontend, BARE string literal, DEVLOG 897) and the Wave E
  external-FILE-STATUS-consistency error (catalog-registered, DEVLOG 905). Root cause: the frontend emit
  bypasses `DiagnosticCatalog`, so catalog-only allocation could not see the earlier claim, and the
  registry drift tests scan only the catalog.
- **Recurring theme (the P10–P13 standing lesson again)**: §-citations drift — 6 confirmed wrong-§ or
  wrong-annex citations shipped in otherwise-correct code; every one was caught by re-reading the spec text,
  none by any test.
- The corrected P13 audit tally at HEAD (finder B1 — itself unverified): 71 rows = 53 done/dispositioned ·
  16 tracked in the remaining-P13 worklists · 1 fallen-through (DB-series NIST goldens) · 1 N/A.

## 2. Confirmed findings (verified by both lenses)

### C1. [CRITICAL | doc-inconsistency | B3-ledger-consistency] COBOLNET1573 is a genuine code collision: two shipped diagnostics share one ID (REF-MOD-ZERO-LENGTH malformed operand vs external FILE STATUS consistency)

- **Refs:** ISO §7.3.23.2 (>>REF-MOD-ZERO-LENGTH {ON|OFF}) vs §12.4.5.3 GR1(i)/§14.8.4.2; E:\CobolSharp\src\Cobol.Net.Frontend\Preprocessor\RefModZeroLengthDirectiveProcessor.cs:64; E:\CobolSharp\src\Cobol.Net.Editions\Diagnostics\DiagnosticCatalog.cs:426; E:\CobolSharp\src\Cobol.Net.Compiler\Binding\BinderDriver.cs:162; E:\CobolSharp\resume-prompt.md:150,159,167; E:\CobolSharp\docs\DIAGNOSTICS.md:94; DEVLOG Entry 897 + Entry 905
- **Evidence:** Not a doc error — both resume-prompt claims describe real code. RefModZeroLengthDirectiveProcessor.cs:64 emits diagnostics.ReportError("COBOLNET1573", ">>REF-MOD-ZERO-LENGTH expects the ON or OFF phrase (ISO §7.3.23.2)...") as a BARE string literal (DEVLOG 897: '1573 is the next unreserved. Verified before commit.'). DiagnosticCatalog.cs:426 later declared COBOLNET1573 = external-file-status-consistency (§12.4.5.3 GR1(i)), emitted at BinderDriver.cs:162 (DEVLOG 905: 'Two diagnostics: COBOLNET1573 ... + COBOLNET1575'). Wave E's allocation saw only the catalog, where the highest descriptor was 1574 and the frontend's bare-literal 1573 was invisible — DIAGNOSTICS.md (generated from the catalog only) lists 1573 solely as external-file-status-consistency, so DiagnosticRegistryDriftTests cannot detect the collision. One user-visible ID now has two unrelated meanings across editions.
- **Proposed action:** Renumber the RefMod malformed-operand diagnostic to the next free code (COBOLNET1576 — verified free in src) and register it as a DiagnosticDescriptor in DiagnosticCatalog so allocation stays catalog-visible; regenerate DIAGNOSTICS.md; fix resume-prompt.md:159 and the line-167 band ledger; add a negative test asserting the new code (today NO test asserts the RefMod 1573, so this side is the cheap renumber — the Wave E 1573 keeps its golden external-file-status-consistency-at-2023.err); DEVLOG entry recording the collision + resolution.
- **Disposition:** _pending_

### C2. [MAJOR | impl-vs-spec-drift | A1-directives] >>DEFINE AS PARAMETER and compile-time arithmetic/boolean expressions silently produce WRONG compilation-variable values

- **Refs:** ISO §7.3.11.2 format + GR4/GR6 (specs/ISO_COBOL.md:4059-4077, 4113-4119), §7.3.6 compile-time arithmetic (3794-3831), §7.3.7 (3834-3860), §7.3.13.2 Format-1 operands (4212-4236); src/Cobol.Net.Frontend/Preprocessor/ConditionalCompilationProcessor.cs:232-248 (ApplyDefine), :169-173 (ResolveFirst), :20-22; docs/PHASE4_RECONCILIATION.md:1472 (M2-PRE-2 'Deferred … no named track')
- **Evidence:** §7.3.11 GR4: with PARAMETER the value comes from the operating environment, and 'If no value is made available … compilation-variable-name-1 is not defined.' ApplyDefine has no PARAMETER branch: `>>DEFINE X AS PARAMETER` falls into `defines[name] = Value.FromToken(v)` and X becomes the alphanumeric string "PARAMETER". §7.3.11 GR6/§7.3.6 require expression evaluation: `>>DEFINE X AS 1 + 2` takes only the first token (X=1; the tokenizer silently skips '+'), and `>>EVALUATE 1 + 1` resolves the subject to 1 via ResolveFirst. No §7.3.6/§7.3.7 evaluator exists anywhere; PHASE4_RECONCILIATION admits the residue with 'no named track', and it appears in no live P13/rearchitecture list. Also unflagged: §7.3.11.3 SR2 (redefinition without OVERRIDE) is silently allowed (line 247 'we always override').
- **Proposed action:** Interim (same-day fix): make ApplyDefine reject-loudly any DEFINE/EVALUATE operand it cannot represent (PARAMETER, multi-token expressions) instead of silently binding a wrong value — a COBOLNET diagnostic, not a silent literal. Full fix (add a named track, e.g. fold into P13 Wave D or the forward-residue ledger): implement §7.3.6/§7.3.7 compile-time expression evaluation (needed by DEFINE, EVALUATE Format 1, the Wave-D >>DISPLAY operands, and §7.3.8.2 SR2 CCE operands), AS PARAMETER sourced from a CLI/-D environment channel with GR4 not-defined fallback, and the SR2 OVERRIDE check.
- **Verifier correction:** >>DEFINE AS PARAMETER and expression operands in DEFINE/EVALUATE directives silently produce WRONG compilation-variable values. Verified against the spec: §7.3.11.2 lists PARAMETER as a format alternative and GR4 (specs/ISO_COBOL.md:4113) requires the value to come from the operating environment, with "compilation-variable-name-1 is not defined" as the no-value fallback — ApplyDefine (ConditionalCompilationProcessor.cs:232-248) has no PARAMETER branch, so X becomes the alphanumeric string "PARAMETER" and 'X IS DEFINED' wrongly evaluates true. GR6/GR7 (:4117-4119) require §7.3.6/§7.3.7 expression evaluation — '>>DEFINE X AS 1 + 2' binds X=1 (the tokenizer skips the spaced '+' at :410), and '>>EVALUATE 1 + 1' resolves the subject to 1 via ResolveFirst (:169-173); §7.3.13.2 Format 1 (:4212-4241) permits expressions for the subject, WHEN, and THRU operands. §7.3.11.3 SR2 redefinition-without-OVERRIDE is also unchecked (:247). CORRECTION to the finding: a §7.3.6 compile-time arithmetic evaluator DOES exist — DataBinder.Constants.cs:283-393 (P10 Step 15, CONSTANT entries §13.10.4 GR4, with SR1a/SR1b/SR1c and GR3 integer truncation) — it is just unreachable from the preprocessor text-manipulation stage; the full fix should LIFT/REUSE that evaluator rather than build a new one (no §7.3.7 boolean evaluator exists anywhere, that part stands). Also a nuance: the deferral IS documented twice (the file's own doc header :20-22 as a WS-2002-FORMAT follow-up, and PHASE4_RECONCILIATION.md:1472 'no named track'), so this is a known-deferred residue with silent-wrong-value behavior and no live tracked owner, not a wholly undocumented drift; the interim reject-loudly action and the named-track proposal remain the right remedy. | >>DEFINE AS PARAMETER, DEFINE/EVALUATE compile-time expression operands, and the SR2 no-OVERRIDE redefinition check are all silently wrong in ConditionalCompilationProcessor.cs (X AS PARAMETER binds the string "PARAMETER" instead of being env-sourced-or-undefined per §7.3.11 GR4; "1 + 2" binds 1 because the tokenizer skips operator characters, violating GR6/§7.3.6 and §7.3.13.2 Format-1; redefinition always overrides, violating SR2), with no diagnostic channel in the class at all. Correction to the original finding: a §7.3.6 compile-time-arithmetic evaluator DOES already exist — DataBinder.Constants.cs:283+ implements it for CONSTANT entries (§13.10.4 GR4) with loud diagnostics — but it is unreachable from the text-manipulation stage and no §7.3.7 boolean evaluator exists anywhere; the full fix should lift/share that evaluator into the preprocessor rather than build a new one. The deferral is acknowledged only in the file's own XML comment (:20-22, itself stale — it still lists >>EVALUATE/>>WHEN as deferred though they are implemented) and in PHASE4_RECONCILIATION.md:1472, which names only the PARAMETER leg and explicitly states "no named track"; it appears in no live P13 list, not in PHASE-13-audit.md, not in CONFORMANCE.md, and the Wave D scout's §7.3.6/§7.3.7 mention is scoped to >>DISPLAY operands only.
- **Disposition:** _pending_

### C3. [MAJOR | impl-vs-spec-drift | A1-directives] >>SOURCE FORMAT honors only the FIRST directive per file — mid-file format switching (§7.3.24 GR1) is silently ignored

- **Refs:** ISO §7.3.24.3 GR1 (specs/ISO_COBOL.md:4945: format applies 'through a subsequent SOURCE FORMAT directive'), GR5 library-text scoping (4953); src/Cobol.Net.Frontend/Preprocessor/ReferenceFormatProcessor.cs:85-112 (first-wins NormalizeToFreeForm), :161-172 (DetectDeclaredFormat returns first match), :178-187 (StripSourceFormatDirectives blanks EVERY directive line); docs/PHASE4_RECONCILIATION.md:1471 (M2-PRE-1 leg (a), fix track 'none')
- **Evidence:** §7.3.24 GR1 makes each SOURCE FORMAT directive effective only until the next one — a file may switch FIXED→FREE→FIXED. The implementation (self-documented at ReferenceFormatProcessor.cs:88-89: 'only a single whole-file declaration is honored … the first directive wins') classifies the whole file by the first directive, then StripSourceFormatDirectives blanks ALL later directives with no diagnostic, so a valid mixed-format program is silently mis-tokenized (e.g. a FIXED tail after '>>SOURCE FORMAT IS FREE' keeps its sequence-number columns as code). Tracked only in the PHASE4_RECONCILIATION ledger row with fix track 'none' — absent from the rearchitecture plan, the P13 audit/scouts, and the P10 forward-residue ledger.
- **Proposed action:** Segment the source at each >>SOURCE FORMAT directive and convert each segment under its declared format (per-line format state), preserving GR5's per-copybook revert (CopyProcessor already normalizes each member independently). Until implemented, emit a loud diagnostic when a second directive selecting a DIFFERENT format is encountered, instead of silently stripping it. Give the residue a named track (P13 Wave D batch or the forward-residue ledger).
- **Verifier correction:** >>SOURCE FORMAT honors only the FIRST directive per file — mid-file format switching mandated by ISO §7.3.24.3 GR1 (specs/ISO_COBOL.md:4945: the directive governs text "continuing through a subsequent SOURCE FORMAT directive") is silently ignored: DetectDeclaredFormat (ReferenceFormatProcessor.cs:161-172) takes the first match, NormalizeToFreeForm (:101-112) classifies the whole file by it, and StripSourceFormatDirectives (:178-187) blanks all later directives with no diagnostic, mis-tokenizing valid mixed-format programs. Minor correction to the tracking claim: the gap is recorded in TWO ledgers, not one — docs/PHASE4_RECONCILIATION.md:1471 (M2-PRE-1 leg (a), Phase4Track "none") AND docs/ISO2023_CONFORMANCE_PLAN.md:577-582 (which itself flags it "MIS-PARSE (real)" citing §7.3.24.3 R1/R5) — but neither assigns a forward fix track, and it is absent from the rearchitecture plan, the P13 audit/scouts (the two scout mentions at PHASE-13-remaining-waves-scout.md:608/:836 are incidental analogies), and the P13 Wave D directive batch.
- **Disposition:** _pending_

### C4. [MAJOR | plan-omission | A1-directives] Conditional-compilation directives inside library text are never processed (CC stage runs before COPY, no post-COPY pass)

- **Refs:** ISO §7.2.1 Step 1/Step 2a (specs/ISO_COBOL.md:3310-3338: COPY incorporation precedes DEFINE/IF/EVALUATE processing on the expanded group), §7.3.3 SR8 (3751: directives may appear 'in source text or in library text'), §7.3.13 SR9 (4282); src/Cobol.Net.Frontend/Pipeline/Frontend.cs:90-100 (ConditionalCompilationProcessor before CopyProcessor, no second pass); src/Cobol.Net.Frontend/Preprocessor/CopyProcessor.cs:446-467 (copied text only format-normalized); docs/PHASE4_RECONCILIATION.md:1471 (M2-PRE-1 leg (b) 'CC-then-COPY order bug', fix track 'none')
- **Evidence:** §7.2.1 Step 2 processes DEFINE/IF/EVALUATE 'in the order encountered in the expanded compilation group' — i.e. AFTER library text is incorporated (Step 1), so >>IF/>>DEFINE/>>EVALUATE inside a copybook are spec-valid. The greenfield pipeline runs ConditionalCompilationProcessor on pre-COPY text and never revisits: a >>IF inside a copybook survives expansion and reaches the lexer, failing with COBOL0001 'unexpected >' (the same failure mode the Wave-D scout probed for unhandled directives). The gap is named only in PHASE4_RECONCILIATION with 'none' as its fix track and its own doc comment ('Deferred … conditional-compilation directives located inside copied library text', ConditionalCompilationProcessor.cs:22) — no live worklist carries it.
- **Proposed action:** Add a named track: restructure Preprocess to match §7.2.1 — either interleave COPY incorporation with the CC scan (process each copybook's lines through the same directive-state machine at its inclusion point, keeping the current ability of a false >>IF path to omit COPY statements), or run the CC stage once on the post-COPY expanded text with COPY-statement gating handled during expansion. Ship with a conformance test using >>IF/>>DEFINE inside a copybook.
- **Verifier correction:** Conditional-compilation directives (>>DEFINE/>>IF/>>EVALUATE) inside copied library text are never processed: ISO §7.2.1 Step 1 incorporates COPY library text into the expanded compilation group and Step 2a processes DEFINE/IF/EVALUATE 'in the order encountered in the expanded compilation group' (specs/ISO_COBOL.md:3312, 3328-3330), §7.3.3 rule 8 (:3751) allows directives 'in source text or in library text', and §7.3.13 SR9 (:4282) explicitly contemplates an EVALUATE directive wholly inside one library text — yet the greenfield pipeline runs ConditionalCompilationProcessor pre-COPY with no post-COPY CC pass (src/Cobol.Net.Frontend/Pipeline/Frontend.cs:94 vs :98-100; only TURN/PROPAGATE/REF-MOD-ZERO-LENGTH run post-COPY), so a copybook >>IF survives to the lexer as a stray-'>' parse error. The gap is named in TWO trackless ledgers — PHASE4_RECONCILIATION.md:1471 (M2-PRE-1 leg (b), Phase4Track 'none') AND ISO2023_CONFORMANCE_PLAN.md:583-590 (M2-PRE-1(b) 'CLEAN-ERROR (real)', which already reconciled the §7.3.16 COPY-gating rationale: gating still works COPY-first via §7.2.1's false-path omission allowance at :3314) — but no live worklist (PHASE-13-audit, PHASE-10 residual catalog, rearchitecture plan, resume-prompt) carries it. Proposed action stands: interleave COPY incorporation with the CC directive-state machine (or run CC once on post-COPY expanded text with COPY-statement gating handled during expansion, per §7.2.1:3314), plus a conformance test with >>IF/>>DEFINE inside a copybook.
- **Disposition:** _pending_

### C5. [MAJOR | plan-omission | A8-rw-screen-mcs] RW SUPPRESS statement (§14.9.45) — the only Report Writer verb with no grammar rule; hard syntax error, not staged loud, on no tracked worklist

- **Refs:** ISO §14.9.45 (specs/ISO_COBOL.md:32545-32581) + Annex A.4.11 item 23 (specs/ISO_COBOL.md:40472) + §7690 (PAGE/LINE-COUNTER unaffected by a suppressed group); docs/COBOLNET_REPORT_WRITER_DESIGN.md:177-178; src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4:301 (token exists, used only by COPY … SUPPRESS PRINTING per docs/rearchitecture/PHASE-13-wave-c-scout.md:456); docs/rearchitecture/PHASE-13-audit.md:53 (tracks only the DIFFERENT §12.4.5.6 SUPPRESS WHEN alt-key phrase)
- **Evidence:** RW support is claimed (plan line 131 'RW-2002 PRESENT WHEN/VARYING/multi-COLUMN'; NIST RW101A-104A green; CONFORMANCE.md §1 blanket module claim), and its host context USE BEFORE REPORTING is implemented (RW design §5), yet grep finds no suppressStatement rule in Grammar/ and no binder/emitter in src/Cobol.Net.Compiler — a SUPPRESS PRINTING program dies with a bare parse error. Every other legal-but-unimplemented RW construct is staged LOUD (COBOLNET0899/RecognizedNotImplemented, e.g. DiagnosticCatalog.cs:241-249 CODE/NEXT GROUP); SUPPRESS alone is documented only inside the internal RW design doc ('has no grammar rule — it cannot parse') and appears in no P10/P13 ledger, VCR row, or remaining-work list — the tracked grammar-batch 'SUPPRESS WHEN' item is the alternate-record-key phrase, a different construct. Same untracked-cannot-parse class (phrase-level): COLUMN LEFT/CENTER/RIGHT (§13.18.14), PAGE COLS/width, LAST CONTROL HEADING (RW design §5).
- **Proposed action:** Implement SUPPRESS PRINTING (small: grammar rule `SUPPRESS PRINTING?` → wait, PRINTING is mandatory per the format `SUPPRESS PRINTING`; bind to a BoundSuppress restricted to USE BEFORE REPORTING procedures per SR1; runtime = a per-presentation flag on the ReportWriter engine inhibiting print lines, page advance, NEXT GROUP processing, and LINE-COUNTER changes for the current group instance per GR3a-d, control-break sensing unaffected per GR3e), with a conformance golden; or minimally add it (plus the three phrase-level no-grammar holes) as a tracked ledger row staged loud, and record the non-support in docs/CONFORMANCE.md.
- **Verifier correction:** RW SUPPRESS statement (ISO §14.9.45, specs/ISO_COBOL.md:32545-32581; Annex A.4.11 item 23 line 40472 lists it as a Report writer module element) has no grammar rule (SUPPRESS is lexer-token-only, CobolLexer.g4:301), no binder/emitter, and hard-fails at parse — even though its mandatory host context USE BEFORE REPORTING is implemented (CobolControlFlow.g4 / ReportWriterEmitter.cs / Runtime ReportWriter.cs) and every other legal-but-unimplemented RW construct is staged loud via COBOLNET0899 (DiagnosticCatalog.cs:237+). It appears on NO tracked worklist: PHASE-13-audit.md:53 and the tracked grammar-batch item are the DIFFERENT §12.4.5.6 SUPPRESS WHEN alt-key phrase; PHASE-10 residual catalog and docs/CONFORMANCE.md have zero mentions; the only record is the internal RW design doc admission (COBOLNET_REPORT_WRITER_DESIGN.md:177-178). ONE CORRECTION to the proposed action: per the spec's format-notation rules (ISO_COBOL.md:2658, :5893 — non-underlined uppercase words are OPTIONAL) and §14.9.45.2 underlining only SUPPRESS, PRINTING is an optional word — the grammar must be `SUPPRESS PRINTING?` (bare `SUPPRESS` is legal), not "PRINTING is mandatory" as the finding's self-correction asserted. Semantics for an implementation: SR1 USE-BEFORE-REPORTING-only, GR1-2 current-instance-of-named-group scope, GR3a-d inhibit print lines/page advance/NEXT GROUP/LINE-COUNTER changes, GR3e control-break sensing unaffected. | RW SUPPRESS statement (§14.9.45, Annex A.4.11 item 23) is a KNOWN-but-UNTRACKED gap: the SUPPRESS token (CobolLexer.g4:301) is referenced by no parser rule, there is no binder/emitter, and no COBOLNET0899 staged-loud entry exists (DiagnosticCatalog.cs RW family lines 237ff has none) — a SUPPRESS PRINTING program dies with generic COBOL0001 (verified by DEVLOG 686, line 5813-5817: 'SUPPRESS neutral (generic COBOL0001)'). Unlike the original finding implies, the project explicitly documents the state in TWO places — docs/COBOLNET_REPORT_WRITER_DESIGN.md:177-178 ('SUPPRESS (§14.9.45) has no grammar rule — it cannot parse') and src/Cobol.Net.Runtime/IO/ReportWriter.cs:53 which pre-stages the suppression-flag hook ('not yet parsed — its suppression flag is staged with it') — so this is a knowingly-deferred item, not a blind spot. But it appears on NO actionable ledger: not in docs/CONFORMANCE.md (zero SUPPRESS/14.9.45 hits — where documented non-supports are supposed to live), not in PHASE-13-audit.md (line 53 tracks only the different §12.4.5.6 SUPPRESS WHEN alt-key phrase), not in PHASE-13-remaining-waves-scout.md, the P10 residual catalog, any VCR row, or constructs.json. This violates the project's own RW §5 convention that every legal-but-unimplemented RW construct is staged LOUD via COBOLNET0899 ('never silent'). The same doc-only-no-ledger status applies to the three phrase-level no-grammar-surface holes named in the same RW design sentence (COLUMN LEFT/CENTER/RIGHT §13.18.14, PAGE COLS/width, LAST CONTROL HEADING). Action: either implement SUPPRESS PRINTING (small: grammar rule + SR1 USE-BEFORE-REPORTING-only bind check + a per-presentation ReportGroup flag inhibiting print/page-advance/NEXT GROUP/LINE-COUNTER per GR3a-d, control-break sensing unaffected per GR3e — the ReportWriter.cs:53 hook is already designed for it) with a conformance golden, or minimally add a COBOLNET0899 'report-suppress-statement' staged-loud entry + a CONFORMANCE.md non-support row + a tracked ledger row (covering the three phrase-level holes too).
- **Disposition:** _pending_

### C6. [MAJOR | plan-vs-impl-error | A8-rw-screen-mcs] CONFORMANCE.md claims all four documented non-support facilities are 'recognized … never a bare syntax error' — only SCREEN's COBOLNET1560 warning exists in code

- **Refs:** docs/CONFORMANCE.md:6-9, :17-18, :116-129 vs src/Cobol.Net.Compiler/Binding/DataBinder.cs:341-347 (the ONLY COBOLNET1560 site, SCREEN only); grep: no COMMIT/ROLLBACK/VALIDATE/SEND/RECEIVE token or rule in src/Cobol.Net.Frontend/Grammar/*.g4; resume-prompt.md:104-106 ('today a bare syntax error'); src/Cobol.Net.Editions/Diagnostics/DiagnosticCatalog.cs:200 ('1560-band stays earmarked'); ISO §4.2.6 (specs:2437, warning mechanism 'shall') applies to A.3 items 4/6-7 (specs:40080-40086)
- **Evidence:** CONFORMANCE.md §1 says the four facilities 'are recognized enough to emit a named §4.2.6/§4.2.13 warning rather than a bare syntax error' and §4 repeats 'Each is recognized at compile time and reported with a named COBOLNET1560-band warning (never a bare syntax error)' — present tense. In code, only the SCREEN SECTION warning exists (DataBinder.cs:346); COMMIT/ROLLBACK/VALIDATE/SEND/RECEIVE have no lexer tokens, so an MCS or commit/rollback program dies with a bare COBOL0312 — exactly what resume-prompt.md:104 concedes. Since MCS and commit/rollback are A.3 processor-dependent items, §4.2.6's mandatory warning mechanism is currently unmet while the conformance record asserts it is met. (The Wave H code half is tracked — the delta reported here is the record's false present-tense claim.) Related staleness: DiagnosticCatalog.cs:93/:200 still say the 1560 band is 'earmarked' although COBOLNET1560 is emitted ad-hoc by DataBinder without a catalog descriptor.
- **Proposed action:** Either land the Wave H recognize-and-name code half before P13 merge, or reword CONFORMANCE.md §1/§4 to current truth (SCREEN warning landed; MCS/COMMIT/ROLLBACK/VALIDATE currently a bare syntax error, named recognition pending) per the docs-current-state-only rule; in the same change register a real COBOLNET1560 descriptor in DiagnosticCatalog and update its 'earmarked' band comments.
- **Verifier correction:** docs/CONFORMANCE.md makes present-tense claims (line 17-18: the four non-support facilities "are recognized enough to emit a named §4.2.6/§4.2.13 warning rather than a bare syntax error"; lines 118-120: "Each is recognized at compile time and reported with a named COBOLNET1560-band warning (never a bare syntax error)") that the code contradicts: the only COBOLNET1560 site is the SCREEN SECTION warning (src/Cobol.Net.Compiler/Binding/DataBinder.cs:346), and src/Cobol.Net.Frontend/Grammar has no SEND/RECEIVE/COMMIT/ROLLBACK/VALIDATE token or rule — those facilities today die with a generic syntax error (resume-prompt.md:104-105 says "today a generic COBOL0001", not COBOL0312 as the original finding stated). For the two A.3 processor-dependent facilities (MCS, specs:40080; commit/rollback, specs:40084-40086) this leaves §4.2.6's mandatory compile-time warning mechanism (specs/ISO_COBOL.md:2437, "shall provide a warning mechanism at compile time ... syntactically-detectable processor-dependent language elements not supported") unmet while the conformance record asserts it is met; VALIDATE (§4.2.13 obsolete optional) and SCREEN (§4.2.7 optional) fall outside that specific "shall". Secondary staleness confirmed: DiagnosticCatalog.cs:93 and :200 still say the 1560 band is "earmarked (PHASE-13)" although COBOLNET1560 is emitted ad-hoc by DataBinder with a raw string ID and no catalog descriptor. Proposed action stands: either land the tracked Wave H code half before P13 merge, or reword CONFORMANCE.md §1/§4 to current truth (SCREEN warning landed; MCS/COMMIT/ROLLBACK/VALIDATE named recognition pending) per the docs-current-state-only rule, and in the same change register a real COBOLNET1560 descriptor in DiagnosticCatalog and fix the stale "earmarked" comments (also fix CONFORMANCE.md:18's dangling "(§2.4)" cross-reference — the facility list is §4). | docs/CONFORMANCE.md §1 (line 18) and §4 (lines 118-119) claim in present tense that all four documented non-support facilities (MCS SEND/RECEIVE, COMMIT/ROLLBACK, VALIDATE, SCREEN) are 'recognized ... never a bare syntax error' with a named COBOLNET1560-band warning, but only the SCREEN SECTION warning exists in code (the sole COBOLNET1560 site, src/Cobol.Net.Compiler/Binding/DataBinder.cs:345-347); the other three have no grammar tokens (grep of src/Cobol.Net.Frontend/Grammar/*.g4: zero matches) and today die with the GENERIC parse error COBOL0001 (CobolErrorListener.cs:39; conceded at resume-prompt.md:104 and DEVLOG 891) — NOT COBOL0312 as the original finding stated (0312 is a Warning-severity hint from CobolErrorStrategy.cs:208). Since MCS (A.3 item 4) and commit/rollback (A.3 items 6-7) are processor-dependent, the §4.2.6 third-paragraph 'shall provide a warning mechanism' (spec line 2437, verified) is currently unmet for 3 of 4 facilities while the §4.2.16 conformance record asserts it is met. The Wave H CODE half is tracked (resume-prompt ①, task #5, DEVLOG 891/903) — the untracked delta is solely the record's false present-tense claim, which violates the project's docs-current-state-only rule and matches the project's own Entry-895 precedent (the '04' false claim in the same doc was treated as doc-drift and reworded to 'not yet emitted — Planned' while the code work stayed tracked). Proposed action: reword CONFORMANCE.md §1/§4 to current truth (SCREEN warning landed; MCS/COMMIT/ROLLBACK/VALIDATE currently a generic COBOL0001, named recognition pending as the tracked Wave H code half) — or land the code half before merge; also fix the dangling '(§2.4)' cross-references at CONFORMANCE.md lines 18/31/33 (the documented non-support section is §4; no §2.4 exists). The original finding's DiagnosticCatalog complaint is overweighted: EditionContext.Warning (EditionContext.cs:85) is an established descriptor-less raw-code channel (used for the 0903 obsolete flags), so the missing 1560 descriptor is at most a comment-staleness at DiagnosticCatalog.cs:93/:200, not a defect requiring a descriptor. Severity: moderate (not major) — a false claim in the §4.2.16 conformance record, but bounded because the tracked Wave H code half is scheduled to land before P13 merge, making it self-healing on the planned path.
- **Disposition:** _pending_

### C7. [MAJOR | plan-vs-impl-error | B3-ledger-consistency] resume-prompt banner claims 'NEXT FREE = 1575' while 1575 is used at HEAD, contradicting line 155 of the same file — the exact stale-next-free pattern that caused the 1573 collision

- **Refs:** E:\CobolSharp\resume-prompt.md:169 ('NEXT FREE = 1575') vs resume-prompt.md:155 ('next free 15xx after 1575 = 1576+'); E:\CobolSharp\src\Cobol.Net.Editions\Diagnostics\DiagnosticCatalog.cs:433 (COBOLNET1575 external-relative-key-consistency, landed 38c4a669)
- **Evidence:** Enumerated all COBOLNET15xx in src: 1501–1549 and 1553–1575 are used (1575 = external-relative-key-consistency at DiagnosticCatalog.cs:433, DEVLOG 905); only 1550–1552 and 1576+ are free. The banner's Wave G ledger line (resume-prompt.md:169) still says 'NEXT FREE = 1575' while the Wave E bullet fifteen lines up (line 155) says 1576+. Two contradictory next-free claims in the LIVE resume banner is precisely how Wave E re-allocated 1573: commit f71faa0f's ledger said 'next free 1575', then Wave E needed two codes and took 1573+1575 instead of 1575+1576.
- **Proposed action:** Correct resume-prompt.md:169 to NEXT FREE = 1576 and keep the next-free claim in exactly ONE place in the banner (delete the duplicate at the other line); state explicitly whether 1550–1552 are allocatable (see the stale PHASE-12 earmark finding) so the enumeration and the claim can't diverge again.
- **Disposition:** _pending_

### C8. [MAJOR | doc-inconsistency | B3-ledger-consistency] VCR derived-status ledger contradicts constructs.json and resume-prompt: 16 P13-landed rows still anchored <!-- todo -->, so the generated Gating-status index omits every P13 construct

- **Refs:** Annex E.2 items 12/20/23/24/27, E.3.3 items 20/25/26/32 etc.; E:\CobolSharp\docs\VERSION_CHANGE_REFERENCE.md:119,128,131,132,135,152,162,171,174,177,180,185,186,192,197,203 (rows 18,27,30,31,34,42,45,54,57,60,63,68,69,75,80,86 all '<!-- todo -->') + the GEN:VCR-STATUS block (15 pre-P13 constructs only); E:\CobolSharp\tests\version-matrix\constructs.json (e.g. "vcr": "VCR row 30 (REF-MOD-ZERO-LENGTH directive — 2023 introduction)", rows for VCR 27/34/42/45/54/57/60/63/68/69/75/80/86); E:\CobolSharp\tests\Cobol.Net.Tests.Conformance\VcrDriftTests.cs:101-111,141-162; resume-prompt.md:130-159
- **Evidence:** The VCR's own contract says status is DERIVED from anchors ('<!-- todo --> — a genuine gate obligation not yet a constructs.json row') and the gate 'ships in the SAME change set as the feature itself'. Yet all 16 P13-landed rows still carry <!-- todo --> while (a) constructs.json rows exist citing those exact VCR rows by number for 14 of them, and (b) resume-prompt marks the VCR items ✅ DONE (e.g. 'VCR 34 DONE', 'VCR 18 + 31 DONE'). Consequently the generated Gating-status index lists only 15 pre-P13 constructs and represents the entire P13 batch as unimplemented. VcrDriftTests cannot catch this: it checks only forward coverage (every gate: anchor resolves) and index↔anchor sync — a stale todo is structurally invisible to CI. Rows 18/31 (COBOLNET1573/1575 cross-SELECT consistency) additionally have NO constructs.json rows at all (Wave E shipped 6 tests/conformance goldens instead), so they cannot be flipped to gate: without new matrix rows.
- **Proposed action:** Flip the 14 anchors that already have constructs.json rows from <!-- todo --> to <!-- gate:construct-id --> and regenerate via pwsh scripts/gen-vcr.ps1; for rows 18/31 add constructs.json rows (a single-source two-program run unit reproduces the cross-SELECT check) or add a documented done-without-matrix-row anchor kind; add a reverse-coverage assertion to VcrDriftTests (every constructs.json "vcr": "VCR row N" citation ⇒ row N carries a gate: anchor) so this drift class becomes CI-detectable. Fold into the Wave I phase-close doc sweep at latest.
- **Verifier correction:** The VCR derived-status ledger is stale for the entire P13 batch — and slightly broader than the original 16 rows: (a) 16 rows already have constructs.json rows citing them by number yet still carry <!-- todo --> (the flagged 14: rows 27/30/34/42/45/54/57/60/63/68/69/75/80/86 at docs/VERSION_CHANGE_REFERENCE.md:128,131,135,152,162,171,174,177,180,185,186,192,197,203 — plus unflagged rows 9 and 46 at lines 110/163, cited by boolean-shift-operators-2023 "VCR rows 9/32/46"); (b) 4 rows are marked DONE in resume-prompt but have neither a constructs.json row nor a non-todo anchor (the flagged 18/31 at lines 119/132, DEVLOG 905 / HEAD 38c4a669 — plus unflagged 21/35 at lines 122/136, DEVLOG 899/898). Consequently the GEN:VCR-STATUS index (lines 79-93) lists only 15 pre-P13 constructs and represents all landed P13 work as outstanding. VcrDriftTests (tests/Cobol.Net.Tests.Conformance/VcrDriftTests.cs:101-111,141-162) checks only forward coverage + index sync + specLines, so this drift class is CI-invisible. Nuance: for the 4 no-matrix-row cases the todo anchor is literally consistent with the legend's wording ("not yet a constructs.json row"), but it violates the banner's same-change-set rule and the derived index misreports shipped features — the proposed fix (flip the 16 citable anchors to gate: + regenerate; add matrix rows or a new done-without-matrix-row anchor kind for 18/31/21/35; add a reverse-coverage assertion to VcrDriftTests) is the right remedy. | The VCR derived-status ledger contradicts constructs.json and resume-prompt: at least 18 P13-landed rows (the finding's 16 — 18/27/30/31/34/42/45/54/57/60/63/68/69/75/80/86 — plus row 35, DONE per resume-prompt:117 with matrix row value-numeric-literal-numeric-edited-2023, and row 21, DONE per resume-prompt:123 under a deliberate implement-at-all-editions/no-gate disposition whose correct anchor is pin-to-spec/ref-only) still carry <!-- todo -->, so the generated Gating-status index (VCR lines 79-93) lists only 15 pre-P13 constructs and represents the entire P13 batch as unimplemented. 14 of the todo rows have live constructs.json rows citing the exact VCR row number, making the anchor legend's meaning ("not yet a constructs.json row", VCR line 52) factually false; rows 18/31 (COBOLNET1573/1575, shipped as 6 conformance goldens per resume-prompt:149-152) have no matrix rows and cannot be flipped under the current anchor grammar. VcrDriftTests is structurally blind to this drift class (forward-coverage only at lines 101-111; the sync test at 141-162 renders FROM the anchors, so a stale todo matches on both sides). Partial tracking exists but does not neutralize it: resume-prompt:54's "matrix/VCR flip" note covers only the audit's pre-P13 verify-and-flip class, and PHASE-13-audit.md's per-row dispositions (e.g. "implement — …; flip VCR row 31", audit line 65) show the flip half was skipped while resume-prompt marks the items ✅ DONE.
- **Disposition:** _pending_

### C9. [MAJOR | impl-vs-spec-drift | C1-waveE-drift] VCR 16 gate checks TYPE presence only — §13.16.3 SR13 ¶2 requires the TYPE to name a STRONGLY typed definition; a weak-TYPE external CONSTANT RECORD compiles clean at 2023 strict

- **Refs:** specs/ISO_COBOL.md:17253 (§13.16.3 SR13 ¶2: 'there shall also be a TYPE clause that specifies a strongly typed definition'), :49148 (Annex E.2 item 10: 'may now only be specified for external items that are strongly typed'), :25535 (§14.8.4.3); src/Cobol.Net.Compiler/Binding/DataBinder.cs:1976-1983 (checks only 'typeRefName is null'), :1107-1112 (SR5 leg fires only when template.TypedefStrong)
- **Evidence:** DataBinder.cs:1981 gates EXTERNAL+CONSTANT RECORD at >=2023 on 'hasExternal && typeRefName is null' and the message claims '…requires a TYPE clause naming a strongly typed definition (ISO §13.16.3 SR13 ¶2)'. But type strength is never verified: `01 C IS EXTERNAL CONSTANT RECORD TYPE WK.` where WK is a WEAK typedef passes this check (typeRefName non-null), and at ExpandType the SR5 pairing error (DataBinder.cs:1109) fires only for STRONG types, so no other diagnostic catches it — the combination the 2023 change exists to forbid (E.2 item 10, §14.8.4.3 ¶3) is accepted at --std 2023 strict. The emitted message overclaims what the code enforces.
- **Proposed action:** Add the strength half at ExpandType (where template.TypedefStrong is known): if item.IsConstantRecord && (item.HasExternalClause || item.ExternalFromType) && !template.TypedefStrong && DialectLevel >= 2023, report DiagnosticCatalog.ConstantRecordRule citing §13.16.3 SR13 ¶2 / Annex E.2 item 10. Add a negative conformance golden (external CONSTANT RECORD TYPE weak-typedef at 2023) plus a 2014-continuity witness.
- **Verifier correction:** VCR 16 gate (DataBinder.cs:1981) enforces only TYPE-clause PRESENCE for EXTERNAL+CONSTANT RECORD at >=2023; §13.16.3 SR13 ¶2 (:17253) additionally requires that TYPE name a STRONGLY typed definition (per Annex E.2 item 10, :49148 — code comment mis-cites it as E.3). No other check (SR5 at :1109 is strong-only per §13.18.22 SR5 :18695; SR6 strong-only; §13.18.15.3 has no strength rule) catches a weak-typedef TYPE, so `01 C EXTERNAL CONSTANT RECORD TYPE WK.` (WK weak) compiles clean at --std 2023 strict and the diagnostic message overclaims. Fix: add the strength half at ExpandType where template.TypedefStrong is known, plus a negative 2023 golden and a 2014-continuity witness; also correct the E.3→E.2 annex cite in the comment. | VCR 16's landed gate (DataBinder.cs:1981) enforces only the TYPE-presence half of §13.16.3 SR13 ¶2; the strength half is unenforced anywhere (ExpandType's SR5 leg at :1109 fires only for STRONG types, and no other check covers CONSTANT RECORD + weak TYPE). Confirmed by live probe: '01 WK TYPEDEF.' (weak) + '01 R IS EXTERNAL CONSTANT RECORD TYPE WK.' compiles and runs clean at --std 2023 strict (prints ABCD, exit 0), though Annex E.3 item 10 (:49148 — labeled E.3, not E.2, in the spec/scout) forbids exactly this. The COBOLNET1549 message text ('requires a TYPE clause naming a strongly typed definition') overclaims what the code checks, and DEVLOG:1295 + PHASE-13-audit.md row 86 both claim the strongly-typed-TYPE requirement is enforced — a doc-vs-impl contradiction. Fix as proposed: add the strength leg in ExpandType where template.TypedefStrong is known — if item.IsConstantRecord && item.HasExternalClause && !template.TypedefStrong && Edition.DialectLevel >= 2023, report DiagnosticCatalog.ConstantRecordRule (§13.16.3 SR13 ¶2; E.3 item 10); decide separately whether the ExternalFromType (external-via-GR3) leg is in scope, since SR13 ¶2 literally triggers on the EXTERNAL clause. Add the weak-TYPE-at-2023 negative golden plus a 2014-continuity witness (weak-TYPE form accepted below 2023, where SR13 ¶2 did not exist).
- **Disposition:** _pending_

### C10. [MAJOR | plan-vs-impl-error | C2-waveG-drift] CONFORMANCE.md still records I-O status '04' as "not yet emitted — Planned" after Wave G shipped it

- **Refs:** docs/CONFORMANCE.md:74-77 vs src/Cobol.Net.Runtime/IO/SequentialConnector.cs:376-419, src/Cobol.Net.Runtime/IO/FileStatus.cs:15, DEVLOG.md Entry 899 (line 143), tests/CobolSharp.Tests.Integration/ConformanceTests.cs:355; spec §9.1.13.2 item 3 (line 11466)
- **Evidence:** CONFORMANCE.md §3 (the authoritative §4.2.16 implementor-choice record): "the record-sequential READ short/long-record status '04' is **not yet emitted** — the record-sequential connector fits the physical record to the fixed record width without flagging a length mismatch (a successful READ returns '00'). Planned (VCR 21...)". But DEVLOG Entry 899 (2026-07-18) landed exactly this: SequentialConnector.Read now sets Status = RecordLengthShortLong ("04") on the fixed leg (n < RecordWidth) and varying leg (n < VaryMin || n > VaryMax), with golden tests/conformance/2002/io_status_04. The conformance ledger now claims a non-support that no longer holds — the inverse of the earlier "'04' false-claim" fix.
- **Proposed action:** Update the CONFORMANCE.md §3 '04' bullet to state it IS emitted (record-sequential READ only; short right-padded / long-frame flagged; line-sequential excluded per §14.9.30 GR15 → '06' path, which remains unimplemented), citing §14.9.30 GR14 / §9.1.13.2 item 3; per process rule 4 this doc sweep should have shipped in the Wave G commit.
- **Verifier correction:** docs/CONFORMANCE.md §3 (lines 74-77) still records I-O status '04' as "not yet emitted — Planned (VCR 21)" even though commit 04c32a93 (Wave G, DEVLOG 899) shipped it in SequentialConnector.Read with golden 2002/io_status_04; no commit since 2265c33a touched CONFORMANCE.md. Update the bullet to state '04' IS emitted (record-sequential READ only, fixed short-final-record + varying out-of-min/max legs; line-sequential excluded), citing §9.1.13.2 item 3 — and re-derive the GR citation from §14.9.30 (READ), since the code/DEVLOG cite §14.9.35 (REWRITE) GR14, a likely secondary citation slip.
- **Disposition:** _pending_

### C11. [MAJOR | impl-vs-spec-drift | C2-waveG-drift] VCR 86 literal-zero exemption below 2023 mis-derives §13.18.63 SR6 and makes VALUE 0 diverge from VALUE ZERO at pre-2023 editions

- **Refs:** src/Cobol.Net.Compiler/Binding/DataBinder.cs:1916-1922,968-981 · src/Cobol.Net.Compiler/CodeGen/DataDivision/ValueInitializer.cs:83-88,99-101 · DEVLOG.md Entry 898 lines 190-193; spec §13.18.63 SR6 (line 23234), Annex E.3.3 item 43 (line 50338), Annex E.2 item 28 (line 49368)
- **Evidence:** The code comment claims "SR6 exempts 'the integer and decimal forms of the literal zero' (and the figurative ZERO — VCR 35) at ALL editions, so only a non-zero numeric literal is gated." SR6's actual zero sentence is a 2023-only rule about fixed-vs-floating FORMAT applicability: "Literals for fixed-point formats shall be specified as fixed-point... though the figurative constant ZERO or ZEROES and the integer and decimal forms of the literal zero may also be specified for EITHER FORMAT and shall be treated identically as the literal zero" — it says nothing about prior editions. The introduction proof the gate itself relies on, E.3.3 item 43 ("It is now permitted to allow numeric-edited data items to be assigned values specified as numeric literals"), has no zero carve-out; only the FIGURATIVE ZERO is documented as pre-2023-legal (E.2 item 28). Result: at --std 85/2002/2014, `PIC $ZZ9.99 VALUE 0` compiles clean and ValueInitializer edit-composes it to `$  0.00` (the NumericEdited numeric leg is not edition-gated), while `VALUE ZERO` on the same item zero-fills to `0000000` — the two spellings SR6 says "shall be treated identically" diverge below 2023, and the literal-zero acceptance itself has no pre-2023 basis.
- **Proposed action:** Either gate the literal-zero numeric VALUE below 2023 with the same COBOLNET0900 (IsNonZeroNumericLiteral → any plain numeric literal), or — if kept for corpus pragmatics — route the <2023 literal-zero through the same figurative-ZERO zero-fill leg and record it as a leniency-registry entry; in all cases fix the DataBinder/DEVLOG-derived comment so SR6 is not cited as an all-editions exemption.
- **Verifier correction:** VCR 86's literal-zero exemption mis-cites §13.18.63 SR6: SR6's zero sentence ("...may also be specified for either format and shall be treated identically as the literal zero", spec line 23234) is a 2023-internal fixed-vs-floating FORMAT applicability rule, not an all-editions exemption — the introduction proof E.3.3 item 43 (line 50338, "It is now permitted...") has no zero carve-out, and E.2 item 28 (line 49368) grandfathers only the FIGURATIVE ZERO pre-2023. Consequently DataBinder.cs:1920 (gating only IsNonZeroNumericLiteral) accepts `VALUE 0` on a numeric-edited item at 85/2002/2014 with no spec or VCR-ledger basis (VERSION_CHANGE_REFERENCE.md line 203 itself says numeric literals were not permitted pre-2023), and ValueInitializer.cs:99-101 (ungated) edit-composes it while the figurative-ZERO leg (83-88) zero-fills below 2023 — an incoherent VALUE 0 vs VALUE ZERO split. Note: below 2023 the split is not strictly a violation OF SR6 (2023 text does not govern earlier editions); the defects are the SR6 mis-citation in DataBinder.cs:1916-1919 / DEVLOG 898 lines 190-191 and the unfounded pre-2023 acceptance. Fix: gate ANY plain numeric literal (including zero) under COBOLNET0900 below 2023, or route pre-2023 literal-zero through the figurative zero-fill leg and record it as a leniency-registry entry; correct the comment/DEVLOG derivation either way. | VCR 86's literal-zero exemption mis-derives §13.18.63 SR6: the "either format" zero sentence is a 2023-only fixed-vs-floating-point format-applicability rule, not an all-editions exemption (E.3.3 item 43 has no zero carve-out; only the figurative ZERO is documented pre-2023-legal via E.2 item 28). Consequently at --std 85/2002/2014 `PIC $ZZ9.99 VALUE 0` compiles clean and edit-composes to `$  0.00` (ValueInitializer.cs:99-101, ungated) while `VALUE ZERO` zero-fills to `0000000` (ValueInitializer.cs:83 gate) — diverging spellings SR6 says shall be treated identically. Additionally, the shipped exemption silently deviates from the trusted scout's plan (PHASE-13-remaining-waves-scout.md:1393 — gate ANY numeric literal below 2023), and the mis-derivation is replicated in DataBinder.cs:1916-1922/:966-967, tests/version-matrix/constructs.json:212, tests/conformance/2023/value_numeric_edited.cob:6, DEVLOG 898, and resume-prompt.md:118-119 — all need the propagated fix.
- **Disposition:** _pending_

### C12. [MAJOR | impl-vs-spec-drift | C2-waveG-drift] The '04' implementation cites §14.9.35 GR14 (REWRITE unsuccessful-execution rule) — the record-length rule is §14.9.30 (READ) GR14

- **Refs:** src/Cobol.Net.Runtime/IO/SequentialConnector.cs:376,399 · src/Cobol.Net.Runtime/IO/FileStatus.cs:13 · tests/CobolSharp.Tests.Integration/ConformanceTests.cs:355 · resume-prompt.md:124; spec §14.9.30 GR14 (line 29953) vs §14.9.35.4 GR14 (line 30611); correct §14.9.35-GR14 uses at FileRegistry.cs:624, PhysicalFileTable.cs:97
- **Evidence:** Four shipped locations cite "§14.9.35 GR14" for the READ '04' behavior. §14.9.35 is the REWRITE statement; its GR14 reads "If the execution of the REWRITE statement is unsuccessful, no logical record updating takes place..." — nothing about '04'. The actual rule is READ §14.9.30 GR14: "For a record sequential file, if the number of bytes in the record that is read is less than the minimum size... greater than the maximum size... the READ statement is successful, and the I-O status value... is set to '04'." The identical citation string is used CORRECTLY elsewhere (FileRegistry.cs:624 "no update on failure") for the real REWRITE GR14, so the wrong cites actively collide with a right one. The '04' behavior itself matches §14.9.30 GR14 (fixed n<RecordWidth, varying n<VaryMin||n>VaryMax, line-seq excluded — defensible, GR15 assigns line-seq long-record '06'). The adjacent comment "its short/long conditions are '06'/'09', never '04'" also mischaracterizes GR15/GR16: a short line-seq record is a successful no-error read (space-padded), '06' is long-record-only, '09' is invalid-characters — and neither '06' nor '09' is actually produced by the connector.
- **Proposed action:** Fix the four citations to "ISO §14.9.30 GR14 / §9.1.13.2 item 3" (per feedback_spec_fidelity_discipline: a cited § must match the code); correct the line-sequential comment to GR15/GR16's actual conditions and add the unimplemented line-seq '06' (long record, position-at-next-unread continuation) to a tracked residue list.
- **Verifier correction:** The finding is accurate as stated; only the count is off: there are 5 wrong-citation instances across 4 files (SequentialConnector.cs:376 AND :399, FileStatus.cs:13, ConformanceTests.cs:355, resume-prompt.md:124), all of which should cite §14.9.30 GR14 / §9.1.13.2 item 3; the SequentialConnector.cs:378 line-seq comment should be corrected to GR15/GR16's actual conditions (short = successful padded read, '06' = long-record truncation with FPI-at-next-unread continuation, '09' = invalid characters), and the unimplemented line-seq '06' path added to a tracked residue list. | The READ '04' implementation (landed 04c32a93, VCR 21) cites "§14.9.35 GR14" — the REWRITE unsuccessful-no-update rule — where the actual rule is READ §14.9.30 GR14 (spec line 29953: record-sequential short/long record → successful READ, status '04'). Wrong-cite sites: src/Cobol.Net.Runtime/IO/SequentialConnector.cs:376,399 · src/Cobol.Net.Runtime/IO/FileStatus.cs:13 · tests/CobolSharp.Tests.Integration/ConformanceTests.cs:355 · resume-prompt.md:124 · origin = docs/rearchitecture/PHASE-13-remaining-waves-scout.md VCR-21 Golden bullet (DEVLOG 899 also carries it but stays as immutable narrative). The identical string is used CORRECTLY for the real REWRITE GR14 at FileRegistry.cs:624 and PhysicalFileTable.cs:97, so the citation now names two different rules. Additionally the SequentialConnector.cs:378 comment "line-seq short/long conditions are '06'/'09'" mischaracterizes §14.9.30 GR15/GR16 (short line-seq = successful space-padded read with no error status; '06' = long-record-only; '09' = invalid characters, not a length condition), and neither '06' nor '09' exists anywhere in the greenfield runtime while the scout falsely claims them "already modelled" — the line-seq '06' long-record path is an untracked gap. Fix: change the five cites to "§14.9.30 GR14 / §9.1.13.2 item 3", correct the line-seq comment to GR15/GR16's actual conditions, and add line-seq '06' to a tracked residue list (CONFORMANCE.md §3 or the P13 worklist). The '04' behavior itself is spec-correct.
- **Disposition:** _pending_

### C13. [MAJOR | impl-vs-spec-drift | C3-ec-refmod-drift] Main-program GOBACK RAISING is not ignored — it terminates the run unit loudly instead of normal termination

- **Refs:** ISO §14.9.18.4 GR3 (specs/ISO_COBOL.md:27745) · src/Cobol.Net.Compiler/CodeGen/Verbs/CallEmitter.cs:302 · src/Cobol.Net.Runtime/Control/ProgramTable.cs:88-104 · docs/COBOLNET_CONDITIONS_EXCEPTIONS_DESIGN.md:104
- **Evidence:** GR3: 'If a GOBACK statement is executed in a program that is not under the control of a calling runtime element, the program operates as if executing a STOP statement, with a status phrase, if any. A RAISING phrase, if specified, is ignored.' The implementation stages RAISING unconditionally (CallEmitter.cs:302 — not gated on __asCalled, unlike EmitExitProgram which gates BOTH staging and return per §14.9.14 GR2, CallEmitter.cs:312-314), and ProgramTable.RunMain then calls ApplyPropagationDefault (ProgramTable.cs:90) which throws CobolFatalException for a fatal staged name (ProgramTable.cs:101-104) → the emitted Main prints 'abnormal run-unit termination' and exits 1 (ProgramEmitter.cs:419). So a main program executing GOBACK RAISING EXCEPTION <fatal-ec> terminates abnormally with exit 1 instead of the GR3-mandated normal STOP-equivalent termination (exit per status/default 0). §14.6.13.1.3 #8 (cited by the code) covers an EC-unchecked ACTIVATOR of a called program — it cannot apply to the main program, whose RAISING never comes into existence per GR3. The deep-dive line 104 codifies the deviation ('RunMain applies the same default for a MAIN program's GOBACK RAISING').
- **Proposed action:** Gate the GOBACK RAISING stage on __asCalled in CallEmitter.EmitGoback (mirroring EmitExitProgram), delete the ApplyPropagationDefault call from ProgramTable.RunMain (keep it on the CallProgram path where §14.6.13.1.3 #8 legitimately applies), correct COBOLNET_CONDITIONS_EXCEPTIONS_DESIGN.md:104 in the same change set, and add a conformance test: main program GOBACK WITH NORMAL STATUS 0 RAISING EXCEPTION <fatal ec> must exit 0 with no stderr diagnostic.
- **Verifier correction:** Main-program GOBACK RAISING contradicts ISO 14.9.18.4 GR3 ("A RAISING phrase, if specified, is ignored") on TWO ungated emission legs, not one: (a) enabled checking - CallEmitter.cs:302 stages unconditionally via EmitRaisingStage -> SetPropagating (line 363), then ProgramTable.RunMain:90 ApplyPropagationDefault (ProgramTable.cs:99-105) throws CobolFatalException for a fatal name; (b) disabled checking + fatal - EmitRaisingStage emits an immediate throw new CobolFatalException at the GOBACK site itself (CallEmitter.cs:359-360), also ungated on __asCalled. Both reach the emitted Main catch (ProgramEmitter.cs:419) -> "abnormal run-unit termination" stderr + exit 1 instead of GR3's normal STOP-equivalent termination. Fix: wrap the EmitRaisingStage call in EmitGoback in the __asCalled gate (covers both legs; mirrors EmitExitProgram lines 316-329 which correctly gates per 14.9.14 GR2, and the STATUS phrase at line 307 which is already gated per GR3/GR10); delete the then-dead ApplyPropagationDefault call from RunMain (keep the CallProgram-path boundary default where 14.6.13.1.3 #8 legitimately applies); INVERT the existing test GobackRaisingFatal_InMainProgram_TerminatesAtRunUnitBoundary (ExceptionConditionConformanceTests.cs:662-673) which currently asserts the deviating behavior; correct COBOLNET_CONDITIONS_EXCEPTIONS_DESIGN.md:104 in the same change set.
- **Disposition:** _pending_

### C14. [MAJOR | impl-vs-spec-drift | C3-ec-refmod-drift] A specified ref-mod length that evaluates negative at runtime is silently treated as the length-omitted form — EC-BOUND-REF-MOD can never raise for negatives

- **Refs:** ISO §8.4.3.3.4 item 5c + raise sentence (specs/ISO_COBOL.md:7087-7089) · src/Cobol.Net.Compiler/CodeGen/Roslyn/PlaceRenderer.cs:129-130 · src/Cobol.Net.Runtime/Values/Text/CobolString.cs:56-66,85-95
- **Evidence:** 5c: 'The evaluation of length shall result in a positive nonzero integer, unless the REF-MOD-ZERO-LENGTH directive is set to ON, when the result may also be zero'; the consequence sentence raises EC-BOUND-REF-MOD for a value out of range (:7089). PlaceRenderer.RmLen renders the OMITTED length as the sentinel -1 ('r.Length is null ? "-1" : $"(int)({r.Length})"', PlaceRenderer.cs:130), and CobolString.RefMod/SpliceInto interpret ANY negative length as 'to the end'/clamp (CobolString.cs:63 'len = length < 0 ? avail : length'; the guard at :56/:85 tests only 'length == 0' and 'length > 0'). So with EC-BOUND-REF-MOD checking ON, X(2:L) where L holds -1 (or any negative) silently reads/writes to the end of the item instead of raising the fatal EC — while L=0 correctly raises. The sentinel collision makes the negative-length violation structurally undetectable at runtime. (Secondary: the ':7089 non-integer value' leg is also unreachable — the '(int)(...)' cast at PlaceRenderer.cs:129 truncates instead of raising.)
- **Proposed action:** Use a non-colliding sentinel for the omitted length (int.MinValue, or a separate RefMod overload / nullable parameter) and extend the runtime guard to raise EC-BOUND-REF-MOD for length < 0 alongside the zero check (zero stays directive-relaxable, negative never is). Add a conformance test with a data-item length of -1 under >>TURN EC-BOUND-REF-MOD checking ON, and one under >>REF-MOD-ZERO-LENGTH ON confirming negative still raises.
- **Verifier correction:** A ref-mod length that evaluates negative at runtime is silently treated as the length-omitted form because PlaceRenderer.RmLen (PlaceRenderer.cs:130) uses -1 as the omitted-length sentinel and CobolString.RefMod/SpliceInto (CobolString.cs:56/:63, :85/:91) interpret any negative length as "to the end" — so EC-BOUND-REF-MOD can never raise for a negative length even with checking ON, violating §8.4.3.3.4 item 5c ("The evaluation of length shall result in a positive nonzero integer, unless the REF-MOD-ZERO-LENGTH directive is set to ON, when the result may also be zero", spec :7087) and the EC-BOUND-REF-MOD Fatal disposition (Table 13, :24644 "Reference modifier out of bounds"). Nuance vs the original wording: the :7089 raise sentence's enumerated legs (non-integer / zero / position outside the area) do not literally name "negative" — the violation is grounded in 5c's positive-nonzero shall-rule, so cite 5c as the primary authority. Impact is confined to checking-ON mode (checking OFF is the deliberate lenient default). Fix as proposed: a non-colliding omitted-length sentinel (int.MinValue or a nullable/overload) + extend both runtime guards to raise for length < 0 unconditionally (the REF-MOD-ZERO-LENGTH directive relaxes only zero, never negative) + conformance tests for a negative runtime length under checking ON and under REF-MOD-ZERO-LENGTH ON. | A specified ref-mod length that evaluates negative at runtime is silently treated as the length-omitted ("to the end") form because PlaceRenderer.RmLen (PlaceRenderer.cs:130) renders the omitted length as the sentinel -1 and CobolString.RefMod/SpliceInto (CobolString.cs:56,63 / :85,91) interpret ANY negative length as to-the-end; the guards test only `length == 0` and `length > 0`, so with EC-BOUND-REF-MOD checking ON, X(2:L) with L=-1 (or any negative) never raises while L=0 correctly does — violating ISO §8.4.3.3.4 item 5c ("The evaluation of length shall result in a positive nonzero integer", specs/ISO_COBOL.md:7087) and the to-end form's precondition ("If length is not specified", same line). DROP the secondary "(int) cast makes the non-integer leg unreachable" point: that is a deliberate, documented disposition (PHASE-13-remaining-waves-scout.md:593 — "our positions are already integers (arithmetic-expression operands truncated by the typed pipeline) ... no distinct runtime raise"). ADD: the fix is also a design-doc-vs-impl drift — COBOLNET_STRING_OPS_DESIGN.md:89-91 specifies validation "length>=1 (or ==0 when REF-MOD-ZERO-LENGTH on)" and a SEPARATE `RefModToEnd` entry point (no sentinel collision by design); the fix should restore a non-colliding omitted-length encoding (or add a `length < -0` guard leg with a distinct sentinel like int.MinValue), raise for length < 0 regardless of the directive, and update the scout predicate + CobolString doc comments + STRING_OPS design in the same change set.
- **Disposition:** _pending_

### C15. [MINOR | plan-omission | A1-directives] No introduction gate for the COBOL-2002 conditional-compilation / SOURCE FORMAT directive facility at --std 85

- **Refs:** ISO §7.3.5/§7.3.11/§7.3.16/§7.3.24 (2002-introduced; the >> compiler-directive facility does not exist in COBOL-85); src/Cobol.Net.Frontend/Preprocessor/ConditionalCompilationProcessor.cs:45-46 (Process takes no dialectLevel), ReferenceFormatProcessor.cs:81-83 (directive regex, no edition check); tests/version-matrix/constructs.json (no conditional-compilation/SOURCE-FORMAT row; contrast the gated >>REF-MOD-ZERO-LENGTH row at line 188); memory feedback_version_test_matrix
- **Evidence:** The sibling directive stages (TurnDirectiveProcessor, PropagateDirectiveProcessor, RefModZeroLengthDirectiveProcessor) all take DialectLevel and emit COBOLNET0900 introduction gates, and >>REF-MOD-ZERO-LENGTH has a constructs.json matrix row. The conditional-compilation stage and the SOURCE FORMAT recognizer are edition-blind: a --std 85 compile happily honors >>DEFINE/>>IF/>>EVALUATE and >>SOURCE FORMAT with no diagnostic, violating the project's per-edition introduction invariant (test as N per-edition compilers). Nothing in the P13 worklist, scouts, or residue ledgers names this gate.
- **Proposed action:** Thread DialectLevel into ConditionalCompilationProcessor (and the SOURCE FORMAT recognizer) and emit the standard COBOLNET0900 introduction diagnostic below 2002 for >>DEFINE/>>IF/>>EVALUATE/>>SOURCE FORMAT; add the corresponding constructs.json introduction rows. Verify the legacy/NIST 85 corpus is unaffected (CCVS-85 uses no >> directives) before enabling.
- **Verifier correction:** No introduction gate for the 2002-introduced >> compiler-directive facility at --std 85: ConditionalCompilationProcessor.Process (src/Cobol.Net.Frontend/Preprocessor/ConditionalCompilationProcessor.cs:45-46) takes no dialectLevel and Frontend.cs:94 calls it edition-blind, so >>DEFINE/>>IF/>>EVALUATE (ISO §7.3.5/§7.3.11/§7.3.16) are honored silently at 85; ReferenceFormatProcessor honors >>SOURCE FORMAT (§7.3.24) at every --std (regex :81-83; its EditionGates only fire >=2023 continuation gates), while sibling TurnDirectiveProcessor.cs:48-52 correctly emits the four-compilers diagnostic below 2002. CORRECTION to the tracking claim: the CC half is NOT wholly unnamed — SURVEY-frontend-plumbing.md §4 (:150-154) + GAP-4 (:266-272) name it, and roadmap COBOLNET_REARCHITECTURE_PLAN.md §7 R2 (:436-438) folds it in tagged →PHASE-02/PHASE-04 under a MUST-apply instruction (:423-424); both phases closed DONE without applying it (neither phase doc mentions it), so the CC half is better classified plan-vs-impl-error (roadmap-mandated refinement dropped at phase close) than pure plan-omission. The >>SOURCE FORMAT introduction gate is genuinely unnamed in any ledger, and constructs.json has no conditional-compilation/SOURCE-FORMAT row (contrast the >>REF-MOD-ZERO-LENGTH row at line 188). Proposed action stands: thread DialectLevel into ConditionalCompilationProcessor and the SOURCE FORMAT recognizer, emit COBOLNET0900 below 2002, add constructs.json rows — naturally folded into the P13 Wave D directives batch, citing roadmap §7 R2 as the re-opened item. | The code gap is real and exactly as described (edition-blind >>DEFINE/>>IF/>>EVALUATE at Frontend.cs:94 → ConditionalCompilationProcessor.cs:45-46 with no dialectLevel, and an ungated >>SOURCE FORMAT recognizer at ReferenceFormatProcessor.cs:81-83, while siblings TurnDirectiveProcessor.cs:48 / PropagateDirectiveProcessor.cs:46 gate at <2002 and RefModZeroLengthDirectiveProcessor gates at <2023 via ConstructRegistry/COBOLNET0900; no ConstructRegistry, constructs.json, or VCR row exists). But it is not wholly unnamed: docs/COBOLNET_REARCHITECTURE_PLAN.md:436-438 (§7 refinement R2, tagged → PHASE-02/PHASE-04, citing SURVEY-frontend-plumbing.md:150-154) explicitly records the ConditionalCompilationProcessor half. The real defect is therefore a DANGLING tracked item / plan-vs-impl error rather than a pure plan-omission: PHASE-02 and PHASE-04 closed as DONE without applying or re-deferring this R2 leg (neither phase doc mentions it), violating §7's 'each executing session MUST apply the refinements tagged to its phase', and no pending worklist now owns it. The >>SOURCE FORMAT half is genuinely untracked (R2 and the survey name only ConditionalCompilationProcessor; WS-2002-FORMAT follow-ups cover mid-file switching, not edition gating). Proposed action stands: thread dialectLevel into ConditionalCompilationProcessor and the SOURCE FORMAT recognizer via the ConstructRegistry funnel (COBOLNET0900 below 2002), add constructs.json rows, and either fold it into the P13 Wave D directives batch or re-tag R2's unapplied leg to an open phase.
- **Disposition:** _pending_

### C16. [MINOR | doc-inconsistency | A1-directives] ConditionalCompilationProcessor doc comment contradicts the code: >>EVALUATE claimed deferred but implemented; FLAG-85/FLAG-NATIVE-ARITHMETIC mislabeled 'ISO §7.3 directives'

- **Refs:** src/Cobol.Net.Frontend/Preprocessor/ConditionalCompilationProcessor.cs:8 and :20-22 (summary says 'Deferred … >>EVALUATE/>>WHEN') vs :88-122 (EVALUATE/WHEN/END-EVALUATE fully implemented, both formats); :29-38 (KnownIgnoredDirectives described as 'Standard ISO §7.3 compiler directives' yet contains FLAG-85 and FLAG-NATIVE-ARITHMETIC); specs/ISO_COBOL.md:4359/4439 (the 2023 §7.3 FLAG directives are FLAG-02/FLAG-14 only — no FLAG-85 or FLAG-NATIVE-ARITHMETIC entry in the §7.3 TOC at lines 164-188)
- **Evidence:** The class summary (lines 8, 20-22) still describes the pre-EVALUATE implementation state — >>EVALUATE/>>WHEN are handled at lines 88-122 including Format 2 (TRUE) and THROUGH ranges — so a reader (or a future wave scout) would wrongly classify EVALUATE as unimplemented. The KnownIgnoredDirectives doc claims every member is a 'Standard ISO §7.3 compiler directive' but FLAG-85 and FLAG-NATIVE-ARITHMETIC exist only in the 2002 edition (absent from the 2023 §7.3 catalog), while the two directives that ARE in the 2023 catalog (FLAG-02/FLAG-14) are deliberately absent pending Wave D — an inversion a doc reader cannot see.
- **Proposed action:** Update the doc comment in the same change set as the next Wave D commit: move EVALUATE from the 'Deferred' list to 'Supported'; keep the genuinely deferred items (compile-time expressions, PARAMETER, CC-in-library-text) pointing at their now-named tracks; annotate FLAG-85/FLAG-NATIVE-ARITHMETIC as 2002-edition-only compatibility swallows (distinct from the 2023 §7.3 set).
- **Verifier correction:** ConditionalCompilationProcessor.cs doc comment contradicts the code twice: (1) lines 20-22 list >>EVALUATE/>>WHEN as "Deferred" while lines 88-122 fully implement EVALUATE/WHEN/END-EVALUATE (both formats), and line 31's own comment lists them as having "real behavior"; (2) the KnownIgnoredDirectives doc (lines 28-33) calls all members "Standard ISO §7.3 compiler directives", but FLAG-85 and FLAG-NATIVE-ARITHMETIC are 2002-edition directives (carried as obsolete in 2014, not 2002-only) that the 2023 standard explicitly REMOVED (specs/ISO_COBOL.md:49272-49276), while the actual 2023 FLAG directives FLAG-02 (§7.3.14) and FLAG-14 (§7.3.15) are absent from the set pending Wave D. | ConditionalCompilationProcessor.cs doc comments contradict the code and the 2023 spec: (1) the class summary (lines 20-22) lists >>EVALUATE/>>WHEN as 'Deferred' while lines 88-122 fully implement EVALUATE/WHEN/END-EVALUATE (both formats + OTHER) and the sibling comment at line 31 says they 'have real behavior'; (2) the KnownIgnoredDirectives comment (line 29) labels every member a 'Standard ISO §7.3 compiler directive', but FLAG-85 and FLAG-NATIVE-ARITHMETIC are prior-edition (2002/2014, obsolete-in-2014) directives REMOVED from the 2023 standard (specs/ISO_COBOL.md:49272-49276; the 2023 §7.3 TOC at lines 172-188 has only FLAG-02/FLAG-14) — not '2002-edition-only' as originally stated. Fix: in the Wave D directive commit, move EVALUATE/WHEN to the Supported list (keeping DEFINE arithmetic/boolean operands, PARAMETER, and CC-in-copied-library-text as the genuinely deferred residues) and annotate FLAG-85/FLAG-NATIVE-ARITHMETIC as pre-2023 compatibility swallows distinct from the 2023 §7.3 set.
- **Disposition:** _pending_

### C17. [MINOR | doc-inconsistency | A8-rw-screen-mcs] CONFORMANCE.md §4 defines the MCS non-support facility with 'the COMMUNICATION SECTION' — a construct that does not exist in ISO/IEC 1989:2023

- **Refs:** docs/CONFORMANCE.md:122-124 vs specs/ISO_COBOL.md (zero matches for 'COMMUNICATION SECTION' / ENABLE / DISABLE / PURGE statements); the 2023 asynchronous-messaging surface = SEND §14.9.38 (specs:30992), RECEIVE §14.9.31 (specs:30139), USAGE MESSAGE-TAG §13.18.60 GR9 (specs:22795) + A.1 item 212 (specs:39803); Annex A.4.3 item 2 APPLY COMMIT clause §12.4.6.3 (specs:40321)
- **Evidence:** The COMMUNICATION SECTION/CD model is the pre-2002 Communication Module, removed from the standard; the 2023 A.3-item-4 asynchronous messaging facility consists of the SEND/RECEIVE statements and USAGE MESSAGE-TAG. Naming a nonexistent section risks steering the tracked Wave H recognize-and-name work at a construct with no 2023 grammar while omitting the syntactically-detectable USAGE MESSAGE-TAG leg. Similarly, the commit/rollback row (:124) names only the COMMIT/ROLLBACK statements — A.4.3 also lists the IO-CONTROL APPLY COMMIT clause (§12.4.6.3) and the EC-FLOW-APPLY-COMMIT/-COMMIT/-ROLLBACK conditions as facility members.
- **Proposed action:** Correct CONFORMANCE.md §4 item 1 to 'SEND (§14.9.38), RECEIVE (§14.9.31), USAGE MESSAGE-TAG (§13.18.60)' and add the APPLY COMMIT clause + EC-FLOW-* members to item 2; scope the Wave H recognizer to these actual 2023 constructs.
- **Verifier correction:** docs/CONFORMANCE.md §4 item 1 (lines 122-123) names "the COMMUNICATION SECTION" as part of the MCS non-support facility, but that construct does not exist in ISO/IEC 1989:2023 (zero occurrences; ENABLE/DISABLE/PURGE also absent — it is the pre-2002 Communication Module). The actual 2023 facility surface is SEND §14.9.38, RECEIVE §14.9.31, and USAGE MESSAGE-TAG §13.18.60 GR9 (A.1 item 212). Additionally, §4 item 2 (line 124) understates the commit/rollback facility: Annex A.4.3 also lists the IO-CONTROL APPLY COMMIT clause §12.4.6.3 and the EC-FLOW-APPLY-COMMIT/EC-FLOW-COMMIT/EC-FLOW-ROLLBACK (plus EC-FLOW-APPLY per §12.4.6.3 GR3/GR4) exception conditions as facility members. Fix: reword §4 items 1-2 to enumerate the real 2023 constructs and keep the Wave H recognize-and-name scope on SEND/RECEIVE/USAGE MESSAGE-TAG and COMMIT/ROLLBACK/APPLY COMMIT. | docs/CONFORMANCE.md §4 item 1 (:122-123) defines the MCS non-support facility as including 'the COMMUNICATION SECTION' — a construct with zero occurrences in specs/ISO_COBOL.md (the '85 Communication Module was deleted in 2002; the scout itself says so at PHASE-13-remaining-waves-scout.md:1643). The 2023 facility surface is SEND (§14.9.38), RECEIVE (§14.9.31), and USAGE MESSAGE-TAG (§13.18.60 GR9 / A.1 item 212). Item 2 (:124) names only COMMIT/ROLLBACK, while Annex A.4.3 (specs:40311-40327) enumerates the facility as five members including the IO-CONTROL APPLY COMMIT clause (§12.4.6.3) and the EC-FLOW-APPLY-COMMIT/-COMMIT/-ROLLBACK conditions. Neither correction is tracked: the Wave H scout plan steps (scout:1673, :1734) say the CONFORMANCE.md rows 'already list' the facilities and only plan to append the diagnostic codes, carrying the wrong/incomplete wording forward. CORRECTIONS to the original finding: (a) MESSAGE-TAG is NOT omitted from the doc row — it is already listed; the omission is only in the planned Wave H recognizer surface (no USAGE MESSAGE-TAG leg), a debatable secondary refinement; (b) the 'risks steering Wave H at a nonexistent construct' framing is overstated — the actual implementation driver (scout VCR 38) correctly scopes recognition to keyword-led SEND/RECEIVE and never mentions COMMUNICATION SECTION, so this is a doc-accuracy fix, not an implementation-steering hazard; (c) APPLY COMMIT's runtime absence is separately tracked (PHASE-10-m2-residual-catalog.md:311), but the CONFORMANCE.md facility-membership wording is not. Proposed action stands: in CONFORMANCE.md §4, replace 'the COMMUNICATION SECTION' with 'USAGE MESSAGE-TAG (§13.18.60)' and cite SEND §14.9.38 / RECEIVE §14.9.31; extend item 2 with the APPLY COMMIT clause (§12.4.6.3) and the EC-FLOW-* members per A.4.3 — fold both edits into the already-planned Wave H CONFORMANCE.md touch. Severity: minor (doc-only).
- **Disposition:** _pending_

### C18. [MINOR | doc-inconsistency | A8-rw-screen-mcs] Wrong spec-clause citations in the conformance record and files design doc (OPEN cited as 14.9.26=MULTIPLY, DISPLAY as 14.9.13=EVALUATE, SORT as 14.9.45=SUPPRESS)

- **Refs:** docs/CONFORMANCE.md:55-56 (rows 32-34 '14.9.26' for the OPEN phrases; actual: OPEN=§14.9.27, specs/ISO_COBOL.md:29127, and A.4.7 item 6 cites 14.9.27) and :49 (row 24 '14.9.13' for DISPLAY positioning; actual: DISPLAY=§14.9.11, specs:26829; 14.9.13=EVALUATE); docs/COBOLNET_FILES_DESIGN.md:159 ('14.9.24 and 14.9.45 SORT and MERGE' — §14.9.45=SUPPRESS, SORT=§14.9.40 specs:31758, MERGE=§14.9.24 specs:28567; the following '14.9.40 and 14.9.22 numeric-key value compare' is likewise garbled since 14.9.40 IS SORT)
- **Evidence:** Verified against the section headings in specs/ISO_COBOL.md: '## 14.9.26 MULTIPLY statement' (29048), '## 14.9.27 OPEN statement' (29127), '## 14.9.11 DISPLAY statement' (26829), '## 14.9.40 SORT statement' (31758), '## 14.9.45 SUPPRESS statement' (32545). Per the project's spec-to-code traceability rule these citations are load-bearing; a reader chasing CONFORMANCE row 32 lands on MULTIPLY.
- **Proposed action:** Fix the three citation sites (OPEN→14.9.27, DISPLAY→14.9.11, SORT/MERGE→14.9.40/14.9.24 and repair the numeric-key-compare reference), then run a quick §-number spot-sweep of CONFORMANCE.md's § column against the spec headings.
- **Verifier correction:** Wrong 2023-spec clause citations in docs/CONFORMANCE.md §2 and docs/COBOLNET_FILES_DESIGN.md, apparently carried over from an earlier edition's numbering: (1) CONFORMANCE.md:55-56 rows 32 and 33-34 cite 14.9.26 (=MULTIPLY) for the OPEN phrases — OPEN is §14.9.27; (2) CONFORMANCE.md:49 row 24 cites 14.9.13 (=EVALUATE) for DISPLAY positioning — DISPLAY is §14.9.11; (3) ADDITIONAL, missed by the original finding: CONFORMANCE.md:53 row 28-30 cites 14.9.7 (=COMMIT) for the CLOSE phrases — CLOSE is §14.9.6; (4) COBOLNET_FILES_DESIGN.md:159 '14.9.24 and 14.9.45 SORT and MERGE' — SORT=§14.9.40, MERGE=§14.9.24, §14.9.45=SUPPRESS; and '14.9.40 and 14.9.22 numeric-key value compare' is garbled (14.9.40=SORT, 14.9.22=INSPECT); (5) ALSO on the same list, line 158: '13.18.41 implicit default record' — §13.18.41 is PRESENT WHEN; the implicit-record rules live under §13.18.43 RECORD clause / §9.1.2. Fix all five sites, then sweep the § column of CONFORMANCE.md §2 against the spec headings (rows verified already correct per 2023: 31=14.9.10 DELETE, 35=14.9.35 REWRITE, 36=14.9.49 USE, 37=14.9.51 WRITE, 39=14.9.30 READ/14.9.41 START; FILES_DESIGN 13.18.13 CODE-SET, 13.18.34 LINAGE, 13.18.43 RECORD are correct).
- **Disposition:** _pending_

### C19. [MINOR | doc-inconsistency | B2-phase-claims] COBOLNET0849 is claimed by two features — PHASE-09's "(new)" mint for multi-base INHERITS collides with the EC-OO RAISING band that already owned it

- **Refs:** docs/rearchitecture/PHASE-09-m2-oo-rearchitect-and-complete.md:107,370 vs docs/COBOLNET_OO_SLICE_BRIEFS.md:640 + docs/COBOLNET_CONDITIONS_EXCEPTIONS_DESIGN.md:30; src/Cobol.Net.Compiler/Oo/OoClassTable.cs:207 vs src/Cobol.Net.Compiler/Binding/Procedure/Verbs/EcBinder.cs:184,191,200; git 140d2e86 (2026-07-11) vs 8d5fb56f (2026-07-16)
- **Evidence:** COBOLNET_OO_SLICE_BRIEFS.md:640 assigns the EC-OO wave "the diagnostic codes are 0848/0849/0858/0859", and EcBinder.cs:184/191/200 emits 0849 for the three RAISING identifier legs (ISO §14.9.18.3 SR4/SR4a/SR4d) — in the tree since before P9 (moved into EcBinder at P7, commit 140d2e86, 2026-07-11). PHASE-09 then instructed "Confirm 0849 is free before minting (Step 10)" (line 107) and recorded "COBOLNET0849 (new)" for the multi-base INHERITS rejection (line 370), landed 2026-07-16 (8d5fb56f, OoClassTable.cs:207). 0849 was not free; the two features now share the code, and the P9 phase record's free/new claim is false. (The registry design does tolerate repeated Codes — DIAGNOSTICS.md preamble: "A Code may repeat across rows — the stable Id is the identity" — but neither doc records this as a deliberate share, and the oo-multi-base-inherits negative golden pins only the number, so it could pass on the wrong check.)
- **Proposed action:** Either document the share explicitly (correct PHASE-09 line 370's "(new)" and add both uses to the diag-band record so future "confirm free" sweeps see it), or re-mint one side onto a free code (cheapest: the P9 INHERITS diagnostic, updating tests/conformance/negative/oo-multi-base-inherits.err and the OoSpineTests fact in the same commit).
- **Verifier correction:** COBOLNET0849 is emitted by two unrelated features: the EC-OO RAISING identifier legs (EcBinder.cs:184/191/200, ISO §14.9.18.3 SR4/SR4d/SR4a; in-tree since 2026-07-04, commit 3a19251e, moved to EcBinder at 140d2e86) and the PHASE-09 multi-base INHERITS rejection (OoClassTable.cs:207, §11.3.2; landed 8d5fb56f 2026-07-16). PHASE-09-m2-oo-rearchitect-and-complete.md:370's "COBOLNET0849 (new)" and line 107's "Confirm 0849 is free" are false — the code was already assigned to the EC-OO band (COBOLNET_OO_SLICE_BRIEFS.md:640; COBOLNET_CONDITIONS_EXCEPTIONS_DESIGN.md:30) and no doc records the share as deliberate. Two refinements: (1) the DIAGNOSTICS.md "a Code may repeat — the Id is the identity" tolerance covers only DiagnosticCatalog registry rows (scope: edition band + 0899 split + 1533), and NEITHER 0849 use is a registry row (both are bare string literals), so the collision is not covered by the documented repeat mechanism — this strengthens the finding; (2) the "golden could pass on the wrong check" parenthetical is theoretical only — oo-multi-base-inherits.cob has no PROCEDURE DIVISION or RAISING construct, so the EC-OO legs cannot fire on that fixture, though the .err does pin only the bare code number.
- **Disposition:** _pending_

### C20. [MINOR | doc-inconsistency | B2-phase-claims] resume-prompt.md top RESUME banner is two commits stale: claims HEAD f71faa0f / conformance 3678, contradicting its own Wave E section (3685) and the verified count at real HEAD (3691)

- **Refs:** resume-prompt.md:26-28,33-34 vs resume-prompt.md:~146-152 (Wave E section updated in 9a33dfa7/38c4a669); git HEAD 38c4a669; verified via dotnet test --list-tests: Cobol.Net.Tests.Conformance = 3691, Cobol.Net.Tests.Unit = 311
- **Evidence:** The banner (the doc every process rule says to trust as the live resume point) reads "pushed through f71faa0f … Battery at HEAD: greenfield conformance 3678 · unit 311" while the branch HEAD is 38c4a669 (Wave E pt1+pt2 landed after the last banner refresh 6ca8d184). The same file's Wave E section records "Conformance 3685" at pt1, and the discoverable conformance test count at the current HEAD build is 3691 (3685 + the 6 Wave E pt2 goldens) — so the banner's tally and pushed-through hash are both stale. The banner's top-line remaining enumeration (line 33: "① GRAMMAR BATCH · ③ Wave D directives · ④ Wave I") also omits the still-pending Wave E VCR 15 (EC-EXTERNAL-*), which the detailed REMAINING list below it still carries.
- **Proposed action:** Refresh the top banner in the next docs commit: pushed-through hash 38c4a669, conformance tally 3691, and add Wave E VCR 15 to the top-line remaining enumeration (the detailed section is already correct). Unit 311 and characterization 33 need no change.
- **Verifier correction:** resume-prompt.md's top RESUME banner is three commits stale (banner refreshed at 6ca8d184; daaa11f8 + 9a33dfa7 + 38c4a669 landed after): it claims pushed-through f71faa0f and conformance 3678, contradicting the same file's Wave E section (3685 at pt1 + 6 pt2 goldens) and the verified count at real HEAD 38c4a669 (3691 discoverable conformance tests). The banner's remaining-work enumeration (line 33) also omits the still-pending Wave E VCR 15 (EC-EXTERNAL-*), which line 153 still carries. | resume-prompt.md's top RESUME banner is stale by three commits (two feature: Wave E pt1 9a33dfa7 + pt2 38c4a669; one docs: daaa11f8): it claims pushed-through f71faa0f and conformance 3678, contradicting the same file's Wave E section (3685 at pt1 + 6 pt2 goldens) and the verified 3691 discoverable conformance tests at HEAD 38c4a669; its top-line remaining enumeration (①·③·④) also drops ② despite Wave E VCR 15 (EC-EXTERNAL-*) still pending at line 153.
- **Disposition:** _pending_

### C21. [MINOR | doc-inconsistency | B3-ledger-consistency] DiagnosticCatalog comments still earmark 1550/1551/1552 for PHASE-12, which closed without consuming them — three codes silently excluded from every next-free computation

- **Refs:** E:\CobolSharp\src\Cobol.Net.Editions\Diagnostics\DiagnosticCatalog.cs:93,176 ('1550/1551/1552 earmarked (PHASE-12)' / '1550/1551/1552 stay earmarked (PHASE-12)'); docs/COBOLNET_REARCHITECTURE_PLAN.md (PHASE-12 DONE 2026-07-17)
- **Evidence:** grep of src shows no COBOLNET1550/1551/1552 emission anywhere; PHASE-12 completed (DEVLOG 880–884) without using them, yet the catalog — the allocation authority the next-free checks grep — still reserves them for a closed phase. Every 'next free' claim (resume-prompt 1576+) silently skips three actually-free codes on the strength of a stale earmark.
- **Proposed action:** Update both DiagnosticCatalog comments to either release 1550–1552 as free (and note them in the resume-prompt band ledger) or re-earmark them for a named live purpose; keep the earmark text next to the descriptors so the catalog remains the single allocation ledger.
- **Verifier correction:** DiagnosticCatalog.cs:93 and :176 still earmark COBOLNET1550/1551/1552 "(PHASE-12)", but PHASE-12 closed (DEVLOG 880-884) without consuming them: the P12 scout (docs/rearchitecture/PHASE-12-scout-notes.md:60,368) explicitly ruled 1552 unnecessary, and the 1550/1551 planned uses (staged-loud SET function-/program-pointer legs) landed under the COBOLNET0899 NotImplemented umbrella instead (DiagnosticCatalog.cs:138-149). The three codes are therefore free-but-reserved-for-a-closed-phase, and the high-water next-free ledger (resume-prompt.md:155 "1576+ (verify DiagnosticCatalog)") skips them on the strength of the stale earmark. Fix: update both catalog comments to either release 1550-1552 as free, or re-earmark 1550/1551 by NAME for the still-tracked P12 pointer residues (FUNCTION-POINTER runtime / restricted PROGRAM-POINTER, currently staged 0899) rather than the closed phase label, and drop 1552 entirely. | DiagnosticCatalog.cs:93 and :176 still earmark COBOLNET1550/1551/1552 for PHASE-12, but PHASE-12 closed (2026-07-17) without consuming any of them: the P12 scout ruled 1552 unnecessary (duplicate of the COBOLNET0899 staged-loud gate, PHASE-12-scout-notes.md:60/256) and 1550/1551's planned purpose (loud not-yet-supported pointer SET-format gates, PHASE-12-m3-2014-deltas.md:233) was likewise superseded by 0899 staging; P12 drew its real codes from 1561-1564. The three codes are the only unused holes in the live 1501-1575 band (verified by grep of src), and no live ledger (resume-prompt band ledger, DEVLOG post-P12, CONFORMANCE.md) releases or re-purposes them — the catalog, the allocation authority, reserves them for a dead purpose. Impact nuance: the next-free computation is grep-of-used-codes + upward allocation, so the earmark does not literally drive the skip, but it is the only live reservation fencing the holes and its rationale is obsolete.
- **Disposition:** _pending_

### C22. [MINOR | impl-vs-spec-drift | C1-waveE-drift] '§13.6.2 GR7' citation is wrong — the external/CONSTANT-RECORD initial-state rule is §11.9.10.4 GR7 (OPTIONS paragraph INITIALIZE clause); §13.6.2 is the LOCAL-STORAGE general format with no GRs

- **Refs:** specs/ISO_COBOL.md:13652 (§11.9.10.4 GR7: 'External items in the Working-storage section are not initialized when runtime elements are put into the initial state, except for those with the CONSTANT RECORD clause') vs :16488-16499 (§13.6.2 = LOCAL-STORAGE SECTION general format); src/Cobol.Net.Compiler/CodeGen/Verbs/OoEmitter.cs:45, src/Cobol.Net.Compiler/Binding/DataBinder.Linkage.cs:40, src/Cobol.Net.Compiler/Binding/DataBinder.cs:1979, resume-prompt.md:142+:147, DEVLOG 904 header, commit 9a33dfa7 subject
- **Evidence:** The implemented behavior is spec-correct (CONSTANT RECORD external cell seeds the VALUE-composed ImageInitOf image, the one initial-state exception; plain externals seed blank per §13.18.63.4 GR4a, spec :23380 — verified), but all three code sites, the resume-prompt banner, and the commit subject cite '§13.6.2 GR7'. In the 2023 spec §13.6 is the Local-storage section and §13.6.2 its general-format diagram — there is no GR7; the quoted rule lives in §11.9.10.4 GR7, backed by §14.6.2.3.3 (:24111, external data always last-used except run-unit activation). A future spec-derivation from the cited § would find unrelated text — the exact failure mode of the project's cite-the-§ rule.
- **Proposed action:** Sweep '§13.6.2' → '§11.9.10.4 GR7 (OPTIONS paragraph INITIALIZE clause)' in OoEmitter.cs:45, DataBinder.Linkage.cs:40, DataBinder.cs:1979, and resume-prompt.md:142/:147 (optionally add §14.6.2.3.3 as the last-used-state anchor). Leave DEVLOG/commit history as-is per the docs-current-state rule.
- **Verifier correction:** The finding is accurate as stated. Optional addition: where the comment covers FD … IS EXTERNAL record areas (OoEmitter.EmitExternalBackings), §11.9.10.4 GR7 (working-storage externals) may be supplemented with §13.18.63.4 GR2 (file-section VALUE takes effect only during INITIALIZE) since GR7's text scopes to the Working-storage section. | The '§13.6.2 GR7' citation is wrong everywhere it appears: the external/CONSTANT-RECORD initial-state rule is §11.9.10.4 GR7 (OPTIONS paragraph INITIALIZE clause, spec line 13652); §13.6.2 (line 16489) is the LOCAL-STORAGE SECTION general format with no GRs (§13.6.4 has only 2 GRs, neither about externals). The mislabel ORIGINATED in docs/rearchitecture/PHASE-13-remaining-waves-scout.md:1033 (which pairs the wrong § label with the CORRECT spec line 13652) and propagated to OoEmitter.cs:45, DataBinder.Linkage.cs:40, DataBinder.cs:1979, resume-prompt.md:142/:147, DEVLOG 904, and commit 9a33dfa7. Sweep '§13.6.2 GR7' -> '§11.9.10.4 GR7' in the three code sites, resume-prompt.md:142/:147, AND the scout doc :1033 (the origin, a LIVE trusted P13 doc); leave DEVLOG/commit history as-is.
- **Disposition:** _pending_

### C23. [MINOR | impl-vs-spec-drift | C1-waveE-drift] 'Annex E.3 item 10' citation is wrong in the shipped ConstructRegistry citation string and comments — the quoted text is Annex E.2 item 10; the scout 'corrected' the audit's valid E.3.3-item-20 pointer into this error, and VCR row 63 now disagrees with code

- **Refs:** specs/ISO_COBOL.md:49148 (E.2 item 10; E.2 starts :49022, E.3 starts :49398; E.3.3 item 10 = :50235, a SIZE-ERROR clarification; E.3.3 item 20 = :50277); src/Cobol.Net.Editions/ConstructRegistry.g.cs:30, tests/version-matrix/constructs.json:199 row, src/Cobol.Net.Compiler/Validation/VersionConformancePass.cs:785-786+:805, src/Cobol.Net.Compiler/Binding/DataBinder.cs:1976, docs/rearchitecture/PHASE-13-remaining-waves-scout.md:991-1002, docs/VERSION_CHANGE_REFERENCE.md:180 (row 63: 'E.3.3 item 20') + :329 (row 16 → line 49148)
- **Evidence:** The 2023-only gating substance is correct, but the user-facing registry citation 'ISO §13.18.22.3 SR1/SR5; §8.5.3; Annex E.3 item 10' (ConstructRegistry.g.cs:30) mislabels the annex: line 49148 ('previously external items could not be strongly typed' — the phrase the comments quote) is E.2 item 10; E.3's actual item 10 (:50235) is about SIZE ERROR rules. The scout (:997) declared the audit's 'E.3.3 item 20' pointer wrong and substituted 'E.3 item 10 (line 49148)' — the line number is right, the annex label wrong — and this propagated into constructs.json, the generated registry, and two code comment blocks. Meanwhile VCR row 63 correctly cites E.3.3 item 20 and the VCR line-map ties row 16 to 49148 (E.2 item 10), so the authoritative docs and shipped citation now disagree.
- **Proposed action:** Fix constructs.json row external-type-declaration-2023's citation to 'ISO §13.18.22.3 SR1/SR5; §8.5.3; Annex E.2 item 10 / E.3.3 item 20' and regenerate via scripts/gen-constructs.ps1 (ConstructRegistryDriftTests). Sweep 'E.3 item 10' → 'E.2 item 10' in VersionConformancePass.cs:785/786/805, DataBinder.cs:1976, resume-prompt.md:143, and add an erratum note in the scout doc (its :997 'correction' of the audit was itself the drift — feedback_propagate_reconciliations sweep).
- **Disposition:** _pending_

### C24. [MINOR | impl-vs-spec-drift | C2-waveG-drift] EXCEPTION-FILE(connector) r2a: EverAccessed not set by non-OPEN access attempts, and SD names accepted where §15.28.3 requires an FD connector

- **Refs:** src/Cobol.Net.Runtime/IO/FileConnector.cs:42-45,121 · src/Cobol.Net.Runtime/IO/FileRegistry.cs:229-234,352 · src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:302-309; spec §15.28.4 r2a/r2b (lines 35464-35466), §15.28.3 rule 1 (line 35435)
- **Evidence:** §15.28.4 r2a returns two spaces only when the connector "has never been opened, attempted to be opened, or OTHERWISE ATTEMPTED TO BE ACCESSED"; otherwise r2b returns status+name. EverAccessed is set only by FileConnector.Open and FileRegistry.DeleteFile — a READ/WRITE/CLOSE/START attempted against a never-opened connector sets the connector's I-O status (e.g. '47' at SequentialConnector.cs:370) yet leaves EverAccessed false, so FUNCTION EXCEPTION-FILE(f) returns "  " where r2b requires "47"+name. Separately, §15.28.3 rule 1 requires argument-1 to name "a file connector that is specified in an FD statement", but BindExceptionFileArg matches ctx.Data.Files without excluding IsSortMerge, so an SD file-name passes where the 1574 rejection should fire.
- **Proposed action:** Set EverAccessed=true at the top of every connector verb entry point (Read/Write/Rewrite/Delete/Start/Close), not just Open/DeleteFile; add an IsSortMerge exclusion (→ COBOLNET1574) in BindExceptionFileArg.
- **Disposition:** _pending_

### C25. [MINOR | impl-vs-spec-drift | C3-ec-refmod-drift] Nonfatal ambient EC raises (EC-BOUND-OVERFLOW, EC-DATA-CONVERSION) never execute an applicable USE F3 declarative

- **Refs:** ISO §14.6.13.1.4 #3 (specs/ISO_COBOL.md:24570) + §8.5.1.9.6 GR1 2nd sentence (:8255) · src/Cobol.Net.Compiler/CodeGen/EcEmitter.cs:64-91 · src/Cobol.Net.Runtime/Exceptions/ExceptionState.cs:214-217
- **Evidence:** §14.6.13.1.4 #3: for a nonfatal EC with checking enabled, 'If there is an applicable USE statement in the source unit that specifies the exception-name … the associated declarative is executed.' §8.5.1.9.6 GR1 explicitly contemplates 'the execution of a declarative procedure … that executes a RESUME statement with the NEXT STATEMENT phrase' for EC-BOUND-OVERFLOW. The implementation's NonfatalAmbientGates wrapper is set/reset ONLY ('no catch, no throw — nonfatal ⇒ the raise only records the last exception status', EcEmitter.cs:66; the wrapper at :82-91 emits no post-statement F3 dispatch) and ExceptionEngine.BoundOverflowError only calls Set(name, fatal:false). A program with >>TURN EC-BOUND-OVERFLOW CHECKING ON plus USE AFTER EXCEPTION CONDITION EC-BOUND-OVERFLOW never runs that declarative when implicit growth crosses the expected capacity — silently, not loudly. Same gap for EC-DATA-CONVERSION. Not recorded in the conditions-exceptions deep-dive or any tracked residue list (grep of the deep-dive finds no mention).
- **Proposed action:** Emit a post-statement F3 selection for the nonfatal ambient gates when the statement's enabled set intersects a USE F3 declarative's names (check LastName after the try/finally, run EcDispatchExpr, honor RESUME AT), or — if deliberately deferred — record the gap as a flagged follow-on in COBOLNET_CONDITIONS_EXCEPTIONS_DESIGN.md and the P13 worklist so the 'EC-BOUND surface CLOSED' claim is qualified.
- **Verifier correction:** Verified as stated, with one refinement: §14.6.13.1.4 orders the declarative BEFORE statement execution continues (#3 precedes #4, 'processing of the statement is interrupted'), so the proposed post-statement F3 dispatch is itself a spec approximation for mid-statement raise sites (implicit table growth, conversion substitution) — acceptable, but the fix/deferral record should note the ordering nuance. | Nonfatal ambient EC raises (EC-BOUND-OVERFLOW, EC-DATA-CONVERSION) never execute an applicable USE F3 declarative, contra §14.6.13.1.4 #3 and §8.5.1.9.6 GR1 2nd para. The drift originates in the scout design (docs/rearchitecture/PHASE-13-remaining-waves-scout.md:701 specified the raise as "nonfatal ⇒ never throws — exactly DataConversionError", i.e. set-status-only), which the code faithfully implements: EcEmitter.EmitChecked (src/Cobol.Net.Compiler/CodeGen/EcEmitter.cs:75-95) emits set/reset of the checking flags with no post-statement F3 selection, and ExceptionState.DataConversionError/BoundOverflowError (src/Cobol.Net.Runtime/Exceptions/ExceptionState.cs:198-201, 214-217) only Set(..., fatal:false). No other __EcDispatch call site covers these two names. Untracked: the conditions/exceptions deep-dive has no mention of the limitation, and no worklist/ledger names it; the golden ec_bound_overflow.cob observes EXCEPTION-STATUS only.
- **Disposition:** _pending_

### C26. [MINOR | doc-inconsistency | C3-ec-refmod-drift] COBOLNET_DATA_MODEL_DESIGN.md still says EC-BOUND-OVERFLOW 'remains the flagged nonfatal follow-on' — the raise landed in P13

- **Refs:** docs/COBOLNET_DATA_MODEL_DESIGN.md:311-314,326 · src/Cobol.Net.Runtime/Values/Tables/CobolDynTable.cs:59-75 · resume-prompt/MEMORY 'EC-BOUND surface CLOSED'
- **Evidence:** The data-model deep-dive states 'EC-BOUND-OVERFLOW (nonfatal, checking-gated) … remain the two flagged follow-ons' (:311-314) and 'CobolDynTable.GrowTo does NOT wire EC-BOUND-OVERFLOW/EC-BOUND-SET (both remain the flagged nonfatal follow-on)' (:326), but the EC-BOUND-OVERFLOW raise is shipped: CobolDynTable.RefReceiving raises it on the first implicit crossing of the expected capacity (CobolDynTable.cs:69-71) under the BoundOverflowChecking gate, and the P13 status banner claims 'EC-BOUND-OVERFLOW + EC-BOUND-REF-MOD raise = EC-BOUND surface CLOSED'. Per process rule 4 / feedback_docs_current_state_only every doc except DEVLOG must read as CURRENT state. (EC-BOUND-SET genuinely remains staged — the sentence is half-stale.)
- **Proposed action:** Update COBOLNET_DATA_MODEL_DESIGN.md D-section to current state: EC-BOUND-OVERFLOW is wired (RefReceiving, first-crossing-only per §8.5.1.9.6 GR1, nonfatal under the ambient checking gate); only EC-BOUND-SET (§14.9.39 GR30 explicit-SET twin) and the FULL variable-length-group MOVE/COMPARE remain flagged follow-ons.
- **Verifier correction:** docs/COBOLNET_DATA_MODEL_DESIGN.md is stale about EC-BOUND-OVERFLOW in THREE spots, not two: (1) :275-278 lists it under "Deferred: (a) EC-BOUND-OVERFLOW … ride the EC-integration pass"; (2) :311-314 calls it one of "the two flagged follow-ons … today both are LOUD"; (3) :326's parenthetical "(both remain the flagged nonfatal follow-on)". The raise is shipped: CobolDynTable.RefReceiving (CobolDynTable.cs:69-71) raises nonfatal EC-BOUND-OVERFLOW on the first implicit crossing of the expected capacity, gated on ExceptionState.BoundOverflowChecking (ExceptionState.cs:214-216, wired in EcEmitter.cs:72), matching §8.5.1.9.6 item 1 (specs/ISO_COBOL.md:8253) including the "already exceeded before an implicit change ⇒ no exception" suppression (`_count <= exp`). Nuance: :326's literal clause "GrowTo does NOT wire EC-BOUND-OVERFLOW" is still technically true (the raise lives in RefReceiving BEFORE GrowTo, per GrowTo's own doc-comment :79-82) — only the "remains the flagged follow-on" parentheticals are wrong. EC-BOUND-SET (§14.9.39 GR30 explicit-SET twin) and the FULL variable-length-group MOVE/COMPARE genuinely remain staged follow-ons. Fix all three spots per feedback_propagate_reconciliations/feedback_docs_current_state_only. | docs/COBOLNET_DATA_MODEL_DESIGN.md is stale in THREE spots, not two: (1) :275 "Deferred: (a) EC-BOUND-OVERFLOW ... ride the EC-integration pass"; (2) :311-314 "EC-BOUND-OVERFLOW ... remain the two flagged follow-ons ... today both are LOUD"; (3) :326 "CobolDynTable.GrowTo does NOT wire EC-BOUND-OVERFLOW/EC-BOUND-SET (both remain the flagged nonfatal follow-on)". The EC-BOUND-OVERFLOW raise shipped in P13 commit e574af41 (CobolDynTable.RefReceiving -> ExceptionState.BoundOverflowError, first-crossing-only per §8.5.1.9.6 item 1, nonfatal, gated on BoundOverflowChecking; conformance test tests/conformance/2014/ec_bound_overflow.cob), but that commit did not touch the doc (last doc change = P12 commit 34bc20a4). Only EC-BOUND-SET (§14.9.39 GR30 explicit-SET twin) and the FULL variable-length-group MOVE/COMPARE genuinely remain follow-ons. Update all three doc spots to current state.
- **Disposition:** _pending_

### C27. [MINOR | doc-inconsistency | C3-ec-refmod-drift] DebugItem.cs doc comments contradict the shipped DEBUG-LINE semantics and the group width (83 vs actual 86)

- **Refs:** src/Cobol.Net.Runtime/Control/DebugItem.cs:73-79,103 · src/Cobol.Net.Compiler/CodeGen/DispatchEmitter.cs:146-150 · X3.23-1985 debug module (facility absent from ISO 2023 — zero DEBUG-ITEM hits in specs/ISO_COBOL.md)
- **Evidence:** Two stale comments in the otherwise-correct Wave F implementation: (1) DebugItem.cs:78-79 documents DEBUG-LINE as 'the compiler-assigned source line of the subject procedure' and Populate's param doc (:103) says 'The subject procedure's source line number', but the emitter passes the CAUSING statement's line for every non-START-PROGRAM cause (DispatchEmitter.cs:149 '__dbgCause == DebugCause.StartProgram ? __subjLine : __dbgLine') — exactly the blocker the Wave F adversarial review fixed; the comments describe the pre-fix design. (2) The GroupWidth comment (:74) computes '6 + 1 + 30 + 1 + 4 + 1 + 4 + 1 + 4 + 1 + 30 = 83' using 4-char subs, but SubWidth is 5 (S9(4) SIGN LEADING SEPARATE = sign + 4 digits, :68-69) so the actual constant evaluates to 86; anyone deriving a record layout from the comment gets the wrong width.
- **Proposed action:** Fix both comments: DEBUG-LINE/Populate line = the causing statement's source line (subject's own line only for the START PROGRAM cause), and the GroupWidth arithmetic = 6+1+30+1+5+1+5+1+5+1+30 = 86.
- **Disposition:** _pending_

## 3. Unverified findings (plausible — verification killed by the session limit; re-verify in Wave I)

Finder-reported detail retained verbatim; treat each as PLAUSIBLE, not established. Duplicates of confirmed
findings are marked.

### U1. [MINOR | plan-omission | A1-directives] >>CALL-CONVENTION and >>LEAP-SECOND are silently swallowed with no docs/CONFORMANCE.md disposition and no named track

- **Refs:** ISO §7.3.9.3 GR1/GR2b (specs/ISO_COBOL.md:3967-3973: call-convention-name-1 mapping is implementor-defined), §7.3.17.3 SR1 + GR2/GR3 (4629-4652: LEAP-SECOND ON reporting is implementor-defined; SR1 forbids the directive inside a compilation unit); src/Cobol.Net.Frontend/Preprocessor/ConditionalCompilationProcessor.cs:34-38 (KnownIgnoredDirectives); docs/CONFORMANCE.md (zero matches for CALL-CONVENTION/LEAP-SECOND/LISTING/PAGE); docs/PHASE4_RECONCILIATION.md:1472 ('>>CALL-CONVENTION inert … no named track')
- **Evidence:** Ignoring >>LISTING/>>PAGE is explicitly conformant (§7.3.18.3 GR1 at 4696, §7.3.19.4 GR3 at 4744: no listing ⇒ ignore/no effect), and ignoring >>LEAP-SECOND ON is a permitted implementor choice (GR2 'the implementor defines whether') — but neither choice is recorded in docs/CONFORMANCE.md, the project's documented-dispositions file (SCREEN/locale/IEEE non-supports are). Worse, >>CALL-CONVENTION some-name is swallowed with no diagnostic although COBOL.NET defines no call-convention-names — a program depending on an implementor convention gets the default silently (only '>>CALL-CONVENTION COBOL' is a conformant no-op per GR1). §7.3.17.3 SR1 placement is also unchecked.
- **Proposed action:** Add docs/CONFORMANCE.md disposition rows: LISTING/PAGE (no source listing produced ⇒ conformant ignore, GR-cited), LEAP-SECOND (implementor choice: no leap-second reporting under ON; OFF semantics naturally satisfied), CALL-CONVENTION (only the COBOL convention defined). Emit a named warning when call-convention-name-1 (anything but COBOL) is specified, and optionally check LEAP-SECOND SR1 placement. Small enough to fold into the P13 Wave D directives batch.
- **Disposition:** _unverified — Wave I re-verify_

### U2. [MAJOR | impl-vs-spec-drift | A2-fundamentals-ec] PAGE is reserved in ISO 2023 §8.9 but the generated word table marks it r2023=false with a false provenance, because the generator explicitly filters the token 'PAGE'

- **Refs:** spec §8.9 specs/ISO_COBOL.md:10618 (PAGE in the alphabetical run PACKED-DECIMAL/PAGE/PAGE-COUNTER); scripts/gen-reserved-words.ps1:111; src/Cobol.Net.Editions/ReservedWords.Table.cs:318; tests/version-matrix/reserved-words.json:2750-2757
- **Evidence:** §8.9 lists PAGE as a 2023 reserved word (line 10618, inside the list that runs to §8.10 at :10797). gen-reserved-words.ps1:111 excludes it: `$t -notmatch '^(PAGE|NOTE)$'` (a page-header noise filter, but the markdown page headers are title-case '## Page N' and never match the ALL-CAPS token test). Both mirrors therefore ship `new("PAGE", true, true, true, false, "high", "...absent from ISO 2023 §8.9")` — the provenance string is factually false — and the drift test can't catch it since both sides come from the same generator. Net effect: at --std 2023 the high-confidence COBOLNET0901 word gate accepts PAGE as a user-defined word, and the version matrix records a bogus 2014→2023 'removal' edition delta (inverting the continuity invariant). Related nit found in the same diff: METHOD's provenance cites 'ISO 2023 §8.9', but METHOD appears in neither the 2023 §8.9 nor §8.10 lists (only METHOD-ID) while 'END METHOD [method-name-1].' remains in the 2023 general formats (:12820) — keeping it reserved is defensible, the citation is not. All other spec-vs-table deltas are markdown artifacts (EMD-START typo :10455, FLOAT-NOT-A-NUMBER-QUIET/-SIGNALING line-wraps, I-OICONTROL mangle :10522) that the table already handles correctly.
- **Proposed action:** Fix gen-reserved-words.ps1:111 to stop excluding the PAGE token (filter page headers positionally/by the '## ' prefix instead), regenerate ReservedWords.Table.cs + reserved-words.json so PAGE is r2023=true with a correct provenance, and add a matrix/0901 golden asserting PAGE-as-user-defined-word rejects at all four editions. In the same sweep, correct METHOD's provenance to cite the END METHOD grammar (:12820)/editorial-omission rationale rather than §8.9.
- **Disposition:** _unverified — Wave I re-verify_

### U3. [MAJOR | impl-vs-spec-drift | A2-fundamentals-ec] A runtime-negative explicit ref-mod length collides with the omitted-length sentinel (-1) and silently behaves as 'to the end' instead of raising EC-BOUND-REF-MOD — a hole in the 'EC-BOUND surface CLOSED' claim

- **Refs:** spec §8.4.3.3.4 item 5c + GR at specs/ISO_COBOL.md:7087-7089; src/Cobol.Net.Runtime/Values/Text/CobolString.cs:56,85; src/Cobol.Net.Compiler/CodeGen/Roslyn/PlaceRenderer.cs:130; src/Cobol.Net.Runtime/Exceptions/ExceptionState.cs:227
- **Evidence:** §8.4.3.3.4 5c: 'The evaluation of length shall result in a positive nonzero integer' (zero allowed only under REF-MOD-ZERO-LENGTH); the GR at :7089 sets EC-BOUND-REF-MOD for a non-integer, zero, or out-of-area value. PlaceRenderer.RmLen renders -1 for the OMITTED-length form but renders an explicit length as `(int)({expr})`, whose runtime value can be negative; CobolString.RefMod/SpliceInto then take every `length < 0` as the omitted 'to the end' form (:56/:85 check only `length == 0` and `length > 0` branches), so `FLD(2:L)` with L = -3 under >>TURN EC-BOUND-REF-MOD CHECKING ON raises nothing and reads/splices to the end of FLD — the fatal EC is required. This survives inside a surface the phase banner declares closed ('EC-BOUND-OVERFLOW + EC-BOUND-REF-MOD raise = EC-BOUND surface CLOSED'). Same file, minor citation drift: ExceptionState.cs:227 cites 'ISO §8.4.2.3 c' for this rule — the exact wrong-clause drift (§8.4.2.3 vs §8.4.3.3.4) the P13 remaining-waves scout corrected in the docs, still live in the code comment.
- **Proposed action:** Separate the omitted-length channel from the value channel (e.g. a bool hasLength/int.MinValue sentinel, or emit `Math.Max(...)`-free explicit int with a distinct overload) and add `length < 0` to the raise predicate in both RefMod and SpliceInto; add a negative-evaluated-length golden under CHECKING ON and OFF; fix the §8.4.2.3→§8.4.3.3.4 citation in ExceptionState.cs:227.
- **Disposition:** _unverified — Wave I re-verify_

### U4. [MAJOR | plan-omission | A2-fundamentals-ec] EC-DATA-NOT-FINITE and EC-DATA-OVERFLOW are catalogued but can never be raised, have no seam, and are absent from every residue/audit list — while the enabling binary32/64 standard-float usages shipped in P12

- **Refs:** spec §14.6.13.2 item 3 specs/ISO_COBOL.md:24897; Table 13 :24655 (EC-DATA-NOT-FINITE Fatal), :24668 (EC-DATA-OVERFLOW Fatal); src/Cobol.Net.Runtime/Values/Numeric/CobolFloat.cs:35,39-40,77; src/Cobol.Net.Runtime/Exceptions/ExceptionCatalog.cs:89-90
- **Evidence:** §14.6.13.2 3): referencing a standard-float sending operand that is NaN/infinity sets EC-DATA-NOT-FINITE (Fatal) outside four named exemptions; Table 13 :24668 defines EC-DATA-OVERFLOW as exponent overflow during MOVE to a standard-float receiver. P12 landed FLOAT-SHORT/FLOAT-LONG as native binary32/64, so both conditions are reachable by conforming programs (e.g. MOVE of an overflowing binary64 into binary32; arithmetic reference to a NaN-bearing FLOAT-LONG). CobolFloat.ToScaled maps NaN→0 and saturates ±Inf silently (:35,:39-40 — the comment claims 'implementor-defined', but that latitude exists only while checking is DISABLED per §14.6.13.1.3 #8; under >>TURN EC-DATA-NOT-FINITE CHECKING ON the condition must be set). Both names are catalogued (ExceptionCatalog.cs:89-90) but have zero raise sites, zero named seam comments (unlike the SORT/REPORT/ODO convention), and zero mentions in any doc, the P13 audit, the scouts, or the tracked P12 residues (which stage only external-float E PICTURE and binary128/decimal non-support): grep over docs/ + src (non-catalog) + tests = 0 hits for either name.
- **Proposed action:** Add checking-gated raise seams on the twin pattern of ExceptionEngine.DataConversionChecking/BoundOverflowChecking: an EC-DATA-NOT-FINITE gate at the float sending-reference funnel and an EC-DATA-OVERFLOW gate at the float→float store when the result lands ±Inf from a finite source; or, if deferred, add them as named rows to the P13 audit residue list + a CONFORMANCE.md staging note in the same change set so the gap is tracked.
- **Disposition:** _unverified — Wave I re-verify_

### U5. [MAJOR | plan-omission | A2-fundamentals-ec] The EC-RANGE conditions of fully-implemented statements (SEARCH, PERFORM VARYING, THROUGH ranges) are catalogued but absent entirely — no raise, no seam comment, no doc/audit row

- **Refs:** spec §14.9.35 GR4 specs/ISO_COBOL.md:30935 + :30946,:30962 (EC-RANGE-SEARCH-INDEX/-NO-MATCH); §14.9.28 GR3 :29561 (EC-RANGE-PERFORM-VARYING, Fatal); §14.7.8 :25189 (EC-RANGE-INVALID); src/Cobol.Net.Runtime/Exceptions/ExceptionCatalog.cs:162-168; src/Cobol.Net.Compiler/Binding/Procedure/Verbs/SearchBinder.cs
- **Evidence:** SEARCH must set EC-RANGE-SEARCH-INDEX when the initial index is out of range (:30935) and EC-RANGE-SEARCH-NO-MATCH on serial/binary exhaustion (:30946,:30962); PERFORM VARYING with an index-name and a non-positive FROM identifier must set the Fatal EC-RANGE-PERFORM-VARYING (:29561); a THROUGH range whose start collates above its end must set EC-RANGE-INVALID and evaluate as empty (:25189). SEARCH/SEARCH ALL, PERFORM VARYING, and THROUGH ranges are all implemented (SearchBinder.cs; EC-FLOW-SEARCH is even raised for dynamic tables at CobolDynTable.cs:105), yet these four names have zero raise sites, zero named 'seam' comments (the convention used for every other default-off EC family: SORT/MERGE in SortEmitter.cs:42-160, ODO in CobolTable.cs:49, REPORT in the RW design), and zero mentions in any doc, audit row, scout, or test (per-name grep across docs/, src non-catalog, tests = 0/0/0). This is 'catalogued but never raised AND untracked' — unlike EC-RANGE-INSPECT-SIZE (staged in COBOLNET_STRING_OPS_DESIGN.md:176) and EC-RANGE-INDEX/PTR (VCR row 100 todo / PHASE4_RECONCILIATION unbounded-pointer disposition).
- **Proposed action:** Wire the four raises through the existing checking-gate pattern (the SEARCH pair is cheap: both terminal branches already exist in the emitted search loop; PERFORM VARYING needs one FROM-value guard; THROUGH needs a start>end comparison at range construction) with conformance goldens per feedback_conformance_tests_per_feature — or minimally add named seam comments + a P13-audit residue row so the family enters a tracked list.
- **Disposition:** _unverified — Wave I re-verify_

### U6. [MINOR | plan-omission | A2-fundamentals-ec] EC-FLOW-USE and EC-FLOW-GLOBAL-EXIT/-GOBACK have no guard or seam in the greenfield although (GLOBAL) USE declaratives are implemented — the legacy oracle's USE-recursion guard was not carried over

- **Refs:** spec Table 13 specs/ISO_COBOL.md:24678-24686; §14.6.13.1.3 item 6 :24546 (PROPAGATE excludes EC-FLOW-GLOBAL-EXIT/-GOBACK); src/Cobol.Net.Compiler/Binding/Procedure/ProcedureTableBuilder.cs:316 (__RunGlobalUse GR4b dispatch); legacy src/CobolSharp.Runtime/FileRuntime.cs:26
- **Evidence:** Table 13 makes EXIT PROGRAM/GOBACK inside a global declarative (EC-FLOW-GLOBAL-EXIT/-GOBACK) and a USE statement causing another USE to execute (EC-FLOW-USE) Fatal conditions. The greenfield implements GLOBAL USE declaratives with a cross-program __RunGlobalUse containment walk (ProcedureTableBuilder.cs:316-318), so all three conditions are reachable, but no greenfield code references any of the three names (non-catalog src grep: EC-FLOW-USE appears only in the frozen legacy FileRuntime.cs:26 Enter/ExitUseDeclarative guard; the GLOBAL twins appear nowhere), no seam comment stages them, and no doc/audit/scout row names them (docs grep = 0). They are also load-bearing for tracked work: §14.6.13.1.3 6) requires the >>PROPAGATE runtime semantics (a tracked P12 residue) to exclude exactly EC-FLOW-GLOBAL-EXIT/-GOBACK from propagation, which cannot be honored while the conditions do not exist.
- **Proposed action:** Port the legacy declarative-activity guard onto the greenfield RunUnit (an in-declarative depth flag around USE dispatch → EC-FLOW-USE) and gate EXIT PROGRAM/GOBACK emitted inside a GLOBAL declarative body with the two GLOBAL raises; at minimum add the three names to the P13 audit residue list next to the >>PROPAGATE runtime-semantics item they interlock with.
- **Disposition:** _unverified — Wave I re-verify_

### U7. [MINOR | doc-inconsistency | A2-fundamentals-ec] COBOLNET_DATA_MODEL_DESIGN.md still states GrowTo does NOT wire EC-BOUND-OVERFLOW — contradicted by the landed Wave-E raise

- **Refs:** docs/COBOLNET_DATA_MODEL_DESIGN.md:326; src/Cobol.Net.Runtime/Values/Tables/CobolDynTable.cs:70; src/Cobol.Net.Runtime/Exceptions/ExceptionState.cs:210-217; src/Cobol.Net.Compiler/CodeGen/EcEmitter.cs:72
- **Evidence:** DATA_MODEL_DESIGN.md:326: 'CobolDynTable.GrowTo does NOT wire EC-BOUND-OVERFLOW/EC-BOUND-SET (both remain the flagged nonfatal follow-on).' The code now calls ExceptionState.BoundOverflowError from CobolDynTable.cs:70 (first-crossing implicit growth, §8.5.1.9.6 GR1), gated by the per-statement BoundOverflowChecking enable that EcEmitter.cs:72 wires — the Wave-E landing the resume banner records as 'EC-BOUND surface CLOSED'. The sentence is now half-false (EC-BOUND-SET genuinely remains the staged follow-on, per the ExceptionCatalog.cs:69 comment), violating the docs-current-state-only rule for a DESIGN-status deep-dive.
- **Proposed action:** Rewrite the sentence at docs/COBOLNET_DATA_MODEL_DESIGN.md:326 to state current behavior: EC-BOUND-OVERFLOW is wired in GrowTo (nonfatal, checking-gated, first crossing only); only EC-BOUND-SET remains the flagged nonfatal follow-on.
- **Disposition:** _unverified — Wave I re-verify_

### U8. [MAJOR | plan-vs-impl-error | A5-statements] EXIT SECTION: SSOT + control-flow deep-dive present the pc-move lowering as current, but the binder emits BoundUnsupported (loud runtime failure)

- **Refs:** ISO §14.9.14.2 Format 4, §14.9.14.3 SR9; docs/COBOLNET_DESIGN.md:402; docs/COBOLNET_CONTROL_FLOW_DESIGN.md:70-72,177-180,242,261; E:\CobolSharp\src\Cobol.Net.Compiler\Binding\Procedure\Verbs\ControlFlowBinder.cs:102; docs/PHASE4_RECONCILIATION.md:1465
- **Evidence:** Spec Format 4: EXIT {PARAGRAPH|SECTION} — exit the structured procedure without executing following statements. The design SSOT table (COBOLNET_DESIGN.md:402 'EXIT SECTION | pc = lastParaIdxInSection+1; break;') and deep-dive D8 (incl. the last-section edge case at :242 and a planned 2002-intro edition row at :261) present the lowering as the CURRENT design (docs must read as current state), but ControlFlowBinder.cs:102 binds `return new BoundUnsupported("EXIT SECTION");   // needs section bounds — later` — no BoundExitSection node exists anywhere in src (grep ExitSection: 0 hits outside this line), no constructs.json exit-section row exists, and the emitter renders BoundUnsupported as a loud RUNTIME guard, so any 2002+ program using EXIT SECTION compiles then fails at run time. EXIT PARAGRAPH (:85) and EXIT PERFORM [CYCLE] (:86) are implemented. Only tracking is the stale PHASE4_RECONCILIATION.md:1465 'phase 7 residue' — phase 7 closed without it; it is absent from the P10 forward-residue ledger and the entire P13 worklist.
- **Proposed action:** Implement BoundExitSection + the D8 emitter pc-move (ProcedureTableBuilder already owns section paragraph ranges — the same data BindPerform uses for SECTION ranges at ControlFlowBinder.cs:129-134), add the exit-section-2002 matrix row + a golden, and enforce SR9 (SECTION phrase only inside a section). If deliberately deferred instead, downgrade the SSOT/deep-dive rows to a staged-residue note and add a P13/P14 worklist row.
- **Disposition:** _unverified — Wave I re-verify_

### U9. [MAJOR | impl-vs-spec-drift | A5-statements] DISPLAY ... UPON mnemonic-name-1 is parsed then silently dropped: output device ignored and SR2 never checked (asymmetric with ACCEPT, which enforces it)

- **Refs:** ISO §14.9.11.2 Format 1 '[UPON mnemonic-name-1]', §14.9.11.3 SR2, §14.9.11.4 GR1; E:\CobolSharp\src\Cobol.Net.Frontend\Grammar\CobolParserCore.g4:1081,1084-1086; E:\CobolSharp\src\Cobol.Net.Compiler\Binding\Procedure\Verbs\AcceptDisplayBinder.cs:21-22,103-115; StatementBinder.cs:371-374
- **Evidence:** SR2: 'Mnemonic-name-1 shall be specified in the SPECIAL-NAMES paragraph ... associated with an implementor-defined device-name ... capable of receiving data'; GR1 transfers the operands to THAT device. The grammar parses `displayUpon : UPON cobolWord` (g4:1084-1086), but BindDisplay (AcceptDisplayBinder.cs:103-115) iterates direct children (StatementBinder.Children is direct-child-only, :371-374) matching only Literal/DataReference/FunctionCall — DisplayUponContext falls through silently and `new BoundDisplay(ops, noAdvancing)` carries no device. So `DISPLAY X UPON E-DEV` (declared SYSERR or entirely UNDECLARED) compiles with zero diagnostics and writes stdout. The mirror-image ACCEPT SR2 IS enforced loudly (BindAcceptFromMnemonic :75-92, COBOLNET0817 for undeclared/non-input mnemonics), and the binder's own header comment (:21-22) names SYSOUT/SYSERR as 'the DISPLAY-side (output-only) names' — yet nothing consumes them for DISPLAY. No doc/ledger names DISPLAY UPON (docs grep: only the unrelated >>DISPLAY directive), and no conformance test exercises it.
- **Proposed action:** Bind displayUpon mirroring BindAcceptFromMnemonic: 0817-style bind-time reject for an undeclared mnemonic or an input-only device (output-capable set CONSOLE/SYSOUT/SYSERR per §12.3.7.3), carry the device on BoundDisplay, route SYSERR to stderr in the emitter/runtime, and add a golden asserting the stream split.
- **Disposition:** _unverified — Wave I re-verify_

### U10. [MAJOR | plan-omission | A5-statements] SUPPRESS statement (§14.9.45, Report Writer, COBOL-85) has no parser rule — hard parse failure — and the gap is absent from CONFORMANCE.md and every P13/P10 worklist

- **Refs:** ISO §14.9.45.2 'SUPPRESS PRINTING' (SUPPRESS required, PRINTING optional word), §14.9.45.3 SR1, §14.9.45.4 GR3; E:\CobolSharp\src\Cobol.Net.Frontend\Grammar\Core\CobolLexer.g4:301; E:\CobolSharp\src\Cobol.Net.Runtime\IO\ReportWriter.cs:53; docs/COBOLNET_REPORT_WRITER_DESIGN.md:177-178
- **Evidence:** The SUPPRESS lexer token exists (CobolLexer.g4:301) but appears in ZERO parser rules (grep across all .g4: lexer-only), so a conforming 1985-era RW program with USE BEFORE REPORTING + SUPPRESS PRINTING fails to parse at every --std. The runtime already stages the flag (ReportWriter.cs:53: 'The SUPPRESS statement, §14.9.45, is not yet parsed — its suppression flag is staged with it'). The RW deep-dive records the hole (§5 residue: 'SUPPRESS (§14.9.45) has no grammar rule — it cannot parse') but it is NOT in docs/CONFORMANCE.md (grep SUPPRESS/14.9.45: empty), NOT in PHASE-13-audit.md or the remaining-waves scout (their only SUPPRESS rows are the §12.4.5.6 SUPPRESS WHEN alternate-key CLAUSE — a different 2023 construct already in the grammar batch), and NOT in the P10 forward-residue ledger.
- **Proposed action:** Ride the already-planned P13 grammar batch (which touches the shared .g4 once, with ONE full legacy guard): add `suppressStatement : SUPPRESS PRINTING? ;` + a statement-dispatch arm + binder SR1 check (only inside a USE BEFORE REPORTING declarative — same declarative-context probe RESUME uses, EcBinder.cs:124) wiring the staged ReportWriter suppression flag per GR3a-e, + golden. At minimum, add a CONFORMANCE.md non-support row and a named worklist entry so the gap is tracked.
- **Disposition:** _unverified — Wave I re-verify_

### U11. [MAJOR | plan-omission | A5-statements] CALL Format 2 (program-prototype: AS NESTED / AS prototype-name) and the OMITTED argument (CALL F2 + INVOKE) have zero grammar surface, while the SSOT states 'OMITTED -> Null carrier' as the current CALL design

- **Refs:** ISO §14.9.4.2 Format 2 (CALL ... [AS] {NESTED | program-prototype-name-1} USING [BY REFERENCE]{identifier-2|OMITTED} ...), §14.9.23.2 INVOKE USING '[BY REFERENCE] OMITTED'; docs/COBOLNET_DESIGN.md:705; E:\CobolSharp\src\Cobol.Net.Frontend\Grammar\CobolParserCore.g4:866-890; Core\CobolOO.g4:130-136; docs/COBOLNET_INTERPROGRAM_DESIGN.md:148
- **Evidence:** callStatement/callTarget/callArgument (g4:866-890) admit only literal|dataReference targets and REFERENCE/VALUE/CONTENT/bare arguments — no `AS {NESTED|prototype-name}` selector and no OMITTED alternative; invokeArgument (CobolOO.g4:130-136) likewise has no OMITTED. OMITTED is a dedicated reserved token (CobolLexer.g4:519) absent from cobolWord (grep: 0), so `CALL "P" USING OMITTED` / `INVOKE obj "M" USING OMITTED` are parse errors today. Yet the design SSOT's inter-program section states 'OMITTED → `Null` carrier' as current design (COBOLNET_DESIGN.md:705), and CallBinder.cs has zero OMITTED handling (grep: 0). The only tracked OMITTED work is UDF-side function-activation arguments (P10 catalog:177,182 — COBOLNET1506 + 0899 optional-formal), and the interprogram deep-dive's per-edition inventory (:148) never mentions Format 2, AS NESTED, or OMITTED. INVOKE's arity error even calls trailing-OMITTED 'a later slice' (OoBinder.cs:380) with no ledger row anywhere.
- **Proposed action:** Decide and track: either implement the OMITTED argument (grammar alternative on callArgument/invokeArgument + the SSOT §9.4 Null carrier + §14.8.2 omitted-argument conformance + omitted-argument condition wiring) and the CALL AS {NESTED|prototype-name} selector, or stage both loud behind a named diagnostic with rows in the P13/P14 worklist + CONFORMANCE.md; in the deferral case reword COBOLNET_DESIGN.md:705 so the SSOT stops reading as implemented.
- **Disposition:** _unverified — Wave I re-verify_

### U12. [MAJOR | impl-vs-spec-drift | A6-intrinsics] National-argument results of the argument-typed function family bound as category ALPHANUMERIC (LOWER-CASE/UPPER-CASE/REVERSE/TRIM/CONCAT/SUBSTITUTE/MAX/MIN)

- **Refs:** ISO §15.57.1, §15.97.1, §15.78.1, §15.96.1, §15.18.1+.4 r2, §15.87.1, §15.59.1, §15.63.1; src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:125-127,109-110,232,236,241; src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:248,259,345,418
- **Evidence:** The spec makes these functions' TYPE argument-dependent — §15.57.1/§15.96.1/§15.87.1 each carry the table "Argument-1 type: National → Function type: National"; §15.18.4 r2 "If argument-1 is of class or usage national, the function will return a national value"; §15.59.1 MAX "National → National" (also "Index → Index"). The catalog pins all of them IntrinsicType.Alphanumeric, and the binder never promotes: the generic path uses `var category = sig.ResultCategory` (IntrinsicBinder.cs:248) with the ONLY adjustment being MAX/MIN → PicCategory.Alphanumeric even for all-national operands (line 259; IsStringOperand at 651-657 counts National as string); BindTrim returns `PicCategory.Alphanumeric` unconditionally (line 345), BindSubstitute likewise (line 418). Only CONVERT computes its category (line 477). So FUNCTION UPPER-CASE(nat-item) etc. bind category alphanumeric — the exact discipline the catalog's own ResultCategory docstring (IntrinsicCatalog.cs:49-53) says drives §14.9.25.3 Table-16 MOVE legality and string-channel class: an illegal national→alphanumeric MOVE of the result is silently accepted, and class-dispatched compare/DISPLAY paths see the wrong category.
- **Proposed action:** Add an argument-typed flag (or a computed-category hook) to the catalog rows for LOWER-CASE, UPPER-CASE, REVERSE, TRIM, CONCAT, SUBSTITUTE, MAX, MIN and compute the bound result category from the operand categories (national args → PicCategory.National; MAX/MIN also honor the Index row), mirroring the existing CONVERT §15.19.1 precedent; add a conformance golden that MOVEs/compares a national-argument result to pin Table-16 legality both directions.
- **Disposition:** _unverified — Wave I re-verify_

### U13. [MAJOR | plan-omission | A6-intrinsics] LENGTH/BYTE-LENGTH residual legs untracked: the [PHYSICAL] keyword is unparsed anywhere and ref-mod/ODO-group arguments stage compile errors on no worklist

- **Refs:** ISO §15.50.2 "FUNCTION LENGTH ( argument-1 [ PHYSICAL ] )" (spec line 36467), §15.50.4 r4/r7-r8; §15.14.2 (line 34646), §15.14.4 r2/r6-r7; src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:671-675,716-720,726-727; docs/rearchitecture/PHASE-11-scout-notes.md:216
- **Evidence:** §15.50.2/§15.14.2 both define the [PHYSICAL] keyword (2014-era, tied to variable-length groups, rules 7-8/6-7), and §15.50.4 r4 / §15.14.4 r2 define the ODO sending-rule (current DEPENDING value) — 1985/1989-era surface for LENGTH. Greenfield: grep for PHYSICAL over src/Cobol.Net.Frontend and the binder = zero hits (no token, no keyword recognition — `FUNCTION LENGTH(X PHYSICAL)` cannot bind); BindLengthFold errors out ref-mod (line 671-672) and ODO-group (673-675) arguments; BindByteLengthFold likewise (716-720). None of this is named in the PHASE-13 audit, either P13 scout, CONFORMANCE.md, or the P12 residue list (which tracks only the DYNAMIC LENGTH national runtime path). The P11 anchor scout explicitly flagged it (PHASE-11-scout-notes.md:216 "grammar must accept FUNCTION BYTE-LENGTH ( arg PHYSICAL )") and it fell off every tracked list. Bonus mis-cite: the ODO loud arms cite §15.50.4 r7 / §15.14.4 r6 (the variable-length-group rules, §8.5.1.12 dynamic-length members) for what is the r4/r2 OCCURS-DEPENDING case.
- **Proposed action:** Add a tracked worklist item (P13 residue or P14 backlog): (a) accept the PHYSICAL keyword in LENGTH/BYTE-LENGTH argument position (no-op for non-variable-length arguments, per rules 8/7's same-value clause); (b) implement the runtime legs for ref-mod (.Length of the view) and ODO-group (current DEPENDING value, §13.18.38 GR8b sending) arguments; (c) correct the two error-message citations to §15.50.4 r4 / §15.14.4 r2.
- **Disposition:** _unverified — Wave I re-verify_

### U14. [MAJOR | doc-inconsistency | A6-intrinsics] Diagnostic code COBOLNET1518 is claimed by two unrelated features: the A.4.9 locale-module non-support and the OO method-REDEFINES scope error

- **Refs:** src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:598 vs src/Cobol.Net.Compiler/Binding/DataBinder.cs:2388; docs/PHASE4_RECONCILIATION.md:1793 ("free 15xx band (1518 is last-used)"); docs/rearchitecture/PHASE-11-intrinsics-backlog-tierc-codec.md:24,217,508; docs/CONFORMANCE.md:50; src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:26
- **Evidence:** Two live Error sites emit COBOLNET1518 for unrelated defects: IntrinsicBinder.cs:598 ("…optional locale module (ISO/IEC 1989:2023 Annex A §A.4.9…)") and DataBinder.cs:2388 ("REDEFINES target … is not a preceding item in the same method scope (ISO §13.18.44.3…)"). PHASE4_RECONCILIATION.md:1793 records 1518 as last-used by the M2-OO REDEFINES leg (DEVLOG 639); the P11 plan then independently assigned 1518 to the locale module, and CONFORMANCE.md item 25 + IntrinsicCatalog.cs:26 + COBOLNET_INTRINSICS_DESIGN.md:11 all document 1518 = locale non-support. A user (or the conformance record) mapping 1518 gets two contradictory meanings, and the "next free" diag-ledger discipline is proven to have double-allocated.
- **Proposed action:** Renumber the DataBinder.cs:2388 method-REDEFINES error to the next free code in the ledger (≥1576 per the resume-prompt band), sweep every doc referencing the old assignment (PHASE4_RECONCILIATION note, any goldens asserting 1518 on the REDEFINES path), and leave 1518 solely = A.4.9 locale non-support as the P11/CONFORMANCE docs state.
- **Disposition:** _unverified — Wave I re-verify_

### U15. [MAJOR | doc-inconsistency | A6-intrinsics] The A.4.9 locale-module non-support — the stated basis for the catalog's 4 Unsupported LOCALE-* rows — is not recorded as a facility in CONFORMANCE.md §4 (which also miscounts "four" and cross-refs "§2.4")

- **Refs:** ISO §4.2.7, Annex A §A.4.1/§A.4.9; src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:20-27,187-198; src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:163-177,588-601; docs/CONFORMANCE.md:17-18,116-127
- **Evidence:** The IntrinsicBind.Unsupported doctrine (IntrinsicCatalog.cs:20-27) rests on "non-support is conforming when DOCUMENTED" (§4.2.7/A.4.1), and 4 catalog rows (LOCALE-COMPARE/-DATE/-TIME/-TIME-FROM-SECONDS) + the LOCALE-phrase rejections for LOWER-CASE/UPPER-CASE/NUMVAL-C/TEST-NUMVAL-C cite the A.4.9 locale module as "documented non-support". But CONFORMANCE.md §4 "Documented non-support facilities" (line 116) lists exactly four facilities — MCS, commit/rollback, VALIDATE, screen handling — with no locale-module row; the only locale mention is the A.3 item-25 note (line 50), which covers just the cultural-ordering/STANDARD-COMPARE piece, not the LOCALE-* functions, the LOCALE phrases, LC_* SET, or the SPECIAL-NAMES LOCALE clause. The §1 summary (lines 17-18) also says "The four documented non-support facilities (§2.4)" — a wrong count once locale is added, and a wrong cross-reference (the section is §4). Note §4 additionally promises a COBOLNET1560-band WARNING per facility, while the locale rejections are COBOLNET1518 ERRORS — the doc should state the locale module's error-level disposition explicitly.
- **Proposed action:** Add a fifth facility row to CONFORMANCE.md §4: "Locale facility (Annex A.4.9 optional module): LOCALE-COMPARE/LOCALE-DATE/LOCALE-TIME/LOCALE-TIME-FROM-SECONDS, the LOCALE phrases of LOWER-CASE/UPPER-CASE/NUMVAL/NUMVAL-C/TEST-NUMVAL-C, SET LC_*, SPECIAL-NAMES LOCALE clause — rejected at bind with COBOLNET1518 (error, not 1560-band)"; fix the "four"→"five" count and the "§2.4"→"§4" cross-refs (lines 17-18, 31, 33).
- **Disposition:** _unverified — Wave I re-verify_

### U16. [MINOR | plan-vs-impl-error | A6-intrinsics] EXCEPTION-FILE/-N file-connector argument form (VCR 68/69) is fully implemented, but the catalog/runtime comments still say it "renders loud" and the VCR rows still carry the "<!-- todo -->" (no-constructs-row) anchor

- **Refs:** ISO §15.28.4 r2 / §15.29.4 r2; src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:172,177-178; src/Cobol.Net.Runtime/Exceptions/EcFunctions.cs:61-62 vs 65-72; src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:293-312; src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:367-372; docs/VERSION_CHANGE_REFERENCE.md:185-186; tests/version-matrix/constructs.json ("exception-file-argument-2023", "exception-file-n-argument-2023")
- **Evidence:** The 2023 arg form landed end-to-end: BindExceptionFileArg (IntrinsicBinder.cs:297-312, COBOLNET0900 gate + COBOLNET1574 non-FD arg), renderer r2 legs (IntrinsicRenderer.cs:367-372), runtime EcFunctions.File(string)/FileN(string) → FileRegistry.ExceptionFile (r2a two spaces when never accessed / r2b status+SELECT-name — matches §15.28.4 r2), and both constructs.json gate rows exist. Yet IntrinsicCatalog.cs:172 still says "the 2023 file-connector-arg form renders loud — VCR row 68" (and 177-178 for -N), EcFunctions.cs:61-62 still says the r2 form "renders loud compiler-side", and VCR rows 68/69 still carry `<!-- todo -->`, which the VCR legend (line 52) defines as "a genuine gate obligation not yet a constructs.json row" — so the GENERATED gating-status index misreports both rows as outstanding obligations (VcrDriftTests only guards the anchor→missing-construct direction, not stale todo anchors).
- **Proposed action:** Update the three stale comment sites to state the implemented r2 legs; replace the `<!-- todo -->` anchors on VCR rows 68/69 with `<!-- gate:exception-file-argument-2023 -->` / `<!-- gate:exception-file-n-argument-2023 -->` and regenerate the index (pwsh scripts/gen-vcr.ps1).
- **Disposition:** _unverified — Wave I re-verify_

### U17. [MAJOR | impl-vs-spec-drift | A7-oo] ISO inline method invocation (§8.4.3.4 '::' operator) is absent; the grammar rule and registry row bearing its name are a non-ISO dataReference(args) STATEMENT mis-gated to 2023

- **Refs:** spec §8.4.3.4 :7121-7159 + §8.7.4 :8902; src/Cobol.Net.Frontend/Grammar/Core/CobolOO.g4:151-157; src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4:656; src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4:724; src/Cobol.Net.Editions/ConstructRegistry.g.cs:151; src/Cobol.Net.Compiler/Binding/Bound/StatementBinder.cs:324; docs/rearchitecture/DESIGN-frontend-grammar.md:221; docs/rearchitecture/PHASE-09-m2-oo-rearchitect-and-complete.md:31-33; docs/ISO2023_CONFORMANCE_PLAN.md:687,697
- **Evidence:** §8.4.3.4.2 defines inline method invocation as an IDENTIFIER form `{object-class-name-1 | identifier-1} :: literal-1 [(args…)]` referencing a temp item equivalent to INVOKE…RETURNING temp (GR1 :7156-7159); the '::' invocation operator is §8.7.4 (:8902). The shipped rule `inlineMethodInvocationStatement : dataReference LPAREN argumentList? RPAREN` (CobolOO.g4:155-157, self-described 'relocated from the deleted non-ISO JSON/XML fragment') is a STATEMENT alternative gated {is2023()}? (CobolParserCore.g4:656); no '::' token exists (CobolLexer.g4:724 has single COLON only — line 728 even cites '::' §8.7.4 as the model); no binder arm consumes it (falls to BoundUnsupported, StatementBinder.cs:324) and the registry constant InlineMethodInvocation2023 has ZERO Check() references (dead gate). ConstructRegistry.g.cs:151 claims the row IS 'ISO 8.4.3 (in-line method invocation, introduced 2023)' — but the VCR 130-row 2002→2023 checklist has no inline-invocation row, i.e. per the project's own edition-derivation it is 2002-introduced. DESIGN-frontend-grammar.md:221 claims CobolOO.g4 covers 'inline method invocation'. The P9 STATUS claims the deferral is 'recorded in docs/ISO2023_CONFORMANCE_PLAN.md … inline-invocation rides its own wave' — that doc's only Recorded-deferral sections are >>PROPAGATE (:687) and GOBACK status (:697), and no wave anywhere names it. A.4.10 (:40408-40414) makes inline invocation MANDATORY (COBOLNET_OO_DESIGN.md:485 states this itself).
- **Proposed action:** Implement §8.4.3.4 for ≥2002: add the '::' separator token, an inline-invocation identifier alternative desugaring to INVOKE…RETURNING a temp cloned from the method's RETURNING description (GR1), with SR1 sending-operand-only, SR2 (class object; not NULL/universal), SR4 (RETURNING not ANY LENGTH/ACTIVE-CLASS); DELETE the non-ISO dataReference(args) statement rule and fix the ConstructRegistry row's edition+citation. At minimum this P13 cycle: strip the false ISO attribution from the registry row and DESIGN-frontend-grammar.md, and add a real named deferral row to a live worklist (the P9 STATUS pointer is dangling).
- **Disposition:** _unverified — Wave I re-verify_

### U18. [MAJOR | plan-omission | A7-oo] Object-view (§8.4.3.5 `identifier AS class/interface/UNIVERSAL`) has zero surface, EC-OO-CONFORMANCE has no raise site, and its only named owner ('the EC-OO/object-view wave') closed without it

- **Refs:** spec §8.4.3.5 :7182-7211; identifier Format 5 :6760-6762; Table 13 :24750; A.4.10 :40408-40414; src/Cobol.Net.Runtime/Exceptions/ExceptionCatalog.cs:139 (sole EC-OO-CONFORMANCE hit in src); docs/COBOLNET_OO_DESIGN.md:242,303; docs/COBOLNET_REARCHITECTURE_PLAN.md + docs/rearchitecture/PHASE-13-audit.md + resume-prompt.md (zero mentions)
- **Evidence:** §8.4.3.5: `identifier-1 AS {[FACTORY OF] object-class-name-1 [ONLY] | interface-name-1 | UNIVERSAL}` — compile-time re-description (GR1) plus a RUNTIME conformance check that sets EC-OO-CONFORMANCE (GR2 :7211); Table 13 (:24750) defines EC-OO-CONFORMANCE solely as 'Failure for an object-view', and A.4.10 leaves object-view outside the three optional OO items (mandatory 2002+). The frontend has no objectView rule and no AS-view identifier tier (grep 'objectView' in Frontend = 0) — conforming source like `x AS UNIVERSAL` is a bare parse error, contra the project's staged-loud doctrine — and EC-OO-CONFORMANCE exists only as a catalog registration (ExceptionCatalog.cs:139) with no raise site anywhere. Tracking exists only as COBOLNET_OO_DESIGN.md prose 'deferred to the EC-OO/object-view wave' (:242, :303); the EC-OO work landed in P9 (RAISE-object, EXCEPTION-OBJECT, EC-OO-UNIVERSAL all live) WITHOUT it, P9 ('mandatory 2002 OO completion') is STATUS DONE, and no live worklist (rearchitecture plan, P13 audit/scouts, resume-prompt REMAINING, P10 forward ledger, CONFORMANCE.md) names an object-view item.
- **Proposed action:** Add a named row to a live worklist (P13 Wave I follow-ons or the P14 entry): parse the AS object-view in the identifier tier, bind it as a compile-time description override reusing OoConformance.ObjectRefWideningMismatch for the static leg, and emit the runtime class/interface check raising EC-OO-CONFORMANCE. Until landed, stage the syntax loud with a named 0899-band descriptor and update COBOLNET_OO_DESIGN.md:242/303 to point at the real owner.
- **Disposition:** _unverified — Wave I re-verify_

### U19. [MAJOR | plan-omission | A7-oo] USAGE OBJECT REFERENCE [FACTORY OF] ACTIVE-CLASS and the ONLY phrase (§13.18.60.2) are unimplemented; ACTIVE-CLASS mis-diagnoses as an unknown class, ONLY is a bare parse error; the tracking row's owning slice closed without them

- **Refs:** spec §13.18.60.2 :22679-22682 + SR16 :22756; §8.4.3.4.3 SR4 :7151; src/Cobol.Net.Frontend/Grammar/Core/CobolOO.g4:145-149; src/Cobol.Net.Compiler/Binding/DataBinder.cs:1798-1803; src/Cobol.Net.Editions/ReservedWords.Table.cs:15; docs/COBOLNET_OO_SLICE_BRIEFS.md:514
- **Evidence:** The USAGE OBJECT REFERENCE bracket is `interface-name-1 | [FACTORY OF] ACTIVE-CLASS | [FACTORY OF] object-class-name-1 [ONLY]` (:22679-22682), with SR16 (:22756) restricting ACTIVE-CLASS placement; A.4.10 makes ACTIVE-CLASS mandatory (COBOLNET_OO_DESIGN.md:485 lists it as such). objectReferenceUsage (CobolOO.g4:145-149) carries only `FACTORY OF className | className | ε` — no ACTIVE-CLASS alternative, no ONLY. ACTIVE-CLASS is in the reserved-word table (ReservedWords.Table.cs:15) but has NO lexer token, so `USAGE OBJECT REFERENCE ACTIVE-CLASS` parses as a class-name and mis-fires COBOLNET0813 "unknown class or interface 'ACTIVE-CLASS'" (DataBinder.cs:1798-1803) — a wrong diagnostic on conforming source; `…object-class ONLY` fails as a bare syntax error, with no exact-class conformance semantics in OoConformance. The only tracking ever written (COBOLNET_OO_SLICE_BRIEFS.md:514: ACTIVE-CLASS owned by 'the INTERFACE-ID slice; the USAGE grammar carries neither yet') is dangling — the INTERFACE-ID slice landed (interface-typed USAGE works via DataBinder.cs:1799 FindInterface) without delivering it, and no live worklist names either phrase.
- **Proposed action:** Add an ACTIVE_CLASS token plus `[FACTORY OF] ACTIVE-CLASS` and trailing `ONLY` alternatives to objectReferenceUsage; bind ACTIVE-CLASS as the covariant self-type (the existing §16.2.1 covariant __New machinery is the natural carrier) with the SR16 placement check, and ONLY as exact-class (no-subclass) conformance in OoConformance/DescriptionMismatch. Or, minimally, stage both loud under named 0899 descriptors (never the misleading 0813) and add a live worklist row.
- **Disposition:** _unverified — Wave I re-verify_

### U20. [MAJOR | plan-omission | A7-oo] Parameterized classes/interfaces (§9.3.12: CLASS-ID/INTERFACE-ID USING, REPOSITORY EXPANDS/AS) are mandatory 2023 surface with zero grammar and a FALSE 'staged-loud 0899 residue' claim in the slice briefs

- **Refs:** spec §9.3.12 :12463-12487; §11.3.2 :13010-13012 + GR5 :13057; §12.3.8 formats :14698-14704, SR3 :14744; A.4.10 :40408-40414; src/Cobol.Net.Frontend/Grammar/Core/CobolOO.g4:39-47,72-78; src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4:362-372; docs/COBOLNET_OO_SLICE_BRIEFS.md:1275-1276
- **Evidence:** CLASS-ID and INTERFACE-ID carry `[USING {parameter-name-1}…]` (:13012/:12790 — GR5 :13057: 'The USING clause specifies that this is a parameterized class'), and REPOSITORY class/interface-specifiers carry `[AS literal] [EXPANDS … USING …]` (:14698-14704). A.4.10 lists EXACTLY three optional OO items and parameterized classes are not among them — so per the compiler's own analysis (COBOLNET_OO_DESIGN.md:485, 'ALL other OO surface … is MANDATORY') they are required 2023 surface. The grammar has no USING on classIdParagraph (CobolOO.g4:39-43) or interfaceDefinition (:72-78), and repositoryEntry has neither EXPANDS nor AS (CobolParserCore.g4:366-372, AS explicitly 'deferred') — conforming source is a bare parse error, not staged. COBOLNET_OO_SLICE_BRIEFS.md:1275-1276 claims a '0899 staged-loud residue: parameterized interfaces (INTERFACE-ID USING / EXPANDS, §12.3.8 SR3 — same deferred status as parameterized classes)' — no such 0899 staging exists anywhere in code, and no live worklist names the feature.
- **Proposed action:** Make the v1 disposition explicit and owned: superset-parse the USING/EXPANDS/AS phrases and stage them loud with a named descriptor plus a live worklist row (P14/P15 candidate), or take an owner decision to record the deviation in CONFORMANCE.md — noting §4.2.7 cannot sanction it since the element is not in A.4 — and in either case correct the slice-brief claim of an existing 0899 staging.
- **Disposition:** _unverified — Wave I re-verify_

### U21. [MINOR | doc-inconsistency | A7-oo] CONFORMANCE.md A.3 row 45 marks parametric-polymorphism method resolution 'Claimed' while the code explicitly rejects/defers parametric polymorphism (COBOLNET0822, D9)

- **Refs:** spec A.3 #45 :40224; A.4.10 #3 :40414; docs/CONFORMANCE.md:66; src/Cobol.Net.Compiler/Oo/OoClassTable.cs:264-268,307-310
- **Evidence:** A.3 item 45 (:40224) reads 'When parametric polymorphism is provided in object orientation, the algorithm used for method resolution may be dependent on the methodology used in the object management system' — it applies ONLY when the optional A.4.10 #3 feature is provided. CONFORMANCE.md:66 marks it '**Claimed** | Single-dispatch OO method resolution over the class table', but the compiler does NOT provide parametric polymorphism: OoClassTable.cs:264-268/307-310 reject duplicate method names (COBOLNET0822 — 'parametric polymorphism, ISO §12063, is an OPTIONAL feature and is deferred', deep-dive D9). The authoritative §4.2.16 record thus claims a facility the code rejects (same family as the Wave-G '04' false-claim fix); the §4.2.7 optional-element record also nowhere states that A.4.10 #1 (multiple-inheritance INHERITS repetition) is unsupported despite the 0849 loud reject.
- **Proposed action:** Flip row 45 to 'N/A — parametric polymorphism (A.4.10 optional item 3) is not provided (COBOLNET0822 enforces unique method names)', and add the three A.4.10 optional-element dispositions to CONFORMANCE.md (item 1 not claimed → 0849; item 2 claimed — interface INHERITS repetition is supported; item 3 not claimed → 0822).
- **Disposition:** _unverified — Wave I re-verify_

### U22. [MINOR | impl-vs-spec-drift | A7-oo] The REPOSITORY-declaration requirement for referenced class/interface names (§11.3.3 SR2 / §11.8.3 SR1 / §12.3.8) is silently unenforced — an unrecorded, non-dialect-gated leniency acknowledged only in a code comment

- **Refs:** spec §11.3.3 SR2 :13020; §12.3.8 :14698-14772; src/Cobol.Net.Compiler/Oo/OoClassTable.cs:369-371; src/Cobol.Net.Compiler/Binding/DataBinder.cs:1798-1803; src/Cobol.Net.Compiler/Binding/ReferenceResolver.cs:72
- **Evidence:** §11.3.3 SR2 (:13020): 'Object-class-name-2 [the INHERITS base] shall be the name of a class specified in the REPOSITORY paragraph of this source element'; the code's own comment cites the parallel IMPLEMENTS rule: OoClassTable.cs:369-371 '§11.8.3 SR1's REPOSITORY requirement is enforced with the repository binding — staged as a documented follow-up' — but no worklist, ledger, or leniency registry documents that follow-up. In practice class/interface names resolve group-wide with no repository check anywhere (DataBinder.cs:1798-1803 resolves USAGE OBJECT REFERENCE names straight off the group table; INHERITS/IMPLEMENTS likewise), while PROPERTY names asymmetrically DO require their repository entry (ReferenceResolver.cs:72). Accepting nonconforming source without a gate contradicts the project's every-leniency-dialect-gated rule (project_dialect_strictness).
- **Proposed action:** Enforce the repository-declaration SRs as a bind diagnostic (class/interface names referenced by INHERITS/IMPLEMENTS/USAGE/INVOKE must appear in the referencing source element's REPOSITORY paragraph, or be the element's own name), mirroring the existing property-name mechanism at ReferenceResolver.cs:72 — or register the leniency in the dialect-strictness leniency registry with a live worklist row.
- **Disposition:** _unverified — Wave I re-verify_

### U23. [MAJOR | plan-omission | A8-rw-screen-mcs] CONFORMANCE.md has no Annex A.4 optional-element disposition — §4.2.7 requires partial-support itemization (Report Writer) and the §1 blanket claim over-claims unimplemented optional facilities

- **Refs:** ISO §4.2.7 (specs/ISO_COBOL.md:2440-2453: 'If an implementor provides support for parts of an optional feature, user documentation shall identify the elements that are supported and those that are not supported'); Annex A.4.1 (specs:40229-40244), A.4.8 (40372-40376), A.4.11 (40417-40482), A.4.13 (40490-40494); docs/CONFORMANCE.md:3-9 (charter claims it covers 'every optional facility this compiler does not implement'), :13-17 (§1 blanket claim), :26-67 (§2 = A.3 only, no A.4 section)
- **Evidence:** CONFORMANCE.md declares itself the §4.2.6/§4.2.7 user documentation but only tables Annex A.3 plus four non-support facilities. Missing A.4 dispositions verified against code: Report Writer (A.4.11) is PARTIALLY supported — the mandatory supported/not-supported itemization exists only in the internal design doc COBOLNET_REPORT_WRITER_DESIGN.md §5, not in the conformance record; FORMAT clause + SELECT WHEN (A.4.8) are unimplemented ('SELECT WHEN (not modeled)', DataBinder.cs:1280; no grammar) and absent from CONFORMANCE.md, VCR, and the P13 audit; REWRITE FILE / WRITE FILE (A.4.13) have zero compiler surface and no doc row. Meanwhile §1 states the compiler 'implements the required nucleus and the standard modules except the optional/processor-dependent facilities listed as not supported below', which affirmatively over-claims all unlisted-and-unimplemented A.4 elements.
- **Proposed action:** Add an A.4 disposition section to docs/CONFORMANCE.md (one row per A.4.2–A.4.14 facility: Claimed — screen/commit-rollback/VALIDATE already covered as non-support; dynamic-capacity tables, DYNAMIC LENGTH, sharing/locking, RESUME, OO items claimed; RW = Partial with the supported/staged/no-grammar itemization or a pointer to the RW design §5 list; FORMAT/SELECT WHEN and REWRITE FILE/WRITE FILE = Not claimed), and scope the §1 blanket sentence accordingly.
- **Disposition:** _unverified — Wave I re-verify_

### U24. [CRITICAL | doc-inconsistency | A9-annex-e3] COBOLNET1573 is emitted by two different features: >>REF-MOD-ZERO-LENGTH malformed-operand AND the VCR-18 external FILE STATUS consistency error *(duplicate of confirmed C1)*

- **Refs:** Annex E.2 items 12/23 (specs/ISO_COBOL.md:49164, :49304); src/Cobol.Net.Frontend/Preprocessor/RefModZeroLengthDirectiveProcessor.cs:64; src/Cobol.Net.Editions/Diagnostics/DiagnosticCatalog.cs:426; src/Cobol.Net.Compiler/Binding/BinderDriver.cs:162; resume-prompt.md:150 vs :159 and :167
- **Evidence:** The diag-band ledger (resume-prompt.md:159/:167 and MEMORY) records 1573 = 'REF-MOD-ZERO-LENGTH malformed' (landed 420eb720, DEVLOG 897), and RefModZeroLengthDirectiveProcessor.cs:64 emits COBOLNET1573 for a non-ON/OFF operand. The next day, Wave E pt2 (HEAD 38c4a669, DEVLOG 905) registered COBOLNET1573 AGAIN as 'external-file-status-consistency' (DiagnosticCatalog.cs:426) and emits it at BinderDriver.cs:162 — resume-prompt.md:150 claims 'FILE STATUS = COBOLNET1573' while line 167 still says '1573 REF-MOD-ZERO-LENGTH malformed' and 'NEXT FREE = 1575' (already consumed by relative-key at DiagnosticCatalog.cs:433). Two distinct diagnostics share one ID in shipped code; the ledger contradicts itself.
- **Proposed action:** Renumber one of the two (external-file-status-consistency to the true next free code, 1576) in DiagnosticCatalog + BinderDriver + its 6 goldens; reconcile the resume-prompt band ledger (1573=REF-MOD malformed, 1575=relative-key, next free 1576+); add a uniqueness test asserting each COBOLNETnnnn appears in exactly one DiagnosticCatalog descriptor and that every ReportError string literal matches a registered descriptor, so a band collision fails CI.
- **Disposition:** _unverified — Wave I re-verify_

### U25. [MAJOR | impl-vs-spec-drift | A9-annex-e3] OPTIONS INITIALIZE (Annex E.3.3 item 33): clause is bound then silently dropped in codegen, and its introduction gate says 2014 against Annex E's 2014-to-2023 delta

- **Refs:** Annex E.3.3 item 33 (specs/ISO_COBOL.md:50310); ISO 11.9.10 GR6 (:13648) + 14.6.2.x initial-state (:24092); tests/version-matrix/constructs.json:462-471; src/Cobol.Net.Compiler/Binding/OptionsBinder.cs:58-101; OptionsModel.cs:47,68; docs/VERSION_CHANGE_REFERENCE.md:193 (row 76 todo)
- **Evidence:** OptionsBinder.cs:58-101 fully binds INITIALIZE {WORKING-STORAGE|...} TO {fill} into OptionsModel.Initialize, but OptionsSections/OptionsFill are referenced NOWHERE outside OptionsModel.cs/OptionsBinder.cs — no CodeGen consumer; ValueInitializer falls back to pic.DefaultInitializer, so 'OPTIONS. INITIALIZE WORKING-STORAGE SECTION TO SPACES.' compiles clean and the spec-defined fill (11.9.10 GR6: without a VALUE the content becomes the specified fill character) never happens — silent wrong content, violating the project's own no-silent-drop doctrine (the audit classes identical silent-drops, e.g. SCREEN, as MISSING rows, but has no OPTIONS INITIALIZE row). Separately, constructs.json:468 sets introducedIn 2014 ('a 2014 clause of the 2002 paragraph') with no spec citation for 2014, while Annex E.3.3 item 33 lists the clause in the 2014-to-2023 substantive-change list — per the VCR's own scope note ('the 2023 spec documents the 2014-to-2023 delta completely'), the in-repo authority says the clause is a 2023 addition, so --std 2014 wrongly accepts it.
- **Proposed action:** Add a P13 worklist row: wire OptionsModel.Initialize into ValueInitializer/allocation (or replace the silent drop with a named staged-loud diagnostic until then); re-derive the introduction edition from Annex E.3.3 item 33 (2023) and fix constructs.json/ConstructRegistry + the below-edition negative, or record an explicit spec-cited determination for 2014; flip VCR row 76's todo anchor to the gate id.
- **Disposition:** _unverified — Wave I re-verify_

### U26. [MAJOR | plan-omission | A9-annex-e3] REPOSITORY FUNCTION ALL INTRINSIC user-defined-word prohibition (ISO 12.3.8 SR12/SR13; Annex E.2 item 13) is neither implemented nor in any worklist

- **Refs:** ISO 12.3.8 SR12/SR13 (specs/ISO_COBOL.md:14788-14790); Annex E.2 item 13 (:49178-49194); docs/VERSION_CHANGE_REFERENCE.md:120 (row 19 todo); src/Cobol.Net.Compiler/Binding/DataBinder.cs:256,330; src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:85
- **Evidence:** SR13: 'If ALL is specified in the intrinsic format of the function-specifier, none of the names of the intrinsic functions may be specified as a user-defined word within the scope of this REPOSITORY paragraph' (SR12 is the per-name twin). E.2 item 13 makes this the 2023 incompatible change for BASECONVERT/CONCAT/CONVERT/FIND-STRING/MODULE-NAME/SMALLEST-ALGEBRAIC/SUBSTITUTE. In code, RepositoryAllIntrinsic (DataBinder.cs:256, set at :330) is consumed ONLY for function-reference resolution (IntrinsicBinder.cs:85); no binder/validation site rejects a colliding user-defined word, and the intrinsic names are not lexer tokens (grep CONCAT/BASECONVERT in CobolLexer.g4 = 0), so '01 CONCAT PIC X.' under FUNCTION ALL INTRINSIC compiles at 2023. Absent from the audit's 71 rows, both scouts, and the resume-prompt REMAINING list; VCR row 19 is still a bare todo.
- **Proposed action:** Add a P13 (or P14 matrix-closure) row: enforce 12.3.8 SR12/SR13 as an edition-aware name-collision diagnostic (collision set = the intrinsic catalog filtered to names introduced at or below the target edition, so the 7 new names reject only at >=2023 while pre-existing names reject at all editions with ALL INTRINSIC, per SR13's general wording); constructs.json row + positive/negative goldens; flip VCR row 19.
- **Disposition:** _unverified — Wave I re-verify_

### U27. [MAJOR | plan-omission | A9-annex-e3] Annex E.2 item 21 removal of the FLAG-85 and FLAG-NATIVE-ARITHMETIC directives: silently swallowed at ALL editions and orphaned from the live remaining-work list

- **Refs:** Annex E.2 item 21 (specs/ISO_COBOL.md:49272-49288); src/Cobol.Net.Frontend/Preprocessor/ConditionalCompilationProcessor.cs:37; docs/VERSION_CHANGE_REFERENCE.md:129 (row 28 anchors cover only move-quote + arithmetic-standard); docs/rearchitecture/PHASE-13-m4-2023-ec-remnants-behavior-rows.md:493-503 (Step 17: 'the non-MOVE-QUOTE legs still TODO') vs resume-prompt.md:89-170 (REMAINING list omits Step 17)
- **Evidence:** E.2 item 21 removes FLAG-85 and FLAG-NATIVE-ARITHMETIC in 2023 (they were obsolete-but-present in 2014). ConditionalCompilationProcessor.cs:37 lists both in KnownIgnoredDirectives — recognized and silently dropped at EVERY edition: no removed-at-2023 rejection (the close-with-lock COBOLNET0902 pattern), no 2014 obsolete flag, violating the VCR's own rule 4 / G1 co-equal diagnostic obligation. Of row 28's four legs only MOVE QUOTE (move-quote-numeric-obsolete-2014) and Standard Arithmetic (arithmetic-standard-2002) have constructs. The only acknowledgment is behavior-rows Step 17's 'still TODO' note, but Step 17 does not appear in the resume-prompt's REMAINING P13 WORK (the live driver, whose Wave G is declared 'FULLY COMPLETE — no deferred residue') nor in the audit's 71 rows — so it will fall through phase close.
- **Proposed action:** Fold FLAG-85/FLAG-NATIVE-ARITHMETIC into the Wave D directive batch (they are preprocessor-only): remove from KnownIgnoredDirectives, reject at >=2023 with a named removed-element diagnostic (COBOLNET0902 family) and flag/accept per their 2014 obsolete status below; constructs.json rows + goldens; also re-add Step 17's residue (rows 93/116 STANDARD-BINARY/SBIDI verify + flipping VCR rows 3/28 status — CALL ON OVERFLOW is landed at VersionConformancePass.cs:1149 but row 3 still reads todo) to the resume-prompt REMAINING list.
- **Disposition:** _unverified — Wave I re-verify_

### U28. [MINOR | plan-omission | A9-annex-e3] ALIGNED clause (ISO 13.18.1) absent entirely, so the Annex E.2 item 2 2023 typed-consistency/bit-alignment delta has no landing surface and is tracked nowhere

- **Refs:** Annex E.2 item 2 (specs/ISO_COBOL.md:49052-49058); ISO 13.18.1 (:17538-17562); ISO 12.x ALIGN in consistency list (:12077); docs/VERSION_CHANGE_REFERENCE.md:109 (row 8 todo); src/Cobol.Net.Editions/ReservedWords.Table.cs:20; src/Cobol.Net.Editions/ConstructRegistry.g.cs:99 (boolean-data-2002 LIVE)
- **Evidence:** 13.18.1 defines the ALIGNED clause for bit group / elementary bit items, and E.2 item 2 adds ALIGN to the typed-item consistency lists plus bit-position alignment for strongly typed items at 2023. Boolean/bit data is LIVE in the greenfield (boolean-data-2002, one char per position), yet ALIGNED exists only as a reserved-word table entry — no lexer token, no grammar rule (grep ALIGNED in Grammar/*.g4 = 0), no binder model — so a conforming 2002+ program using ALIGNED dies with a generic parse error; the E.2 item 2 consistency check is unimplementable. Not in the audit's 71 rows, either scout, the resume-prompt REMAINING list, the P10 residual catalog (grep = 0), or CONFORMANCE.md; VCR row 8 is a bare todo.
- **Proposed action:** Add a worklist row: parse the ALIGNED clause with a 2002 introduction gate; given the char-per-position boolean representation, byte-boundary alignment is trivially satisfied — record that determination in CONFORMANCE.md — and add the >=2023 E.2-item-2 typed-consistency arm to the existing strong-typing checks; flip VCR row 8.
- **Disposition:** _unverified — Wave I re-verify_

### U29. [MINOR | plan-omission | A9-annex-e3] Annex E.2 item 6 (compile-time arithmetic mode now implementor-defined) has no documented disposition anywhere

- **Refs:** Annex E.2 item 6 (specs/ISO_COBOL.md:49108-49114); docs/VERSION_CHANGE_REFERENCE.md:113 (row 12 todo); docs/CONFORMANCE.md (grep 'compile-time arithmetic' = 0 matches); docs/rearchitecture/PHASE-13-audit.md:37-107 (no row)
- **Evidence:** E.2 item 6 makes the arithmetic mode for evaluating compile-time arithmetic expressions and its intermediate-result handling 'explicitly implementor defined' at 2023 — the conforming action is a documented implementor determination, exactly the disposition class the audit DID row-ize for E.2 item 17 (I-O '0x' case equivalence, audit row at PHASE-13-audit.md:38, now a CONFORMANCE.md 3 note). But CONFORMANCE.md has no compile-time-arithmetic note, the audit has no row for it, and it appears in no remaining list; VCR row 12 is a bare todo, so the phase-close sweep has nothing to flip it against.
- **Proposed action:** Add a CONFORMANCE.md implementor-defined note stating the mode the greenfield actually uses for compile-time expression evaluation (constant folding / >>EVALUATE and >>IF arithmetic), cite E.2 item 6, and flip VCR row 12 to a ref-only/disposition anchor — a doc-only Wave H/I item.
- **Disposition:** _unverified — Wave I re-verify_

### U30. [MAJOR | plan-omission | B1-audit-reverify] DB-series NIST golden establishment (audit row VCR 7.17, line 66) is in no remaining worklist — falls through at phase close

- **Refs:** X3.23-1985 debug module (CCVS DB corpus is the in-repo authority per scout); docs/rearchitecture/PHASE-13-audit.md:66; docs/rearchitecture/PHASE-13-remaining-waves-scout.md:1290-1296 ('DB-series movement'); resume-prompt.md:95-166 (REMAINING lists ①-④); src/Cobol.Net.Compiler/Binding/Procedure/ProcedureTableBuilder.cs:213-245
- **Evidence:** Fallen-through row ID: PHASE-13-audit.md line 66 — 'MISSING | DB-series NIST programs (DB101A / DB103M / DB301M–305M etc.) — golden-less residue | 7.17'. Its stated blocker (the DEBUG-ITEM register row) landed in Wave F (0efb0d88, DEVLOG 896), and the scout's Wave F section explicitly prescribes the next step ('with steps 1–5 landed, DB101A/DB201A become compile-and-run candidates; generate their goldens'), yet `ls tests/nist/valid | grep -ci '^DB'` is still 0 and neither resume-prompt.md's REMAINING ①-④, the task list, nor the P14 plan names DB-series. Related untracked residue on the same axis: the DB201A prerequisite ALL-REFERENCES-OF/data-name, file-name, and cd-name USE FOR DEBUGGING legs are loud-staged COBOLNET1571 (ProcedureTableBuilder.cs:213-245) with no worklist item to unstage them.
- **Proposed action:** Add a named item to the remaining-P13 (or P14 matrix-closure) list: establish the DB101A golden now (procedure-trigger legs are live), stage DB201A behind un-staging the COBOLNET1571 ALL-REFERENCES leg, and record an explicit disposition (attempt vs named-residual) for DB103M/DB301M–305M in the audit progress note so the row cannot silently vanish at phase close.
- **Disposition:** _unverified — Wave I re-verify_

### U31. [MAJOR | doc-inconsistency | B1-audit-reverify] CONFORMANCE.md still declares I-O status '04' "not yet emitted" — contradicted by the landed VCR 21 code and golden *(duplicate of a confirmed C-item — the CONFORMANCE.md '04' claim)*

- **Refs:** ISO §9.1.13.2 item 3 / §14.9.35 GR14 (E.2 item 15); docs/CONFORMANCE.md:74-77 vs src/Cobol.Net.Runtime/IO/SequentialConnector.cs:379-419, src/Cobol.Net.Runtime/IO/FileStatus.cs:15, tests/conformance/2002/io_status_04.cob/.out; DEVLOG 899 (commit 04c32a93)
- **Evidence:** CONFORMANCE.md §3 bullet: "the record-sequential READ short/long-record status '04' is **not yet emitted** — ... Planned (VCR 21)". At HEAD, FileStatus.cs:15 defines RecordLengthShortLong="04" and SequentialConnector.Read sets it for fixed n<RecordWidth and varying n<VaryMin||n>VaryMax (lines 399/415/419), with the enabled golden io_status_04. The bullet was made accurate at 2265c33a (when '04' was truly absent) but commit 04c32a93 landed the emission without updating this authoritative §4.2.16 conformance record — violating CONFORMANCE.md §5's own same-change-set rule and CLAUDE.md process rule 4.
- **Proposed action:** Rewrite the '04' bullet to claim the support as-built: '04' on a record-sequential READ whose physical record is outside the fixed width / varying min-max (line-sequential excluded), version-invariant per the E.2 item 15 clarification, FLAG-14 I-O-STATUS-04 twin pending Wave D.
- **Disposition:** _unverified — Wave I re-verify_

### U32. [MAJOR | plan-vs-impl-error | B1-audit-reverify] 6 of the 8 new-2023 intrinsics (+ the TRIM charset enhancement) have no constructs.json matrix row — the audit-DONE row's action 'none' skipped the mandated flip

- **Refs:** ISO §15.12/§15.18/§15.19/§15.37/§15.65/§15.87/§15.96 (E.3.3 items 22-24, 27-31); docs/rearchitecture/PHASE-13-audit.md:5,98; tests/version-matrix/constructs.json (only smallest-algebraic-2023 among the eight); docs/VERSION_CHANGE_REFERENCE.md:182-191 (rows 65-74 all <!-- todo -->); src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:235-241; src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:137-142
- **Evidence:** The audit preamble says DONE rows are 'verify-and-flip only', but the intrinsics DONE row (line 98, VCR 65-74) carries action 'none'. The code-side edition gate exists (IntrinsicCatalog introducedIn=2023 → IntrinsicBinder COBOLNET1502 by name and edition), yet the (construct × edition) matrix — the mission's validation instrument per feedback_version_test_matrix — has rows only for smallest-algebraic-2023: BASECONVERT, CONCAT, CONVERT, FIND-STRING, MODULE-NAME, SUBSTITUTE and the 2023 TRIM char-set enhancement have no constructs.json row, so P14 'matrix closure' has nothing to flip for them and no fixture proves the below-2023 rejection per edition. VCR rows 65-74 remain <!-- todo -->.
- **Proposed action:** Add seven constructs.json rows (introducedIn 2023, expectDiagnostic COBOLNET1502; TRIM-charset as a 2023 enhancement row on the 2014-introduced base) and flip the VCR 65-74 anchors to gate:… — either now or as an explicitly named P14 matrix-closure item; annotate audit row 98's action accordingly.
- **Disposition:** _unverified — Wave I re-verify_

### U33. [MINOR | doc-inconsistency | B1-audit-reverify] The P13 VCR-anchor flips were re-scoped to P14 only inside DEVLOG 895 — the audit, resume-prompt, and the VCR ledger itself still present them as pending P13 work / stale todo

- **Refs:** docs/VERSION_CHANGE_REFERENCE.md:47-58 ('the ledger can no longer go stale') and rows at lines 110,117,119,122,128,131,132,152,153,162,171,174,175,177,180,192,194,195,197 (all <!-- todo --> for landed constructs); docs/rearchitecture/PHASE-13-audit.md:5 ('DONE rows are verify-and-flip only'); resume-prompt.md:52-54; DEVLOG 895
- **Evidence:** ~20 VCR rows whose constructs are landed AND gated in constructs.json at HEAD (9/16/18/21/27/30/31/34/35/42/43/45/54/57/58/60/63/68/69/75/77/78/80 → usage-packed-no-sign-2023, sync-on-group-2023, ref-mod-zero-length-2023, merge-in-sort-merge-proc-removed-2023, exception-file-argument-2023, external-type-declaration-2023, boolean-shift-operators-2023, inspect-backward-2023, etc.) still carry <!-- todo --> ('a genuine gate obligation not yet a constructs.json row' — now false), and the generated index lists only 15 pre-P13 constructs. The deferral to P14 exists only in DEVLOG 895 ('The VCR row-flips … are P14 matrix-closure bookkeeping'), while the audit preamble and resume-prompt §HOW-TO-RESUME item 2 still tell the next session the flips are P13 scope — a docs-current-state violation (VcrDriftTests cannot catch a todo-anchor-vs-catalogue mismatch).
- **Proposed action:** Either run the anchor flips + gen-vcr.ps1 now, or record the P14 deferral where the next session will read it (the audit STATUS note + resume-prompt REMAINING list + a named P14 step), and consider extending VcrDriftTests to fail when a <!-- todo --> row's construct exists in constructs.json.
- **Disposition:** _unverified — Wave I re-verify_

### U34. [MINOR | doc-inconsistency | B1-audit-reverify] Audit bookkeeping errors: VCR 78 double-counted as both PARTIAL and DONE rows, and row VCR 21 prescribes a nonexistent standalone I-O-STATUS-04 directive

- **Refs:** specs/ISO_COBOL.md:164-188 (§7.3 directive TOC — no I-O-STATUS-04 directive) and :4515 (FLAG-14 GR4 e sub-option); docs/rearchitecture/PHASE-13-audit.md:37,88,89; docs/rearchitecture/PHASE-13-remaining-waves-scout.md:1364 (the drift already caught, never annotated back)
- **Evidence:** (a) The 71-row table carries the DELETE-FILE-statuses item twice — line 88 'PARTIAL | DELETE FILE 5 new I-O statuses 05/37/39/41/62 — are they actually SET? | 78' and line 89 'DONE | DELETE FILE may set I-O statuses 05,37,39,41,62 | 78' — so the headline 31/21/18/1 split double-counts one item. (b) Line 37's action 'implement '04' … + the I-O-STATUS-04 directive (Wave D pattern)' names a directive that does not exist: the spec's §7.3 clause list (7.3.9-7.3.25) has no I-O-STATUS-04 entry; it is only the FLAG-14 GR4 e) flag ('a reference to a FILE STATUS item that tests for 04 shall be flagged'). The scout caught this (line 1364) but the audit table — 'THE WORKLIST' per resume-prompt — was never annotated, so a literal phase-close burn-down would chase a phantom directive.
- **Proposed action:** Annotate the audit progress note: mark line 37's directive half as audit drift (superseded by the scout — FLAG-14 twin, Wave D) and mark lines 88/89 as one item, restating the true row count.
- **Disposition:** _unverified — Wave I re-verify_

### U35. [MAJOR | plan-vs-impl-error | B2-phase-claims] CONSTANT … AS BYTE-LENGTH OF still staged on a rationale the P11-landed code contradicts ("§15.14 is a Deferred catalog row" — it is live Fold)

- **Refs:** ISO §13.10.4 GR5 (specs/ISO_COBOL.md:16857) + §15.14; src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:155 vs src/Cobol.Net.Editions/Diagnostics/DiagnosticCatalog.cs:122, src/Cobol.Net.Compiler/Binding/DataBinder.Constants.cs:45-46,189,206, docs/DIAGNOSTICS.md:20; docs/rearchitecture/PHASE-10-m2-residual-catalog.md Step 15 + STATUS banner; docs/rearchitecture/PHASE-11-intrinsics-backlog-tierc-codec.md Step 7
- **Evidence:** Spec §13.10.4 GR5: the constant's value "is determined as specified in the BYTE-LENGTH intrinsic function with the exception that when data-name-1 is an occurs-depending group item, the maximum size of the data item is used". P11 Step 7 landed BYTE-LENGTH as a live compile-time fold — IntrinsicCatalog.cs:155 binds IntrinsicBind.Fold and the byte-width authority exists (DataItem.ByteWidth:373-375; IntrinsicBinder.BindByteLengthFold:710) — and the P11 headline claims the Deferred backlog went to ZERO. Yet the CONSTANT AS BYTE-LENGTH OF leg is still staged loud (DataBinder.Constants.cs:206 emits COBOLNET0899 constant-byte-length), and its recorded justification in THREE places — the user-facing DiagnosticCatalog.cs:122 message (hence DIAGNOSTICS.md:20) and DataBinder.Constants.cs:45-46/189 — still reads "the §15.14 intrinsic itself is a Deferred catalog row, and the byte-width authority lands ONCE, with it (the singular-pattern rule)", which the catalog now contradicts. The staging's stated blocking condition dissolved at P11 without the leg landing or being re-ledgered: it is absent from the P10 STATUS-banner carry-forward-by-name list, the P11 residue list (Tier-C codec only), and every P12/P13 tracked list.
- **Proposed action:** Implement the GR5 leg (fold the constant from the same DataItem.ByteWidth authority; the GR5 ODO-maximum exception holds by construction since ByteWidth is maximum-allocation based, mirroring the AS LENGTH OF / ImageWidth precedent at DataBinder.Constants.cs:216-219) with a positive golden + negative retirement in the same commit; or, if deliberately deferred, re-ledger it by name in the live forward ledger with a corrected rationale. Either way fix the three stale rationale texts and regenerate DIAGNOSTICS.md (DiagnosticRegistryDriftTests gates the drift).
- **Disposition:** _unverified — Wave I re-verify_

### U36. [MINOR | impl-vs-spec-drift | C1-waveE-drift] CheckExternalFileConsistency skips single-describer groups, leaving the cited §14.8.4.2 'shall be external data items' leg unenforced with no runtime backstop (and the LINAGE-item leg unaddressed)

- **Refs:** specs/ISO_COBOL.md:25518 (§14.8.4.2: 'For each external file connector, the file status, linage and relative key data items shall be external data items…'), :49164 (E.2 item 12), :49312 (E.2 item 24), :15203 (GR1 NOTE routing LINAGE to §13.4.5 GR2); src/Cobol.Net.Compiler/Binding/BinderDriver.cs:154 ('if (conns.Count < 2) continue'), :162+:176 (diagnostics citing §14.8.4.2)
- **Evidence:** Both emitted diagnostics cite §14.8.4.2, but the pass returns early for an external file described by only ONE in-group file control entry (BinderDriver.cs:154), so a run unit whose single SELECT of an external file names a NON-external FILE STATUS or RELATIVE KEY item compiles clean at 2023 strict — violating §14.8.4.2's unconditional 'shall be external data items' and E.2 item 12's 'all corresponding SELECT statements' (which with one SELECT still requires an external item). The comment routes singles to the tracked VCR-15 runtime EC-EXTERNAL raise, but that check detects descriptor CONFLICTS between describers — a lone describer produces no conflict, so this case is unreachable by it. The LINAGE data item named in §14.8.4.2 is also never checked (nor tracked anywhere by name).
- **Proposed action:** Drop the conns.Count<2 early-out for the externality half: for every external file connector (any describer count, >=2023) require that a specified FILE STATUS / RELATIVE KEY item IS an external data item (ExternalItemIdentity non-null), keeping the same-identity comparison for multi-describer groups. Either add the LINAGE-item leg (§13.4.5 GR2 / §14.8.4.2) in the same pass or name it explicitly in the remaining Wave E VCR-15 scope so it is tracked.
- **Disposition:** _unverified — Wave I re-verify_

### U37. [MINOR | impl-vs-spec-drift | C2-waveG-drift] COBOLNET1570 cites §13.18.63 SR4/SR5, whose category scope excludes numeric-edited; the national-literal half of E.2 item 27 is uncovered

- **Refs:** src/Cobol.Net.Compiler/Binding/DataBinder.cs:1923-1934,1024-1027 · src/Cobol.Net.Editions/Diagnostics/DiagnosticCatalog.cs:399-405 · src/Cobol.Net.Editions/ConstructRegistry.g.cs:32; spec SR4 (line 23230), SR5 (line 23232), SR7 (line 23236), E.2 item 27 (line 49360)
- **Evidence:** SR4 opens "If the item is of category alphabetic, alphanumeric, or alphanumeric-edited..." and SR5 "If the item is of category national or national-edited..." — neither covers category numeric-edited, the 1570 check's actual subject; the normative anchors for the 2023 numeric-edited literal-conformance check are SR7 (class conformance) plus Annex E.2 item 27: "Alphanumeric AND NATIONAL literals in the VALUE clause for numeric-edited items are now checked to ensure they conform to their PICTURE and USAGE clauses." The '>' operator is correct ("shall not exceed" — DEVLOG 898 rightly rejected the scout's '=='), but the check only reaches literals starting with '"' (alphanumeric): item 27's national-literal half is absent — an N"…" literal on a numeric-edited item is instead rejected outright at ALL editions by the 0898 class case (DataBinder.cs:1024), which SR7 contradicts for a usage-NATIONAL numeric-edited item ("the class of the literal shall conform to that of the data item").
- **Proposed action:** Re-cite 1570 (descriptor + DataBinder comment + registry row) to §13.18.63 SR7 + Annex E.2 item 27; add the national-literal size leg or document usage-national numeric-edited as a non-support so the unconditional 0898 rejection is the recorded disposition.
- **Disposition:** _unverified — Wave I re-verify_

## 4. Per-area finder tallies

Every completed area produced findings; none came back empty: A1 directives 6 · A2 fundamentals/EC 6 ·
A5 statements 4 · A6 intrinsics 5 · A7 OO 6 · A8 RW/SCREEN/MCS 5 · A9 Annex E 6 · B1 audit re-verify 5 ·
B2 phase claims 3 · B3 ledgers 4 · C1 Wave E 5 · C2 Wave G 5 · C3 EC/ref-mod 5.

## 5. Coverage gaps of THIS review (owed work)

> **⛔ RESUME STATUS (2026-07-19):** a completion pass was attempted the same day
> (`Workflow resumeFromRunId: wf_099b899f-7eb`); it was killed by the **MONTHLY spend limit** (107 of 119 agent
> calls failed) after landing 5 finder re-runs (their new findings are §7) + 3 confirming verdicts. **To finish
> the review once the limit is raised:** re-invoke the same resume (script + run id are in the session workflow
> store; or simply re-run the missing pieces as fresh Wave I agents) — the journal preserves all completed
> results. Until then the gaps below stand.

| Gap | What was missed | Close-out |
|---|---|---|
| ~~Finder A3 never ran~~ **RAN 2026-07-19 (batch 1)** | §9 + §12 sweep | findings in §8 — verify + route |
| ~~Finder A4 never ran~~ **RAN 2026-07-19 (batch 1)** | §13 clause sweep | findings in §8 — verify + route |
| ~~Finder C4 never ran~~ **RAN 2026-07-19 (batch 1)** | Wave C re-derivation | findings in §8 — verify + route |
| 37 unverified findings | the 2-lens verification | Wave I verify batch |
| §7 resume-pass findings (23 raw) | dedupe vs §2/§3 + verification | Wave I verify batch |
| B1 corrected tally unverified | the 53/16/1/1 re-classification of the 71 audit rows | spot-check at phase close |

## 6. Disposition plan (initial routing; keep current)

| Batch | Items | Route |
|---|---|---|
| FIX NOW (docs+diagnostics remediation) | C1 1573-collision renumber (RefMod malformed-operand → next free code, catalog-registered); resume-prompt banner + next-free ledger refresh; CONFORMANCE.md '04' claim; stale notes (EC-BOUND-OVERFLOW in the data-model deep-dive, DebugItem comment, ConditionalCompilationProcessor comment); wrong-§ corrections (§13.6.2→§11.9.10.4 GR7, 'E.3 item 10'→E.2 item 10, §14.9.35→§14.9.30 GR14, the A8 citation trio, MCS 'COMMUNICATION SECTION' wording); 1550-1552 earmark cleanup; 0849 double-claim reconciliation | this session, doc/diag commits |
| SPEC-DERIVE + FIX (P13 review-fix wave) | VCR 16 strong-TYPE gate depth (§13.16.3 SR13); VCR 86 below-2023 SR6 derivation (VALUE 0 vs VALUE ZERO); GOBACK RAISING in a main program (normal-termination rule); ref-mod negative-length sentinel collision (EC-BOUND-REF-MOD never raises for negatives); COBOLNET1570 scope §; EXCEPTION-FILE r2a EverAccessed + SD-name rejection | serial greenfield wave, full-Conformance gated |
| FOLD INTO GRAMMAR BATCH | RW SUPPRESS statement rule (found independently by 2 finders — hard parse failure today); CALL Format 2 / OMITTED argument (scope decision needed — mandatory 2023 surface per the unverified A5 finding) | existing P13 grammar batch |
| FOLD INTO WAVE D | >>DEFINE AS PARAMETER + the §7.3.6/§7.3.7 compile-time expression evaluator; >>SOURCE FORMAT mid-file switching; conditional-compilation directives inside COPY text; FLAG-85/FLAG-NATIVE-ARITHMETIC Annex E.2 item 21 removal handling; the CC/SOURCE-FORMAT intro gate at --std 85; >>CALL-CONVENTION / >>LEAP-SECOND dispositions | existing P13 Wave D |
| WAVE I: VERIFY THEN ROUTE | all 37 unverified findings (OO surface gaps incl. parameterized classes + object-view + ACTIVE-CLASS; EC seam gaps EC-RANGE/EC-FLOW/EC-DATA twins; intrinsics national-category results + PHYSICAL; OPTIONS INITIALIZE dropped in codegen; REPOSITORY rules; ALIGNED clause; DB-series NIST goldens; matrix-row flips; COBOLNET1518 double-claim; EXIT SECTION doc-vs-impl; DISPLAY UPON silently dropped) | Wave I |
| P14 CANDIDATES | matrix-row flips for the 6 new-2023 intrinsics + TRIM charset (B1); the VCR-ledger todo-anchor sweep (B3) | P14 matrix closure |

## 7. Resume-pass results (2026-07-19 second run — folded per the owner directive; the run was killed by the MONTHLY spend limit)

The resume re-executed some finders fresh; their NEW findings (not in section 2/3) are below, RAW and unverified.
The 3 verdicts that landed before the limit confirmed C2 (>>DEFINE AS PARAMETER), C3 (>>SOURCE FORMAT first-wins, twice) and C4 (CC-in-COPY) with high confidence.

### R1. [MAJOR | plan-vs-impl-error | B2-phase-claims] RANDOM sequence state is a process-wide static, not RunUnit-owned — contradicts the P8 'ONE RunUnit run-unit-state owner' completion claim and the code's own §15.75.3 citation

- **Refs:** ISO §15.75.3 rule 4 + §15.75.4 rule 2 (specs/ISO_COBOL.md:37792-37803); src/Cobol.Net.Runtime/Intrinsics/CobolIntrinsics.Float.cs:79 (field), :74-78 (comment claiming per-run-unit), :83,:91 (uses); src/Cobol.Net.Runtime/Control/RunUnit.cs:12-26 (concurrent-run-units contract); P8 claim in docs/COBOLNET_REARCHITECTURE_PLAN.md / CLAUDE.md banner ('runtime reorged onto ONE RunUnit run-unit-state owner; every pre-P8 static name kept as an emitted-surface shim')
- **Evidence:** CobolIntrinsics.Float.cs:79 declares `private static Random _random = new();` and its own doc comment (lines 74-78) says 'ONE current pseudo-random sequence per run unit (§15.75.3)'. §15.75.3 rule 4 scopes the sequence to the run unit ('the first reference to this function in the run unit'), and RunUnit.cs:12-13 explicitly supports hosts that 'run run units concurrently, each see their own' state via AsyncLocal. Yet _random is per-PROCESS: two run units (concurrent or sequential in one host) share one sequence — a seeded RANDOM in run unit A restarts run unit B's sequence, interleaved argument-less calls steal numbers from each other's sequence (breaking §15.75.4 rule 2 same-seed determinism), and System.Random is not thread-safe under concurrent NextDouble. Every other pre-P8 static in the runtime is a shim over RunUnit.Current (ExceptionState.cs:270, CobolFile.cs:17); this one holds real state. Not tracked anywhere: PHASE-13-plan-vs-spec-review.md has no RANDOM/§15.75 finding, and it is in none of the deferred-work ledgers. Single-run-unit CLI hosts are unaffected, so severity is major not critical.
- **Proposed action:** Move the Random instance onto RunUnit (e.g. a lazily-initialized `Random` member with a seed-replace method), and make CobolIntrinsics.Random()/Random(seed) shims over RunUnit.Current — the exact pattern ExceptionState and CobolFile already use. Keep the emitted surface (CobolIntrinsics.Random) unchanged so no regeneration of goldens is needed; add a unit test that two RunUnits see independent sequences.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R2. [MINOR | plan-vs-impl-error | B2-phase-claims] CobolTable out-of-range Scratch<T>.Slot is shared process-wide mutable state — under concurrent run units it can violate its own documented 'reads see a fresh default-valued element' contract

- **Refs:** src/Cobol.Net.Runtime/Values/Tables/CobolTable.cs:28-35 (static Slot + ref return), :15-17 (the documented implementor-defined rule); P8 claim 'ONE RunUnit run-unit-state owner' (docs/COBOLNET_REARCHITECTURE_PLAN.md); RunUnit.cs:12-13 concurrent-run-units contract
- **Evidence:** CobolTable.At<T> returns `ref Scratch<T>.Slot` where `Scratch<T>.Slot` is `public static T Slot` (CobolTable.cs:32-35) — one slot per element type per PROCESS, not per run unit or per thread. The file's own contract (lines 15-17) is 'reads see a fresh default-valued element ... re-defaulted on every out-of-range access'. With two run units running concurrently (the model RunUnit.cs:12-13 explicitly supports), run unit A can write through its returned ref between run unit B's re-default (line 28) and B's read, so B observes A's absorbed write instead of the documented fresh default. Out-of-range subscript behavior is implementor-defined latitude, but the implementor-defined rule is stated in this comment and the race breaks it. Minor because the leaked value is only observable on the already-undefined out-of-range path and single-run-unit hosts are unaffected.
- **Proposed action:** Make the scratch slot per-thread ([ThreadStatic] on Scratch<T>.Slot is sufficient — it is transient statement-scope scratch, not run-unit state, so the P8 AsyncLocal carrier is not required and a ref return needs a real field), or document the cross-run-unit caveat in the CobolTable contract comment if the race is accepted.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R3. [MAJOR | plan-vs-impl-error | A8-rw-screen-mcs] CONFORMANCE.md claims all four documented non-support facilities are 'recognized … never a bare syntax error' — only SCREEN's COBOLNET1560 exists; ISO §4.2.6's mandatory compile-time warning is unmet for MCS and commit/rollback while the record asserts it is met

- **Refs:** ISO §4.2.6 ¶3 (specs/ISO_COBOL.md:2437) + Annex A.3 items 4/6-7 (specs:40080,40084-40086); docs/CONFORMANCE.md:17-18 and :118-120; E:\CobolSharp\src\Cobol.Net.Compiler\Binding\DataBinder.cs:346 (the ONLY COBOLNET1560 site, SCREEN SECTION only); src/Cobol.Net.Frontend/Grammar/*.g4 (zero SEND/RECEIVE/COMMIT/ROLLBACK/VALIDATE tokens, verified by grep); resume-prompt.md:82,116; prior ledger C6 at docs/rearchitecture/PHASE-13-plan-vs-spec-review.md:74-80, disposition still _pending_ at clean HEAD 38c4a669
- **Evidence:** §4.2.6: 'An implementation shall provide a warning mechanism at compile time to indicate use of syntactically-detectable processor-dependent language elements not supported'. MCS is A.3 item 4 and commit/rollback A.3 items 6-7, so the warning is a conformance SHALL for them. CONFORMANCE.md §1/§4 claim in present tense that all four facilities emit a named COBOLNET1560-band warning 'never a bare syntax error', but the sole 1560 emit is the SCREEN SECTION warning; SEND/RECEIVE/COMMIT/ROLLBACK/VALIDATE have no lexer tokens and die with generic COBOL0001 — resume-prompt.md:82/:116 concedes exactly this ('today a generic COBOL0001'). The Wave H code half is tracked (task #5); the untracked delta is the conformance record's false present-tense claim (docs-current-state-only rule). Also: '(§2.4)' at CONFORMANCE.md:18 is a dangling cross-reference — the facility list is §4; no §2.4 exists.
- **Proposed action:** Either land the Wave H recognize-and-name code half before P13 merge, or (FIX NOW, doc batch) reword CONFORMANCE.md §1/§4 to current truth: SCREEN warning landed; MCS/COMMIT/ROLLBACK/VALIDATE currently a generic COBOL0001 with named recognition pending (tracked Wave H). Fix the '(§2.4)' dangling reference to '(§4)' in the same edit. Then mark ledger C6's disposition.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R4. [MAJOR | plan-omission | A8-rw-screen-mcs] RW SUPPRESS statement (§14.9.45, Annex A.4.11 item 23) — the only Report Writer verb with no grammar rule: hard parse error, no COBOLNET0899 staged-loud entry, and absent from the live grammar-batch worklist wording (which tracks only the DIFFERENT §12.4.5.6 SUPPRESS WHEN alt-key phrase)

- **Refs:** ISO §14.9.45 (specs/ISO_COBOL.md:32545) + A.4.11 item 23 (specs:40472); E:\CobolSharp\src\Cobol.Net.Frontend\Grammar\Core\CobolLexer.g4:301 (SUPPRESS token referenced by no parser rule; CobolReportWriter.g4 has initiate/generate/terminate rules only); E:\CobolSharp\src\Cobol.Net.Runtime\IO\ReportWriter.cs:53 (suppression hook pre-staged, unreachable); docs/COBOLNET_REPORT_WRITER_DESIGN.md:178 ('has no grammar rule — it cannot parse'); prior ledger C5 (_pending_) + its FOLD-INTO-GRAMMAR-BATCH row at PHASE-13-plan-vs-spec-review.md:532
- **Evidence:** RW support is claimed (P10 'RW-2002 complete'; NIST RW101A-104A; CONFORMANCE.md §1 blanket claim) and its mandatory host context USE BEFORE REPORTING is fully implemented (CobolControlFlow.g4:181, ReportWriterEmitter.cs:266-275, Runtime ReportWriter.cs BeforeReporting hooks), yet SUPPRESS — a first-class A.4.11 facility member — cannot parse and is not staged loud, violating the project's own RW convention that every legal-but-unimplemented RW construct gets a named COBOLNET0899. The resume-prompt/task-#5 grammar-batch enumeration names only 'SUPPRESS WHEN' (§12.4.5.6 alternate-record-key), a different construct; only the review ledger's pending disposition row folds RW SUPPRESS in, and that has not propagated to any live worklist. Note per format-notation rules (specs:2658) PRINTING is an OPTIONAL word — the rule is `SUPPRESS PRINTING?`.
- **Proposed action:** Implement in the P13 grammar batch (small): rule `SUPPRESS PRINTING?`; bind-check SR1 (only within a USE BEFORE REPORTING procedure); set the pre-staged ReportWriter suppression flag inhibiting print lines/page advance/NEXT GROUP/LINE-COUNTER for the current group instance (GR3a-d; control-break sensing unaffected GR3e); conformance golden. Minimally: a COBOLNET0899 staged-loud entry + CONFORMANCE.md row. Either way, name RW SUPPRESS explicitly in the resume-prompt grammar-batch line and task #5 so the fold-in survives the ledger.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R5. [MAJOR | plan-omission | A8-rw-screen-mcs] VERIFIED (was unverified U23): CONFORMANCE.md has no Annex A.4 optional-element disposition — §4.2.7's partial-support itemization is missing for Report Writer, and A.4.8 FORMAT/SELECT WHEN + A.4.13 REWRITE FILE/WRITE FILE are unimplemented, on no tracked list, and affirmatively over-claimed by §1's blanket sentence

- **Refs:** ISO §4.2.7 (specs/ISO_COBOL.md:2442-2453: 'If an implementor provides support for parts of an optional feature, user documentation shall identify the elements that are supported and those that are not supported'); Annex A.4.8 (specs:40372-40376), A.4.11 (specs:40417-40482), A.4.13 (specs:40490-40494); docs/CONFORMANCE.md:13-17 (§1 blanket claim) and :26-67 (§2 = A.3 only); E:\CobolSharp\src\Cobol.Net.Compiler\Binding\DataBinder.cs:1280 ('SELECT WHEN (not modeled)'); prior ledger U23 (PHASE-13-plan-vs-spec-review.md:405-410, disposition '_unverified — Wave I re-verify')
- **Evidence:** I verified all three legs of U23 against spec and code. (1) §4.2.7's partial-support itemization SHALL: Report Writer is partial — staged-loud holes (CODE §13.18.12, NEXT GROUP §13.18.37, report-group OCCURS, LINE NEXT PAGE, multiple LINE — DataBinder.Reports.cs:255,357,365,409) plus no-grammar holes (SUPPRESS §14.9.45, LAST CONTROL HEADING, COLUMN LEFT/CENTER/RIGHT — RW design doc :177-178) — itemized only in the internal design doc, not in the §4.2.16 conformance record. (2) A.4.8: grep of src finds no formatClause/SELECT WHEN grammar or binder surface; the only '13.18.24' hit in src is an unrelated mis-cited EXTERNAL descriptor (finding 5). (3) A.4.13: zero REWRITE FILE/WRITE FILE code surface. Neither A.4.8 nor A.4.13 appears in CONFORMANCE.md, the VCR, the P13 audit, or any residue ledger — while §1's 'implements … the standard modules except the … facilities listed as not supported below' affirmatively claims them.
- **Proposed action:** Add an A.4 disposition section to docs/CONFORMANCE.md: one row per A.4.2-A.4.14 facility (screen/commit-rollback/VALIDATE already §4 non-support; dynamic-capacity tables, DYNAMIC LENGTH, extended letters, sharing/locking, locale=not-claimed(1518), OO, RESUME claimed as implemented; RW = Partial pointing at the RW design §5 supported/staged itemization; FORMAT+SELECT WHEN and REWRITE FILE/WRITE FILE = Not claimed) and scope §1's blanket sentence to 'except the optional and processor-dependent elements dispositioned below'. Flip U23's ledger disposition from unverified to CONFIRMED.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R6. [MINOR | doc-inconsistency | A8-rw-screen-mcs] CONFORMANCE.md §4 defines the MCS facility with 'the COMMUNICATION SECTION' — a construct with zero occurrences in ISO/IEC 1989:2023 (ENABLE/DISABLE/PURGE statements likewise absent) — and understates the commit/rollback facility (no APPLY COMMIT clause, no EC-FLOW-* members)

- **Refs:** docs/CONFORMANCE.md:122-124; specs/ISO_COBOL.md — grep 'COMMUNICATION SECTION' = 0 matches, no ENABLE/DISABLE/PURGE statement headings in §14.9; actual 2023 surface: SEND §14.9.38 (specs:30992), RECEIVE §14.9.31 (specs:30139), USAGE MESSAGE-TAG §13.18.60; Annex A.4.3 (specs:40311-40327: APPLY COMMIT clause §12.4.6.3, EC-FLOW-APPLY-COMMIT/-COMMIT/-ROLLBACK, COMMIT §14.9.7, ROLLBACK §14.9.36); prior ledger C17 (_pending_)
- **Evidence:** The COMMUNICATION SECTION/CD model and the ENABLE/DISABLE/PURGE verbs are the pre-2002 Communication Module, deleted from the standard — I verified zero spec matches. The 2023 A.3-item-4 asynchronous-messaging facility is SEND/RECEIVE/USAGE MESSAGE-TAG only. Separately, A.4.3 enumerates the commit/rollback facility as five members; CONFORMANCE.md §4 item 2 names only the COMMIT/ROLLBACK statements, omitting the IO-CONTROL APPLY COMMIT clause and the EC-FLOW-* conditions.
- **Proposed action:** In the same Wave H CONFORMANCE.md touch: reword §4 item 1 to 'SEND (§14.9.38), RECEIVE (§14.9.31), USAGE MESSAGE-TAG (§13.18.60)' (drop COMMUNICATION SECTION), and extend item 2 with the APPLY COMMIT clause (§12.4.6.3) + EC-FLOW-APPLY-COMMIT/-COMMIT/-ROLLBACK per A.4.3. Keep the Wave H recognizer scoped to the real 2023 keyword-led constructs.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R7. [MINOR | doc-inconsistency | A8-rw-screen-mcs] Four fresh wrong-§ citations in shipped RW/EXTERNAL code, NOT in prior ledger C18 (whose CONFORMANCE.md/FILES_DESIGN sites also remain unfixed at HEAD): GENERATE cited as §14.9.19 (=IF), TERMINATE as nonexistent §14.9.62, GROUP INDICATE as §13.18.29 (=GROUP-USAGE) twice, ExternalRecordNotCellBacked as §13.18.24 (=FORMAT clause)

- **Refs:** E:\CobolSharp\src\Cobol.Net.Frontend\Grammar\Core\CobolReportWriter.g4:200 ('GENERATE … (§14.9.19)') and :207 ('TERMINATE … (§14.9.62)') vs spec headings '## 14.9.16 GENERATE statement' (specs:27522), '## 14.9.19 IF statement' (specs:27774), '## 14.9.46 TERMINATE statement' (specs:32592); E:\CobolSharp\src\Cobol.Net.Compiler\Binding\DataBinder.Reports.cs:136 and :425 ('§13.18.29' for GROUP INDICATE) vs '## 13.18.28 GROUP INDICATE clause' (specs:19061) / '## 13.18.29 GROUP-USAGE clause' (specs:19101); E:\CobolSharp\src\Cobol.Net.Editions\Diagnostics\DiagnosticCatalog.cs:410 ('ISO §13.18.24' on the EXTERNAL-record descriptor, propagated to docs/DIAGNOSTICS.md:27) vs '## 13.18.22 EXTERNAL clause' (specs:18672) / '## 13.18.24 FORMAT clause' (specs:18787)
- **Evidence:** Each citation re-derived against the spec headings: the RW grammar comments cite GENERATE at the IF statement's clause number and TERMINATE at a clause that does not exist (TERMINATE is §14.9.46 — the runtime ReportWriter.cs cites it correctly, so the .g4 comments are internally inconsistent with their own subsystem); DataBinder.Reports.cs twice cites GROUP INDICATE one clause off (GROUP-USAGE); the COBOLNET0899 external-record-not-cell-backed descriptor cites the FORMAT clause instead of the EXTERNAL clause and DIAGNOSTICS.md regenerates the error. Same recurring wrong-§ theme the review flagged (6 confirmed instances); none caught by any test.
- **Proposed action:** Fold these five sites into the in-progress remediation task #7 'FIX NOW doc/citation batch' alongside C18's still-unfixed sites (CONFORMANCE.md rows 24/28-30/32-34: DISPLAY 14.9.13→14.9.11, CLOSE 14.9.7→14.9.6, OPEN 14.9.26→14.9.27; FILES_DESIGN:158-159): CobolReportWriter.g4 §14.9.19→§14.9.16 and §14.9.62→§14.9.46; DataBinder.Reports.cs §13.18.29→§13.18.28 (both sites); DiagnosticCatalog.cs:410 §13.18.24→§13.18.22 + regenerate DIAGNOSTICS.md.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R8. [MAJOR | impl-vs-spec-drift | A6-intrinsics] Argument-dependent function type family binds category Alphanumeric for national arguments (LOWER-CASE/UPPER-CASE/REVERSE/TRIM/CONCAT/SUBSTITUTE/MAX/MIN)

- **Refs:** ISO §15.57.1 (spec line 36793 type table), §15.18.1 (line 34826), §15.97.1/§15.78.1/§15.96.1/§15.87.1/§15.59.1/§15.63.1; src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:125-127,232,236,241 (rows typed Alphanumeric); src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:248,259,290
- **Evidence:** §15.57.1: 'The type of this function depends on the type of argument-1 as follows: ... National -> National'; §15.18.1 has the same table (national/numeric-usage-national args -> National). The binder sets `var category = sig.ResultCategory;` (Alphanumeric for these rows) and never flips to National — the only category adjustments are CONVERT (IntrinsicBinder.cs:477, dst==3) and the MAX/MIN all-string arm which hardcodes `category = PicCategory.Alphanumeric` (line 259) even when every argument is national. So FUNCTION LOWER-CASE(nat-item) carries ResultCategory Alphanumeric into Table-16 MOVE/compare legality and the string channels — the exact class error the catalog's own doc-comment (IntrinsicSig.ResultCategory, lines 49-53) says must not happen. Previously logged as U12 in docs/rearchitecture/PHASE-13-plan-vs-spec-review.md:328 and routed 'VERIFY THEN ROUTE' to Wave I (line 534); verified still unfixed at HEAD 38c4a669.
- **Proposed action:** In BindIntrinsicCall, compute the result category from the bound argument categories for the eight argument-typed functions (any/all-national arguments -> PicCategory.National per each section's type table; MAX/MIN all-national -> National, mirroring the existing CONVERT dst==3 flip), and cover with a version-matrix/conformance test that MOVEs a national-arg result to a PIC N item.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R9. [MAJOR | doc-inconsistency | A6-intrinsics] Four LOCALE-* Unsupported catalog rows claim 'documented non-support' but docs/CONFORMANCE.md documents neither the A.4.9 locale module nor the four functions (only STANDARD-COMPARE is recorded)

- **Refs:** ISO §4.2.7, Annex A.4.1/A.4.9; src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:21-28,193-196; src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:598-601; docs/CONFORMANCE.md:50,116-129
- **Evidence:** IntrinsicCatalog.cs:21-24 states Unsupported = 'the containing OPTIONAL language module is DOCUMENTED NON-SUPPORT ... non-support is conforming when documented'. But CONFORMANCE.md documents only STANDARD-COMPARE/ORDER TABLE (§3 row 25, line 50); its §4 'Documented non-support facilities' lists exactly four facilities (MCS, COMMIT/ROLLBACK, VALIDATE, screen handling — lines 122-129) with no locale-module entry, and a case-insensitive grep for the four function names (LOCALE-COMPARE/LOCALE-DATE/LOCALE-TIME/LOCALE-TIME-FROM-SECONDS) finds zero hits anywhere in the file. The conformance-legality premise of the catalog's four Unsupported rows is therefore not actually documented. Additionally §4's preamble (lines 118-120) says every non-support facility 'is recognized at compile time and reported with a named COBOLNET1560-band warning', while the locale rejection is the COBOLNET1518 ERROR — a second internal inconsistency. Previously logged as U15; verified still open at HEAD.
- **Proposed action:** Add an A.4.9 locale-module entry to CONFORMANCE.md §4 naming the four LOCALE-* functions, the LOCALE keyword phrases of NUMVAL-C/TEST-NUMVAL-C/LOWER-CASE/UPPER-CASE, and the COBOLNET1518 bind-time rejection (citing §4.2.7 + A.4.1, and A.3 item 25 for STANDARD-COMPARE), and reword the §4 preamble so the 1560-band-warning claim admits the 1518 error route (or move the locale rejection to the documented band).
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R10. [MAJOR | plan-omission | A6-intrinsics] LENGTH/BYTE-LENGTH [PHYSICAL] keyword unparsed and ref-mod/ODO-group runtime-length legs bind to compile errors — still on no executed worklist

- **Refs:** ISO §15.50.2 (spec line 36467) / §15.50.4 r4,r7-r8; §15.14.2 (line 34646) / §15.14.4 r2,r6-r7; src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:124,155 (MaxArgs 1); src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:671-675,716-722; docs/rearchitecture/PHASE-13-plan-vs-spec-review.md:994-998,534
- **Evidence:** §15.50.2/§15.14.2 general formats are 'FUNCTION LENGTH ( argument-1 [ PHYSICAL ] )' / 'FUNCTION BYTE-LENGTH ( argument-1 [ PHYSICAL ] )' (2014+, needed for §8.5.1.12 variable-length groups). A repo-wide grep shows no PHYSICAL handling in any binder/grammar (only reserved-word metadata at ReservedWords.Table.cs:323); both catalog rows cap MaxArgs at 1 so `FUNCTION LENGTH(X PHYSICAL)` cannot bind, and ref-mod/ODO-group arguments return BoundExprError (IntrinsicBinder.cs:671-675, 716-720) though §15.50.4 r4/§15.14.4 r2 define runtime values for them. The ODO-group BYTE-LENGTH error also miscites '§15.14.4 r6' (the variable-length-group rule) when the DEPENDING rule is r2. This is U13 of the prior review, whose proposed action was 'add a tracked worklist item' — verified NOT yet executed: PHYSICAL appears in no PHASE-13-audit item, no resume-prompt REMAINING entry, and no residue ledger; it exists only in the review file's un-executed Wave-I verify-then-route table.
- **Proposed action:** Execute U13's routing now: add a named residue item (P13 residue or P14 backlog) covering (a) accepting PHYSICAL in the LENGTH/BYTE-LENGTH argument position (no-op for fixed shapes per r8/r7 same-value clauses), (b) runtime legs for ref-mod (.Length of the view) and ODO-group (current DEPENDING value) arguments, and (c) correcting the two error-message citations to §15.50.4 r4 / §15.14.4 r2.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R11. [MINOR | plan-vs-impl-error | A6-intrinsics] EXCEPTION-FILE/-N file-connector-argument form is fully implemented but VCR rows 68/69 still say 'No / <!-- todo -->' and three code comments still say it 'renders loud'

- **Refs:** ISO §15.28.4 r2 / §15.29.4 r2 (Annex E.3.3 items 25/26); docs/VERSION_CHANGE_REFERENCE.md:185-186; src/Cobol.Net.Compiler/Binding/Procedure/Verbs/IntrinsicBinder.cs:293-312; src/Cobol.Net.Compiler/CodeGen/Emit/IntrinsicRenderer.cs:367-372; src/Cobol.Net.Runtime/Exceptions/EcFunctions.cs:61-62,68,72; src/Cobol.Net.Compiler/Binding/IntrinsicCatalog.cs:172,177-178; src/Cobol.Net.Editions/ConstructRegistry.g.cs:174
- **Evidence:** The 2023 form is implemented end-to-end at HEAD: BindExceptionFileArg gates via ConstructRegistry (COBOLNET0900) and resolves the FD file-name (COBOLNET1574 on failure), the renderer emits EcFn("File"/"FileN", FileKeyExpr) for the named connector, and EcFunctions.File(string)/FileN(string) read the connector per r2a/r2b (FileConnector.EverAccessed, FileRegistry.cs:225). Yet VCR rows 68/69 still read '| No | new-feature-gate <!-- todo -->', IntrinsicCatalog.cs:172/177-178 say the form 'renders loud — VCR row 68/69', EcFunctions.cs:61-62 says it 'renders loud compiler-side', and the constructs.json note behind ConstructRegistry.g.cs:174 says 'the 2023 optional-argument form stays loud'. A false 'No' in the edition-change checklist risks duplicate re-implementation during the remaining P13 batches. (= prior-review U16, still present; possibly intended for the in-flight 'FIX NOW doc/citation batch' task, so verify before duplicating that work.)
- **Proposed action:** One doc sweep: flip VCR rows 68/69 to handled (replace the '<!-- todo -->' anchor with the constructs.json row ids exception-file-argument-2023 / exception-file-n-argument-2023), and delete the three stale 'renders/stays loud' claims in IntrinsicCatalog.cs, EcFunctions.cs, and the constructs.json exception-file-n-2002 note (regen ConstructRegistry.g.cs).
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R12. [MAJOR | impl-vs-spec-drift | C3-ec-refmod-drift] WriteFill (figurative-constant MOVE into a ref-mod slice) drops AllowZeroLength — spurious fatal EC-BOUND-REF-MOD under >>REF-MOD-ZERO-LENGTH ON

- **Refs:** ISO §8.4.3.3.4 GR5c (specs/ISO_COBOL.md:7087-7089) + §7.3.23.3 GR1 (:4914); E:\CobolSharp\src\Cobol.Net.Compiler\CodeGen\Roslyn\PlaceRenderer.cs:153-154 vs :72-73; E:\CobolSharp\src\Cobol.Net.Runtime\Values\Text\CobolString.cs:85; E:\CobolSharp\src\Cobol.Net.Compiler\CodeGen\Verbs\MoveEmitter.cs:37-38
- **Evidence:** §8.4.3.3.4 GR5c: length "shall result in a positive nonzero integer, unless the REF-MOD-ZERO-LENGTH directive is set to ON, when the result may also be zero" and the GR at :7089 adds "when the REF-MOD-ZERO-LENGTH directive is in effect, a zero-length result is allowed". The ordinary write path threads the directive (PlaceRenderer.cs:72-73 passes `allowZeroLength: r.AllowZeroLength`), but `WriteFill` (PlaceRenderer.cs:153-154) — the arm MoveEmitter.cs:37-38 routes every figurative-constant MOVE into a ref-mod target through — calls `RuntimeApi.StrSpliceInto(...)` WITHOUT the allowZeroLength argument, so `CobolString.SpliceInto` (CobolString.cs:85) sees `allowZeroLength=false` and its `(length == 0 && !allowZeroLength)` leg raises fatal EC-BOUND-REF-MOD. Failure scenario: `>>REF-MOD-ZERO-LENGTH ON` + `>>TURN EC-BOUND-REF-MOD CHECKING ON` + `MOVE SPACES TO X(1:L)` with L=0 → abnormal run-unit termination, though the directive sanctions the zero-length receiver.
- **Proposed action:** In PlaceRenderer.WriteFill pass `allowZeroLength: p.AllowZeroLength` to StrSpliceInto (mirroring the Write arm), and add a conformance case: directive ON + TURN checking ON + MOVE SPACES/ZEROS into a zero-length slice must NOT raise; directive OFF must raise.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R13. [MINOR | impl-vs-spec-drift | C3-ec-refmod-drift] Sibling-assembly STOP RUN WITH STATUS is discarded — Environment.ExitCode sink gated on the MAIN group's own status-phrase usage

- **Refs:** ISO §14.9.42.4 GR5 (specs/ISO_COBOL.md:32250); E:\CobolSharp\src\Cobol.Net.Compiler\Binding\BinderDriver.cs:124-129; E:\CobolSharp\src\Cobol.Net.Compiler\CodeGen\ProgramEmitter.cs:383-387,424-427; E:\CobolSharp\src\Cobol.Net.Compiler\CodeGen\StatementEmitter.cs:93-96
- **Evidence:** §14.9.42.4 GR5 applies to STOP wherever it executes in the run unit: the status "passed to the operating system". BoundStop always writes RunUnit.ExitStatus (StatementEmitter.cs:95), but the `Environment.ExitCode = (int) RunUnit.Current.ExitStatus` sink in the generated Main is emitted only when `UsesTerminationStatus` is true for THIS compilation group (BinderDriver.cs:129 scans only the group's parse tree; ProgramEmitter.cs:424-427). The emitter itself documents that a run unit spans sibling assemblies via the ProgramRegistry.ResolveVisible rule-4 probe (ProgramEmitter.cs:383-387). Failure scenario: a status-free main group CALLs a separately compiled program that executes STOP RUN WITH ERROR STATUS 8 → RunUnit.ExitStatus=8, StopRun unwinds to the main group's `catch (StopRun) {}` (no sink) → process exits 0 instead of 8.
- **Proposed action:** Make the sink unconditional in the entry wrapper (read RunUnit.ExitStatus on both the normal-return and catch(StopRun) paths regardless of UsesTerminationStatus) and re-baseline the affected characterization goldens, or document the single-group restriction in docs/CONFORMANCE.md §4.2.16 alongside the existing implementor mapping.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R14. [MINOR | impl-vs-spec-drift | C3-ec-refmod-drift] Internal ODO-group receive splice can spuriously raise EC-BOUND-REF-MOD when the DEPENDING count makes the extent zero

- **Refs:** ISO §8.4.3.3.4 (specs/ISO_COBOL.md:7089 — the EC is defined for reference-modification only); E:\CobolSharp\src\Cobol.Net.Compiler\CodeGen\Roslyn\PlaceRenderer.cs:162-163; E:\CobolSharp\src\Cobol.Net.Runtime\Values\Text\CobolString.cs:85
- **Evidence:** EC-BOUND-REF-MOD exists only for a reference-modification whose leftmost/length evaluation violates §8.4.3.3.4. `ReceiveInto` (PlaceRenderer.cs:162-163) reuses `StrSpliceInto(image, "1", LengthExpr(p), ...)` for a GROUP receive over an occurs-depending group — no ref-mod in the source — with allowZeroLength defaulted false. For `OCCURS 0 TO n DEPENDING` with count 0 and no fixed prefix, LengthExpr evaluates to 0, so under `>>TURN EC-BOUND-REF-MOD CHECKING ON` the `(length == 0 && !allowZeroLength)` leg in SpliceInto (CobolString.cs:85) raises a fatal EC-BOUND-REF-MOD for a plain group MOVE — an EC the spec never defines for this operation (same class as the Wave F DebugRegisterPlace neutrality leak).
- **Proposed action:** Internal (non-ref-mod) splices must not consult the EC-BOUND-REF-MOD gate: pass `allowZeroLength: true` in ReceiveInto (and audit the other internal StrSpliceInto call sites, e.g. RedefViewPlace at PlaceRenderer.cs:84, for the same reuse), or give the runtime a check-free internal splice entry point.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R15. [MINOR | impl-vs-spec-drift | C3-ec-refmod-drift] The "non-integer value" raise leg of §8.4.3.3.4 GR5 is unimplementable in the current pipeline — fractional leftmost/length silently truncates

- **Refs:** ISO §8.4.3.3.4 GR5/GR5b/GR5c (specs/ISO_COBOL.md:7085-7089); E:\CobolSharp\src\Cobol.Net.Compiler\CodeGen\Roslyn\PlaceRenderer.cs:126-130; E:\CobolSharp\src\Cobol.Net.Runtime\Values\Text\CobolString.cs:47,56
- **Evidence:** The GR at :7089: "If the evaluation of leftmost-position or length results in a NON-INTEGER value, a zero value, or a value that references a position outside the area of identifier-1, the EC-BOUND-REF-MOD exception condition is set to exist." CobolString.RefMod/SpliceInto take `int leftmost, int length`, and RmStart/RmLen (PlaceRenderer.cs:129-130) render `(int)(expr)` over the integer-valued transitional index carrier — a leftmost/length arithmetic expression that evaluates non-integer (e.g. `X(W / 2 : 2)` with W=3) is truncated before the runtime check ever sees it, so the non-integer raise condition can never fire; the zero-value and out-of-range legs are the only two implemented.
- **Proposed action:** This gap is not named in the tracked P15/D10 carrier-removal item (which is about the SUBSCRIPT lexer mode, not the EC leg): either record it in docs/CONFORMANCE.md as a documented restriction until PHASE-15 replaces Start/Length with BoundExpr, or make the P15 BoundExpr conversion evaluate at full scale and raise EC-BOUND-REF-MOD on a fractional result, citing GR5 explicitly.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R16. [MINOR | doc-inconsistency | C3-ec-refmod-drift] Wrong-§ citations against the 2023 numbering in the shipped EC/exit-code code: §8.4.2.3/§8.4.2.4 for ref-mod, §14.9.43 for STOP RUN

- **Refs:** 2023 §8.4.2.3 = Subscripts (specs/ISO_COBOL.md:6628), ref-mod = §8.4.3.3/.4 (:7023,:7071); 2023 §14.9.43 = STRING (:32261), STOP = §14.9.42 (:32214); E:\CobolSharp\src\Cobol.Net.Runtime\Exceptions\ExceptionState.cs:227; E:\CobolSharp\src\Cobol.Net.Compiler\CodeGen\Roslyn\PlaceRenderer.cs:30,32; E:\CobolSharp\src\Cobol.Net.Runtime\Control\Signals\StopRun.cs:7; E:\CobolSharp\src\Cobol.Net.Compiler\CodeGen\ProgramEmitter.cs:387
- **Evidence:** The P13 scout already established ref-mod is §8.4.3.3.4, not §8.4.2.3/.4, yet ExceptionState.cs:227 still cites "ISO §8.4.2.3 c" for the EC-BOUND-REF-MOD raise (2023 §8.4.2.3 is Subscripts) and PlaceRenderer.cs:30/32 cite "ISO §8.4.2.4" for the ref-mod slice. StopRun.cs:7 and ProgramEmitter.cs:387 cite "§14.9.43" for the STOP RUN unwind — 2023 §14.9.43 is the STRING statement; STOP is §14.9.42. Violates the cited-§-must-match rule (feedback_spec_fidelity_discipline); behavior at these sites is otherwise correct.
- **Proposed action:** Fold these four exact locations into the in-flight review-remediation citation batch (task #7): §8.4.2.3/§8.4.2.4 → §8.4.3.3.4 (keep the :7089 pointer), §14.9.43 → §14.9.42.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R17. [MINOR | doc-inconsistency | C3-ec-refmod-drift] DebugItem XML doc contradicts the implemented (DB101A-pinned) DEBUG-LINE causing-statement semantics; GroupWidth comment arithmetic wrong (83 vs actual 86)

- **Refs:** X3.23-1985 debug module (facility absent from ISO 2023 — DEBUG-ITEM has zero hits in specs/ISO_COBOL.md); E:\CobolSharp\src\Cobol.Net.Runtime\Control\DebugItem.cs:74-76,78-79,103; E:\CobolSharp\src\Cobol.Net.Compiler\CodeGen\DispatchEmitter.cs:49,146-150
- **Evidence:** The Wave F blocker fix made DEBUG-LINE the CAUSING statement's line — DispatchEmitter.cs:49 (`__dbgLine // the CAUSING statement's source line`) and :146-150 (`__line = __dbgCause == StartProgram ? __subjLine : __dbgLine`) — but DebugItem.cs:78-79 still documents DebugLine as "the compiler-assigned source line of the subject procedure" and the Populate param doc (:103) as "The subject procedure's source line number", true only for the StartProgram cause. Separately, the GroupWidth comment (:74) sums "6 + 1 + 30 + 1 + 4 + 1 + 4 + 1 + 4 + 1 + 30 = 83" using 4-char subs while SubWidth=5 (S9(4) SIGN LEADING SEPARATE) — the computed constant is 86; the comment's arithmetic is wrong and understates the register image the group-MOVE path reads.
- **Proposed action:** Fix DebugItem.cs docs: DebugLine/Populate `line` = the causing statement's source line (subject's own first statement only for START PROGRAM, per DB101A), and correct the GroupWidth sum to 6+1+30+1+5+1+5+1+5+1+30 = 86.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R18. [MAJOR | impl-vs-spec-drift | A7-oo] ISO inline method invocation (§8.4.3.4 '::' identifier form) is absent; the rule and registry row bearing its name are a non-ISO dataReference(args) STATEMENT falsely attributed to 'ISO 8.4.3, introduced 2023' (CONFIRMED — was U17)

- **Refs:** spec §8.4.3.4.2 (specs/ISO_COBOL.md:7119-7139) + §8.4.3.4.3 SR1-SR4 (:7142-7151) + §8.7.4; src/Cobol.Net.Frontend/Grammar/Core/CobolOO.g4:151-157; src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4:656; src/Cobol.Net.Frontend/Grammar/Core/CobolLexer.g4:724-729; src/Cobol.Net.Editions/ConstructRegistry.g.cs:151; src/Cobol.Net.Editions/Constructs.g.cs:149; src/Cobol.Net.Compiler/Binding/Bound/StatementBinder.cs:324
- **Evidence:** Spec: inline method invocation is an IDENTIFIER form '{object-class-name-1 | identifier-1} :: literal-1 [(args…)]' referencing a temporary data item equivalent to INVOKE…RETURNING temp (GR1), never a statement, and SR1 says it 'shall not be specified as a receiving operand'. Shipped: 'inlineMethodInvocationStatement : dataReference LPAREN argumentList? RPAREN' is a STATEMENT alternative gated {is2023()}?; no '::' token exists (CobolLexer.g4 has single COLON only — its own comment at :728 cites "'::' §8.7.4" as a model); no binder arm consumes it (grep 'inlineMethod' in Compiler = 0 hits; falls to BoundUnsupported at StatementBinder.cs:324); the gate constant InlineMethodInvocation2023 has zero Check() references (dead gate). ConstructRegistry.g.cs:151 claims 'ISO 8.4.3 (in-line method invocation, introduced 2023)' — wrong shape AND wrong edition (the construct is 2002-introduced; the 130-row 2002→2023 VCR has no inline-invocation row). Conforming '::' source is a bare parse error at every edition.
- **Proposed action:** Implement §8.4.3.4 for ≥2002: add the '::' separator token and an inline-invocation identifier alternative desugaring to INVOKE…RETURNING a temp cloned from the method's RETURNING description (GR1), with SR1 sending-only, SR2 (class object; not NULL/universal), SR4 (RETURNING not ANY LENGTH/ACTIVE-CLASS); DELETE the non-ISO dataReference(args) statement rule and fix the registry row's edition+citation. Minimum this cycle: strip the false ISO attribution from ConstructRegistry.g.cs:151 and DESIGN-frontend-grammar.md, add a named deferral row to a live worklist. Flip the U17 ledger disposition to CONFIRMED.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R19. [MAJOR | plan-omission | A7-oo] Object-view (§8.4.3.5 'identifier AS [FACTORY OF] class [ONLY] | interface | UNIVERSAL') has zero surface, EC-OO-CONFORMANCE has no raise site anywhere, and no live worklist names it (CONFIRMED — was U18)

- **Refs:** spec §8.4.3.5.2-.4 (specs/ISO_COBOL.md:7189-7229, GR2-GR6 each 'the EC-OO-CONFORMANCE exception condition is set to exist'); Table 13 :24750; A.4.10 :40404-40414 (not among the 3 optional OO items → mandatory 2002+); src/Cobol.Net.Runtime/Exceptions/ExceptionCatalog.cs:139 (sole EC-OO-CONFORMANCE hit in src); docs/COBOLNET_OO_DESIGN.md:242,303; src/Cobol.Net.Compiler/Binding/Procedure/Verbs/OoBinder.cs:707,773
- **Evidence:** §8.4.3.5.1: an object-view 'causes an object reference to be treated as though it had the specified description. A runtime conformance check for this description will be done on the object' — the compile-time re-description (GR1) plus the runtime EC-OO-CONFORMANCE check (GR2-GR6). A.4.10 lists exactly three optional OO items (multiple-inheritance repetition x2, parametric polymorphism); object-view is mandatory. Grep confirms: no objectView/AS-view rule in the Frontend grammar (conforming 'x AS UNIVERSAL' is a bare parse error, contra the staged-loud doctrine), and EC-OO-CONFORMANCE exists only as the catalog registration with no raise site. The shipped SET binder even depends on it existing: OoBinder.cs:773 diagnoses 'a universal sender needs an object view to narrow' — the narrowing tool it points users to does not exist. Its only ever-named owner ('the EC-OO/object-view wave', COBOLNET_OO_DESIGN.md:242/303) closed with P9 without it; no live worklist (rearchitecture plan, P13 audit/scouts, resume-prompt, P10 forward ledger, CONFORMANCE.md) names it.
- **Proposed action:** Add a named row to a live worklist (P13 Wave I follow-ons or P14 entry): parse the AS object-view in the identifier tier, bind it as a compile-time description override reusing OoConformance.ObjectRefWideningMismatch for the static leg, and emit the runtime class/interface check raising EC-OO-CONFORMANCE. Until landed, stage the syntax loud with a named 0899-band descriptor and repoint COBOLNET_OO_DESIGN.md:242/303 at the real owner. Flip the U18 ledger disposition to CONFIRMED.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R20. [MAJOR | plan-omission | A7-oo] USAGE OBJECT REFERENCE [FACTORY OF] ACTIVE-CLASS and the ONLY phrase are unimplemented; ACTIVE-CLASS mis-diagnoses as COBOLNET0813 'unknown class', ONLY is a bare parse error; the owning slice closed without them (CONFIRMED — was U19)

- **Refs:** spec USAGE format (specs/ISO_COBOL.md:22679-22682: 'OBJECT REFERENCE' bracket = interface-name-1 | [FACTORY OF] ACTIVE-CLASS | [FACTORY OF] object-class-name-1 [ONLY]) + SR16 :22755-22756; A.4.10 :40404-40414; src/Cobol.Net.Frontend/Grammar/Core/CobolOO.g4:145-149; src/Cobol.Net.Compiler/Binding/DataBinder.cs:1798-1803; src/Cobol.Net.Editions/ReservedWords.Table.cs (ACTIVE-CLASS reserved, no lexer token); docs/COBOLNET_OO_SLICE_BRIEFS.md:514
- **Evidence:** The spec bracket carries three alternatives incl. '[FACTORY OF] ACTIVE-CLASS' and the trailing '[ONLY]', with SR16 restricting ACTIVE-CLASS to factory/instance definitions or a method's linkage/local-storage; ACTIVE-CLASS is not among A.4.10's three optional items → mandatory. objectReferenceUsage (CobolOO.g4:145-149) carries only 'FACTORY OF className | className | ε'. ACTIVE-CLASS has no lexer token, so 'USAGE OBJECT REFERENCE ACTIVE-CLASS' lexes as a cobolWord, parses as className, and DataBinder.cs:1798-1803 fires COBOLNET0813 "names the unknown class or interface 'ACTIVE-CLASS'" — a wrong diagnostic on conforming source; '… object-class ONLY' is a bare syntax error, and OoConformance has no exact-class (no-subclass) leg. The only tracking (COBOLNET_OO_SLICE_BRIEFS.md:514: owned by 'the INTERFACE-ID slice; the USAGE grammar carries neither yet') is dangling — that slice landed (interface-typed USAGE resolves via FindInterface at DataBinder.cs:1799) without delivering it, and no live worklist names either phrase.
- **Proposed action:** Add an ACTIVE_CLASS token plus '[FACTORY OF] ACTIVE-CLASS' and trailing 'ONLY' alternatives to objectReferenceUsage; bind ACTIVE-CLASS as the covariant self-type (the §16.2.1 covariant __New machinery is the natural carrier) with the SR16 placement check, and ONLY as exact-class conformance in OoConformance.DescriptionMismatch/ObjectRefWideningMismatch. Or minimally stage both loud under named 0899 descriptors (never the misleading 0813) and add a live worklist row. Flip the U19 ledger disposition to CONFIRMED.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R21. [MAJOR | plan-omission | A7-oo] Parameterized classes/interfaces (§9.3.12: CLASS-ID/INTERFACE-ID USING, REPOSITORY EXPANDS/AS) are mandatory surface with zero grammar, and the slice briefs' '0899 staged-loud residue' claim for them is FALSE (CONFIRMED — was U20)

- **Refs:** spec §9.3.12 (specs/ISO_COBOL.md:12463-12469); §11.3.2 format :13009-13014 ('CLASS-ID. object-class-name-1 [AS literal-1] [IS FINAL] [INHERITS…] [USING {parameter-name-1}…]'); §12.3.8 class/interface-specifiers :14694-14704 ('[AS literal] [EXPANDS … USING …]'); A.4.10 :40404-40414; src/Cobol.Net.Frontend/Grammar/Core/CobolOO.g4:39-47,72-78; src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4:356-372; docs/COBOLNET_OO_SLICE_BRIEFS.md:1275-1277
- **Evidence:** §9.3.12: 'A parameterized class is a generic or skeleton class that has formal parameters…'; §11.3.2 puts '[USING {parameter-name-1}…]' in the CLASS-ID format and §12.3.8's class-specifier carries 'CLASS … [AS literal-1] [EXPANDS object-class-name-2 USING {…}…]' (interface-specifier likewise). A.4.10 lists exactly three optional OO items — parameterized classes are not among them, so per the project's own A.4.10 analysis (COBOLNET_OO_DESIGN.md:485) they are mandatory conforming-2023 surface. The grammar has no USING on classIdParagraph (CobolOO.g4:40, which also lacks the §11.3.2 'AS literal-1' externalized-name phrase) or interfaceDefinition (:72-78), and repositoryEntry (CobolParserCore.g4:366-371) has neither EXPANDS nor AS (comment: 'The AS external-name phrase is deferred') — conforming source is a bare parse error. COBOLNET_OO_SLICE_BRIEFS.md:1275-1277 claims a '0899 staged-loud residue: parameterized interfaces (INTERFACE-ID USING / EXPANDS, §12.3.8 SR3 …)' — false: no 0899 staging exists for any of these phrases (only the FACTORY OF usage tail genuinely parses-then-0899s); no live worklist names the feature.
- **Proposed action:** Make the v1 disposition explicit and owned: superset-parse USING/EXPANDS/AS and stage them loud with a named descriptor plus a live worklist row (P14/P15 candidate), or take an owner decision recording the deviation in CONFORMANCE.md (noting §4.2.7 cannot sanction it — the element is not in A.4); in either case correct the slice-brief 0899 claim at COBOLNET_OO_SLICE_BRIEFS.md:1275-1277. Flip the U20 ledger disposition to CONFIRMED.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R22. [MINOR | doc-inconsistency | A7-oo] CONFORMANCE.md A.3 row 45 marks parametric-polymorphism method resolution 'Claimed' while the code explicitly rejects/defers parametric polymorphism (COBOLNET0822, D9) (CONFIRMED — was U21)

- **Refs:** spec A.3 #45 (specs/ISO_COBOL.md:40224: 'When parametric polymorphism is provided…'); A.4.10 #3 :40414; docs/CONFORMANCE.md:66; src/Cobol.Net.Compiler/Oo/OoClassTable.cs:264-268,307-310
- **Evidence:** A.3 item 45 applies ONLY when the optional A.4.10 #3 feature is provided: 'When parametric polymorphism is provided in object orientation, the algorithm used for method resolution may be dependent on the methodology used…'. CONFORMANCE.md:66 (verified still current after the remediation commits) reads '| 45 | Parametric-polymorphism method resolution | 11.x | **Claimed** | Single-dispatch OO method resolution over the class table |' — but the compiler does NOT provide parametric polymorphism: OoClassTable.cs:264-268/307-310 reject duplicate method names with COBOLNET0822 ('parametric polymorphism … is an OPTIONAL feature and is deferred', deep-dive D9). The authoritative §4.2.16 record claims a facility the code rejects (same family as the Wave-G '04' false-claim fix). CONFORMANCE.md also nowhere records the three A.4.10 optional-element dispositions.
- **Proposed action:** Flip row 45 to 'N/A — parametric polymorphism (A.4.10 optional item 3) is not provided (COBOLNET0822 enforces unique method names)', and add the three A.4.10 optional-element dispositions to CONFORMANCE.md (item 1 not claimed → 0849; item 2 claimed — interface INHERITS repetition supported; item 3 not claimed → 0822). Cheap doc-only fix — fold into the in-progress FIX NOW doc batch. Flip the U21 ledger disposition to CONFIRMED.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

### R23. [MINOR | impl-vs-spec-drift | A7-oo] The REPOSITORY-declaration requirement for referenced class/interface names (§11.3.3 SR2 / §11.8.3 SR1 / §12.3.8) is silently unenforced — an unrecorded, non-dialect-gated leniency acknowledged only in a code comment (CONFIRMED — was U22)

- **Refs:** spec §11.3.3 SR2 (specs/ISO_COBOL.md:13020: 'Object-class-name-2 shall be the name of a class specified in the REPOSITORY paragraph of this source element'); §12.3.8 :14694+; src/Cobol.Net.Compiler/Oo/OoClassTable.cs:369-371; src/Cobol.Net.Compiler/Binding/DataBinder.cs:1798-1803; src/Cobol.Net.Compiler/Binding/ReferenceResolver.cs:68-76
- **Evidence:** §11.3.3 SR2 requires the INHERITS base to be REPOSITORY-declared in the referencing source element (§11.8.3 SR1 is the IMPLEMENTS parallel). OoClassTable.cs:369-371 self-documents the gap: '§11.8.3 SR1's REPOSITORY requirement is enforced with the repository binding — staged as a documented follow-up' — but no worklist, ledger, or the dialect-strictness leniency registry documents that follow-up. In practice class/interface names resolve group-wide with zero repository check (DataBinder.cs:1798-1803 resolves USAGE OBJECT REFERENCE names straight off the group table; INHERITS/IMPLEMENTS/INVOKE likewise), while PROPERTY names asymmetrically DO enforce their §12.3.8 specifier (ReferenceResolver.cs:72-75, COBOLNET0843 'requires a PROPERTY specifier in the REPOSITORY paragraph'). Accepting nonconforming source ungated contradicts the every-leniency-dialect-gated rule (project_dialect_strictness).
- **Proposed action:** Enforce the repository-declaration SRs as a bind diagnostic (class/interface names referenced by INHERITS/IMPLEMENTS/USAGE/INVOKE must appear in the referencing element's REPOSITORY paragraph or be the element's own name), mirroring the property-name mechanism at ReferenceResolver.cs:72 — or register the leniency in the dialect-strictness leniency registry with a live worklist row. Flip the U22 ledger disposition to CONFIRMED.
- **Disposition:** _raw resume-pass capture — dedupe vs sections 2/3, verify, route (Wave I)_

## 8. Batch-1 finder results (2026-07-19 — the three previously-unrun areas, now COVERED; raw, verdicts pending)

Run as the first ≤10-agent batch of the owner-directed batched completion (3 agents, 0 errors, ~595k tokens).
These close the A3/A4/C4 coverage gaps of §5; each finding below is RAW (unverified) — verify + route.

### B1-A3-io-env — 3 findings

#### F1. [MAJOR | impl-vs-spec-drift | A3-io-env] ASSIGN USING data-name (dynamic file assignment) parses and compiles clean but is silently ignored — the runtime opens a file derived from the SELECT name, not the data item's content

- **Refs:** specs/ISO_COBOL.md §12.4.5.3 GR3b + GR4 (association from the content of data-name-1 at OPEN/SORT/MERGE), §9.1.21 Dynamic file assignment, §9.1.13.6 item 2 (status '31'); src/Cobol.Net.Frontend/Grammar/Core/CobolIO.g4:36-39 (assignClause both USING forms); src/Cobol.Net.Compiler/Binding/DataBinder.cs:682-686
- **Evidence:** BindFileControl reads only assignClause()?.assignTarget(); the USING dataReference is never bound in either form, and for the bare `ASSIGN USING data-name` form AssignTarget silently stays = the SELECT file name (DataBinder.cs:682 `AssignTarget = name`). Probe (cobol.exe, --std 2023): `SELECT OUTF ASSIGN USING WS-FNAME` with WS-FNAME = "dynamic-target.dat" compiled with ZERO diagnostics, ran, and created `outf.txt` — the data item's runtime content is completely ignored. No intro gate either (USING is 2002+; compiles at 85). I-O status '31' (USING content inconsistent) consequently has no seam. Zero hits for '9.1.21' / 'ASSIGN USING' / 'dynamic file assignment' across docs/, resume-prompt.md, the P13 audit, both scouts, and the review ledger — untracked.
- **Proposed action:** Either implement it (bind the USING data item onto FileModel; runtime resolves the physical name from the item's content at each OPEN/SORT/MERGE per §12.4.5.3 GR3b, with a '31'/'30' determination documented in CONFORMANCE.md §3) or, minimally, reject the USING phrase loudly with a named COBOLNET diagnostic + a CONFORMANCE.md disposition and a VCR intro row (assign-using-2002). Silent acceptance of a semantics-bearing phrase is the U9 (DISPLAY UPON) pattern and must not survive P13 close.
- **Disposition:** _raw batch-1 capture — verify + route_

#### F2. [MAJOR | plan-omission | A3-io-env] File control entry COLLATING SEQUENCE clause (§12.4.5.7) has zero grammar surface — hard generic parse error, on no worklist, and only its cultural/ISO-14651 legs are disposed

- **Refs:** specs/ISO_COBOL.md §12.4.5.7 (lines 15389-15476: alphanumeric + FOR NATIONAL forms, alphabet-name operands; GRs make it govern indexed-file key comparison/ordering); src/Cobol.Net.Frontend/Grammar/Core/CobolIO.g4:46-71 (fileControlClauses — no collating alternative; genericClause cannot match, COLLATING is a lexer token CobolLexer.g4:367); docs/CONFORMANCE.md:66 (A.3 rows 41-42 dispose only CULTURAL collating)
- **Evidence:** Probe: `SELECT IXF ... RECORD KEY IS IX-KEY COLLATING SEQUENCE IS MY-ALPHA` fails with generic `error COBOL0312: unexpected 'COLLATING'` at all editions. The plain alphabet-name form is core COBOL-2002+ surface, NOT the processor-dependent cultural leg disposed by A.3 items 41-42. Zero hits for '12.4.5.7' anywhere in docs/ (audit, both P13 scouts, VCR, CONFORMANCE, review ledger). The adjacent machinery exists (PROGRAM COLLATING SEQUENCE + SORT/MERGE COLLATING landed in P10 Step 4, incl. the shared collatingForPhrase subrule), and the tracked P13 SUPPRESS WHEN design (PHASE-13-wave-c-scout.md:438,558) explicitly keys its equality test on 'the file's collating sequence' — which today cannot be declared at all. Same class as review finding C5/U10 (RW SUPPRESS: no grammar rule, untracked), which the ledger rated MAJOR.
- **Proposed action:** Add to the P13 GRAMMAR BATCH (shared .g4 ⇒ the one legacy guard): a collatingSequenceClause alternative in fileControlClauses reusing collatingForPhrase, bound onto FileModel and consumed by IndexedConnector key ordering/START comparisons (reuse the P10 CollatingModel machinery); intro-gate 2002 (COBOLNET0900 row). If deferred instead, stage it loud with a NAMED diagnostic + a CONFORMANCE.md non-support note covering the non-cultural form — then record it on the remaining-work list either way.
- **Disposition:** _raw batch-1 capture — verify + route_

#### F3. [MINOR | impl-vs-spec-drift | A3-io-env] OBJECT-COMPUTER CHARACTER CLASSIFICATION (locale module) is silently swallowed by the computerAttributes token sink — compiles with zero diagnostics while every other unclaimed locale leg rejects loudly

- **Refs:** specs/ISO_COBOL.md §12.3.6.2/§12.3.6.4 (CHARACTER CLASSIFICATION general rules; Annex A.4.9 item 7); src/Cobol.Net.Frontend/Grammar/CobolParserCore.g4:398-400 (computerAttributes: ~(DOT|PROGRAM)+); src/Cobol.Net.Compiler/Validation/VersionConformancePass.cs:460-482 (token-scan cases: MEMORY, SEGMENT-LIMIT, DEBUGGING only); docs/CONFORMANCE.md:6-9 (§4.2.6 'warning when syntactically detectable' claim)
- **Evidence:** Probe: `OBJECT-COMPUTER. HOST-BOX CHARACTER CLASSIFICATION IS LC-LOC.` compiles and runs with ZERO diagnostics at --std 2023 — the clause (which changes class-condition and UPPER-/LOWER-CASE classification semantics per §12.3.6.4) is silently dropped into the computerAttributes sink, whose deleted-element token-scan handles MEMORY/SEGMENT-LIMIT/DEBUGGING but has no CLASSIFICATION case. All sibling legs of the unclaimed A.4.9 locale module are loud (probe: SPECIAL-NAMES LOCALE and ORDER TABLE → parse errors; LOCALE-* intrinsics → COBOLNET1518), so this is the one locale entry point that is silently ACCEPTED — a hole in the §4.2.6 'syntactically-detectable ⇒ warning' posture. Review ledger U15 covers only the CONFORMANCE.md documentation gap for the locale facility, not this code-level silent swallow; no other tracker names it.
- **Proposed action:** Add a "CLASSIFICATION" case to the VisitComputerAttributes token-scan emitting the named locale-module non-support diagnostic (the COBOLNET1518/1560-band pattern), and fold the clause into the locale-facility disposition row that the U15 remediation adds to CONFORMANCE.md §4 — one-line code fix, same change set as the U15 doc fix.
- **Disposition:** _raw batch-1 capture — verify + route_

### B1-A4-data-clauses — 5 findings

#### F4. [MAJOR | plan-omission | A4-data-clauses] VALUE clause Format 2 (table) — §13.18.63.2 — has no grammar, no gate, no ledger row; multi-literal VALUE on a data item silently drops all but the first literal

- **Refs:** specs/ISO_COBOL.md:23155-23162 (§13.18.63.2 Format 2), :43244 (spec example `VALUES ARE "Leeds", "Bordeaux", "Pisa" FROM (1) TO (3)`); src/Cobol.Net.Frontend/Grammar/Core/CobolData.g4:487-496 (valueClause — no FROM/TO-subscript phrase); src/Cobol.Net.Compiler/Binding/DataBinder.cs:2189-2192 (ExtractValue takes FirstOrDefault); docs/COBOLNET_DESIGN.md:267; docs/COBOLNET_DATA_MODEL_DESIGN.md:89,584
- **Evidence:** The 2002+ table format `{VALUE IS|VALUES ARE} {{literal-1}... FROM ({subscript-1}...) [TO ({subscript-2}...)]}...` cannot parse — FROM is unconsumed by valueClause, so a conforming program dies with a raw COBOL0001, violating the staged-loud convention. Meanwhile the grammar's valueItem loop ACCEPTS `VALUE 1 2 3` on an ordinary data item (only legal for 88s/report format 4) and ExtractValue silently binds the FIRST literal — silent wrong-value acceptance. COBOLNET_DESIGN.md:267 admits 'ExtractValue currently grabs only FirstOrDefault — a bug for 88s and table VALUEs' (the 88 half was since fixed; the table half was not), and DATA_MODEL_DESIGN.md:89 mis-describes the feature as a bare positional list 'padded with the last/default' — the ISO format keys literals to occurrence ranges via required FROM (subscript). Not on the P13 audit, either scout, PHASE-10/PHASE4 ledgers, CONFORMANCE.md, the review ledger, or any VCR/constructs.json row (VALUE rows 34/35/36/86 are all numeric-edited-VALUE, not format 2).
- **Proposed action:** Add a tracked worklist row (P13 review batch or the grammar batch, since valueClause is in the shared .g4 ⇒ full legacy guard): (1) parse the FROM/TO-subscript phrase with a 2002 intro gate (COBOLNET0900 + constructs.json row); (2) bind literals to occurrence ranges in the OCCURS initializer; (3) until then, reject >1 valueItem on a non-88/non-report entry with a NAMED diagnostic instead of silent-first; (4) correct COBOLNET_DATA_MODEL_DESIGN.md:89/584 to the FROM/TO-subscript model and verify the report-section format-4 multi-literal path separately.
- **Disposition:** _raw batch-1 capture — verify + route_

#### F5. [MAJOR | impl-vs-spec-drift | A4-data-clauses] BLANK WHEN ZERO / JUSTIFIED / SIGN applicability syntax rules unenforced — the clause is silently inert (or silently dropped) on illegal subjects

- **Refs:** specs/ISO_COBOL.md:17824-17826 (§13.18.8.3 SR1/SR2), :19260-19264 (§13.18.32.3 SR1/SR3), :21915-21921 (§13.18.52.3 SR1/SR2); src/Cobol.Net.Compiler/Binding/PictureAnalyzer.cs:241-250; src/Cobol.Net.Compiler/Binding/Model/PicInfo.cs:377-378; src/Cobol.Net.Compiler/Binding/DataBinder.cs:1710-1715,1749-1750; src/Cobol.Net.Compiler/Validation/ (zero matches for §13.18.8/.32/.52)
- **Evidence:** No diagnostic exists for any of: BWZ on a picture containing 'S' or a non-DISPLAY usage (§13.18.8 SR1/SR2 — PictureAnalyzer:244 only honors BWZ when `!signed && usage is Display`, so `PIC S9(3) BLANK WHEN ZERO` or `PIC 9 COMP BLANK WHEN ZERO` compiles with the clause silently dropped — zero stores '000', not spaces); JUSTIFIED on a numeric/numeric-edited/edited item or a group (§13.18.32 SR1 elementary-only, SR3 alphabetic/alphanumeric/boolean/national only — the flag is captured unconditionally and numeric paths silently ignore it); SIGN on an entry whose picture lacks 'S' or whose usage is not display/national (§13.18.52 SR1/SR2 — SignKindFor:377 returns 'unused for an unsigned item', non-display silently maps to BinaryMinus). The only negative test in the family is sync_group_below_2023. This is the project's named W2 silent-misbind hazard class, yet none of these three clauses' applicability SRs appear on any residue ledger, the P13 audit, or the review ledger.
- **Proposed action:** Add one bind-time applicability check in BindEntry (post-Analyze, where Pic category/usage are known) emitting a named 15xx-band diagnostic per violated SR (BWZ+S / BWZ+non-display / JUSTIFIED on numeric-or-group / SIGN without S / SIGN on non-display-national), plus negative goldens; sweep TYPE/SAME-AS clones (the flags are description-copied at DataBinder.cs:1174-1184) so the check runs on expanded entries too.
- **Disposition:** _raw batch-1 capture — verify + route_

#### F6. [MINOR | doc-inconsistency | A4-data-clauses] JUSTIFIED cited as §13.18.34 (=LINAGE clause) instead of §13.18.32 at three shipped code sites

- **Refs:** src/Cobol.Net.Compiler/Binding/DataBinder.cs:1723; src/Cobol.Net.Compiler/Binding/Model/DataItem.cs:177; src/Cobol.Net.Compiler/CodeGen/Verbs/MoveEmitter.cs:106; specs/ISO_COBOL.md:19241 ('## 13.18.32 JUSTIFIED clause'), :19345 ('## 13.18.34 LINAGE clause')
- **Evidence:** Three sites carry 'JUSTIFIED [RIGHT] (ISO §13.18.34 …)' / '§13.18.34 attaches to the receiver' — §13.18.34 is the LINAGE clause; JUSTIFIED is §13.18.32 (as the same codebase correctly cites at MoveEmitter.cs:343/350 and Cobol.Net.Runtime/Values/Text/CobolString.cs:22). Not covered by review-ledger C18 or R7 (whose §-citation lists name GENERATE/TERMINATE/GROUP INDICATE/ExternalRecordNotCellBacked/OPEN/DISPLAY/CLOSE/SORT sites only — C18's verifier note even confirms 13.18.34 as correct for the FILES_DESIGN LINAGE row, a different subject). Violates the load-bearing spec-to-code traceability rule.
- **Proposed action:** Fold the three sites into the in-flight FIX-NOW wrong-§ citation batch (task #7): §13.18.34 → §13.18.32 at DataBinder.cs:1723, DataItem.cs:177, MoveEmitter.cs:106.
- **Disposition:** _raw batch-1 capture — verify + route_

#### F7. [MINOR | doc-inconsistency | A4-data-clauses] CONFORMANCE.md §4 item 3's VALIDATE-facility element list omits the VALIDATE-STATUS clause, data description entry format 4, and VALUE format 5 that A.4.14 enumerates

- **Refs:** docs/CONFORMANCE.md:133-135; docs/rearchitecture/PHASE-13-remaining-waves-scout.md:1771 (§A.4.14 element enumeration); specs/ISO_COBOL.md:23020 (§13.18.62), :23187-23202 (§13.18.63.2 Format 5)
- **Evidence:** The scout's §A.4.14 row (trusted per the standing re-scout rule) enumerates the optional VALIDATE element as: data description entry format 4 (13.16), DEFAULT, DESTINATION, INVALID, PRESENT WHEN, VALIDATE-STATUS (13.18.62), VALUE format 5 content-validation (13.18.63), VARYING, the VALIDATE statement, and EC-VALIDATE. CONFORMANCE.md item 3 — the doc the Wave H recognize-and-name half will implement from — names only 'the `VALIDATE` statement, the validation clauses (`CLASS`/`DEFAULT`/`DESTINATION`/`INVALID`/`PRESENT WHEN`/`VARYING`), and EC-VALIDATE', dropping VALIDATE-STATUS, the format-4 entry shape, and VALUE format 5 (CLASS, which it does list, is not even in A.4.14's list — it is core §13.18.11). A non-claimed optional element must be completely identified for the §4.2.13 obsolete-flagging claim to hold.
- **Proposed action:** Reword CONFORMANCE.md item 3 to the full A.4.14 enumeration (add VALIDATE-STATUS §13.18.62, data description entry format 4 §13.16, VALUE format 5 §13.18.63; keep CLASS with a note it is validation-context) — fold into the in-flight CONFORMANCE.md rewording batch (task #7).
- **Disposition:** _raw batch-1 capture — verify + route_

#### F8. [MINOR | doc-inconsistency | A4-data-clauses] The SYNCHRONIZED = no-alignment/no-padding implementor determination is documented only in code comments, not in CONFORMANCE.md §3's pinned-determinations record

- **Refs:** src/Cobol.Net.Compiler/Binding/Model/DataItem.cs:187-191,371; src/Cobol.Net.Compiler/Binding/DataBinder.cs:1715,2240-2248; docs/CONFORMANCE.md:72-119 (§3 — no SYNCHRONIZED row); specs/ISO_COBOL.md:22197 (§13.18.55)
- **Evidence:** SYNCHRONIZED is deliberately a no-op in the typed-native model ('COBOL.NET has no SYNCHRONIZED physical padding' — DataItem.cs:371), with only the 2023 SYNC-on-group introduction gated (DataBinder.cs:2245, negative sync_group_below_2023). §13.18.55 leaves alignment/padding implementor-defined, so the no-op is a conforming determination — but CONFORMANCE.md §3, the stated home for 'pinned implementor choices', has no row for it, and the doc's own maintenance rule (§5) requires the claims doc to carry such determinations. The review ledger's U28 plans exactly this recording for the sibling ALIGNED clause ('record that determination in CONFORMANCE.md') but does not cover SYNCHRONIZED.
- **Proposed action:** Add a one-line §3 determination row to CONFORMANCE.md: SYNCHRONIZED (§13.18.55) accepted, no alignment/slack bytes in the typed-native data model (elementary and, at ≥2023, group), record layouts are dense — done together with the planned U28 ALIGNED row.
- **Disposition:** _raw batch-1 capture — verify + route_

### B1-C4-waveC-drift — 4 findings

#### F9. [MAJOR | impl-vs-spec-drift | C4-waveC-drift] GOBACK/STOP status-phrase with a non-numeric operand (spec-valid per SR6/SR7/SR8) is routed through the numeric expression spine and emits invalid C# — conforming programs fail with garbage Roslyn errors

- **Refs:** specs/ISO_COBOL.md:27700-27706 (§14.9.18.3 SR6 'integer data item or a data item with usage display or usage national', SR7 'If literal-1 is numeric, it shall be an integer', SR8 zero-length); §14.9.42.3 STOP sibling; E:\CobolSharp\src\Cobol.Net.Compiler\Binding\Procedure\Verbs\ControlFlowBinder.cs:42-49; E:\CobolSharp\src\Cobol.Net.Compiler\Binding\Procedure\ExpressionBinder.cs:324-346; E:\CobolSharp\src\Cobol.Net.Compiler\CodeGen\Emit\EmitCore.cs:115-136; E:\CobolSharp\src\Cobol.Net.Compiler\CodeGen\Emit\NumericRenderer.cs:282-283
- **Evidence:** SR7 constrains only NUMERIC literals and SR6 explicitly admits a usage-display/national identifier, so `GOBACK WITH ERROR STATUS "ABC"` is conforming. BindTerminationStatus (ControlFlowBinder.cs:45-47) binds literal-1/identifier-2 via the NUMERIC BindExpr spine: NumLiteral's fallthrough (ExpressionBinder.cs:345) wraps the QUOTED text in BoundNumLiteral, and NumericRenderer.ExitStatus → EmitText.UnscaledLit → IntLiteral (EmitCore.cs:134) emits `"ABC"L` — invalid C#, so the generated assembly fails Roslyn compilation with raw CS errors (the reject-valid-input class Wave I called the worst; it reviewed only the grammar shape of statusPhrase). Same path serves STOP RUN (ControlFlowBinder.cs:34), so the 2002-era STOP status phrase is affected too. Additionally SR8 (zero-length literal, e.g. STATUS "") is unchecked (same invalid-C# emission) and SR7's integer requirement is undiagnosed (STATUS 1.5 silently truncates at Align scale 0).
- **Proposed action:** In BindTerminationStatus, branch on operand category: a non-numeric literal / display-national identifier is carried as text and converted per a documented CONFORMANCE.md §4.2.16 rule (GR10 leaves the value treatment implementor-defined — e.g. parse-integer-or-error-status-default), or at minimum rejected with a NAMED diagnostic; add the SR7 integer-literal check and the SR8 zero-length check at bind. Ship a golden for the display-identifier form plus negatives for SR7/SR8.
- **Disposition:** _raw batch-1 capture — verify + route_

#### F10. [MAJOR | impl-vs-spec-drift | C4-waveC-drift] SET [SIZE OF] dyn-length never sets EC-STORAGE-NOT-AVAIL on the GR37 negative / GR38 clamp legs; the in-code deferral rationale ('checking-off no-op either way') is false and the gap is on no worklist

- **Refs:** specs/ISO_COBOL.md:31737-31741 (§14.9.39.4 GR37: negative → length 0 AND 'an EC-STORAGE-NOT-AVAIL exception condition is set to exist'; GR38: >max → clamp AND EC set); E:\CobolSharp\src\Cobol.Net.Runtime\Values\Text\CobolDynString.cs:30-44; E:\CobolSharp\src\Cobol.Net.Compiler\CodeGen\Verbs\SetEmitter.cs:83-89; E:\CobolSharp\src\Cobol.Net.Compiler\Binding\Procedure\Verbs\ControlFlowBinder.cs:56-61 (the CONTINUE pattern); E:\CobolSharp\src\Cobol.Net.Runtime\Exceptions\ExceptionCatalog.cs:200
- **Evidence:** GR37 and GR38 each mandate 'an EC-STORAGE-NOT-AVAIL exception condition is set to exist' (negative expression → 0 + EC; expression > max → clamp + EC). CobolDynString.SetSize (:39-44) floors/clamps but has no EC path, no check flag, and BoundSetSize/EmitSetSize carry none; the only mention is the doc comment (:34-36) claiming the EC 'is a checking-off no-op — the stored value is identical either way — so it is a staged follow-on'. That rationale is wrong: under `>>TURN EC-STORAGE-NOT-AVAIL CHECKING ON` (catalogued nonfatal, ExceptionCatalog.cs:200) the EC is observable via FUNCTION EXCEPTION-STATUS — exactly why the SAME Wave C wired the identical nonfatal pattern for CONTINUE (BindContinue captures TurnState.Enabled at :59; CobolTiming raises via ExceptionState.Set). Untracked: the resume-prompt/DEVLOG-890 SET-SIZE follow-on names only the SR34 compile-time check ('the runtime already floors ... so the behavior is correct' — overlooking the EC half of GR37/GR38); no ledger/worklist row carries the runtime EC.
- **Proposed action:** Mirror the CONTINUE pattern: capture ctx.EcState.Turn.Enabled("EC-STORAGE-NOT-AVAIL", ...) at bind onto BoundSetSize, thread a check flag into CobolDynString.SetSize (or wrap in the emitter) and ExceptionState.Set("EC-STORAGE-NOT-AVAIL", fatal:false) on the negative and clamp legs; fix the false comment; golden with >>TURN ... CHECKING ON + FUNCTION EXCEPTION-STATUS hitting both legs. If deferred instead, add a named row to the P13 remaining-work list.
- **Disposition:** _raw batch-1 capture — verify + route_

#### F11. [MAJOR | plan-vs-impl-error | C4-waveC-drift] Inline PERFORM accepts multiple performOptions and the binder first-wins silently — SR8's same-statement UNTIL EXIT + VARYING/UNTIL combination parses and mis-executes, contradicting the Wave-I follow-on's 'cannot parse' claim

- **Refs:** specs/ISO_COBOL.md:29509 (§14.9.28.3 SR8: 'The UNTIL EXIT phrase shall not be specified in a PERFORM statement WITH ... the VARYING phrase or either the TEST BEFORE or TEST AFTER phrase'); E:\CobolSharp\src\Cobol.Net.Frontend\Grammar\Core\CobolControlFlow.g4:26,44-47; E:\CobolSharp\src\Cobol.Net.Compiler\Binding\Procedure\Verbs\ControlFlowBinder.cs:155-173; E:\CobolSharp\DEVLOG.md:613-614; E:\CobolSharp\resume-prompt.md:89
- **Evidence:** DEVLOG 890 / the tracked follow-on assert only the nested-under-VARYING half is open because 'the same-statement VARYING+UNTIL-EXIT form cannot parse (they are alternatives of performOptions)'. That is false for the inline form: `PERFORM performOptions+ statementBlock* END_PERFORM` (CobolControlFlow.g4:26) repeats performOptions, so `PERFORM UNTIL EXIT VARYING I FROM 1 BY 1 UNTIL C …`, `PERFORM VARYING … UNTIL C UNTIL EXIT …`, and even `PERFORM UNTIL EXIT UNTIL X > 3 …` all parse as two options. BindPerformControl (ControlFlowBinder.cs:157) takes `p.performOptions().FirstOrDefault()` and binds only it: the first example becomes PerformForever (VARYING and its condition silently dropped → infinite loop), the second binds plain VARYING (UNTIL EXIT dropped) — silently-wrong execution of an SR8-invalid statement, the exact class the review policy stages loud.
- **Proposed action:** In BindPerform/BindPerformControl, reject `performOptions().Length > 1` with a named diagnostic (a Format-1/2 PERFORM carries at most ONE of TIMES/UNTIL/VARYING; cite §14.9.28.2 + SR8 when one of them is UNTIL EXIT) instead of first-wins. Correct the DEVLOG-890/resume-prompt follow-on wording so the tracked SR8 residue covers both the nested-under case and this same-statement inline case until the check lands.
- **Disposition:** _raw batch-1 capture — verify + route_

#### F12. [MINOR | impl-vs-spec-drift | C4-waveC-drift] Boolean-shift count accepts any arithmetic expression — §8.8.2 rule 5's integer-operand requirement is unenforced and a fractional count silently truncates, an ungated leniency

- **Refs:** specs/ISO_COBOL.md:9366 (§8.8.2 rule 5: the second operand of a shift SHALL be an integer operand; Table 4 permits only identifier-or-literal after a shift operator); E:\CobolSharp\src\Cobol.Net.Frontend\Grammar\Core\CobolExpressions.g4:141-143; E:\CobolSharp\src\Cobol.Net.Compiler\Binding\Procedure\Verbs\ConditionBinder.cs:69-93; E:\CobolSharp\src\Cobol.Net.Compiler\CodeGen\Emit\BooleanRenderer.cs:48-52
- **Evidence:** Rule 5 is a formation (compile-time) rule: the shift's second operand shall be an INTEGER operand. The grammar deliberately supersets (`booleanShiftSuffix : (B_SHIFT_*) arithmeticExpression`, comment at CobolExpressions.g4:141-142) but BindBoolShift (ConditionBinder.cs:69-93) adds no integer check — `COMPUTE R = A B-SHIFT-L 1.5` (or a non-integer item/expression count) binds clean and the renderer truncates at scale 0 (BooleanRenderer.cs:51-52). The Wave-C scout (gotcha 10) prescribed a bind-time COBOLNET1511 formation error for a non-integer second operand; the leniency is documented only in the grammar comment and is not dialect-gated, violating the every-leniency-gated dialect rule. (The parenthesized/complex-expression superset beyond Table 4's identifier-or-literal is the same class.)
- **Proposed action:** In BindBoolShift, reject a statically non-integer count (a fractional numeric literal, or an expression whose bound scale/operands are non-integer) with the COBOLNET1511 formation-error family citing §8.8.2 rule 5 / Table 4; if the runtime-value truncation for non-literal counts is retained, record it as a documented implementor determination in docs/CONFORMANCE.md rather than a silent comment-only superset.
- **Disposition:** _raw batch-1 capture — verify + route_

## 9. Batch-2 verdicts (2026-07-19 — worklist items 1–5, 10 agents, 0 errors)

The owner-directed batched verification, batch 2 of N. Both lenses ran per finding; full verdict notes in
the batch-2 journal (session store) + `scratchpad/batch2-verdicts.json`.

### V1. PAGE reserved-word table data (r2023=false with false provenance)

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED — fix: drop PAGE from gen-reserved-words.ps1:111, regen both mirrors (r2023=true), 4-edition negative witness. NOTE the code-lens correction: NO live acceptance hole (PAGE is a dedicated lexer keyword → parse error as a user word at every edition); the defect is table/matrix DATA fidelity + every future table consumer (ReservedWordSet COBOL-WORDS layer, matrix invariants).

### V2. runtime-negative ref-mod length vs the -1 omitted sentinel

- **Verdicts:** spec-lens REAL (high) · code-lens refuted/dedupe (high)
- **Disposition:** CONFIRMED-AS-DUPLICATE — merge into confirmed C14 (same defect); NEW increment for the C14 fix: ExceptionState.cs:227 still cites "ISO §8.4.2.3 c" (correct: §8.4.3.3.4), missed by the citation batch; also apply the fix to SpliceInto AND WriteFill. Spec nuance: the raise obligation for a negative is GR5c ("positive nonzero integer") — the EC trigger list never says "negative"; a specified length may NOT be reinterpreted as omission (omission is SYNTACTIC).

### V3. EC-DATA-NOT-FINITE / EC-DATA-OVERFLOW catalogued-unraisable, untracked

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED — spec anchors: NOT-FINITE raise rule §14.6.13.2 item 3 (:24897, four exemptions: class condition, sign condition, same-standard-usage MOVE, VALIDATE); OVERFLOW = Fatal :24668. Route: raise seams at the float conversion/MOVE paths (checking-gated) or a NAMED residue row — currently in NO ledger.

### V4. the EC-RANGE family catalogued-unraisable, untracked (SEARCH-INDEX/SEARCH-NO-MATCH/PERFORM-VARYING/INVALID)

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED — the four names are EC-RANGE-SEARCH-INDEX (§14.9.35→serial SEARCH GR4 :30935), EC-RANGE-SEARCH-NO-MATCH (:30946/:30962), EC-RANGE-PERFORM-VARYING (§14.9.28 GR3 :29561 — non-POSITIVE, so zero too), EC-RANGE-INVALID; catalogued at ExceptionCatalog.cs:164-168 with no seam and no ledger row. Route: seams or a named residue row.

### V5. EXIT SECTION doc-vs-impl (BoundUnsupported while the docs claim the pc-move lowering is current)

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED — ControlFlowBinder.cs:102 binds BoundUnsupported("EXIT SECTION") while EXIT PARAGRAPH (:85) works; §14.9.14 Format 4 GR7 = control to an unnamed empty paragraph after the last paragraph of the section (the pc-move the docs describe). Route: implement (small — section EndPc exists) or fix the docs; the review-fix wave.

## 10. Batch-3 verdicts (2026-07-19 — worklist items 6/8/9/10/11, 10 agents, 0 errors)

Item 7 (RW SUPPRESS) was skipped as a duplicate of confirmed C5. Full verdict notes:
`scratchpad/batch3-verdicts.json` + the batch-3 journal.

### V6. DISPLAY … UPON silently dropped (SR2 unchecked, device ignored)

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED (both high). Route: review-fix wave — bind displayUpon mirroring BindAcceptFromMnemonic (0817-style reject; output set CONSOLE/SYSOUT/SYSERR is an IMPLEMENTOR choice to document, §12.3.7.3 delegates), carry device on BoundDisplay, SYSERR→stderr, golden. Corrections: the intrinsics design doc :188 already PROMISES the routing; legacy-side tests cover UPON CONSOLE/SYSOUT only; nit — AcceptDisplayEmitter.cs:17 cites §14.9.8, 2023 numbering is §14.9.11. Also: MnemonicRegistry does NO device validation at declaration.

### V8. CALL Format 2 (AS NESTED / prototype) + the OMITTED argument — zero grammar surface, SSOT reads as implemented

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED (both high). Route: decide implement-vs-stage-loud (owner-scope candidate for the grammar batch); in the deferral case reword COBOLNET_DESIGN.md:705 AND COBOLNET_INTERPROGRAM_DESIGN.md:56/:128 (the deep-dive example `CALL "P" USING OMITTED` is itself invalid ISO — OMITTED is Format-2-only; the optional bracket is the whole `{id|lit} AS` prefix). Correction: the deep-dive :149 DOES list OMITTED as 2002+; it is Format 2/AS NESTED/prototypes the inventory omits.

### V9. National-argument intrinsic results bound category ALPHANUMERIC (8-function family)

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED (both high). Route: review-fix wave — computed-category hook on the 8 catalog rows (CONVERT precedent); addition from the code lens: CONCAT rides the generic path (no BindConcat), and §15.18.1 also has an "All alphabetic → Alphabetic" row (a further lesser drift). Golden pinning Table-16 MOVE legality both directions.

### V10. LENGTH/BYTE-LENGTH residual legs untracked (PHYSICAL keyword unparsed; ref-mod/ODO-group error out)

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED (both high). Route: tracked residue row (P13 residue or P14 backlog) — PHYSICAL keyword acceptance, ref-mod/.Length + ODO/DEPENDING runtime legs, and the two error-message § corrections (r7/r6 → r4/r2).

### V11. COBOLNET1518 double-claimed: locale non-support vs method-REDEFINES scope error

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED (both high) — the THIRD live diag collision (after 1573 fixed, 1535 documented-deliberate). Fix: renumber the DataBinder method-REDEFINES emit (now at :2400) to 1577, catalog descriptor per the new drift-guard discipline, doc sweep; 1518 stays = A.4.9 locale non-support. NOTE: the compiler dot-prefixed Edition.Error channel is exactly where the drift guard does not yet scan — this collision validates extending it.

## 11. Batch-4 verdicts (2026-07-19 — worklist items 12/16/17/18/19, 10 agents, 0 errors)

ALL CONFIRMED high/high. The four OO items are MANDATORY surface (A.4.10's optional list = multiple
inheritance ×2 + parametric polymorphism ONLY) — a plan-scope decision, not a quiet residue: route to an
owner-visible OO-surface disposition at P13 close (implement vs staged-loud + P14 plan rows). Full notes:
`scratchpad/batch4-verdicts.json` + the batch-4 journal.

### V12. CONFORMANCE.md lacks the A.4.9 locale-facility row the code claims exists

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED (both high). Fix: add the fifth §4 facility row (scope per the code-lens corrections: the 1518 ERROR covers the 5 Unsupported functions + the four functions' LOCALE phrases — NUMVAL has no LOCALE phrase; SET LC_*/SPECIAL-NAMES LOCALE legs have NO diagnostic today, F3 is the companion code fix); four→five counts; the surviving (§2.4) refs at lines 34/36. Route: FIX NOW docs (next remediation slice).

### V16. Inline method invocation (§8.4.3.4 '::' identifier form) absent; the shipped rule is a non-ISO statement shape

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED (both high) — MANDATORY 2002+ (A.4.10 lists only multi-inheritance ×2 + parametric polymorphism as optional). The shipped inlineMethodInvocationStatement (dataReference(args) STATEMENT, {is2023()}?-gated) is NON-ISO: the ISO form is an IDENTIFIER `{class|id} :: literal [(args)]` usable in sending positions, never a statement; no :: token exists; the registry row is a dead gate claiming introduced-2023 (ISO: 2002). Route: OWNER-VISIBLE plan scope — the OO-surface batch (P13-close decision: implement vs staged-loud + plan row; the non-ISO statement rule needs a disposition either way).

### V17. Object-view (§8.4.3.5 identifier AS class/interface/UNIVERSAL) — zero surface, mandatory 2002+

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED (both high) — mandatory 2002+ identifier Format 5; zero surface. Same OO-surface batch routing as V16.

### V18. USAGE OBJECT REFERENCE [FACTORY OF] ACTIVE-CLASS + the ONLY phrase — unimplemented, mis-diagnosed

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED (both high) — §13.18.60.2: ACTIVE-CLASS mis-diagnoses as unknown-class; ONLY silently ignored where implemented-adjacent. Same OO-surface batch routing.

### V19. Parameterized classes/interfaces (CLASS-ID/INTERFACE-ID USING, REPOSITORY EXPANDS/AS) — mandatory 2023 surface, zero grammar

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED (both high) — mandatory 2023 surface (E.3-listed); zero grammar; the P3 registry row exists as a validator-catalogue entry only (a dead gate — PHASE-03 doc propagates the drift). Same OO-surface batch routing.

## 12. Batch-5 verdicts (2026-07-19 — worklist items 22/24/25/26/27, 10 agents, 0 errors)

Item 23 was verified merged into 22. Full notes: `scratchpad/batch5-verdicts.json` + the batch-5 journal.

### V22. CONFORMANCE.md has no Annex A.4 optional-element disposition section (§4.2.7 itemization for partial RW; A.4.8/A.4.13 over-claimed by the §1 blanket)

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED (both high). Nuance: §4.2.7 mandates itemizing PARTIAL support (RW — its itemization lives only in the internal design doc) and does not itself require listing unclaimed elements — but CONFORMANCE.md's own charter + §1 blanket convert every omission into an affirmative false claim (A.4.8 FORMAT/SELECT WHEN, A.4.13 REWRITE FILE/WRITE FILE — zero surface). Tracking correction: A.4.8/A.4.13 dispositions ARE planned (PHASE-14:112/403 + the G8 Cut-3 doc set) — the live doc is still non-compliant today. Route: add the A.4 disposition section (one row per A.4.2–A.4.14, incl. multi-inheritance) OR rescope §1 to positively enumerate claimed facilities + explicitly point to the P14 rows — FIX NOW docs slice.

### V24. OPTIONS INITIALIZE bound then silently dropped in codegen + its intro gate mis-dated 2014 (E.3.3 item 33 = 2023)

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED (both high). Two halves: (a) the initial-state fill is structurally unreachable (OptionsInitialize zero references outside the binder — user-silent capture-only, acknowledged only in a code comment); (b) constructs.json introducedIn:2014 is UNCITED drift — E.3.3 item 33 is a 2014→2023 delta (the repo's own VCR row 76 says 2014→2023), so --std 2014 wrongly accepts. Route: review-fix wave (implement the fill via the ValueInitializer channel or stage loud; flip the row to 2023 + negative witness; sweep OptionsBinder:22-24 + NUMERIC_DESIGN:103).

### V25. REPOSITORY FUNCTION ALL INTRINSIC user-word prohibition (§12.3.8 SR12/SR13; E.2 item 13) unenforced

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED (both high). Refinement: not wholly untracked — the code names it deferred (DataBinder.cs:254-255) and the 2023 seven-new-intrinsics half has partial coverage; the SR12/SR13 user-word PROHIBITION has no enforcement or row. Route: review-fix wave (small bind-time check at the REPOSITORY binder) or a named residue row.

### V26. FLAG-85 / FLAG-NATIVE-ARITHMETIC — E.2 item 21 removal silently swallowed at all editions

- **Verdicts:** spec-lens REAL (high) · code-lens refuted/tracked (high)
- **Disposition:** SPLIT → REAL-BUT-TRACKED. The code gap is real (KnownIgnoredDirectives swallows both at ALL editions; no ≥2023 rejection, no 2014 obsolete flag) but it is ALREADY routed: Wave D's list in the resume-prompt banner (this session's ②/③ update) names FLAG-85/FLAG-NATIVE-ARITHMETIC E.2-item-21 handling. Disposition: fold into Wave D (no new row needed); the finding's "orphaned" claim is superseded.

### V27. DB-series NIST golden establishment (audit VCR 7.17) — falls through at phase close

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED (both high). The audit row VCR 7.17's DB-series golden-establishment half was never picked up by any remaining worklist (the Wave F implementation landed; the NIST DB101A-DB105A goldens were not established). Route: add to the P13 phase-close checklist (or explicitly re-scope to P14 with a row — decide at Wave I).

## 13. Batch-6 verdicts (2026-07-19 — PARTIAL: the monthly spend limit killed 5 of 10 agents)

Completed: items 30 (both lenses) + 31 (both lenses) + 33 (spec lens only). KILLED by the limit: items 28
(new-2023 intrinsics matrix rows), 29 (BYTE-LENGTH staging rationale) — both lenses — and 33's code lens.
**RESUME STATE:** re-run items 28/29/33 in the next batch once the limit is raised; then the ~17 minors
(worklist 32/34-50) and the 12 batch-1 raw findings (§8 F1-F12) remain — the batch-of-10 cadence continues
from here. Full notes: `scratchpad/batch6-verdicts.json`.

### V30. RANDOM sequence state is process-wide static — the P8 "ONE RunUnit owner" claim is contradicted

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED — §15.75.3 rule 4 scopes the sequence to the run unit; `CobolIntrinsics.Float.cs:79`
  holds a process-wide `static Random` while the code's own comment cites the per-run-unit rule and `RunUnit.cs`
  contracts AsyncLocal concurrency. **Tracking CORRECTION (strengthens it):** this was plan §7 refinement **R3
  must-apply P8 work** (`_random` · `CobolSort.Files` · `ExternalSwitches.States` · `CobolTable.Scratch<T>.Slot`
  all to be homed on RunUnit) — P8 executed it PARTIALLY (switches homed; `_random` + `CobolSort.Files:36` +
  `Scratch<T>.Slot:34` still process-global) and closed claiming the ONE-owner criterion. Route: ONE remediation
  unit = the R3 siblings + the SURVEY G2 CI-grep broadening (catch `static Random` etc.), the
  ExceptionState/CobolFile shim pattern, emitted surface unchanged. Severity major (single-run-unit CLI hosts
  unaffected; multi/sequential run units get sequence bleed + broken §15.75.4 rule-2 determinism).

### V31. WriteFill drops AllowZeroLength — spurious fatal EC-BOUND-REF-MOD under >>REF-MOD-ZERO-LENGTH ON

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** CONFIRMED — §8.4.3.3.4 GR5c + §7.3.23.3 GR1: with the directive ON a zero-length result is
  allowed, and §14.9.25.4 GR1 sanctions a zero-length MOVE receiver ("leaves identifier-2 unchanged"). The Write
  RefModPlace arm threads `r.AllowZeroLength` (PlaceRenderer.cs:72-73) but the figurative-fill arm WriteFill
  (:153-154) omits it and `RuntimeApi.StrSpliceInto` defaults false ⇒ a figurative MOVE into a zero-length slice
  spuriously raises fatal EC-BOUND-REF-MOD when checking is ON. Fix: pass `allowZeroLength: p.AllowZeroLength`
  in WriteFill, mirroring the Write arm — **FIXED same-day (see the commit referencing this section)**.

### V33 (spec lens only — code lens killed). EC-FLOW-USE / EC-FLOW-GLOBAL-* seams

- **Disposition:** _half-verified — re-run the code lens with items 28/29 in the resume batch._

## 14. Batch-7 verdicts (2026-07-19 — the batch-6 casualties re-run: items 28/29/33, 6 agents, 0 errors)

### V28. New-2023 intrinsics constructs.json rows — SPLIT → NOT A GAP (already-tracked P14 ledger closure)

- **Verdicts:** spec-lens REAL-but-downgraded (high) · code-lens REFUTED (high)
- **Disposition:** the below-2023 gating is IMPLEMENTED (IntrinsicCatalog D8 window, COBOLNET1502) and
  FIXTURE-PROVEN for all eight items (IntrinsicFunctionDifferentialTests GatedBelow2023 suite); the absent
  constructs.json rows match the DOCUMENTED §1.1 exception-ledger doctrine (intrinsic windows live in their
  OWN tables, not constructs.json — smallest-algebraic-2023 is the P11 exception, not the norm). The open
  remainder is the already-routed P14 todo burn-down: resolve VCR rows 65-74 as gate-rows OR a DISPOSITION
  citing the catalog-window mechanism (rows 68/69 stay todo — EXCEPTION-FILE optional-arg is staged); reword
  audit row 98's action "none" → "VCR flip/disposition at P14"; optionally extend the differential fixtures
  from the single 2014 point to the 85/2002/2014 sweep.

### V29. CONSTANT … AS BYTE-LENGTH OF staged on a now-false rationale — CONFIRMED

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** BYTE-LENGTH §15.14 is a live compile-time Fold since P11, yet the CONSTANT leg
  (§13.10.4 GR5) stays staged loud with the user-facing message "the §15.14 intrinsic is itself a Deferred
  catalog row" in THREE places (DataBinder.Constants.cs:45-46/:189 emit + the ConstantByteLength descriptor
  DiagnosticCatalog.cs:143-148 → DIAGNOSTICS.md:24) — factually false today. GR5's ODO exception (MAXIMUM
  size) is honored by construction by a ByteWidth-based fold. Route: the Opus fix session — either activate
  the constant leg via the live fold (small) or at minimum truth-correct the three rationale sites; the
  resume-prompt:304 P10 note ("BYTE-LENGTH unblocks the staged CONSTANT AS BYTE-LENGTH OF") proves the plan
  expected the leg to land with P11.

### V33. EC-FLOW-USE / EC-FLOW-GLOBAL-EXIT/-GOBACK — CONFIRMED (with a headline correction)

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** the three raises are absent and untracked; STRONGER spec anchors than the finder cited:
  EC-FLOW-GLOBAL-GOBACK has GOBACK §14.9.18 GR6, EC-FLOW-USE has USE §14.9.49.4 GR2, and §14.6.13.1.3 item 6
  verbatim EXCLUDES EC-FLOW-GLOBAL-EXIT/-GOBACK from PROPAGATE. HEADLINE CORRECTION: "the legacy USE-recursion
  guard was not carried over" is WRONG — the greenfield emits per-declarative __useActive re-entrancy guards
  (DispatchEmitter.cs:156-193) incl. the GLOBAL walk; what is missing is only the RAISE side (the guard
  silently skips instead of raising EC-FLOW-USE where the spec sets the condition). Route: EC-seam batch with
  the EC-RANGE/EC-DATA twins (V3/V4) — seams or a named residue row.

## 15. Batch-8 verdicts (2026-07-19 — minors 32/35/37, 6 agents, 0 errors)

Also dispositioned without agents: item 34 (data-model EC-BOUND-OVERFLOW stale note) = ALREADY FIXED in
remediation pt3 (DEVLOG 911); item 44 (Scratch<T>.Slot process-global) = SUBSUMED by V30's R3 remediation unit.

### V32. >>CALL-CONVENTION / >>LEAP-SECOND (+ LISTING/PAGE) silently swallowed, undocumented — CONFIRMED

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** behavior is conformant-or-implementor-permissible in every leg EXCEPT the sharp edge: an
  undefined call-convention-name-1 is silently accepted with the default mapping (§7.3.9.3 GR2b makes the
  mapping implementor-defined — and COBOL.NET defines none, undocumented), and §7.3.17.3 SR1 (LEAP-SECOND
  placement) is unchecked. Citation fix: the LEAP-SECOND GR2/GR3 rules are §7.3.17.4 (not .3). The runtime
  LEAP-SECOND OFF-only choice is already implemented+commented (CobolDate.cs:163/:391) but undocumented.
  Route: Wave D (already named there) — CONFORMANCE.md rows for all four + the non-COBOL convention-name
  warning + optionally the SR1 placement check.

### V35. EXCEPTION-FILE arg form fully implemented but five doc sites still say "renders loud" — CONFIRMED

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** the implementation matches §15.28.4 r2 / §15.29.4 r2 exactly (verified to the r2a/r2b
  letter); stale sites = IntrinsicCatalog.cs:172/177-178 · EcFunctions.cs:61-62 · the constructs.json
  exception-file-n-2002 note (edit the JSON, not the generated .g4) · VCR rows 68/69 todo anchors (both
  constructs.json rows EXIST — flip to gate: anchors + regen gen-vcr.ps1). CORRECTION: the rows' "Affects
  existing? No" column is CORRECT — the stale signal is the anchor, not that column. Route: Opus fix session
  (doc/comment truth sweep + anchor flip).

### V37. CONFORMANCE.md row 45 parametric-polymorphism "Claimed" — SPLIT → REAL-doc-flip-only

- **Verdicts:** spec-lens REAL (high) · code-lens REFUTED-as-found/residual-tracked (high)
- **Disposition:** A.3 item 45 is CONDITIONAL ("When parametric polymorphism is provided…") and it is NOT
  provided (COBOLNET0822 rejects overload signatures) — so row 45's "Claimed" is wrong: flip to N/A/
  not-provided. ALSO: the new §5 A.4.10 row mis-lumps item 2 — INTERFACE multi-INHERITS IS supported
  (OoClassTable.cs:152-165); only CLASS multi-base INHERITS rejects 0849 — split the wording. Route: Opus fix
  session (two-line doc flip).

## 16. Batch-9 verdicts (2026-07-19 — minors 38/39/40, 6 agents, 0 errors)

### V38. REPOSITORY-declaration SRs for referenced class/interface names unenforced — CONFIRMED (worse than filed)

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** §11.3.3 SR2 + §11.8.3 SR1 + (under-cited) §13.18.60.3 SR8/SR9 + §12.3.8.1 all require the
  repository declaration; INHERITS/IMPLEMENTS/USAGE resolve group-wide with NO check, and the DataBinder
  repository loop does not even CAPTURE class/interface specifier names (only PROPERTY/FUNCTION) — no
  enforcement infrastructure exists. The OO slice brief PROMISED 0836 enforcement "once REPOSITORY INTERFACE
  parses" — that condition is NOW met without the enforcement landing. Cheapest fix (recorded for Opus):
  capture the names in the existing loop + check at the resolution sites, self-name carve-out per §12.3.8.3
  SR5/SR8; drop INVOKE from the enforcement list (covered via the USAGE rules). Or: a dialect-gated leniency
  descriptor pair + registry row.

### V39. Wrong-§ citations in shipped RW/EXTERNAL code — CONFIRMED, now SIX sites (one more found; two C18 rows already fixed)

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition (the exact fix list for Opus):** CobolReportWriter.g4:200 §14.9.19→§14.9.16 (GENERATE) ·
  :207 §14.9.62 (nonexistent)→§14.9.46 (TERMINATE) · DataBinder.Reports.cs:136 AND :418 AND :425
  §13.18.29→§13.18.28 (GROUP INDICATE; :418 is the NEW sixth site — "§13.18.29.4 GR1"→"§13.18.28.4 GR1") ·
  DiagnosticCatalog.cs:410 ExternalRecordNotCellBacked §13.18.24→§13.18.22 + regen DIAGNOSTICS.md (:27) ·
  CONFORMANCE.md CLOSE row (:57) 14.9.7→14.9.6 (the DISPLAY/OPEN rows are ALREADY fixed by 451cede8/885d90e4).

### V40. ALIGNED clause (§13.18.1) absent entirely — CONFIRMED

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** no lexer token, grammar rule, or binder model — only the reserved-word row (≥2002, so it
  cannot even parse as a user word; a conforming 2002+ program dies as a parse error); the Annex E.2 item 2
  2023 consistency delta has no landing surface; tracked nowhere. ALIGNED is bit/boolean-data-adjacent
  surface — route: a named staged-loud diagnostic + a plan row (the bit-data family owner), P14 candidate.

## 17. Batch-10 verdicts (2026-07-19 — minors 41/42/43, 6 agents, 0 errors)

### V41. Annex E.2 item 6 (compile-time arithmetic mode) — CONFIRMED, and STRONGER than filed

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** §7.3.6 GR2 + implementor-defined-items annex entry 29 make documenting the mode a REQUIRED
  conformance-documentation item (not a nicety). The constructs.json `arithmetic-intermediate-precision-2023`
  pending candidate probes RUNTIME arithmetic — mis-anchored for the compile-time mode; it does not discharge
  the duty. Route: land the CONFORMANCE.md implementor-defined note + the VCR row-12 disposition WITH the
  tracked Wave D §7.3.6/§7.3.7 evaluator work (the evaluator forces the mode determination). Nuance: 2014
  mandated Standard Arithmetic for compile-time expressions — keep row 12 a disposition anchor, not ref-only.

### V42. VCR todo-anchor staleness — SPLIT → the P14 home EXISTS; the residue is stale P13-scope phrasing

- **Verdicts:** spec-lens REAL (high, as doc-doctrine inconsistency) · code-lens REFUTED-as-filed (high)
- **Disposition:** the factual core holds (~20 landed constructs still carry todo anchors; the generated index
  lists only the 15 pre-P13 rows; VcrDriftTests validates only gate: anchors — the todo-vs-catalogue mismatch
  is CI-invisible), BUT the P14 burn-down has ALWAYS owned the flips (PHASE-14 Step 1 "Drive the VCR to
  zero-TODO" + exit criterion 1 + roadmap :311) — "the deferral exists only in DEVLOG 895" is false. Residue:
  (a) the audit preamble + resume-prompt HOW-TO-RESUME item 2 still phrase the flips as pending P13 scope —
  update both to point at P14 Step 1; (b) extend VcrDriftTests to FAIL when a todo anchor's construct exists
  in constructs.json (closes the blind spot). Both cheap; Opus session.

### V43. Audit bookkeeping errors — CONFIRMED (VCR-78 double-count; the nonexistent I-O-STATUS-04 "directive")

- **Verdicts:** spec-lens REAL (high) · code-lens REAL (high)
- **Disposition:** (a) audit lines 88/89 count one VCR-78 item twice in the 31/21/18/1 split; (b) audit :37
  prescribes a standalone I-O-STATUS-04 directive that does not exist in §7.3 (I-O-STATUS-04 is a FLAG-14
  option word only) — the landed VCR-21 work correctly ignored the phantom prescription. Route: annotate the
  audit rows (a LEDGER doc — annotate, don't rewrite history) at the phase-close sweep.
