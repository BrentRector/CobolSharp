<!--
  THE ONE HAND-WRITTEN SECTION OF THE CONFORMANCE LEDGER.

  `scripts/spec/gen_ledger.py` inserts this file VERBATIM as the body of the artifact's
  "In flight right now" section (inside <section aria-label="In flight">, after its <h2>), so it is an HTML
  fragment in a .md file rather than markdown — verbatim means verbatim, and a renderer standing between the
  author and the page would be a second author of the only part a person writes.

  ⛔ NO COUNT THAT THE GENERATOR CAN MEASURE BELONGS HERE. Row totals, verdict histograms, register standing,
  A.1 coverage, gate results and the corpus are all computed from the tree; writing one of them by hand here is
  how this page would start lying again. What belongs here is NARRATIVE: which lanes are running, what is
  queued behind the lander, which owner questions are open. Numbers are allowed only when they describe work
  that has NOT landed yet — a queued landing's row count exists nowhere the generator can read.

  ⛔ AND IT IS NOT A WORK LIST (CLAUDE.md rule 8). The register is `kb/Work/`. Anything here that starts to
  look like a checklist of remaining items is a note that should have been filed instead.

  Classes available: .flight (the accent panel), .cardgrid + .card, .pill.good/.warn/.crit, .mono, .num, .dim.
-->
  <div class="flight">
    <h3>Twenty-two landing trains and a fourth golden round since the pause; the CONFORMS-but-untested population is down to the four rows that wait on the owner; main now refuses any commit without a green run, train 22 landed the owner's two decided features through that gate, and the campaign is paused on the owner's instruction</h3>
    <p><strong>What landed since the fourth pause.</strong> Four landing trains, each a fast-forward with one commit per cluster and one gate over the union of their filters. Train 1 (four clusters) and train 2 (six) carried the NIST guard's evidence rule, a variable-length group across CALL and INVOKE, the Report Writer CONTROL operand and the VALUE clause's one screen for every subject. Train 3 (five) carried the USAGE BIT leaf's carriage gate, level-number validation, EC-DATA-INCOMPATIBLE wired for every statement class with its exemption table as a structure, and the not-implemented carrier's three jobs split into three. Train 4 (four) carried the CALL statement whole: its exception partition (a CALL with only NOT ON EXCEPTION no longer swallows a failed activation), its program-prototype registry, a Format-2 argument classified once on its own carrier, and the VALUE size sentences that stopped binding a condition-name. Train 5 (four) carried the integer-argument screen made one primitive, <span class="mono">&gt;&gt;COBOL-WORDS</span> reaching every §8.9/§8.10 word through one resolution rule, NUMVAL on the SDIDI carrier in every arithmetic mode, and the exact carrier no longer wrapping in SUM, RANGE or MEAN. Between the trains the lane-3 registrar filed batch 3 (the data-description clauses of §13.18), golden-lane round 3 witnessed batch 2's resolved-but-untested rows with fifty-nine spec-derived programs, and round 4 witnessed batch 3's with nineteen more — after which exactly four CONFORMS rows in the whole inventory lack a test, and each of them waits on an owner decision rather than on a writer.</p>
    <p><strong>The instruments.</strong> Batteries #44, #45 and #46 were each attributed to the row: #44's two differential flips were one pre-existing hole (no level-number validation), fixed as <strong>PB485</strong> in train 3; #45's five were that pair, two rejections the standard licenses, and one over-rejection the VALUE screen completed at level 88 (<strong>PB598</strong>, fixed in train 4); #46 is all green on every compiler leg and names exactly that PB598 flip; #47, at train 4's head, prints ALL GREEN with the differential at zero after one spec-licensed rebaseline — the first battery since #43 with nothing to attribute; #48, at train 5's head, is green on the compiler with the differential still at zero, and its one NIST regression is a false DIFF the guard scored under contention on a print file identical to the golden (<strong>PB642</strong>, the third face of the guard's evidence rules). #49, at golden round 4's head, is all green with IF137A matching again; #50, at train 6's head, is green on the compiler and its one red was a phantom clause number in a register note, fixed; #51, at train 7's head, is all green after a restart on a corrupt build artifact in the battery worktree; #52, at train 8's head, is green on the compiler and its one differential flip is legal source that <strong>PB303</strong>'s <span class="mono">AS literal</span> phrase now accepts, rebaselined and attributed; #53, at train 9's head, is all green with nothing to attribute — the first battery since the OPEN registry was rewritten under Table 19, so the arbitration now runs over every OPEN in the corpus and no golden moved; #54, at train 10's head, is all green as well — the first battery over a tree where every OPEN probes presence with three answers and every keyed WRITE branches on the access mode alone, and nothing in the NIST file programs or the differential's file cases moved. #55, at train 11's head, is all green too — the first battery over a tree where every file record is its bytes, and nothing in the NIST file programs or the differential's file cases moved. #56, at train 12's head, is all green as well — the fifth in a row with nothing to attribute, and the first over a tree where every OPEN and sequential WRITE entry takes the executing element's operands with no defaults. Battery #57 runs at train 13's head once it lands. <strong>The trend line steps UP at the denominator commit, and that is the instrument being corrected rather than the compiler regressing:</strong> the rule catalog closed a clause's rule block at any markdown heading while the transcription rendered nine of the standard's in-clause group labels as headings, so five clauses were short by sixty normative rules — the WRITE statement worst, at sixteen of its forty-two general rules — and every GAP figure before this point was measured against a denominator that flattered the project by that much.</p>
    <ol>
      <li><strong>Fix lane, at its cap</strong> — train 6 landed: <strong>PB253</strong> (the arithmetic mode decides a prose-float function's container, asked first), <strong>PB254</strong> (a total function taken out of a partial function's landing), <strong>PB288</strong> (the CALL argument crossing lands with the standard's COMPUTE semantics, not a raise) and <strong>PB292</strong> (RECORD DELIMITER declined by name on both arms). Train 7 landed five: <strong>PB231</strong>'s NATIONAL third (a BASED record with a national leaf carried on the byte window), <strong>PB623</strong> (one exact float-to-fixed landing for every channel), <strong>PB297</strong> (a figurative constant sized by the operand it is compared with), <strong>PB639</strong> (the inline arithmetic store's wrap, and a checked arm that measured the wrap) and <strong>PB635</strong> (the float lane entered by the whole argument run). Train 8 landed five more: <strong>PB647</strong> (the float landing's scale and mode as one decision), <strong>PB300</strong> (PADDING CHARACTER gated to its edition window, the reservation list under it generated), <strong>PB305</strong> (the cross-argument class rule projecting through Table 2 before merging — ten rows that already read CONFORMS had refused legal source), <strong>PB303</strong> (the <span class="mono">AS literal</span> phrase on all five identification paragraphs, every unit carrying two names) and <strong>PB316</strong> (the OPEN statement's repeated group as the bound tree's shape). Train 9 landed four: <strong>PB231</strong>'s pointer third (the note closes after three thirds, and the byte-window gate closes with it), <strong>PB319</strong> (one site for OPEN's SR8, with its whole antecedent), <strong>PB317</strong> (OPEN WITH NO REWIND reaching the runtime and reporting '07' as its CLOSE twin always did) and <strong>PB321</strong> (Table 19 as a structure that arbitrates every OPEN — the note's own fix shape overturned by the printed table). Train 10 landed four: <strong>PB323</strong> (OPEN's presence probe with the standard's three states — absent, present, refused — asked once per OPEN and answered in one place for all three organizations), <strong>PB324</strong> (ASSIGN USING data-name bound at last, the connector-to-file association made at every OPEN, SORT and MERGE through one entry point), <strong>PB325</strong> (the keyed WRITE branch selecting on the access mode alone, and Table 20's first whole-table walker) and <strong>PB326</strong> (the report engine told the connector's open mode and the BEFORE REPORTING range, with four catalogued conditions given raise sites). Train 11 landed four: <strong>PB327</strong> (the record image becomes the record's bytes, so a national leaf rides a file record — and the group-NATIONAL width determination taken with it), <strong>PB331</strong> (the READ statement's two printed lock brackets, measured on the rendered page and split all the way down to the runtime), <strong>PB336</strong> (the READ preconditions once in the connector base, in the standard's own order) and <strong>PB347</strong> (the record-name operand meaning the record itself, once, for WRITE, REWRITE and RELEASE). Train 12 landed four: <strong>PB340</strong> (ADVANCING ON LOCK's skip-scan as one governed Format-1 read for all three organizations, with PB335's SR9 half), <strong>PB328</strong> (OPEN's write-capability question asked once above the organizations, so a read-only file answers '37' everywhere), <strong>PB673</strong> (the ASSIGN USING source and the LINAGE operands as arguments of the statement that reads them, the connector-side closure deleted) and <strong>PB329</strong> (the line-sequential character set determined as a required Annex A.1 item, '09' on READ and the WRITE '71' arm that never existed). Train 13 is on main (GAP 2853 → 2820): <strong>PB334</strong> (the sequential READ arm's three unread phrases, the read direction one fact on both bound nodes), <strong>PB345</strong> (a record-less file description entry given its implied record at bind), <strong>PB332</strong> (the grammar's optional-word audit, which had been a silently green gate over 596 rules), <strong>PB352+PB354</strong> (START on a sequential file, and one key lookup answering two rules), <strong>PB341+PB342</strong> (the release ordinal is a vector and the file position indicator a key value) and <strong>PB364</strong> (four USE duplicate screens that were one sentence, all four over-rejecting) — six cluster commits, landed by a second, continuation lander after the afternoon's session limit killed every live agent with all six rows already committed in the first lander's worktree; nothing was re-applied, the checkpoint was merged, and the full Unit assembly caught a drift red the train's own filter could not have seen. Trains 14 through 19 are on main. Train 18 carried the condition-name subject's class read once for a group too (<strong>PB728</strong>+<strong>PB575</strong>), ORDER given its own token (<strong>PB704</strong>), a conflict marker as a red unit test (<strong>PB735</strong>), the ISO §9.1.15 file lock DERIVED from the sharing rule (<strong>PB740</strong>, its three lock rows PARTIAL because RELATIVE and INDEXED files hold no lock against another run unit — <strong>PB771</strong>, open) and the national THROUGH stage discharged (<strong>PB761</strong>). Train 19 landed five: two more file-control key rules and the key category arm (<strong>PB742</strong>+<strong>PB743</strong>), the screen module's tokenized words in the name slot with the surface derived from the facility's own report types (<strong>PB301</strong>), the filter-population guard at four more sites (<strong>PB751</strong>+<strong>PB752</strong>), the guard's NIST leg finally driving the shipped compiler after every battery record through #58 had measured the oracle (<strong>PB750</strong>), and the ALPHABET literal-phrase validator its national twin always had (<strong>PB770</strong>). The sixth cluster, <strong>PB732</strong>, was DROPPED by the lander: its VALUE-operand screen turned a silent grammar swallow — a report group entry's <code>VALUE "…" COLUMN 20</code> parses COLUMN as a VALUE operand — into a rejection of legal NIST source (RW104A), which no train may carry; the over-greedy report-section clause loop is the root defect and gets its own note, and PB732 re-lands behind it with the version-matrix and NIST legs in its gate. Batteries #60 and #61 were green on every compiler leg; #61's two differential flips are GnuCOBOL's LINAGE samples now compiling, a group record's figurative condition-name VALUE that used to break the generated C#, fixed by PB728+PB575 and re-baselined by hand. The owner stopped the whole fleet once this morning; every agent's work survived as a checkpoint on its own branch, a fresh lander finished train 18 from the killed lander's branch, and six finishers closed their mechanisms from the checkpoints in under an hour each. Train 20 then landed all five of its clusters: the compiler-directive facility's one edition gate, four times the filed size, with COBOLNET0875 retired (<strong>PB725</strong>); WRITE's print-control choice indicators transcribed as repetition, and the two-amount path deleted (<strong>PB712</strong>); the read side of the release rule, whose probes the finisher found half-blind and repaired (<strong>PB753</strong>); five shape-keyed edition gates moved to recognition with the arm assignment now a CI fact (<strong>PB353</strong>); and every INTO/FROM implicit MOVE bound where the explicit one is, which closed the RELEASE … FROM run-time abort (<strong>PB348</strong>) — seven rows, fourteen merges resolved by hand against the spec, and the generators re-run to a byte-identical tree. Three more registrars then filed fifteen notes: cite.py's two matching defects (<strong>PB779</strong>), RESERVE with no carrier at all (<strong>PB781</strong>), the gitignored checkpoint file (<strong>PB783</strong>), the shared scratchpad collision (<strong>PB784</strong>), the figure-note-versus-grammar check (<strong>PB786</strong>), the construct rows whose printed citation describes a gate that moved (<strong>PB787</strong>), three ALPHABET and CURRENCY SIGN residues (<strong>PB789</strong>–<strong>PB791</strong>), and <strong>PB792</strong>, which corrected the account of the PB732 drop: the swallow of COLUMN into a VALUE operand list happens only under <code>--permissive</code>, through the blanket bypass that admits any reserved word as a user word in migration mode, and the printed report-group VALUE clause takes one or more literals, so the grammar must not be narrowed. Battery #62 at train 20's head is green on every compiler leg, and its guard NIST line measures the shipped compiler for the first time (364 MATCH, the eleven programs the old leg skipped as legacy-divergent among them); of its twelve differential flips eleven are the two trains doing what their notes say — PB770 closing the ALPHABET over-acceptance and refusing EBCDIC as a code-name (the PB793 question), PB742 rejecting numeric record keys that GnuCOBOL accepts as its extension — and one is a regression: PB725's directive gate consumes a malformed <code>&gt;&gt;SOURCE FORMAT</code> operand where the parser used to choke on it (<strong>PB794</strong>, filed and being fixed alone). Then the owner said the one thing no local gate had: the CI was broken. Measured from the GitHub history, main's "Build and Test" had been red on every run since train 10 the day before — sixteen consecutive failures over twenty-nine hours behind ten landings that all reported green — because the battery and every lander gate run on one Windows machine and no landing step ever read the run it had just triggered. Two tests were asserting host capabilities they had never measured: a file-ACL deny helper whose directory-scoped denial never denied the child on any Windows host and had worked locally only through an inheritance side effect, and a file-lock posture that Linux's advisory locks cannot express. <strong>PB795</strong> replaced both with a measured host-capability probe, with the Linux limit written into the runtime design as a determination, and <strong>PB796</strong> put the post-push CI check into the lander briefs, the workstream and gate skills, plan §9 and the session probe. The next run on main was green on every job, and so was train 21's: <strong>PB792</strong> keyed the permissive-mode reserved-word bypass on each word's reservation history through the one edition-severity policy and found, by its own drift test, thirteen hyphenated words whose gate had never fired because the generator emitted token names where the lookup expects spellings; <strong>PB732</strong> re-landed unchanged in mechanism behind it, its three dropped legs passing by name; and <strong>PB794</strong> closed battery #62's regression four times wider than filed — the directive recognition point had an edition column and no operand column, eight hand-rolled line slicings none of which knew the inline comment, and five conforming directives rejected for carrying one — as one line parse, one catalog column and one producer, nine §7.3 rows to CONFORMS. Battery #63 at that head is ALL GREEN with zero differential flips — the SOURCE FORMAT row agrees again through PB794, the guard's NIST leg reads 364 matches against the shipped compiler, and the same head's CI run is green on every job. The owner then answered three questions in one message. Question 23: main carries a required <code>ci-gate</code> status check with administrators included — every push here uses the owner's credentials, so an exemption would bind nobody — and docs-only pushes skip the matrix inside the run rather than skipping the run; <code>scripts/push-main.sh</code> is now the only way a commit reaches main (push a landing branch, watch the run, fast-forward on green), measured both ways: a docs-only landing in eighteen seconds, a direct unchecked push refused by the server. Question 21: the phase-13 evidence JSON is frozen, its refuted claim carries a machine-readable superseded-by marker, and a third audit beside the two citation audits fails on any evidence entry citing a grammar rule or path that no longer exists without one (<strong>PB785</strong>). Question 22: <code>ASCII</code> and <code>EBCDIC</code> are supported as ALPHABET code-names following GnuCOBOL's latitude — ISO 646 and IBM CCSID 37, the table derived from .NET's own code page — reaching PROGRAM COLLATING SEQUENCE, SORT, the SYMBOLIC and CLASS ordinals, and for the first time CODE-SET translation at the medium boundary (<strong>PB793</strong>, four rows, Annex A.1 item 183 live). Train 22 landed both through the new gate with both runs green; the two GnuCOBOL programs that had disagreed over EBCDIC agree again. Battery #64 at train 22's head is ALL GREEN with zero differential flips: the two re-baselined EBCDIC rows hold, the evidence audit runs beside the citation audits for the first time, and the tree under test is the first to have reached main through the required check. <strong>The campaign is paused on the owner's instruction.</strong> Three registrars filed twenty-one notes today and refused four ids to existing owners; the owner card carries twenty-two questions, the newest two being whether the phase-13 evidence JSON is frozen evidence or a current doc (it still asserts the refuted WRITE grammar as fact), and whether ASCII and EBCDIC should be accepted as implementor code-names, which the latitude protocol answers by following GnuCOBOL. Every implementer this night has found its note's mechanism wrong in some respect and reported it; sixty-four of those findings are now notes of their own — among them PB689, which says the burn-down's own denominator is short by at least the twenty-six WRITE rules an interior heading hid from the rule catalog.</li>
      <li><strong>Adjudication lane (lane 3)</strong> — batch 3 registered; batch 4 is next, with the registrar's reference-shape repairs folded into the merge script first.</li>
      <li><strong>Golden lane</strong> — round 4 landed: nineteen spec-derived goldens, every refuter correction applied and every expected value re-run on the lander's own build; the round found no compiler defect and one draft error (a reserved word used as a data-name, caught by the compiler). The four rows it left are the whole remaining CONFORMS-but-untested population, listed under the owner's decisions.</li>
      <li>Behind: the PB55 absorption campaign with its step-0 harvest precondition.</li>
    </ol>
  </div>
  <div class="cardgrid">
    <div class="card">
      <h3>Owner decisions open <span class="pill warn">20</span></h3>
      <p><strong>Answered 2026-09-04:</strong> witness-first adjudication — <em>no</em>, the lanes stay separate; the 160/220 turn caps — <em>yes</em>; five-cluster landing trains — <em>yes</em>. <strong>Answered 2026-09-06:</strong> the evidence JSON is frozen with a superseded-by marker (PB785); ASCII and EBCDIC are supported code-names following GnuCOBOL (PB793); main requires the <code>ci-gate</code> check and docs-only pushes skip the matrix (PB796). <strong>Answered 2026-09-05:</strong> the implementer cap — <em>raised from three to six</em>, on the measurement that the three-slot lane ran at full utilization all morning while the lander sat idle half the time; the slots are now filled one item per subsystem rather than down the rank list.</p>
      <ul>
        <li><strong>Five from the process review</strong>, held in PB468 and asked one at a time as each becomes relevant: table-driven tests closing many rows on one drift test; effort tiers; the already-OK "shall" rows with positive-only evidence; whether Report Writer stays claimed; extending PB386's derivation arm class by class — PB205's §13.18.16.4 GR6 row and round 3's two Annex A.2 rows wait on exactly that.</li>
        <li><strong>Two from the batch-3 registrar</strong> (PB579): whether the decimal-float rows convert to documented non-support on the witness already on disk, and whether a documented-non-support row stands while the decline behind it has a measured hole (VALIDATE's Format-5 <span class="mono">VALUE literal VALID</span> reaches the backend as a raw error).</li>
        <li><strong>One from the golden lane</strong> (PB592): A.1 item 206's "exactly the minimum range" determination contradicts §13.18.60.4 GR12's own asymmetric table and the compiler, which stores −128.</li>
        <li><strong>One from train 3</strong> (PB235): CLOSE's §14.9.6.4 GR1 row was closed on the same unpopulatable-antecedent derivation the owner signed for the READ rows — a class-by-class extension of PB386's arm, made before the standing answer on that class.</li>
        <li><strong>One from train 5</strong> (PB248): the strict verdict for a float operand at an integer argument position changed from ACCEPT to REJECT, on §15.3's own definition of an integer argument; a landed golden that pinned the old verdict is retired and a second is narrowed.</li>
        <li><strong>One from train 6</strong> (PB292): should COBOL.NET ever <em>provide</em> the RECORD DELIMITER facility — the STANDARD-1 arm's ISO/IEC 1001:2012 tape framing, a standard not in the repository, or a feature-name of its own under A.1 item 150 — rather than keep a documented decline that has no owner decision behind it? The §4.2.6 warning is required under either answer and has landed.</li>
        <li><strong>The four rows golden round 4 could not close</strong> — two Annex A.2 derivation rows (INITIALIZE GR9, overlapping PERFORM GR2) drafted as §8 rows awaiting the owner's signature in the round's scratch, and two §13.18.52 SIGN rows that need A.1 items 177 and 178, which the conformance document does not yet carry.</li>
        <li><strong>One from train 7, now decided by the code in train 11</strong> (PB546, taken by PB327 because the record codec could not land without it): a group containing a NATIONAL member now presents its national characters as their bytes — two positions each — in every character-position channel, DISPLAY, group MOVE, reference modification and the FD/SD record area, as §13.18.29.4 GR2's own note predicts; the conformance document's "one sanctioned width divergence" is gone. The question if the owner wants it reversed: should DISPLAY of such a group show the characters instead of the bytes? One arm and two goldens.</li>
        <li><strong>One owner-visible change from train 7</strong> (PB623): <span class="mono">MOVE &lt;COMP-2 8.2&gt; TO PIC 9V9</span> now stores 8.1, because the float-to-fixed transfer is the binary64's exact value at every magnitude (§14.6.8.2 r1, no latitude) and rule 4 truncates; <span class="mono">ROUNDED</span> still gives 8.2. The question if the owner wants it reversed: should a FLOAT-LONG sending item convert by its shortest round-trip decimal instead, accepting that a MOVE and a COMPUTE of the same item then disagree?</li>
        <li><strong>One from train 9</strong> (PB317): sign the drafted §8 derivation row for §14.9.27.4 GR12 — GR11 and GR12 partition the storage media between them, and the non-unit placement that makes every '07' this compiler reports correct empties GR12 on both arms, so the row can close only on the owner's signature. A determination the implementer recorded is also for the owner's eye: an absent OPTIONAL input file opened WITH NO REWIND reports '07' (GR11's assignment) rather than '05'.</li>
        <li><strong>One more from train 9</strong> (PB670, PB322's family): now that Table 19 arbitrates every OPEN, two clause-less connectors opening one file in conflicting modes answer '61' at COBOL-85 too, where the file-sharing statuses do not exist — permit the open as 1985's silence allows, refuse it with the operating system's '30' as before, or keep the deterministic '61'? The runtime has no edition, so any answer but the last needs a new seam.</li>
        <li><strong>One from train 12's sweep</strong> (PB690): every sequential reader and writer is built on Latin-1, so a character above U+00FF is written as a question mark with no status — and the fix needs a decision no implementer should make alone: for a sequential file with no CODE-SET clause, what is its coded character set? UTF-8 (lossless, variable bytes per character), Latin-1 refusing an unrepresentable character with a record-content status (one byte per character, never corrupts silently), or UTF-16 (the program's own repertoire, no interchange)? The surveyed model implementation is byte-transparent.</li>
        <li><strong>One from train 13's sweep</strong> (PB701): the indexed store now keeps one release order per key of reference, but the physical file carries one sequence of record images, so a mutually cyclic pair of per-key duplicate orders — a REWRITE moving a record into another's duplicate set under one key while both stay duplicates under another — cannot survive a CLOSE. Should the on-disk format carry the per-key ordinals (a format change with a migration for every existing indexed file), or does the acyclic-only guarantee stand as a documented determination?</li>
        <li><strong>One from the registrar's transcription check</strong> (PB738): a content-stream scan of all 1,261 pages found exactly five places where the published standard itself drops a character ("The valu62'" for "The value '62'"); the transcription reproduces the printed defect faithfully, so <code>cite.py</code> and the rule catalog work on the garbled text. Keep it faithful, or add an editorial-note convention for recovered readings?</li>
        <li><strong>One from the runtime's sharing posture</strong> (PB740): two clause-less connectors on one ASSIGN that the standard's own arbiter permits to coexist (OPEN INPUT, then OPEN EXTEND) are refused by the operating-system handle with status 30 and the appended record is never written, while a connector declared SHARING WITH NO OTHER admits an external writer. The in-run-unit half is being fixed; the question left: should a clause-less connector keep its exclusive operating-system posture against processes outside the run unit, or take the arbiter's answer only?</li>
        <li><strong>Still open, and not yet a bare question:</strong> the introducing edition of <span class="mono">USAGE POINTER TO type-name</span> — and now, from train 10, the introducing edition of the ASSIGN clause's <span class="mono">USING data-name</span> phrase (PB674), which the compiler accepts at every edition because nothing in the tree can derive an 85↔2002 boundary. Both are one question already on file as R28: is the ISO/IEC 1989:2002 text available to the project? The per-edition mission cannot be finished without it.</li>
      </ul>
    </div>
    <div class="card">
      <h3>Open builds</h3>
      <ul>
        <li><strong>The denominator's unnumbered and multi-sentence obligations</strong> — PB479 and PB581: "shall" sentences the standard did not number have no rows, and a numbered rule with several sentences can close on one; measure both classes before deciding the row shape</li>
        <li><strong>The citation audits' three blind spots</strong> — PB591, PB597 and PB616: a prose rule reference, a wrong rule number inside a correct clause, and a clause one level short of its sub-clause, all invisible to audits that validate quoted fragments only</li>
        <li><strong>Report Writer placement</strong> — PB484: the first body group prints one line too low, and a golden pins the wrong line</li>
        <li><strong>Named diagnostics for three measured modules</strong> — PB281 (FORMAT / SELECT WHEN), PB282 (REWRITE / WRITE FILE), PB283 (VALIDATE), each a generic parse error today where the posture is <em>declined</em></li>
      </ul>
    </div>
  </div>
