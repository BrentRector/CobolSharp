# CobolSharp — Spec-Gap Inventory (live COBOL-85 features vs NIST coverage)

Generated from the Wave-1 audit. Per live module, COBOL-85 spec features and whether a *baselined* NIST
test exercises each. **untested/partial features are the WS-SPEC authoring scope.** `passing` rows are
elided to keep this focused on the gap; counts are summarized per module.

## Nucleus

Coverage: **47 passing**, 5 partial, 1 untested, 2 unknown.

| Feature | Spec | Coverage | Covering test | Proposed extra test |
|---|---|---|---|---|
| INSPECT combined TALLYING ... REPLACING (Format 3) | ISO_COBOL.md §14.9.22 (Format 3) | partial | NC216A claims 'all four formats' but separate TALLYING and REPLACING dominate; combined TALLYING+REPLACING in one statement not clearly isolated/verified | tests/nist/extra/nc/inspect_tally_and_replace.cob — single INSPECT with both TALLYING FOR ALL "A" and REPLACING ALL "A" BY "X" on the same operand; verify the count register equals the number of A's AND the receiver shows the replacement applied (per §14.9.22 GR ordering: tallying evaluated before replacing-by-position) |
| STRING with multiple sources / mixed DELIMITED clauses in one statement | ISO_COBOL.md §14.9.43 | partial | NC217A exercises single-source-per-statement plus POINTER/OVERFLOW; multi-source concatenation with differing delimiters per source is lightly covered | tests/nist/extra/nc/string_multi_source_delims.cob — STRING A DELIMITED BY "-" B DELIMITED BY SIZE C DELIMITED BY "X" INTO RESULT WITH POINTER P; verify RESULT byte content and final POINTER value match the spec's left-to-right transfer rules |
| SET mnemonic-name (external switch) TO ON / OFF | ISO_COBOL.md §14.9.39 (SET ... TO ON/OFF STATUS) | partial | NC254A baselines switch SETTINGS (UPSI/Level-2 features); explicit SET sw TO ON/OFF appears in only 1-2 NC programs and is not strongly verified on output | tests/nist/extra/nc/set_switch_on_off.cob — with an UPSI/external switch mapped via SPECIAL-NAMES, SET SW TO ON, test its 88 cond-name = true, SET SW TO OFF, retest = false; verify both branch outcomes print correctly |
| INITIALIZE ... REPLACING with FILLER/VALUE clause interaction (and 2023 default-byte options) | ISO_COBOL.md §14.9.20 (FILLER / WITH FILLER / TO VALUE) | partial | NC223A covers REPLACING-by-category but not the WITH FILLER or THEN TO DEFAULT/VALUE refinements | tests/nist/extra/nc/initialize_filler_value.cob — group with FILLER bytes and items having VALUE clauses; INITIALIZE ... WITH FILLER ... THEN TO DEFAULT (or REPLACING) and verify FILLER bytes are touched/untouched and VALUE-clause items reset per §14.9.20 GR |
| Reference modification on subscripted/indexed table items and inside arithmetic expressions | ISO_COBOL.md §8.4.2 (ref-mod of table elements) | partial | NC224A focuses on scalar ref-mod; ref-mod of an OCCURS element with a subscript, e.g. T(I)(2:3), is not clearly isolated | tests/nist/extra/nc/refmod_subscripted.cob — OCCURS table; MOVE/compare TBL(IDX)(2:3) and use a computed (start:len) where both come from data items; verify extracted substring and a write-back via ref-mod receiver |
| ACCEPT FROM DATE / DAY / TIME / DAY-OF-WEEK (temporal Format) | ISO_COBOL.md §14.9.1 ACCEPT (temporal); §D.31.2 Temporal format ACCEPT | untested |  | Only NC214M (UNBASELINED — dropped as non-deterministic) and NC401M (M-flag, 3-line empty CCVS report) exercise ACCEPT FROM DATE/DAY/TIME/DAY-OF-WEEK; no passing baseline. tests/nist/extra/nc/accept_temporal.cob — ACCEPT into 6-digit/5-digit/8-digit/1-digit numeric items FROM DATE/DAY/TIME/DAY-OF-WEEK; verify each receiver is all-numeric and within range (DATE yymmdd: mm 01-12 dd 01-31; DAY-OF-WEEK 1-7; TIME hhmmsscc bounds) rather than fixed value, sidestepping NC214M's non-determinism |
| DISPLAY WITH NO ADVANCING | ISO_COBOL.md §14.9.11 (WITH NO ADVANCING phrase) | unknown |  | NC DISPLAYs use UPON device with implicit advancing; NO ADVANCING not located in any baselined NC. tests/nist/extra/nc/display_no_advancing.cob — DISPLAY "AB" WITH NO ADVANCING then DISPLAY "CD"; verify the two outputs land on the SAME physical line ("ABCD") per §14.9.11 |
| Figurative constant SYMBOLIC CHARACTERS / NULL | ISO_COBOL.md §8.3.3.6 (symbolic-character / NULL figurative) | unknown |  | No NC baseline located for SYMBOLIC CHARACTERS figurative or NULL. tests/nist/extra/nc/symbolic_char_figurative.cob — SPECIAL-NAMES SYMBOLIC CHARACTERS sym IS n; MOVE sym TO X and verify X holds the byte with ordinal n in the program collating/native sequence |

## Sequential/Relative/Indexed I-O (SQ/RL/IX)

Coverage: **34 passing**, 1 partial, 3 untested, 0 unknown.

| Feature | Spec | Coverage | Covering test | Proposed extra test |
|---|---|---|---|---|
| CLOSE ... REEL/UNIT (multi-volume) and CLOSE ... WITH LOCK | ISO_COBOL.md 14.9.7 CLOSE statement (REEL/UNIT, LOCK, NO REWIND phrases) | partial | WITH LOCK: SQ114A, SQ211A, IX204A, IX216A (baselined). CLOSE ... REEL/UNIT: SQ123A, SQ124A (baselined). | LOCK and REEL/UNIT are baselined. CLOSE ... NO REWIND appears only in SQ401M (unbaselined, ...M flagging). Propose tests/nist/extra/seq/close_no_rewind.cob: OPEN OUTPUT a seq file, WRITE, CLOSE WITH NO REWIND, re-OPEN, READ first record; verify NO REWIND is accepted and data intact (status 00 throughout). |
| READ ... PREVIOUS RECORD | ISO_COBOL.md 14.9.30 READ statement (READ ... PREVIOUS phrase) | untested |  | READ PREVIOUS is a COBOL-2002+ addition, not COBOL-85, and no NIST SQ/RL/IX program uses it (grep returns 0). For the live 2023 dialect propose tests/nist/extra/ix/read_previous.cob: build a 5-record indexed file, OPEN I-O, START KEY = highest, then READ ... PREVIOUS down to AT END; verify keys emitted in descending order and a status 10/AT END at file start. |
| USE GLOBAL ... AFTER ERROR/EXCEPTION (global declarative inherited by nested/called programs) | ISO_COBOL.md 14.9.49 USE statement, GLOBAL phrase | untested |  | Grep for 'USE GLOBAL' across SQ/RL/IX returns 0 — no file-I/O NIST program exercises it (it is covered only in the IC inter-program suite, e.g. IC233A/234A per project memory). Propose tests/nist/extra/ix/use_global_decl.cob: a main program with a GLOBAL FD and USE GLOBAL AFTER ERROR declarative, plus a nested/contained program that triggers an INVALID KEY on that file; verify the outer GLOBAL declarative fires and emits its message (status 23) rather than the run aborting. |
| MULTIPLE FILE TAPE clause (I-O-CONTROL) | ISO_COBOL.md 12.5 I-O-CONTROL paragraph, MULTIPLE FILE clause (obsolete in 85, removed later) | untested |  | Only SQ303M (flagging, unbaselined) and SQ401M reference MULTIPLE FILE TAPE — no passing path. This is an obsolete COBOL-85 element flagged for removal, so the correct deliverable is a FLAGGING test, not a runtime test: propose tests/nist/extra/seq/multiple_file_flag.cob exercising MULTIPLE FILE TAPE CONTAINS and asserting the compiler emits an obsolete-element diagnostic under --standard cobol85 while still parsing (matching the WS-DIALECT/WS-FLAG track). |

## Inter-Program Communication

Coverage: **14 passing**, 0 partial, 4 untested, 0 unknown.

| Feature | Spec | Coverage | Covering test | Proposed extra test |
|---|---|---|---|---|
| PROGRAM-ID ... IS INITIAL clause (initial program attribute) | ISO_COBOL.md 14.9.4 (D.6.5 inter-program comm.); grammar commonProgramAttribute : INITIAL_ | untested |  | The explicit 'PROGRAM-ID. name IS INITIAL.' clause appears ONLY in IC401M, a flagging-conformance module (not baselined; it has no CCVS report and expects a NON-CONFORMING-STANDARD flag). The runtime effect is covered by IC203A via CANCEL, but the clause itself never runs on a passing path. Compiler parses it (commonProgramAttribute, ProgramSymbol.IsInitial). Proposed test: tests/nist/extra/ic/initial_clause.cob — a driver with a contained 'PROGRAM-ID. SUBPROG IS INITIAL.' subprogram that increments a WORKING-STORAGE counter (VALUE 0) on each CALL; call it twice without CANCEL and assert the counter reads 1 BOTH times (proving auto re-initialization to initial state on every entry), versus a non-INITIAL control subprogram whose counter reads 1 then 2. |
| PROGRAM-ID ... IS COMMON clause (common contained program) | ISO_COBOL.md D.6.5 (scope of CALL); grammar commonProgramAttribute : COMMON | untested |  | The 'IS COMMON' clause appears ONLY in IC401M (flagging module, NESTEDPROG IS COMMON; not baselined). Visibility of a COMMON contained program to sibling nested programs is never asserted on a passing path. Compiler grammar parses COMMON (commonProgramAttribute) but ProgramSymbol has no IsCommon property — scoping enforcement is unverified. Proposed test: tests/nist/extra/ic/common_scope.cob — outermost program contains two siblings A and B; A is declared 'IS COMMON'; B calls A and prints A's returned value. Assert B successfully reaches A (COMMON makes it visible to siblings) and prints the expected transformed value; a parallel control where A is NOT common would be a compile error (out-of-scope CALL). |
| CALL ... RETURNING identifier (return-value phrase) | ISO_COBOL.md 14.9.4 (FORMAT 1/2: RETURNING identifier-3); grammar callReturningPhrase | untested |  | No IC program issues 'CALL ... RETURNING' on a passing path. The only RETURNING tokens in IC114A (the closest candidate) are in COMMENT lines ('returning to the main program'), not the phrase. Compiler grammar supports callReturningPhrase and PROCEDURE DIVISION ... RETURNING (returningClause). Proposed test: tests/nist/extra/ic/call_returning.cob — main CALLs a subprogram that has 'PROCEDURE DIVISION USING X RETURNING Y', computes Y = X * 2 + 1, and the main uses 'CALL ... USING X RETURNING R'; assert R holds the computed value (e.g. X=20 -> R=00041) printed in CCVS PASS/FAIL form. |
| CALL ... USING BY VALUE arithmetic-expression | ISO_COBOL.md 14.9.4 (FORMAT 2: BY VALUE); grammar callByValue gated {is2002()} | untested |  | BY VALUE is a COBOL-2002+ extension (not in the COBOL-85 CALL Format 1) and is grammar-gated behind is2002(); no IC NIST program uses it (grep found zero 'BY VALUE'). Out of scope for the COBOL-85 NIST suite but listed for completeness. Proposed test (2002 dialect only): tests/nist/extra/ic/call_by_value.cob — compile with --standard 2002; main does 'CALL sub USING BY VALUE (A + B)' where the callee receives a BY VALUE parameter, modifies its local copy, and the test asserts the caller's A/B are unchanged AND the callee saw the summed value. |

## Sort-Merge

Coverage: **15 passing**, 3 partial, 5 untested, 0 unknown.

| Feature | Spec | Coverage | Covering test | Proposed extra test |
|---|---|---|---|---|
| Variable-length record SORT (SD with RECORD IS VARYING IN SIZE) — varying records preserved through sort | §14.9.40.3 rule 5 + GR (variable-length); §13.18.43 RECORD VARYING; §14.9.40.4 record-size on RETURN | partial | ST124A (consumer only) | The actual variable-length SORT (SORT SORTFILE-1J USING SORTIN-1J GIVING SORTOUT-1J over an SD with RECORD IS VARYING IN SIZE FROM 50 TO 100) lives in ST123A, which is UNBASELINED; ST124A (baselined) only RE-READS the already-sorted file, so it never exercises the compiler's varying-record sort. Propose tests/nist/extra/st/sort_varying_records.cob: a self-contained program that RELEASEs ~6 records of differing lengths (e.g. 50, 75, 100 chars) into a RECORD IS VARYING SD, SORTs DESCENDING on a key in the minimum-length prefix, RETURNs them, and DISPLAYs each returned record's length and key — verify both the key order AND that each record's original length is preserved (per §14.9.40 GR: returned record size = released size). |
| DUPLICATES interaction with INPUT PROCEDURE ordering / DUPLICATES on USING input | §14.9.40.4 GR3a-3b (DUPLICATES order = input-file order, then RELEASE order within a file) | partial | ST127A | ST127A covers DUPLICATES with an INPUT PROCEDURE. There is no baselined test of DUPLICATES with a USING file (GR3a: order follows the input file's record order) nor of DUPLICATES across multiple keys where a secondary key still resolves ties. Propose tests/nist/extra/st/sort_duplicates_using.cob: SORT ... WITH DUPLICATES IN ORDER USING in-file GIVING out-file where several records share the major key; verify the duplicates emerge in their original input-file order, not implementation-arbitrary order. |
| SORT/MERGE …M flagging modules (obsolete/non-conforming sort-merge syntax under flag) | NIST CCVS flagging convention; ST301M MERGE TFIL / SORT TFIL | partial |  | ST301M (and ST112M/113M/114M) are flagging-conformance modules — ST301M is intentionally NOT baselined (no .txt) and is excluded from the guard. The flagging behavior (compiler emitting obsolete-feature diagnostics for these sort/merge constructs) is not asserted by any harness yet. This belongs to the WS-FLAG flagging-harness track rather than a normal output baseline; note here only so the gap is recorded. |
| SORT COLLATING SEQUENCE phrase (alphabet-name) on a Format-1 file SORT | §14.9.40.2 'COLLATING SEQUENCE IS alphabet-name-1'; grammar sortCollatingPhrase | untested |  | No baselined (or any) ST program puts the COLLATING SEQUENCE phrase on a SORT statement — every 'COLLATING SEQUENCE' string in ST108A/118A/127A is a MOVE literal, not a phrase. COLLATING is only exercised on MERGE (ST139A/140A/147A). Propose tests/nist/extra/st/sort_collating_alphabet.cob: SORT a file ON ASCENDING KEY with COLLATING SEQUENCE IS an alphabet-name that maps lowercase below uppercase (e.g. native vs ASCII), GIVING an output file; verify the sorted order differs from the default program collating sequence (e.g. records ordered by the custom alphabet, not native). |
| Variable-length record MERGE (RECORD IS VARYING in the merge SD) | §14.9.24.3 rule 3 (variable-length); §14.9.24.4 GR7b (record size on transfer) | untested |  | All baselined MERGE programs (ST139A/140A/144A/147A) use FIXED LENGTH records (their objective comments say 'FIXED LENGTH RECORDS'). No NIST MERGE exercises variable-length records. Propose tests/nist/extra/st/merge_varying_records.cob: two presorted variable-length-record files merged into a RECORD IS VARYING merge SD with GIVING; verify merged key order and that each merged record retains its source length. |
| SORT Format 2 — table sort (SORT data-name ... where data-name has OCCURS; optional KEY taken from OCCURS...KEY) | §14.9.40.2 Format 2 (table); §14.9.40.3 rules 13-15; §14.9.40.4 | untested |  | The grammar's sortStatement supports ONLY Format 1 (sortFileName: dataReference fed to USING/INPUT PROCEDURE + GIVING/OUTPUT PROCEDURE); there is NO table-sort path and no KEY-from-OCCURS handling. NIST CCVS (a COBOL-85 suite) contains no Format-2 SORT (it is a COBOL-2002+ feature). This is a genuine spec gap with zero coverage. Propose tests/nist/extra/st/sort_table_format2.cob: define an OCCURS 10 TIMES table, fill it unsorted, SORT the-table ON DESCENDING KEY elem-field, then DISPLAY the 10 elements — verify in-place descending order. Add a sort_table_omitted_key.cob variant where the OCCURS clause itself carries ASCENDING KEY IS and the SORT omits the KEY phrase (rule 15). |
| COLLATING SEQUENCE split form — FOR ALPHANUMERIC IS / FOR NATIONAL IS (separate alphanumeric vs national alphabets) | §14.9.40.2 / §14.9.24.2 COLLATING SEQUENCE {FOR ALPHANUMERIC IS alphabet-1}{FOR NATIONAL IS alphabet-2} | untested |  | Grammar sortCollatingPhrase is 'COLLATING? SEQUENCE IS? cobolWord (cobolWord)?' — it accepts one or two bare alphabet-names (Format 1 of the COLLATING clause) but NOT the FOR ALPHANUMERIC / FOR NATIONAL keyword form. No NIST test uses it (national data is COBOL-2002+). Propose tests/nist/extra/st/sort_collating_for_alphanumeric.cob: SORT ... COLLATING SEQUENCE FOR ALPHANUMERIC IS my-alpha; verify it parses and sorts alphanumeric keys by my-alpha. (Lower priority: requires grammar extension before a runtime test is meaningful.) |
| Sort/merge exception conditions — EC-SORT-MERGE-SEQUENCE (MERGE USING file out of order), EC-FLOW-RELEASE / EC-FLOW-RETURN (RELEASE/RETURN out of range) | §14.9.24.4 GR6 (sequence error); §14.6 EC-FLOW-RELEASE/RETURN, EC-SORT-MERGE-* table | untested |  | No baselined ST program checks the error conditions: a MERGE USING file not presorted (EC-SORT-MERGE-SEQUENCE → files closed, result undefined), or RELEASE outside an INPUT PROCEDURE / RETURN outside an OUTPUT PROCEDURE (EC-FLOW-*). Propose tests/nist/extra/st/merge_unsorted_input.cob: MERGE two deliberately-unsorted USING files and verify the runtime raises the documented sequence error (or, in the permissive dialect, the documented status) rather than silently producing garbage. (Edge/negative behavior; confirm compiler's chosen semantics first.) |

## Source Text (COPY/REPLACE)

Coverage: **16 passing**, 0 partial, 5 untested, 0 unknown.

| Feature | Spec | Coverage | Covering test | Proposed extra test |
|---|---|---|---|---|
| Nested COPY (a COPY statement inside copied library text) | ISO_COBOL.md §7.2.3 SR 1 (a COPY shall not appear within a COPY statement, but a copied member may itself contain COPY statements — handled recursively in §7.2 step 1) ; SR for recursion/circular include | untested |  | No NIST SM program nor any copylib member contains a true nested COPY statement (grep hits like 'COPY-WRITE-16'/'SECT-COPY-1' are paragraph names; SM106A's single COPY generating 3 divisions is NOT nested). The compiler DOES support it (CopyProcessor.ExpandCopyStatements recurses with depth+1 at line 337, MaxDepth guard, and a circular-include guard CBL3621). Propose tests/nist/extra/sm/nested_copy.cob: an outer copybook OUTER.cpy that contains 'COPY INNER.' where INNER.cpy defines 01 INNER-FIELD PIC X(5) VALUE "INNER"; main program does COPY OUTER then DISPLAY INNER-FIELD; verify output line 'INNER'. Add a second variant nested_copy_replacing.cob where the outer COPY ... REPLACING substitutes a word that appears only in INNER.cpy, verifying REPLACING propagates through the nested expansion. |
| COPY ... REPLACING LEADING / TRAILING ==partial-word-1== BY ==partial-word-2== (partial-word replacement within a single text-word) | ISO_COBOL.md §7.2.3 Format ({LEADING\|TRAILING} ==partial-word-1== BY ==partial-word-2==); SR 11–13; GR 9 b (leftmost/rightmost char-position match) | untested |  | No NIST SM program uses the LEADING/TRAILING partial-word phrase (grep for 'LEADING'/'TRAILING ==' in SM*.cob is empty; SM206A's 'partial-word' label actually means multi-word pseudo-text on qualified names, not the LEADING/TRAILING phrase). Compiler has NO LEADING/TRAILING handling in CopyProcessor — it is a real gap. Propose tests/nist/extra/sm/copy_replacing_leading.cob: copybook with 01 PRE-FIELD-A, 01 PRE-FIELD-B; main 'COPY x REPLACING LEADING ==PRE-== BY ==NEW-=='; DISPLAY NEW-FIELD-A; verify it compiles and the renamed items resolve. And copy_replacing_trailing.cob using TRAILING ==-OLD== BY ==-NEW== plus a zero-word partial-word-2 (deletion) case; verify substring (not whole-word) replacement. |
| COPY ... SUPPRESS PRINTING phrase | ISO_COBOL.md §7.2.3 Format ([SUPPRESS PRINTING]); GR 4 (incorporated text not listed); §6.4.7 mapping marks SUPPRESS as Implementor-defined | untested |  | No NIST SM program uses SUPPRESS PRINTING (grep empty). It is listing-only (implementor-defined) so semantically it must be a no-op that still copies the text. Compiler does not parse SUPPRESS/PRINTING after the library qualifier. Propose tests/nist/extra/sm/copy_suppress.cob: 'COPY K1W01 SUPPRESS PRINTING.' (or with a literal text-name) then reference the copied item; verify the program compiles and the copied data is present and usable (i.e. SUPPRESS does not alter program semantics, only listing). |
| COPY literal-1 (quoted alphanumeric literal as the text-name) | ISO_COBOL.md §7.2.3 Format (COPY {literal-1 \| text-name-1}); SR 4/5 (literal-1 alphanumeric, value implementor-defined) | untested |  | Every SM COPY uses an unquoted text-name (e.g. COPY K1FDA); none uses the quoted literal-1 form 'COPY "K1FDA".'. Compiler ReadWord-based COPY parse may not accept a quoted literal text-name. Propose tests/nist/extra/sm/copy_literal_name.cob: 'COPY "K1W01".' (mirror SM101A's K1W01) and 'COPY "ALTLB" OF "XXXXX047".' (mirror SM207A) — verify the literal text-name and literal library-name resolve identically to the word form. |
| REPLACE Format 1 with ALSO phrase / REPLACE OFF LAST (push/pop queue of nested REPLACE statements) | ISO_COBOL.md §7.2.4 GR 4 a (ALSO pushes active onto inactive queue), GR 4 c (OFF LAST pops one), GR 4 d (OFF cancels all) | untested |  | SM208A only uses plain Format-1 REPLACE + plain REPLACE OFF — never ALSO or OFF LAST (grep for 'ALSO'/'LAST' in SM*.cob is empty). Compiler ApplyReplaceStatements just Clear()s and re-parses active replacements; it has no inactive queue, so nested ALSO/LAST semantics are unimplemented. Propose tests/nist/extra/sm/replace_also_last.cob: REPLACE ==A== BY ==X==; then REPLACE ==B== BY ==Y== ALSO ==A== BY ==Z== (or nested REPLACE...ALSO), then REPLACE OFF LAST to pop back to the outer rule; emit lines containing A and B in each region; verify the active substitution at each point matches the spec's push/pop queue. |

## Intrinsic Functions

Coverage: **43 passing**, 3 partial, 15 untested, 1 unknown.

| Feature | Spec | Coverage | Covering test | Proposed extra test |
|---|---|---|---|---|
| Integer vs numeric RETURN type distinction (integer funcs usable where integer required; numeric funcs carry fractional digits) | §15.2 types 4 (numeric) vs 5 (integer) | partial | IF110A/111A/114A/124A (integer returns: FACTORIAL/INTEGER/INTEGER-PART/MOD) and IF136A/120A (numeric: SQRT/MEAN) | Return-class IS exercised implicitly, but no test asserts an integer function used in an integer-only context (e.g., as a subscript or table index) vs a numeric function rejected/truncated there. Add tests/nist/extra/if/integer_return_as_subscript.cob: use FUNCTION INTEGER(2.9) and FUNCTION MOD(7,3) directly as an OCCURS subscript and verify the correct element is selected, confirming integer-class return. |
| Table-reference argument with ALL subscript over an ODO (OCCURS DEPENDING ON) table | §15.3 — ALL range for ODO table is the ODO object's current value | partial | IF119A/120A/121A/122A/123A/128A/129A/132A/137A/138A/141A all use IND(ALL) but over FIXED OCCURS tables | ALL is exercised on fixed tables only; the ODO-bounded ALL range (§15.3) is untested. Add tests/nist/extra/if/all_subscript_odo.cob: table OCCURS 1 TO 5 DEPENDING ON N; set N=3, populate 5 cells; verify FUNCTION SUM(T(ALL)) sums only the first 3, and FUNCTION ORD-MAX(T(ALL)) ranges over 3. |
| Nested FUNCTION as argument (function-identifier as an argument of another function) | §8.4.3.2 example FUNCTION MAX(FUNCTION RANDOM(A) B) (lines 6922-6934) | partial | IF101A — FUNCTION ACOS(FUNCTION ACOS(D/D)) (same-function nesting) | Only same-function single-arg nesting is baselined. The spec's canonical mixed-function/variadic nesting (e.g. MAX(FUNCTION RANDOM..., B)) is not. Add tests/nist/extra/if/nested_mixed_function.cob: verify FUNCTION MAX(FUNCTION INTEGER(4.2), 3, FUNCTION FACTORIAL(3)) = 6, exercising a numeric arg, an integer-function arg, and another integer-function arg inside a variadic MAX. |
| FUNCTION keyword OMITTED via REPOSITORY ... FUNCTION intrinsic-function-name-1 INTRINSIC / ALL INTRINSIC | §8.4.3.2 rule 2 (line 6902); §13 REPOSITORY (line 14710,14887): word FUNCTION may be omitted only when the name (or ALL) is in REPOSITORY | untested |  | REPOSITORY/INTRINSIC appears ONLY in the non-baselined flagging modules IF401M/402M/403M. Add tests/nist/extra/if/repository_intrinsic_omit_keyword.cob: REPOSITORY paragraph with FUNCTION MAX MIN SQRT INTRINSIC (and a second program using ALL INTRINSIC), then call MAX(3,7,2), SQRT(16), INTEGER(4.9) WITHOUT the FUNCTION keyword; verify output 7, 4, 4 — proving keyword-optional dispatch matches the FUNCTION-prefixed result. |
| Reference modification applied to a function RESULT, e.g. FUNCTION CURRENT-DATE (1:8) or FUNCTION REVERSE(X)(1:3) | §8.4.3.2 function-identifier allows trailing reference-modifier on alphanumeric/national functions | untested |  | No IF program ref-mods a function result (grep for FUNCTION X(..)(a:b) = 0 hits). Add tests/nist/extra/if/funcresult_refmod.cob: MOVE FUNCTION CURRENT-DATE(1:8) TO WS (verify it equals the 8-char YYYYMMDD prefix) and DISPLAY FUNCTION REVERSE("ABCDEF")(1:3) → verify "FED". |
| Reference-modified data item AS a function argument, e.g. FUNCTION LENGTH(X(2:3)) / FUNCTION UPPER-CASE(X(1:4)) | §15.3 argument is an alphanumeric data item; ref-mod is a permitted identifier form | untested |  | No IF test passes a ref-modded item as an argument (grep = 0). Add tests/nist/extra/if/refmod_as_arg.cob: WS PIC X(6) VALUE "abcdef"; verify FUNCTION UPPER-CASE(WS(2:3)) = "BCD" and FUNCTION LENGTH(WS(2:3)) = 3. |
| ABS — numeric absolute value | §8.11 ABS (line 10943); §15. Dispatched in runtime IntrinsicFunctions.cs:33 | untested |  | No IF program references ABS (NIST IF suite stops at the COBOL-85 list it implements, ABS not among IF101-142). Compiler supports it. Add tests/nist/extra/if/abs.cob: COMPUTE WS = FUNCTION ABS(-7.5) → verify 7.5; FUNCTION ABS(3) → 3; FUNCTION ABS(0) → 0. |
| SIGN — integer (-1/0/+1) | §8.11 SIGN; runtime IntrinsicFunctions.cs:63 | untested |  | Not in any IF program. Add tests/nist/extra/if/sign.cob: verify FUNCTION SIGN(-4)=-1, SIGN(0)=0, SIGN(9)=1. |
| FRACTION-PART — numeric | §8.11 FRACTION-PART; runtime line 62 | untested |  | Not exercised by IF suite. Add tests/nist/extra/if/fraction_part.cob: FUNCTION FRACTION-PART(3.75) → verify .75 (and -3.75 → -.75). |
| EXP / EXP10 — numeric (e^x and 10^x) | §8.11 EXP, EXP10; runtime lines 43-44 | untested |  | Not in IF suite. Add tests/nist/extra/if/exp.cob: FUNCTION EXP(0)=1, FUNCTION EXP10(2)=100 (compare within tolerance). |
| PI / E — numeric constants (COBOL-2002+) | §8.11 PI, E; runtime supports PI | untested |  | Not in COBOL-85 IF suite. Add tests/nist/extra/if/pi.cob: verify FUNCTION PI is in (3.14159,3.14160) and 4*ATAN(1) ≈ PI. |
| CONCAT / CONCATENATE — alphanumeric, variable args (COBOL-2023) | §8.11 CONCAT; runtime Concatenate line 180 | untested |  | Not a COBOL-85 IF test. Add tests/nist/extra/if/concat.cob: FUNCTION CONCAT("AB","CD","EF") → verify "ABCDEF". |
| TRIM (leading/trailing/both keyword forms) | §8.11 TRIM; §15 keyword arg LEADING/TRAILING; runtime Trim/TrimLeading/TrimTrailing lines 170-172 | untested |  | No IF test; runtime has all three modes. Add tests/nist/extra/if/trim.cob: FUNCTION TRIM("  AB  ")="AB", TRIM("  AB  " LEADING)="AB  ", TRIM("  AB  " TRAILING)="  AB". |
| BYTE-LENGTH — integer | §8.11 BYTE-LENGTH; runtime line 179 | untested |  | Not in IF suite. Add tests/nist/extra/if/byte_length.cob: WS PIC X(5); verify FUNCTION BYTE-LENGTH(WS)=5 and contrast with LENGTH for a multi-byte usage if supported. |
| NUMVAL-F / TEST-NUMVAL / TEST-NUMVAL-C / TEST-NUMVAL-F (validation functions, COBOL-2002+) | §8.11 NUMVAL-F, TEST-NUMVAL* | untested |  | IF suite only covers NUMVAL/NUMVAL-C. Add tests/nist/extra/if/test_numval.cob: FUNCTION TEST-NUMVAL("12.3")=0 (valid) and TEST-NUMVAL("12.3X")=nonzero position; FUNCTION NUMVAL-F("1.5E2")=150. |
| FORMATTED-DATE / FORMATTED-TIME / FORMATTED-DATETIME / FORMATTED-CURRENT-DATE / INTEGER-OF-FORMATTED-DATE / SECONDS-* (COBOL-2014 intl date-time) | §15.3.1-15.3.3 international date/time formats | untested |  | No COBOL-85 IF test. Runtime has FORMATTED-CURRENT-DATE / SECONDS-PAST-MIDNIGHT / LOCALE-* stubs. Add tests/nist/extra/if/formatted_datetime.cob exercising FUNCTION FORMATTED-DATETIME("YYYYMMDDThhmmss", integer-date, integer-time) and verify the ISO-8601 string; gate on compiler support. |
| DATE-TO-YYYYMMDD / DAY-TO-YYYYDDD / YEAR-TO-YYYY (windowed-year date conversion, COBOL-2002) | §8.11 DATE-TO-YYYYMMDD etc; runtime lines for DateToYyyymmdd/YearToYyyy | untested |  | Not in IF suite. Add tests/nist/extra/if/year_to_yyyy.cob: FUNCTION YEAR-TO-YYYY(85,50,2000) → verify 1985 (sliding window) and FUNCTION DATE-TO-YYYYMMDD(850101,50,2000) → 19850101. |
| HIGHEST-ALGEBRAIC / LOWEST-ALGEBRAIC / SMALLEST-ALGEBRAIC (COBOL-2002 numeric-range) | §8.11; runtime HighestAlgebraic/LowestAlgebraic/SmallestAlgebraic | untested |  | Not in IF suite. Add tests/nist/extra/if/highest_algebraic.cob: WS PIC S99V9; verify FUNCTION HIGHEST-ALGEBRAIC(WS)=99.9 and LOWEST-ALGEBRAIC(WS)=-99.9. |
| SUBSTITUTE / FIND-STRING (COBOL-2023 string functions) | §8.11 SUBSTITUTE, FIND-STRING | unknown |  | Not in IF suite; verify compiler support before testing. If dispatched, add tests/nist/extra/if/substitute.cob: FUNCTION SUBSTITUTE("ABABAB","A","X") → verify "XBXBXB". |

## Data Division

Coverage: **44 passing**, 3 partial, 7 untested, 0 unknown.

| Feature | Spec | Coverage | Covering test | Proposed extra test |
|---|---|---|---|---|
| PICTURE — numeric-edited combined forms (floating currency + check-protect + sign + BLANK WHEN ZERO together) | ISO_COBOL.md §13.18.40.5; §13.18.40.6 (precedence rules) | partial | NC104A covers many individual edited forms | Individual symbols covered, but maximal precedence-rule combinations are not all in one passing path. Propose tests/nist/extra/data/pic_edit_combo.cob: a battery of edited receivers (e.g. $**,**9.99CR, --,--9.99, ++,+++.99) fed positive/negative/zero senders, verifying exact spaced/signed output strings. |
| SYNCHRONIZED clause (SYNC LEFT/RIGHT) | ISO_COBOL.md §13.18.55 (SYNCHRONIZED clause); alignment rules | partial | NC105A, NC106A, NC107A, NC118A, NC138A, NC175A (SYNC present) | SYNC is present in baselined programs and parsed, but NIST treats it as benign (alignment slack is implementation-defined, so tests don't assert inter-field bytes). SYNC LEFT vs RIGHT and its effect on REDEFINES/group length are not output-verified. Propose tests/nist/extra/data/sync_redefines_len.cob: a SYNC COMP item followed by another item, REDEFINES the group, DISPLAY computed offsets/LENGTH to confirm deterministic slack-byte placement per the compiler's documented rule. |
| VALUE clause — level-88 multiple literals (literal-1, literal-2, ...) | ISO_COBOL.md §13.18.63 | partial | IF107A/IF142A (88 with multiple/THRU values) | Multiple-literal AND mixed (single literal + THRU range in one 88) lists are only thinly exercised. Propose tests/nist/extra/data/level88_multi_value.cob: 88 OK VALUES 1, 3, 5 THRU 9; verify the condition is TRUE for 1/3/5/7/9 and FALSE for 2/4/10 via IF tests emitting PASS/FAIL lines. |
| USAGE COMP-1 (single-precision float) | ISO_COBOL.md §13.18.60 (implementor-defined floating) | untested |  | COMP-1 appears NOWHERE in the NIST corpus, yet the compiler grammar supports it (CobolLexer.g4) with IEEE-754 per CLAUDE.md. Propose tests/nist/extra/data/usage_comp1.cob: MOVE/COMPUTE with a COMP-1 item (e.g. 3.14159), DISPLAY rounded to N digits, verify single-precision result; also COMP-1 -> DISPLAY MOVE rounding. |
| USAGE COMP-2 (double-precision float) | ISO_COBOL.md §13.18.60 | untested |  | COMP-2 absent from the NIST corpus; compiler grammar supports it. Propose tests/nist/extra/data/usage_comp2.cob: COMP-2 arithmetic (e.g. 1.0/3.0) DISPLAYed to ~15 significant digits, verifying double precision and COMP-2<->packed/display conversions. |
| USAGE COMP-3 / PACKED-DECIMAL | ISO_COBOL.md §13.18.60 | untested |  | Neither COMP-3 nor PACKED-DECIMAL appears anywhere in the NIST corpus; the compiler grammar supports both. Major commercial-COBOL usage with ZERO passing coverage. Propose tests/nist/extra/data/usage_comp3.cob: PIC S9(7)V99 COMP-3 (and PACKED-DECIMAL synonym), do ADD/SUBTRACT/MULTIPLY/DIVIDE, MOVE to an edited DISPLAY field, verify exact values incl. sign-nibble behavior for negative and zero. |
| USAGE COMP-4 | ISO_COBOL.md §13.18.60 | untested |  | COMP-4 absent from NIST corpus AND absent from the compiler grammar (CobolData.g4 usageClause lists COMP/1/2/3/5 + BINARY + PACKED-DECIMAL + INDEX, NOT COMP_4). Gap on both axes. Propose (after adding COMP-4 == binary synonym to grammar) tests/nist/extra/data/usage_comp4.cob: PIC S9(9) COMP-4 arithmetic equal to BINARY results. |
| USAGE COMP-5 (native binary) | ISO_COBOL.md §13.18.60 | untested |  | COMP-5 absent from NIST corpus; compiler grammar supports it. Propose tests/nist/extra/data/usage_comp5.cob: PIC 9(4) COMP-5 holding a value beyond the PIC's decimal range (native-binary semantics), verify no decimal truncation on storage vs COMP. |
| GROUP-USAGE clause (BIT/NATIONAL group usage) | ISO_COBOL.md §13.18.29 (GROUP-USAGE clause) | untested |  | GROUP-USAGE appears nowhere in the NIST corpus (NIST is COBOL-85; GROUP-USAGE is 2002+). Out of COBOL-85 NIST scope but present in the live spec. If the compiler claims support, propose tests/nist/extra/data/group_usage.cob asserting group-level usage propagation; otherwise document as a deferred 2002+ feature. |
| USAGE POINTER / PROGRAM-POINTER / function-pointer / object-reference | ISO_COBOL.md §13.18.60 (USAGE clause, pointer forms) | untested |  | Not in the NIST COBOL-85 corpus (2002+). Document as out-of-NIST-scope; a compiler unit test (not a NIST program) is the right home if pointer usage is advertised. |

## Environment Division

Coverage: **29 passing**, 2 partial, 13 untested, 1 unknown.

| Feature | Spec | Coverage | Covering test | Proposed extra test |
|---|---|---|---|---|
| SPECIAL-NAMES: device-name/feature-name IS mnemonic-name (ACCEPT FROM / DISPLAY UPON device) | ISO_COBOL.md 12.3.7.2 (line 14152-14153) | partial | NC211A (switch mnemonic), NC204M/NC220M (ACCEPT-INPUT-DEVICE/DISPLAY-OUTPUT-DEVICE) | The device-name IS mnemonic form is exercised on a PASSING path only by the flagging M-modules NC204M/NC220M (baselined but obsolete-tagged) and is referenced (not used in I/O) by NC211A. No clean non-M A-test does ACCEPT ... FROM mnemonic / DISPLAY ... UPON mnemonic. Propose tests/nist/extra/env/mnemonic-display-upon.cob: SPECIAL-NAMES 'feature-name IS DISP-OUT', then DISPLAY "HELLO" UPON DISP-OUT; verify HELLO reaches the mapped device output stream. |
| SELECT ... COLLATING SEQUENCE clause (per-file, indexed) | ISO_COBOL.md 12.4.5.7 (collating-sequence-clause, line 14989, 15122) | partial | IX210A, IX214A, IX215A (PROGRAM COLLATING SEQUENCE referenced) | Program-level collating is covered; the per-SELECT COLLATING SEQUENCE clause specifically on an indexed file's keys is not clearly exercised independently. Propose tests/nist/extra/env/select-collating.cob: indexed file with a SELECT-level COLLATING SEQUENCE alphabet that reverses key order, WRITE then READ NEXT -> verify retrieval order follows the alphabet, not native. |
| OBJECT-COMPUTER PROGRAM COLLATING SEQUENCE ... FOR ALPHANUMERIC / FOR NATIONAL (split form) | ISO_COBOL.md 12.3.6.2 (line 13949-13953) | untested |  | No NIST program uses the FOR ALPHANUMERIC IS / FOR NATIONAL IS split form (matches found in ST144A were NATIVE-COLLATING comments, not the clause). This is a COBOL-2002+ extension over '85. Propose tests/nist/extra/env/collate-for-alphanumeric.cob: declare two ALPHABETs, set PROGRAM COLLATING SEQUENCE FOR ALPHANUMERIC IS alph1, then a relation condition whose truth value depends on alph1 — verify the comparison flips vs NATIVE. |
| OBJECT-COMPUTER CHARACTER CLASSIFICATION (locale) | ISO_COBOL.md 12.3.6.2 / GR5-8 (line 13943, 14006) | untested |  | COBOL-2002+/2023 locale feature; outside COBOL-85 scope and unused by NIST. Propose only if locale support is in scope: tests/nist/extra/env/char-classification.cob verifying UPPER-CASE/LOWER-CASE honor a CHARACTER CLASSIFICATION locale. Likely out-of-scope for the '85 live target. |
| SPECIAL-NAMES: ALPHABET ... IS code-name / UCS-4 / UTF-8 / UTF-16 / LOCALE | ISO_COBOL.md 12.3.7.2 (line 14180-14194) | untested |  | National/code-name/locale alphabet forms are COBOL-2002+/2023, not in NIST '85 corpus. Out-of-scope unless national-character support is targeted. If desired: tests/nist/extra/env/alphabet-utf8.cob declaring ALPHABET x FOR NATIONAL IS UTF-8 and CODE-SET on a national file — likely out-of-scope for the live '85 module. |
| SPECIAL-NAMES: CURRENCY SIGN ... WITH PICTURE SYMBOL literal (distinct sign vs picture symbol) | ISO_COBOL.md 12.3.7.2 (line 14128) | untested |  | The WITH PICTURE SYMBOL phrase (multi-char currency string + separate one-char PIC symbol) is not exercised by any NIST program, and CLAUDE.md notes it was historically blocked by the PICMODE lexer. Propose tests/nist/extra/env/currency-with-picture-symbol.cob: CURRENCY SIGN IS "EUR" WITH PICTURE SYMBOL "E", PIC EEE9.99, MOVE 12.34 -> verify edited output begins with 'EUR'. |
| SPECIAL-NAMES: multiple CURRENCY SIGN clauses (repetition '...') | ISO_COBOL.md 12.3.7.2 (CURRENCY ... clause repeats, line 14128) | untested |  | Repeating CURRENCY SIGN to define several currency symbols is a COBOL-2002+ extension; NIST '85 uses at most one. Propose tests/nist/extra/env/multi-currency.cob with two CURRENCY SIGN clauses (e.g. '$' and 'E') used in two PICs; verify each editing independently. Lower priority (extension). |
| SPECIAL-NAMES: SYMBOLIC CHARACTERS clause | ISO_COBOL.md 12.3.7.2 symbolic-characters-clause (line 14235-14237); figurative use 8.x (line 6343, 6419) | untested |  | The only NIST occurrence (NC401M 'SYMBOLIC CHARACTERS A IS 32') is in a flagging M-module where the construct is flagged NON-CONFORMING and the report path is not a normal pass. No A-test exercises a defined symbolic-character as a figurative constant. Propose tests/nist/extra/env/symbolic-characters.cob: SYMBOLIC CHARACTERS HT IS 10, then MOVE HT TO a 1-char field and DISPLAY/inspect its ordinal -> verify it equals the 10th collating position (ASCII 0x09). |
| SPECIAL-NAMES: CONSOLE IS mnemonic-name / ACCEPT FROM CONSOLE / DISPLAY UPON CONSOLE | ISO_COBOL.md 12.3.7 system-name relating (line 14092); ACCEPT/DISPLAY device | untested |  | No NIST program defines CONSOLE IS mnemonic or does ACCEPT FROM CONSOLE / DISPLAY UPON CONSOLE (only generic DISPLAY-OUTPUT-DEVICE mnemonics in M-modules). Propose tests/nist/extra/env/console-mnemonic.cob: 'CONSOLE IS CRT-DEV', DISPLAY "X" UPON CRT-DEV; verify text routed to console stream. |
| SPECIAL-NAMES: CRT STATUS IS / CURSOR IS data-name | ISO_COBOL.md 12.3.7.2 (line 14126, 14130); grammar CobolSpecialNames.g4:101-108 | untested |  | Screen-handling clauses; parsed by the compiler (grammar present) but no NIST program uses them and they belong to the screen/extension facility. Likely out-of-'85-scope. If screen section is targeted, propose tests/nist/extra/env/cursor-crt-status.cob exercising cursor positioning + status return. |
| SPECIAL-NAMES: LOCALE / ORDER TABLE / DYNAMIC LENGTH STRUCTURE | ISO_COBOL.md 12.3.7.2 (line 14136, 14157, 14212-14226) | untested |  | All three are COBOL-2002+/2023 features absent from the NIST '85 corpus. Out-of-scope for the live '85 module; no extra test proposed unless those modules are scheduled. |
| SELECT ... ORGANIZATION IS LINE SEQUENTIAL | ISO_COBOL.md 12.4.5.1 Format 3 (line 15086); SR12 (line 15157) | untested |  | No NIST program uses LINE SEQUENTIAL (grep found zero .cob matches). It is a COBOL-2002+ organization. Propose tests/nist/extra/env/line-sequential.cob: WRITE three variable-length text records to a LINE SEQUENTIAL file then READ them back; verify each record delimited by newline and trailing spaces stripped. |
| SELECT ... SHARING WITH / LOCK MODE (COBOL-2002+ concurrency) | ISO_COBOL.md 12.4.5.1 (SHARING line 15011/15052/15098; LOCK MODE line 14994/15043/15081) | untested |  | SHARING and LOCK MODE are COBOL-2002+ concurrency clauses absent from the '85 NIST corpus. Out-of-scope for the live '85 module unless concurrency is targeted; no extra test proposed now. |
| I-O-CONTROL: RERUN clause (obsolete COBOL-85, removed in 2023) | ISO_COBOL.md I-O-CONTROL (RERUN removed; not in 2023 grammar at 12.4.6.2 line 15853) | untested |  | RERUN appears in ZERO NIST programs and is ABSENT from the compiler grammar (grep of CobolIO.g4/SpecialNames found no RERUN rule). It is an obsolete COBOL-85 element removed in 2023. Propose, under the WS-DIALECT removed-features track, tests/nist/extra/env/rerun-flag.cob containing 'RERUN ON ... EVERY 1000 RECORDS' that should parse-and-flag as an obsolete/removed element (diagnostic check), NOT produce executable output. If '85 dialect fidelity requires running it, the runtime effect is a no-op checkpoint. |
| I-O-CONTROL: APPLY COMMIT clause (COBOL-2002+) | ISO_COBOL.md 12.4.6.3 (line 15860) | untested |  | COBOL-2002+ commit/rollback facility; absent from NIST '85 corpus and out-of-scope for the live '85 module. No extra test proposed. |
| OBJECT-COMPUTER MEMORY SIZE (obsolete COBOL-85, removed) | ISO_COBOL.md 12.3.6 (clause removed in 2023; was COBOL-85 obsolete) | unknown |  | Not in the 2023 grammar text shown; no NIST program in the corpus uses MEMORY SIZE. If a flagging/dialect path is desired, propose tests/nist/extra/env/memory-size-flag.cob that should emit an obsolete-element diagnostic (pairs with WS-DIALECT removed-features work), not an executable output check. |

## Report Writer

Coverage: **19 passing**, 1 partial, 17 untested, 0 unknown.

| Feature | Spec | Coverage | Covering test | Proposed extra test |
|---|---|---|---|---|
| TERMINATE report-name... (final group presentation) | ISO_COBOL.md §14.9.62 TERMINATE; grammar terminateStatement; ReportWriterRuntime.TerminateReport (removes report only) | partial | RW101A | TERMINATE is reached and the report is closed cleanly in all tests, but the runtime merely removes the report context — it does NOT present the final CONTROL FOOTINGs, the last PAGE FOOTING, or the REPORT FOOTING required by §14.9.62. RW104A's comment even expects 'TERMINATE ... PAGE FOOTING ... on LINE 30 of PAGE 3' but the test's own pass/fail logic does not assert that footing's bytes, so it stays green. Propose tests/nist/extra/rw/terminate-footings.cob: a report with PF + CF FINAL + RF; GENERATE 2 details; verify TERMINATE emits the trailing PF, the CF FINAL (with final SUM totals), and the RF, in that order, before the file is closed. |
| FD REPORTS ARE r1 r2 ... (multiple reports on one file) | ISO_COBOL.md §13.4.5 REPORT(S) clause (plural/multiple report-names) | untested |  | All baselined tests bind exactly one report per FD. Propose tests/nist/extra/rw/two-reports-one-file.cob: an FD with REPORTS ARE RPT-A RPT-B, two RDs, INITIATE both, GENERATE a detail in each, TERMINATE both; verify both reports' lines appear in the single output file in interleaved write order with independent LINE-/PAGE-COUNTERs. |
| Report group TYPE REPORT HEADING / RH | ISO_COBOL.md §13.18.57; ReportGroupKind.ReportHeading | untested |  | Parsed into the symbol model but never registered/presented (FileIoLowerer only registers PageHeading/PageFooting). No baselined test defines an RH group. Propose tests/nist/extra/rw/report-heading-footing.cob: RD with TYPE RH (LINE 1 VALUE 'REPORT START') and TYPE RF (VALUE 'REPORT END'); GENERATE 3 details; verify RH is presented exactly once before the first page heading at INITIATE-time of first GENERATE, and RF exactly once at TERMINATE. |
| Report group TYPE REPORT FOOTING / RF | ISO_COBOL.md §13.18.57; ReportGroupKind.ReportFooting | untested |  | Parsed but never presented; TERMINATE does no footing output. Covered by the proposed report-heading-footing.cob above — verify RF text appears once as the last report line after the final page footing. |
| Report group TYPE CONTROL HEADING / CH [data-name \| FINAL] | ISO_COBOL.md §13.18.57 TYPE CONTROL HEADING; grammar reportGroupType CH (FINAL\|dataReference); ReportGroupKind.ControlHeading + ControlField | untested |  | Parsed (ControlField captured) but no control-break logic in runtime (EmitGroup has no control comparison). No baselined test. Propose tests/nist/extra/rw/control-heading.cob: CONTROL IS DEPT, a CH DEPT group (VALUE 'DEPT:' SOURCE DEPT), DETAIL lines; GENERATE rows with DEPT values 10,10,20; verify CH is presented at the first detail and again when DEPT changes 10->20 (i.e. before the detail that triggers the break), and CH FINAL once at INITIATE. |
| Report group TYPE CONTROL FOOTING / CF [data-name \| FINAL] (control breaks) | ISO_COBOL.md §13.18.57 TYPE CONTROL FOOTING; ReportGroupKind.ControlFooting; §14.9.x control-break processing | untested |  | Parsed but no control-break detection or footing presentation. Propose tests/nist/extra/rw/control-footing-break.cob: CONTROL IS DEPT, CF DEPT group; GENERATE details for DEPT 10,10,20,20; verify the DEPT-10 CONTROL FOOTING is presented when DEPT changes to 20, a second CF at TERMINATE for DEPT 20, then CF FINAL. (Pairs with the SUM test below — CF is where summed totals print.) |
| CONTROL(S) IS/ARE {FINAL \| data-name}... hierarchy | ISO_COBOL.md §13.18.16 CONTROL clause; grammar reportControlClause; ReportSymbol.ControlFields | untested |  | The control hierarchy is parsed into ReportSymbol.ControlFields but never read by lowering/runtime (no break detection). No baselined test declares CONTROL. Propose tests/nist/extra/rw/multi-level-control.cob with CONTROLS ARE REGION DEPT (major->minor), CH/CF at both levels; GENERATE rows crossing a minor break then a major break; verify minor (DEPT) footing/heading fire on a DEPT change and BOTH minor+major fire (minor inside major) on a REGION change, in correct major->minor heading / minor->major footing order. |
| SUM data-name... (summation counters in CONTROL FOOTING) | ISO_COBOL.md §13.18.54 SUM clause; grammar reportSumClause/sumOperand; ReportGroupSymbol.SumFields | untested |  | SUM fields are captured into the symbol model but no accumulator is created/incremented and no value is placed. No baselined test uses SUM. Propose tests/nist/extra/rw/sum-control-footing.cob: CONTROL DEPT, a CF DEPT group with '05 PIC 9(4) COLUMN 10 SUM AMT', DETAIL with SOURCE AMT; GENERATE AMT=10,20 (DEPT 1) then AMT=30 (DEPT 2); verify the DEPT-1 CF prints total 0030 and DEPT-2 CF prints 0030, and the counter RESETs to zero across the control break. |
| SUM ... UPON data-name (sum upon a specific detail) | ISO_COBOL.md §13.18.54 UPON phrase; grammar reportSumClause UPON dataReference | untested |  | UPON is parsed but unused (no per-detail crediting). Propose tests/nist/extra/rw/sum-upon.cob: two DETAIL groups DET-A/DET-B both with field AMT, a CF with 'SUM AMT UPON DET-A'; GENERATE DET-A (AMT=5), DET-B (AMT=99), DET-A (AMT=5); verify the CF total is 0010 (only DET-A generations credited, DET-B ignored). |
| SUM ... RESET ON {data-name \| FINAL} | ISO_COBOL.md §13.18.54 RESET phrase; grammar reportSumReset; ReportGroupSymbol (RESET not modeled) | untested |  | RESET ON is parsed (reportSumReset) but not modeled on the symbol or honored. Default reset is on the counter's own control break; RESET ON a higher control suppresses that. Propose tests/nist/extra/rw/sum-reset.cob: CONTROLS ARE MAJ MIN, a CF MIN with 'SUM AMT RESET ON MAJ' (running subtotal across MIN breaks, reset only on MAJ); verify the MIN-level total accumulates across minor breaks and only zeroes on a MAJ change. |
| GROUP INDICATE | ISO_COBOL.md §13.18 GROUP INDICATE phrase; grammar reportGroupIndicateClause; not modeled on ReportGroupSymbol | untested |  | Parsed (reportGroupIndicateClause) but the flag is not stored on ReportGroupSymbol nor honored — the field would print on every detail. No baselined test. Propose tests/nist/extra/rw/group-indicate.cob: a DETAIL with a GROUP INDICATE field (VALUE 'DEPT-10') plus a normal field; GENERATE 3 details on page 1 then force a page advance and 1 more; verify the GROUP INDICATE field prints only on the first detail of each page (and after a control break), blank on the others. |
| NEXT GROUP IS {integer \| PLUS integer \| NEXT PAGE} | ISO_COBOL.md §13.18.39 NEXT GROUP clause; grammar reportNextGroupClause; not modeled/honored | untested |  | Parsed but not stored on the symbol and ignored by EmitGroup (only the LINE advance of the NEXT group drives spacing). No baselined test. Propose tests/nist/extra/rw/next-group.cob: a DETAIL with 'NEXT GROUP IS PLUS 3'; GENERATE two details; verify 3 blank lines separate them (LINE-COUNTER jumps by the NEXT GROUP amount after the group, independent of the next group's own LINE clause). Add a 'NEXT GROUP NEXT PAGE' variant verifying the following group starts a fresh page. |
| LINE NUMBER ... ON NEXT PAGE / LINE NEXT PAGE | ISO_COBOL.md §13.18.28 LINE clause NEXT PAGE phrase; grammar reportLineClause (ON? NEXT PAGE \| NEXT PAGE); ReportGroupSymbol.LineNextPage | untested |  | Parsed (LineNextPage) but EmitGroup's nextPage path is driven only by overflow, never by a group's own LINE NEXT PAGE. No baselined test uses it. Propose tests/nist/extra/rw/line-next-page.cob: a body group with 'LINE NUMBER 5 ON NEXT PAGE'; GENERATE it after some details so it is forced to a fresh page; verify it starts page 2 at line 5 with PAGE-COUNTER incremented even though LAST DETAIL was not reached. |
| SOURCE of arbitrary program data in a PAGE HEADING / FOOTING group | ISO_COBOL.md §13.18.53 SOURCE clause; FileIoLowerer.ClassifyPageField returns Kind 3 (blank) for non-counter sources | untested |  | RW104A only sources LINE-COUNTER/PAGE-COUNTER in its page groups; a page heading sourcing a working-storage field (e.g. a report title or run date) would be blanked. Propose tests/nist/extra/rw/page-heading-source-data.cob: a PH with 'COLUMN 10 PIC X(8) SOURCE WS-TITLE' where WS-TITLE='HEADING1'; GENERATE 1 detail; verify the page heading line contains 'HEADING1' at column 10 (currently emits spaces). |
| GENERATE report-name (summary reporting) | ISO_COBOL.md §14.9.19 GENERATE summary form; BoundGenerateStatement.Group (null => report-name) — no summary lowering | untested |  | GENERATE of a report-name (vs a group-name) must perform all control-break processing and present CONTROL FOOTINGs without printing a detail. No baselined test uses it, and LowerGenerate only emits gen.Lines (group lines). Propose tests/nist/extra/rw/generate-summary.cob: CONTROL DEPT, CF DEPT with SUM AMT, several details with AMT credited via UPON, then 'GENERATE THE-REPORT'; verify only the CONTROL FOOTING totals are produced (no detail line) and totals are correct. |
| USE BEFORE REPORTING report-group-name (declarative) | ISO_COBOL.md §14.x USE BEFORE REPORTING; BoundNodes.cs IsBeforeReporting + report name (parsed); no declarative dispatch before group presentation | untested |  | The USE statement parses BEFORE REPORTING with a report-group name and a flag, but nothing dispatches the declarative procedure immediately before a group is presented. No baselined test. Propose tests/nist/extra/rw/use-before-reporting.cob: a DECLARATIVES section 'USE BEFORE REPORTING CH-GROUP' that MOVEs a computed value into a SOURCE field and/or suppresses the group via the suppression mechanism; GENERATE details that trigger the CH; verify the declarative ran (the injected value appears) exactly before each presentation of CH-GROUP. |
| CODE literal (per-line report code prefix) | ISO_COBOL.md §13.18.12 CODE clause; grammar reportCodeClause; ReportSymbol.CodeValue | untested |  | CODE is parsed into ReportSymbol.CodeValue but never prepended to emitted lines by the runtime. No baselined test uses CODE. Propose tests/nist/extra/rw/code-clause.cob: 'RD ... CODE "AB"'; GENERATE 1 detail and present a page heading; verify every report line written to the file begins with the 2-char code 'AB' followed by the composed line. |
| RD ... IS GLOBAL | ISO_COBOL.md §13.18.23 GLOBAL clause; grammar reportGlobalClause; ReportSymbol.IsGlobal | untested |  | Parsed but no cross-program report sharing is wired (GLOBAL report visibility in nested programs). No baselined RW test is a nested-program. Propose tests/nist/extra/rw/global-report.cob: an outer program with 'RD ... IS GLOBAL' and a contained nested program that INITIATEs/GENERATEs/TERMINATEs that report; verify the nested program drives the outer report and a single coherent output file results. |

---
**Total untested/partial/unknown features = 92** (the WS-SPEC implementation backlog).

