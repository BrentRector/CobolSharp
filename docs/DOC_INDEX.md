# COBOL.NET — Documentation Index

> ⛔ **ONE WORK REGISTER: `kb/Work/`.** `python scripts/spec/work.py next` answers "what now"; `kb/Work.base` is the view. **Keep it current in the SAME change set as the work**, and **never start a list, table, tracker or "remaining work" section anywhere else** — five registers accumulated by 2026-08-04 and three each claimed to be canonical (CLAUDE.md rule 8).


> **The map of the surviving docs.** Referenced by `CLAUDE.md`. **Keep in sync:** when you add, retire, or materially
> change a doc, update its row here. There is exactly **one canonical doc per subsystem** — extend it, never fork a second.
>
> ⛔🔥 **The project is a blank-slate COBOL→C#/Roslyn rewrite (COBOL.NET, `src/Cobol.Net.*`).** The authoritative design
> is **`docs/COBOLNET_DESIGN.md`** (the decision-complete SSOT). The pre-PIVOT byte-engine plan + architecture docs
> (MASTER_PLAN, PROJECT_PLAN, the DATA_MODEL_* / RECORD_STRUCT migration docs, the ~50 `CobolSharp …` architecture/guide
> docs, the CIL-emitter/binder decomposition docs, the byte-engine plans/audits, and the extracted spec excerpts) were
> **removed** as obsolete + misleading. Backend = **C# source via Roslyn (primary)**
> behind the backend-neutral `ICodeGenBackend` over ONE bound tree (`--backend roslyn|cil`; a Cecil/CIL backend is
> future-additive with its OWN private structure→branch lowering — NO shared lowered IR); numerics are native scaled
> `long`/`Int128` (+ IEEE `float`/`double` for COMP-1/2; no `decimal`/`BigInteger`); the legacy `CobolSharp.*` engine
> survives in `src/` ONLY as a
> differential oracle until cut-over (G8). The ISO spec is the submodule **`specs/ISO_COBOL.md`** (authoritative — the
> extracted excerpts were removed as redundant).

**Type legend:** **LIVE** = binding, keep current · **DESIGN** = target design (banner shows real status) ·
**LEDGER** = catalog/record · **SPEC** = ISO reference.

## Start here (live kickoff + plan)

| Doc | Type | Subject |
|---|---|---|
| `docs/COBOLNET_DESIGN.md` | LIVE | **The SSOT.** Decision-complete design for the rewrite: locked invariants (§1), cross-cutting (§14), settled decisions (§18), the G0–G8 build order, and pointers to the per-subsystem deep-dives. |
| `docs/COMPLETION_ROADMAP_COUNCIL.md` | LIVE | **RATIFIED — the EXECUTION ROADMAP for G7→G8** (validator waves → M2 OO/residuals → M3 → M4 → matrix closure → G8 in 3 cuts), phase-by-phase with exit criteria. Upholds the SSOT §16 spine + 7 ratified deltas (its §3); its §5 decision packet is owner-resolved (all council defaults; #1 = no standards acquisition — the in-repo 2023 spec is the sole ISO authority). Read via the plan's §0 banner (largely subsumed by the plan; retained for the ratified-decision record). |
| `docs/COBOLNET_REARCHITECTURE_PLAN.md` | LIVE | **THE ONE PLANNING DOCUMENT (owner-directed consolidation 2026-07-19) — read §0 FIRST every session; update §0 before ending.** Absorbed and replaced: `resume-prompt.md`, all 17 `PHASE-00..16-*.md` step-by-steps (live detail in its Part II; completed records in Part III), `PHASE-13-audit.md` (+ scout JSON), `PLAN-bindtime-gating-migration.md`. Contains: the §0 live resume state · mission + D13 (100% CONFORMING) · north-star + principles + the dual-backend mandate · the §3 execution model · the §4 phase index · §6 owner decisions D1–D20 · §7 backfill refinements · the §8 consolidated residue ledger · §9 verification/corpus mechanics · the §10 doc map. Still separate: the DESIGN-* deep-dives (design SSOTs), the review/scout LEDGERS (`PHASE-13-plan-vs-spec-review.md` = the fix SSOT via its §24 queue; `PHASE-11/12-scout-notes.md`), SURVEY/CRITIQUE (frozen analyses, delete at P15), DEVLOG (the only history). |
| `CLAUDE.md` | LIVE | Project instructions / agent playbook (points here + to the SSOT). |
| `docs/DOC_INDEX.md` | LIVE | This file — the doc map + maintenance guide (one row per surviving doc; keep in sync). |
| `DEVLOG.md` | LIVE | Narrative log of decisions/failures/breakthroughs. **DESCENDING — newest `## Entry` first**, with a `YYYY-MM-DD HH:MM TZ` stamp. |
| `CONSTRAINTS.md` | LIVE | Doctrine: anti-patterns, process rituals (some examples are byte-engine-era — the rules generalize). |
| `PROMPT.md` | LIVE | Doctrine + the anti-pattern catalog (multi-session continuity; C# 14 / .NET 10 **or later** — a .NET 11 upgrade is pre-authorized when it helps the goals). |
| `README.md` | LIVE | Repo front page. |

## COBOL.NET design corpus — one canonical deep-dive per subsystem

| Doc | Type | Subject |
|---|---|---|
| `docs/COBOLNET_ARCHITECTURE.md` | DESIGN | Brief overview of the greenfield architecture (companion to the SSOT). |
| `docs/COBOLNET_PIPELINE_DESIGN.md` | DESIGN | The compile pipeline (6 phases; edition gating is its OWN pass): Frontend (preprocess → superset ANTLR parse) → Bind (edition-agnostic; bound tree = ALL semantics) → `VersionConformancePass` (the sole edition gate; HALT on errors — emit is unreachable on an errored tree) → Desugar → `ICodeGenBackend` (Roslyn C#-emit primary; CIL future-additive) → Roslyn compile — emitters only render. |
| `docs/COBOLNET_DATA_MODEL_DESIGN.md` | DESIGN | Typed-native data model: groups→`record struct`, elementary→native fields, OCCURS→`T[]`, OCCURS DYNAMIC→out-of-line `CobolDynTable<T>` (D9), TYPEDEF/TYPE clause→a template registry + subtree clone (D17), `Place`/`ReferenceResolver` — including **D18**, a function-identifier in a subscript / ref-mod position materializing into a compiler temp hoisted as a statement pre-op (deleted by D10 at PHASE 15). **Also owns the one data-division section that is deliberately NOT modelled — the SCREEN SECTION, root of the declined Annex A.4.2 module (COBOLNET1560 / 1707, `ScreenFacility`): why an unclaimed A.4 module is an ERROR where an A.3 processor-dependent facility is a warning, the six refused source shapes, and the §8.10 screen words that stay legal user-defined words.** |
| `docs/COBOLNET_NUMERIC_DESIGN.md` | DESIGN | Native scaled-integer numerics (`CobolNum`): scale/round, `TryStore`, ON SIZE ERROR, signed-DISPLAY. |
| `docs/COBOLNET_CONTROL_FLOW_DESIGN.md` | DESIGN | The PC dispatcher (`__Dispatch`): GO TO / DEPENDING / PERFORM (THRU/TIMES/UNTIL) / EXIT. |
| `docs/COBOLNET_REDEFINES_DESIGN.md` | DESIGN | REDEFINES/RENAMES — the 4-tier one-canonical-backing model. |
| `docs/COBOLNET_STRING_OPS_DESIGN.md` | DESIGN | INSPECT / STRING / UNSTRING / reference-modification (`CobolStrings`). |
| `docs/COBOLNET_FILES_DESIGN.md` | DESIGN | File I/O — connectors, FILE STATUS, OPEN/CLOSE/READ/WRITE/REWRITE state machines (§9.1.13). **D10 carries the persisted §9.1.6 FIXED FILE ATTRIBUTE catalog** — the `.cbattr` sidecar written when the OPEN statement creates a file (§14.9.27.4 GR18/GR17) and compared on every later OPEN (GR10 → I-O status '39'); the Annex A.1 item 129 validated set it realizes is documented in `docs/CONFORMANCE.md` §7. |
| `docs/COBOLNET_INTERPROGRAM_DESIGN.md` | DESIGN | CALL / interprogram linkage / nested programs. |
| `docs/COBOLNET_OO_DESIGN.md` | DESIGN | OO — classes/methods/INVOKE as typed-native .NET. |
| `docs/COBOLNET_OO_SLICE_BRIEFS.md` | LEDGER | OO — workflow-regenerated implementation briefs for the remaining slices (FACTORY / OVERRIDE-FINAL / universal / EC-OO / INTERFACE+PROPERTY); decisions fold into the OO deep-dive per slice. |
| `docs/COBOLNET_CONDITIONS_EXCEPTIONS_DESIGN.md` | DESIGN | Conditions + the EC exception model / declaratives. |
| `docs/COBOLNET_INTRINSICS_DESIGN.md` | DESIGN | Intrinsic FUNCTION resolution + semantics. |
| `docs/COBOLNET_REPORT_WRITER_DESIGN.md` | DESIGN | Report Writer — the `CobolReport` RWCS engine, compose-at-presentation lines, counters, CONTROL/SUM, USE BEFORE REPORTING (closes the SSOT's RW seam flag). |
| `docs/COBOLNET_PROJECT_ORG_DESIGN.md` | DESIGN | Project/folder organization + the Cobol.NET / `cobol.exe` naming (G0 executed; G8 namespace big-bang pending). |
| `docs/COBOLNET_VALIDATION_DESIGN.md` | DESIGN | **Diagnostics + the §8.9 reserved-word funnel:** the strict/permissive channels + `Removed()` seam, the 0900–0903 band, the four-source reserved-word tables + the cobolWord funnel, the ConstructDialectStatus registry + drift disciplines, the corpus runner shells, and the measurable G7 exit criteria. Edition GATING itself lives in the ONE `VersionConformancePass` — the canonical mechanism doc is `docs/rearchitecture/DESIGN-version-conformance-pipeline.md`. |
| `docs/rearchitecture/EVAL-antlr-leverage-and-traversal.md` | DESIGN | **Architecture evaluation (owner-requested, 2026-07-11): are we leveraging ANTLR4 / reinventing the wheel?** Verdict: the `CST→bound-tree→passes→emit` design is SOUND (mirrors Roslyn), but the traversal machinery is not — NO shared bound-tree visitor (205 duplicated `case Bound` arms across ~5 bespoke walkers → the `UsageCollectionPass` completeness bugs), a 50-arm hand-rolled binder dispatch + 334 `GetText()` pokes, zero ANTLR listener use, no `SymbolTable`. Recommendation = TOOLING-FIRST resequence (source-generated visitor [P7 Step 6] + `SymbolTable` [P6] forward); drives `COBOLNET_REARCHITECTURE_PLAN.md §4.1`. [[project_leverage_antlr_roslyn_tooling]]. |
| `docs/rearchitecture/DESIGN-version-conformance-pipeline.md` | DESIGN | **Version conformance (edition gating) — the canonical mechanism doc:** superset parse (committed-match construct-id annotation; the two load-bearing forward-detects) · edition-agnostic bind · ONE `VersionConformancePass` over the bound tree (the sole edition-gating funnel — all 88 compiler-embedded `ConstructRegistry.Check` sites + the §8.9 reserved-word funnel) · emit-if-clean; §5 execution stages 1–3 (residue migration → delete the recogniser → the pipeline skeleton). |
| `docs/rearchitecture/DESIGN-locale-facility.md` | DESIGN | **The LOCALE facility (ISO Annex A.4.9) — IMPLEMENTED WHOLE (adopted 2026-08-18; T1–T7 all landed, the claim completed by T6 2026-08-28; kb/Work PB64):** the five seams (`LocaleSymbol` / `LocaleRef` / `LocaleState` on `RunUnit` / the ONE `CobolCollation` abstraction / `LocaleFacts` over .NET `CultureInfo`), the twelve implementor determinations L1–L12 (external identification, the two defaults from `COBOL_USER_LOCALE` / `COBOL_SYSTEM_LOCALE`, saved-locale handles, one sequence for both classes, EC-LOCALE-INCOMPATIBLE, ORD/CHAR order vectors, indexed keys, simple case mapping, d_fmt/t_fmt), the .NET mapping table with its documented limits, the seven-increment migration plan T1–T7, and the four owner decisions of 2026-08-18 — including the verbatim ISO/IEC 14651 conformance statement for STANDARD-COMPARE. |
| `src/Cobol.Net.Runtime/Collation/README.md` | LIVE | **The collation subsystem (kb/Work PB101, PB105, PB106):** the DERIVED multi-level collation table generated from Unicode CLDR/UCA data (`scripts/collation/generate-collation-table.py` over `data/unicode/`, provenance in `data/unicode/SOURCES.md`; format 2 with the CLDR reordering groups and case bits), why not `CompareInfo`, the engine (`Collator` with `CollationOptions` — strength, alternate/maxVariable, caseFirst, backwards secondaries — / `CollationKey` / `CollationEngine`), the two tailoring layers (the CLDR collation of the tag, then the `.tailor` site override), how the compiler reaches it and where the key cache is wired, verification, regeneration (all three artifacts), the pipeline at a glance. |
| `src/Cobol.Net.Runtime/Unicode/README.md` | LIVE | **The Unicode normalization subsystem (`CobolNet.Runtime.Unicode`):** the public `UnicodeNormalizer` (`Normalize` / `IsNormalized` / `CompareNormalized` / `IsNfcAvailable`) over the two canonical forms — why normalization precedes comparison (canonical equivalence: precomposition, mark order, Hangul), NFC vs NFD and why the compatibility forms are refused, the ONE NFD (the collation engine's table-driven decomposition — host-independent, the derived table's Unicode version) and where the engine already normalizes by itself, the host dependence of NFC with its invariant-globalization fallback, and the upgrade path (composition data + `Full_Composition_Exclusion` into the generated table). |
| `src/Cobol.Net.Runtime/README.md` | LIVE | **The runtime's text-processing overview (kb/Work PB101/PB104–PB106):** the six subsystems side by side — collation engine, CLDR locale loader, locale selection, key cache, normalization, grapheme segmentation — with the collation, normalization, segmentation and locale PIPELINES drawn, the CLDR and cache integration rules, and the initialization/wiring (`CollationRuntime.Initialize` from every `RunUnit`, `Warmup`, what the compiler does and does not touch). Start here for "how do these fit". |
| `src/Cobol.Net.Runtime/Collation/CLDR/README.md` | LIVE | **The CLDR locale loader (kb/Work PB105):** what CLDR is and what it gives collation (rules, not weights), the complete rule syntax the parser reads (with what is applied and what is reported), the JSON mirror, sources and the fallback chain (CLDR `parentLocales` per LDML — the collations component; nb → no; zh_Hant → zh), the builder (rules → weights: insertions, renumbering, script reordering, canonical closure, case bits, settings), the ICU cross-check, the residue (caseLevel / numericOrdering / hiraganaQ / `<<<<`), and how it integrates with `CollationTable`, `CollationEngine.ResolveLocale` and `LocaleManager`. |
| `src/Cobol.Net.Runtime/Collation/Cache/README.md` | LIVE | **The collation key cache (kb/Work PB106):** the per-collator `CollationKeyCache` (LRU / size-based eviction, `CacheConfig`, environment variables), the measured rule of where keys pay (SORT/MERGE key columns, INDEXED LOCALE keys) and where the streaming comparison wins (relation conditions), and why keys are per text. |
| `src/Cobol.Net.Runtime/Unicode/Segmentation/README.md` | LIVE | **Grapheme cluster segmentation (kb/Work PB104):** UAX #29 extended grapheme clusters from the derived property table (`GraphemeBreaker` / `GraphemeEnumerator` / `GraphemeCluster`), the rules in the engine's words, the full `GraphemeBreakTest.txt` verification, and why segmentation matters to collation and normalization (clusters survive normalization; cluster-safe truncation keeps a collation prefix; keys are NOT per cluster — contractions cross cluster boundaries). |
| `docs/rearchitecture/DESIGN-compile-time-expressions.md` | DESIGN | **Compile-time expression evaluation (§7.3.6 arithmetic / §7.3.7+§8.8.2 boolean / §7.3.8 constant-conditional) — the ONE shared evaluator (ledger C2):** the `CompileTimeExpressionEvaluator` used by BOTH the frontend conditional-compilation stage (directive operands, fragment-parsed) and the CONSTANT-entry binder; the `BooleanExpressionResolver` (§8.8.2 rule-7b precedence incl. the context-inherited shift, shared with runtime COMPUTE-Format-2); the ANTLR fragment/cce grammar + the `PrimeDirectiveExpr` lexer flag; GR5/GR3 at the public boundary; code-preserving diagnostics + per-consumer citations. ANTLR-for-all-parsing (no hand-rolled parser); no deferrals. |
| `docs/rearchitecture/DESIGN-flag-directives.md` | DESIGN | **FLAG-02 / FLAG-14 migration-flagging directives (§7.3.14 / §7.3.15) — IN PROGRESS (P13 Wave D):** the ONE shared flagging subsystem — a frontend-collected per-option `FlagState` (the `>>TURN`/`>>REF-MOD-ZERO-LENGTH` toggle-fold template), a dedicated post-bind `FlagConformancePass` (sibling to `VersionConformancePass`, reusing the drift-proof `StatementChildren()` traversal), and the two frontend-inline detectors (compile-time-arithmetic, `>>EVALUATE`) that have no bound residue. Carries the adversarially-verified per-option census (FLAG-14 ALL+12, FLAG-02 ALL+5) mapped to GR4 sub-rules + Annex E.2 items + detection sites, the directive-word edition gates, diagnostics COBOLNET1620/1621 (Warning), and the 5-increment plan. Drives VCR **Table 5**. |
| `docs/rearchitecture/DESIGN-cobol-words-directive.md` | DESIGN | **`>>COBOL-WORDS` directive (§7.3.10; Annex D.12; E.3.3 item 12) — IMPLEMENTED (P13 Wave D):** the per-compilation-group reserved/context/intrinsic word-table modification. The `CobolWordsMap` override carrier (Editions), a post-lex `CobolWordsRewriter` (EQUATE/UNDEFINE/SUBSTITUTE token retyping) + the map-aware lexer data-name gate, a composed `ReservedWordSet` (RESERVE/UNDEFINE), and `IntrinsicBinder` synonym resolution for function names. All four options for reserved, context-sensitive, AND intrinsic-function words; SR1–SR5 enforced; COBOLNET0900 gate + COBOLNET1623. |
| `docs/rearchitecture/DESIGN-cc-in-copy.md` | DESIGN | **CC-directives-inside-COPY (§7.2.1) — IMPLEMENTED (P13 Wave D):** the merged interleaved text-manipulation driver — `ConditionalCompilationProcessor.ProcessWithCopy` fuses the CC branch-selection state machine with COPY expansion so directives INSIDE copybooks are processed (Step 1 incorporate → Step 2 CC over the expanded group), while a main-source `>>IF` still gates a COPY and an omitted-branch COPY is never expanded (false-branch missing copybook raises no error). Shared directive state threaded through recursive COPY; legacy `Process`/COPY byte-identical. |
| `docs/DIAGNOSTICS.md` | LEDGER | **Generated — do not hand-edit.** The index of the first-class diagnostic descriptors: code · stable id · severity · ISO § · suppress key · title. Source of truth = `src/Cobol.Net.Editions/Diagnostics/DiagnosticCatalog.cs`; regen `pwsh scripts/gen-diagnostics-doc.ps1`; drift-guarded by `DiagnosticRegistryDriftTests`. |
| `docs/rearchitecture/DESIGN-ec-oo-superbatch.md` | DESIGN | **The EC-infra + OO super-batch coordinated fix plan (DESIGN, not yet implemented):** the 13 remaining EC/OO conformance findings (CA9/10/11/12/V57 · CA21/22/V58 · CA29/30/V55 · CA37/38) that share the EC dispatch scaffold. §1 maps the scaffold (EcBinder/EcEmitter/ExceptionState) + the canonical fatal/nonfatal EC-gate recipe; §2 the synthesis — the shared fix pattern, 5 tracks (E/C/D parallel-safe, serial EC chain A + CA12 last), an 11-step commit plan, risks + 3 owner decisions; §3 per-finding re-verification + delta + anchors. From anchor re-scout `wf_d20dadb7-de9`. |
| `docs/rearchitecture/DESIGN-spec-conformance-review.md` | DESIGN | **The FULL implementation↔spec conformance review (P14 Step-0, spec-first) — the definition of DONE (D13):** decision-complete methodology for the EXHAUSTIVE, traceable review of every normative ISO rule → implementation → verified verdict → spec-derived test. Phase A = the spec-rule catalog (denominator); Phase B = map+verify (the traceability inventory, the GAP burn-down); Phase C = close DIVERGES(§24)/NOT-IMPLEMENTED/untested-CONFORMS. **§5 is the EXECUTION-MODEL doctrine and the only place it is written** (owner decision `kb/Work/PB278`, 2026-09-01): adjudication and fixing are TWO CONCURRENT LANES — the adjudication lane read-only in a pinned worktree, one batch ahead — plus three cheaper row-shape lanes ordered by rows-closed-per-token, the dossier rule (§5.2, agents verify rather than search) and the cluster-sized fleet (§5.3). Motivated by the differential-blindness lesson; the two audits below are its first installment. |
| `docs/rearchitecture/evidence/PROCESS-REVIEW-2026-09-04.md` | EVIDENCE | **Frozen record of the 2026-09-04 burn-down process re-evaluation** — the owner's "is there a faster process to zero GAP?" question, the measured data brief and the three-proposal synthesis reproduced verbatim (earned rate 69 rows/day not 138; evidence derived twice; the fix lane under 15 % utilized; `tokens ≈ 0.115·T + 0.00031·T²`, n = 239), and the three owner decisions taken on it: **witness-first adjudication DECLINED — the lanes stay separate** (independent derivation of evidence), 160/220-turn caps ADOPTED, five-cluster landing trains ADOPTED. ⛔ Frozen evidence: never edited to stay current. The CURRENT process is `.claude/skills/workstream/SKILL.md`; the six questions still owed the owner are `kb/Work/PB468`; the measurement is `kb/Work/PB469`. |
| `docs/rearchitecture/DESIGN-SPEC-RECONCILIATION.md` | LEDGER | **Design-doc↔spec audit SSOT** (`wf_480d50f5` + correction `wf_16d53d4e`): the 54 doc↔spec conflicts across 13 docs (doc side CORRECTED spec-faithful) + the 6 spec-wrong-AND-implemented code-bugs routed to §24 V54–V59. |
| `docs/rearchitecture/CODE-SPEC-AUDIT.md` | LEDGER | **Code↔spec audit SSOT** (`wf_4ce42db6`, 14 behavioral areas): CA1–CA38 candidate conformance bugs — the implementation checked against the ISO spec rule-by-rule (the first spec-first oracle). Verify-then-fix; feeds §24. |
| `kb/Work/` + `kb/Work.base` | **REGISTER** | ⛔ **THE ONE PLACE WORK IS AUTHORED (2026-08-04).** One note per item — defect · analysis · adjudication · decision — tracked in git, frontmatter carrying `kind`, `status`, `severity`, `area` and the harm flags; the forensic prose lives in each note's body. It replaced FIVE overlapping registers, THREE of which each declared themselves canonical (plan §0 NEXT, `CONFORMANCE-FIX-QUEUE.md`, `kb/Remaining Work Tracker.md`) plus the §11 analysis backlog and the queue's never-itemised RESIDUE block — which hid a WRONG-ANSWER defect no work list could see. `kb/Work.base` is the view; **`Fix next`** ranks on what a defect DOES to a user's program (`not landed AND (wrong-answer OR crashes) AND not blocked`), because severity cannot separate PB24 from PB39 — both are `[MAJOR]`. `python scripts/spec/work.py next` prints it; session-probe shows it every session. ⚠ **Never quote a count from this table** — run `work.py stats`. |

## Version-correctness (multi-edition)

> ⛔ **Cross-cutting obligation:** `cobol.exe` is FOUR compilers in one (`--std` 1985/2002/2014/2023). Every subsystem
> deep-dive above must state, for each edition-varying construct it designs, BOTH the per-edition behavior AND the
> diagnostic emitted by every edition that lacks it (not-yet-introduced or removed), keyed to the rows of
> `VERSION_CHANGE_REFERENCE.md`. A deep-dive that designs a post-1985 feature without its pre-introduction diagnostic
> is incomplete.

| Doc | Type | Subject |
|---|---|---|
| `docs/VERSION_CHANGE_REFERENCE.md` | LEDGER | Checklist of every edition-to-edition change documented in the 2023 spec (Annex E.2/E.3, Annex F, FLAG-02/14, NOTES) — drives version-gating. **Status is DERIVED:** each row carries a `<!-- gate:id -->`/`ref-only`/`pin-to-spec`/`todo` anchor; the "Gating status index" is a GENERATED block (`pwsh scripts/gen-vcr.ps1`; `VcrDriftTests` guards it). Edit rows/prose by hand, never the generated block. |
| `docs/VERSION_TEST_MATRIX_DESIGN.md` | DESIGN | Test the compiler as N per-edition compilers: the (construct × edition) matrix, the 3 invariants, the rollout (Phase 0 done). |
| `docs/CONFORMANCE.md` | LIVE | **The §4.2.16 conformance record + implementor's user documentation.** Per-item disposition of the Annex A.3 processor-dependent elements (claimed / not claimed), the pinned §4.2.6 behavior determinations, and the four documented-non-support facilities (MCS, commit/rollback, VALIDATE, screen). The catalogue behind the COBOLNET1560-band §4.2.6 warnings. Keep in sync with the supported surface. **Now also carries the STORAGE-REPRESENTATION family (Annex A.1 items 205 · 206 · 207 · 208 · 211 · 215) — the width ladder, radix, byte order and a worked byte example per usage, i.e. the documented user-facing guarantee of what lands on disk (V59) — and the §15.4.1/§14.9.11.4 GR1 determination for a numeric FUNCTION's text image (DA2).** ⚙ **§7's rows are keyed by the inventory rule-id `DOC-A.1-<n>`, not the bare item number** — that key IS the anchor the traceability row computes, so a determination cannot be filed under a neighbouring item; the token appears nowhere else in the file and `audit_annex_a1.py` fails if it does. Each row's `Pinned by` cell names the spec-derived test(s) machine-readably and must agree with the inventory row's `test-ref`. **§8 is a SECOND register in the same file** — the owner-signed DERIVATIONS that stand in place of a spec-derived test for a rule carrying no observable obligation (`kb/Work/PB386`, 2026-09-03), keyed `DRV-<rule-id>` and read by the same one-parser-per-language, which now stops at the next `## ` heading rather than at end of file. |
| `docs/GnuCOBOL extensions.md` | LIVE (register) | **Non-ISO constructs COBOL.NET does not support**, found by running the GnuCOBOL testsuite through the compiler (plan §11 A4 / P14 Step 13). Exists so that adopting any of them is a deliberate future decision, not an accident; NOTHING in it is scheduled — the mission is ISO-2023 ×4 (D13) and every row is outside that target. ⚠ Rows carry a CONFIDENCE column: **NEEDS VERIFICATION** rows are unadjudicated and some may prove to be ISO constructs we wrongly reject (i.e. OUR bugs) — two such have already moved out of it. Also carries the opposite-direction table (source we wrongly ACCEPT), which seeds §11 A2. Never cite it as authority that something is non-ISO until its row says CONFIRMED. |

## Conformance feature catalog

| Doc | Type | Subject |
|---|---|---|
| `docs/ISO2023_CONFORMANCE_PLAN.md` | LEDGER | The M2/M3/M4 feature catalog (post-85 features to implement). ⚠ Its data-model-migration framing is obsolete (banner inside) — use it only for the feature list. |
| `docs/PHASE4_RECONCILIATION.md` | LEDGER | **The GREENFIELD-truth view of the M2/M3/M4 catalog** (the ratified Phase-4 entry audit): per-item LANDED/PARTIAL/STAGED-LOUD/NOT-STARTED/OBSOLETE with evidence + per-track wave sizing. SUPERSEDES the catalog's legacy ☑/◐ marks. Keep in sync as Phase-4 tracks land. |

## ISO specification (authoritative)

| Doc | Type | Subject |
|---|---|---|
| `specs/ISO_COBOL.md` | SPEC | The ISO/IEC 1989:2023 COBOL standard, transcribed. The authority for all syntax/semantics/behavior — cite the §. **PUBLIC** (in this repo). Opens with a Preface carrying ISO's acknowledgment, and closes with an **Addendum listing every correction with the printed form**, so each is reversible. **PAGES ARE GONE** — a page is a typesetting artifact with no meaning in Markdown, so every cross-reference is an intra-document `#section-N-N` link and anything page-keyed must be re-keyed onto the clause hierarchy. Figures are GENERATED from the printed page, never hand-drawn. |
| `specs-private/…​.pdf` | SPEC | The licensed PDF, **private submodule** — per-copy licence, not redistributable. `git submodule update --init specs-private`. Needed only by the tools that MEASURE the printed page. |
| `docs/rearchitecture/spec-reconciliation/PDF-TEXT-LAYER.md` | REFERENCE | Why the PDF's text was unreadable (missing `/ToUnicode`, NOT obfuscation) and how it was recovered. Read before touching `pdf_deobfuscate.py`. |
| `docs/rearchitecture/spec-reconciliation/FIGURE-STYLE.md` | DESIGN | How a general format is DRAWN — glyph family, brackets vs braces, bar spacing, minimum row count, and the `line-height: 1` requirement. Settled by rendering, not reasoning; several rules are counter-intuitive. Read before changing `render_figure.py`. |
| `docs/rearchitecture/spec-reconciliation/TRANSCRIPTION-STATE.md` | PLAN | **Start here for transcription work.** What is done, what is next (Figure D.6), the gate commands with their last-known numbers, and the six rules for drawing an Annex D figure. Plan §0 stays the project SSOT; this is the detail behind its spec bullets. |
| `docs/rearchitecture/spec-reconciliation/REPAIR-PLAN.md` | PLAN | The transcription repair batches, their order and mechanism, and the measure-don't-squint rules. |
| `docs/rearchitecture/spec-rule-catalog.json` | LEDGER | **Generated — the P14 DENOMINATOR.** Every normative rule extracted from the standard with its FULL TEXT: **3,861 items** (1352 SR · 1513 GR · 226 AR · 226 RV · 222 Annex-A.1 doc obligations · 322 general formats), validated against the PDF and cross-checked against the spec's own TOC. Regen `python scripts/spec/extract_rule_catalog.py`. Rows are CLAUSE-keyed (no page field), so the extractor runs post-de-paging. It reports any rule-shaped heading its KINDS map cannot resolve, and declares the two deliberate dispositions (`EXCLUDED_BLOCKS` §5.3 · `KNOWN_EMPTY_BLOCKS` §13.18.40.6) so an expected condition is never reported as a warning. |
| `tests/version-matrix/traceability-inventory.json` | LEDGER | **The P14 burn-down SSOT — one row per catalog rule, and its GAP count IS the definition of v1.0** (D13: zero GAP = P14 done). Seeded by `build_inventory.py`; verdicts recorded ONLY by `record_verdicts.py`; never hand-edited. `session-probe.ps1` reports the live count. Schema + mechanism: `DESIGN-spec-conformance-review.md` §4 + §8. |
| `tests/version-matrix/inventory-schema.json` | LEDGER | **The verdict rules, as DATA in one place** — the six verdicts, each one's `resolves` flag and required evidence, the legal editions, the `code-location` pattern and the `test-ref` forms. Read by all three engines (`build_inventory.py`, `record_verdicts.py`, `SpecTraceabilityInventoryDriftTests`) so none carries a copy. Adding a verdict or a test-ref form is an edit to this file and nothing else. **Also carries `kinds` — the PER-KIND evidence rules, orthogonal to the verdicts**: `kinds.DOC` gives an Annex A.1 row the register anchor it must carry, COMPUTED from its own rule-id (`docs/CONFORMANCE.md#{rule-id}`), and the `^src/Cobol\.Net\.` predicate that decides whether it names anything observable. Plus `code-location.anchored-files`, which holds a file whose fragments are a named anchor space to that space (`docs/CONFORMANCE.md` → §7's `DOC-A.1-<n>`, §1's `4.2.16`, §5's `A.4.<n>`) and forbids citing it bare — the mechanism that retired eight live rows' `#7` and bare-path citations. ⚠ Whether a row OWES its computed anchor is decided by the REGISTER, not by the verdict (`anchor-exempt-verdicts` names the verdicts that may be exempt; §7 decides which are): a withdrawn A.1 item has no determination to point at, a documented decline does. **And `derived-verdicts`** — a verdict that follows mechanically from one owner determination, written as a predicate both `derive_verdict_batch.py` and `DerivedVerdictDriftTests` read; its `determination-prefix` arm reads `docs/CONFORMANCE.md` §7 through the one parser per language (`inventory_schema.section7_rows` / `tests/_shared/ConformanceRegister.cs`). **And `derivation`** — §1.1's owner-signed ALTERNATIVE to a spec-derived test, for a rule that carries no observable obligation (owner decision `kb/Work/PB386`, 2026-09-03): the record field, the COMPUTED anchor `docs/CONFORMANCE.md#DRV-{rule-id}`, the §8 register heading, the literal owner signature and the three arms with their `names-pattern` + `check` (`undefined-A.2` mechanical against `tests/version-matrix/annex-a2-undefined.json`; `unpopulatable-antecedent` and `indistinguishable-consequent` shape-checked reviewed arguments). Removing the object closes the door in both engines at once. |
| `scripts/spec/build_inventory.py` + `inventory_schema.py` | TOOL | Seeds/refreshes the inventory from the catalog, PRESERVING every adjudicated verdict across a rebuild (Phase B spans many sessions), and reports the GAP by top-level clause — the P14 work map. `inventory_schema.py` is the shared loader: it parses the schema, derives `state`, and owns the ATOMIC inventory write. Neither ever writes a verdict. |
| `scripts/spec/phase_b_batch.py` | TOOL | Prepares a Phase-B adjudication batch: one self-contained input file per SUBJECT holding the full text of its rules, plus the slug list for the workflow. Excludes rules that already carry a verdict, so a re-run after a partial batch is safe. ⛔ One agent, one input file — never "read the shared catalog and take element k" (feedback_agent_dispatch). The batch PLAYBOOK — the fan-out shape and the nine things a batch prompt must carry — is `DESIGN-spec-conformance-review.md` §9. |
| `scripts/spec/record_verdicts.py` | TOOL | **The only way a verdict enters the inventory.** Merges a batch file all-or-nothing after validating its SHAPE (rule-id real · verdict in vocabulary · required evidence present · editions legal · each reference parses), then points at the gate. Deliberately does NOT check that references RESOLVE — that is the gate's job, because it must keep holding as the tree changes under an already-recorded row. `--dry-run` reports the GAP delta without writing. |
| `SpecTraceabilityInventoryDriftTests` (`tests/Cobol.Net.Tests.Unit/`) | GATE | The battery gate that makes the burn-down honest: the inventory covers the catalog exactly, `state` is RECOMPUTED rather than believed, every verdict carries its required evidence, every `code-location` names a file and a symbol that still exist, and every `test-ref` resolves to a test really on disk. ⚠ Carries its own proof it can fail — every check is run against rows built to break it, plus positive controls. |
| `scripts/spec/relist_index.py` + `measure_index_levels.py` | TOOL | Rewrites the index as a nested list. Sub-entry LEVELS come from the printed indentation, measured into the committed `data/index-levels.json` so the transform works in the public repo without the PDF; alphabetical order is the fallback where the measurement cannot name an entry unambiguously. Gated on word conservation. |
| `scripts/spec/link_table_figure_lists.py` | TOOL | Anchors every `**Table N — …**` / `**Figure N — …**` caption and GENERATES the front-matter lists from them. Generated, because the hand-maintained figure list had 12 of 15 entries pointing at the wrong clause. |
| `scripts/spec/strip_page_rules.py` | TOOL | Removes the `---` rules left behind by de-paging (758 of them). Gated on: no word changes, no heading created or destroyed, and never removing a rule under a paragraph — `---` under TEXT is a setext H2 marker. Front matter is exempt. |
| `scripts/spec/audit_annex_a1.py` | GATE | **Re-derives every A.1 item number in `docs/CONFORMANCE.md` §7 against the register parsed out of Annex A.1**, and reports coverage (the script's own run is the count's owner — 36 of 195 discharged / 159 remain as of 2026-09-02, the denominator being 199 required less the 4 a declined module withdraws; never trust a count written in prose, including this one). ⛔ It exists because nothing checked those numbers, and §7's own preamble calls a wrongly-documented determination worse than an undocumented one — it found one on its first run (the §15.3.3.2 fractional-seconds determination filed under item **87**, which is FORMATTED-CURRENT-DATE; it is item **202**). ⚠ Identity is the CITED CLAUSE, not prose overlap: A.1's headers are explicitly a *paraphrase*, so demanding lexical similarity rejects correct rows — the first draft did exactly that. `--check` gates, `--json` emits one machine-readable line, `--self-test` proves every check can fail. ⛔ **It now RUNS EVERY BUILD** — `AnnexA1RegisterDriftTests` shells it from the Unit project (the `ExternalCorpusPopulationDriftTests` precedent), where until 2026-09-02 it was wired into nothing at all and ran only when a human remembered. Three checks were added with the DOC-row shape: §7's keys are the inventory rule-id `DOC-A.1-<n>`; the COUNT of each such token in the file equals the number of §7 rows keyed by it (a prose mention would otherwise resolve as a filed determination — and a set difference could not see the duplicate); and each row's `Pinned by` cell agrees with the inventory row's own spec-derived `test-ref`, which is what caught item 87 being closed on item 171's test. ⚙ **Its `DOC-A.1-<n>` token invariant counts BOTH registers since 2026-09-03** — §8's derivation rows are keyed `DRV-<rule-id>`, so `DRV-DOC-A.1-19` is a legitimate second occurrence; `check_pins` was widened from "closes on a spec-derived test-ref" to "closes" in the same change set, so a derivation-closed A.1 row does not drop out of the check at the moment it starts moving the burn-down (`kb/Work/PB386`). |
| `scripts/spec/extract_annex_a2.py` + `tests/version-matrix/annex-a2-undefined.json` | TOOL + LEDGER | **Annex A.2 — the standard's own UNDEFINED LANGUAGE ELEMENT LIST — as data.** Each of the 66 items ends in the standard's own citation of the rule it is about (`(14.9.5, CANCEL statement, General rule 11)`), so the item→`rule-id` mapping is DERIVED from the standard rather than typed: the script parses those citations and resolves them against `spec-rule-catalog.json`. It is the mechanical arm of the `derivation` evidence kind — a §8 determination naming `A.2 item <n>` closes a row only if item *n* resolves to THAT row. ⛔ A GENERATED artifact rather than a parse at check time, for two reasons: the C# gate would otherwise need a second markdown parser of the spec, and `EveryRowState_IsDerived_NotAsserted` recomputes 4,311 rows every build. `--check` re-derives and diffs, `--self-test` proves every check can fail, and `AnnexA2UndefinedListDriftTests` runs both every build. |
| `scripts/spec/audit_derivations.py` + `tests/version-matrix/derivation-parity-cases.json` | GATE | **The gate over the `derivation` evidence kind, and the CROSS-LANGUAGE parity fixture.** `--check` runs the one predicate (`Derivation.refusals`) over every live inventory row claiming a derivation, asserting its POPULATION first; `--parity` evaluates 19 fabricated cases whose world is a fabricated §8 register, §7 item set, Annex A.2 resolution and catalog. ⛔ Both engines and the recorded expectation must agree on the derived `state` AND on the refusal CODES — codes, not a boolean, because two evaluators rejecting one row for two different reasons look identical under "it was refused", which is the shape `kb/Work/PB315` hid behind for months. `SpecTraceabilityInventoryDriftTests.TheDerivationRule_AgreesWithTheFixtureAndWithPython` runs the Python engine for real and compares all three. |
| `scripts/spec/gen_ledger.py` (+ `data/ledger.css`) | TOOL | **Renders the owner's live status artifact, “COBOL.NET Conformance Ledger”, from the repo — every figure COMPUTED, none retyped.** Inputs: the traceability inventory · `kb/Work` frontmatter through **`work.py`'s own `load()`/`actionable()`** (imported, never a second copy of the predicate that once hid nine open items) · `audit_annex_a1.py --json` · `docs/CONFORMANCE.md` §2/§4/§5 · plan §0's `⛔ BATTERY REFERENCE — CURRENT` bullet · the VCR anchors · `constructs.json` · the golden corpus · git. ⭐ **The page is stamped with the last commit that touched a MEASURED input, not `git HEAD`** — otherwise a DEVLOG edit or a WIP checkpoint renders a correct page as stale and `--check` never converges. ⭐ The GAP burn-down series is the one thing the current tree cannot recompute, so it is DATA: `docs/rearchitecture/evidence/ledger-trend.json`, one point per battery or GAP-moving landing, appended by a write run and never by a `--check`. The ONLY hand-written part is `ledger-in-flight.md`, inserted verbatim — narrative, and ⛔ never a count the generator can measure and never a work list (rule 8). `--check` diffs the rendered page against `--out` (the `gen_conformance_notes.py` contract, including *absent is not stale*). Refresh = one run + one publish to the artifact's existing URL. |
| `scripts/spec/grammar_findings_to_batch.py` | TOOL | **Makes ONE grammar pass feed BOTH ledgers** (plan §5b step 4). The grammar↔spec audit scopes 1,659 items and the inventory holds FMT 322 + SR 1,352 = 1,674 — the same territory counted twice — so this converts the workflow's findings into a `record_verdicts.py` batch: MATCHES→CONFORMS · DIVERGES→DIVERGES · NOT-IMPLEMENTED→NOT-IMPLEMENTED · **UNCLEAR→nothing** (the absence of an adjudication is not a kind of one). ⛔ It REFUSES to expand a finding across a section's other rules — one observation stamped onto 19 syntax rules is manufactured coverage — so a finding must name the `rule_ids` it checked and the rest stay GAPs. `--coverage` names them. ⚠ An UNCLEAR rule counts as NOT adjudicated: the first draft counted it as covered, so a pass deciding nothing reported full coverage. `--self-test` covers 9 cases. |
| `scripts/spec/clause_page.py` | TOOL | **Resolves a CLAUSE to the printed page that carries it** — the inverse de-paging left missing. Every other figure/geometry tool runs page→clause; nothing ran the other way, because the rule catalog used to stamp a `page` on every rule and callers just read it. That field is gone (plan §0: rows are CLAUSE-keyed), which silently broke `.claude/workflows/spec-grammar-conformance.js`. `--render` renders the resolved page(s). ⛔ It EXITS NON-ZERO on a clause that does not resolve: the lookup it replaces returned EMPTY for a clause that plainly exists, and the agent proceeded with no page at all. Reuses `render_figure.headings()` rather than re-implementing the bold-numbered-heading test. |
| `scripts/spec/cite.py` | GATE | **Validate a spec citation before it ships** — `--check <clause> "<text>"` exits non-zero unless the text is inside that clause's own region; `--find` locates text whose clause you do not know. Reads the MARKDOWN, not `spec-rule-catalog.json`: the catalog has no block for a prose clause (§14.8.2.3.3) and its ordinal does not track the printed rule number once a rule has sub-letters — both give confident wrong answers. CLAUDE.md rule 1 requires this on every citation. |
| `scripts/spec/audit_code_citations.py` | GATE | **A citation must name the construct its comment is about** — the half of rule 1 that `cite.py` and `audit_doc_citations.py` structurally cannot check, because both need a QUOTE and the commonest citation here has none (`// MOVE (§14.9.24)`). Three checks: **PHANTOM** (the standard has no such clause — §8.3.1.2, §8.8.4.1.1 and §8.8.4.1.2/.3/.4 were cited ~200 times between them), **SUBJECT** (the cited clause names a DIFFERENT construct than the comment is about), **HEADER** (a definition-header comment cites a clause that is not its construct's). `--check` gates (baseline ZERO), `--self-test` proves each check fires on a real defect and is silent on its repair. Wired into `build-local.{sh,ps1}` and `battery.sh` PHASE -1; **not** into CI, which checks out `submodules: false` and so cannot see `specs/ISO_COBOL.md` (the audit says SKIPPED rather than passing blind). Corpus comes from `scripts/spec/citation_corpus.py`, shared with the doc auditor. kb/Work PB159 / PB182 / PB290. |
| `scripts/spec/citation_corpus.py` | TOOL | The ONE definition of "the files that carry spec citations", imported by both citation audits so a citation cannot be covered by one rule and not the other purely by where it lives. `declaration_files()` = grammars + golden headers (where "name the construct, then cite its clause" is the convention); `prose_files()` = docs + source doc-comments; `all_files()` = everything. Frozen evidence (`spec-reconciliation/`, `evidence/`) is excluded by name — repairing a citation inside an agent's recorded output would rewrite the record. |
| `scripts/spec/audit_doc_citations.py` | GATE | Sweeps every citation-bearing file for the INHERITED-citation defect CLAUDE.md rule 1 warns about — a quoted fragment that is genuinely in the standard, filed under the wrong clause. `--check` gates (baseline ZERO), `--self-test` proves it fires on a defect and is silent on the repair, `--all` adds the unresolvable residue. Wired into `build-local.{sh,ps1}` and `battery.sh` PHASE -1 beside `audit_code_citations.py`, from which it also borrows the whole-file opt-out (`audit-doc-citations: names-misfilings`) for the one note that TABULATES misfilings. It found the fabricated "§12.3.7 GR7 k3 … distinct ascending" (a phrase in NO clause) sitting in `COBOLNET_DESIGN.md`, whence a Phase-B agent inherited it into a fix-queue finding, plus four one-level-short citations. ⛔ **It reads BOTH citation orders** — `§14.9.7.3 "quote"` and `"quote" (§14.9.7.3)` — because reading only the first blamed whichever § sat nearest before a quote and accused four correct sites (kb/Work PB379), while a postfix-cited quote went unchecked entirely. A postfix `§` must CLOSE (bracket, comma, stop, `/`, `·`); one that opens an aside is a pointer at a construct the quote names, not its source. The BACK-REFERENCE order (`§15.2 … "quote" (item 5)`) is deliberately not mechanised: a citation no tool can resolve is one nothing checks, and the repair is to write the clause beside the quote. ⚠ Deliberately narrow otherwise: the broad form (every §+quote must resolve) reported 133 failures of which nearly all were its own — docs quote LABELS and paraphrases as well as spec text. A hit still deserves a human glance. |
| `scripts/spec/repairs/rule_numbering.py` · `reserved_word_list.py` · `reserved_word_gaps.py` | TOOL | The three repairs for **Markdown re-parsing the standard's own notation as its syntax** — the class that did the most damage. `rule_numbering` escapes the rule labels (`1)` was an ordered-list marker: it rendered `1.`, colliding with the third level, and ATE the leading `>` of eight rules, one of which then made a false statement about `=`). `reserved_word_list` makes §8.9 a real list and puts the fifteen special-character words in inline code (`+` was an empty bullet; `>`/`>>` vanished; `<`/`<>` became an `<h1>` off the `=` beneath them). `reserved_word_gaps` splits the three run-together lines and carries `--check`, the standing gate that §8.9 equals the printed page. ⚠ **Each asserts WORD CONSERVATION** — the absence of that check in the first `reserved_word_gaps` let it duplicate 18 words it thought were missing. |
| `scripts/spec/repairs/annex_d_*.py` | TOOL | The three Annex D figure generators — `_flowcharts` (VARYING charts, D.1), `_truth_charts` (condition evaluation, D.7–D.10), `_structure` (the two that are structure rather than flow — D.3's nested schematic and D.6's page layout). ⚠ **Each needs the collision guard**: `put` refuses to overwrite a non-blank cell, with a separate `junction()` for the one legitimate case. It has caught seven defects that would have rendered as plausible pictures. Border WEIGHT is notation in D.3 (heavy/light/dashed = group/unit/element); in D.6 VERTICAL DISTANCE is, so its rows are placed from the measured printed y, and its `<…>` notation is escaped at write time or a renderer eats it. |
| `scripts/spec/resolve_index_folios.py` | TOOL | Maps a printed folio to the clause its page begins, for the index references de-paging left as bare numbers. ⚠ Skips the front matter (its TOC is full of `heading … number`) and prefers a heading printed ON the page — getting either wrong silently produces confident, wrong links. |
| `scripts/spec/lint_rendering.py` | GATE | The only check that measures whether the transcription is LEGIBLE rather than faithful — unbalanced emphasis outside code, `<pre>` missing `line-height:1`, an unescaped `<` inside a `<pre>` (raw HTML — a renderer drops the tag and the words with it), an undrawn figure, a column header repeating inside a table body, ragged rows, caption-as-heading, unbalanced tags, dangling links. Needs no PDF. Run it on every change to `specs/ISO_COBOL.md`; it takes a file argument so it can be pointed at an older revision to confirm it still fails. |
| `tests/_shared/ProcessObservation.cs` | GATE | **THE one child-process observer for every test project** — the verdict-evidence invariant in code (`DESIGN-test-build-ci.md` §3.10; plan §11 A12). It replaced SEVEN copies of "start `dotnet`, wait N s, return whatever came back", every one of which returned PARTIAL OR EMPTY stdout on a timeout for the caller to compare against a golden — so a contention timeout reported as a value mismatch. A run that does not complete now raises `HarnessNonObservationException` after ONE serialized retry; it never fabricates output. Budget `COBOLNET_RUN_TIMEOUT_MS` (default 120 s); every retry/non-observation appends to `COBOLNET_HARNESS_LOG` so the rate is MEASURABLE. ⚠ The retry is re-attempting a measurement that did not complete — not re-rolling a failed assertion. |
| `ProcessObservationDriftTests` (`tests/Cobol.Net.Tests.Unit/`) | GATE | Keeps that extraction collapsed (the `TestRepoDriftTests` pattern): no test source may run a child process under its own bounded wait. It found the seventh copy on its first run. ⚠ Its predicate is a REGEX over `WaitForExit(<arg>)`, not a list of numeric prefixes — the first draft's hand-written list would have blessed `WaitForExit(45000)` silently. Carries five behavioural proofs, including "a process that never finishes RAISES instead of returning empty output". |
| `scripts/guard-nist-audit.sh` | GATE | **The NIST population + expectation audit**, consumed by BOTH guards so the rule lives in one place. Asserts the run is a PARTITION of the declared population (one verdict per program, no strays), that `tests/nist/corpus.tsv` agrees with the goldens on disk, and that each program's verdict CLASS is the one the manifest predicts — replacing "353 MATCH" as a number a human remembered. ⛔ Without it, `guard-fast` printed `ALL GREEN` at 352 MATCH because a program VANISHED and only REGRESSIONs were counted. `--self-test` proves all eleven checks can fail, each with its OWN named finding. |
| `scripts/battery.sh` | GATE | **The comprehensive gate, run with the concurrency correctness allows** (§11 A13). PHASE 0 builds the solution once; PHASE 1 runs Conformance ∥ Unit ∥ Characterization as three independent `--no-build` assemblies (wall-clock becomes the POLE, not the SUM); PHASE 2 the REBUILDING guard alone; PHASE 3 the differential. ⛔ §0's "run the long legs ONE AT A TIME" is specifically about a rebuilding leg swapping assemblies under a `--no-build` one — two `--no-build` assemblies cannot do that to each other. Every verdict is read from its own redirected file, never `\| tail -N` into a `&&` chain, and a leg that produces NO verdict line is a FAILURE rather than a silent pass. |
| `tests/external/gnucobol-verdict-baseline.tsv` | GATE | **The GnuCOBOL differential's PER-CASE verdict manifest** — 1323 rows of `id`⇥`tier`⇥`verdict`, sorted, so a flip is a ONE-LINE git diff, the same shape `tests/nist/corpus.tsv` gives NIST. ⛔ It exists because the differential's four summary TOTALS cannot detect OFFSETTING flips, so "0 per-case flips" used to be inferred rather than measured — and no prior run's report was even kept to diff against. `gnucobol_differential.py --baseline` gates on it and an ABSENT baseline is a RED. ⚖ Every column is OURS (owner decision 2026-08-04); their source and expected output are never committed, and titles/keywords are omitted because the diff does not need them. Regenerate ONLY with `--write-baseline`, attributing every changed row in that commit. |
| `scripts/corpus_sweep.py` | GATE | ⛔ **THE reachability instrument — the ONE answer to "does any program we test against have this shape?"** (kb/Work PB209). It reads all seven populations — conformance 966 · nist-programs 459 · nist-copylib 53 · characterization 17 · version-matrix 0 · differential 0 · **gnucobol-external 1,611 = 3,106 programs** — and the external half is EXTRACTED through `gnucobol_extract.differential_cases`/`iter_programs`, the same definition `gnucobol_differential.py` compiles, never globbed. ⛔ It exists because `find … -name '*.cob'` over `tests/external/gnucobol` finds **two files** (the 1,323 programs are `AT_DATA` heredocs inside 36 `.at` wrappers), and two new bind-time rejections shipped on a "ZERO candidates, 1,448 files" claim the next differential immediately refuted — one of the refuting cases titled *"REDEFINES: with OCCURS"*. `--pattern` sweeps a shape; `--codes COBOLNETnnnn` answers the half no grep can (whether a screen actually FIRES, from the differential's per-case report); `--verify-population` is the drift check `ExternalCorpusPopulationDriftTests` runs every build. **The census prints always**, and a failed population check makes it REFUSE to report hit counts rather than return the clean zero that is the defect. ⚖ GPL: program text is matched in memory and never written out; hits are named by our own case id. |
| `scripts/profile-test-parallelism.py` | TOOL | **Per-CLASS wall-clock profile of a test assembly from a `--logger trx` run** (§11 A13(b)). xUnit 2.9.2 parallelizes per test COLLECTION and each class is one collection by default, so every row of a `[Theory]` runs SERIALLY on one thread — and nothing in `dotnet test`'s output says so. ⭐ Its first run measured the greenfield Unit assembly at **1.3× average concurrency on 32 cores**, with ONE class (`StorageFormEquivalenceTests`, 6 tests, 210 s) accounting for the assembly's entire 210 s wall clock. Read `sum` (serial cost) beside `span` (what it occupied): `sum ≈ span` marks a splitting candidate. |
| `scripts/measure-battery-determinism.sh` | TOOL | Runs one leg (`guard`/`conformance`/`unit`) N times on an UNCHANGED tree and prints the distribution — the evidence §11 A12/A12d ask for, replacing "assume the variance is gone". For the guard leg it reports per-program verdict instability, treating a program that is ABSENT in one run as a distinct outcome; for every leg it reports the harness retry/non-observation counts from the observation log. |

## Derived knowledge base (Obsidian vault at `kb/`)

> **Not authoritative.** A cross-linked Obsidian "second brain" derived (paraphrased) from `docs/*` + the source. The
> docs above remain the SSOT — **the doc wins on any conflict**. Contains **no verbatim ISO text** (that stays in the
> `specs/ISO_COBOL.md` submodule). The notes are tracked; Obsidian's volatile `.obsidian/` state is gitignored.

| Path | Type | Subject |
|---|---|---|
| `kb/` | DERIVED | MOCs, lookup tables (keywords · grammar · semantic-rules · IR mapping · runtime mapping · diagnostics · construct catalogue), diagrams, reverse indexes (ISO-clause→phase, runtime-class→IR) and data-flow traces. ⛔ The top-level `Remaining Work Tracker.md` was RETIRED 2026-08-04 — **`kb/Work/` is the one work register** (see its row above). Setup/role: `kb/Context/Vault & Docs Integration.md`. Refresh notes when the docs they cite change; each carries a `last_updated` stamp. |
