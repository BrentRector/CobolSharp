# COBOL.NET — Control Flow (the PC dispatcher) (deep-dive design)

> **Status: LIVE / authoritative subsystem design** for the COBOL.NET rewrite (COBOL -> idiomatic
> typed-native C# via Roslyn; no byte substrate). The condensed cross-referenced view is
> `docs/COBOLNET_DESIGN.md` §5; THIS is the full design (decisions + rationale + C# mapping + hard
> problems + edge cases). The locked invariants and cross-cutting consistency live in the SSOT.
> **IMPLEMENTED** as `__Dispatch` in `src/Cobol.Net.Compiler/CodeGen/DispatchEmitter.cs` — emitted names
> are `__`-prefixed (`__Dispatch`/`__pc`/`__startPc`/`__exitPc`/`__atExit`/`__N`) to avoid collision with translated
> COBOL names; this doc's unprefixed `Dispatch`/`pc` sketches denote the same shapes.

## Summary

A decision-complete design for COBOL.NET's control-flow engine: a single program-counter (PC) state machine emitted as ONE C# method `int Dispatch(int startPc, int exitPc)` per program unit, in which every paragraph (and, for sectioning, every paragraph of every section, flattened into one source-order PC space) is a `case` in one `switch (pc)`, NOT a separate method. This ports the LEGACY compiler's PROVEN return-address / exit-bounded dispatch semantics (which passes 364 NIST tests) but realizes them in idiomatic C# (switch + structured loops + goto) instead of CIL, and replaces the legacy's ambiguous "-1 return AND a catch" run-unit termination with the new runtime's clean exception signals. The dispatcher backbone is a deliberate state machine (the task explicitly accepts not-pretty for irregular control flow); "idiomatic" applies to CASE CONTENTS — IF→if/else, EVALUATE→switch, inline PERFORM→real for/while/do-while loops, typed-native field ops — not to the backbone. Sequential fall-through is `pc=i+1`, GO TO is `pc=idxTarget`, out-of-line PERFORM/THRU is a recursive `Dispatch(idxEntry, idxExit)`, STOP RUN/GOBACK throw distinct exceptions. Key invariant the design preserves verbatim from legacy: the exit-bounded full-switch dispatch gives correct handling of inverted THRU ranges (proc-2 physically before proc-1) and GO-TO-out-of-and-back-into a PERFORM range FOR FREE, because control is followed by PC value, never by physical block extent.

## Backend neutrality (G4 — dual backend)

This deep-dive fixes the SEMANTIC control-flow model — the flattened source-order PC space, the ParagraphTable /
EntryParagraphIndex layout, the exit-bounded recursive dispatch contract, and the two termination exceptions
(StopRun/ProgramReturn). That model lives in the binder/bound tree and is BACKEND-NEUTRAL behind `ICodeGenBackend`
(`--backend roslyn|cil`). The C# realizations shown here (one `switch` method, `for`/`while`/`do-while` for inline
PERFORM, `goto __sent_<n>` for NEXT SENTENCE) are the Roslyn backend's RENDERING of that model; the future Cecil/CIL
backend implements the SAME bound contract with its OWN private structure-to-branch lowering (NO shared lowered IR).
Bound control-flow nodes carry structured forms (paragraph symbols/PC indices, loop descriptors, sentence-boundary
markers) — never pre-rendered C#-specific fragments (SSOT §18 #23).

## Decisions

### D1. Single PC-dispatch method per program unit: paragraphs/sections are `case` labels in one `int Dispatch(int startPc, int exitPc) { int pc=startPc; while ((uint)pc < (uint)N) { bool atExit = pc==exitPc; switch(pc){ case 0:...pc=1;break; ... } __xfer: ; if (atExit && pc==exitPc+1) return pc; } return pc; }`. NOT paragraph-per-method. (`__xfer` is the planted landing of D1a — every transfer OUT of a paragraph jumps there; the `pc=1;break;` shown is the sequential fall-through epilogue of D2, which is the only `break` the dispatcher writes.)

**Rationale.** Owner-LOCKED: 'paragraphs/sections are LABELS (PC cases) in one flow, NOT separate methods. GO TO sets the PC; fall-through is PC++; PERFORM range is a recursive bounded dispatch.' The legacy semantics (return-address model, exit-bounded Dispatch) are proven (364 NIST) and port directly; only the realization changes from CIL-with-method-per-paragraph to one C# switch. The exit-bounded full switch handles arbitrary GO TO and inverted THRU ranges by construction (follow pc, not physical extent).

**Rejected alternatives.** (1) Paragraph-per-method + a dispatcher that CALLS them returning next-pc (the legacy CIL realization, CilEmitter.EmitDispatchHelper, and a sequential-call-chain approach) — rejected: owner locked NOT-separate-methods; and the call-chain cannot express GO TO out of a range, ALTER, or VARYING. (2) Structured-only reconstruction (relooper/Stackifier producing pure if/while/goto-free C#) — rejected for v1: arbitrary COBOL GO TO/ALTER is irreducible; task says correctness over prettiness for irregular flow. Recorded as a deferred open question (well-behaved paragraph → real C# method).

### D1a. Leaving a paragraph body is ALWAYS `__pc = <target>; goto __xfer;` — a planted label immediately after the dispatcher's `switch (__pc)` — and NEVER a bare C# `break;`.

**Rationale (kb/Work PB405, PB414).** C# binds `break` to the innermost enclosing BREAKABLE statement, and this emitter lowers COBOL containers to real C# breakables: an inline PERFORM is a `for`/`while`/`do` (D6), a GO TO … DEPENDING selector is a `switch` (D3). A `break` written for the dispatcher's `switch (__pc)` was therefore captured by whatever container the statement happened to sit in — the transfer was discarded and the paragraph's fall-through epilogue (D2) then overwrote the `__pc` the statement had set. Four rules were measured wrong that way: EXIT PARAGRAPH silently degraded to EXIT PERFORM (§14.9.14.4 GR6), EXIT SECTION's target was discarded so the remaining paragraphs of the section ran (GR7), NEXT SENTENCE in a LAST sentence resumed inside the sentence it was told to leave (§14.9.19.4 GR4/GR6), and GO TO did not transfer at all (§14.9.17.4 GR1/GR2). A `goto` cannot be captured, so the NEXT container this emitter learns to lower is correct by construction instead of by review.

**The shape.** `DispatchState.TransferOut(pc)` / `TransferJump()` / `ResumeTransfer(var)` are the ONLY renderers of a paragraph-leaving transfer (the same one-place discipline as `DispatchCall` and `RunUseCall`), and `DispatchEmitter.EmitDispatchMethod` plants the landing — `__xfer: ;` (`__mxfer:` in an OO method's local-function dispatcher, whose own label a `goto` may not leave) — between the `switch` and the `__atExit` tail-check, so a transfer skips the epilogue and still meets the bounded-dispatch return test. The label is planted ONLY when a transfer was rendered, so a transfer-free program's generated source carries no unreferenced label. `DispatcherTransferIdiomDriftTests` re-measures the invariant over the GENERATED C#: a `__pc = …;` whose next jump is `break;` is legal only at case-body depth.

**The two statements that are NOT plain jumps.** EXIT SECTION renders the section's own return mechanism (`if (__exitPc == sectionEndPc) return __pc;`, D8) BETWEEN the assignment and the jump — a C# `return` no container can capture. The GO TO … DEPENDING selector's arms set `__pc` and `break` out of their OWN selector switch; the transfer is then completed by the single `if (in range) goto __xfer;` after it (D3).

**Rejected alternative — a sentinel `__pc` plus per-container re-checks** (each lowered loop testing "did a transfer happen?" and breaking outward): rejected — it is O(containers × statements) of emitted noise, it is exactly the hand-maintained-list shape the project forbids, and it re-introduces the defect the moment a new container forgets its check.

### D2. Sequential fall-through emits `pc = i+1; break;` as the LAST statement of each non-terminating case; a paragraph that ends by reaching the next paragraph just advances the PC. This `break` is written at CASE-BODY level, where it really does leave `switch (__pc)`, and it sits BEFORE the D1a landing label, so a transfer that jumped to the label can never have its target overwritten by it.

**Rationale.** Direct port of legacy `IrReturnConst(myIndex+1)` (Binder.cs:243). COBOL paragraphs are not isolated — control falls from one into the next textually (ISO §14.4 sequential execution). pc=i+1 with the switch covering [0,N) makes the last paragraph's fall-through (pc==N) exit the loop = run-unit end.

**Rejected alternatives.** Emitting the next paragraph's body inline after the current (textual concatenation) — rejected: breaks PERFORM THRU exit detection (no clean boundary for the named exit paragraph) and breaks GO TO targeting a mid-program paragraph.

### D3. GO TO (simple) → `pc = idxTarget; goto __xfer;` (D1a). GO TO ... DEPENDING ON selector → `switch ((int)selector) { case 1: pc=idx0; break; ... case k: pc=idx(k-1); break; default: /* out of range: no transfer, fall through */ }` — those `break`s are the SELECTOR switch's own — followed by ONE `if (selector in 1..k) goto __xfer;`; if no case matched, control continues to the paragraph's normal fall-through.

**Rationale.** ISO §14.9.17.4 GR2: GO TO transfers control (set pc) when the selector is one of the positive/unsigned integers 1..k; a selector outside 1..k is a no-op (control passes to the next statement) — modeled by letting the switch fall out without setting pc, so the paragraph's trailing pc=i+1 runs. If the content of the selector item is NOT numeric, the EC-DATA-INCOMPATIBLE exception condition is set to exist (and, when checking for it is enabled, processed as for that condition) before the range test. Selector resolved to int via the typed-native numeric value (the unscaled long / its integer reading), no byte decode.

**Which format this is, is decided ONCE, and by the GRAMMAR (kb/Work PB412).** `goToStatement` carries one alternative per printed §14.9.17.2 format — `GO TO? procedureName+ DEPENDING ON? dataReference` (Format 2), `GO TO? procedureName` (Format 1) — plus the edition-gated ANSI-85 target-less arm `GO TO?` (D4), which the 2023 standard prints no format for and which stays only so COBOLNET0811 can name the edition. `CobolNet.Frontend.Parsing.GoToFormats.Of` is therefore TOTAL, and it is the single answer every reader takes: `ControlFlowBinder.BindGoTo`'s three-arm switch, `VersionConformancePass.VisitGoToStatement`'s gate, and `SetAlterBinder.AlterIsSoleGoToParagraph`'s "is this paragraph alterable" test. It used to be the union rule `GO TO? procedureName? (procedureName)* (DEPENDING ON? dataReference)?` with the format reconstructed from optional children at each of those three sites: `GO TO A B.` fell into the Format-1 arm, which read `names[0]` and DISCARDED the rest with no diagnostic, and `GO TO DEPENDING ON X.` reached a `BoundUnsupported` and aborted the run unit. See `COBOLNET_PIPELINE_DESIGN.md` D7 for the general rule this is an instance of.

**Rejected alternatives.** C# `goto caseLabel` between cases — rejected: C# forbids goto into a different switch section's label and the PC-return model composes with PERFORM/Dispatch recursion which raw goto cannot. A jagged if-else cascade for DEPENDING ON — rejected: switch is clearer and is what idiomatic C# uses for a dense 1..k dispatch. A PAIR OF BIND-TIME `if`s screening the two illegal shapes (kb/Work PB412's fallback proposal) — rejected: the grammar would keep claiming shapes the language does not have, the diagnostic would land a phase too late, and every future edit to either format would need a new hand-written validation clause.

### D4. ALTER + alterable GO TO → a per-alter-target mutable C# field `private static int _alter_<para> = <defaultTargetIdx>;`. A bare/alterable `GO TO` in that paragraph emits `pc = _alter_<para>; goto __xfer;` (D1a). ALTER proc TO PROCEED TO new emits `_alter_<para> = idxNew;`.

**Rationale.** C#-NATIVE per owner decision ('ALTER → a mutable target var'), replacing the legacy `_alterTable[slot]` int array (CilControlFlowEmitter.EmitAlter/EmitReturnAlterable). A named int field is readable and exactly the legacy semantics. Default value = the GO TO's static written target (or -1/STOP if the bare GO TO was never given a target, ISO §14.9.17 undefined-then archaic).

**Rejected alternatives.** The legacy shared `int[] _alterTable` indexed by slot — rejected: owner wants a mutable var; an array index is opaque. ALTER is edition-scoped: it is an obsolete element of COBOL-85 (§14.9 has no ALTER statement in 2002 onward — GO TO has only Formats 1 and 2), so it is supported only at `--std 85` and REJECTED at 2002/2014/2023 as not-in-edition (COBOLNET0810/0811, `Constructs.AlterRemoved2002`); see the per-edition gating & diagnostics section. The NIST corpus exercises it at `--std 85`.

### D5. Out-of-line PERFORM p [THRU q] → recursive `Dispatch(idxP, idxQ)` (idxQ=idxP when no THRU); the returned pc is discarded (PERFORM is a call, not a branch). PERFORM ... n TIMES / UNTIL / VARYING wrap that call in a real C# loop. The body of the case becomes e.g. `Dispatch(idxP, idxQ); pc = i+1; break;` for simple PERFORM, or a loop containing `Dispatch(...)`.

**Rationale.** ISO §14.9.28: PERFORM transfers control to a procedure and returns implicitly when it completes. Recursive Dispatch with exitPc=idxQ is the legacy's proven mechanism (EmitPerformThru → Dispatch(start,end)): it follows control flow ANYWHERE within/leaving/re-entering the range and returns only when paragraph idxQ falls through (pc becomes idxQ+1) — handling inverted ranges (idxQ<idxP) and GO-TO-out for free. Recursion (a real C# call stack) gives correct nesting of overlapping/recursive PERFORM ranges automatically.

**The TIMES count (kb/Work PB86, 2026-08-18).** §14.9.28.2 Format 2 is `{identifier-1 | integer-1} TIMES`, and an identifier includes a function-identifier (§8.4.3.1.2; §8.4.3.2.4 GR1 — it references a temporary item), so the grammar arm is `(integerLiteral | functionCall | dataReference) TIMES` and the keyword-omitted intrinsic (§8.4.3.2 SR2, FUNCTION ALL INTRINSIC) binds through `FieldOperand` as before. §14.9.28.3 SR2 "Identifier-1 shall be an integer" is enforced at BIND for every shape through the ONE integer classifier, `IntrinsicResultType.IsIntegerOperand` (a scale ≤ 0 numeric elementary item that is not USAGE INDEX — a trailing-P picture IS an integer; a function whose RESOLVED type is integer, §15.2 type 5) — COBOLNET1646. The emitter renders the count (GR7 — determined once) as a literal verbatim or, for every other operand, `(long)(NumericRenderer.Align(num.AsNum(count), 0))` — the ONE integer landing, so a P-scaled or unsigned-wide item counts by VALUE. Before this the count switch had a `_ => "1"` default: a function count ran the body ONCE, silently. The same shape was swept: the GO TO … DEPENDING selector, WRITE … ADVANCING identifier LINES (whose figurative ZERO advanced ONE line — kb/Work PB87, NIST SQ101M), and the STRING/UNSTRING POINTER and TALLYING reads all land through `Align(…, 0)`, and no integer-identifier consumer keeps a silent default.

**Rejected alternatives.** Inlining the THRU range's paragraph bodies into the call site — rejected: cannot express a GO TO that leaves and re-enters the range, breaks on inverted ranges, and duplicates code. A return-address stack data structure managed by hand — rejected: the C# call stack already IS the return-address stack; recursion is simpler and re-entrant.

### D5a. A resolved procedure range is a `PcRange` whose EMPTINESS is a CARRIED bit, never `End < Start` arithmetic; an EMPTY range emits the control-phrase loop around an EMPTY body and is never handed to `Dispatch`.

**Rationale.** A section with ZERO paragraphs is legal — ISO §14.4.2, "A section consists of a section header followed by
zero, one, or more successive paragraphs" — and it makes §14.9.28.4 GR4's *specified set of statements* empty. GR4
DEFINES that set and an empty set is a set, so the PERFORM's own semantics still run in full: GR13 a) sets every
induction variable BEFORE any transfer, GR9 takes the TIMES count once, GR10 evaluates the condition, GR5 then finds
no first statement to transfer to. **The control phrase is the statement; the range is only what the body does.**

`ResolveProcedure` therefore returns `PcRange` (`src/Cobol.Net.Compiler/Binding/Model/PcRange.cs`), which carries
`IsEmpty` alongside `Start`/`End`, composes THRU through `Through` (GR4/GR5b) and answers ALTER's
"procedure-name-1 shall be a paragraph" through `IsParagraph`. `BoundOutOfLinePerform` carries the range;
`ControlFlowEmitter` emits `EmitPerform(control, body)` for EVERY range and supplies an EMPTY body action when
`IsEmpty`, so the emitted C# is the inline emission minus the body and GR4's "function identically" is structural.
`DispatchState.DispatchCall` is the ONE place a range becomes `__Dispatch(a, b)` and THROWS on an empty one.

**Why the bit cannot be derived (kb/Work PB440).** `End < Start` is ALSO true of a legal INVERTED THRU range — GR6:
"There is no necessary relationship between procedure-name-1 and procedure-name-2" (NIST NC102A PFM-TEST-F1-10) —
and when the two procedures are ADJACENT the two cases produce the IDENTICAL pair `(s, s-1)`. One must execute
nothing; the other must execute from `s` until control reaches `s-1`. Every hand-written `start > end` /
`Start <= End` test was answering the wrong question, and both answers it got wrong were wrong-answer defects:
the binder returned a no-op that DELETED the PERFORM's control phrase (no init, no condition, no augment, no
loop), and with THRU written that test was skipped so the dispatcher ran `Dispatch(s, s-1)` — whose return test
`__atExit && __pc == __exitPc + 1` can never fire on it — from `s` to the END of the pc space, executing the
FOLLOWING sections once per iteration. The identical decode in `SortEmitter` dropped an entire inverted
`INPUT PROCEDURE IS p THRU q`, releasing no records at all.

The canonical empty range is still `(s, s-1)` rather than a sentinel, because that pair is exactly what makes GR4's
THRU composition come out right with no special case: an empty procedure-name-1's `Start` is where execution
continues past it, and an empty procedure-name-2's `End` is the last pc before it. `Through` marks the composition
empty only when BOTH endpoints are empty and nothing lies between them.

**Drift test.** `ProcedureRangeEmptinessDriftTests` pins all of it: the `(s, s-1)` collision, the eight `Through`
compositions, `DispatchCall`'s refusal, and two SOURCE scans — no second dispatch-call construction site, and no
`.Start`-vs-`.End` comparison anywhere in the compiler. Both scans carry positive controls and were proved to fail.

**Rejected alternatives.** A guard bolted onto the THRU branch (`if (start > end) return Nop`) — rejected: it is the
same ambiguous test in a third place, and it keeps deleting the control phrase. An assert inside the GENERATED
`__Dispatch` — rejected as the primary defence: it costs a branch in every user program and fails at run time; the
emit-time refusal in `DispatchCall` fails in the compiler's own suite instead. Note that `BoundDeclarative`'s
ranges stay plain pcs: `DeclCollectSection` materializes a zero-sentence paragraph for an empty declarative
section, so a USE range is never empty by construction.

### D6. Inline PERFORM (... END-PERFORM, no procedure-name) → REAL idiomatic C# loop INSIDE the case, never a Dispatch call: PERFORM TIMES→`for`, PERFORM UNTIL TEST BEFORE→`while(!(cond))`, TEST AFTER→`do{...}while(!(cond))`, PERFORM VARYING→`for`-style with COBOL FROM/BY/UNTIL, PERFORM (once)→`do{...}while(false)`. EXIT PERFORM→`goto __pexit{id};` (the label just past the loop) and EXIT PERFORM CYCLE→`goto __pcont{id};` (the label at the loop-control boundary, so a VARYING augment + re-test still run). Both are PLANTED labels rather than `break`/`continue`: a multi-level VARYING is nested C# loops, and a bare break/continue would leave or cycle only the innermost (§14.9.14.4 GR5a/GR5b; CA31/CA32) — the same capture argument D1a makes for the dispatcher.

**Rationale.** Inline PERFORM scope is lexically bounded (END-PERFORM) and structured, so it maps to a native C# loop — this is exactly where 'idiomatic' applies. EXIT PERFORM/CYCLE are scoped to the nearest inline PERFORM (§14.9.14.4 GR5a/GR5b, "the most closely preceding, and as yet unterminated, inline PERFORM") → a goto to that PERFORM's own planted label. Two DISTINCT mechanisms (inline=loop, out-of-line=Dispatch) because their scoping and EXIT semantics differ.

**Rejected alternatives.** Routing inline PERFORM through Dispatch too (uniformity) — rejected: there are no paragraphs to dispatch, EXIT PERFORM would need PC plumbing, and it would produce unreadable output for the common structured case. Using C# goto for EXIT PERFORM CYCLE — acceptable fallback only when the increment block can't be reached by `continue` (TEST BEFORE VARYING).

### D7. STOP RUN → `throw new StopRun();` caught ONLY at the run-unit Main. GOBACK → `throw new ProgramReturn();` caught at the current program's Entry wrapper. EXIT PROGRAM → `if (__asCalled) throw new ProgramReturn();` — a DISTINCT exception, thrown ONLY when the program is under the control of a caller. Normal transfers (GO TO, fall-through, range completion, EXIT PARAGRAPH/SECTION) stay integer-pc; exceptions are used ONLY for program/run-unit termination.

**Rationale.** A recursive Dispatch means a STOP RUN N PERFORMs deep must unwind ALL frames AND cross CALL boundaries — only an exception does that cleanly (ISO §14.9.42 STOP terminates the run unit). GOBACK and EXIT PROGRAM in a CALLED program must unwind THIS program's PERFORM frames and return to the CALL site WITHOUT killing the run unit (ISO §14.9.18.4 GR2 / §14.9.14.4 GR3) — a separate exception caught at Entry. **GOBACK and EXIT PROGRAM DIVERGE when the program is NOT under the control of a caller** (a main program): EXIT PROGRAM is treated as a CONTINUE — control passes to the next statement (§14.9.14.4 GR2; §14.6.3), so it is emitted `if (__asCalled) throw new ProgramReturn();` and is a plain fall-through in a main program; GOBACK in a main program acts as a STOP statement, terminating the run unit (§14.9.18.4 GR3) — its `ProgramReturn` reaches the run-unit boundary, which ends the run unit. The new runtime already provides StopRun; add ProgramReturn.

**Rejected alternatives.** The legacy pc=-1 return-code propagation (CilControlFlowEmitter.EmitStopRun/EmitGoBack both return -1) — rejected and explicitly called out as the legacy's internal inconsistency (it returns -1 yet also has a 'StopRunException catch' comment): a -1 return unwinds only ONE Dispatch frame, so a STOP RUN inside a PERFORM would wrongly resume the caller; and -1 cannot cross a real C# CALL boundary without a re-check at every call site. The discriminator: which mechanism unwinds ARBITRARY nesting + crosses CALL for STOP RUN while stopping at the program boundary for GOBACK = exceptions.

### D8. EXIT PARAGRAPH → `pc = myIdx+1; goto __xfer;` (return from current paragraph). EXIT SECTION → `pc = sectionEndPc+1; if (__exitPc == sectionEndPc) return pc; goto __xfer;` (transfer to the section end AND fire the section's return mechanism). Both leave through the D1a landing, because GR6/GR7's NOTE names the return mechanisms they precede — "those associated with language elements such as PERFORM, SORT, and USE" — and an INLINE PERFORM is not one of them: it is a loop inside the paragraph, with no standing to intercept either transfer. EXIT (bare) is a no-op (ISO §14.9.14: common end point). CONTINUE (bare, §14.9.9) → no statement emitted (no-op); CONTINUE AFTER arithmetic-expression-1 SECONDS → suspend execution for the interval, raising EC-CONTINUE-LESS-THAN-ZERO when the count evaluates below zero (§14.9.9 GR1).

**Rationale (§14.9.14.4 GR6/GR7; §14.9.14.3 SR9).** EXIT PARAGRAPH (GR6) transfers to the current paragraph's end — `pc=myIdx+1; goto __xfer;` — and the bounded dispatch's own `__atExit` tail-check (`if (__atExit && __pc == __exitPc+1) return __pc;`), which sits just past the landing label, returns from a `PERFORM PARA` / `PERFORM … THRU PARA` when that paragraph IS the exit. EXIT SECTION (GR7) transfers to "the unnamed empty paragraph that follows the last paragraph of the current section, preceding any return mechanisms for that section" — it must FIRE the section's PERFORM/SORT/USE return, not merely move the pc. The emit therefore sets `pc = sectionEndPc+1` and adds an EXPLICIT `if (__exitPc == sectionEndPc) return pc;`: a mid-section EXIT SECTION cannot reach the `__atExit` tail-check (which captures the *current* pc, not the section end), so the explicit test is what replicates the return for a `PERFORM SECTION` / `PERFORM … THRU` the section end / SORT-or-USE procedure / the top-level end wall. The jump handles the exit≠section-end fall-through (top level → the next section; a THRU range extending past the section end). §14.9.14.3 SR9: the SECTION phrase may be specified only in a section (`ctx.CurrentSection` null ⇒ COBOLNET0827).

**Rejected alternative — a bare `pc = sectionEndPc+1; goto __xfer;` for EXIT SECTION, with no explicit return.** Incorrect from a mid-section paragraph of a PERFORMed section: `__atExit` is false there, so the bounded loop does not return and instead runs the FOLLOWING section — violating GR7's "preceding any return mechanisms for that section." The section return must be fired explicitly.

**Rejected alternatives.** Emitting bare EXIT as control flow — rejected: ISO §14.9.14 bare EXIT is a no-op placeholder (the only sentence in its paragraph), control falls through normally.

### D9. NEXT SENTENCE → forward `goto __sent_<n>;` to a label placed at the next sentence boundary within the same case; in the LAST sentence there is no following boundary, so the implicit CONTINUE before the separator period IS the paragraph fall-through and the statement renders the D1a transfer `pc = myIdx+1; goto __xfer;`. Statements after an unconditional NEXT SENTENCE in the same sentence are unreachable and are NOT emitted.

**Rationale.** ISO §14.9.19.4 GR4 (NEXT SENTENCE in the THEN phrase) and GR6 (in the ELSE phrase) + Annex F.1: both state the SAME transfer — control goes to an implicit CONTINUE immediately preceding the next separator period (sentence boundary), NOT past a scope delimiter — and the binder does not record which arm produced the node, so one lowering serves both and they cannot diverge. The last-sentence arm was a bare `break` until kb/Work PB414: inside an inline PERFORM it bound to the loop and control resumed inside the very sentence the rule says to leave. A C# forward goto to a sentence-boundary label models this exactly and stays within one switch case (legal C# goto). Legacy used a CurrentSentenceEnd block (ControlFlowLowerer.LowerNextSentence).

**Rejected alternatives.** Treating NEXT SENTENCE like END-IF scope exit — rejected: ISO explicitly warns this is the common MISunderstanding (line 50371); it goes to the period, not the scope delimiter.

### D10. DECLARATIVES + USE procedures: declarative sections occupy PC indices [0..declEnd); `EntryParagraphIndex` = first NON-declarative paragraph, so normal flow starts AFTER declaratives (the main Dispatch call uses startPc=EntryParagraphIndex, exitPc=-1). A USE procedure is invoked from the runtime I/O/error path as its own `Dispatch(useStart, useEnd)` call. For a NONFATAL condition, normal completion of the declarative returns control to the statement following the triggering I/O; for a FATAL exception condition, normal completion of the declarative TERMINATES the run unit abnormally (§14.6.13.1.3 GR5 → §14.6.12) — resuming after the I/O requires an explicit RESUME statement (§14.9.33). (ISO §14.3, §14.6.13.)

**Rationale.** Port of legacy ir.EntryParagraphIndex (CilEmitter.EmitEntryMethodBody:723) which skips the declaratives region, and the GlobalUse declarative dispatch (CilEmitter EmitDispatchGlobalUse). Declaratives must never run by fall-through, only by event. The USE-from-runtime wiring is realized by the emitted `__RunUse`/`__IoCheck` machinery (DispatchEmitter.EmitUseMachinery); the PC layout (declaratives first, EntryParagraphIndex marker) is fundamental — designed in from the start so it is not a retrofit.

**Rejected alternatives.** Emitting declaratives as a separate dispatch space/method — viable but inconsistent with the single-Dispatch model; keeping them as low PC indices reuses the one switch and one set of paragraph labels.

### D11. PERFORM VARYING ... AFTER: on each increment of an outer varying identifier, ALL inner (AFTER) identifiers are RESET to their FROM values before the outer UNTIL is re-tested; ALL identifiers (including AFTER levels) are set to FROM before the first UNTIL test. Emitted as nested C# loops where the outer loop body re-initializes the inner index(es).

**Rationale.** ISO §14.9.28 + illustrated in Annex D.26 figures D.11-D.14 (the canonical VARYING flow diagrams). Direct port of legacy EmitAfterReinitialization (ControlFlowLowerer.cs:237-249). This is the #1 VARYING correctness gotcha a naive nested-for misses.

**Rejected alternatives.** Plain nested C# for-loops without re-init (relying on C# for-init) — works ONLY if the inner FROM is a constant; COBOL FROM can be a data item re-read each outer pass and AFTER reset is mandatory, so explicit reset is required.

### D12. SEARCH ALL's Format-2 syntax rules (ISO §14.9.37.3 SR7–SR13) are SEVEN PREDICATES OVER ONE COMPILE-TIME MODEL, built in `Binding/Validation/SearchAllFormat2Rules.cs` and reached through the ONE check-catalog entry `StatementValidation.CheckSearchAllFormat2`. The model has exactly two halves — (a) the table's OCCURS KEY phrase as ONE ordered list (`OccursSpec.Keys`, phrase order, resolved by `OdoModel.KeyItems`), and (b) the WHEN phrase decomposed into Format-2 conjuncts, each with a KEY side and a SENDING side. The three diagnostics divide the rules by the OPERAND a violation is about: **COBOLNET1964** the table's declaration (SR7), **COBOLNET1965** the KEY side (SR8, SR9, SR11, SR12's data-name arm), **COBOLNET1966** the SENDING side (SR10, SR13, SR12's identifier arm). A WHEN whose SHAPE is not Format 2 at all — an ordering operator, OR, XOR, NOT, a class/sign condition, an abbreviated combined relation — is the EXISTING `StatementValidation.RejectStatementOperand` (COBOLNET1757), never a fourth code.

**Rationale.** All seven rules read one of those two things and nothing else, which is why all seven were unenforced together for as long as neither existed (kb/Work PB445): `BindSearchAll` read `IndexNames` and `Occurs` and bound each WHEN as one opaque condition. Building the model once makes the NEXT Format-2 rule a predicate rather than another parse-tree walk, and `SearchAllFormat2RuleCoverageDriftTests` re-derives the rule list from `specs/ISO_COBOL.md` so that stays true. The KEY half is ONE ordered list because §13.18.38.4 GR3 makes the phrase's order the order of significance and SR11 is a rule about it — a mixed `ASCENDING KEY IS A B DESCENDING KEY IS C` phrase loses which key precedes which the moment it is split per direction word. SR8/SR9 are asked of the SUBSCRIPT AS WRITTEN (`ReferenceResolver.SubscriptSegments`): "shall be subscripted by the first index-name … shall not be followed by a '+' or a '–'" is a fact the rendered C# index expression has already erased.

**Rejected alternatives.** (1) Tightening the `searchAllWhenClause` grammar rule to the printed Format-2 operand shape — rejected: the repo's parse-wide / bind-narrow doctrine, and a parser-level rejection can only report an unexpected token where a bind-level one quotes the rule. (2) Putting the key model on `BoundSearch` — rejected: nothing at run time consumes it (the scan is serial, which §14.9.37.4 GR9 permits outright — "A non serial type of search operation may take place"), so a bound-node field would be exactly the write-only lookup PB445 is about. (3) Seven ad-hoc checks against the raw parse tree — rejected: it reproduces the fragility the standard's own structure avoids, and rule 12's predicate (`VariableLengthCompatibility.IsVariableLength`) already existed and was simply never called, because the operands it would be called on had nowhere to come from.

⛔ The serial scan's conformance argument does NOT rest on SR7, and `SearchBinder`'s doc comment says so: SR7 is a requirement on the DATA DESCRIPTION ENTRY and says nothing about run-time ordering. The technique is licensed by §14.9.37.4 GR9 directly, and §14.9.37.4 GR6 a) 2. ("It is undefined which of these alternatives occurs") covers the case where GR5 a)'s sequencing precondition is false.

### D13. A `procedure-name` OPERAND resolves through ONE reporting step — `ProcedureTableBuilder.ResolveProcedureOperand` — which either returns the `ResolvedProcedure` (the `PcRange` **plus the section that owns the denoted procedure** — D13b) or reports **COBOLNET1639** and returns null; the raw, silent resolver is named `ResolveProcedureQuiet` and belongs to the two PRESCANS that are not a statement's own bind (the ALTER switch-field prepass, the USE FOR DEBUGGING SORT/MERGE-overlap scan). Every statement whose general format prints procedure-name enters the reporting step: PERFORM (procedure-name-1 §14.9.28.3 SR12 and procedure-name-2 SR13, each arm quoting its OWN rule number), GO TO Format 1 and Format 2, ALTER's two operands, RESUME AT, and the SORT/MERGE INPUT and OUTPUT PROCEDURE phrases. The message lives in the ONE syntax-rule catalog (`StatementValidation.RejectProcedureName`), never at a call site, and composes three facts the caller cannot: whether the word is declared as a DATA ITEM, whether resolution was confined to a method (§11.7), and the caller's own quoted rule. The SET statement's two operand rules moved with it — Format 4's §14.9.39.3 SR6 (`RejectSetConditionName`) and Format 3's SR5 — and so did §14.9.25.3 SR12's second half, "shall not be reference-modified", which `CheckCorrespondingGroupOperand` now asks of the **`Place`** rather than the `DataItem`.

**Rationale.** kb/Work PB390: these verdicts were DECIDED at bind and then shipped as `BoundUnsupported`, which the emitter renders as `NotImplemented.Run(...)`. A misspelled `PERFORM` therefore COMPILED, produced an assembly, and aborted the run unit with an unhandled .NET exception telling the programmer that COBOL.NET had not implemented a feature — the diagnosis sent to the wrong party, and on a path the flow skipped not sent at all, where ISO §4.2.2 ¶2 requires the compile-time mechanism. It is PB236's separation finished: the carrier keeps job one (a construct this compiler has not built, announced as the COBOLNET1756 WARNING), and a violated syntax rule is an ERROR naming the rule. The code is the EXISTING COBOLNET1639 ("this source element defines no such name", §8.4.2.1, with §8.4.6.1's scope for paragraph-names and section-names) rather than a new spelling of "not defined" — which is also why SORT's private `RejectUnknownProcedure` folded into this step, taking with it a small lie: it reported `procedureName(0)` whichever of a THRU pair had failed, so a bad second name accused the good first one.

**Rejected alternatives.** (1) Leaving the reporting at each call site and adding the missing ones — rejected: that per-site habit is exactly what produced the asymmetry PB236 measured (two of START's five refusals carried a diagnostic and three did not), and it was still producing it here eight sites later. (2) A new diagnostic code for procedure-names — rejected under the COBOLNET1651/1757 precedent: the CODE is the identity of the MECHANISM, the MESSAGE carries the rule. (3) A drift test that greps `new BoundUnsupported(` for rule-shaped wording — rejected, and the reason is worth keeping: several GENUINE deferrals legitimately cite the rule describing the feature they defer (`GOBACK RAISING identifier … §14.9.18.3 SR4`), so the text test is both over- and under-inclusive and its residue would have to be a hand-maintained exclusion list. The drift suite (`DecidedRuleStageDriftTests`) is BEHAVIOURAL instead: each moved rule asserts the error, its rule citation, AND the absence of COBOLNET1756, with a legal-control half that a reject-everything compiler would fail. ⚠ **Superseded in part by kb/Work PB909:** the text scan now exists (`BoundDeferralDriftTests`) and needs NO exclusion list, because the premise changed — the refusal has its own type (`BoundRejected`), and the three RAISING-identifier deferrals were reworded to name the FORM they defer ("the exception-object form of the RAISING phrase") rather than the syntax rule that describes it. A deferral message is about what COBOL.NET has not built; the rule vocabulary belongs to the refusal. The behavioural suite stays, alongside it.

### D13a. A procedure-name scope KEEPS every declaration of a spelling (`ProcedureNameMap`), and §8.4.2.2's uniqueness question is answered by ONE resolution algorithm running over whichever scope the reference stands in — the program's maps, or a method's. "Identifies nothing" and "identifies more than one" are DIFFERENT outcomes with different codes: the first stays **COBOLNET1639**, an unqualified collision with no rule-2-through-6 excuse is **COBOLNET2121** (§8.4.2.2.1 rule 1 + §8.4.2.2.3 SR1), and a paragraph-name duplicated *within one section* and explicitly referenced is **COBOLNET2122** (§8.4.2.2.3 SR7). `ResolveProcedureQuiet` returns nothing for an ambiguous name exactly as for an unknown one; the statement's own operand bind is what reports.

**Rationale.** kb/Work PB466. Five scopes registered their names with `Dictionary.TryAdd` — whose contract is "keep the first, discard the rest" — so the compiler could not merely fail to DIAGNOSE ambiguity, it could not REPRESENT it: by the time a reference asked, the multiplicity was gone. `ResolveProcedure` then fell through to the globally first-defined paragraph, and `GO TO DUP-P` with `DUP-P` in two other sections compiled clean and ran S-ONE's copy. §8.4.2.2.1 offers no tie-breaker for procedure-names — it says qualification is required — so "resolve to the nearest preceding definition", or to the last, would be a second wrong answer in a different flavour. The candidate count sums PARAGRAPHS AND SECTIONS, because §14.4.1 makes a procedure-name "a word used to refer to a paragraph or section"; a duplicated section-name is therefore the same defect, with the sharper edge that §8.4.2.2.2 format 4 gives a section-name no qualifier at all, so its only repair is a rename — which is why the message distinguishes the two rather than telling every user to qualify. The checks sit at the REFERENCE, never at the declaration, because both rules are conditioned on "explicitly referenced": a duplicated paragraph-name nothing mentions is conforming and shall compile (§4.2.2), and `tests/conformance/85/pb466_procedure_name_uniqueness.cob` measures exactly that alongside rule 6 and format-4 qualification.

The method scope was the same algorithm written twice, and it had inherited the identical hole one level down — so the two arms were merged into one body that selects its maps (`ResolveProcedureWord`), and the USE FOR DEBUGGING `ON procedure-name` subject, which the grammar spells as a `dataReference` and which had grown a third private copy of the lookup, now resolves through it too. `ProcedureNameUniquenessDriftTests` pins both halves: no scope may register a declaration with `TryAdd` or an overwriting indexer (with a fact that fails the scanner on the pre-fix lines), and the behaviour is asserted at all four editions — §8.4.2.2 is unchanged across them.

**Rejected alternatives.** (1) A uniqueness check bolted onto the resolver's global fall-through — rejected: the information it would test is destroyed at REGISTRATION time, so the test would have nothing to test; the map had to learn to keep it. (2) Folding the ambiguity into COBOLNET1639 — rejected, and this is NOT a reversal of D13's "no new code for procedure-names": D13 refused a per-NAME-CLASS copy of an existing mechanism, and the principle it refused it under ("the CODE is the identity of the MECHANISM") is what grants these two. 1639's message says no paragraph or section carries the name, which is a FALSE statement about a program that carries it twice. (3) One code for both uniqueness rules — rejected: SR7 admits no qualifier, so a shared message could only offer a repair that is impossible for half its cases. (4) A `Dictionary<string, List<int>>` for every scope — rejected on the performance lens: the duplicate list is empty for every well-formed program, so `ProcedureNameMap` keeps the first definition in the same single-probe dictionary it always used and allocates the candidate list only when a program actually collides.


### D13b. A RESOLVED procedure-name carries its owning SECTION, not a naked pc pair — `ResolvedProcedure(PcRange Range, SectionInfo? Section)`, built by the ONE success constructor `ProcedureTableBuilder.Found`. `SectionInfo.IsDeclarative` is recorded by the declaratives walk that already knows it, so every rule about the PORTION a procedure is in asks the resolution instead of re-deriving it from pc arithmetic. §14.9.28.3 SR11 — "When procedure-name-1 and procedure-name-2 are both specified and either is the name of a procedure in the declaratives portion of the procedure division, both shall be procedure-names in the same declarative section" — is then ONE predicate in the syntax-rule catalog, `StatementValidation.CheckDeclarativesRange`, called where the PERFORM range is COMPOSED (both ends still resolutions) and reporting **COBOLNET2186**, an error at every edition because the rule is X3.23-1985's carried unchanged through 2023.

**Rationale.** kb/Work PB433. `ResolveProcedure` used to return `(int Start, int End)`, which has already discarded which section a name came from, so SR11 was implemented NOWHERE and both of its halves were live: one end inside the declaratives and one outside compiled clean and then ran the positional range forward out of the declarative section into whatever physically followed — including the PERFORM itself — until the CLR killed the run unit with a stack overflow; both ends declarative but in DIFFERENT sections ran quietly across two USE procedures with different dispatch. They are ONE test ("the two ends are procedures of the same declarative section"), which is why a smallest-diff screen written for the loud half would have left the quiet one open. The same field immediately retired the one pc-arithmetic re-derivation in the tree: RESUME AT's §14.9.33.3 SR3 test read `target.Start < ctx.Table.EntryPc` and now reads `target.IsDeclarative`. `ProcedureRangeDeclarativesDriftTests` pins the single success constructor, sweeps `Binding/**` for the arithmetic shape, and holds both halves plus the three legal shapes SR11 does not forbid.

**What is still open.** GO TO, ALTER and the SORT/MERGE procedure phrases now RECEIVE the section with their resolution but enforce no declaratives constraint of their own; §14.9.49.3 SR3 ("Within a declarative procedure, there shall be no reference to any nondeclarative procedures except in a RESUME statement") and SR4 ("Procedure-names within a declarative section may be referenced in a different declarative section or in a nondeclarative procedure only with a PERFORM statement") are implemented nowhere. They are a different clause's rules and are tracked in `kb/Work/`, not here.

### D14. Every PLACEMENT syntax rule — "statement X may be specified only in Y" — asks ONE bind-position probe, `BinderContext.Enclosing` (type `EnclosingContext`), and none of them re-derives "where am I bound?" for itself. The probe carries three things: the **source-element kind** (`SourceElementKind`, ISO §14.2.2 SR10's five — program definition, program prototype, function definition, function prototype, method definition), the **enclosing-construct stack** (`EnclosingConstruct`: inline PERFORM · exception-checking PERFORM · a WHEN phrase · a FINALLY phrase, pushed by the binder that descends into the construct's statement block), and the **containing declarative** (`BoundDeclarative?`, the ONE pc-range lookup). It holds no state of its own and is recomputed per read, so it cannot go stale.

The askers today: §14.9.14.3 SR7 (`InProgramProcedureDivision`), SR8 both sentences (`InPerform`, `InExceptionCheckingPerform`), SR9 (`ctx.CurrentSection`); the pre-2023 EXIT FUNCTION / EXIT METHOD formats and §8.4.3.8's RAISE SELF / INVOKE SELF|SUPER (`SourceElement`, reached through the `InMethod` alias); §14.9.33.3 SR1/SR2 (`InPerformWhen`, `Declarative`, `Declarative.Global`); §14.9.45.3 SR1 (`Declarative.ReportGroup`). §14.9.14.3 SR2/SR6 and §14.9.18.3 SR1/SR5 are the remaining members of the family and are each one question away.

**Rationale.** kb/Work PB403/PB404. Eight placement rules over four verbs ask parts of the same question, and before this decision each answered it alone: RESUME scanned `ctx.Table.Declaratives` in its own binder, the Format-3 PERFORM binder answered the EXIT-PERFORM-CYCLE half by WALKING ITS OWN PARSE SUBTREE after the fact, EXIT PROGRAM answered "is this a program procedure division?" with the single boolean `host.InMethod`, and EXIT PERFORM's placement half was not answered at all. The costs were measured, not theoretical. (a) SR8's first sentence unenforced: `EXIT PERFORM` written outside every PERFORM compiled clean and **hung** — the emitter's `F3Region.None` arm wrote a bare C# `break;` into the pc dispatcher's `switch (__pc)` without advancing `__pc`, so the paragraph re-executed forever, under a comment calling the arm a "defensive fallback (SR8: never reached for a valid bind)" for an invariant the binder never established. (b) SR7's scalar predicate: a FUNCTION definition ending in `EXIT PROGRAM` was accepted **and** given the program-activation machinery (`__asCalled`, `throw new ProgramReturn()`), so program-shaped emission leaked into a function. A scalar where the rule names a SET, and one rule written down in two places where one of the two stays empty — the same two shapes this compiler keeps finding.

**Two readings recorded, because the rule text alone does not settle them.** (1) §14.9.14.3 SR7 says "a program procedure division"; §14.2.2 SR10 lists program DEFINITION and program PROTOTYPE definition as distinct source elements. A program prototype's procedure division is admitted here — the noun in both SR10 items is *program*, and the alternative reading would reject source the standard nowhere forbids, which is the costlier error. ⚠ That arm is currently UNEXERCISED and is recorded so rather than claimed: the grammar's `programIdAttribute` admits only COMMON / INITIAL / RECURSIVE / GLOBAL, so §11.10.2's Format-2 `PROGRAM-ID. name IS PROTOTYPE.` does not parse (COBOLNET0901) and `BinderDriver.MakeUnit` reads `IsPrototype` off the FUNCTION-ID paragraph alone — a separate, filed defect, pinned by a drift fact that goes RED when the grammar arm lands. A FUNCTION prototype's procedure division, by contrast, IS parsed and bound (measured: one carrying `EXIT PROGRAM` reaches `BindExit`), so three of the five members are exercised today. (2) §14.9.14.3 SR10 ("EXIT … PARAGRAPH only in a paragraph") is implemented as NO CHECK, and the absence is the implementation: §14.4.3 defines a paragraph as a paragraph-name and its sentences "or, if the paragraph-name is omitted, one or more successive sentences following the procedure division header or a section header", so every sentence of a procedure division is in a paragraph and SR10 admits no violating program. A screen would be dead code that could only reject legal source.

**Rejected alternatives.** (1) One more `if` per rule at each verb — rejected: that is what produced the state above, and there are at least eight rules and four verbs, so the next one added silently would be the ninth. (2) A per-verb "am I in a PERFORM?" flag on `EcBindState` beside the deleted `InF3When` — rejected for the same reason `InF3When` was deleted: a bool owned by one subsystem answers for one asker, and save/restore discipline at each nesting site is a bug waiting for the next nested construct. (3) Keeping the CYCLE prohibition as the PERFORM binder's parse-subtree walk and adding only the placement half at `BindExit` — rejected: SR8 is ONE rule about ONE statement, and splitting its two sentences across two files is precisely how the first sentence ended up with no home. The drift net is `ExitPlacementContextDriftTests`, a MATRIX of every EXIT format against every enclosing context, plus RESUME's unchanged verdicts as the extraction's control.

### D15. SEARCH is lowered ONCE PER FORMAT, not once with a flag: `ControlFlowEmitter.EmitSerialScan` (Format 1, ISO §14.9.37.4 GR4 with GR3's index/VARYING augmentation) and `ControlFlowEmitter.EmitAllScan` (Format 2, GR9). Both are reached from the ONE frame `EmitSearchScan`, which computes the scan bound, carries the EC-raise plumbing, and emits the AT-END funnel; the WHEN arms are ONE emission (`EmitSearchWhens`). The split is exactly where the standard's texts DISAGREE and the sharing is exactly where they are ALL FORMATS rules. Format 1 advances and then judges the new value, so an unsuccessful serial search ends one past the table and carries the GR3 b)/c) varied item with it; Format 2 tests the bound BEFORE advancing, so its index never leaves [1, N] — and on an empty table it makes no probe at all. `BoundSearch.IsAll` (formerly `FromStart`) selects the lowering.

**Rationale.** kb/Work PB447. GR4 says the process repeats “unless the new value for the search index corresponds to a table element outside the permissible range of occurrence values” — the new value is FORMED and then judged, so the overshoot is the rule's own model. GR9 says of the Format-2 index “At no time is it set to a value that exceeds the value that corresponds to the last element of the table or is less than the value that corresponds to the first element of the table” — unconditional, and binding on the unsuccessful exit too, because GR1 b) 1. makes the AT END phrase part of the statement's own execution and a subscript written there reads THROUGH the search index. One advance-then-test loop serving both formats gave SEARCH ALL the overshoot only Format 1 is licensed for: a five-occurrence table left the index at 6 and `K(KX)` under AT END silently read outside the table. GR9's later “the final setting of the search index is undefined” forbids a PROGRAM to rely on WHICH in-range occurrence is left; it does not license leaving the table, so the tests assert the RANGE on the Format-2 arm and the exact value on the Format-1 arm (`SearchIndexRangeSpecTests`).

**Rejected alternatives.** (1) Bounding the advance in the shared loop for both formats — rejected: it breaks Format 1, whose GR4 requires the N+1 (and requires the VARYING item to take that last increment “at the same time”), and it leaves the format invisible at the one step where the rules differ, which is how the defect survived. (2) Restoring the bound only on the no-match EXIT of the shared loop — rejected: “At no time” is a rule about every setting, not only the last, and a repair at the exit would be undone by the next reader who moves the test. (3) Replacing the Format-2 probe order with a binary search — not required by any rule (GR9's technique is implementor-specified) and not a conformance question; it needs the OCCURS KEY list and each WHEN conjunct's direction on the bound node, which is a binder change, so it is a PERFORMANCE increment recorded separately, not part of this rule's repair.
### D16. SEARCH's `identifier-1` is a WRITTEN REFERENCE resolved by ISO §8.4.2.2, and the three ALL-FORMATS syntax rules (§14.9.37.3 SR1–SR3) are three predicates over it. `ReferenceResolver.ResolveTableOperand` is the ONE resolution of a reference written where a general format prints a **table** operand: it reads the whole written reference (`ReferenceResolver.ReadWritten` — the ONE decomposition of a `dataReference` into its qualifier chain, subscript group and reference-modification carriers), resolves the name through the SAME candidate-set matcher every other operand uses, reports **COBOLNET1639** for a name that identifies no item, and returns the item plus the two facts the rules are about — how many subscripts were written, and whether a reference modification was. It is deliberately NOT `Resolve`: SR2 says identifier-1 "shall not be subscripted at the level for which the SEARCH is applicable", so there is no subscript for the searched level and therefore no `Place` to build. The rules then live in the one check catalog, `StatementValidation.ResolveSearchTable`, under a single new code **COBOLNET2075** — one code for one operand position, the division D12 already established — and BOTH formats reach it through one `SearchBinder.Identifier1` helper. SR2's second sentence is the only written-form rule the subscript count can decide, and it is an UPPER bound: a table-item reference normally takes one subscript per OCCURS level (§8.4.2.3), SR2 removes the searched level's, so MORE subscripts than identifier-1 has superordinate tables means one sits at that level. ⛔ THERE IS NO LOWER BOUND, AND SR3 IS NOT ONE. "Identifier-1 may be contained within one or more other tables, for which the subscripting is still required" reads like a demand on identifier-1's written form; §14.9.37.4 GR1 says in so many words that it is not — "the subscript that is used to determine the occurrence of each superordinate table to search is specified by the user IN THE WHEN PHRASES" — so both the bare and the superordinate-subscripted spellings are legal and a lower bound enforced here REJECTS LEGAL SOURCE. The Format-2 WHEN's condition-name operand resolves through the ONE §8.4.2.2 Format-2 condition-name resolution, `ConditionBinder.ConditionOf`, which gained a one-report-per-reference guard so the screen and the bind do not both report an ambiguity.

**Rationale.** kb/Work PB443: both binder arms wrote `dref.cobolWord()` — the reference's BASE WORD — and then `candidates.FirstOrDefault(i => i.IsTable)`, so with a table `E` in each of two groups, `SEARCH E IN G2` searched whichever `E` was declared FIRST. Measured: swapping the two 01 entries and changing nothing else was the whole difference between the right answer and the wrong one, which isolates the cause to name-only resolution. That is a SILENT WRONG ANSWER on legal, unambiguous COBOL — §8.4.2.2 makes qualification the thing that establishes uniqueness and §14.9.37.4 GR1 has the statement vary "the first or only index associated with identifier-1", which for a qualified reference is the QUALIFIED table. The same line made SR1, SR2's second sentence and SR3 unaskable: nothing downstream ever saw a modifier or a subscript, so a reference-modified or subscripted identifier-1 produced output byte-identical to the legal form. The condition-name arm had the same shape and BOTH failure directions — with `ASCENDING KEY IS K1 K2` an SR11 violation went unreported, and with `K2 K1` the legal `WHEN CN OF K2 (IX)` was REJECTED for "referencing K1 and not K2" when it referenced neither. One written reference, one resolution.

**Rejected alternatives.** (1) Three screens bolted onto the existing base-word lookup — rejected: two of the three rules are about facts the lookup had already thrown away, so the screens would have had nothing to read; the resolution IS the fix, and the rules fall out of it in a line each. (2) A fourth and fifth diagnostic code, one per syntax rule — rejected under D12's own division and the COBOLNET1651/1757 precedent: the CODE is the identity of the operand position, the MESSAGE carries the rule (COBOLNET2076 and COBOLNET2077 were allocated and returned unused). (3) Keeping the `BoundUnsupported` for "identifier-1 is not a table" — rejected as D13 rejected its siblings: the rule was DECIDED at bind and shipped as a run-time "not implemented" abort, under a message that miscited itself as SR1 in both arms. The nested-dynamic-capacity guard KEEPS the carrier, because it is a genuine COBOL.NET increment rather than a rule the source violates. (4) A LOWER bound on identifier-1's subscript count, which the note proposed in those words ("exactly one per enclosing table is required") and the first implementation shipped — rejected because it is WRONG, and the way it was caught is the point: the standard's own CCVS suite writes the bare spelling throughout (NC233A line 516, `SEARCH ALL GRP2-ENTRY … WHEN SEC (IDX-1, IDX-2)` over `02 GRP-ENTRY OCCURS 10 … 03 GRP2-ENTRY OCCURS 10`, the outer occurrence coming from IDX-1 in the WHEN exactly as GR1 describes), and the bound failed SIX NIST programs outright — NC231A, NC232A, NC233A, NC234A, NC237A, NC238A. The 85 golden now runs the SAME nested table both ways and asserts the SAME answer, and the drift suite's legal half carries the bare form with that derivation beside it. (5) A drift test that only asserts the diagnostics — rejected: the wrong-answer half produced no diagnostic at any stage, so `SearchOperandResolutionDriftTests` RUNS each program and reads what it printed, every row paired with the declaration order swapped, plus a structural guard that `SearchBinder.cs` holds no base-word reduction in its code lines (4 hits before, 0 after).

### D17. The PERFORM statement's FORMAT rules are TWO screens in ONE place each, and the varying-phrase operand's COBOL KIND is decided exactly once. (a) **The inline head's cardinality.** §14.9.28.2 Format 2 prints ONE pair of square brackets over three STACKED alternatives (`PERFORM [ times-phrase | until-phrase | varying-phrase ] imperative-statement-1 END-PERFORM`) and Format 3 prints NO loop-control phrase at all (its head is `[ WITH LOCATION ]`) — both rendered from the printed page 682 / PDF 712, not read off the OCR. The grammar keeps `performInlineHead : performOptions performOptions* | performLocationPhrase` (a LIST) on the standing superset posture, and `ControlFlowBinder.CheckInlineHeadCardinality` is the ONE screen, called from `BindPerform` **above** the Format-2/Format-3 split: **COBOLNET2118** when an exception-checking PERFORM carries any loop-control phrase, **COBOLNET2117** when an ordinary inline PERFORM carries more than one. (b) **The varying-phrase operand.** Each FROM/BY slot is the brace group §14.9.28.2's varying-phrase prints — FROM `{identifier-3|index-name-2|literal-1}`, BY `{identifier-4|literal-2}` (printed page 683 / PDF 713) — parsed as the shared `valueOperand` superset and narrowed in `ControlFlowBinder.PerformVarying.cs`: **COBOLNET2119** for a shape outside the group, **COBOLNET2120** for the §14.9.28.3 SR4 a)/b)/c), SR5 a)/b)/c) and SR6 operand rules, and the existing COBOLNET0844 (threaded with SR3 as the position rule) for a non-numeric literal. The classification produces one `VaryingOperandKind` (Literal | IndexName | Identifier) carried on `VaryingLevel.FromKind`, and **that** is what the emitter's §14.9.28.4 GR3 EC-RANGE-PERFORM-VARYING guard asks. (c) **The evaluation window.** `IsPerEvaluationWindow(VaryingSlot)` is the one table: the FIRST level's FROM is statement-hoisted, every other slot (an AFTER level's FROM, any BY) rides `BoundUdfEvaluatedExpr`, whose activations the emitter emits as STATEMENTS at the setting/augmenting operation through `ControlFlowEmitter.RenderPerEvaluation`.

**Rationale.** kb/Work PB431, PB432, PB437, PB439 — four notes, one rule family, and each was the same mistake told a different way: the FORMAT was never asked. (a) `BindPerformControl` read `FirstOrDefault()` over the head, so `PERFORM 3 TIMES UNTIL X > 100` ran three times and `PERFORM UNTIL X > 4 3 TIMES` ran five — the FIRST phrase always won and the rest never reached the bound tree, so no later pass could complain either; the Format-3 binder never asked for a control phrase at all, so `PERFORM 3 TIMES … WHEN EC-ALL …` ran its body ONCE. The screen sits above the split because the two formats ask the same question with a different bound, and only one arm had ever been written — this project's most reproducible defect shape. (b) Both operand slots were typed `arithmeticExpression`, which was simultaneously WIDER than the printed group (`FROM A + B BY B * 2` compiled at every `--std`) and NARROWER: the figurative `FROM ZERO` — legal, because §14.9.28.3 SR3 restricts every literal here to numeric, which is exactly §8.3.3.6.3 SR1's precondition for permitting ZERO — was a hard parse error, since `primaryExpression`'s only figurative arm is the `ZERO_ARITH` token `ZeroTokenRewriter` mints by ADJACENCY to an operator. `valueOperand` fixes both halves with the rule the binder already reads. All seven lettered obligations of SR4/SR5/SR6 were absent from one function that applied no screen at all, and SR5's premise (an index-name in the FROM phrase) was never even computed; SR6 is the one whose absence changed a program's OUTCOME rather than its legality, because `BY 0` is a guaranteed non-terminating loop. (c) The BY and AFTER-FROM windows used to stage LOUD (COBOLNET1509) when the operand carried a function reference — a capability limit shipped as a rejection of conforming source, in a slot the compiler accepted one operand position over, pinned green by a negative corpus case and a unit theory.

**Rejected alternatives.** (1) Narrowing the FROM/BY slots in the grammar to the brace group literally — rejected: `FROM A + B` would become a bare `COBOL0001 no viable alternative`, which names no rule; the superset parse is what lets COBOLNET2119 quote the format. (2) A dedicated `performVaryingOperand` rule — rejected as a duplicate: it would be textually identical to `valueOperand`, and `OperandWalkCoverageTests` already guarantees every alternative of that rule reaches the binder's operand walk. (3) `lv.From is BoundNumRef || lv.From is BoundIntrinsicCall` for the GR3 guard — rejected under rule 5: it closes the function-identifier case and leaves the next identifier shape open, which is the failure already on display; the KIND is a property the binder knows and the emitter was re-deriving badly. (4) Rendering `BoundUdfEvaluatedExpr` as an immediately-invoked `Func<T>`, the way `BoundUdfEvaluated` renders a condition — rejected: the IIFE form exists because a condition sits in a C# loop HEADER where no statement can precede it per iteration; the set/augment windows ARE statement positions, so the activations are emitted directly, at no runtime cost. (Both forms render the activation through the ONE statement emitter, and in both a RESUME for a condition it propagates leaves through the PERFORM's `BoundActivationSite` — kb/Work PB892, `COBOLNET_CONDITIONS_EXCEPTIONS_DESIGN.md`.) (5) One diagnostic code per lettered syntax rule — rejected under D12's division: the CODE is the identity of the operand position, the MESSAGE carries the rule, and COBOLNET2120's message quotes the exact letter.

## C# mapping

Generated shape (one Dispatch per program unit; paragraphs flattened to a single source-order PC space, declaratives first):

```csharp
// COBOL:
//   PROCEDURE DIVISION.
//   MAIN-PARA.        DISPLAY "A". PERFORM SUB THRU SUB-END. DISPLAY "B". STOP RUN.
//   SUB.              DISPLAY "S1". IF X = 1 GO TO SKIP.
//   MID.              DISPLAY "MID".
//   SKIP.             CONTINUE.
//   SUB-END.          DISPLAY "SE".
private const int N = 5;               // paragraph count
private static int Dispatch(int startPc, int exitPc) {
  int pc = startPc;
  while ((uint)pc < (uint)N) {
    bool atExit = pc == exitPc;
    switch (pc) {
      case 0:   // MAIN-PARA
        System.Console.WriteLine("A");
        Dispatch(1, 4);                // PERFORM SUB THRU SUB-END  (idxSUB=1, idxSUB-END=4)
        System.Console.WriteLine("B");
        throw new StopRun();           // STOP RUN
      case 1:   // SUB
        System.Console.WriteLine("S1");
        if (X == 1L) { pc = 3; break; }            // IF X=1 GO TO SKIP (idxSKIP=3) — scale-aligned native long compare (COBOLNET_NUMERIC_DESIGN; never decimal)
        pc = 2; break;                 // fall-through to MID
      case 2:   // MID
        System.Console.WriteLine("MID");
        pc = 3; break;                 // fall-through
      case 3:   // SKIP
        /* CONTINUE = no-op */
        pc = 4; break;                 // fall-through
      case 4:   // SUB-END
        System.Console.WriteLine("SE");
        pc = 5; break;                 // fall-through (pc==N) -> loop exits
      default: pc = N; break;
    }
    if (atExit && pc == exitPc + 1) return pc;   // named THRU exit paragraph fell through
  }
  return pc;
}
internal static void Main() {
  try { Dispatch(/*EntryParagraphIndex*/0, -1); }   // exitPc=-1: run to STOP RUN / off-end
  catch (StopRun) { }
  // (run-unit cleanup: implicit CLOSE of open files, ISO 14.6.11)
}
```

Inline PERFORM (idiomatic loops, no Dispatch):
```csharp
// PERFORM VARYING I FROM 1 BY 1 UNTIL I > 3  ... END-PERFORM
for (long _v = 1; ; ) { I = CobolNum.Store(_v, 0, _P_I); if (I > 3L) break; /*body*/ _v += 1; }
// PERFORM UNTIL DONE  ... END-PERFORM (TEST BEFORE)     -> while (!(DONE)) { /*body; EXIT PERFORM=break*/ }
// PERFORM 3 TIMES ... END-PERFORM                       -> for (long _i=0;_i<3;_i++) { /*body*/ }
```

ALTER (C#-native mutable var):
```csharp
private static int _alter_GATE = 6;          // default GO TO target paragraph index
// paragraph GATE body:  case k: pc = _alter_GATE; break;   // alterable GO TO
// ALTER GATE TO PROCEED TO OTHER:            _alter_GATE = 9;   // emitted at the ALTER site
```

GO TO DEPENDING ON:
```csharp
// GO TO P1 P2 P3 DEPENDING ON SEL
switch ((int)SEL) { case 1: pc=idxP1; break; case 2: pc=idxP2; break; case 3: pc=idxP3; break;
  default: pc = thisIdx+1; break; }            // out of range -> normal fall-through
break;
```

GOBACK / EXIT PROGRAM in a CALLed program:
```csharp
internal static int Entry(ManagedPointer[] args) {
  try { Dispatch(EntryParagraphIndex, -1); }
  catch (ProgramReturn) { }                     // GOBACK/EXIT PROGRAM -> return to caller, run unit lives
  return 0;                                      // StopRun is NOT caught here -> propagates to run-unit Main
}
// GOBACK body:        throw new ProgramReturn();                        // in a main program: acts as STOP (§14.9.18.4 GR3) — the run-unit boundary ends the run unit
// EXIT PROGRAM body:   if (__asCalled) throw new ProgramReturn();         // §14.9.14.4 GR2: CONTINUE (plain fall-through) when NOT under a caller
```

EXIT PARAGRAPH / EXIT SECTION:
```csharp
// EXIT PARAGRAPH:   pc = thisIdx + 1; break;
// EXIT SECTION:     pc = sectionEndPc + 1; if (__exitPc == sectionEndPc) return pc; break;   // §14.9.14.4 GR7: fire the section's return mechanism (NOT a bare pc move — see D8)
```

New runtime signal (add alongside StopRun.cs):
```csharp
public sealed class ProgramReturn : Exception;   // GOBACK / EXIT PROGRAM (program boundary, not run unit)
```
Emitter implementation note: build a per-unit `ParagraphTable` (flattened source-order list of (cobolName, symbol, index, sectionName), declaratives first), a `nameToIndex`/`symbolToIndex` map (symbol-keyed first for duplicate names across sections — port of ParagraphSymbolIndices), `EntryParagraphIndex` (first non-declarative), and per-section `lastIndex`. A per-case `bool terminated` flag stops emitting after an unconditional transfer (GO TO/STOP/GOBACK/EXIT *) so no unreachable C# is generated (warnings-as-errors safe).

## Hard problems

### STOP RUN / GOBACK unwinding nested PERFORMs and CALL frames in a RECURSIVE Dispatch (the central correctness fork; the legacy is internally inconsistent here — returns -1 yet has a stop-run catch comment).

Two distinct exceptions, never integer-pc: STOP RUN throws StopRun (caught only at run-unit Main → unwinds ALL Dispatch frames in ALL programs); GOBACK/EXIT PROGRAM throws ProgramReturn (caught at the current program's Entry → unwinds this program's frames, returns to the CALL site, run unit survives). Integer pc is used ONLY for in-program transfers. ISO §14.9.42 (STOP terminates run unit), §14.9.18 (GOBACK = logical end of THIS program/method).

### GO TO that leaves a PERFORM range and later re-enters it; inverted THRU range (proc-2 physically precedes proc-1).

Inherited FREE from the exit-bounded full-switch model: Dispatch(idxP, idxQ) follows pc anywhere in [0,N), and returns ONLY when paragraph idxQ falls through (atExit && pc==idxQ+1). It is a RETURN-ADDRESS model, not a physical-range model — never iterate a contiguous [min,max] block. Verbatim port of the legacy invariant (CilEmitter.EmitDispatchHelper doc; NC102A). The atExit flag is captured BEFORE the case body runs because the body overwrites pc.

### Overlapping / recursive PERFORM ranges (range B PERFORMed from inside range A, or a paragraph PERFORMing a range that contains itself).

Each PERFORM is a recursive C# `Dispatch(...)` call; the C# call stack IS the return-address stack, so arbitrary nesting and re-entrancy compose automatically with correct restore of the enclosing range's exit bound. No hand-managed stack.

### Dead/unreachable C# after an unconditional COBOL transfer (GO TO / STOP RUN / EXIT mid-sentence). Generated C# compiled warnings-as-errors would FAIL on unreachable code.

Track a `terminated` flag while emitting a case's statements; once an unconditional transfer (GO TO, STOP RUN, GOBACK, EXIT PROGRAM/PARAGRAPH/SECTION, bare GO TO) is emitted, stop emitting the rest of that sentence/case (it is unreachable-but-legal COBOL). Conditional transfers (IF c GO TO P) do not set terminated. Legacy modeled this with dead IrBasicBlocks.

### EXIT PERFORM CYCLE must reach the loop INCREMENT (not just re-test) for PERFORM VARYING / UNTIL-with-step.

Inline VARYING is emitted so the increment is reachable by `continue` (loop tail) OR, where C# `continue` would skip a needed increment block (TEST BEFORE with a separate increment region), emit a labeled structure / do-while-with-increment-at-bottom so EXIT PERFORM CYCLE = jump-to-increment. Port of legacy PerformContinueStack (increment block target) vs PerformExitStack (break target).

### PERFORM VARYING ... AFTER reinitialization of inner indices on every outer increment.

Nested C# loops where the OUTER loop body re-MOVEs each AFTER index to its FROM value before re-testing the outer UNTIL; all indices set to FROM before the first test. ISO §14.9.28 + Annex D.26 figs D.11-D.14; port of EmitAfterReinitialization. A plain nested-for is WRONG when FROM is a data item or any AFTER level exists.

### Duplicate paragraph names in different sections (legal COBOL); a GO TO / PERFORM must resolve to the correct one.

Index the PC table by paragraph SYMBOL (bound identity), not by name; keep a name→index fallback (first-definition-wins) only for unqualified references the binder couldn't disambiguate. Port of ParagraphSymbolIndices/ParagraphSymbolMethods. The switch is symbol-ordered so symbol-based resolution and the case order agree.

### NEXT SENTENCE semantics (archaic, commonly misunderstood) — goes to the next separator period, NOT past a scope delimiter.

Forward C# goto to a `__sent_<n>` label emitted at each sentence boundary within the case; last sentence's NEXT SENTENCE = paragraph fall-through. ISO IF GR (lines 27834/27838) + Annex F.1; the goto stays inside one switch case (legal C#). Explicitly NOT modeled as END-scope exit (ISO line 50371 warns of that misconception).

### DECLARATIVES must never execute by fall-through; only via the runtime error/I-O event path.

Lay declarative paragraphs at the LOW PC indices [0..declEnd); set EntryParagraphIndex = first non-declarative paragraph so the main Dispatch(start=EntryParagraphIndex) skips them. A USE procedure runs as its own Dispatch(useStart,useEnd) invoked from the runtime I/O path, returning to the statement after the triggering operation. The PC layout (declaratives first) is designed in from the start; the USE wiring is realized by the emitted `__RunUse`/`__IoCheck` machinery.

## Edge cases

- PERFORM proc THRU proc where the two endpoints are the same paragraph: idxQ==idxP, Dispatch(i,i) returns when paragraph i falls through (pc==i+1).
- Inverted THRU: PERFORM proc-2 THRU proc-1 where proc-1 physically follows proc-2 in source but is the named exit — handled by following pc, exit on the NAMED exit paragraph's fall-through, never by [min,max] block iteration (NIST NC102A).
- A paragraph that is BOTH fallen into sequentially AND a PERFORM target: identical case body serves both; PERFORM uses Dispatch(idx,idx) (exit-bounded return), fall-through uses pc=idx (continues).
- Last paragraph falls off the end: pc becomes N, (uint)pc<(uint)N is false, loop exits = normal run-unit termination (no STOP RUN needed; ISO §14.6.11 then does implicit CLOSE of open files).
- Empty paragraph (no statements): case body is just `pc = i+1; break;` (pure fall-through). It has its OWN pc,
  so `PERFORM` of it is an ordinary one-pc range — never the empty-SECTION case below.
- **Section with ZERO paragraphs** (ISO §14.4.2): the resolved range is `PcRange.IsEmpty`. `PERFORM` of it emits the
  control-phrase loop around an EMPTY body (D5a) — GR13 a) still initializes, GR9 still takes the count, GR10 still
  tests — and never calls `Dispatch`. `GO TO` it transfers to `Start`, which is the pc at which execution continues
  past it (the following section's first paragraph, or the end of the pc space when it is last) — a plain transfer,
  correct for free. A SORT/MERGE `INPUT`/`OUTPUT PROCEDURE` naming one releases/returns nothing. `ALTER` excludes it
  (`IsParagraph` is false). A DECLARATIVE section is never empty: `DeclCollectSection` materializes a zero-sentence
  paragraph so the USE range always has a pc.
- Empty/absent PROCEDURE DIVISION or zero paragraphs: emit a Main with an empty try (N==0 → no Dispatch).
- GO TO DEPENDING ON selector value 0 or > number of targets: no transfer, control passes to the next statement (fall-through) — ISO §14.9.17.
- Bare GO TO (alterable) never targeted by an ALTER and never given a written target: default _alter_X = -1 → on dispatch pc=-1 → treated as run-unit end (undefined behavior; ALTER is archaic Annex F.1).
- ALTER targeting a GO TO that was written with an explicit target: the explicit target is the _alter_X initial value, ALTER overwrites it at runtime.
- EXIT PROGRAM executed in the MAIN program (not a CALLed subprogram): treated as CONTINUE — control passes to the next statement (§14.9.14.4 GR2; §14.6.3); emitted as `if (__asCalled) throw ProgramReturn`, so in a main program it is a plain fall-through. GOBACK executed in the MAIN program: acts as a STOP statement, ending the run unit (§14.9.18.4 GR3) — its `ProgramReturn` reaches the run-unit boundary, which terminates the run unit.
- STOP RUN inside a USE declarative or inside the deepest of several nested PERFORMs: StopRun exception unwinds every frame up to the run-unit Main regardless of depth.
- Sentence with multiple statements where an early one is IF...GO TO and the rest are unconditional: only the GO TO branch is conditionally terminating; emit the remaining statements (reachable when the IF is false).
- PERFORM of a SECTION name (not a paragraph): Dispatch(firstParaInSection, lastParaInSection) — sections are contiguous PC ranges in flattened order.
- EXIT SECTION in the last section of the program: `sectionEndPc+1` equals N and the top-level dispatch's `__exitPc` is the section end, so the explicit `if (__exitPc == sectionEndPc) return pc;` returns and the run unit ends cleanly.
- Recursive PERFORM (a range that re-PERFORMs a range containing itself): each Dispatch is a fresh C# frame with its own exitPc; correct because the call stack restores the enclosing exit bound — but unbounded COBOL recursion → C# stack overflow (acceptable, mirrors real COBOL stack exhaustion).
- CONTINUE as the sole statement of a paragraph (common with SKIP-style targets): no-op body, pure fall-through.
- NEXT SENTENCE inside an inline PERFORM or EVALUATE WHEN: still targets the enclosing sentence period (the inline PERFORM/EVALUATE is within one sentence) — the goto-to-sentence-end label is correct.

## Per-edition gating & diagnostics (G1 — four compilers in one executable)

Control flow is edition-varying. Every edition-varying construct carries TWO co-equal obligations: (1) the complete
per-edition ISO-spec behavior in every edition that HAS it; (2) the correct DIAGNOSTIC in every edition that LACKS it
(not-yet-introduced or removed). Tests (NIST etc.) only VERIFY; they never SCOPE. Gating sources:
`docs/VERSION_CHANGE_REFERENCE.md` (the 130-row edition-change checklist; 2002→2023 deltas ONLY — it has NO 85→2002
rows; derive 85↔2002 gating from the 2002 standard / the ISO2023_CONFORMANCE_PLAN M2 catalog) and
`docs/VERSION_TEST_MATRIX_DESIGN.md` (the (construct × edition) matrix; Phase 0 done).

| Construct | 85 | 2002 | 2014 | 2023 | Gating source |
|---|---|---|---|---|---|
| GO TO (simple/DEPENDING), out-of-line + inline PERFORM (TIMES/UNTIL/VARYING/AFTER, TEST BEFORE/AFTER), bare EXIT, CONTINUE, NEXT SENTENCE, STOP RUN | ✔ | ✔ | ✔ | ✔ (NEXT SENTENCE archaic-flagged) | invariant; NEXT SENTENCE archaic = row 127 (Annex F.1 #2) |
| GOBACK | ✘ diagnose not-in-edition | ✔ | ✔ | ✔ | 2002 introduction (derive from the 2002 standard) |
| GOBACK with STOP-style status phrase (main program only) | ✘ | ✘ | ✘ | ✔ | row 75 (E.3.3 item 32) |
| EXIT PERFORM [CYCLE], EXIT PARAGRAPH, EXIT SECTION | ✘ diagnose not-in-edition | ✔ | ✔ | ✔ | 2002 introduction (derive from the 2002 standard) |
| PERFORM … UNTIL EXIT | ✘ | ✘ | ✘ | ✔ | row 80 (E.3.3 item 37) |
| Exception-checking PERFORM variant | ✘ | ✘ | ✘ | ✔ (owned by the EC deep-dive) | row 79 (E.3.3 item 36) |
| ALTER + alterable GO TO | ✔ (obsolete element; warning channel pending) | ✘ REJECTED as deleted (COBOLNET0810/0811 — ISO/IEC 1989:2002 deleted ALTER and the target-less GO TO) | same as 2002 | same as 2002 (the 2023 standard has no ALTER; §14.9.17 F1–F2 only) | ISO history (2002 deletion); ANSI X3.23-1985; §18 #10 (reconciled) |
| EXIT PROGRAM | ✔ | ✔ | ✔ | ✔ + archaic flag | rows 89/126 (Annex F.1 #1) |

Diagnostics: use below the introduction edition ⇒ reject with the standard not-in-this-edition diagnostic (the
negative half of the version test matrix); archaic/obsolete elements flag under the flagging options. Every row
above requires its (construct × edition) matrix case — tests verify, they never scope.

## ISO citations

- ISO/IEC 1989:2023 §14.9.28 — PERFORM statement (control transfer to a procedure and implicit return on completion; VARYING/TIMES/UNTIL/TEST BEFORE-AFTER; the range-completion = exit-paragraph-falls-through rule)
- ISO/IEC 1989:2023 §14.9.17 — GO TO statement (simple transfer = set PC; GO TO DEPENDING ON selector, out-of-range = no transfer; bare/alterable GO TO)
- ISO/IEC 1989:2023 §14.9.18 — GOBACK statement (logical end of a function/method/program; returns to caller, does not terminate the run unit unless main)
- ISO/IEC 1989:2023 §14.9.14 — EXIT statement (bare EXIT = common end point/no-op; EXIT PERFORM [CYCLE]; EXIT PARAGRAPH; EXIT SECTION; GR2 EXIT PROGRAM-as-CONTINUE when not under a caller)
- ISO/IEC 1989:2023 §14.9.14.3 SR7–SR10 — the EXIT statement's PLACEMENT rules, all asked of the D14 probe: SR7 EXIT-PROGRAM-only-in-a-program-procedure-division (with §14.2.2 SR10's enumeration of the five source elements that may carry one), SR8 EXIT-PERFORM-only-in-an-inline-or-exception-checking-PERFORM and its CYCLE prohibition, SR9 SECTION-phrase-only-in-a-section, SR10 PARAGRAPH-phrase-only-in-a-paragraph (no violating program exists — §14.4.3 makes a paragraph-name-omitted run of sentences a paragraph)
- ISO/IEC 1989:2023 §14.9.14.4 GR4 / GR5 a) / GR5 b) — what EXIT PERFORM MEANS, and therefore why SR8 is a rejection and not a no-op: control passes to an implicit CONTINUE positioned relative to "the END-PERFORM phrase that matches the most closely preceding, and as yet unterminated, inline PERFORM statement"
- ISO/IEC 1989:2023 §14.9.42 — STOP statement (STOP RUN terminates execution of the run unit)
- ISO/IEC 1989:2023 §14.9.9 — CONTINUE statement (no operation; control passes to the next executable statement)
- ISO/IEC 1989:2023 §14.9.13 — EVALUATE statement (multi-subject WHEN matching → C# switch/if-cascade in case contents)
- ISO/IEC 1989:2023 §14.3 — Declaratives (USE procedures executed only on the associated condition, never by fall-through) and §14.6.13 (declarative procedure execution/return)
- ISO/IEC 1989:2023 §14.4 / §14.2 — Procedure division structure and sequential execution of statements/sentences/paragraphs (fall-through semantics)
- ISO/IEC 1989:2023 §14.9.37 — SEARCH / SEARCH ALL (AT END, WHEN, index varying; control transfer including NEXT SENTENCE form)
- ISO/IEC 1989:2023 §14.9.37.3 SR7–SR13 — the Format-2 (SEARCH ALL) operand rules: the KEY phrase requirement, key membership and the first-index subscript with its no-'+'/'–' clause, the single-valued condition-name, the sending operand's exclusion from the key phrase and from the search index, the preceding-keys requirement, the variable-length-group ban and the zero-length-literal ban (D12)
- ISO/IEC 1989:2023 §14.9.37.4 GR4 vs GR9 — the two formats' scans, and the RANGE each leaves its search index in: GR4 forms the new value before judging it (so an unsuccessful serial search ends one past the table), GR9 bounds the Format-2 index at all times to the first and last elements (D15)
- ISO/IEC 1989:2023 §14.9.37.4 GR9 / GR5 a) / GR6 a) 2. — the search TECHNIQUE is implementor-specified ("A non serial type of search operation may take place"); the key-sequencing precondition is a condition on the PROGRAM, and GR6 defines the behaviour when it is false (D12)
- ISO/IEC 1989:2023 §14.9.37.3 SR1–SR3 — SEARCH's identifier-1, ALL FORMATS: not reference-modified; a data description with an OCCURS clause and an INDEXED phrase, and no subscript at the searched level; SR3 PERMITS a nested identifier-1 and its "subscripting is still required" is satisfied in the WHEN phrases per §14.9.37.4 GR1, so it is no screen on the operand (D14)
- ISO/IEC 1989:2023 §8.4.2.2 / §8.4.2.2.1 — qualification establishes uniqueness, and is required when spellings collide; §14.9.37.4 GR1 fixes identifier-1 as the QUALIFIED table whose first index the statement varies, the superordinate occurrence coming from the WHEN phrases (D14)
- ISO/IEC 1989:2023 §14.9.28.2 — the PERFORM general formats, rendered from the printed pages 682-683 (PDF 712-713): Format 2's ONE bracket over three stacked loop-control alternatives, Format 3's head of `[ WITH LOCATION ]` alone, and the varying-phrase's FROM/BY BRACE GROUPS (D17)
- ISO/IEC 1989:2023 §14.9.28.3 SR3–SR6 — the varying-phrase operand rules: every literal numeric; with an index-name in the VARYING/AFTER phrase the FROM/BY identifiers integer data items, the FROM literal a positive integer and the BY literal a nonzero integer; with an index-name in the FROM phrase the varied and BY identifiers integer data items and the BY literal an integer; and, unconditionally, a BY literal that is not zero (D17)
- ISO/IEC 1989:2023 §14.9.28.4 GR3 — an index-name varied from an IDENTIFIER whose value is not positive sets the FATAL EC-RANGE-PERFORM-VARYING; §8.4.3.1.2 prints three identifier formats and §8.4.3.2.4 GR1 makes a function-identifier one of them (D17)
- ISO/IEC 1989:2023 §14.9.28.4 GR12 — the varying-phrase operands' EVALUATION WINDOW: item identification for identifier-3/-4/-6/-7 and index-name-2/-4 "is done each time … used in a setting or augmenting operation", so only the first level's FROM is a once-per-statement window (D17)
- ISO/IEC 1989:2023 §8.3.3.6.3 SR1 — a figurative constant may be used wherever 'literal' appears, and where the literal is restricted to a numeric literal the one permitted figurative is ZERO (D17)
- ISO/IEC 1989:2023 Annex D.26, Figures D.11–D.14 — canonical PERFORM VARYING (TEST BEFORE/AFTER, one/two conditions) flow diagrams establishing the AFTER-index reinitialization-on-outer-increment rule
- ISO/IEC 1989:2023 Annex F.1 — Archaic language elements (NEXT SENTENCE; ALTER), and the IF-statement general rules (NEXT SENTENCE transfers to the implicit CONTINUE before the next separator period, NOT past a scope delimiter)
- ISO/IEC 1989:2023 §14.6.11 — implicit CLOSE of files still open at normal run-unit termination

## Resolved questions (settled in `COBOLNET_DESIGN.md` §18; edition gating in the per-edition section above)

- Should well-behaved paragraphs (no GO TO out, not a THRU exit target, only fallen into / simple-PERFORMed) be emitted as REAL standalone C# methods for readability, with only the irregular subset using the PC switch? Owner LOCKED not-separate-methods for v1; this is a deferred post-conformance prettiness optimization (a hybrid emitter that proves a paragraph is structured and lifts it to a method). SETTLED (§18 #9): v1 always emits the PC dispatcher; the structured lift is a deferred post-conformance pretty pass — no owner decision currently pending.
- GOBACK status phrase: ISO 2023 (change item 32, line 50308) lets GOBACK carry the same status phrase as STOP, but only in a main program. SETTLED (§18 #10): a main-program GOBACK-with-status throws `ProgramReturn` carrying the status, surfaced as the process exit code via RETURN-CODE (§18 #20). The status phrase is 2023-only (VERSION_CHANGE_REFERENCE row 75, E.3.3 item 32) — diagnosed as not-in-edition at `--std` 85/2002/2014.
- ALTER is an obsolete element of COBOL-85 (ANSI X3.23-1985), DELETED by ISO/IEC 1989:2002 — the 2023 standard has no ALTER statement (§14.9.17 GO TO has only Formats 1 and 2). SETTLED (§18 #10): ALTER is supported only at `--std 85` (where the NIST corpus exercises it) and REJECTED as not-in-edition at 2002/2014/2023 (COBOLNET0810/0811, `Constructs.AlterRemoved2002`). The alterable target-less GO TO shares the same edition scope.
- UNTIL EXIT phrase (ISO 2023 change item 37, line 50318: PERFORM UNTIL EXIT = infinite loop until EXIT PERFORM) — SETTLED (§18 #10): in scope — `while(true){...}` with EXIT PERFORM=`break`. 2023-only (VERSION_CHANGE_REFERENCE row 80, E.3.3 item 37): diagnosed as not-in-edition at `--std` 85/2002/2014.
- Exact CALL boundary realization (how a C# CALL site catches ProgramReturn vs lets StopRun propagate, and how RETURNING / BY REFERENCE args cross) is owned by the CALL/inter-program subsystem; this design only fixes that GOBACK=ProgramReturn-at-Entry and STOP RUN=StopRun-at-Main. The seam is owned by `docs/COBOLNET_INTERPROGRAM_DESIGN.md` (the CALL/interprogram deep-dive); this design fixes only GOBACK=ProgramReturn-at-Entry and STOP RUN=StopRun-at-Main.
