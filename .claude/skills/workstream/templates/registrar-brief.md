You are the REGISTRAR for adjudication batch {BATCH} of the COBOL.NET conformance burn-down (lane 3). You are in a fresh worktree cut from current `main`. Read CLAUDE.md first (rules 1, 6, 7, 8 — ⛔ there is EXACTLY ONE work register, `kb/Work/`, one note per item), `.claude/skills/kb-sync/SKILL.md`, and the generic landing procedure `{SCRATCH}\cluster-lander-prompt.md` (gate in your worktree; land via `git push origin HEAD:main`; checkpoint-commit per step + STATUS.md).

INPUTS under `{OUT_DIR}`: `out-<slug>.json` (adjudicator results: records + findings), `refute-<slug>.jsonl` (refuter verdicts, one per line), and the merge output `batch-{BATCH}.json` (record_verdicts records — CONFORMS rows only where a refuter checkpoint exists; refuted rows already carry the corrected verdict and `|| REFUTED:` reason; defective rows carry a `@@NOTE@@` marker in `notes` awaiting the register-note id) and `findings-{BATCH}.json` (one finding per (slug, mechanism)). Regenerate the merge yourself first (`python {MERGE_SCRIPT} {OUT_DIR} {BATCH}`) so it reflects every refuter checkpoint on disk.

THE WORK:
1. CLUSTER the findings BY MECHANISM ACROSS FILES — one statement's parts often report the same root cause (e.g. OPEN's sharing/retry phrases hoisted to statement scope appear in p1, p2 and p3): one `kb/Work/` note per mechanism, never per row, never per file. Batch 8's precedent: 343 findings → 31 notes. Before writing a note, grep `kb/Work/` for an OPEN/HALF note that already owns the mechanism (`spec_refs`, `inventory_rows`, title keywords) — extend THAT note's `inventory_rows` and body instead of creating a duplicate; the dossier's `register` section in `{IN_DIR}/in-<slug>.json` lists the candidates.
2. Write each new note (`kind: defect` for a behavioural defect, `kind: analysis` when the finding is a measurement gap; `status: open`; `severity` from harm: a wrong answer or crash on legal source is MAJOR, an under-reject MINOR unless it hides a wrong answer; the harm flags `wrong_answer`/`crashes`/`rejects_legal_source`/`under_rejects`/`silent` from the finding; `area` = the code_site's subsystem; `spec_refs`; `inventory_rows` = EVERY rule-id the mechanism owns across all files; `blocked`/`blocked_by` only when another note must land first). Allocate ids sequentially from the first free id above the highest `kb/Work/PB*.md` on the MERGED tree (check `ls kb/Work | sort -V | tail -3` — other landers allocate concurrently; if a collision appears at push time, renumber yours). Body: the repro program, the rule text QUOTED with its `cite.py --check` line (re-run every citation yourself — an adjudicator's inherited clause number is the failure mode), expected-from-spec vs observed, the code site, the sibling sweep the fixer must do. Every citation validated: `python scripts/spec/cite.py --check <clause> "<text>"`.
3. Fill the `@@NOTE@@` markers: each defective record's `notes` gets its owning note id (record_verdicts requires it; the DefectiveRowCoverage drift gate requires that every PARTIAL/NOT-IMPLEMENTED/DIVERGES row be claimed by a non-terminal note's `inventory_rows` — that gate turning red is exactly the event this step prevents). Rows verdicted NEEDS-OWNER-DECISION are excluded from the batch: list them with the question in a single `kind: decision`, `status: owner` note (or append to `kb/Work/PB280.md` if the question is one of its three).
4. `python scripts/spec/record_verdicts.py --dry-run batch-{BATCH}.json` → fix every shape violation at its source (never by weakening a record) → apply. Read the GAP delta and the verdict histogram from its output. `python scripts/spec/work.py check` → all well-formed.
5. GATE in your worktree: `dotnet build CobolSharp.sln -c Debug`; `dotnet test tests/Cobol.Net.Tests.Unit --no-build --filter "FullyQualifiedName~SpecTraceabilityInventory|FullyQualifiedName~DefectiveRowCoverage|FullyQualifiedName~DerivedVerdict"`; output to a file; read the verdict line (the two ExternalCorpusPopulation reds are the known worktree shape).
6. DEVLOG (TOP; number = top entry + 1; CRLF-safe): the batch's shape — rules adjudicated, verdict histogram, CONFORMS closed vs test-needed (the test-needed rows go to the golden lane's next input set), the refuter overturn count and direction, the mechanisms clustered (id + one line each), the owner questions, the dossier gaps the adjudicators reported (`dossier_gaps` — feed them to the generator as a paragraph in `kb/Work/PB286.md`'s successor or the tooling note that owns the dossier). Plan §0: GAP and adjudicated counts. `python scripts/spec/gen_conformance_notes.py` to refresh the burn-down view.
7. Commit (attribution lines), then land with **`bash scripts/push-main.sh`** (⛔ never a bare `git push origin HEAD:main` — `main` carries a REQUIRED `ci-gate` check with `enforce_admins: true`, so the server refuses an unverified commit; the script pushes HEAD to `ci/<short-sha>`, waits for the run on that sha, and only on green fast-forwards main. It is idempotent — re-run it if your command times out — and a red is a BLOCKING finding, reported first and attributed by job / step / failing test). REPORT: sha; ⛔ the CI run id and its conclusion for the head you pushed; `git show --stat`; the record_verdicts output verbatim; notes created / extended (ids + mechanisms); the CONFORMS-test-needed rule-ids (for the golden lane); the owner questions; gate verdict lines verbatim; anything in the adjudicator/refuter outputs you found false.
