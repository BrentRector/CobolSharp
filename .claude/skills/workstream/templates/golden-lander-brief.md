You are the LANDER for the golden lane (lane 1) of the COBOL.NET conformance burn-down: 157 inventory rows already verdicted CONFORMS (empty `test-ref`) or DOCUMENTED-NON-SUPPORT (owing a witness) get their spec-derived goldens. You are in a fresh worktree cut from current `main`. Read CLAUDE.md first (rules 1, 6, 7, 8), then `.claude/skills/land-a-fix/SKILL.md` §5–§7, then the generic landing procedure `{SCRATCH}\cluster-lander-prompt.md` (gate in YOUR worktree; land only via `git push origin HEAD:main` fast-forward; checkpoint-commit per step + STATUS.md; semgrep baseline rule; the ExternalCorpusPopulationDriftTests reds are the known worktree shape).

INPUTS (all under `{SCRATCH}\lane1\`):
- `reports/<slug>.json` — 16 writer reports (rows with disposition existing-golden / new-golden / defect-suspected / not-closable; `manifest_entries` per edition dir; `negative_manifest_entries`; `records` for record_verdicts; `defects`). `reports/<slug>.refute.json` — the refuter verdicts; `reports/<slug>.fix.json` — what the fixer applied/refused. Where a fixer wrote `out/<slug>/REPORT.json`, THAT supersedes `reports/<slug>.json` for that slug.
- `out/<slug>/tests/conformance/...` — the draft `.cob`/`.out`/`.err` files at their repo-relative destination paths (142 `.cob` drafts).
- `validate/<slug>.jsonl` — the compile-validation results, one line per draft: status pass / output-mismatch / compile-error / runtime-error / negative-not-rejected / negative-wrong-diagnostic / not-run, with a classification (pass / suspected-compiler-defect / draft-error / environment) and a repro_note. THESE DECIDE WHAT LANDS.

THE RULES OF THIS LANDING:
1. A draft lands ONLY if its validation status is `pass`. A `draft-error` is fixed here only when the validator's note shows exactly what the rule requires AND you re-derive it yourself from specs/ISO_COBOL.md (cite.py --check); otherwise that row stays CONFORMS-but-untested and is reported. A `suspected-compiler-defect` NEVER becomes a golden edit: the draft is kept OUT of the tree (leave it in the scratchpad), the row stays open, and the defect becomes a `kb/Work/` note — `kind: defect`, harm flags from the repro (wrong_answer / rejects_legal_source / under_rejects / crashes), `inventory_rows` = the rows whose goldens it blocks, body = the repro program, the rule text quoted, the cite.py line, expected vs observed. Allocate ids sequentially starting at the FIRST FREE id above the highest existing `kb/Work/PB*.md` on the merged tree (other landers may have taken PB288–PB294 — check `ls kb/Work | sort -V | tail -3` and grep the ids you plan to use); cluster the writers' `defects` lists by MECHANISM first (several rows often share one cause) — one note per mechanism, never per row. `environment` results: re-run that draft yourself with the worktree's built `cobol.exe` before deciding.
2. Copy each landing draft to its destination path; register every positive in its edition dir's `manifest.json` `enabled` and every negative in `tests/conformance/negative/manifest.json`; a negative's FIRST line must be `*> reject-at: <years>`; unique PROGRAM-IDs (the `L1` prefix convention) — verify no collision with existing programs (`grep -rn "PROGRAM-ID. <id>" tests/conformance`).
3. Build the record_verdicts batch from the writers' `records` FILTERED to rows whose covering drafts all passed (a record naming a golden that did not land is dropped and reported); `python scripts/spec/record_verdicts.py --dry-run` then apply; read the GAP delta from its output. Rows the refuter overturned carry the fixer's corrected expectations — confirm the `.out` on disk matches the fix report before landing.
4. GATE in your worktree: `dotnet build CobolSharp.sln -c Debug`; `dotnet test tests/Cobol.Net.Tests.Conformance --no-build --filter "FullyQualifiedName~Corpus|FullyQualifiedName~Negative|FullyQualifiedName~Intrinsic|FullyQualifiedName~VersionMatrix"`; `dotnet test tests/Cobol.Net.Tests.Unit --no-build --filter "FullyQualifiedName~SpecTraceabilityInventory|FullyQualifiedName~DefectiveRowCoverage|FullyQualifiedName~Manifest|FullyQualifiedName~AnnexA1Register"`; full Characterization; redirect each to a file and read the verdict lines; confirm by name in the log that the new goldens RAN (CorpusRunnerTests lists them) — a golden that is registered but not discovered is a red by absence. Every new negative must have failed for the RIGHT diagnostic (the runner asserts the `.err` substring).
5. Docs in the same change set: `docs/CONFORMANCE.md` only where a landed golden pins a §3/§7 determination (add its name to the row's `Pinned by` cell); `kb/Work/PB260.md` / `PB261.md` if any DNS witness landed (their rows re-verdicted with the witness test-ref — they stay open until ALL their rows are witnessed; say how many remain). No new list anywhere.
6. DEVLOG (TOP; number = top entry + 1; CRLF-safe): the lane's shape (157 rows → dispositions), how many landed, the GAP before → after, the refuter overturn count (67) and what kinds, every suspected compiler defect (id, one line each), the not-closable rows and why, the drafts held back and why. Plan §0: GAP + the CONFORMS-test-needed count.
7. Commit (attribution lines), `git push origin HEAD:main`. REPORT: sha; `git show --stat`; goldens landed / held back per slug; GAP before → after; new notes; gate verdict lines verbatim; anything in the writer/refuter/validator reports you found false.
